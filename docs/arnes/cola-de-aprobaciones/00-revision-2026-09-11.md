# La cola de aprobaciones, revisada contra el estado actual — 2026-09-11

**Versión base:** rama `feat/1.34-cierre-alcance` @ `863aa9d`. **Autorizada por el propietario**
(2026-09-11) como revisión **de lectura y diagnóstico**: no acepta riesgos, no reclasifica hallazgos y
**no vacía la cola**. Las entradas resueltas se **señalan con su evidencia** para reconciliarlas por el
procedimiento vigente; **ninguna se borra**.

## Método, para re-derivarlo sin preguntar

```sh
# el recuento que usa la máquina, con su propio lector
bash -c '. hooks/lib.sh; arnes_cola_pendientes PENDING_APPROVAL.md; echo $ARNES_COLA'
bash tools/arnes-lectura.sh | grep -i cola

# los bloqueantes que D2 cuenta, con el método que D2 declara
for f in requirements/REQ-0*.md; do
  sed -n 's/^Hallazgos abiertos: *//p' "$f" | tr ',' '\n' \
    | grep -cE '\((usuario/dinero|contrato)\)'
done | paste -sd+ | bc
```

## 1. La cifra: **16, confirmada por el mecanismo**

`arnes_cola_pendientes` devuelve **16** con `rc 0` —es decir, **medible**, no estimada— y
`tools/arnes-lectura.sh` publica la misma cifra. **No se tomó «16» por vigente: se midió.**

## 2. ¿Bloquea REALMENTE todos los cierres? — **Sí, y sólo los cierres**

La afirmación **hay que acotarla**, y ésta es la acotación exacta:

- El gate **A2** de `hooks/guard-completado.sh` deniega **marcar un REQ como `completado`** mientras
  `ARNES_COLA > 0`. El propio `tools/arnes-lectura.sh` lo dice: *«16 pendiente(s) — ningún REQ puede
  cerrarse»*.
- **No bloquea** commitear, empujar, abrir o fusionar un PR, ni ninguna otra transición de estado
  (`en-progreso`, `en-revisión`, `bloqueado`). **Todo el trabajo de 1.34.0 puede avanzar**; lo único
  imposible hoy es **cerrar** un REQ.
- **Y es fail-closed por diseño**: si el archivo tuviera un comentario sin cerrar, el hook **también**
  deniega, porque entonces no puede medir cuántas hay (`SEC-051`).

## 3. La tabla

| id | Decisión solicitada | Evidencia / sede | Estado hoy | Qué bloquea realmente | Recomendación y consecuencia |
|---|---|---|---|---|---|
| **`D2`** | Los hallazgos bloqueantes: resolver, declarar residual o aceptar | La propia entrada, re-derivada el 2026-09-10 sobre `a139155` | **VIGENTE con cifra DESFASADA.** Dice **23 en 8 REQ**; re-derivado hoy **con su método**: **16 en 5** (`REQ-020` 8 · `REQ-024` 3 · `REQ-026` 2 · `REQ-019` 2 · `REQ-013` 1) | El cierre de esos 5 REQ | **Actualizar la cifra antes de decidir.** 8 de los 16 son de `REQ-020`, cuya ventana es `D2` mismo en `docs/propuesta-cierre-1.34.0.md`; moverlo deja **8** |
| **`D3`** | `SEC-072` y `SEC-073` de `REQ-026`: qué son y qué se hace | `registro-seguridad.md:6432`, `:6458` | **VIGENTE.** Los dos siguen `contrato` · `abierto` | `REQ-026` (trabajo 2 del alcance) | Decisión tuya. El propietario ya prohibió (2026-09-09) cerrarlos por redacción o aceptación implícita |
| **`D4`** | Ventana para la superlinealidad heredada | `docs/qa/1.34.0-req023-vuelta3-metodo.md` §11.6 | **VIGENTE, y es la más barata.** El analista **propone** 1.35.0 y no la fija | Nada de 1.34.0 — es alcance **fuera** | **Fijar 1.35.0.** Consecuencia: cero trabajo en esta ventana; el arreglo queda descrito y no aplicado |
| **`D6`** | El fail-before de `CA-03` | La entrada misma | **RESUELTA — «decidida bajo delegación»**, y con su premisa **corregida dentro** (el diagnóstico de `k=1` era falso) | Nada | **Reconciliar**: dejó de ser decisión pendiente. Su corrección se conserva |
| **`D7`** | `REQ-024 CA-04` — gate humano **previo** | `requirements/REQ-024.md:191-195` | **VIGENTE.** El criterio **contrata su propio gate**, y una delegación general no lo revoca | `CA-04` de `REQ-024` | Decisión tuya. No es reclasificable: si una delegación lo cubriera, el gate no sería gate |
| **`D8`** | El manifiesto cambiado **antes** de su gate | `ADR-009` línea 3; `.arnes/config.json:37` | **VIGENTE como ratificación**, con **riesgo vivo cero** medido: la llave nace `false` en las dos sedes | Nada en la práctica | **Ratificar a posteriori.** Es un fallo de procedimiento de la coordinadora, ya declarado |
| **`D9`** | Dos preguntas al analista sobre la evidencia de `D6` | `docs/arnes/req-017-ca-03-modo-de-medicion/01-evidencia.md:72-74` | **VIGENTE**, y es la corrección de una decisión propia: la tabla **cambiaba dos variables a la vez** | El método de medición de `CA-03` | Decisión tuya, **acoplada a `D6`**: conviene resolverlas juntas |
| **`D10`** | `SEC-083` — informativa | `registro-seguridad.md` § `R-027` §3 | **RESUELTA.** `SEC-083` **cerrado** por el auditor en `R-027`; la entrada ya decía «no te pido permiso» | Nada | **Reconciliar** |
| **`D11`** | `ADR-011` — el gate del propio ADR | La entrada, con **corrección urgente del 2026-09-10** | **VIGENTE, con aviso ⚠️**: *no firmar la parte (b) como está* — la fila que autorizaba es **hoy falsa** | Superficie heredada | **No firmar (b) hoy.** Firmarla metería una promesa **al revés** en `AGENTS.md` y la plantilla |
| **`D12`** | El precio de la salida (b) de `CA-12` | `requirements/REQ-024.md`, `CA-12` | **VIGENTE, y es la llave de `REQ-024`** | `CA-12 (ii)`, sin el cual `REQ-024` no cierra | Decisión tuya. Cambia la conducta **de un proyecto sin migrar**, con la llave apagada |
| **`D13`** | Otro bloqueo en `REQ-023` (`CA-11` sin acto) | La entrada | **VIGENTE** | `REQ-023`, `bloqueado` | Decisión tuya. Misma familia que `CA-13`: un criterio enunciado **sin acto** |
| **`D14`** | `REQ-024`: `bloqueado` o cierre con residual | La entrada | **VIGENTE.** El `bloqueado` ya está aplicado; lo que falta es **la alternativa**, que es de aceptación | `REQ-024` | Decisión tuya. Y su premisa la **refuerza** el trabajo de hoy: lo que queda no es residual |
| **`D15`** | `veredictos.caducan_con_codigo` | La entrada, con su premisa **corregida antes de firmarse** | **VIGENTE pero VACIADA de urgencia**: el motivo que la justificaba resultó falso al medirlo | Nada | **Decidir sin prisa**, o moverla a backlog |
| **`D16`** | `QA-016-04` — un valor que cae abierto en lo publicado | La entrada, con **anotación del 2026-09-10** | **PARCIALMENTE SUSTITUIDA**: su tabla describe **1.33.0**; `v1.33.1` corrigió esa mitad | Lo que quede sin corregir | **Re-medir sobre `v1.33.2`** antes de decidir — es el mismo error que el propietario ya corrigió con `SEC-084` |
| **`D17`** | `SEC-084` en lo publicado | La entrada, con **anotación del 2026-09-10** | **PARCIALMENTE SUSTITUIDA y AMPLIADA.** El vector que describe **ya no se reproduce**; pero hoy se midió que **la clase sobrevivía en el disparador del aviso**, vivo en `v1.33.2` | La mitad de **texto** de `SEC-084` | **Reescribirla con lo medido hoy.** Su descripción original ya no es la buena |
| **`D18`** | `REQ-017` `bloqueado` + la prueba que oscila | La entrada | **VIGENTE**, y su segunda mitad **ya tiene respuesta**: la oscilación se caracterizó y el propietario decidió el techo el 2026-09-11 | `REQ-017` | **Acotarla a la primera mitad.** La segunda quedó resuelta por decisión posterior |

## 4. Recuento

| | |
|---|---|
| Entradas medidas | **16** |
| **Resueltas con evidencia** (reconciliables) | **2** — `D6`, `D10` |
| **Parcialmente sustituidas** (re-medir antes de decidir) | **3** — `D16`, `D17`, `D18` |
| **Vigentes, decisión tuya** | **11** |
| De ellas, **sin coste si se deciden** | `D4` (fijar ventana), `D8` (ratificar), `D15` (sin urgencia) |

## 5. Lo que NO hace esta revisión

No vacía la cola, no retira entradas, no cierra ni reclasifica hallazgos, y **no decide ninguna**.
Señalar `D6` y `D10` como resueltas **no las borra**: la retirada de una entrada es acto del
propietario, según el mecanismo de gate de `AGENTS.md` §6.
