# Método y evidencia — revisión ACOTADA de la mitad de texto de `SEC-079`

**Versión base: `1154417`** («SEC-079: la promesa absoluta se BORRA, no se anota»), padre
`89440e9`. **Validado sobre el árbol COMITEADO**, en un `git worktree --detach`, **no** sobre el
árbol de trabajo: el árbol de trabajo tenía WIP vivo de otro agente (`REQ-024`) en `hooks/lib.sh`,
`tools/arnes-lectura.sh`, `hooks/guard-completado.sh`, `hooks/estado-derivado.sh`,
`.arnes/config.json` y `templates/arnes-config.json.tpl`; medir ahí habría medido su WIP.

```
git worktree add --detach <dir>/wt-1154417 1154417
git worktree add --detach <dir>/wt-89440e9 89440e9     # línea base A/B
```

Alcance: (A) la cláusula nueva de `CA-10` y su control negativo · (B) las dos filas · (C) la
asimetría fila/`CA-01` · (D) el veredicto `QA:` · (E) el conteo de `CA-12 (iii)` · (F) el CI.
**No** se revalidó `REQ-023` entero; **no** se re-midió `CA-09 (iii)`, que sigue **sin acreditar**.

---

## 1. Las dos filas (punto B) — reproducido

| Medición | Comando | Resultado |
|---|---|---|
| Sede 1 | `grep -n "Una línea de la cabecera que la máquina" AGENTS.md` | línea **349** |
| Sede 2 | `… templates/AGENTS.md.tpl` | línea **314** |
| Una sola fila por archivo | `grep -c` | **1** y **1** (la anterior **sustituida**) |
| Identidad byte a byte | `md5sum` de `sed -n '349p'` / `sed -n '314p'` | **`1f4384edfeede06e61aa5e0e0a372e0e`** en las dos |
| Tamaño | `wc -c` | **2287 B** en las dos |
| `cmp` | `cmp fila-agents.txt fila-tpl.txt` | **sin diferencias** |
| Delta por sede | `git show --numstat 1154417` | **1/1** en `AGENTS.md` y **1/1** en `templates/AGENTS.md.tpl` |
| Promesa absoluta vieja | `grep -c 'y \*\*nunca\*\* se permite por \*\*ausencia\*\*'` | **0** y **0** (BORRADA) |
| Fila anterior | `git show 1154417^:AGENTS.md` | 1274 B, `md5 721abd51…` |

**La `Е` del ejemplo es cirílica**, verificado en bytes y en las **dos** apariciones del ejemplo
(`` `Еstado` `` y `` `Е` ``):

```
hexdump: 68 6f 6d c3 b3 67 6c 69 66 6f 2a 2a 20 28 60 d0 95 73 74 61 64 6f ...
                                                        ^^^^^ d0 95 = U+0415 CYRILLIC Е
grep -c 'Estado`, con la' fila-agents.txt  ->  0     (no hay `E` latina en el ejemplo)
```

**Cita por ID y no por número de línea** — verificado: la fila contiene `R-024`, `SEC-078`,
`SEC-079` y `registro-seguridad.md` (una vez cada uno) y **cero** referencias del tipo `:69xx` /
`:70xx`. **Decisión correcta**, y no es preferencia: en el proyecto que nace de
`templates/AGENTS.md.tpl` el registro `docs/seguridad/registro-seguridad.md` **no existe con esas
líneas** —ni con ninguna—, así que una cita por línea nace rota en cada `arnes-init`. El ID
sobrevive a la rotación del registro (`AGENTS.md` §13, rotación: mueve secciones a `-archivo.md`);
el número de línea no. Es además lo que `CA-10 (ii)` pide comprobar: «la cita al sitio único con los
**dos números de hallazgo**» — no con líneas.

## 2. El control negativo (punto A) — **DISCRIMINA**, medido con tres candidatas

Se construyeron y se barrieron **oración a oración**, que es el método que `CA-10 (iii)` prescribe:

| Candidata | Qué es | Oración con promesa **sin condición** | Veredicto (iii) |
|---|---|---|---|
| **R0** | la fila **anterior** (`1154417^`, 1274 B) | or. 2: «Se **deniega** […] y **nunca** se permite por **ausencia** del campo que ese carácter borró» | **INCUMPLE** |
| **R1** | R0 + **sólo** «Tres fronteras, lista **no exhaustiva**:» | la **misma** or. 2, intacta | **INCUMPLE** |
| **R1b** | R1 + el puntero a `R-024`/`SEC-078`/`SEC-079` | la **misma** or. 2, intacta | **INCUMPLE** |
| **R2** | la fila **nueva** (`1154417`, 2287 B) | or. 2 termina en «aunque los veredictos estén en verde»; la promesa pasa a or. 3 **con su condición** y con su rama `else` | **CUMPLE en or. 2–3** |

Comando: `tr '.' '\n' < fila-Rn.txt | grep -n 'nunca\*\* se permite\|no\*\* se permite\|deniega\|no deja cerrar\|no puede medir'`.

**Conclusión: el control negativo es falsable y separa exactamente lo que el propietario quería
separar.** Añadir «no exhaustiva» —y también añadir el puntero— **no toca** la oración que
contiene la promesa absoluta, así que (iii) sigue rojo; borrarla y condicionarla lo pone verde. No
es una frase que suene bien: es una comprobación que **distingue tres textos distintos**.

**Y con el mismo barrido aparece lo que (iii) también caza en R2: la oración 1.** Ver
`QA-023-18`.

## 3. Verdad de la fila contra el código (punto B) — sonda ejecutada

Dos sondas, las dos **de sólo lectura** sobre `wt-1154417/hooks/`:

- `probe-clave-oculta.sh` — llama a `_arnes_clave_oculta` (`hooks/lib.sh:1836-1854`) directamente.
- `probe-e2e.sh` — arma el JSON real de `PreToolUse`/`Write` y ejecuta
  `hooks/guard-completado.sh` contra un proyecto efímero con el manifiesto base del banco.

| Entrada en la clave | `_arnes_clave_oculta` | Puerta (e2e) | Lo que dice la **or. 3** de la fila |
|---|---|---|---|
| `Estado` (limpia) | no dispara | deny por `QA: pendiente` (control) | igual |
| `Sensible a  seguridad` (blanco duplicado) | **dispara** → `Sensible a seguridad` | — | igual |
| `Sensibleaseguridad` (blanco **borrado**) | **dispara** → `Sensible a seguridad` | **DENY** | **calla y permite por ausencia** ✗ |
| `Est\xc2\xa0ado` (NBSP **dentro** de la palabra) | **dispara** → `Estado` | — | **calla y permite** ✗ |
| `\xef\xbb\xbfEstado` (BOM delante) | **dispara** → `Estado` | — | **calla y permite** ✗ |
| `Sensible a\xc2\xa0seguridad` | **dispara** | — | igual |
| `Hallazgos\tabiertos` | **dispara** | — | igual |
| `\xd0\x95stado` (**homóglifo** Е) | **no dispara** | **ALLOW** (salida vacía) | igual |
| `Sensible \xd0\xb0 seguridad` (**homóglifo** а) | **no dispara** | **ALLOW** con `Rigor: ligero` y los dos veredictos `pendiente` | igual |
| `Módulo`, `Archivos` (ajenas) | no dispara | — | igual |

Las dos últimas filas del homóglifo **confirman la afirmación de la fila**: permite, y permite
**por ausencia** del campo que el homóglifo borró (`SEC-047` fila 1 reproducida, y con `Еstado` la
puerta no ve ninguna transición y no emite nada). Las tres marcadas ✗ son `QA-023-19`.

Lo que hace el código, literal (`hooks/lib.sh:1841-1845`):

```bash
ajeno="${1//[!$ARNES_CLAVES_ALFA]/}"      # (1) retirado lo ajeno al alfabeto...
limpio="${ajeno//[[:blank:]]/}"           # (2) ...y retirados TODOS los blancos
case "$ARNES_CLAVES_MAPA" in *"|$limpio|"*) … ;; *) return 0 ;; esac
```

y su propio comentario (`:1826-1828`) lo dice igual: «(1) se retira lo ajeno al alfabeto, (2) se
retiran **TODOS los blancos**, y se pregunta si lo que queda es una clave **SIN SUS BLANCOS**». No
existe en `hooks/` ni en `tools/` ninguna reposición de blanco ni colapso de repetidos
(`grep -rn "colaps\|repues\|repone"` → sólo `rotar-artefactos.sh` y `reponer_f` de `set -f`, ajenos).

## 4. `CA-12 (iii)` — contadas, **tres** (punto E)

`awk` sobre § «Fuera de alcance» de `requirements/REQ-023.md`:

| | `1154417^` | `1154417` |
|---|---|---|
| Ítems (`- **`) | **9** | **9** |
| `HOMÓGLIFO` | 1 | 1 |
| `byte NUL y el archivo en UTF-16` | 1 | 1 |
| `` invisible delante del `## ` `` | 1 | 1 |

Y los criterios que no se debían tocar, **byte a byte idénticos** entre padre y commit:
`CA-01` (`md5 4d0d1495…`), `CA-03` (`995631d1…`), `CA-11` (`3d526776…`), `CA-12` (`fe573e29…`,
`diff` literal vacío). **12 criterios** antes y después. El apartado ganó la anotación del forzador
disparado y **ningún ítem**.

## 5. El banco y el CI (punto F)

**Quality gates de `AGENTS.md` §7 sobre `1154417`: las tres en verde.**
`bash -n` de `hooks/*.sh` y `tools/*.sh` → rc 0 · `jq -e .` de `hooks/hooks.json`,
`.claude-plugin/plugin.json` y `.claude-plugin/marketplace.json` → válidos (versión declarada
`1.33.0`).

**Banco completo, A/B en la misma máquina y en la misma sesión:**

| Árbol | Resultado |
|---|---|
| `89440e9` (padre) | **959 PASS · 0 FAIL · 7 SKIP** = 966 |
| `1154417` | **957 PASS · 1 FAIL · 8 SKIP** = 966 |
| CI `hooks-en-linux` sobre `1154417` (run `34431267106`) | **954 PASS · 1 FAIL · 11 SKIP** = 966 |

**Cuadra en 966 en las tres**: ningún caso se perdió ni se añadió.

**Diferencia A/B caso por caso** (`diff` de los 966 nombres con su veredicto). Ningún veredicto se
mueve **por el cambio**; lo que se mueve es:

1. **Reloj/ruido:** `DEV v3: heredoc CITADO ~300 KB` PASS→**FAIL** (4450 ms sobre techo holgado
   4000 ms, veredicto `allow` **correcto**); `REQ-017 CA-08 (ii)` cambia **cuál** de sus dos formas
   abstiene; `REQ-017 CA-09` PASS→SKIP por solapamiento de rangos; y milisegundos sueltos.
   Reproducido aislado 5/5 **PASS** a 3224–3970 ms, con el peor caso **30 ms** por debajo del techo:
   el margen del caso es menor que el ruido de la máquina. **Fenómeno ya registrado** en este mismo
   documento (corridas B y C: 4466 ms y 4357 ms), misma clase que `SEC-080`. **No lo abro como
   hallazgo nuevo.**
2. **Sorteo por diseño:** `REQ-023 CA-03` cambia de semilla en cada corrida (917960210 → 779805993).
3. **El censo del corpus de `CA-04`**, y esto **sí** es efecto del cambio: 5336→**5356** líneas con
   campo, 1931→1937 clave no ASCII, 2257→2273 valor no ASCII, 3315→3334 clave decorada y
   33→**34** donde la guarda **dispara**. Causa medida, no supuesta: el corpus es
   `cat secciones/*.sh` **+ `cat requirements/*.md` enteros**
   (`39-caracter-invisible-5-el-corpus.sh:175-179`), y `REQ-023.md` creció ~137 líneas. Pasando por
   el **mismo evaluador del banco** sólo las 136 líneas añadidas: `RESUMEN 42 21 29 41 1` — **una**
   dispara, y es una línea del **cuerpo** del Historial que contiene `` `QA-: pendiente` `` (guion
   ASCII, reconstruye la clave `QA`). El cuerpo **no** se restringe (`CA-01`, tercera frontera), y el
   caso siguiente sigue midiendo **0 divergencias** y **la guarda sin disparar en ninguno de los 27
   REQ del árbol**. El censo se mueve **hacia arriba**: el suelo de anti-vacuidad de `CA-04` queda
   **más** satisfecho, no menos.

**Y el cambio no puede mover un veredicto por la vía de las dos filas, verificado:** ninguna
sección del banco lee la fila de `§13`. Las únicas que abren esos dos archivos son
`36-cabecera-comentario-html-4-el-informe-y-los-textos.sh:183-187` (mira un texto de `REQ-016`) y
`34-arnes-paralelo.sh:25,79` (usa la ruta como cadena). Ver `QA-023-20`.

**El FAIL del CI (`REQ-017 CA-03 fail-before`, 2.329× bajo el techo 2,600×) NO es de este cambio, y
lo confirmo por mi vía:** el caso materializa el tag **`v1.32.1`** (`mat37 v1.32.1`,
`37-coste-del-escaner-2-las-razones.sh:142`), un árbol **inmutable**; y el **mismo caso** en mi
máquina dio **4.667×** sobre `89440e9` y **3.562×** sobre `1154417` —el mismo par de sujetos, dos
corridas, 1,1× de deriva— o sea exactamente el ruido que hace que el veredicto lo decida la máquina.
Escalado por la coordinadora; fuera de mi alcance y **no** lo cuento como hallazgo mío.

## 6. Lo que NO acredito, dicho expresamente

- **`CA-09 (iii)`: sin acreditar.** No lo medí y la autorización del propietario no lo acredita. En
  el CI de `1154417` abstiene en los dos sujetos (medianas 1.023× y 1.011×, dispersión ≥ margen).
- **La mitad de `CA-10` que vive en `skills/arnes-upgrade/SKILL.md`**: `1154417` no la toca (el
  commit son 5 archivos, ninguno en `skills/`), así que reutilizo la evidencia de la revalidación
  sobre `808f9ca` y **no** la re-mido.
- **`REQ-024`**: no lo leí ni lo juzgué, por instrucción. Consecuencia declarada: la fila afirma
  «**ninguna** versión lo deniega, **tampoco 1.34.0**», y eso lo acredito **sobre `1154417`**, que es
  lo que de 1.34.0 existe hoy. Si algo que entre después en la ventana 1.34.0 llegara a denegar el
  homóglifo, la fila pasaría a ser falsa por el otro lado y hay que releerla antes de publicar.
- **La prueba de carga / umbral de rendimiento**: `AGENTS.md` no define usuarios concurrentes ni
  latencia objetivo para este proyecto; los techos que hay son los de coste de los hooks y los mide
  el banco. Nada que añadir aquí.
- **Limitación material de la medición:** las corridas locales se hicieron con **trabajo de otro
  agente vivo en la misma máquina** (12 CPU, load ~2). Por eso el A/B se hizo en la **misma sesión**
  y el caso de reloj se repitió aislado; y por eso el único FAIL local se declara **flaky conocido**
  y no regresión.

## 7. Artefactos de esta revisión

Sondas y salidas (fuera del repositorio, en el scratchpad de la sesión; los comandos están arriba,
enteros, para re-derivarlas):
`probe-clave-oculta.sh`, `probe-e2e.sh`, `eva93.sh` (extraído tal cual del banco),
`fila-agents.txt`, `fila-tpl.txt`, `fila-R0.txt`, `fila-R1.txt`, `fila-R1b.txt`, `fila-R2.txt`,
`banco-1154417.txt`, `banco-89440e9.txt`, `veredictos-*.txt`, `lineas-anadidas-req023.txt`,
`ci-34431267106.txt`.

## 8. Apéndice — las dos sondas, enteras, para que esto se re-derive sin preguntar

Las dos se ejecutan con el árbol comiteado como argumento (`bash <sonda> <dir>/wt-1154417`) y
**no modifican nada**: cargan `hooks/lib.sh` por `source` o ejecutan `hooks/guard-completado.sh`
contra un proyecto efímero en `mktemp -d`.

### `probe-clave-oculta.sh` — verdad de terreno de `_arnes_clave_oculta`

```bash
#!/usr/bin/env bash
# Sonda QA (solo lectura sobre hooks/lib.sh @1154417): verdad de terreno de _arnes_clave_oculta
. "$1/hooks/lib.sh" 2>/dev/null || { echo "no se pudo cargar lib.sh"; exit 2; }
probe() {
  ARNES_CLAVE_OCULTA=0; ARNES_CLAVE_OCULTA_CLAVE=''; ARNES_CLAVE_OCULTA_REPR=''
  _arnes_clave_oculta "$2"
  printf '%-34s | entrada=%-28q | oculta=%s clave=%-22s repr=%s\n' "$1" "$2" "${ARNES_CLAVE_OCULTA:-0}" "${ARNES_CLAVE_OCULTA_CLAVE:-()}" "${ARNES_CLAVE_OCULTA_REPR:-()}"
}
echo "ALFABETO=[$ARNES_CLAVES_ALFA]"
echo "MAPA=$ARNES_CLAVES_MAPA"
echo "---"
probe "clave limpia"                 'Estado'
probe "blanco DUPLICADO"             'Sensible a  seguridad'
probe "blanco BORRADO (sin blancos)" 'Sensibleaseguridad'
probe "NBSP DENTRO de la palabra"    "$(printf 'Est\xc2\xa0ado')"
probe "NBSP en sitio del blanco"     "$(printf 'Sensible a\xc2\xa0seguridad')"
probe "TAB en sitio del blanco"      "$(printf 'Hallazgos\tabiertos')"
probe "ZWSP delante"                 "$(printf '\xe2\x80\x8bEstado')"
probe "BOM delante"                  "$(printf '\xef\xbb\xbfEstado')"
probe "HOMOGLIFO E cirilica"         "$(printf '\xd0\x95stado')"
probe "HOMOGLIFO a cirilica"         "$(printf 'Sensible \xd0\xb0 seguridad')"
probe "clave ajena (Modulo)"         'Módulo'
probe "clave ajena (Archivos)"       'Archivos'
```

### `probe-e2e.sh` — la puerta real con su JSON de `PreToolUse`

```bash
#!/usr/bin/env bash
# Sonda QA end-to-end sobre guard-completado.sh @1154417 (solo lectura del arbol).
WT="$1"
RAIZ="$(mktemp -d)"; PROJ="$RAIZ/proj"
mkdir -p "$PROJ/.arnes" "$PROJ/requirements" "$PROJ/src"
printf '%s\n' '{"agentes":{"agente_codigo":"desarrollador","conocidos":["analista-requerimientos","desarrollador","qa-tester","auditor-seguridad"]},"codigo_app":{"globs":["src/*"]},"quality_gates":["true"],"estados":{"completado":"completado"},"requirements_dir":"requirements","pending_approval":"PENDING_APPROVAL.md"}' > "$PROJ/.arnes/config.json"
printf '## Pendientes\n\n## Resueltas\n' > "$PROJ/PENDING_APPROVAL.md"
export CLAUDE_PROJECT_DIR="$PROJ"
printf '# REQ-980\nEstado: en-revisión\n' > "$PROJ/requirements/REQ-980.md"

caso() {  # <nombre> <contenido>
  local json out dec mot
  json="$(jq -n --arg fp "$PROJ/requirements/REQ-980.md" --arg c "$2" \
    '{hook_event_name:"PreToolUse",tool_name:"Write",cwd:env.CLAUDE_PROJECT_DIR,tool_input:{file_path:$fp,content:$c}}')"
  out="$(printf '%s' "$json" | "$WT/hooks/guard-completado.sh" 2>/dev/null)"
  dec="$(printf '%s' "$out" | jq -r '.hookSpecificOutput.permissionDecision // "allow(sin salida)"')"
  mot="$(printf '%s' "$out" | jq -r '.hookSpecificOutput.permissionDecisionReason // empty')"
  printf '%-46s -> %-6s %s\n' "$1" "$dec" "${mot:0:150}"
}

SENS_OK='Sensible a seguridad: sí'
SENS_HOM="$(printf 'Sensible \xd0\xb0 seguridad: sí')"   # a cirilica U+0430
EST_OK='Estado: completado'
EST_HOM="$(printf '\xd0\x95stado: completado')"          # E cirilica U+0415
BOM="$(printf '\xef\xbb\xbf')"

caso "1 control: cabecera limpia, QA pendiente" "# REQ-980
$EST_OK
$SENS_OK
QA: pendiente
Seguridad: pendiente
Rigor: ligero
"
caso "2 control: BOM en Sensible (guarda viva)" "# REQ-980
$EST_OK
${BOM}$SENS_OK
QA: pendiente
Seguridad: pendiente
Rigor: ligero
"
caso "3 blanco BORRADO en Sensible" "# REQ-980
$EST_OK
Sensibleaseguridad: sí
QA: pendiente
Seguridad: pendiente
Rigor: ligero
"
caso "4 HOMOGLIFO en Sensible (a cirilica)" "# REQ-980
$EST_OK
$SENS_HOM
QA: pendiente
Seguridad: pendiente
Rigor: ligero
"
caso "5 HOMOGLIFO en Estado (E cirilica)" "# REQ-980
$EST_HOM
$SENS_OK
QA: pendiente
Seguridad: pendiente
Rigor: ligero
"
rm -rf "$RAIZ"
```

### `probe-cabecera.sh` — control de que la cabecera que YO escribí sigue siendo medible

```bash
#!/usr/bin/env bash
. "$1/hooks/lib.sh" >/dev/null 2>&1 || { echo "no carga"; exit 2; }
ARNES_TEXTO="$(cat "$2")"
ARNES_OCULTA=0; ARNES_OCULTA_CLAVE=''; ARNES_OCULTA_REPR=''
arnes_campos_req "$ARNES_TEXTO" 2>/dev/null
echo "QA=<${ARNES_QA:-}>"
echo "SEG=<${ARNES_SEG:-}>"
echo "SENS=<${ARNES_SENS:-}>"
echo "RIGOR=<${ARNES_RIGOR:-}>"
echo "HALL=<${ARNES_HALL:0:120}>"
echo "GUARDA ARNES_OCULTA=${ARNES_OCULTA:-0} clave=${ARNES_OCULTA_CLAVE:-()} repr=${ARNES_OCULTA_REPR:-()}"
```

Salida sobre `requirements/REQ-023.md` después de escribir mi veredicto:
`QA=<con-hallazgos>`, `SEG=<con-hallazgos>`, `SENS=<si>`, `RIGOR=<critico>`, 13 hallazgos
todos con clase, y **`ARNES_OCULTA=0`**: mi propia edición no deja la cabecera sin medir.
El evaluador del corpus se extrae del banco sin tocarlo con:
```bash
awk '/^cat > "\$EVA93" <<.EVA./{f=1;next} f&&/^EVA$/{exit} f' \
  tests/escenarios/hooks/secciones/39-caracter-invisible-5-el-corpus.sh > eva93.sh
```

## 9. Nota de vigencia — la punta se movió mientras medía, y no invalida nada

Al terminar, la rama había avanzado a **`a49fe99`** («D6 completada…»). Comprobado, no supuesto:
ese commit toca **sólo** `CHANGELOG.md` y `PENDING_APPROVAL.md` (+55 y +82), y
`git diff 1154417 a49fe99 -- hooks/lib.sh` sale **vacío**. Las dos filas del árbol de trabajo son
**idénticas** a las de `1154417` en las dos sedes. Por tanto todo lo medido sobre `1154417` —la
guarda, la puerta, las filas— sigue describiendo la punta. Lo que **no** cubre mi veredicto es el
WIP no comiteado de `REQ-024` (`hooks/lib.sh`, `tools/arnes-lectura.sh`,
`hooks/guard-completado.sh`, `hooks/estado-derivado.sh`, `.arnes/config.json`,
`templates/arnes-config.json.tpl`), que no leí ni juzgué por instrucción.
