# QA acotada — reparación del disparador del AVISO (`SEC-084` · `QA-024-19`)

**Veredicto: `con-hallazgos` · VUELTA 5 dev↔QA de `REQ-024`.** La reparación del **código** es
correcta y la acredito entera: **ninguna decisión se movió**, el lector **no** se ensanchó, la
propiedad se cumple por sus dos bordes y la frontera declarada sigue declarada y abierta. Lo que
impide cerrar `QA-024-19` **no es un defecto de este entregable**: es que su **write-back** no
existe (§7), y un hallazgo cuyo comportamiento aceptado el REQ no describe es deriva
(`AGENTS.md` §9).

> **La vuelta va numerada y no renombrada.** Es un despacho nuevo de QA sobre `REQ-024` tras una
> entrega nueva del `desarrollador` (`a05994f`), así que **gasta vuelta** aunque el encargo se
> titule «acotada». La autorización expresa del propietario del 2026-09-11 cubre el **alcance**
> (1.34.0 vigente, autoalojamiento aligerado); **no** saca esta vuelta del contador, y no consta
> que lo haga. Vuelta 4 = `f612b84..d82c127`; ésta es la **5**.

---

## 0. Versión base y método — cómo re-derivar cada cifra sin preguntar

| | |
|---|---|
| **Worktree** | `/home/juan/dev/ArnesJuan-1.34-reparaciones` |
| **Rama / candidato** | `feat/1.34-cierre-alcance` @ **`a05994f`**, árbol limpio |
| **Base (árbol previo a la reparación)** | **`6327b8c`** |
| **Publicado comparado** | **`v1.33.2`** |
| **Fecha / plataforma** | 2026-09-11 · Linux WSL2 · bash 5.3 · `load` entre 0,66 y 3,85 |

Los tres árboles de `hooks/` se materializan **desde el repositorio**, no se copian:

```bash
S=<scratchpad>/qa
for ref in v1.33.2 6327b8c a05994f; do
  mkdir -p "$S/t-$ref"; git archive "$ref" hooks | tar -x -C "$S/t-$ref"
done
```

**El arnés de prueba es MÍO, reescrito desde el contrato del hook** (`PreToolUse`, JSON por
`stdin`, JSON por `stdout`) y no copiado del desarrollador:
`<scratchpad>/qa/harness.sh`, `c1b.sh`, `c2-cierre.sh`, `c3-bordes.sh`. Proyecto de prueba en
`<scratchpad>/qa/proj` con `.arnes/config.json` mínimo (`quality_gates: ["true"]`,
`codigo_app.globs: ["src/*"]`, `estados.completado: "completado"`).

**Dos modos de edición, y el segundo es el que vale.** `old_string` que **no** casa deja
`reconstruido=0` y lee fragmentos; `old_string` que **sí** casa reconstruye el documento
resultante, que es lo que hace la herramienta real. **Todas las matrices de abajo están en modo
reconstruido** (`old_string = 'Seguridad: pendiente'` o `'Estado: en-revisión'`).

> **Un error propio, escrito porque cambia cómo se leen los primeros números que medí.** Mi
> primera corrida dio `ALLOW` en **todas** las celdas, incluida la del control. No era un
> hallazgo: el arnés resolvía su propia ruta desde `$0` y al hacer `source` apuntaba a `/bin`,
> así que el hook nunca se invocó. **Sin el control positivo en la misma corrida lo habría
> reportado como «no se reproduce».** La segunda causa fue mía también: un cuerpo de fixture que
> contenía la letra `x` hacía casar el `old_string:"x"` y la reconstrucción sustituía dentro del
> cuerpo. Las dos corridas están descartadas; las que se publican llevan **el control del auditor
> dentro** (`Seguridad: aprobado` sobre `QA: pendiente` → **DENY**).

---

## 1. Quality gates de `AGENTS.md` §7 — las tres en verde

| gate | resultado |
|---|---|
| `for f in hooks/*.sh tools/*.sh; do bash -n "$f"; done` | **OK** |
| `jq -e . hooks/hooks.json` | **OK** |
| `jq -e . .claude-plugin/plugin.json` y `marketplace.json` | **OK** |
| `tests/escenarios/hooks/autoprueba-corredor.sh` | **106 PASS · 0 FAIL** (`rc 0`) |

---

## 2. El banco entero — tres corridas, y una de ellas es la que decide

Es un cambio de `hooks/`: el banco se corre **entero**. Y se corre **también sobre el árbol base
aislado**, porque un `FAIL` sin su pareja no dice si es del cambio o del host.

| # | árbol | condición | resultado | cuadre | `rc` |
|---|---|---|---|---|---|
| r1 | candidato `a05994f` | **contaminada por mí** (corrí el arnés en paralelo; `load` 5,07) | 1099 PASS · **3 FAIL** · 13 SKIP | 1115 ✓ | 1 |
| r2 | candidato `a05994f` | **limpia**, nada más en el host | **1103 PASS · 1 FAIL · 11 SKIP** | 1115 ✓ | 1 |
| r3 | **base aislado** (`6327b8c` en `hooks/` + `run.sh`, sección 42 retirada) | limpia | **1083 PASS · 1 FAIL · 11 SKIP** | 1095 ✓ | 1 |

**El delta atribuible es exacto: +20 casos, los 20 de la sección 42, los 20 en PASS.**
`1083 → 1103`. Ni un caso preexistente cambió de veredicto.

**El único `FAIL` NO es atribuible a la reparación, y la evidencia es una pareja, no una
repetición.** Es `DEV v3: heredoc CITADO de ~300 KB -> allow y barato` — veredicto `allow`
**correcto**, coste desbocado: **7881 ms** en el candidato (r2) y **5802 ms** en el **base**
(r3), contra un presupuesto de 1000 ms y techo holgado 4000 ms. **Falla igual sin la
reparación**, en el mismo host y en la misma sesión. Es la vía de análisis de `guard-codigo`,
que este cambio no toca. Lo dejo **anotado como condición del host, no desmentido**: no repito
hasta que salga verde.

Los 3 `FAIL` de r1 (los dos de reloj de `REQ-023 CA-09 (ii)` y `REQ-024 CA-07 (ii)`, más el
mismo heredoc) **desaparecen al no contaminar el host**. La contaminación fue mía y va escrita.

### 2.1 La diferencia de `SKIP` — mi juicio, y no coincide con el del desarrollador en la forma

El desarrollador declaró **12 SKIP (base) → 8 SKIP (candidato)** y lo atribuyó a variabilidad
de los instrumentos de tiempo, apoyándose en que los cuatro casos volvieron a `PASS` **en una
segunda corrida del propio árbol base**.

**Ese argumento no basta, y lo dije antes de medir:** repetir hasta que salga verde no desmiente
nada. Lo que **sí** decide es una comparación **pareada**, base y candidato adyacentes en la
misma sesión y el mismo host. La hice:

| | candidato (r2) | base aislado (r3) |
|---|---|---|
| nº de `SKIP` | **11** | **11** |
| conjunto | idéntico salvo **un** elemento en cada dirección | idem |

El intercambio es: el base abstuvo `REQ-017 CA-03` y el candidato no; el candidato abstuvo
`REQ-024 CA-07 (iv)` y el base no. **Uno en cada dirección** — no un desplazamiento monótono.

**Lo que la evidencia sostiene, y nada más:** la diferencia de `SKIP` **no es atribuible a la
reparación**, porque medida en pareja el recuento es **idéntico** y el conjunto difiere en un
elemento **en cada sentido**. **Lo que la evidencia NO sostiene:** que sea «ruido» en sentido
fuerte —no lo caractericé, y no toca: el instrumento de rendimiento está fuera de alcance por
instrucción del propietario—. La conclusión del desarrollador es correcta; **su argumento no lo
era**, y la corrida pareada es lo que la sostiene. Conservo las tres corridas como evidencia.

### 2.2 Fail-before, replicado por mí

Con `ARNES_HOOKS_DIR` apuntando al árbol base, los **8** casos discriminantes de la sección 42
fallan exactamente donde el desarrollador declaró: **4 en `42/A`** (clave decorada → sin aviso)
y **4 en `42/B`** (minúscula y sus variantes → sin aviso), todos con
`systemMessage esperado=si, fue=no`. Los otros 12 pasan. **12 PASS · 8 FAIL, replicado.**

*(Esa misma corrida arrastra ~100 `FAIL` espurios en los casos de `tools/`: `git archive … hooks`
no trae `tools/`, así que `rc=127`. Es un defecto de **mi** instrumento y por eso esa corrida
sólo se usa para el fail-before de la sección 42.)*

---

## 3. Comprobación 1 — **ningún `DENY` pasó a `ALLOW`**

### 3.1 Acto de FIRMAR (guardián del orden), modo reconstruido, 37 formas

Control del auditor **dentro de la corrida**: `Seguridad: aprobado` sobre `QA: pendiente` →
**DENY** en los tres árboles.

| forma | `v1.33.2` | base `6327b8c` | **candidato `a05994f`** |
|---|---|---|---|
| `Seguridad: aprobado` *(control +)* | DENY | DENY | **DENY** |
| `_Seguridad_` · `__…__` · `*…*` · `**…**` · `` `…` `` · `**Seguridad:**` | DENY ×6 | DENY ×6 | **DENY ×6** |
| ` Seguridad:` (sangrada) · `Seguridad :` · `Seguridad<TAB>:` | DENY ×3 | DENY ×3 | **DENY ×3** |
| `Seguridad: aprobado (R-045)` · `Seguridad: **aprobado**` | DENY ×2 | DENY ×2 | **DENY ×2** |
| `vetado` · `preventiva` · `n/a` · `pendiente` *(no son firma)* | ALLOW ×4 | ALLOW ×4 | **ALLOW ×4** |
| `seguridad:` · `SEGURIDAD:` · `SeGuRiDaD:` · `_seguridad_:` | ALLOW/mudo | ALLOW/mudo | **ALLOW + AVISO** |
| `Seguridad: aprobado-ish` *(control del aviso)* | AVISO | AVISO | **AVISO** |
| `_Seguridad_: aprobado-ish` · `` `Seguridad`: … `` · `**QA**: aprobadisimo` | **mudo** | **mudo** | **AVISO** |
| `Notas:` · `Sensible a seguridad:` · `Notas de seguridad:` · `qa-tester:` · `Modulo:` · `Archivos:` · `Prioridad:` | ALLOW/mudo | ALLOW/mudo | **ALLOW/mudo** |

**Cero celdas `DENY → ALLOW`. Cero celdas `ALLOW → DENY`.** Lo único que cambia es la columna
`mudo → AVISO`, que es el objeto de la reparación.

### 3.2 Acto de CIERRE — no se mueve **ni una** celda

`critico`, `QA: aprobado`, `Hallazgos abiertos: (ninguno)`, y el **único** veredicto de seguridad
escrito en la forma bajo prueba. `ALLOW` = esa forma **gobierna**.

| forma | `v1.33.2` | base | **candidato** |
|---|---|---|---|
| `Seguridad: aprobado` y sus **9** formas decoradas/sangradas/con paréntesis | ALLOW | ALLOW | **ALLOW** |
| `seguridad:` · `SEGURIDAD:` · `SeGuRiDaD:` · `_seguridad_:` · `Seguridád:` · `Segurid ad:` | DENY | DENY | **DENY** |
| `pendiente` · `preventiva` · `vetado` · `aprobado-ish` *(controles −)* | DENY | DENY | **DENY** |

**19 de 19 idénticas.** El cierre sigue fail-closed con la minúscula; la decorada sigue
gobernando (que es lo que `REQ-016 CA-04` contrata, no un defecto).

### 3.3 Falsación dirigida — la llamada nueva vive **entre** publicar y consumir el estado

`arnes_declara_clave` se llama en `guard-completado.sh:265`, **después** de que
`arnes_campos_req` publique `ARNES_OCULTA`/`ARNES_CITA_ABIERTA` y **antes** de que las ramas de
denegación los consuman (`:381`, `:474`, `:504`). Si los pisara, el `DENY` por cabecera no
medible se perdería: **fail-open**. Medido, con el desfase presente para forzar el recorrido:

| cabecera | base | **candidato** |
|---|---|---|
| BOM (`\xef\xbb\xbf`) delante de `Sensible a seguridad:` | DENY *(clave con algo insertado)* | **DENY, mismo motivo** |
| lo mismo **+** una línea `seguridad: aprobado` | DENY *(clave con algo insertado)* | **DENY, mismo motivo** |
| `<!--` sin cerrar en la cabecera **+** línea de desfase | DENY *(rango de comentario abierto)* | **DENY, mismo motivo** |

Los acumuladores del escáner van `local` y **no** se filtran. Verificado ejecutando, no leyendo.

### 3.4 Falsación dirigida — aviso **y** deny en la misma llamada

Riesgo real: dos objetos JSON en `stdout` dejarían la salida del hook inválida. Medido con una
edición que **avisa** (`qa: aprobado`, desfase) y **deniega** (`Seguridad: aprobado` sobre
`QA: pendiente`): la salida es **un solo objeto JSON válido** (`jq -s length` = 1), con el
`deny` íntegro. Igual que el base.

---

## 4. Comprobación 2 — **el lector NO se ensanchó**

Evidencia **estructural**, que es la que cierra la pregunta:

```
$ git diff --numstat 6327b8c a05994f -- hooks/lib.sh hooks/guard-completado.sh
39	2	hooks/guard-completado.sh
67	0	hooks/lib.sh
```

`hooks/lib.sh`: **67 añadidas, 0 borradas** — una función nueva, **ninguna existente tocada**.
`arnes_norm_clave`, `arnes_norm_campo`, `arnes_campo_linea`, `_arnes_clave_oculta`,
`arnes_campos_req` y `hooks/campos-req.awk` están **intactos**. Las 2 líneas retiradas de
`guard-completado.sh` son exactamente los dos `grep` de cadena literal.

Evidencia **de conducta**: la matriz §3.2, 19 de 19 celdas idénticas. La minúscula sigue **sin
ser** el campo; la tolerancia de `REQ-016 CA-04` sigue **exactamente** donde estaba.

Y ya no queda **ningún** disparador de decisión por cadena literal en código vivo:
`grep -n "grep -q '\(QA\|Seguridad\|Estado\|Rigor\|Hallazgos\)" hooks/*.sh tools/*.sh` → **0
resultados**. La única aparición de `'Seguridad: aprobado'` como cadena es el **texto** de un
`deny` (`guard-completado.sh:389`).

---

## 5. Comprobación 3 — la propiedad, **por sus dos bordes**

37 formas ejercidas contra el candidato, con el borde que importa medido primero.

### 5.1 Borde que no debe avisar — **0 falsos positivos de 18**

Ninguna de éstas avisa, incluidas cuatro que inventé yo y no están en el banco del
desarrollador:

`Sensible a seguridad: sí` · `Sensible a seguridad: no` · `Notas de seguridad: ninguna` ·
**`Auditor de seguridad: juan`** · **`Riesgo de seguridad: bajo`** · `qa-tester: juan` ·
**`QA-024: abierto`** · `Modulo:` · `Archivos:` · `Prioridad:` · `Versión destino:` ·
`NFR relacionados:` · `Estado:` · `Rigor:` · `Hallazgos abiertos: SEC-084 (contrato)` ·
`seguridadd: aprobado` · `seguro: aprobado` · **`https://x/seguridad: y`**

**Éste es el borde que decide**, porque un aviso sobre `Sensible a seguridad:` sería el falso
positivo que acaba con alguien ignorando los avisos. Está limpio.

### 5.2 Borde que sí debe avisar — avisa, y en formas que el banco no cubre

`seguridad:` · `SEGURIDAD:` · `**seguridad**:` · `SEGURIDAD :` · `_seguridad_:` ·
**`S e g u r i d a d:`** · `qa:` · `Qa:` · `QA :` · **`Q A:`** · `seguridad:aprobado` ·
**`Seguridád:`** (tilde) · **`Segurid ad:`** (blanco insertado) · **`seguridad\r:`** (CR dentro
de la clave) · desfase con **valor vacío** · **2000 líneas** de desfase → **un** aviso, no 2000.

### 5.3 Las formas que quedan mudas — y por qué **no** son un agujero

Busqué activamente «una forma que debiera avisar y no avise». Encontré ocho. **Siete se caen al
medirlas**, y la que queda es la frontera ya declarada:

| forma | aviso | acto de CIERRE | juicio |
|---|---|---|---|
| `Seguridad<TAB>: aprobado` | mudo | **ALLOW: gobierna** | **el lector SÍ la lee** (`lee`). Mudo es lo correcto; con valor fuera de vocabulario **sí** avisa. Mi expectativa era la equivocada |
| `«Seguridad»: aprobado` | mudo | **DENY** «clave con algo INSERTADO DENTRO» | la coge **la guarda de medibilidad**, con motivo explícito que **nombra el campo** |
| `- Seguridad: aprobado` | mudo | **DENY**, mismo motivo | idem |
| `> Seguridad: aprobado` | mudo | DENY | idem |
| `Seguridad.: aprobado` | mudo | **DENY**, mismo motivo | idem |
| `SEGURIDAD!: aprobado` | mudo | **DENY**, mismo motivo | idem |
| `Q.A.: aprobado` | mudo | — | idem |
| `Segurida:` / `Seguridd:` (letra omitida) | mudo | DENY por ausencia | **frontera declarada**: reponer *qué* letra exigiría elegir entre candidatos |

**La conclusión es que el reparto de trabajo es coherente y está completo por propiedad**: lo
ajeno al alfabeto de las claves lo coge la guarda de medibilidad **con diagnóstico**; la caja y
el marcado los coge el lector (`lee`) o el aviso nuevo (`desfase`); y sólo la
**sustitución/omisión de letra** queda fuera, que es la frontera declarada. No hay una franja
silenciosa entre las tres.

---

## 6. Comprobación 4 — la frontera declarada **sigue declarada y NO se presenta como cerrada**

Medido **con su control en la misma corrida**:

| entrada | acto de FIRMAR | acto de CIERRE (`critico`, único veredicto) |
|---|---|---|
| `Ѕeguridad: aprobado` (`Ѕ` = `U+0405` cirílica) | **ALLOW, sin aviso** | **DENY** (por ausencia — fail-closed) |
| `seguridad: aprobado` *(control +)* | ALLOW **con aviso** | DENY |
| `Seguridad: aprobado` *(control ++)* | **DENY** (orden) | ALLOW |

Sigue **pasando sin aviso**, tal y como se declara. Y está escrita **donde toca y sin
edulcorar**, en tres sedes que verifiqué:
`docs/arnes/sec-084-qa-024-19-disparador/00-reproduccion-y-reparacion.md` §4 (con el homóglifo
medido y su control), el comentario de `hooks/lib.sh` («ensanchar el LECTOR … no se hace») y
`AGENTS.md` §13, que ya la nombra por su nombre y dice que **ninguna** versión lo deniega,
«tampoco 1.34.0». **Nadie la presenta como cerrada.** Nada que objetar.

---

## 7. Hallazgos

### `QA-024-19` — **NO lo cierro.** Clase `contrato`, sin cambio

**Su mitad de código está cubierta, y lo acredito:**

| mitad del daño declarado (`docs/qa/1.34.0-req024-vuelta3-metodo.md` §12) | estado |
|---|---|
| **(a)** «el guardián del **orden** sobre esa edición» | **cubierta.** Para toda forma que el lector lee —incluidas las decoradas— el orden **deniega** (§3.1, 12 de 12). Para la minúscula **no hay firma que juzgar**, porque el lector no la lee |
| **(b)** «**el aviso**» | **cubierta.** `ALLOW + AVISO` que **cita la línea** y da la salida (§3.1, §5.2) |

**Y aun así no cierra, por una razón que no es un defecto de este entregable.** `AGENTS.md` §9:
*«El hallazgo no se cierra hasta que el requerimiento lo refleje —un criterio de aceptación
nuevo—… El `qa-tester` no da su veredicto `aprobado` hasta que existe.»* Faltan **dos**
write-backs, los dos del `analista-requerimientos`:

1. **El comportamiento aceptado no es criterio de nada.** `grep -rn "QA-024-19\|desfase"
   requirements/` no devuelve **ningún** criterio de aceptación sobre el aviso nuevo, su
   propiedad ni su frontera. Y es material, no formal: la resolución **cambia el `Esperado:` del
   propio hallazgo**. `QA-024-19` pedía «el mismo `DENY` que con `Seguridad: aprobado`»; lo
   entregado es **`ALLOW` + aviso**, con el argumento —que comparto— de que ensanchar el lector
   movería decisiones y estrecharía `CA-04`. Cambiar lo que se acepta y no escribirlo en el REQ
   es exactamente la deriva de §9.
2. **La frase que `QA-024-19` nombró como falsa sigue escrita igual.** `requirements/REQ-024.md`
   § «Una INSTANCIA medida que entra por `CA-01`…», **línea 815**: *«Cerrada la dirección, la vía
   se cierra **por construcción**»*. El hallazgo ya dijo que eso es **cierto para el acto de
   CIERRE y falso para el acto de FIRMAR**, y lo dejó nombrado para el analista. **Esta
   reparación no la vuelve verdadera** —el acto de FIRMAR sigue sin cerrarse por construcción:
   ahora **avisa**—, y el texto no se ha tocado. Mientras siga así, el REQ afirma algo falso
   sobre lo construido: `contrato`.

*Clase:* **`contrato`**, sin cambio. *Dueño de lo que queda:* `analista-requerimientos`.
*Forzador:* el propio write-back de `SEC-084 (2)`, que ya toca esta familia de frases.

### `QA-024-32` — **NUEVO** · clase `instrumento` · severidad **baja**

**El aviso de desfase se dispara sobre el CUERPO del REQ cuando la edición no arrastra el `## `.**

*Pasos de reproducción* (candidato `a05994f`; el mismo fixture da `mudo` en `6327b8c`):

1. REQ con cabecera limpia y un `## Criterios de aceptación` en el cuerpo.
2. `Edit` con `old_string = '- **CA-01** — texto viejo'` y un `new_string` que es texto de
   **cuerpo** —un criterio nuevo— que contiene, dentro de un bloque de código, la línea
   `seguridad: aprobado`.

*Esperado:* sin aviso — la línea está **después** del primer `## ` del documento y el lector
nunca la leería como campo. *Observado:* **aviso** de desfase: *«la linea 'seguridad:' NO se lee
como el campo 'Seguridad:' … el REQ no podra cerrarse por ausencia de veredicto de seguridad»* —
sobre un REQ cuya cabecera declara el campo perfectamente.

*Por qué pasa:* la rama nueva juzga `$nuevo` (los fragmentos concatenados), y `arnes_declara_clave`
para en el primer `## ` **del fragmento**. Si el fragmento no lo lleva, sus líneas se recorren
como si fueran cabecera. Con `Write` **no ocurre** (el contenido trae el `## `); con `Edit` y
`MultiEdit` **sí**. Confirmado también con un `MultiEdit` que añade una fila al Historial.

*Alcance real, sin inflarlo:* **no mueve ninguna decisión** —`ALLOW` antes y después, el `deny`
intacto cuando lo hay— y **no es nuevo en el radio**: el `grep` anterior miraba el documento
entero. Lo nuevo es que **ahora habla**.

*Lo que además queda medidamente falso, y es la parte que sí hay que arreglar:*
- **el comentario** de `hooks/guard-completado.sh:250`: *«una linea igual dentro de una seccion
  no dispara nada»* — con la rama de desfase **ya no es cierto**;
- **el nombre del caso** `42/D tras el primer '## ' ya no hay cabecera -> NO avisa`, que enuncia
  una propiedad **del documento** midiendo sólo el caso en que el `## ` viaja **dentro** del
  fragmento. El caso pasa; lo que promete su nombre, no.

*Ironía útil y forzador medido:* **el write-back de `QA-024-19` dispara este aviso**. Quien
escriba en `REQ-024` el criterio que cita `seguridad: aprobado` lo verá.

*Clase:* **`instrumento`** — nadie ve, decide ni cobra distinto: es un `systemMessage` y ninguna
puerta cambia. Ningún REQ afirma nada falso sobre esto (lo falso está en un comentario y en el
nombre de un caso, que son el **instrumento**). **No bloquea.** *Dueño:* `desarrollador`.
*Forzador:* el write-back de `QA-024-19`. *Vencimiento:* 1.35.0.

---

## 8. `SEC-084` — **no lo cierro yo** (es del auditor). Qué está y qué no

| # | remediación prescrita en `registro-seguridad.md` §R-027.4 | dueño | estado medido por mí |
|---|---|---|---|
| **1** | **El disparador**: que el acto se entre **por el lector**, no por una cadena, en las **dos** sedes | `desarrollador` | **PRESENTE y verificada.** Sede del **orden**: `v1.33.1`/`#48` (disparador por el **valor** leído) — medido: la firma decorada **DENY** en `v1.33.2`, base y candidato. Sede del **aviso**: esta reparación (`arnes_declara_clave`). **0** disparadores por cadena literal en código vivo |
| **2** | **El texto**: la fila de `AGENTS.md` §13 y su gemela de plantilla declaran su **alcance real**, con la oración titular verdadera **leída sola** | `analista-requerimientos` + **gate humano** | **AUSENTE.** `AGENTS.md:353` y `templates/AGENTS.md.tpl:311` siguen literalmente: *«Seguridad no firma lo que QA no ha validado (salvo `Seguridad: preventiva`)»*, **sin** declarar que «firma» significa *lo que el lector lee como el campo* |
| **3** | **Caso de banco** con fail-before, la cadena completa y los tres controles | `desarrollador` | **PRESENTE.** Sección 41 (26 casos, la matriz del orden) + sección 42 nueva (20 casos, la sede del aviso), con fail-before **replicado por mí**: 12 PASS · 8 FAIL contra el base (§2.2) |
| **4** | La verificación que la puerta **no** hace: `QA:` nunca comprueba si la aprobación **cubre el árbol** (`veredictos.caducan_con_codigo`) | `desarrollador`, **de contrato** | **AUSENTE.** Fuera del alcance de este encargo |

**Mi respuesta a la pregunta que se me hizo:** la remediación de `SEC-084` **está en su mitad de
código (1 y 3) y no en su mitad de texto (2 y 4)**. El **vector medido** que el auditor citó
—clave decorada que firma sin ser juzgada— **no se reproduce** en `v1.33.2`, ni en el base, ni en
el candidato; la línea `:334` que citó ya no existe. La clase que quedaba viva en la otra sede
**queda cerrada** por este entregable. **La evidencia histórica no se toca: fue cierta cuando se
tomó.** El auditor decide sobre (2) y (4).

---

## 9. Lo que NO validé, por su nombre

1. `CA-12 (ii)` y `CA-03` de `REQ-024` — fuera de alcance por el encargo (dependen de firmas que
   no han llegado).
2. Los `instrumento` abiertos de `REQ-024` (`04`–`11`, `15`–`18`, `22`, `23`, `27`–`31`) — fuera
   de alcance; **no bloquean**.
3. El instrumento de rendimiento y su falso rechazo — **prohibido reabrirlo** por el propietario.
   Por eso el `FAIL` del heredoc se **anota** con su pareja de base y no se investiga.
4. **Prueba de carga / NFR de rendimiento con umbral acordado:** `AGENTS.md` **no define** umbral
   de usuarios concurrentes ni de latencia para un plugin de hooks. Los techos de coste de
   `CA-07` y `CA-09` sí existen y los cubre el banco. **Queda pendiente de acordar con el
   humano**, como ya se dejó anotado en la vuelta 3. No afirmo «cumple».
5. **UI:** no hay. El único canal de usuario es el `systemMessage`, y ése sí lo ejercí.
6. **`docs/usuario/`: no lo escribo.** `QA-024-19` sigue abierto y su write-back está pendiente;
   documentar el flujo de la firma sería documentar un texto que va a cambiar.
7. **Integridad de dependencias:** no aplica — el proyecto no tiene gestor de paquetes ni
   lockfile (bash, `jq`, `awk`).
8. **CI:** ningún verde local lo sustituye. `hooks-en-linux` es la puerta requerida de `main`.

## 10. Flakiness

Ninguna inconsistencia en la conducta del disparador: las matrices de §3, §5 y §6 son
**deterministas** y se reprodujeron idénticas (la misma edición **tres veces seguidas** da el
mismo `ALLOW + AVISO`). La única variabilidad observada es **de los instrumentos de tiempo**,
acotada en §2 y §2.1, y no se presenta como pase ni como fallo.

## 11. Rutas de la evidencia

- Arnés de prueba propio y matrices: `<scratchpad>/qa/{harness.sh,c1b.sh,c2-cierre.sh,c3-bordes.sh}`
- Corridas del banco: `<scratchpad>/{banco-r1.txt,banco-r2.txt,banco-base-aislado.txt}`
- Conjuntos de `SKIP` pareados: `<scratchpad>/{skip-cand.txt,skip-base.txt}`
- Árbol base aislado: `<scratchpad>/base-aislado/`
- Evidencia del desarrollador reutilizada: `docs/arnes/sec-084-qa-024-19-disparador/00-reproduccion-y-reparacion.md`

*(El scratchpad es de sesión. Las cifras, los comandos y los fixtures están arriba elemento por
elemento para re-derivarlos sin preguntar; los árboles se materializan con el `git archive` de §0.)*
