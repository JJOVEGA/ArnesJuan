# REQ-023 · vuelta 3 de 3 (desarrollador) — método y evidencia para re-derivar sin preguntar

**Versión base:** árbol de trabajo sobre `5305de9` (rama `rel/registro-1.33.0`), **sucio a
propósito** (la partición de la sección 39 sin comitear). Los cuatro hallazgos que esta comisión
cierra están descritos en `docs/qa/1.34.0.md` § «REQ-023 · re-validación vuelta 1 de 3» (que es la
**vuelta 2** del contador del REQ).
**Fecha:** 2026-09-09 · **Agente:** `desarrollador` (Opus, política de autoalojamiento).
**Máquina:** WSL2 Linux 6.18.33.2, `bash` 5.3.9, `jq` 1.8.2, GNU awk 5.3.2, **sin `cygpath`**.
Cifras **operativas**: son la medición de este día en esta máquina y se rehacen midiendo.

Este archivo existe porque, sin él, la auditoría de seguridad y el write-back del analista
tendrían que **re-medir** para cumplir algo ya escrito (`AGENTS.md` §14.B.7).

## 0. Árboles usados, y por qué ninguno movió el árbol de trabajo

Nada de `git stash`, `reset --hard`, `clean -f`, `checkout .` ni `restore .` (`git.prohibidos`).
Se reusa el método de `docs/qa/1.34.0-req023-vuelta2-metodo.md` §0 en vez de inventar otro.

```
git worktree add --detach <tmp>/wt-95764db 95764db     # hooks ANTES del arreglo de QA-023-01
mkdir -p <tmp>/hooks-v1330 && git archive v1.33.0 hooks | tar -x -C <tmp>/hooks-v1330 --strip-components=1
```

Comprobación de que `v1.33.0` es de verdad la línea heredada:
`grep -c ARNES_CLAVES <tmp>/hooks-v1330/lib.sh` → **0** (la constante no existe ahí).
Los dos árboles se **retiran** al terminar (`git worktree remove`).

## 1. Lo que se verificó ANTES de tocar nada (no se heredó del informe de QA)

| Afirmación de QA | Verificación propia | Resultado |
|---|---|---|
| `run.sh:23` no define `SKIP` | `awk 'NR==23'` | `PASS=0; FAIL=0` — **confirmado** |
| `SKIP=0` de `:1241` es otro ámbito | `sed -n '1240,1241p'` | está en «Cuadre 1», **después** de correr las secciones — **confirmado** |
| las secciones se ejecutan con `source` en un subshell | `run.sh:1162-1176` | `source "${SECCIONES[sec_i]}"` dentro de `( )` — así que `PASS`/`FAIL` de `:23` **sí** son visibles y `SKIP` **no** existe |
| el recuento de SKIP sale del texto | `run.sh:1218` | `/^  SKIP / { s[n]++ }` — **confirmado**: el incremento es código muerto |
| «14 instancias» del patrón | `grep -rc 'SKIP=\$((SKIP+1))' …/secciones/*.sh` | **15**, no 14. Reparto medido: `39/4`=**7**, `39/5`=**4**, `39/2`=**1**, `39/3`=**1** (13 de REQ-023) + `28/1:149`=1 y `28/4:87`=1 (2 pre-existentes) |
| los ayudantes del corredor ya lo hacen bien | `run.sh:783`, `:808` | emiten `SKIP` con `echo` y **no** incrementan nada — la convención ya existía |

**Las cuatro muertes que QA situó**, confirmadas por línea: `39-…-2-la-clase.sh:288`,
`39-…-3-los-lectores-y-el-coste.sh:235`, `39-…-4-el-coste.sh:172`, `39-…-5-el-corpus.sh:172`.
Y hay **seis más** en `39-…-4-el-coste.sh` (`:139`, `:142`, `:188`, `:190`, `:203`, `:211`) y
**tres** en `39-…-5-el-corpus.sh` (`:174`, `:192`, `:204`) que matarían por la misma vía si su
rama se ejerciera: QA midió las cuatro muertes que sus dos provocaciones alcanzaron, no todas
las rutas letales que existen.

## 2. `QA-023-08` — el eje de la guarda, medido antes de escribir el arreglo

La variable **por línea** no es `ARNES_OCULTA` sino `ARNES_CLAVE_OCULTA`: `ARNES_OCULTA` es
**estado acumulado del llamador** (se pone a 0 al empezar una cabecera y se consulta al
terminarla, `hooks/lib.sh:1858-1862`), así que en un barrido línea a línea es **pegajoso** y
diverge en TODAS las líneas posteriores a la primera que dispare. Medirlo con la variable
equivocada da 50 618 divergencias sobre 25 309 líneas (= 2 × 25 309: todas), que es ruido, no señal.
Por eso el arreglo usa **`ARNES_CLAVE_OCULTA`** en la mitad por línea y **`ARNES_OCULTA`** en la
mitad por documento, que es donde la puerta la consulta.

Corpus del caso (el mismo que usa la sección: `secciones/[0-9][0-9]-*.sh` + `requirements/*.md`):
**25 309 líneas**.

```
# por linea, con ARNES_CLAVE_OCULTA y su representacion imprimible, normalizando a 0
# la version heredada (que no define la variable):  ${ARNES_CLAVE_OCULTA:-0}
bash <eva>.sh hooks/lib.sh              < corpus > hoy.txt
bash <eva>.sh <tmp>/hooks-v1330/lib.sh  < corpus > base.txt
diff base.txt hoy.txt | grep -c '^[<>]'     # -> 62  (31 lineas, dos lados)
awk -F'\t' '$5==1' hoy.txt | wc -l          # -> 31
```

**31 líneas** del corpus donde la guarda dispara hoy y no en `v1.33.0`. Ninguna es una cabecera
de REQ: son **prosa y código del propio banco** (`# QA`, `# Estado`, `- Estado:`,
`| 2026-09-09 | Estado`, `(QA`, `'Seguridad`, `^Estado`, `\Estado`) **más los fixtures
deliberados** de la sección 39 (`${BOM39}Sensible a seguridad`, `${BOM93}Estado`, `Est ado`).

Y por **documento**, que es como la puerta lo lee (`arnes_campos_req` + `arnes_estado_cabecera`
sobre el texto completo, imprimiendo `ARNES_OCULTA`, su clave, su representación y el estado que
esa línea declaraba):

```
para cada requirements/*.md (27, sin README):  diff <base> <arbol>
# -> REQ juzgados=27  divergentes=0
```

**0 divergencias** sobre los 27 REQ reales con las columnas de la guarda incluidas. Es la cifra
que hace que la mitad (2) del caso **pueda fallar** y hoy pase por medición, no por ceguera.

## 3. `QA-023-06` — fail-before / pass-after, con el método de la vuelta 2

**Remedio elegido: RETIRAR los incrementos, no definir `SKIP=0` en `run.sh:23`** — y el motivo no
es estilo. La lección ya estaba escrita **dos veces** en el propio corredor: *«El recuento sale DEL
TEXTO, no de variables. Los contadores del padre se quedaron a cero: cada sección incrementó los
suyos en su propio proceso y se los llevó al morir»* (`run.sh:1204-1208`), y sus **propios
ayudantes de SKIP** (`run.sh:783`, `:808`) emiten la línea con `echo` y **no** incrementan nada.
Definir `SKIP=0` habría dejado vivo un contador que **nadie lee** y que da un segundo número capaz
de desfasarse del que la línea `Resultado:` publica: dos transcripciones del mismo recuento.

Reproducción (copias en `ARNES_SECCIONES_DIR`, sin tocar el banco; la referencia a la línea base se
sustituye por un tag inexistente para **forzar** la abstención, como en la vuelta 2 §3):

```
cp secciones/39-*.sh <tmp>/sec-after/ ; cp secciones/39-*.sh <tmp>/sec-before/
# "before" = el arbol tal como estaba: se RE-INYECTA el incremento en cada rama de SKIP
sed -i -E 's/^([[:space:]]*echo "  SKIP .*)$/\1; SKIP=$((SKIP+1))/'          <tmp>/sec-before/39-*.sh
sed -i -E 's/^([[:space:]]*echo "  SKIP .*)"; return 0$/\1"; SKIP=$((SKIP+1)); return 0/' <tmp>/sec-before/39-*.sh
#   -> 13 incrementos re-inyectados (39/2=1 · 39/3=1 · 39/4=7 · 39/5=4), los mismos 13 retirados
sed -i -E 's/(mat9[0-9]) v1\.33\.0/\1 v0.0.0-inexistente/'  <tmp>/sec-{before,after}/39-*.sh
ARNES_SECCIONES_DIR=<tmp>/sec-before bash tests/escenarios/hooks/run.sh
ARNES_SECCIONES_DIR=<tmp>/sec-after  bash tests/escenarios/hooks/run.sh
```

| | **fail-before** | **pass-after** |
|---|---|---|
| `SKIP: unbound variable` | **4** (`39/2:288`, `39/3:235`, `39/4:172`, `39/5:172`) | **0** |
| `ABORT: … murió a mitad` | **4 secciones** | **0** |
| líneas en pantalla | 29 PASS · 6 FAIL · 4 SKIP | 29 PASS · 6 FAIL · **7 SKIP** |
| línea `Resultado:` | **`21 PASS, 0 FAIL`** | **`29 PASS, 6 FAIL, 7 SKIP`** |
| `rc` | 1 | 1 |

**Los tres SKIP que aparecen nuevos son el valor de este arreglo, y no se silencian** — son ramas
que llevaban desde que existen sin ejecutarse porque la primera abstención mataba la sección:

1. **`REQ-023 CA-03 rama 2: el BLANCO duplicado (P3) o insertado (E2) DENIEGA — esa rama se quedó
   sin ninguna tirada que abriera en la heredada`.** Es **la cláusula anti-tautología que `CA-03`
   eleva a criterio de aceptación** («aborta con SKIP y su motivo — nunca PASS— si … alguna de las
   dos ramas que exigen DENY se queda sin ninguna tirada»). **Nunca se había ejecutado**: la rama 1
   abstenía primero y la sección moría antes de llegar a la 2.
2. `REQ-023 CA-09 (i) la guarda no añade ni un proceso por evaluación` — sin línea base.
3. `REQ-023 CA-04 decisión a decisión sobre los REQ del árbol` — sin línea base.

**Y sí: la línea `Resultado:` puede publicar un recuento que contradiga la pantalla.** Medido
arriba: publicó `21 PASS, 0 FAIL` con **29 PASS y 6 FAIL impresos**. La causa está en `run.sh:1248-1255`
— cuando falta la marca `fin-N`, el cuadre emite el `ABORT:` y hace `continue`, **saltándose** la
acumulación de `:1271`, así que los casos de una sección muerta (que sí se contaron del texto, en
`NPASS/NFAIL/NSKIP`) **se descartan del total**. No es un fail-open —`PROBLEMAS` sube y el `rc` es
1— pero es un titular no veraz. **Queda anotado y NO se arregla en esta comisión** (`AGENTS.md`
§14.B.4): es un defecto **distinto** del hallazgo, vive en el cuadre del corredor y `REQ-024`
declara `run.sh` en su `Archivos:`. Clase propuesta: `instrumento`. Con este arreglo el disparador
que lo hizo visible desaparece del banco, pero la causa sigue ahí para la próxima sección que muera.

**Lo que NO cierro de `QA-023-06`, dicho con nombre y línea.** Quedan **2** instancias del patrón,
las dos **pre-existentes** y las dos en archivos de la **sección 28**:

| Sitio | Cuándo mata | Por qué no lo toco |
|---|---|---|
| `28-rotacion-seccion-1-la-historia.sh:149` | plataforma sin enlaces simbólicos (no es este host) | el archivo está en el `Archivos:` de **REQ-026**, que tiene `QA: aprobado (2026-09-09)` **sobre ese árbol**: editarlo dejaría la firma de QA sobre un árbol que ya no es |
| `28-rotacion-seccion-4-la-carrera.sh:87` | **la carrera no se provoca** (`CAR_DUR < 170000 µs`): depende del reloj, así que puede disparar **en cualquier corrida cargada** | mismo motivo: es la sección de `CA-18 (vi)`, dentro del alcance que la firma de QA de REQ-026 declara |

El segundo es el que importa: es una **bomba de flakiness** sobre la puerta requerida de `main`
—una máquina cargada mata la sección 28/4 y la vuelta sale roja por un motivo que no es el suyo—.
Dueño: quien tome la siguiente comisión sobre la sección 28 (forzador natural: el cierre de
`REQ-026`). No se abre como hallazgo nuevo: es **la misma clase** de `QA-023-06`, con dos sitios
que este REQ no puede tocar sin invalidar una firma ajena.

## 4. `QA-023-08` — el arreglo y su PAR DISCRIMINANTE

**Qué se cambió, y por qué en dos comparaciones y no en una.** El evaluador de `CA-04`
(`39-…-5-el-corpus.sh`) ahora publica la guarda en las dos mitades:

- **mitad (1), por línea:** dos columnas nuevas, `${ARNES_CLAVE_OCULTA:-0}` y su representación
  imprimible. Las **cuatro primeras** columnas (clave, valor, decorada) se siguen comparando
  **estrictas** —esa es la propiedad de `CA-06`, la guarda es observacional— y las **dos nuevas** se
  **cuentan y se publican**. No se exige identidad en las seis, y el motivo es que sería
  **insatisfacible**: una guarda que no cambia ninguna decisión no existe. Lo que sí se exige es lo
  contrario, y es la parte que vuelve el caso capaz de fallar: un **cuarto suelo** de anti-vacuidad
  —**al menos una** línea del corpus donde la guarda dispare—, porque con cero el «0 divergencias»
  es cierto por vacío en el único eje del que `CA-04` habla.
- **mitad (2), por documento:** `ARNES_OCULTA` con su clave, su representación y el estado que esa
  línea declaraba, impreso **dos veces**: tras `arnes_campos_req` y tras `arnes_estado_cabecera`,
  porque son **dos recorridos** y cada uno reinicia la variable. Aquí sí se exige **identidad**: una
  divergencia sobre un REQ real **es** la sobre-denegación que `CA-04` prohíbe.

**Par discriminante** (`AGENTS.md` §14.B.1: la mitad que no puede fallar no acredita nada). Se
inyecta una **sobre-denegación sintética** en una copia de `hooks/`: se retira el atajo
«si ya **es** una clave, aquí no hay nada» (`hooks/lib.sh:1840`), con lo que la guarda dispara
también sobre **toda clave legítima**.

```
cp -r hooks <tmp>/hooks-sobre
sed -i 's|^  arnes_en_vocab "$1" "$ARNES_CLAVES" && return 0$|  : # regresion sintetica|' <tmp>/hooks-sobre/lib.sh
# comprobado directamente sobre un REQ real:  oculta=1|oclave=Estado|orepr=Estado|oestado=en-progreso
# las copias de la seccion van DENTRO de un worktree, porque REPO92 se deriva de SEC_DIR
# (`${SEC_DIR%/}/../../../..`, seccion 39/5:32) y no de HOOKS_DIR: fuera de un repo, mat92
# no encuentra el tag y las dos mitades se ABSTIENEN — el par no discriminaria por otra razon.
ARNES_SECCIONES_DIR=<wt>/tests/escenarios/hooks/sec-{viejo,nuevo} ARNES_HOOKS_DIR=<tmp>/hooks-sobre \
  bash tests/escenarios/hooks/run.sh
```

| Evaluador | corpus | guarda dispara | campo a campo | decisión a decisión | `Resultado:` |
|---|---|---|---|---|---|
| **ciego** (pre-arreglo) | 3 013 líneas | **0** (no lo mira) | PASS | **PASS** | **3 PASS, 0 FAIL** |
| **arreglado** | 3 018 líneas | **163** | PASS | **FAIL** (`27 de 27 REQ deciden distinto`, el primero `REQ-001.md`) | **2 PASS, 1 FAIL** |

**El evaluador ciego da verde a una guarda que sobre-deniega los 27 REQ del árbol.** Eso es
`QA-023-08` medido, y el arreglo lo caza nombrando el primer REQ divergente.

**Pass-after sobre los hooks reales** (`bash run.sh 'secciones/39-caracter-invisible-5-el-corpus.sh'`
→ **3 PASS, 0 FAIL**):

```
PASS  CA-04 anti-vacuidad: 5223 líneas con campo, 1874 clave no ASCII, 2182 valor no ASCII,
      3230 clave decorada y 32 donde la GUARDA DISPARA
PASS  CA-04 campo a campo: IGUAL en las 5223 líneas (clave, valor, decorada) — la GUARDA
      difiere en 32 (la primera: «\x23\x20QA»)
PASS  CA-04 decisión a decisión: los 27 REQ deciden IGUAL, y la GUARDA no dispara en ninguno
```

Las **32** (31 antes de este cambio; el glob del corpus recoge el propio comentario nuevo de la
sección, que contiene `# QA`) son **prosa y código del banco** más los fixtures deliberados de la
sección 39. Ninguna es una cabecera de REQ, y **eso lo mide la mitad (2)**, no la (1): el texto de
cada caso dice ahora qué mide **cada uno**, en vez de atribuir a la mitad (1) una conclusión que
sale de la (2) — que es la falta que este hallazgo persigue.

## 5. `QA-023-04` — la derivación de claves de la sección 36

**Reproducción del hallazgo** con el mismo `sed` de QA (`docs/qa/1.34.0-req023-vuelta2-metodo.md` §8),
sobre los tres árboles:

```
v1.33.0 : Estado|Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad     (6)
95764db : Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad            (5)
árbol   : Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad            (5)
. hooks/lib.sh; printf '%s\n' "$ARNES_CLAVES"
        -> QA|Seguridad|Sensible a seguridad|Hallazgos abiertos|Rigor|Estado    (6)
```

**Arreglo:** `36-…-2-los-lectores.sh:228` deja de raspar el **texto** del `case` y lee la
**constante** `ARNES_CLAVES` (`$( . "$HOOKS_DIR/lib.sh"; printf '%s' "${ARNES_CLAVES:-}" )`), que ya
viene delimitada por `|` —el separador que la alternancia del `awk` necesita—. Es la lección que
`tools/arnes-lectura.sh:64-69` ya tenía escrita para su propia derivación: *«una derivación que lee
CÓDIGO se rompe con la primera mudanza del código»*. Y **1 proceso** contra los 3 de antes
(`sed`+`sort`+`paste`). Fail-closed conservado: si el lector de `$HOOKS_DIR` no declara la constante
(hooks anteriores a 1.34.0) `CLAVES36` sale vacío, el corpus sale vacío y el caso del tamaño de
corpus **FALLA** — y ahora su mensaje **nombra esa causa** en vez de dejarla adivinar.

**La cobertura que se había perdido en silencio, medida** (mismo `awk` de cosecha de la sección,
sobre el glob real `secciones/[0-9][0-9]-*.sh`):

| Derivación | rachas cosechadas | rachas que contienen una línea `Estado:` |
|---|---|---|
| `sed` sobre el texto (5 claves) | 127 | **0** |
| constante `ARNES_CLAVES` (6 claves) | 154 | **102** |

Cero de 127 contra 102 de 154, **sobre la clave que decide el cierre**. Las rachas son **runs de
líneas de campo consecutivas**, así que añadir `Estado` **funde** runs que antes se partían: por eso
el número de rachas sube y, sobre un corpus de una sola sección, puede **bajar** (medido: 21 → 18).

**Lo que NO acredito de este arreglo, y por qué.** No construí un par fail-before / pass-after de
**veredicto**, y no es un olvido: la propiedad de `36-…-2` compara **una versión contra sí misma**
—la misma cabecera con y sin el rango `<!-- … -->` insertado— y **re-deriva sus bases con los mismos
hooks**, así que una regresión que convierta un `deny` en `allow` **retira la base** en vez de poner
el caso rojo. Verificado inyectando una regresión sintética en una copia (`arnes_estado_cabecera`
gobernada por la **última** aparición de `Estado:` en vez de la primera, `hooks/lib.sh:2173`): las
dos derivaciones dan **4 PASS, 0 FAIL**, y lo único que se mueve es el **número de bases que
deniegan** (6 → 2 con la derivación vieja; 4 → 2 con la nueva). Lo acreditado aquí es la
**cobertura**, que es lo que el hallazgo dice, no un veredicto.

**Y un defecto DISTINTO que sale de esa comprobación, anotado y no arreglado** (`AGENTS.md` §14.B.4):
el caso tiene suelo de anti-vacuidad para las **cabeceras cosechadas** (`>= 10`) pero **ninguno para
las bases que DENIEGAN**, que son las únicas que la propiedad puede violar. Con la regresión
sintética bajaron a **2** y nada lo dijo. Clase propuesta: `instrumento`, severidad baja. Dueño
natural: quien tome la siguiente comisión sobre la sección 36. No entra aquí porque es alcance
nuevo en la última vuelta.

## 6. `QA-023-05` — la FORMA de la línea, publicada; y un número que sube la apuesta

**Arreglo (la mitad que es mía).** El esqueleto de la línea que se dobla vive ahora en **una**
variable (`S94`) que es a la vez lo que la sonda repite y lo que el caso **publica**, así que no se
puede publicar una forma que no se midió. La línea del caso pasa de decir el **par** a decir el par
**y la forma**:

```
PASS  REQ-023 CA-09 (iii) arnes_norm_clave no crece más que linealmente al doblar la línea
      cociente de duplicación (1000→2000 bytes, k=200, forma «título», se dobla la CLAVE,
      esqueleto «# REQ-023 — El carácter que no se ve apaga el enforcement, y » repetido hasta
      n bytes y cerrado con «: pendiente») = 2.079× (techo 2.200×; 638214µs sobre 306875µs)
```

La abstención por debajo del suelo de 50 ms también publica la forma: un µs sin su forma no es
comparable con el de la vuelta siguiente. **El techo de 2,2 no se toca** y no se usa como margen.
El segundo **parámetro del criterio** sigue siendo del `analista-requerimientos`; esto es sólo que
la salida deje de ser incomparable.

**Y el número que sube la apuesta, medido por mí y repetido cinco veces.** Con el **mismo par**
(1000→2000), el **mismo `k`** (200, `--r 3`, mínimo de k) y el **mismo sujeto**
(`arnes_norm_clave`), tres formas conformes del criterio:

| Forma que se dobla | esqueleto | 1000 µs | 2000 µs | cociente |
|---|---|---|---|---|
| **título** (la que el caso mide) | `'# REQ-023 — El carácter…'` repetido + `: pendiente` | 301 294 | 615 201 | **2,042** |
| **clave** | `Sensible a <n×'a'> seguridad: sí` | 253 288 | 566 096 | **2,235** ⚠ |
| **valor** | `Rigor: <n×'a'>` | 87 627 | 151 296 | **1,727** |

Cinco tomas de la forma **clave**: **2,235 · 2,284 · 2,260 · 2,255 · 2,240** — las cinco **por
encima del techo de 2,2**. QA midió esa misma forma en **2,181** (por debajo) en su sesión.

**Lo que esto añade al hallazgo, y lo que NO afirmo.** *Afirmo* que en esta sesión, con el par y el
`k` que el criterio publica, hay una lectura **conforme** de `CA-09 (iii)` que **pasa** (título) y
otra igualmente conforme que **no pasa** (clave, y la clave es una clave **real** del lector, no un
fixture): el parámetro que falta no es cosmético, **decide el veredicto**. *No afirmo* que el techo
se supere: el estadístico es el mínimo de k y su contaminación por carga **no es monótona en un
cociente** —afecta a los dos términos—, así que un cociente por encima del techo en una máquina
cargada no es prueba concluyente. Lo robusto es la **dispersión entre formas medidas en la misma
sesión** (1,727 → 2,235, un 29 %), que es exactamente lo que el hallazgo dice.

**No cambio la forma que el caso mide, y el motivo importa:** elegir la forma que pasa sería la
misma tautología que `QA-023-02` un eje más allá —un sujeto seleccionado para no fallar—. La forma
se **declara**; qué forma debe fijar el criterio es del analista.

## 7. Puertas de esta comisión (cifras propias)

```
for f in hooks/*.sh tools/*.sh; do bash -n "$f" || exit 1; done              # rc 0
jq -e . hooks/hooks.json >/dev/null                                          # rc 0
jq -e . .claude-plugin/plugin.json && jq -e . .claude-plugin/marketplace.json  # rc 0
for f in tests/escenarios/hooks/secciones/*.sh; do bash -n "$f"; done        # rc 0 (57 secciones)
bash tests/escenarios/hooks/run.sh            # 962 PASS · 0 FAIL · 4 SKIP · rc 0  (43,8 s)
bash tests/escenarios/hooks/run.sh            # 962 PASS · 0 FAIL · 4 SKIP · rc 0  (44,7 s)
bash tests/escenarios/hooks/run.sh            # 962 PASS · 0 FAIL · 4 SKIP · rc 0  (49,8 s)
#   la TERCERA es DESPUÉS de editar requirements/REQ-023.md y CHANGELOG.md, que están DENTRO
#   del corpus del propio banco: sin ella, las cifras publicadas serían de un árbol anterior
#   al que se entrega. 42 casos de REQ-023 en las tres.
bash tests/escenarios/hooks/autoprueba-corredor.sh   # 106 PASS · 0 FAIL · rc 0
bash tools/arnes-lectura.sh .                 # rc 0
bash tools/arnes-paralelo.sh REQ-023 REQ-024  # colisiona hooks/lib.sh (esperado), rc 0
```

**La LISTA de SKIP (nunca el total), idéntica en las dos corridas y a la de QA:**
`ruta estilo Windows con backslashes` (sin `cygpath`) · `REQ-017 CA-05 (i)` · `REQ-017 CA-05 (ii)` ·
`REQ-021 CA-08 (iii)`. `REQ-017 CA-08 (ii)` **no** skipeó en ninguna de las dos. **El total 962 no
se movió**: no se creó ni se perdió ningún caso (los cuatro arreglos cambian lo que los casos
**publican**, no cuántos son, y `CASOS_ESPERADOS` sigue en 966 con los 4 SKIP dentro).

**Ninguna abstención NUEVA apareció en el banco real**, y eso es lo esperado: las tres que el
arreglo de `QA-023-06` desbloquea sólo se emiten cuando la **línea base falta**, y en este árbol
`mat94 v1.33.0` la materializa. Los tres SKIP nuevos se midieron **provocando** esa falta (§3), que
es la única forma de ejercerlos sin romper el árbol.

## 8. Limitaciones materiales de esta medición

- Un solo host, un solo sistema operativo: **nada** se midió en Windows/MSYS, que es de donde sale
  el BOM y donde el `SKIP` de `28/1:149` (plataforma sin enlaces simbólicos) **sí** dispararía.
- Las cifras de reloj son de una máquina con carga variable. En un **cociente** la contaminación
  **no es monótona** (afecta a los dos términos), así que el 2,235 de la forma «clave» **no** prueba
  que el techo se supere; lo robusto es la dispersión entre formas de la **misma** sesión.
- Dos corridas del banco no acreditan ausencia de flakiness: acreditan que **en esta ventana** la
  lista de SKIP y el total no se movieron. Y hay una **bomba conocida** que no se toca: `28/4:87`
  depende del reloj (§3).
- El par discriminante de `QA-023-04` **no existe** y se dice por qué (§5): la propiedad de esa
  sección compara una versión contra sí misma.

## 9. Estado de la comisión

- [x] Verificación previa de los cuatro hallazgos (§1 y §2).
- [x] `QA-023-06` — retirados **13** de 15 (§3); los 2 de la sección 28 quedan declarados con dueño.
- [x] `QA-023-08` — la guarda se publica en las dos mitades, con par discriminante (§4).
- [x] `QA-023-04` — la derivación lee la constante; cobertura 0/127 → 102/154 (§5).
- [x] `QA-023-05` — `CA-09 (iii)` publica la forma; y la forma «clave» cruza el techo (§6).
- [x] Quality gates, banco entero, autoprueba, lista de SKIP (§7).
- [x] Fail-before / pass-after de `QA-023-06` y `QA-023-08`; para `QA-023-04` y `QA-023-05` va la
      medición que el hallazgo pide, y se declara que **no** son pares de veredicto.
- [ ] **No hecho a propósito, por instrucción de la comisión:** no se firma `QA:` ni `Seguridad:`,
      no se cambia `Estado:` (sigue en `en-progreso`), no se hace el write-back de `QA-023-07`
      (`analista-requerimientos`), no se comitea ni se empuja, y no se toca `docs/ESTADO.md`.
- [ ] **Fuera de alcance y sin tocar:** `REQ-024`, los hallazgos de `REQ-026`, y las tres clases
      declaradas fuera de alcance del REQ (homóglifo, NUL/UTF-16, el invisible delante del `## `).

## 10. Defectos distintos, anotados y NO arreglados (§14.B.4)

| # | Qué | Dónde | Clase propuesta | Dueño natural |
|---|---|---|---|---|
| 1 | La línea `Resultado:` **descuenta** los casos de una sección muerta: el `ABORT:` hace `continue` **antes** de la acumulación, así que `NPASS/NFAIL/NSKIP` de esa sección se pierden. Medido: `21 PASS, 0 FAIL` con 29 PASS y 6 FAIL en pantalla. No es fail-open (`PROBLEMAS` sube, `rc` 1); es un titular no veraz | `run.sh:1248-1255` frente a `:1271` | `instrumento` | quien tome `run.sh` (está en el `Archivos:` de **REQ-024**) |
| 2 | El caso de `36-…-2` tiene suelo de anti-vacuidad para las **cabeceras cosechadas** (`>= 10`) pero **ninguno** para las **bases que DENIEGAN**, que son las únicas que su propiedad puede violar. Con una regresión sintética bajaron a **2** y nada lo dijo | `36-cabecera-comentario-html-2-los-lectores.sh` | `instrumento` (baja) | siguiente comisión sobre la sección 36 |
| 3 | Los **2** `SKIP=$((SKIP+1))` restantes. El de `28/4:87` depende del **reloj** y puede matar su sección en cualquier corrida cargada | `28-rotacion-seccion-1:149`, `28-rotacion-seccion-4:87` | misma clase que `QA-023-06` (`instrumento`) | siguiente comisión sobre la sección 28 (forzador: cierre de **REQ-026**) |

## 11. `QA-023-05` — LA BASE HEREDADA `v1.33.0` SOBRE LA MISMA FORMA: quién paga el cociente

Esta sección es la **continuación** de §6 y responde a la única pregunta que quedaba abierta ahí
(«*la candidata apenas mueve el número, así que el coste puede no estar en la guarda*»): si la
línea heredada **ya** pagaba este cociente, `CA-09 (iii)` no mide lo que REQ-023 añade.

**Fecha:** 2026-09-09 · **Agente:** `desarrollador` (Opus) · **Versión base del árbol medido:**
árbol de trabajo sobre `5305de9` (rama `rel/registro-1.33.0`, sucio a propósito) contra el tag
**`v1.33.0`**. **Nada se arregló y nada se firmó**: es una medición.

### 11.1 Método — re-derivable sin preguntar, y sin retipear ni un parámetro

Los parámetros y el medidor **se extraen de la propia sección** y el juez del registro **de
`run.sh`**: retipearlos sería una segunda transcripción, y dos transcripciones se desfasan (es la
misma lección de `QA-023-04`, §5).

```
git archive v1.33.0 hooks | tar -x -C <tmp>/hooks-v1330 --strip-components=1
grep -c ARNES_CLAVES <tmp>/hooks-v1330/lib.sh        # -> 0  (la constante no existe: es la heredada)
bash -c "source <lib>; type -t arnes_norm_clave arnes_campo_linea"   # -> function function EN LAS DOS

# el juez del registro, extraido de run.sh (sonda_num + sonda_es_util + sonda_emisor_conocido + sonda_lee)
sed -n '415,417p' run.sh; sed -n '/^sonda_es_util()/p;/^sonda_emisor_conocido()/p' run.sh
awk '/^sonda_lee\(\) \{/,/^\}/' run.sh                       #  -> <tmp>/juez.sh   (56 lineas)
# los parametros y el medidor, extraidos de 39-caracter-invisible-4-el-coste.sh
sed -n '148,151p' 39-…-4-el-coste.sh                         # PAT94 REL94 PRE94/POS94 FORMA94
awk '/^mide94\(\) \{/,/^\}/' 39-…-4-el-coste.sh ; sed -n '215,216p' 39-…-4-el-coste.sh
                                                             #  -> <tmp>/medidor.sh (28 lineas)
```

**Los tres parámetros del cociente son los del caso, sin una sola variación:** par
**1000 → 2000** bytes, **`k` = 200** (`--r 3`, mínimo de k), forma **«clave»** (esqueleto
`Sensible a @ seguridad: no`, `@` = `a` repetido hasta que la línea entera mida `n` bytes), y los
**dos** sujetos `arnes_norm_clave` y `arnes_campo_linea`.

**Todo en UNA tanda, con base y candidata intercaladas — que es la razón de ser de esta medición**
(`REQ-007 CA-59`: comparar dos tandas distintas es el error). El intercalado se hace **al nivel de
`n`**, no al nivel de versión: para cada sujeto y cada `n` se mide **base y candidata seguidas**,
de modo que cualquier deriva de carga entre el bloque de 1000 y el de 2000 cae **sobre las dos
versiones por igual** — y eso es lo que vuelve comparable la *relación* entre ellas. Además el
**orden** base/candidata **se alterna por toma y se publica** (`base,cand` en las impares,
`cand,base` en las pares), para que un sesgo sistemático de «la segunda mide caliente» no caiga
siempre del mismo lado del cociente. **5 tomas** (el caso pide 3; más son conformes), 40
invocaciones de la sonda, **118 s** de reloj.

**Estado de la máquina, publicado porque hoy da medidas altas** (el propietario midió un FAIL por
reloj en `DEV v3: heredoc CITADO de ~300 KB`, 4835 ms contra techo 4000, con veredicto `allow`
correcto): `loadavg` **0,40 / 2,26 / 1,69** al empezar y **1,06 / 1,89 / 1,62** al terminar.

### 11.2 Las tomas, elemento por elemento (no sólo las medianas)

Cociente `us(2000)/us(1000)`, cinco tomas, con los dos términos de cada una:

| sujeto | ver. | t1 | t2 | t3 | t4 | t5 |
|---|---|---|---|---|---|---|
| `arnes_norm_clave` | **base** | **5,815** (439 679→2 556 813 µs) | 2,979 (419 536→1 249 848) | 2,392 (438 380→1 048 726) | 2,474 (436 437→1 080 144) | 2,492 (434 463→1 083 114) |
| `arnes_norm_clave` | cand | 2,532 (530 244→1 343 065) | 2,257 (514 031→1 160 644) | 2,048 (578 548→1 185 288) | 2,295 (521 246→1 196 411) | 2,367 (516 609→1 222 934) |
| `arnes_campo_linea` | **base** | 2,528 (490 125→1 239 110) | 2,135 (579 391→1 237 366) | 2,587 (481 804→1 246 679) | 2,446 (485 548→1 187 792) | 2,318 (518 735→1 202 503) |
| `arnes_campo_linea` | cand | 2,291 (545 019→1 248 988) | 2,586 (551 227→1 425 824) | 2,145 (592 489→1 270 964) | 2,237 (580 127→1 298 313) | 2,022 (642 069→1 298 522) |

La `t1` de la base de `arnes_norm_clave` es una **ráfaga de carga sobre el término largo**
(2,56 s frente a ~1,07 s en las otras cuatro): se **publica y no se descarta**, y se muestra abajo
que la mediana no depende de ella.

### 11.3 Mediana y dispersión, con la misma regla del caso

| sujeto | versión | mediana | rango | dispersión | margen al techo 2,2 | ¿afirmable? |
|---|---|---|---|---|---|---|
| `arnes_norm_clave` | **base `v1.33.0`** | **2,492** | 2,392 – 5,815 | 3,423 | 0,292 | **NO** (disp ≥ margen) |
| `arnes_norm_clave` | candidata | **2,295** | 2,048 – 2,532 | 0,484 | 0,095 | **NO** |
| `arnes_campo_linea` | **base `v1.33.0`** | **2,446** | 2,135 – 2,587 | 0,452 | 0,246 | **NO** |
| `arnes_campo_linea` | candidata | **2,237** | 2,022 – 2,586 | 0,564 | 0,037 | **NO** |

Retirando de cada serie su toma más alta, **ninguna mediana se mueve** (2,492 · 2,295 · 2,446 ·
2,237) y las dispersiones bajan a 0,587 · 0,319 · 0,393 · 0,269: el resultado **no lo sostiene el
valor extremo**. Y las dos medianas de la candidata **reproducen** las de la corrida del banco que
el propietario verificó tras la parada (2,277 y 2,318, dispersiones 0,807 y 0,352), lo que
acredita que esta tanda no es un caso aparte.

### 11.4 La relación emparejada cand/base, que es lo robusto en un cociente

En un cociente la contaminación por carga **no es monótona** —cae sobre los dos términos—, así que
lo comparable no es cada cifra suelta sino la **relación entre las dos versiones dentro de la misma
toma**:

| sujeto | t1 | t2 | t3 | t4 | t5 | **mediana** |
|---|---|---|---|---|---|---|
| `arnes_norm_clave` | 0,435 | 0,758 | 0,856 | 0,928 | 0,950 | **0,856** |
| `arnes_campo_linea` | 0,906 | 1,211 | 0,829 | 0,915 | 0,872 | **0,906** |

**En 9 de los 10 pares la candidata tiene el cociente MENOR que la base heredada**, y en las cinco
tomas de `arnes_norm_clave` **sin una sola excepción**. El único par en que sube (`campo_linea`
t2, 1,211) queda dentro de la dispersión observada de la propia base (0,452).

**Y el mecanismo que lo explica, para que no quede como una coincidencia** — el coste **absoluto**
que la guarda añade (materia de `CA-09 (ii)`, **no** de (iii), que tiene su propio caso y su propio
sujeto; aquí sólo se publica el dato):

| sujeto | cand/base a n=1000 | cand/base a n=2000 |
|---|---|---|
| `arnes_norm_clave` | 1,206 (1,189 – 1,320) | 1,108 (0,525 – 1,130) |
| `arnes_campo_linea` | 1,195 (0,951 – 1,238) | 1,080 (1,008 – 1,152) |

La guarda añade ~20 % a 1000 bytes y ~10 % a 2000: su peso **relativo baja** al alargar la línea,
así que **diluye** el cociente en vez de empeorarlo. La superlinealidad que `(iii)` mide sobre la
forma «clave» **no está en la guarda**: está en el camino que `arnes_norm_clave` ya recorría en
`v1.33.0`.

### 11.5 Qué decide esto, y qué NO afirmo

**Lo que decide.** La base heredada paga un cociente **equivalente o mayor** que la candidata sobre
la forma contratada (medianas 2,492 y 2,446 contra 2,295 y 2,237). Por tanto **`CA-09 (iii)`, tal
como está redactado, no mide lo que REQ-023 añade: mide una propiedad que el lector ya tenía en
`v1.33.0`.** Un criterio de aceptación de este REQ que la línea heredada **también** incumple no
puede acreditar ni desacreditar la guarda. **`QA-023-05` es un hallazgo contra el CRITERIO**, y su
dueño es el `analista-requerimientos` — no hace falta que la decisión suba al propietario por falta
de vueltas, porque no hay nada que arreglar en `hooks/`.

**Lo que NO afirmo, y es la mitad honesta.** *No* afirmo que ninguna de las cuatro series supere el
techo de 2,2: las cuatro tienen **dispersión ≥ margen**, así que por la regla del propio caso
**ninguna es afirmable** hoy en esta máquina, ni la base ni la candidata. Esta medición **no**
resuelve dónde debe estar el techo; resuelve **de quién es el número**. *No* afirmo tampoco nada
sobre `CA-09 (ii)`: el 1,2× absoluto de 11.4 se mide sobre un sujeto sintético de 1000 bytes, no
sobre el sujeto de (ii), y (ii) tiene su propio caso con su propia línea base.

### 11.6 Lo que un arreglo en `hooks/lib.sh` costaría — DESCRITO Y NO APLICADO

Se describe porque la comisión lo pide y **no se aplica**: `hooks/` es gate humano (`AGENTS.md` §6)
y estamos en la última vuelta. **Y sobre todo porque 11.5 dice que no toca:** el coste no está en
la guarda, así que un cambio en la guarda **no bajaría el cociente** — bajaría el 1,2 × absoluto de
11.4, que es otra cosa. Lo que el cociente pide es tocar el camino **heredado** de
`arnes_norm_clave` (el recorte de blancos y los `${l%%:*}` sobre líneas largas), es decir **alcance
nuevo** sobre código que este REQ no introdujo, con su propio par fail-before/pass-after y su
propia auditoría. No cabe en esta vuelta y no se propone como parte de este REQ.

### 11.7 Puertas de esta medición

**El banco NO se vuelve a correr, y el motivo:** esta sección **no toca ningún archivo del banco**
ni de `hooks/`. Los únicos archivos escritos son este documento y `CHANGELOG.md`. Las cifras del
banco vigentes son las que el propietario verificó tras la parada (960 PASS · 0 FAIL · 6 SKIP;
autoprueba 106 · 0), y ahorrarse los ~80 s es correcto porque no hay nada nuevo que puedan medir.

```
for f in hooks/*.sh tools/*.sh; do bash -n "$f" || exit 1; done                # rc 0
jq -e . hooks/hooks.json >/dev/null                                            # rc 0
jq -e . .claude-plugin/plugin.json && jq -e . .claude-plugin/marketplace.json  # rc 0
git worktree list          # sin worktrees: la base se materializo con `git archive`, no con worktree
```

### 11.8 Limitaciones materiales de esta medición (además de las de §8)

- **Un host, una ventana de 118 s, con carga ajena variable y una máquina que hoy da reloj alto**
  (el FAIL de 4835 ms citado en 11.1). La `t1` de la base lo demuestra dentro de la propia tanda.
- **La conclusión es de RELACIÓN, no de nivel.** Se afirma «la base paga tanto o más que la
  candidata» (9 de 10 pares, mediana 0,86 y 0,91); **no** se afirma el valor absoluto de ningún
  cociente ni que el techo se supere.
- **Sólo la forma «clave».** Nada se remidió sobre «título» ni «valor» (§6): si el analista fija
  otra forma, este número **no se recicla**.
- **`k` = 200 y par 1000→2000, por instrucción.** No se exploró si un par mayor separa las dos
  versiones: podría, y sería otra medición.

---

## 12. EXTENSIÓN EXCEPCIONAL de `REQ-023` (alcance CERRADO, autorizada por el propietario) — `CA-10` y el re-apunte de la prueba de `CA-09 (iii)`

**Fecha:** 2026-09-09 · **Agente:** `desarrollador` (Opus) · **Versión base del árbol medido:**
`710e3e7` (rama `rel/registro-1.33.0`, árbol limpio al empezar) · **Línea base de las relaciones:**
tag **`v1.33.0`**.

**Qué es y qué NO es.** `REQ-023` está en `Estado: bloqueado` tras agotarse las **tres** vueltas
dev↔QA. Esto **no es una cuarta vuelta** y no reinicia el contador: es la extensión de alcance
**cerrado** que el propietario autorizó, limitada a **tres** cosas — (a) completar `CA-10`,
(b) corregir la prueba de `CA-09 (iii)` conforme al **contrato vigente**, (c) consolidar. **Nada se
firmó, nada se cerró y ningún umbral se relajó:** `Estado:`, `QA:`, `Seguridad:` y
`Hallazgos abiertos:` no se tocan, y `QA-023-15` sigue abierto. Ninguna prueba se retiró.

### 12.1 `CA-10` — la superficie heredada deja de nombrar el carácter y pasa a nombrar la propiedad

**Archivos escritos (los tres que `CA-10` nombra):** `AGENTS.md` §13, `templates/AGENTS.md.tpl` (la
**misma** fila) y `skills/arnes-upgrade/SKILL.md` (apartado de migración «Hacia 1.34.0»).

**(a) La fila de la tabla de enforcement, en los DOS archivos y byte a byte idéntica** (comprobado
con `diff <(sed -n '349p' AGENTS.md) <(sed -n '314p' templates/AGENTS.md.tpl)` → sin salida). Antes
definía la invariante **por el retorno de carro**; ahora la enuncia **por el estado** —una línea de
la cabecera que la máquina **no puede medir** no deja cerrar—, con el CR como **instancia**, con
otras instancias nombradas (BOM, anchura cero, control C0, multibyte partido, **blanco de más o
puesto en el sitio de otro**), con la frase de que la clase **no** se cierra con una lista de
caracteres (la reconstrucción de la clave) y con las **tres fronteras**: CR/LF **final** =
transporte, el **cuerpo** del REQ no se restringe, **reabrir** no se bloquea.

**Por qué era `usuario/dinero` y no instrumento:** el efecto sale del repositorio. Un proyecto
consumidor con su `AGENTS.md` congelado no se enteraba de que pudo cerrar un REQ `critico` sin
validación ni auditoría. Ningún guardián cubre estos tres archivos (no están en
`codigo_app.globs`), así que la disciplina es del agente y la evidencia tiene que ser esta.

**(b) El apartado de migración**, con lo que `CA-10` exige en su letra: la frase sin eufemismo
(«**pudiste cerrar un REQ `critico` sin validación de QA y sin auditoría de seguridad aprobada**»),
la pregunta **por estado** («cuáles de tus REQ en estado terminal NO cerrarían hoy») **antes** de
cualquier comando, y **cada** barrido por vía acompañado, **en el mismo sitio que el comando**, de
las tres declaraciones de `REQ-016 CA-09 (ii)`. **Lo que se dice de los comandos es lo cierto hoy:**
ninguno responde la pregunta por estado; el modo de `tools/arnes-lectura.sh` que la respondería
sigue en «Fuera de alcance» de `REQ-016` (`instrumento`, dueño `desarrollador`) y el apartado
«Hacia 1.32.1» lo anunciaba para 1.33.0, que se publicó **sin** él — se escribe que no llegó,
porque una guía que arrastra una promesa vencida manda a buscar un comando que no existe.

**Y las dos vías se midieron antes de escribirlas, no después** (control positivo con seis fixtures
en `/tmp`, cada uno con un REQ `Estado: completado`):

| fixture | qué lleva la clave | el barrido `awk` | `tools/arnes-lectura.sh` | la puerta |
|---|---|---|---|---|
| REQ-901 | **BOM** delante de `Sensible a seguridad` | lo ve | «cabecera no medible» | deniega |
| REQ-902 | **blanco duplicado** (`Sensible a  seguridad`) | lo ve | «cabecera no medible» | deniega |
| REQ-903 | **`QA-:`** — un guion **ASCII imprimible** | **NO lo ve** | «cabecera no medible» | **deniega** |
| REQ-904 | **tabulador** (`Hallazgos<TAB>abiertos`) | lo ve | «cabecera no medible» | deniega |
| REQ-905 | **CR interior** (`Seg`+CR+`uridad`) | lo ve | «cabecera no medible» | deniega |
| REQ-906 | `Módulo:` **legítimo** | lo ve (**falso positivo**) | no dice nada (rc sólo por los otros) | permite |

La fila **REQ-903** es la que justifica la declaración «este barrido no encuentra un byte ASCII
imprimible ajeno al alfabeto de las claves, que la puerta **sí** deniega», y la **REQ-906** la de
«su ruido no es un hallazgo». Comprobado además sobre los 27 REQ de este árbol: el barrido saca
**53 líneas** —27 de `Módulo:` y 26 de `Versión destino:`— y **todas** son legítimas (`cut`+`uniq -c`
sobre la salida). El comando publicado en la skill se ejecutó **tal como quedó escrito**,
extrayéndolo del propio archivo con `sed -n '/# Barrido POR VÍA/,/cat -v$/p' | sed 's/^  //'`: no es
un comando tecleado en la guía y nunca probado.

**Fail-before / pass-after de `CA-10`, por cláusulas y no por impresión.** Se comprobaron **26**
cláusulas con `grep -c -E` contra **las dos** versiones del texto —`git show v1.33.0:<archivo>` y el
árbol— en la misma corrida:

| | `v1.33.0` (fail-before) | árbol `710e3e7` + esta comisión (pass-after) |
|---|---|---|
| cláusulas satisfechas | **7 de 26** | **26 de 26** |

Las 7 que ya pasaban en la base son las que el apartado «Hacia 1.32.1» y la fila del CR ya traían
(la pregunta por estado, «no envejece con la vía siguiente», «no acredita ausencia de exposición»,
el sitio único del registro y «reabrir no se bloquea»): **19 cláusulas voltean**. Método
re-derivable sin preguntar — el script entero, para volver a correrlo:

```bash
modo="$1"                     # 'base' | 'arbol'
leer() { if [ "$modo" = base ]; then git show "v1.33.0:$1"; else cat "$1"; fi; }
n=0; ok=0
mira() { n=$((n+1)); local hay
  hay="$(leer "$2" 2>/dev/null | grep -c -E -- "$3" || true)"; hay="${hay:-0}"
  if [ "$hay" -gt 0 ]; then ok=$((ok+1)); printf '  SI  %s\n' "$1"; else printf '  NO  %s\n' "$1"; fi; }
for f in AGENTS.md templates/AGENTS.md.tpl; do
  mira "$f propiedad"   "$f" 'no puede medir\*\* no deja cerrar'
  mira "$f CR instancia" "$f" 'retorno de carro\*\* que no termina la línea es una \*\*instancia\*\*, no la definición'
  mira "$f otras instancias" "$f" 'BOM .`.xef.xbb.xbf`.*anchura cero.*control C0.*multibyte partido.*blanco de más'
  mira "$f no por lista"  "$f" 'La clase \*\*no\*\* se cierra con una lista de caracteres'
  mira "$f frontera 1"    "$f" 'CR/LF \*\*final\*\* es transporte \(CRLF decide igual que LF\)'
  mira "$f frontera 2"    "$f" 'el \*\*cuerpo\*\* del REQ no se restringe'
  mira "$f frontera 3"    "$f" 'reabrir\*\* no se bloquea'
done
SK=skills/arnes-upgrade/SKILL.md
mira "sin eufemismo"      "$SK" 'pudiste cerrar un REQ .critico. sin validación de QA y sin auditoría de seguridad aprobada'
mira "estado antes que comando" "$SK" 'SE DICE ANTES QUE NINGÚN COMANDO: la pregunta es de ESTADO, no de vía'
mira "forma de la pregunta"     "$SK" 'cuáles de tus REQ en estado[ ]*terminal NO cerrarían hoy|estado$'
mira "no envejece"        "$SK" 'no envejece con la vía siguiente'
mira "cierto hoy"         "$SK" 'Ningún comando de este apartado la responde todavía, y el que se anunció no llegó'
mira "dueño del instrumento" "$SK" 'con dueño .desarrollador'
mira "una vía, no la propiedad" "$SK" 'Este comando interroga una VÍA, no la propiedad'
mira "silencio no acredita"     "$SK" 'no hallar nada NO acredita ausencia de exposición'
mira "vías que NO encuentra"    "$SK" 'byte ASCII imprimible ajeno al alfabeto de las claves, que la puerta SÍ deniega'
mira "sitio único"        "$SK" 'el sitio único donde viven es'
mira "pertenencia por historial" "$SK" "grep -c '_arnes_clave_oculta'"
mira "tres fronteras"     "$SK" 'que son las tres fronteras'
printf '%s: %d de %d\n' "$modo" "$ok" "$n"
```

**Y la pertenencia de versiones no se escribió a mano:** se derivó del historial del lector, tag a
tag, con el comando que la propia skill publica —`git show <tag>:hooks/lib.sh | grep -c
'_arnes_clave_oculta'`— sobre `v1.23.0`, `v1.28.0`, `v1.30.3`, `v1.31.0`, `v1.32.0`, `v1.32.1` y
`v1.33.0`: **0 en todas** (afectadas). `arnes_norm_clave` nace en `v1.31.0` (0 antes, 3 desde),
dato que se publica para que nadie confunda «el lector cambió» con «la guarda existe».

### 12.2 `CA-09 (iii)` — la prueba re-apuntada a la magnitud que el contrato VIGENTE pide (`QA-023-12`)

**El defecto que se corrige, dicho como lo dijo QA:** el caso de
`tests/escenarios/hooks/secciones/39-caracter-invisible-4-el-coste.sh` seguía midiendo el
**cociente absoluto** de duplicación contra un **techo absoluto de 2,2** — la magnitud y el techo
que el write-back del 2026-09-09 **retiró** (§11.5: la base `v1.33.0` paga 2,492 y 2,446 sobre la
misma forma, así que el techo absoluto lo incumple igual el código que este REQ no escribió).

**Qué mide ahora, con los CUATRO parámetros publicados en la misma línea del veredicto:** la
**relación emparejada** `cociente(candidata) / cociente(base)`, calculada **dentro de cada toma**,
con las dos versiones **intercaladas al nivel de `n`** —para cada longitud se miden base y
candidata **seguidas**, así que una deriva de carga entre el bloque de 1000 y el de 2000 cae sobre
las dos por igual— y con el **orden alternado por toma y publicado** (`base,cand` en las impares,
`cand,base` en las pares). Techo **1,000×**. Parámetros: **par 1000→2000 bytes**, **`k` = 200**,
**forma «clave»** (el esqueleto `Sensible a @ seguridad: no`, `@` = `a`, que ya vivía en un solo
sitio del caso) y **línea base `v1.33.0`** — el cuarto, nuevo.

**Lo que se conservó, y no por inercia:** los cocientes **absolutos** siguen publicándose, ahora
etiquetados **«CONTROL no acreditativo»** con sus microsegundos, porque sin ellos nadie puede
rederivar la relación; el estadístico sigue siendo el **mínimo de k** de la sonda y la **mediana**
de las tomas; el suelo de ruido de **50 ms** por serie; la regla de **no menos de tres tomas** y la
**abstención cuando la dispersión alcanza al margen** — con la corrección que el criterio exige:
**la dispersión que decide es la de la serie contratada (la RELACIÓN)**, no la de los cocientes
absolutos.

**Dos precondiciones nuevas que abstienen en vez de mentir:** (1) si la línea base **no se
materializa**, SKIP con el registro del materializador —el cociente absoluto **no** la sustituye—;
(2) si el tag **no define** el sujeto, SKIP diciendo eso. Sin (2) un sujeto ausente en la base se
cronometraría como «orden no encontrada», caería bajo el suelo y el caso abstendría **citando el
suelo en vez del motivo verdadero**.

**Lo que NO se tocó:** el techo (**1,000×**, y el 1,25× de (ii)), `k`, el par, la forma, el número
mínimo de tomas y el suelo de ruido. **Ninguno se movió a la baja ni al alza**, y `hooks/` no se
tocó en absoluto (ni `run.sh`, ni `lib.sh`, ni `guard-completado.sh`, ni `tools/arnes-lectura.sh`:
son el `Archivos:` de `REQ-024`, que entra detrás).

### 12.3 Lo que la prueba re-apuntada MIDIÓ — cinco corridas, con dispersión y sin promedios

**Diez veredictos de (iii) en cinco corridas: 1 PASS, 9 SKIP, 0 FAIL.** Todas las medianas de la
relación quedan **en 1,000× o por debajo**; ninguna corrida produjo un rojo.

| corrida | sujeto | mediana | rango | dispersión | margen | veredicto |
|---|---|---|---|---|---|---|
| aislada 1 | `arnes_norm_clave` | **0,883** | 0,881 – 0,993 | 0,112 | 0,117 | **PASS** |
| aislada 1 | `arnes_campo_linea` | 0,960 | 0,920 – 0,973 | 0,053 | 0,040 | SKIP |
| aislada 2 | `arnes_norm_clave` | 0,929 | 0,905 – 0,987 | 0,082 | 0,071 | SKIP |
| aislada 2 | `arnes_campo_linea` | 0,983 | 0,897 – 0,986 | 0,089 | 0,017 | SKIP |
| aislada 3 | `arnes_norm_clave` | 0,896 | 0,827 – 0,931 | 0,104 | 0,104 | SKIP |
| aislada 3 | `arnes_campo_linea` | 0,950 | 0,922 – 1,011 | 0,089 | 0,050 | SKIP |
| banco entero 1 | `arnes_norm_clave` | 0,976 | 0,878 – 1,767 | 0,889 | 0,024 | SKIP |
| banco entero 1 | `arnes_campo_linea` | 0,916 | 0,901 – 1,042 | 0,141 | 0,084 | SKIP |
| banco entero 2 | `arnes_norm_clave` | **1,000** | 0,931 – 1,507 | 0,576 | 0,000 | SKIP |
| banco entero 2 | `arnes_campo_linea` | 0,911 | 0,908 – 1,084 | 0,176 | 0,089 | SKIP |

**Cómo leer las dos mitades de la tabla, porque no son la misma medición.** En el **banco entero**
el corredor ejecuta las 57 secciones **a la vez**, así que la sonda mide bajo contención masiva: la
`t1` de la corrida 1 da una relación de **1,767** con una base de **1,505** —un cociente de base
absurdamente bajo, es decir carga cayendo sobre el término corto de la base— y eso infla la
dispersión hasta 0,889. Las corridas **aisladas** son la aproximación más limpia disponible en esta
máquina, y ahí la dispersión baja a **0,053–0,112**. **La lección de las nueve corridas anteriores
se aplica también aquí: una abstención repetida NO es un verde acumulado.** Lo único que estas diez
observaciones acreditan es que **el caso no produce FAIL** y que la relación se mueve por debajo del
techo; **quién afirma el techo es el CI**, y esta comisión **no lo corrió** (§12.5).

**El coste en reloj:** la sección `39/4` pasa de 2 a **4** mediciones por toma y sujeto (24
invocaciones de la sonda contra 12), y tarda **≈ 39 s** aislada. El banco entero tarda **1 m 17 s**
y **1 m 15 s** en las dos corridas, contra los ~80 s de la ventana: el intercalado no cambia el
reloj del banco de forma apreciable porque la sección corre en paralelo con las demás.

### 12.4 Puertas de esta comisión (cifras propias, con la lista de SKIP)

```
for f in hooks/*.sh tools/*.sh; do bash -n "$f" || exit 1; done                # rc 0
jq -e . hooks/hooks.json >/dev/null                                            # rc 0
jq -e . .claude-plugin/plugin.json && jq -e . .claude-plugin/marketplace.json  # rc 0
bash -n  sobre run.sh, autoprueba-corredor.sh y las 57 secciones               # rc 0
tests/escenarios/hooks/autoprueba-corredor.sh          # 106 PASS · 0 FAIL  (incluye CA-18)
tests/escenarios/hooks/run.sh                          # x2: 960 PASS · 0 FAIL · 6 SKIP · cuadre 966
```

**Los 6 SKIP, elemento por elemento** (los mismos en las dos corridas; ninguno nuevo respecto de la
ventana, y los dos de `CA-09 (iii)` ya abstenían antes de este cambio — lo que cambia es **qué**
magnitud abstienen):

1. `ruta estilo Windows con backslashes -> deny` — sin `cygpath`: caso sólo de Windows.
2. `REQ-017 CA-05 (i)` — no se pide; evidencia acreditada en el Historial de REQ-017 (se repite con `ARNES_COSTE_RUTA_CRITICA=1`).
3. `REQ-017 CA-05 (ii)` — igual que el anterior.
4. `REQ-021 CA-08 (iii)` — la mitad en **procesos** no es medible en ese instrumento (publica `procesos=no-aplica`).
5. `REQ-023 CA-09 (iii)` `arnes_norm_clave` — dispersión ≥ margen (cifras en §12.3).
6. `REQ-023 CA-09 (iii)` `arnes_campo_linea` — dispersión ≥ margen (cifras en §12.3).

**`REQ-014 CA-18`:** el archivo de la sección pasa de 327 a **388** líneas y su
`PISO_AUTONOMO_SECCION` de 267 a **330**, derivado término a término (31 preámbulo + 78 maquinaria
compartida duplicada + 221 bloque indivisible mayor). **El piso no se sube para comprar techo:**
388 ya cabía bajo el techo **anterior** de 400, y la autoprueba comprueba las tres cosas —que todos
los archivos lo declaran, que los términos **suman** y que `piso ≤ líneas`— en sus 106 PASS.
`CASOS_ESPERADOS_SECCION` sigue en **4** y `CASOS_ESPERADOS` en **966**: no se creó ni se perdió
ningún caso, que es exactamente lo que ese literal existe para comprobar.

### 12.5 Limitaciones materiales de esta medición (además de las de §8 y §11.8)

- **El CI no se corrió, y es el juez.** Esta comisión no comitea ni empuja (instrucción explícita),
  así que `hooks-en-linux` **no se ejecutó** sobre este árbol. Lo que sé del rojo anterior es que
  venía del **techo absoluto** (mediana 2,365× > 2,200×), y esa magnitud **ya no la mide el caso**;
  lo que **no** puedo afirmar es qué veredicto —PASS o SKIP— emitirá el CI, porque eso lo decide la
  **dispersión** de su máquina contra el margen. **FAIL exigiría que la mediana de la relación
  supere 1,000× con margen mayor que la dispersión**, y las diez observaciones de §12.3 quedan en
  1,000× o por debajo.
- **`CA-10` no tiene caso de banco, y no por olvido.** Añadir casos obliga a mover
  `CASOS_ESPERADOS` en `tests/escenarios/hooks/run.sh`, que está en el `Archivos:` de **`REQ-024`**
  (`tools/arnes-paralelo.sh REQ-023 REQ-024` responde `colisiona hooks/lib.sh`). El par
  fail-before/pass-after de §12.1 se toma con `grep` contra el tag, **no** con el banco: es
  evidencia de esta comisión, no un guardián permanente. El precedente de que sí se puede
  automatizar existe —`36-cabecera-comentario-html-4-el-informe-y-los-textos.sh` hace exactamente
  esto para `REQ-016 CA-09/CA-10`—, así que es trabajo enrutable, no un imposible. **Residual
  `instrumento`, dueño a decidir por la coordinadora, junto con `REQ-024`.**
- **Un host, y sucio.** Las diez observaciones salen de la misma máquina, con `arnes_norm_clave`
  abstenido incluso en el CI de la corrida anterior (dispersión 1,088×): ese sujeto **no** está
  resuelto por ninguna vía, y esta comisión no lo resuelve.
- **La forma sigue siendo una sola** («clave»). Nada se remidió sobre «título» ni «valor».
- **Defecto distinto, anotado y NO arreglado (§14.B.4):** en la misma sección, `REGHER94` se declara
  vacío en la línea 111 y **nunca se asigna**, así que los SKIP de `CA-09 (i)` y `(ii)` que citan
  «no hay línea base» publican un paréntesis **vacío** en vez del registro del materializador (que
  vive en `MAT94_REG`). Es `instrumento`, preexistente a esta comisión, y no se toca porque el
  alcance está cerrado: un SKIP que no publica su motivo es exactamente lo que `REQ-020` existe para
  cazar, así que **queda nombrado con dueño `desarrollador`**.
- **`docs/ESTADO.md` aparece modificado y no lo escribí yo:** es el bloque entre marcadores que el
  hook de parada **deriva** en cada parada de agente (`19:26` → `19:31`, `86389ac` → `710e3e7`,
  cola 4 → 3). Se deja como está para que la coordinadora consolide.
