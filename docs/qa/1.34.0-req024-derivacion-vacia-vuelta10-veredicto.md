# QA — REQ-024 · candidato de la DERIVACIÓN VACÍA · vuelta 10 del bucle `dev↔QA`

**Árbol juzgado:** worktree `/home/juan/dev/ArnesJuan-1.34-reparaciones`, rama
`feat/1.34-cierre-alcance` @ **`d40dcb5`** (árbol limpio antes y después de esta comisión).
**Delta juzgado:** `5d1b1d1..d40dcb5`, un commit.
**Fecha:** 2026-09-12. **Modelo:** Opus 5. **Vuelta:** **10** de un presupuesto de **10**
(`dev↔QA`); el bucle `analista↔QA` sigue en **5** de **5**. Ninguna se renombra.

---

## 0. Qué acredita esta firma, y qué no

**Acredita:** que la **reparación del instrumento** del delta `5d1b1d1..d40dcb5` hace lo que dice
hacer — distingue DETECTAR de COMPROBAR, declara lo que no pudo comprobar citando sede y motivo, y
no se satisface callando (probado con dos mutaciones, § 4).

**NO acredita:** el cumplimiento de `CA-14`, que sigue **parcialmente sin acreditar**; ni el cierre
de `REQ-024`; ni ningún hallazgo previo; ni rendimiento. Un instrumento que declara honestamente su
carencia **no convierte la carencia en cobertura**. Estas dos cosas se juzgan por separado y aquí
salen con signos distintos.

**Veredicto:** `QA: con-hallazgos` — **un hallazgo nuevo de clase `contrato`** (`QA-024-42`), que
está en el **texto del REQ**, no en el instrumento.

---

## 1. Reutilización de evidencia — comprobada, no asumida

`git diff 0440cfd d40dcb5 -- hooks tools .github` sale **VACÍO** (verificado en esta comisión). El
mecanismo no cambió en todo el delta, así que **no** se repitieron: las 56 celdas del disparador,
las 41 formas, las 48 de fidelidad, las 72 de no-regresión ni las 42 del candidato de `D12`. Se
citan y se reutilizan.

Lo que cambia y se midió aquí: **un instrumento del banco** (sección `46`, partida en dos) y **un
bloque de criterio** (`CA-14 (ii)`, bloque `[PRECISIÓN, 2026-09-12]`, sin criterio nuevo).

---

## 2. Quality gates de `AGENTS.md` §7 — 4 de 4 en verde

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | PASS |
| `jq -e . hooks/hooks.json` | PASS |
| `jq -e . .claude-plugin/plugin.json` · `marketplace.json` | PASS |
| `bash -n` sobre los dos archivos nuevos de la `46` | PASS |

## 3. Banco completo — mi corrida, con su variabilidad conservada

**1279 PASS · 0 FAIL · 11 SKIP**, `rc=0`. **Cuadre exacto: 1279 + 11 = 1290** contra
`CASOS_ESPERADOS=1290`.
Evidencia:
`/tmp/claude-1000/-home-juan-dev-ArnesJuan/23a8ee5a-8587-4923-8f15-1fc9bf03aaf2/scratchpad/banco-completo.txt`

**Difiere de lo declarado (`1280 · 0 · 10`) en un caso, y NO se re-corrió para hacerlo coincidir.**
El caso que basculó es **`REQ-017 CA-08 (ii)`** («una cabecera de 200 líneas: el reloj no sube más
de 1,25× el de v1.32.1»), que abstuvo con el motivo *«el techo cae DENTRO del recorrido observado
[1.169×, 1.280×]: el instrumento no distingue el factor que vigila»*. Tres hechos lo sitúan:

1. Es un **oscilador documentado por nombre** en `CHANGELOG.md:3556` — *«saltó en su primera corrida
   y pasó en las tres siguientes — el oscilador documentado, y la razón por la que el recuento se lee
   **por lista y no por total**»*.
2. Vive en un archivo con **cero cambios** en este delta (el delta toca `CHANGELOG.md`,
   `docs/arnes/coste-de-comision/00-metodo-y-base.md`, `requirements/REQ-024.md`, `run.sh` y los dos
   archivos de la `46`).
3. La dirección es **`PASS → SKIP`**, nunca hacia verde. Ningún resultado bloqueante pasó a verde.

### TRES corridas, TRES resultados distintos — y una de ellas con `FAIL`

Tras editar el REQ se corrió el banco otras dos veces (la sección `40/5` lee el `REQ-024` real, así
que había que comprobar que mis ediciones no lo movían). El resultado obliga a declarar **flaky** un
caso:

| Corrida | Resultado | Cuadre |
|---|---|---|
| 1 (antes de mis ediciones) | **1279 PASS · 0 FAIL · 11 SKIP** | 1290 |
| 2 (después) | **1279 PASS · 1 FAIL · 10 SKIP** | 1290 |
| 3 (después) | **1280 PASS · 0 FAIL · 10 SKIP** | 1290 |

**El caso que oscila es `REQ-017 CA-03`** («el escáner no crece más que linealmente»), identificado
por diferencia de las listas de SKIP publicadas: es **el único** elemento que separa la lista de la
corrida 1 (11) de la de la corrida 3 (10), y en la corrida 2 —misma lista de 10 SKIP más un FAIL—
sólo puede ser él. **Osciló `SKIP → FAIL → PASS` sobre el mismo árbol.**

**No es atribuible a mis ediciones**: `REQ-017 CA-03` es una sonda de coste del escáner y no lee
`requirements/REQ-024.md`; lo único que toqué son tres líneas de ese archivo.

**Se conserva el `FAIL` y no se usa la corrida 3 para desmentirlo.** Repetir hasta el verde no
demuestra que el rojo fuera falso. Se registra en la dirección contraria: la corrida 3 coincide
exactamente con lo que declaró el desarrollador (`1280 · 0 · 10`), y eso **no** convierte su cifra en
reproducible — la convierte en **una de tres**.

**Por qué importa, y no es cosmético:** `hooks-en-linux` es la puerta **requerida y estricta** de
`main`. Un `SKIP` no la rompe; un **`FAIL` sí**. Este caso puede ponerla roja sin que nada haya
cambiado, y el hábito que eso crea —re-correr hasta el verde— es exactamente el modo de fallo que
`AGENTS.md` §13 describe.

**NO se abre hallazgo nuevo**, por el mismo motivo por el que no lo abrió la vuelta 6: *«la
investigación de ese instrumento está PROHIBIDA por el propietario»*. Se deja **escrito y medido**,
que es lo único que queda disponible. Consecuencia para esta firma: **«banco entero en verde» no es
reproducible en esta máquina y NO se acredita como tal**; lo que sí se acredita es el **cuadre**, que
salió **1290 exacto en las tres corridas**, y que **ninguna de las tres movió un solo caso de la
sección 46**.

### El reparto de los SKIP, y el «tercero» que se buscaba

El baseline correcto **no es 7 sino 8**, y está en disco:
`docs/qa/1.34.0-porte-1.33.1-falsacion/banco-corrida-unica-guarda.txt` lista **8** preexistentes
(Windows-backslashes · `REQ-017 CA-03` · `REQ-017 CA-05 (i)` · `REQ-017 CA-05 (ii)` ·
`REQ-021 CA-08 (iii)` · `REQ-023 CA-12` · `REQ-023 CA-09 (iii)` ×2).

- **Corrida del desarrollador: 10 = 8 preexistentes + 2 nuevos** (`46/11` × 2). **No hay ningún
  tercer SKIP sin explicar**: el «7» de partida es una lista más antigua, y el recuento se lee por
  lista y no por total, que es justamente la advertencia del CHANGELOG.
- **Mi corrida: 11 = 9 + 2**, donde el noveno preexistente es el oscilador `REQ-017 CA-08 (ii)`.

**Los 2 SKIP de la sección nueva son el hallazgo dicho en voz alta**, no una avería: `46/11` sobre
`AGENTS.md` y sobre `templates/AGENTS.md.tpl`, cada uno nombrando **las 2 de 4 sedes** detectadas y
no interpretadas, con su motivo íntegro.

---

## 4. Falsación del instrumento — el control EJERCIDO en las dos direcciones

No basta leer que el par `46/12`/`46/13` discrimina; se mutó el instrumento y se midió. Las dos
mutaciones se aplicaron sobre el archivo real y **se revirtieron por copia** (md5 verificado idéntico
antes y después: `cb7ecd49900cbe084ef2f197c4ae5ba7`; árbol limpio).

| Mutación | `46/11` | `46/12` (regresión) | `46/13` (control) |
|---|---|---|---|
| **M1 — instrumento CIEGO** (`interp46` devuelve vacío: toda sede muda) | SKIP 4/4 | **PASS** | **FAIL** — *«una afirmación VÁLIDA subió las MUDAS (4 → 5)»* |
| **M2 — instrumento CRÉDULO** (`interp46` devuelve siempre una combinación) | PASS 4/4 | **FAIL** — *«la frase falsa subió las INTERPRETADAS (4 → 5)»* | PASS |
| **Sin mutar** | SKIP 2/4 con motivo | PASS | PASS |

**Conclusión medida:** la afirmación del desarrollador —*«sin él, un instrumento que declarase muda
a toda sede pasaría en verde»*— es **exacta**: bajo M1, `46/12` pasa en verde y **sólo `46/13` lo
caza**. El par cubre las dos formas de mentir (callar y afirmar de más). La reparación **no se puede
satisfacer callando**.

### Fail-closed del suelo, comprobado aparte

El suelo de 12 celdas distintas por eje está **exactamente en el límite** (12 y 12, § 5), así que se
probó qué pasa al perder una sede. Con una raíz mutilada vía `ARNES_DOCS_RAIZ` (quitada la fila del
CIERRE de los dos documentos): el eje `ausencia` cae a **3 celdas distintas** y emite **SKIP**
—*«el instrumento no alcanza a acreditar el eje, y un verde aquí sería por vacío»*—, no PASS.
**Abstiene hacia el lado seguro.**

---

## 5. Las cinco propiedades del propietario, una por una

| # | Propiedad | Veredicto | Evidencia medida |
|---|---|---|---|
| 1 | Distinguir **detectadas / interpretadas / contrastadas** | **CUMPLE** | `46/1`–`46/4` publican los tres en una línea: *«DETECTADAS 4 · INTERPRETADAS aquí 2 · CONTRASTADAS 12 celdas con el hook real · 2 MUDA(s)…»*. `interp46` es la función que separa los estados. |
| 2 | **No presentar como comprobada** una afirmación que se omitió | **CUMPLE** | El `continue` mudo desapareció: `46/6` publica `<f>f/<c>c` —`[3f/24c] [1f/0c] [2f/15c] [1f/0c]`— y `0c` es sede muda. Antes decía `[1]` de una muda igual que de una contrastada. |
| 3 | **Identificar sede y motivo** cuando no pueda interpretarla | **CUMPLE** | `46/11` cita las dos sedes por su texto y adjunta `motivo46`, que dice sólo lo que la máquina sabe —por qué entró y qué le faltó— y **no diagnostica la causa**, que exigiría el analizador del español que `CA-14` prohíbe. |
| 4 | **Evitar que otras frases contrastadas oculten** esa carencia | **CUMPLE** | El recuento de mudas va **en la misma línea** que las celdas verdes (`cola` en `empareja46`), no en una nota aparte. Era el defecto exacto: doce celdas verdes tapaban una sede muda. |
| 5 | **Conservar las comparaciones existentes que sí son válidas** | **CUMPLE, y la cifra del desarrollador se reproduce** | Ver abajo. |

### Verificación independiente de la propiedad (5)

El desarrollador declara haber descartado derivar **por oración** porque perdía **18 de 24 celdas**.
Se extrajo la maquinaria (`barre46`, `oraciones46`, `deriva46`, `interp46`…) a un banco aparte y se
midió sobre `AGENTS.md`:

- Derivando **por sede entera** (lo implementado): **24 celdas distintas** = **12 valor + 12
  ausencia**.
- Derivando **por oración** con el partidor real `oraciones46 … consec`: **6 celdas distintas** (6
  ausencia, **0 valor**).

**Se pierden 18 de 24. La cifra del desarrollador es exacta**, y la alternativa habría hundido los
dos ejes por debajo del suelo de 12. Descartarla era correcto. Las comparaciones válidas se
conservan: `46/1`–`46/4` siguen en PASS y `46/5` acredita **12 y 12 celdas distintas**.

## 6. Los dos límites del propietario

| Límite | Veredicto | Evidencia |
|---|---|---|
| **No ensanchar el reconocedor** ni acumular expresiones para esa frase | **RESPETADO** | `git diff` de la sección: los patrones de `barre46` y `oraciones46` son **idénticos** a los de `5d1b1d1`. Lo que se añadió es `interp46`/`motivo46`, que preguntan por el **estado** «derivación vacía». La prueba conductual es `46/12`: la frase falsa **sigue sin interpretarse** (interpretadas 2 → 2) y lo que cambia es que ahora **se declara** (mudas 2 → 3). Si se hubiera ensanchado el reconocedor, las interpretadas habrían subido y `46/12` habría dado FAIL — que es exactamente lo que M2 demuestra. |
| **El caso de Codex no puede acabar en acreditación completa** | **RESPETADO** | La sede inyectada **no se acredita**: sale con `SKIP`, citada, bajo la política de inconclusos. `46/12` PASS acredita *«se declara no comprobada en vez de pasar en verde»* — acredita **la declaración**, no la afirmación. |

### Comprobación textual que pidió el propietario

1. **«La sede añadida por Codex aparece identificada como no comprobada»** — **SÍ**. `46/12`:
   *«detectadas 4→5, interpretadas 2 sin moverse, mudas 2→3, y la sede sale CITADA»*.
2. **«Las comparaciones válidas se conservan»** — **SÍ**. 24 celdas distintas, 12 por eje,
   `46/1`–`46/5` en PASS, y el control `46/13` cuadra sus 6 celdas con el hook real.
3. **«Ningún resumen convierte esos `SKIP` en cobertura completa»** — **SÍ en el instrumento y en el
   CHANGELOG; NO en el REQ.** El corredor los recapitula con motivo íntegro bajo *«ninguna causa
   común: cada uno con su motivo»*; el CHANGELOG les dedica la sección *«Lo que el candidato NO
   acredita, y ahora se ve»*. **Pero el punto 8 del REQ sí los convierte en cobertura** — es
   `QA-024-42`, abajo.

**Sobre el total idéntico:** no se usa contra la reparación. `19 · 0 · 2` en originales y en mutación
es consistente **porque el detalle distingue**: en la mutación el recuento de mudas sube 2→3 y la
sede sale citada. Se miró el detalle, no el total.

---

## 7. HALLAZGO NUEVO

### `QA-024-42` (**contrato**) — el punto 8 del REQ acredita con dos casos que no comprueban nada sobre esas sedes

**Sede:** `requirements/REQ-024.md:2098` (§ «Qué queda SIN VERIFICAR», punto 8) y, transcrito,
`requirements/REQ-024.md:2323` (fila del Historial, *«(i)–(v) sostenidos sólo por `46/7` y `46/8`»*).
Barrido por propiedad sobre todo el árbol —`.md`, `.sh`, `.json`— : **dos sedes, ambas documentales.
El programa NO la imprime.**

**Qué afirma el REQ:**

> «de esas dos sedes **ninguna celda** de la rejilla está emparejada con el hook, así que **(i)–(v)**
> de `CA-14` están **acreditados** sobre ellas **sólo** por los casos que no dependen de la
> derivación —`46/7` (la condición dentro de la oración) y `46/8` (la gemela byte a byte)—»

**Por qué es falso, medido:**

**(a) `46/7` examina CERO oraciones en las dos sedes mudas.** Ejecutado `oraciones46 <sede> neces`
sobre cada una:

| Sede muda | oraciones clase `consec` | oraciones clase `neces` (**lo que `46/7` examina**) |
|---|---|---|
| «Seguridad no firma lo que QA no ha validado…» | 1 | **0** |
| «Anti-deriva — el techo honesto…» | 1 | **0** |

Su PASS sobre esas sedes es **cierto por vacío**: pasa porque el reconocedor de necesidad no
encuentra nada que examinar, no porque haya comprobado algo. Es la familia exacta que el propio
párrafo de anti-vacuidad de `CA-14` persigue y que `REQ-020` existe para cazar.

**(b) `46/8` acredita `(vi)`, no `(i)–(v)`.** Comprueba que cada sede es idéntica byte a byte a su
gemela. **Dos sedes mudas idénticas byte a byte siguen siendo dos sedes mudas**: la gemelidad no
acredita contenido, y atribuirle `(i)–(v)` es un error de atribución.

**(c) `(i)`, `(iii)` y `(iv)` no los toca ningún caso sobre esas sedes.** `(i)` (separar los dos
juicios), `(iii)` (el eje de la ausencia por clave) y `(iv)` (el aviso no decide) dependen todos de
la derivación, que sobre estas sedes sale vacía.

**Reproducción:**
```
# desde el worktree, con la maquinaria de la sección 46 extraída
oraciones46 "<sede 'Seguridad no firma…'>" neces | grep -c .   # -> 0
oraciones46 "<sede 'Anti-deriva…'>"        neces | grep -c .   # -> 0
```

**Esperado:** que el REQ diga que `46/7` y `46/8` acreditan **sus comprobaciones concretas** y que
**no sustituyen el emparejamiento ausente**; que `(i)–(v)` quedan **sin acreditar** sobre esas dos
sedes.
**Observado:** el REQ los declara **acreditados**.

**Severidad: alta. Clase: `contrato`** — el REQ afirma algo falso sobre lo construido
(`AGENTS.md` §9). **Bloquea hasta el write-back**, que es del `analista-requerimientos`; el
`qa-tester` no reescribe el criterio.

**Observación que importa para la causa raíz:** el punto 8 comete, **en el texto que documenta la
reparación de la derivación vacía**, la misma forma de defecto que la reparación corrige —presentar
como acreditado lo que no se comprobó—. El instrumento quedó bien; la prosa que lo describe recayó.
Corregir la frase señalada no basta: hay **dos** sedes (`:2098` y `:2323`) y se corrigen juntas.

---

## 8. Limitaciones que CONFIRMO y que NO se resuelven aquí

1. **La cuarta sede («Anti-deriva — el techo honesto») y la del orden de las firmas siguen sin
   emparejar.** De las dos, **ninguna celda** está contrastada con el hook. Confirmado por medición
   propia: 4 sedes detectadas, **2 interpretadas, 2 mudas**, y así en los dos documentos. Esto es
   **cobertura faltante de `CA-14`**, no un defecto del instrumento — el instrumento la declara
   correctamente. **No se amplía el reconocedor ni se resuelven automáticamente**: va como decisión
   conjunta antes de declarar satisfecho `CA-14` o lista 1.34.0. La evaluación editorial de «puede
   impedir» sobre `AGENTS.md` es del propietario, y `AGENTS.md` **no se tocó**.
2. **La mitad 2 de la sección queda exactamente en el techo de `CA-18`: 400 líneas contra un techo
   de 400.** Verificado (`wc -l`): 372 y **400**. Conforme hoy; **la próxima línea obliga a
   repartir**. La mitad 1 tiene margen (372/400).
3. **El suelo de 12 celdas distintas se cumple exactamente, sin margen** (12 y 12). Es conforme —el
   suelo es operativo y hacia arriba— y el fail-closed está probado (§ 4), pero conviene saber que
   perder una sola sede interpretada tumba un eje a SKIP.
4. **Rendimiento NO acreditado**: hay otra cuenta del propietario en esta máquina. Los SKIP de
   `REQ-017`, `REQ-021` y `REQ-023 CA-09 (iii)` son abstenciones por ruido de reloj, no medidas.
   **No concluyente**, no «cumple».
5. **CI no ejecutado** en esta comisión: la puerta requerida `hooks-en-linux` sobre `d40dcb5` no
   forma parte de esta evidencia.

---

## 9. Contadores y parada

- `dev↔QA`: **vuelta 10 de 10 — AGOTADO**.
- `analista↔QA`: **5 de 5 — AGOTADO**. `QA-024-42` es write-back del analista, y no queda vuelta.

**No se abre una vuelta 11.** Los impedimentos se presentan juntos y el pipeline para, conforme al
encargo. `Estado:` sigue **`bloqueado`**; `Seguridad:` sigue **`pendiente`**; `Hallazgos abiertos:`
gana **`QA-024-42` (contrato)** y ningún hallazgo previo se cierra ni se reclasifica.

**Lo que hace falta para avanzar es una decisión del propietario**, no otra vuelta:

| # | Decisión pendiente | Por qué es suya |
|---|---|---|
| A | **Autorizar una vuelta de write-back fuera del contador** para `QA-024-42`, o aceptarlo como residual declarado | Los dos contadores están agotados; sacar una vuelta del contador es excepción expresa suya (`AGENTS.md` §6) |
| B | **Cómo evaluar «puede impedir»** en `AGENTS.md` y qué hacer con la sede del orden de las firmas | Decisión **editorial** sobre `AGENTS.md`, con gate humano |
| C | Si `CA-14` puede declararse satisfecho con **2 de 4 sedes** sin emparejar | Es alcance, y condiciona la ventana 1.34.0 |
| D | `D14` (la alternativa al bloqueo), que sigue sin resolver | Ya escalada |
