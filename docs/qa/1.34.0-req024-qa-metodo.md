# REQ-024 — método y evidencia primaria de la validación de QA (vuelta 1)

**Versión base medida:** `d54761e` (rama `rel/registro-1.33.0`).
**HEAD avanzó a `3e49b98`** durante la validación (comisión de `REQ-017`, ajena): verificado que
**toda la superficie de `REQ-024` es byte a byte idéntica** entre las dos —
`git diff d54761e 3e49b98 -- hooks/ tools/ .arnes/config.json templates/ AGENTS.md
docs/decisions/ skills/arnes-upgrade/SKILL.md tests/…/run.sh tests/…/31-* tests/…/40-*` → **sin
salida**—, y que el **árbol de trabajo** sobre el que corrí el banco también lo es
(`git diff d54761e -- <misma lista> requirements/REQ-016.md tests/…/inventario.sh
tests/…/autoprueba-corredor.sh` → **0 líneas**). Fecha: **2026-09-09**.

**Límite de método, dicho primero porque acota todo lo demás:** el banco se corrió sobre el
**árbol de trabajo**, no en un `worktree` congelado, y otra comisión estaba **escribiendo
archivos** durante la ventana (`docs/arnes/…`, `tests/…/37-*`, `requirements/REQ-017.md`). Eso no
toca la superficie de `REQ-024` —verificado arriba— pero **sí** mueve el denominador de un caso de
`REQ-023` que deriva su corpus de los archivos del árbol (`CA-04`: 5662 → 5671 líneas con campo
entre mis dos corridas). Se dice para que nadie lea ese movimiento como volatilidad del
instrumento.

**Máquina:** WSL2, Linux 6.18.33.2. **El juez de cualquier umbral de reloj es el CI**
(`hooks-en-linux`), no esta máquina: ninguna cifra de reloj de aquí acredita un techo.

---

## 1. Señales automáticas, reproducidas

### 1.1 Las tres quality gates de `AGENTS.md` §7

```
for f in hooks/*.sh tools/*.sh; do bash -n "$f" || exit 1; done   # OK
jq -e . hooks/hooks.json >/dev/null                               # OK
jq -e . .claude-plugin/plugin.json && jq -e . .claude-plugin/marketplace.json  # OK
```

**Las tres en verde.**

### 1.2 Banco completo — DOS corridas, con `loadavg` y reloj

| Corrida | Resultado | Cuadre | rc | Reloj | `loadavg` antes → después |
|---|---|---|---|---|---|
| 1 | **1004 PASS · 0 FAIL · 8 SKIP** | 1012 (1004+0+8) | 0 | **63,2 s** | 0,19 → 2,45 |
| 2 | **1004 PASS · 0 FAIL · 8 SKIP** | 1012 | 0 | **72,3 s** | 4,04 → 3,93 |

Sin `ABORT`. **Autoprueba del corredor: 106 PASS · 0 FAIL**, rc 0.

**El desvío con la cifra del `desarrollador` (1005 · 0 · 7) es de UN caso y está reconciliado, no
es una regresión.** El caso es `REQ-017 CA-08 (ii)` («una cabecera de 200 líneas»), que en mis dos
corridas sale **SKIP** con el motivo «el techo cae DENTRO del recorrido observado [1.155×,
1.295×]: el instrumento no distingue el factor que vigila». El propio `desarrollador` lo declaró
como el caso que oscila PASS↔SKIP con la carga del host
(`docs/qa/1.34.0-req024-ca06-metodo.md:196-198`). Aritmética: 1005 − 1 = 1004 PASS y 7 + 1 = 8 SKIP.
**Es flaky por diseño declarado** —su veredicto lo decide el ruido de la máquina—, no lo trato ni
como pase ni como fallo, y es de `REQ-017`, no de este REQ.

Los 8 SKIP, uno por uno, con su motivo propio: ruta Windows (sin `cygpath`), `REQ-017 CA-05` (i) y
(ii) (evidencia acreditada en su Historial, se repite con `ARNES_COSTE_RUTA_CRITICA=1`),
`REQ-017 CA-08 (ii)`, `REQ-021 CA-08 (iii)`, `REQ-023 CA-12` (precondición de atribuibilidad no
cumplida: el lector cambió a propósito por `REQ-024 CA-08/CA-09`) y `REQ-023 CA-09 (iii)` ×2
(dispersión ≥ margen). **Ninguno es de `REQ-024`.**

### 1.3 `fail-before` / `pass-after`

**Las tres primeras secciones (31, 40/1, 40/2 — 55 casos), contra `v1.33.0`:**

```
git archive v1.33.0 hooks tools | tar -x -C <tmp>/base133b
chmod +x <tmp>/base133b/hooks/*.sh <tmp>/base133b/tools/*.sh
ARNES_HOOKS_DIR=<tmp>/base133b/hooks bash tests/escenarios/hooks/run.sh \
  'secciones/31-cola-una-sola-regla.sh' \
  'secciones/40-ausencia-que-abre-1-la-clase-derivada.sh' \
  'secciones/40-ausencia-que-abre-2-migracion-y-punteros.sh'
```

**44 PASS · 9 FAIL · 2 SKIP** (rc 1) — **exactamente la cifra del `desarrollador`**. Sin la
variable: **55 · 0 · 0** (de la corrida entera).

**Y una advertencia de método que me costó una vuelta:** mi primer intento dio **15 FAIL · 3
SKIP** porque materialicé la línea base con `git archive v1.33.0 hooks` — **sin `tools`**. Seis de
esos FAIL eran mi propio instrumento (`rc=127` del informe ausente, y un tercer lector vacío en los
tríos `1|1|`). La línea base de estas secciones necesita **`hooks` y `tools`**, que es lo que
`MAT24_RUTAS='hooks tools'` declara dentro del banco. Queda escrito para que nadie repita el error
ni lo lea como un desvío del `desarrollador`.

**`CA-06` (sección 40/3 — 16 casos), contra el texto anterior:**

```
git show 6537e83:skills/arnes-upgrade/SKILL.md > <tmp>/SKILL-antes.md
ARNES_SKILL_UPGRADE=<tmp>/SKILL-antes.md bash tests/escenarios/hooks/run.sh \
  'secciones/40-ausencia-que-abre-3-los-textos-heredados.sh'
```

**3 PASS · 13 FAIL** — **exactamente la cifra del `desarrollador`**. Sin la variable: **16 · 0**.

### 1.4 Inventario: ninguna pérdida, verificado por identidad y no por conteo

Comparación `6537e83` → `d54761e` (el delta de `CA-06`), con el banco corrido en
`git worktree add --detach`:

- `6537e83`: **988 PASS · 0 FAIL · 8 SKIP** (cuadre 996). Este árbol: 1004 · 0 · 8 (cuadre 1012).
- `diff` del inventario: **43 líneas** en mi corrida (el `desarrollador` publicó 32; la diferencia
  son grupos volátiles, abajo).
- **Prueba de que ninguna es pérdida, hecha por IDENTIDAD:** normalicé semilla, `µs` y toda tirada
  de ≥3 dígitos y comparé con `comm -23`. Sólo **2** identidades aparecen únicamente en el «antes»
  —`REQ-023 CA-03 rama 1/2`—, y las dos existen en el «después» con **otro número de tiradas**
  (2→3 y 6→4): el sorteo estratificado cambia cada corrida. **Cero casos suprimidos.** Y **+18**
  identidades nuevas: las **16** de `REQ-024 CA-06` más las 2 líneas de `CA-03` con su tirada
  nueva.

Comparación de contexto `v1.33.0` → `d54761e` (delta de la ventana entera, no de este REQ): 884 →
1012 líneas, diff de 178, y las 9 líneas «sólo en antes» son todas denominadores/µs/semillas o
SKIP↔PASS del mismo caso. **Ninguna pérdida tampoco ahí.**

---

## 2. Verificación independiente, criterio por criterio

Todo lo de aquí lo medí **ejecutando la puerta real** (`hooks/guard-completado.sh` con un
`PreToolUse` de `Edit` que escribe el estado terminal), no leyendo el banco. Proyecto de sonda en
`<tmp>/probe`, con **un manifiesto de LEGADO que no tiene el bloque `campos` en absoluto**.

### 2.1 `CA-05` — un proyecto que no hace nada no nota nada (manifiesto SIN la llave)

Manifiesto de sonda, literal, sin bloque `campos`:

```json
{ "requirements_dir": "requirements", "estados": { "completado": "completado" },
  "pending_approval": "PENDING_APPROVAL.md", "agentes": { "agente_codigo": "desarrollador" },
  "codigo_app": { "globs": ["hooks/**"] }, "quality_gates": [] }
```

| Cabecera (sobre `Estado: en-revisión` → `completado`) | `v1.33.0` | `d54761e` |
|---|---|---|
| sin `QA:` | ALLOW | **ALLOW** |
| sin `Hallazgos abiertos:` | ALLOW | **ALLOW** |
| sin `Sensible a seguridad:` (con `Rigor: ligero`) | ALLOW | **ALLOW** |
| sin `Rigor:` | ALLOW | **ALLOW** |
| completa | ALLOW | **ALLOW** |

**Idéntico, decisión a decisión.** Y el motivo por el que un manifiesto viejo no rompe está en el
código: la validación de tipos hace `map(select(.[1] != null and …))`, así que la clave **ausente**
no se reporta como error de tipo, y `if $m.campos.ausencia_exige == true then "true" else "false"`
la resuelve a `false` (`hooks/lib.sh:83`, `:95`).

**La llave nace apagada en las DOS sedes** (lo que el auditor sólo verificó a medias):
`.arnes/config.json:37` y `templates/arnes-config.json.tpl:37`, las dos `"ausencia_exige": false`.

### 2.2 `CA-01` — con la llave ENCENDIDA, ninguno de los seis campos abre

| Campo ausente | Veredicto | Cómo no abre |
|---|---|---|
| `QA` | **DENY** | motivo: «su cabecera NO declara el campo 'QA:'» |
| `Hallazgos abiertos` | **DENY** | motivo nombra `'Hallazgos abiertos:'` |
| `Sensible a seguridad` | **DENY** | gobierna `si` → suelo `critico`; el motivo **nombra el campo que falta** |
| `Rigor` | **DENY** | gobierna `critico`; el motivo **nombra el campo que falta** |
| `Seguridad` | **DENY** | ya cerraba (`:389`, sin `-n`) |
| `Estado` | n/a | es el sujeto de la transición |

Casos borde, todos con la llave encendida y todos **DENY**: `QA:` con valor **vacío**; `QA:` con
**sólo blancos**; `QA:` **comentado** (`<!-- QA: aprobado -->`); `qa:` en **minúscula**;
`Hallazgos abiertos:` vacío; `Sensible a seguridad:` vacío; `Rigor: pizza` (valor no reconocido) +
`QA` ausente; `Rigor: LIGERO` en mayúsculas + `QA` ausente.

**`QA-023-09` queda cerrada por construcción, y lo confirmo midiendo:** `qa: aprobado` en minúscula
con la llave encendida → **DENY nombrando `'QA:'`**. Con la llave apagada → **ALLOW** (la instancia
que `REQ-023` midió y enrutó aquí).

### 2.3 El agujero de `Rigor: ligero` — `QA-024-01`

Reproducción exacta (llave `true`, manifiesto de sonda):

```
# REQ-940
Estado: en-revisión
Seguridad: aprobado
Sensible a seguridad: no
Hallazgos abiertos: (ninguno)
Rigor: ligero
```

| Variante | Veredicto |
|---|---|
| la de arriba, **`QA:` omitido**, llave `true` | **ALLOW** (sin diagnóstico ninguno) |
| la misma con `Rigor: estandar` | **DENY** nombrando `'QA:'` |
| la de arriba en `v1.33.0` | ALLOW (la llave no cambia nada ahí) |
| `Rigor: ligero` + `QA: con-hallazgos` + `Hallazgos abiertos: (ninguno)`, llave `true` | **ALLOW** |
| `Rigor: ligero` + `QA` y `Hallazgos abiertos` **ausentes**, llave `true` | **DENY** — y deniega por **`Hallazgos abiertos:`**, no por `QA` |
| `Rigor: ligero` + `QA`/`HALL` ausentes, llave **`false`** | ALLOW hoy y en `v1.33.0` |

**Causa:** la resolución de la ausencia de `QA` está **dentro** del corto-circuito
`if [ "$rigor" != "ligero" ]` (`hooks/guard-completado.sh:410`, llamada en `:421`), y la de
`Hallazgos abiertos` está **fuera** (`:551`). Con la llave encendida y `Rigor: ligero`, la clase del
hallazgo deniega y el veredicto de QA no.

**Qué NO es:** no es un bypass nuevo. Bajo `ligero`, un `QA:` **declarado** con el valor más
restrictivo tampoco gatilla (fila 4), así que la ausencia **no es más permisiva que la presencia** y
la clase derivada de `CA-01` sigue **vacía** bajo el procedimiento de `REQ-023 CA-02`. Lo que es
falso es la **promesa**, en tres sedes que un proyecto hereda; el detalle y las citas están en el
hallazgo.

**Y no es alcanzable en ESTE repositorio:** `tools/arnes-lectura.sh` publica
`rigor efectivo: critico 27 · estandar 0 · ligero 0`. Es un mecanismo que se entrega a otros.

### 2.4 `CA-02` — el sitio único, y la fila que declara sin decidir

El banco lo mide **por mutación** (pone `n/a` donde la tabla decía `deniega`/`gobierna:` y exige que
los **dos** sitios de bash vuelvan a abrir): PASS en mi corrida. No lo repito.

Lo que sí anoto, porque es la frontera honesta de este criterio y **el propio `ADR-009` lo declara**:
la fila de `Seguridad` **declara** `deniega` y **no decide** — la decisión sigue en
`hooks/guard-completado.sh:389` (la comparación sin `-n`). `ADR-009` lo escribe: «Ya lo hacía …; la
tabla lo **declara** para que el sitio único sea exhaustivo». Consecuencia latente, dicha aquí para
que no se descubra al implementar: para los **veredictos** los llamadores usan **sólo el `rc`** de
`arnes_resuelve_ausencia` y **no** `ARNES_AUSENCIA_APLICA`, así que una dirección `gobierna:<valor>`
puesta en la fila de un veredicto **abriría** en silencio en vez de aplicar el valor. Hoy no ocurre
—ninguna fila de veredicto es `gobierna:`— y no abro hallazgo por ello; queda escrito.

Y un fail-open latente que **no** es alcanzable hoy y por eso tampoco es hallazgo: los dos
llamadores de `hooks/lib.sh` (`arnes_sens_efectiva:1532`, `arnes_rigor_efectivo:2377`) **ignoran el
`rc`** de `arnes_resuelve_ausencia`, así que si una clave llegara al lector sin declarar dirección
caerían al valor heredado —`no` / `estandar`—, que es el lado que **ABRE**, en contra de lo que el
comentario de `arnes_ausencia` promete («eso es fail-closed: quien pregunte trata el rc 1 como
`deniega`»). No es alcanzable porque `ARNES_AUSENCIA` se **deriva** de las mismas constantes
`ARNES_CLAVE_*` que el lector, y el discriminante de `CA-02` falla nombrando la clave si alguien
rompe eso — es decir, **la protección es la prueba, no el código**.

### 2.5 `CA-03` — la salida (ii) está declarada, y a medias ejecutada

- El banco publica el control en cada corrida: **2 de los 4** campos de la clase
  (`Sensible a seguridad`, `Rigor`) no tienen su ausencia decidida en `hooks/guard-completado.sh`,
  que es el archivo que el puntero cita → el sitio único es **otro** → salida **(ii)**.
- El caso **declara expresamente que no acredita `CA-03`** («ESTE CASO PUBLICA LA MEDICIÓN Y NO
  ACREDITA CA-03: el write-back es del analista y no se ha hecho»). Eso es honesto y lo acredito.
- La salida (ii) exige **tres** cosas. Verificadas una por una leyendo el árbol:
  1. el sitio único es otro — **hecho** (`ARNES_AUSENCIA` en `hooks/lib.sh:1873`);
  2. **`REQ-016` vuelve a `en-progreso`** — **NO hecho**: `requirements/REQ-016.md:2` dice
     `Estado: completado`;
  3. **su `CA-11` reescrito con el puntero real** — **NO hecho**: `requirements/REQ-016.md:152-153`
     sigue diciendo «Qué campos perdona la ausencia lo decide **un solo sitio**,
     `hooks/guard-completado.sh`, y ahí vive la lista exhaustiva».
- **El motivo de la elección está declarado y es correcto**, y lo verifiqué: `ADR-009` §Alternativas
  lo argumenta por medición —`hooks/lib.sh` lo cargan **siete** puntos de entrada y sólo uno es la
  puerta; una tabla en `guard-completado.sh` sería invisible para los otros seis y «el informe diría
  `estandar` donde la puerta dice `critico`»—. **Lo comprobé ejecutando** (ver §2.7): sin el renglón
  de `hooks/estado-derivado.sh` el tablero diría `estandar`. Así que la salida **no** se invocó: se
  midió.
- **La deuda con `REQ-016` está nombrada con dueño** (`analista-requerimientos`) en el Historial de
  `REQ-024` y en `ADR-009`. Lo que **no** tiene es entrada en ningún `Hallazgos abiertos:`, así que
  `guard-completado` no la ve. Eso lo corrige esta validación.

### 2.6 `CA-06` — los tres PASS que pasan antes y después: NINGUNO es tautología, y lo probé inyectando

| Caso | Antes | Después | Inyección que lo pone en rojo | Resultado |
|---|---|---|---|---|
| `(ii) orden en el apartado` | PASS (viñeta 2 < 3) | PASS | una viñeta con bloque de comando **al principio** del apartado | **FAIL** «propiedad en la viñeta 3, primer comando en la 1 (de 21)» |
| `(iii) los N barridos POR VÍA declaran las tres cosas` | PASS (**1**) | PASS (**2**) | quitar `interroga una VÍA, no la propiedad` del **segundo** barrido | **FAIL** «2 medidos y falta algo en viñeta-952: no-dice-que-interroga-una-vía» |
| `completitud: N de N «exhaustiv» negadas` | PASS (3/3, 1/1) | PASS (5/5, 2/2) | escribir «Lo que NO cambia (**lista exhaustiva**)» | **FAIL** «5 negadas de 6» |

**Juicio:** los tres son **falsables** y **publican su denominador**; su PASS-before no es vacuidad,
es un **hecho sobre el documento anterior** (el texto de `REQ-023` ya cumplía esas tres
propiedades). Y el par declarado del `desarrollador` es real y complementario, verificado por
construcción: la inyección que rompe el caso **del apartado** deja el **del sub-bloque** en PASS, y
el fail-before del sub-bloque (viñeta 0 / 0) deja el del apartado en PASS. **Ninguno de los dos
sobra: cada uno ve lo que el otro no.**

Nota de método propia: mi primera inyección (`sed` sobre «en estado terminal NO cerrarían hoy`)
**no** casó, porque en el archivo la frase **cruza un salto de línea**. Es exactamente el motivo por
el que la sección mide sobre el apartado **aplanado** y no renglón a renglón, y lo confirma por
accidente.

**El límite del caso `(iii)`, que sí es hallazgo (`QA-024-05`):** el conjunto de barridos se
reconoce por la **cadena literal** `Barrido POR VÍA|barrido POR VÍA`. Inyecté un tercer barrido
—`- **(3) Otro barrido por via, sin ninguna declaracion:** grep -r QA requirements/`— con **cero**
de las cuatro declaraciones: el caso siguió en **PASS con denominador 2**.

### 2.7 `CA-08` y `CA-09` — la cola, en los tres lectores y con 25 formas

`arnes_cola_pendientes` invocada directamente (`. lib.sh; arnes_cola_pendientes <archivo>`),
`hoy` = `d54761e`, `base` = `v1.33.0`:

| # | Fixture (`## Pendientes`) | hoy | base | Lectura |
|---|---|---|---|---|
| 1 | `<!-- nota` sin cerrar + 2 entradas detrás | `rc=1`, cola vacía, abre_ln=**3** | `rc=0` cola **0** | `CA-08`, con su discriminante |
| 2 | `<!--` en la **última** línea | `rc=1` abre_ln=4 | `rc=0` cola 1 | conforme |
| 3 | `<!--` sin cerrar **en `## Resueltas`** | `rc=1` abre_ln=5 | `rc=0` cola 1 | **más ancho que el criterio** → `QA-024-07` |
| 4 | `### Migrar A --> B` | `rc=0` cola **1** | cola **0** | `CA-09` |
| 5 | `### Migrar A --> B` + `### D2` | cola **2** | cola **1** | `CA-09` |
| 6 | `### D1 <!-- x --> uno` | cola **1** | cola **1** | grano de línea, `ADR-010` |
| 7 | `<!-- nota -->` cerrado + 2 entradas | cola **2** | cola **2** | control |
| 8 | rango cerrado, luego 2 sin cerrar | `rc=1`, abre_ln=**4** (el primero **sin cerrar**) | `rc=0` cola 0 | cita el correcto |
| 9 | sin `## Pendientes` | cola 0 `rc=0` | ídem | medida |
| 10 | `## Pendientes` vacía | cola 0 `rc=0` | ídem | medida |
| 11 | archivo inexistente | cola 0 `rc=0` | ídem | «sin archivo no hay cola» |
| 12 | cierre huérfano y **después** un `<!--` sin cerrar | `rc=1` abre_ln=4 | `rc=0` cola 0 | conforme |
| 13 | `<!--` sin cerrar **dentro de un bloque cercado** ` ``` ` | `rc=1` abre_ln=4 | `rc=0` cola 0 | fail-closed |
| 20 | **`## pendientes`** en minúscula + 2 entradas | cola **0** `rc=0` | cola **0** | → `QA-024-08` |
| 21 | `##Pendientes` sin espacio | cola 0 | cola 0 | preexistente |
| 22 | `## Pendientes de aprobacion` | cola 1 | cola 1 | prefijo, conforme |
| 23 | `##`+TAB+`Pendientes` | cola 1 | cola 1 | conforme |
| 24 | entrada con `####` | cola 0 | cola 0 | preexistente |
| 25 | `--> b <!-- c` (cierra y abre en la misma línea) | cola 1 | cola 1 | sobre-cuenta ⇒ lado que cierra |
| 26 | entrada con anotación cerrada en su línea | cola 0 | cola 0 | `CA-09 (ii)`, no-regresión |
| 27 | `###`+TAB | cola 1 | cola 1 | conforme |
| 28 | `###D1` sin espacio | cola 0 | cola 0 | preexistente |
| 29 | entrada **sangrada** | cola 0 | cola 0 | preexistente |
| 30 | **dos** secciones `## Pendientes` | cola 2 | cola 2 | conforme |
| — | `templates/PENDING_APPROVAL.md.tpl` entero | cola **0** | cola **0** | `REQ-009 CA-07`, no-regresión |

**Los tres lectores sobre el fixture 1** (rango sin cerrar):

- **la puerta**: DENY — «la cola de aprobaciones (PENDING_APPROVAL.md) **ABRE** un rango de
  comentario '<!--' en la **linea 3** —«<!-- nota»— que NO se cierra …». En `v1.33.0`: **ALLOW**.
- **la parada** (`hooks/stop.sh`): **rc 0** (no bloquea) y la celda dice
  `**Aprobaciones pendientes:** sin datos`. Y el **texto humano de fuera de los marcadores** quedó
  intacto (lo comprobé con dos párrafos propios).
- **el informe** (`tools/arnes-lectura.sh`): **rc 1**, y dice las tres cosas —
  «LA COLA DE APROBACIONES NO SE PUDO MEDIR — este informe **NO puede afirmar** «Ningún valor
  anómalo»», «la puerta de cierre **DENIEGA**» y `cola de aprobaciones (…): sin datos — ABRE un
  rango … en la línea 3 («<!-- nota») que no cierra`.

**Los tres lectores sobre el fixture 4** (cierre huérfano): puerta **DENY** «hay **1**
aprobación(es) pendiente(s)»; informe «**1** pendiente(s) — ningún REQ puede cerrarse»; bloque
derivado `**Aprobaciones pendientes:** 1`. En `v1.33.0`: **ALLOW** y **0**.

### 2.8 `CA-07` — el coste, y la DESVIACIÓN de `hooks/estado-derivado.sh` (item D)

Del banco, en la corrida 1: (i) **2 procesos** por evaluación de la puerta contra **2** de la línea
base, y **5** por parada contra **5** → **0 añadidos**; (ii) **1.080×** contra techo 1.250×
(69779 µs sobre 64569 µs, k=4 r=6, sujetos intercalados); (iii) mediana **2.042×** contra 2.200×
(margen 0.158× > MAD 0.081×, 5 tomas, par 400 → 800 entradas); (iv) mediana **2.019×** contra
2.200× (margen 0.181× > MAD 0.068×, par 1000 → 2000 bytes de línea). Los cuatro publican su par y
(iii)/(iv) usan **mediana y MAD**, no rango.

**No acredito (ii) como techo cumplido en general:** es una corrida de una máquina cargada
(`loadavg` 0,19 → 2,45) y su sonda emite SKIP por debajo del suelo — de hecho **abstuvo** en mi
`fail-before` («serie-bajo-el-suelo-de-50000us, a=48997 µs b=48087 µs»). El juez es el CI.

**La desviación de `hooks/estado-derivado.sh` es legítima y el renglón hace lo que dice.** Medido:
un REQ (`REQ-950`) que **no declara** `Sensible a seguridad:` ni `Rigor:`, con la llave

| llave | bloque derivado de `docs/ESTADO.md` | resumen del informe |
|---|---|---|
| `false` | `\| REQ-950 \| … \| **estandar** \| …` | `rigor efectivo: critico 0 · estandar 1 · ligero 0` |
| `true` | `\| REQ-950 \| … \| **critico** \| …` | `rigor efectivo: critico 1 · estandar 0 · ligero 0` |

Sin el renglón, el tablero diría `estandar` donde la puerta dice `critico`. Es **un** renglón dentro
de la llamada a `jq` **que ya existía** (0 procesos, y el banco lo mide: 5 y 5), está declarado en
**dos** sitios —el comentario del propio código (`hooks/estado-derivado.sh:409-414`) y el Historial
de `REQ-024`— y la regla que `REQ-024` se escribió para este caso («Si hiciera falta editarlo, misma
regla: desviación declarada») se cumple. **Desviación legítima.**

### 2.9 `CA-11` — ningún valor de campo se movió

`hooks/campos-req.awk` emite los valores **crudos** de los seis campos (`:12-14`, `:78-80`) y
`tools/arnes-paralelo.sh` **no lee el rigor en absoluto** (lee `Archivos:`), así que la dirección de
la ausencia no puede mover ningún **valor**: lo que cambia es el **derivado** (rigor efectivo), y
sólo con la llave encendida. Coherente con que `campos-req.awk` no necesitara ni una línea, que es lo
que `CA-11 (ii)` predijo. El banco lo mide sobre los 31 REQ contra `v1.33.0`: PASS.

### 2.10 `CA-10` — la frontera y su medición

`ADR-010` **mantiene** la frontera; `hooks/lib.sh:1191` y `:1653` la declaran **citando `ADR-010`** y
nombrando la forma medida; y `31-cola-una-sola-regla.sh` tiene el caso «los dos lectores DECIDEN AL
CONTRARIO sobre la misma forma, y se mide». Los dos controles de no-regresión de `CA-09 (i)/(ii)`
pasan y los verifiqué a mano (tabla §2.7, filas 26 y la plantilla). **Cumple.**

---

## 3. El techo de 400 líneas de `40/2` (item F): bomba ANOTADA, no defecto

Medido en `git worktree add --detach`, añadiendo **una** línea:

```
echo "# linea de mas" >> <wt>/tests/…/40-ausencia-que-abre-2-migracion-y-punteros.sh
bash <wt>/tests/escenarios/hooks/autoprueba-corredor.sh   # rc 1
  FAIL  CA-18 ningún archivo excede max(N, piso × k) — con N=400 y k=1,25
        obtenido=< 40-ausencia-que-abre-2-migracion-y-punteros.sh(401 líneas, techo 400)>
Autoprueba: 105 PASS, 1 FAIL
```

Y la tabla de la propia autoprueba publica el porqué: `lineas= 401 piso= 194 techo= 400 (gobierna
N)` — con piso 194, `piso × 1,25 = 242`, así que manda **N = 400** y no hay holgura ninguna.

**Juicio: bomba anotada, no defecto.** (1) El techo **falla ruidosamente y nombra el archivo**, que
es la conducta contratada por `REQ-014 CA-18`. (2) Está **declarado por adelantado** en el Historial
de `REQ-024` y en `docs/qa/1.34.0-req024-ca06-metodo.md:97`, y ya **forzó la decisión correcta**: la
sección `40/3` nació aparte por eso **y** por tema. (3) La consecuencia —que el próximo caso de
`CA-03`/`CA-05`/`CA-07` obligue a una parte 4— es visible en el momento en que se intente. Lo único
que anoto es que el aviso vive en el Historial y en un método de QA, **no en el preámbulo del propio
`40/2`**, que es donde lo leería quien vaya a añadir el caso.

---

## 4. Los dos ADR (item E)

| | `ADR-009` | `ADR-010` |
|---|---|---|
| Papel | dirección de la ausencia | grano de la cola |
| `Estado:` | **propuesta** (gate humano pendiente) | **propuesta** (gate humano pendiente) |
| ¿coincide con el código? | **sí**, verificado fila a fila | **sí**, verificado |

**`ADR-009`, comprobado punto por punto contra el árbol:**
- «el sitio único es `hooks/lib.sh`, la tabla `ARNES_AUSENCIA` derivada de `ARNES_CLAVE_*`» →
  `hooks/lib.sh:1873-1881`, derivada en `_arnes_deriva_ausencia`. **Coincide.**
- la tabla campo→dirección (`gobierna: si` / `gobierna: critico` / `deniega` ×3 / `n/a`) →
  coincide **fila a fila** con `:1875-1879`, y con lo que yo medí ejecutando (§2.2).
- «los dos despachos … ya no deciden: preguntan» → coincide, con la salvedad **declarada por el
  propio ADR** de la fila de `Seguridad` (§2.4).
- «la llave viaja dentro de la lectura de manifiesto que ya se hacía … 0 forks; medido 2 y 2, 5 y 5»
  → coincide (`hooks/lib.sh:83`, `hooks/estado-derivado.sh:415`, `tools/arnes-lectura.sh:47`), y el
  banco reproduce las cuatro cifras.

**`ADR-010`**: su punto 3 dice «Verificado ejecutando: el ejemplo multilínea comentado de la
plantilla sigue contando **0** y la anotación cerrada dentro de su línea sigue contando **0**». **Lo
reproduje: las dos cosas.**

**Y el gate: `ADR-009` lo necesita y NO lo tuvo antes del acto** — `QA-024-03`.

---

## 5. `SEC-081` (item G): la fila no se creó, pero la FORMA reaparece dos veces

**Lo que confirmo del encargo:** el `desarrollador` **no escribió** la fila del rigor y **no cerró**
`SEC-081`. Correcto: su forzador es `CA-04`, que está detenido en `D7`. **No existe una cuarta
instancia de la fila.**

**Lo que no puedo confirmar:** que ninguna de las frases nuevas reproduzca la forma. Reproducen la
forma **dos**, y las dos son `QA-024-06`. La medición que lo demuestra, con la llave **apagada** y
sobre la puerta real:

| Vía que produce el MISMO estado ausente | Veredicto con `QA: pendiente` |
|---|---|
| comentar / borrar / no escribir (las tres nombradas) | ALLOW (medido en §2.1) |
| **otra capitalización**: `qa: pendiente` | **ALLOW** (control: `QA: pendiente` → **DENY**) |
| **letra sustituida**: `QÁ: pendiente` | **ALLOW** |

Las dos últimas ya tienen dueño (`QA-023-09`, `SEC-078`), así que **no son vía nueva**: lo que falla
es el **número cerrado** en el párrafo del residual.

---

## 6. Lo que NO se midió, dicho expresamente

1. **`CA-04`**: fuera de encargo por instrucción. No lo probé, no lo exijo y no lo acredito.
   Verificado sólo que las cuatro sedes siguen **intactas** (`AGENTS.md:203-209` y `:343`;
   `templates/AGENTS.md.tpl:167-173` y `:308` — `git diff v1.33.0` sobre esas sedes muestra
   únicamente la fila de `SEC-079`, autorizada por `D5`, **no** la del rigor).
2. **El CI (`hooks-en-linux`)**: no lo corrí. Ningún techo de reloj de este documento acredita nada
   más allá de esta máquina, y los tres SKIP de la familia `SEC-080` desvían local↔CI por diseño.
3. **Rendimiento bajo concurrencia o carga**: `AGENTS.md` no fija umbral de usuarios concurrentes ni
   de latencia objetivo para este proyecto —no hay usuarios finales—, así que **no hay prueba de
   carga y queda pendiente de acordar con el humano** si alguna vez tiene sentido aquí. Lo que sí
   hay son los cuatro techos de `CA-07`, medidos por el banco y **no concluyentes** en esta máquina
   (§2.8).
4. **La inmutabilidad del árbol de referencia** (`v1.33.0` como tag): `SEC-048` sigue abierto. Todo
   `fail-before` de este documento supone que ese tag es el que dice ser.
5. **Firma de seguridad**: no la emito ni la sustituyo. El `auditor-seguridad` va después.
6. **`REQ-023 CA-12`**: su SKIP declara que no puede atribuir. Es de `REQ-023` (`bloqueado`) y está
   fuera de mi encargo; sólo constato que es **determinista** (mismo texto en mis dos corridas).
