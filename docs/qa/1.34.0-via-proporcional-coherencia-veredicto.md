# QA — Vía proporcional a producto · revisión única y acotada de coherencia

- **Árbol medido:** `/home/juan/dev/ArnesJuan-via-proporcional`, rama `rel/via-proporcional`, commit `a80a1cb`, árbol limpio.
- **Base de comparación:** `f387b1c` (diff: 9 archivos, +191/−9).
- **Fecha:** 2026-09-13. **Autorización:** propietario, revisión única y acotada.
- **Objeto único:** que las cuatro sedes digan lo mismo y que ninguna obligación se pierda.

## Veredicto: NO ADOPTABLE tal cual

La política es correcta en su diseño y las tres capacidades están enunciadas, pero **la
corrección no está completa en las sedes**: la contradicción que este cambio existe para no
cometer **sigue viva en dos de los cuatro agentes**, una de ellas en el sitio exacto donde se
produce el fallo que el commit dice haber cerrado. Además, la instrucción de migración puede
**detener la migración entera** en los proyectos consumidores.

Son ocho defectos de texto, todos dentro de los mismos 9 archivos. Ninguno exige rediseñar la
política.

## Lo mecánico, confirmado

| Comprobación | Resultado |
|---|---|
| Quality gates §7 (4 de 4), corridas por mí sobre `a80a1cb` | **OK 4/4** |
| `hooks/`, `tools/`, `tests/`, `.arnes/`, `.github/`, `.claude-plugin/`, `requirements/` tocados | **NINGUNO** |
| Cuerpo del diff `AGENTS.md` vs `templates/AGENTS.md.tpl` | **idéntico línea a línea** |
| Banco, filtro `'44-*' '46-*'` | **103 PASS · 0 FAIL · 1 SKIP** |

Nota sobre el banco: mi selector arrastró también la sección `45`, por eso el conteo difiere del
medido por el propietario (76·0·2). **Lo relevante coincide: 0 FAIL.** No investigo la no
reproducibilidad del banco — prohibido expresamente.

## (4) Exclusión del experimento descartado — CONFIRMADA por diff

- `referencia/`: **ausente** en el working tree y en `a80a1cb`.
- `§13` de `AGENTS.md`: **idéntica byte a byte** a la base. `sha256` coincidente
  (`6e206361…50fc3`) en `f387b1c` y `a80a1cb`.
- `§13` de `templates/AGENTS.md.tpl`: **idéntica byte a byte** a la base.
- `RUTA_ARNES`, `referencia/`, «extracción documental»: aparecen en **un solo archivo**,
  `CHANGELOG.md`, y **sólo** en la entrada que **declara la exclusión** (líneas añadidas 16-17).
  Son menciones, no artefactos. Ningún otro archivo del árbol los contiene.
- La exclusión es **estructural**: la base `f387b1c` es anterior al experimento.

**Discrepancia menor de método (no de hecho):** la cifra `17 807 B` no es re-derivable. Midiendo
§13 con encabezado obtengo **18 023 B**; sin encabezado, **17 973 B**. La afirmación sustantiva
—identidad byte a byte— queda **confirmada por `sha256`**; sólo el número carece de método
reproducible.

## (1) Coherencia entre las cuatro sedes — barrido por propiedad

Propiedad barrida: *toda frase, en cualquiera de las cuatro sedes, que atribuya una fase o un
write-back a un rol fijo*. Se barrió también `docs/gobernanza/autoalojamiento.md`, por ser la
sede única de la enmienda.

### H-1 · `agents/qa-tester.md:76` — la regla vieja sigue viva, y en la sección del veredicto
**Clase: `contrato`. Bloquea.**

El commit corrigió la ocurrencia de la línea 38-44. **Dejó intacta la de la línea 76**, dentro de
`## Resultado y cambio de estado`:

> - **Write-back (anti-deriva):** un hallazgo que añade comportamiento aceptado no se cierra hasta
>   que ese comportamiento quede como **criterio de aceptación** en el REQ (vía
>   `analista-requerimientos`).

**Esperado:** que `agents/qa-tester.md` no atribuya el write-back a un rol fijo.
**Observado:** lo atribuye, en la sección que QA lee **en el acto de firmar**.
**Efecto:** es exactamente el fallo que el commit declara haber cerrado —*«QA habría rechazado el
de un desarrollador»*—, y sigue alcanzable. Peor que la ocurrencia ya corregida: la 38-44 está en
la sección de crítica de criterios; la 76 está en la del veredicto.
**Consecuencia sobre el propio commit:** su justificación —*«Ahora QA sabe que quien transcribe
depende de la vía»*— **afirma algo falso sobre lo construido**, y la misma frase está en
`CHANGELOG.md:26-28` y en `docs/gobernanza/autoalojamiento.md:67-68`.
**Reproducción:** `grep -n 'vía .analista-requerimientos.' agents/qa-tester.md`.

### H-2 · `agents/analista-requerimientos.md:52` — promesa absoluta, sin excepción ni puntero
**Clase: `contrato`. Bloquea.**

> - **Write-back de hallazgos (anti-deriva):** … **eres quien lo refleja en el requerimiento** …

**Esperado:** una sola doctrina de write-back en el archivo.
**Observado:** dos, contradictorias y a 5 líneas de distancia. El párrafo corrector (líneas 57-62)
dice *«No te toca cuando…»*; la línea 52 dice *«eres quien lo refleja»*, sin excepción y sin
remitir a §6.
**Causa del defecto:** el párrafo corrector se insertó bajo `## Estados — vocabulario único`,
sección que trata del vocabulario de `Estado:`, no del write-back; quedó **separado de la promesa
que corrige**. Corregir sólo la línea 52 deja la doctrina repartida en dos secciones sin relación.
**Efecto:** el analista puede duplicar, rechazar o pisar el write-back de un desarrollador, o la
coordinadora abrir una comisión de analista que la política declara innecesaria — que es el coste
que esta vía existe para eliminar.

### H-3 · `AGENTS.md:116` — afirmación ahora falsa, no tocada por el commit
**Clase: `contrato`. Bloquea.**

> **Cambia cómo desarrollamos este repositorio; no cambia nada de lo que reciben los proyectos.**

**Observado:** este commit lleva la vía a `templates/AGENTS.md.tpl`, a los cuatro agentes
distribuidos y a la migración de `arnes-upgrade`. La frase es **absolutamente falsa** ahora.
El commit tachó el límite 1 en la sede de gobernanza pero **no barrió sus transcripciones**.
**Reproducción:** `git diff f387b1c..a80a1cb -- AGENTS.md | grep 'no cambia nada'` → vacío.

### H-4 · `docs/gobernanza/autoalojamiento.md:3-4` — el preámbulo contradice su propio límite tachado
**Clase: `contrato`. Bloquea.**

> **Alcance: sólo cómo desarrollamos ArnesJuan.** No cambia las protecciones ni los valores por
> defecto que reciben los proyectos nuevos o existentes.

**Observado:** 59 líneas más abajo, en la misma sección, el límite 1 está tachado y levantado
precisamente para cambiar lo que reciben los proyectos nuevos y existentes. El preámbulo es la
**premisa** de la que se deduce H-3; corregir H-3 y dejar viva ésta reproduce el defecto.

### H-8 · `agents/qa-tester.md:55` — la deriva sigue ofreciendo sólo dos salidas
**Clase: `instrumento`. No bloquea.**

> Repórtalo y devuélvelo para que el `analista-requerimientos` actualice el REQ … o el
> `desarrollador` alinee el código.

Falta la tercera salida que la vía nueva habilita: **el desarrollador actualiza el REQ**. Mismo
patrón de rol fijo, efecto menor porque las líneas 38-44 ya cubren quién transcribe.

### Observación · `AGENTS.md:248` y `templates/AGENTS.md.tpl:205`
> Lo fija el analista; el auditor **puede subirlo** y nadie lo baja sin su firma.

El `Rigor:` y `Sensible a seguridad:` se atribuyen al analista, a quien **dos de las cuatro filas
de la tabla nueva no despachan**. Para una reparación sobre un REQ existente no hay pérdida: el
rigor ya está fijado. Si una reparación de las filas 2 o 3 necesitara **REQ nuevo**, ningún rol
queda asignado a fijar la sensibilidad, y el disparador del auditor depende de ese flag. El texto
nuevo lo roza en voz pasiva (*«el rigor y la sensibilidad se fijan por el efecto»*) sin reconciliar
la línea 248. **No lo elevo a hallazgo** porque la vía de reparación presupone REQ existente; lo
dejo nombrado por si el propietario quiere cerrarlo.

## (2) Las tres capacidades, ejercidas contra el texto

No deduzco ninguna del rigor que exige el hook, conforme a la instrucción del propietario.

### ¿Puede el `desarrollador` actualizar la documentación de una reparación ya decidida? — **SÍ**
- `agents/desarrollador.md:20-25` lo concede explícitamente, con la frontera correcta
  (*«transcribir no es decidir»*, para y escala).
- `AGENTS.md` §6 y §9 lo conceden en las dos sedes.
- **Nada se lo prohíbe en otra sede**: barrido `agents/desarrollador.md` en busca de límites sobre
  `requirements/` — no hay ninguno; y `codigo_app.globs` del manifiesto cubre `hooks/`, `tools/`,
  `.github/`, **no** `requirements/`.
- **Reserva:** H-2 no se lo impide, pero pone al analista a disputárselo.

### ¿Puede el `qa-tester` revisarla? — **PARCIALMENTE, y ése es H-1**
- Sabe que **quién transcribe depende de la vía** (líneas 38-41). ✔
- Sabe que debe **verificar que el write-back existe y no firmar sin él, venga de quien venga**
  (líneas 41-43). ✔ Enunciado literalmente y bien.
- Sabe que si el write-back **decide** algo debe **devolverlo al analista** (líneas 43-44). ✔
- **Pero la línea 76, en la sección del veredicto, sigue diciendo «vía `analista-requerimientos`».**
  La capacidad está enunciada en un sitio y desmentida en otro, dentro del mismo archivo. ✘

### ¿Permanecen las obligaciones de seguridad aplicables? — **SÍ, las tres**
- **Disparador:** `agents/auditor-seguridad.md:25` **mejora**: pasa de *«marcado como sensible por
  el analista»* a *«marcado como sensible»*, **sin depender de quién lo despache**. Correcto y
  necesario: en las vías 2 y 3 el analista no interviene, y la redacción anterior habría dejado el
  disparador sin sujeto.
- **Veto:** `agents/auditor-seguridad.md:24` intacto.
- **No firma lo que QA no ha validado:** `agents/auditor-seguridad.md:26-31` intacto; `AGENTS.md`
  §6 conserva *«El orden no es una sugerencia»* y los tres puntos de lo que no se paraleliza,
  inmediatamente **después** de la tabla nueva. La excepción `Seguridad: preventiva` sigue
  declarada al emitirla.
- La tabla nueva **no menciona a seguridad en la fila 2**, pero tampoco la excluye, y el párrafo
  siguiente la reintroduce por efecto. No hay pérdida de obligación.

## (3) La migración — NO ADOPTABLE tal cual

Contraste de la tabla nueva (`skills/arnes-upgrade/SKILL.md:1226-1236`) contra la tabla canónica
de la propia skill (líneas 45-50) y sus reglas de terminalidad (37, 61-68, 83, 145).

| Estado | Canónica | Tabla nueva | Veredicto |
|---|---|---|---|
| `NUEVO` | No existía en la base → Añadir | Añadir, sin texto que conservar | **coherente** |
| `INTACTO` | Idéntica a la base → Actualizar | Aplicar sin preguntar | **coherente** |
| `MODIFICADO` | Difiere → **Conflicto** (no pisar; se lista para el humano; no se toca) | «Conflicto: preguntar. Conserva tu texto **y añade encima lo nuevo**» | **H-7** |
| `ELIMINADO` | Conflicto; **no reponer**, pudo borrarse a propósito | idéntico, y además dice qué poner en el informe | **coherente, y bien** |
| `UNKNOWN` | **Terminal**: «se detiene»; «si hay algún `UNKNOWN`, **no se aplica nada**» | «**Preguntar**, y dejarlo constar» | **H-6** |

**Los cinco estados existen y ninguno se declara imposible.** ✔
**Conserva personalizaciones** (`MODIFICADO` → conserva tu texto) y **presenta conflictos en vez de
resolverlos solos** (`MODIFICADO` y `ELIMINADO` → preguntar). ✔
**El caso de la base que no tenía la sección está resuelto explícitamente y bien** (líneas
1234-1236): *«un proyecto instalado con una versión anterior puede no tener alguno, y ahí el estado
correcto es `NUEVO`, no `INTACTO`. Clasifica cada uno por separado»*. ✔ Responde justo a la
pregunta del propietario.

### H-5 · Los agentes no son migrables, y exigirlo **detiene la migración entera**
**Clase: `contrato`. Bloquea.**

La tabla nueva clasifica «`§6` / `§9` / **un agente**» por estado de merge. Pero:
1. `arnes-init` guarda en `.arnes/plantillas-origen/` *«una copia literal de cada `.tpl` usado»*
   (SKILL de init, paso 4). **Los agentes no son `.tpl`.** Comprobado: `.arnes/plantillas-origen/`
   contiene 11 archivos y **ninguno es un agente**.
2. Los agentes **los provee el plugin** en `.claude/agents/` (`AGENTS.md` §12 y `CLAUDE.md`); el
   proyecto **no tiene copia propia** que clasificar. En este mismo repo, `.claude/agents/` no
   existe en el worktree ni está versionado.

**Encadenado con las reglas de la propia skill:** sin base recuperable el estado es `UNKNOWN`
(líneas 37 y 83); y *«Si hay algún `UNKNOWN`, **no se aplica nada**»* (línea 145). Resultado: una
migración que siga esta instrucción al pie **clasifica los cuatro agentes como `UNKNOWN` y no
aplica nada**, incluidas `§6` y `§9`, que **sí** son migrables. La alternativa —improvisar— la
prohíbe la línea 64 (*«Nunca conviertas incertidumbre en decisión»*).
**Y es innecesario:** los agentes se actualizan **con el plugin**, no por migración. La entrada
debería decirlo y no lo dice.

### H-6 · La fila `UNKNOWN` rebaja una regla declarada «no negociable»
**Clase: `contrato`. Bloquea.**

Canónica (línea 64): *«`UNKNOWN` es tan terminal como `CONFLICTO`, y esto no es negociable»*, con
`se detiene` y `no se aplica nada` **para toda la corrida**. La fila nueva dice sólo «**Preguntar**,
y dejarlo constar» — **per sección**, sin detención ni el efecto global. Una obligación se pierde.

### H-7 · La fila `MODIFICADO` prescribe una acción que la canónica reserva al humano
**Clase: `instrumento`. No bloquea.**

«Conserva tu texto **y añade encima lo nuevo**» puede leerse como la acción a ejecutar. La canónica
dice que en `CONFLICTO` *«el contenido del proyecto no se ha tocado»* (línea 1010), que los
conflictos *«se listan para el humano»* (145) y que sólo se aplica lo marcado `SAFE` (Fase 3).
Leída benévolamente, «añade encima» es lo que se **propone** tras el «preguntar»; leída al pie,
autoriza a escribir en una sección `MODIFICADO`. La ambigüedad es el defecto.

### Observación menor, preexistente
El encabezado canónico dice «**Clasificación: cuatro estados**» y la tabla lista cuatro, pero la
Fase 2 (línea 141) exige clasificar en **cinco**. La entrada nueva dice «los cinco», que es lo
correcto. No es defecto de este commit; se anota para que quien corrija H-5/H-6 no lo tome por
contradicción suya.

### Observación · fila 1 de la tabla de §6 frente a `guard-codigo`
La fila 1 asigna a la coordinadora la documentación sin cambio de obligaciones. Si la errata vive
en un comentario de `hooks/`, `tools/` o `.github/`, `guard-codigo` **deniega**. Es **fail-closed**
—el trabajo cae al desarrollador, no se cuela—, y el límite 5 de la enmienda ya prevé el caso; pero
el texto que llega al producto (`templates/AGENTS.md.tpl`) **no lleva ese límite 5**, porque la
sede de gobernanza no se propaga. No lo elevo a hallazgo: el efecto es una denegación visible, no
una protección reducida.

## Resumen de hallazgos

| Id | Sede | Clase | Bloquea |
|---|---|---|---|
| H-1 | `agents/qa-tester.md:76` | `contrato` | **sí** |
| H-2 | `agents/analista-requerimientos.md:52` | `contrato` | **sí** |
| H-3 | `AGENTS.md:116` | `contrato` | **sí** |
| H-4 | `docs/gobernanza/autoalojamiento.md:3-4` | `contrato` | **sí** |
| H-5 | `skills/arnes-upgrade/SKILL.md:1226-1236` (agentes) | `contrato` | **sí** |
| H-6 | `skills/arnes-upgrade/SKILL.md:1232` (`UNKNOWN`) | `contrato` | **sí** |
| H-7 | `skills/arnes-upgrade/SKILL.md:1229` (`MODIFICADO`) | `instrumento` | no |
| H-8 | `agents/qa-tester.md:55` | `instrumento` | no |

No corrijo ninguno: mido y reporto. La corrección es del `desarrollador`.

## Qué NO acredita esta revisión

1. **No acredita la publicación.** No me pronuncio sobre fusionar, etiquetar ni publicar; sigue
   detenido.
2. **No acredita `REQ-024`, `REQ-023` ni `REQ-026`**, ni ningún hallazgo suyo. No los miré.
3. **No acredita el banco completo.** Corrí el filtro `'44-*' '46-*'` (que arrastró también `45`):
   **0 FAIL**. El resto de las ~73 secciones **no se corrió** en este árbol. La no reproducibilidad
   del banco queda **sin investigar** por prohibición expresa.
4. **No acredita el comportamiento real de `arnes-upgrade`.** Leí la instrucción y la contrasté con
   las reglas de la propia skill; **no ejecuté ninguna migración** contra ningún proyecto
   consumidor. H-5, H-6 y H-7 son defectos **del texto de la instrucción**, medidos por lectura
   cruzada, no por corrida.
5. **No acredita que los agentes lleguen corregidos a ningún proyecto**, ni que esta política se
   aplique sola: **ningún guardián del banco cubre `agents/*.md`**. Las secciones 44 y 46 vigilan
   `AGENTS.md` §13 y `templates/AGENTS.md.tpl`, no los agentes. Por eso H-1 y H-2 **no los caza
   ninguna máquina**: sólo se ven leyendo.
6. **No acredita seguridad.** No soy el auditor y no emito `Seguridad:`.
7. **No propuse ampliar la política** ni la evalué más allá de la coherencia entre las cuatro
   sedes.

---

# Segunda vuelta — verificación de la reparación conjunta

- **Árbol medido:** mismo worktree, rama `rel/via-proporcional`, commit **`c65ce65`**, árbol limpio.
- **Diff verificado:** `81d9d89..c65ce65` — 8 archivos, +167/−29.
- **Fecha:** 2026-09-13. **Autorización:** QA única y acotada sobre el candidato completo.

## Veredicto: ADOPTABLE CON RESERVA

**Los diez están reparados**, ocho de ellos limpiamente. Las dos sedes que el desarrollador
destapó por su cuenta (`agents/auditor-seguridad.md` líneas 3 y 38) eran reales y están cerradas.

**Pero mi barrido encontró tres sedes más que nadie miró, y el ejercicio sobre copias temporales
destapó un defecto de migración que NO es fail-closed.** Ese último es el que impide firmar
`adoptable` a secas.

## 1 · Los ocho, uno a uno

| Id | Sede | Estado | Evidencia |
|---|---|---|---|
| H-1 | `agents/qa-tester.md:76` | **reparado** | `grep 'vía \`analista-requerimientos\`' agents/qa-tester.md` → **0 ocurrencias**. Sustituido por «Quién lo transcribe depende de la vía … **lo tuyo no cambia: exiges que exista … venga de quien venga**» |
| H-2 | `agents/analista-requerimientos.md:52` | **reparado, y bien** | La promesa absoluta se **condicionó en su propio sitio** (`siempre que quede una decisión de requisitos o de diseño`) y el párrafo corrector se **movió** de `## Estados` a la sección del write-back. Se corrigió la causa, no sólo la frase |
| H-3 | `AGENTS.md:116` | **reparado** | `grep 'no cambia nada de lo que reciben' AGENTS.md` → **0**. Sustituido por el alcance fechado en dos tramos |
| H-4 | `docs/gobernanza/autoalojamiento.md:3-4` | **reparado, y bien** | Se corrige **la premisa**, no la conclusión: «Alcance al nacer» + «Alcance vigente, tras el levantamiento del límite 1», nombrando qué se propaga y qué no, y **declarando que la frase anterior dejó de ser cierta el 2026-09-12** |
| H-5 | migración, agentes | **reparado** | Párrafo previo a la tabla: «los agentes **NO se migran** … no los clasifiques, no los busques y no los pongas en el plan». Cabecera de la tabla pasa de `§6 / §9 / un agente` a **`§6 / §9`**. Verificado: `.arnes/plantillas-origen/` contiene **0** agentes |
| H-6 | migración, `UNKNOWN` | **reparado** | «**Terminal, como `CONFLICTO`, y no es negociable: te DETIENES y NO se aplica NADA de toda la corrida**, ni siquiera lo que salió `SAFE`» — recupera el efecto global de la línea 145 |
| H-7 | migración, `MODIFICADO` | **reparado** | «Conflicto: preguntar, **y NO tocar la sección** … **no escribas nada en ella** … **propón** … **aplicarlo es decisión tuya, no de la migración**». Deshace la ambigüedad exactamente donde estaba |
| H-8 | `agents/qa-tester.md:55` | **reparado** | La deriva pasa de dos salidas a **tres**, explicitadas por vía |

## 2 · Las dos observaciones

**El rigor — reparada, y con reserva menor.** El punto 2 de «lo que esta vía NO cambia» pasa de
una frase en voz pasiva a **restituir el procedimiento entero**: los campos declarados siguen
vigentes tal cual, clasificar no reclasifica, y subirlo o bajarlo «sigue su procedimiento de
siempre, **y esta tabla no es un atajo a él**» — con los cuatro elementos nombrados (lo fija el
analista, el auditor puede subirlo, nadie lo baja sin su firma, `Sensible a seguridad: sí` impone
`critico` como suelo). Reconcilia con `AGENTS.md:264` / `tpl:217`. **Viaja a la plantilla.**
*Reserva:* al nombrar de nuevo al analista como quien lo fija, la segunda contradicción abierta
(REQ nuevo en vías 2 o 3) queda **enunciada en dos sedes más**. Ver §6 de este informe.

**La edición en archivo protegido — reparada, y viaja.** Párrafo nuevo tras la tabla: «**La fila 1
no levanta el control de edición de un archivo protegido** … elegir vía decide **quién revisa**,
nunca **quién puede escribir** … ahí `guard-codigo` **deniega la edición, y hace bien** — la puerta
mira la **ruta**, no si el cambio es documental». **Comprobado que no se queda en la enmienda:**
está en `AGENTS.md`, en `templates/AGENTS.md.tpl` y resumido en la entrada de `arnes-upgrade`.

## 3 · Las gemelas — afirmación CONTRASTADA y confirmada

No la acepté: la medí.

| Comprobación | Resultado |
|---|---|
| Cuerpo de §6 (bloque de la vía) | `AGENTS.md` 134-183 · `tpl` 89-138 — **50 líneas, 4074 B, `sha256` `f695e4ba…22b1c` idéntico** |
| Cuerpo de §9 (bullet CAMBIOS POR HALLAZGO) | 13 líneas, **1273 B, `cmp` sin diferencias** |
| `ALIGERADA` en la plantilla | **0 ocurrencias**, y `git log -S'ALIGERADA' -- templates/AGENTS.md.tpl` **vacío**: nunca existió |
| `Política de autoalojamiento` en la plantilla | **0 ocurrencias**, `git log -S` **vacío**: nunca existió |

**La divergencia es la declarada y ninguna más**, y es correcta: los dos blockquotes son de
autoalojamiento y no deben propagarse.

## 4 · Tres sedes nuevas, encontradas barriendo por propiedad

Barrí sin fiarme de la lista, por la propiedad completa y sobre todo el árbol, frontmatters
incluidos. El barrido de la propiedad (b) —afirmar que la enmienda no llega a los proyectos—
sale **limpio**. El de la propiedad (a) deja tres.

### N-1 · `templates/requirements-README.md.tpl:236` — `contrato`. Bloquea.
> la prueba, y quien lo reescribe es **el analista** (write-back, `AGENTS.md` §9).

Es una **plantilla de producto**: `arnes-init` la instala como `requirements/README.md` en **todo
proyecto nuevo**. Resultado: la misma instalación entrega un `AGENTS.md` §9 que dice «depende de la
vía» y un `requirements/README.md` que dice «lo reescribe el analista». **Dos documentos del mismo
install que se contradicen.** Nota de alcance: el levantamiento del límite 1 nombra
`templates/AGENTS.md.tpl`, `AGENTS.md`, los cuatro agentes y la migración — **no** este archivo; si
corregirlo excede lo autorizado, es decisión del propietario, pero **dejarlo obliga a declarar la
contradicción**, no a ignorarla.

### N-2 · `skills/arnes-close/SKILL.md:29` — `contrato`. Bloquea.
> devuélvelo al `analista-requerimientos` para el write-back antes de entregar.

Skill **distribuida**, y no es una cualquiera: es la que `AGENTS.md` §13 designa para comprobar
«la **trazabilidad y no-deriva** de cada REQ `completado`». Bajo la vía nueva, un write-back
ausente por una reparación clara es del **desarrollador**. Rol fijo, en el punto de cierre.

### N-3 · `description` de `agents/analista-requerimientos.md` y `agents/desarrollador.md` — `contrato`. Bloquea.
Por el **mismo criterio** con el que el desarrollador corrigió el frontmatter del auditor —*«es la
línea que la coordinadora lee al despachar»*—, la pareja sigue describiendo el régimen viejo:

- analista: «Úsalo … **cuando haya que crear/actualizar archivos en `requirements/`**» → manda al
  analista **toda** actualización de `requirements/`, sin la condición de la vía.
- desarrollador: «escribiendo y editando **el código de la app, sus pruebas y la documentación
  técnica**» → **omite el write-back**, que es justo la capacidad que este cambio le concede.

Una coordinadora que despache leyendo las dos descripciones enruta el write-back al analista. Es la
misma forma del defecto del auditor, en los dos agentes que la vía convierte en protagonistas.
Ninguna de las dos líneas se tocó en `c65ce65`.

## 5 · El procedimiento de actualización, ejercido sobre copias temporales

**Distinción explícita, y es parte del veredicto: NO he migrado ningún proyecto.** No toqué ningún
proyecto consumidor. Lo que hice fue construir **copias temporales** en
`…/scratchpad/mig/` a partir de artefactos **reales** del repositorio —la base congelada
`.arnes/plantillas-origen/AGENTS.md.tpl` y plantillas recuperadas de tags publicados— y **aplicar a
mano la instrucción**, comprobando que cada paso es ejecutable y adónde lleva. Es una comprobación
**en papel sobre archivos reales**, no una corrida de `arnes-upgrade`. No construí ningún analizador
de instrucciones.

| Caso | Montaje | Resultado |
|---|---|---|
| **A · instalación nueva** | plantilla destino → proyecto | Llegan la tabla de vías, el párrafo de `guard-codigo` y el punto 2 del rigor. **0** agentes dentro del `AGENTS.md`: los provee el plugin, conforme |
| **B · instalado, sin tocar** | proyecto = base congelada | §6 `INTACTO`, §9 `INTACTO` → aplicar sin preguntar. Conforme |
| **C · §6 personalizado** | base + edición local en el `Flujo:` | §6 `MODIFICADO`, §9 `INTACTO`. **`sha256` del archivo idéntico antes y después**: la sección personalizada **no se toca**, conforme a H-7 reparado |
| **D · §9 borrada** | base menos §9 | `ELIMINADO` → conflicto, no reponer. Conforme |
| **E · agentes** | — | **0** agentes en `.arnes/plantillas-origen/` → sin base. La instrucción nueva manda **no clasificarlos**, que es lo que evita el `UNKNOWN` y la detención. **H-5 confirmado en ejercicio** |
| **F · instalado en v1.2.0** | base y proyecto = plantilla real de `v1.2.0` | **FALLA. Ver N-4** |

### N-4 · La colisión de número de `§9` — `contrato`. **Bloquea, y NO es fail-closed.**

**Medido sobre plantillas publicadas, no supuesto.** `## 9.` no siempre significó lo mismo:

- `08f17ee` (v1.1.0) …: `## 9. Convenciones de trabajo`
- `ccd6371` (bump 1.4.0): `## 9. Cambios de requerimientos (versionado y deriva)`
- **Versiones publicadas con el título viejo: `v1.2.0`, `v1.3.0`, `v1.3.1`, `v1.3.2`, `v1.3.3`.**
- `## 6.` **nunca** cambió de título: la colisión afecta **sólo a §9**.

**Reproducción (caso F):** proyecto instalado en `v1.2.0` y **sin tocar**. Su `.arnes/plantillas-origen/`
guarda la plantilla de `v1.2.0`, cuyo `## 9.` es «Convenciones de trabajo». La migración compara
§9 del proyecto contra §9 de la base:

- **Esperado:** que la instrucción detecte que el proyecto no tiene la sección de cambios de
  requerimientos y la clasifique `NUEVO` → «Añadir».
- **Observado, clasificando por número:** proyecto y base son **idénticos** → **`INTACTO`** → y
  `INTACTO` es la **única** fila que dice **«Aplicar el contenido nuevo *sin preguntar*»**. La
  doctrina del write-back se escribiría **dentro de «Convenciones de trabajo»**, en silencio, en el
  `AGENTS.md` de un consumidor.
- **Clasificando por título:** `NUEVO` → «Añadir» → correcto.

**El defecto es que la instrucción no dice cuál de las dos.** Y su propio salvavidas **no dispara**:
dice «un proyecto instalado con una versión anterior **puede no tener alguna**, y ahí el estado
correcto es `NUEVO`, no `INTACTO`» — condición de **ausencia**, cuando el caso real es **presencia
con el mismo número y otro significado**. La sección §9 **sí** está en la base; por eso el aviso
no la salva.

**La skill ya tiene la doctrina correcta y esta entrada no la hereda** (línea 847): *«si el
`AGENTS.md` del proyecto ya tiene una sección `## 14.` propia, el número está tomado: **`UNKNOWN`,
se detiene y se pregunta***». Y avisa, en esa misma línea, de que las referencias apuntan **por
número** y romperlas «**en silencio**» es el riesgo.

**Por qué bloquea:** es la única vía de las cinco que **actúa sin preguntar**, y aquí actúa mal
sobre el archivo de gobernanza de un consumidor. Clasifico `contrato` porque el enunciado
—«los cinco, sin declarar ninguno imposible», y el aviso que dice cubrir las instalaciones
antiguas— **afirma algo falso sobre lo construido**. Si el propietario pondera más el efecto sobre
el archivo del consumidor que la falsedad del enunciado, **sube a `usuario/dinero`**; en los dos
casos bloquea.

**Residuo colateral, sin elevar:** por número, `NUEVO` es **inalcanzable** para §6 y §9 en todo el
histórico publicado (las dos secciones existen en todas las plantillas desde `08f17ee`). Por
título, sí es alcanzable —y es justo el caso F—. La afirmación «ninguno imposible» sólo es cierta
en la lectura por título, que es la que la instrucción no declara.

## 6 · Las dos contradicciones declaradas abiertas — verificadas

No las toqué. Mi verificación:

**`docs/seguridad/registro-seguridad.md:709` — CONFIRMADA, y acotada.** Ocurrencia **única** en el
archivo. Vive bajo «Obligación de write-back abierta (`AGENTS.md` §9)» y ata `SEC-009`, `SEC-010`,
`SEC-011` y el **veto de `REQ-005`** a un write-back «(vía `analista-requerimientos`)». Matiz que
añado: como esos hallazgos son **de seguridad** y exigen **NFR**, es muy probable que «quede una
decisión de requisitos o de diseño» y el analista **siga siendo el correcto**; la contradicción es
que el texto lo fija **incondicionalmente**, no que el rol sea el equivocado.

**REQ nuevo en las vías 2 o 3 — CONFIRMADA, y la reparación AMPLIÓ su superficie.** Sigue sin rol
asignado para fijar `Rigor:` y `Sensible a seguridad:` en un REQ que nace de una reparación, y el
disparador del auditor depende de ese flag. **Matiz material:** al reparar la observación del rigor,
el punto 2 nuevo **vuelve a nombrar al `analista-requerimientos`** como quien lo fija — y lo hace en
**dos sedes más** (`AGENTS.md`, `templates/AGENTS.md.tpl`) y en el resumen de `arnes-upgrade`. La
reparación es correcta en lo suyo, pero deja la contradicción **enunciada tres veces en vez de una**,
y ahora también **en el producto**. No es motivo para revertirla; es información para quien decida.

## 7 · Exclusión del experimento — CONFIRMADA otra vez, por diff

- `referencia/`: **ausente** en `c65ce65`.
- §13 de `AGENTS.md`: `sha256` **`6e206361…50fc3`**, **idéntico** al de la base `f387b1c`.
- `RUTA_ARNES` / `referencia/`: en **dos** archivos, `CHANGELOG.md` (la entrada que **declara** la
  exclusión) y este mismo informe de QA (que la **cita**). Ningún artefacto.

## 8 · Quality gates y banco

- **Gates §7: 4/4**, corridas por mí sobre `c65ce65`.
- **Banco, filtro `'44-*' '46-*'`: 103 PASS · 0 FAIL · 1 SKIP** — corrido por mí sobre `c65ce65`,
  **idéntico** a mi corrida sobre `a80a1cb`. El `SKIP` es el caso sólo-Windows (sin `cygpath`).
- No corrí el banco entero, no lo amplié y no investigué su no reproducibilidad.

## 9 · Hallazgos abiertos de esta vuelta

| Id | Sede | Clase | Bloquea |
|---|---|---|---|
| N-1 | `templates/requirements-README.md.tpl:236` | `contrato` | **sí** |
| N-2 | `skills/arnes-close/SKILL.md:29` | `contrato` | **sí** |
| N-3 | `description` de `agents/analista-requerimientos.md` y `agents/desarrollador.md` | `contrato` | **sí** |
| N-4 | `skills/arnes-upgrade/SKILL.md` — colisión de número de §9 | `contrato` | **sí** |

`H-1`…`H-8` y las dos observaciones: **cerrados**. No corregí nada.

## 10 · Qué NO acredita esta segunda vuelta

1. **No he migrado ningún proyecto.** Los casos A-F son la instrucción **aplicada a mano sobre
   copias temporales** de artefactos reales (base congelada y plantillas de tags publicados). `N-4`
   es un defecto **de la instrucción**, medido en papel; **no** es una corrida de `arnes-upgrade`
   observada fallando. Un ejercicio en papel puede errar sobre cómo un agente real interpretaría el
   texto — pero la ambigüedad número/título está **en el texto**, y eso sí queda medido.
2. **No acredita la publicación**, que sigue detenida, ni me pronuncio sobre ella.
3. **No acredita el banco completo** (~73 secciones sin correr aquí) ni `REQ-024`/`REQ-023`/`REQ-026`.
4. **No acredita que nada más herede la contradicción.** Barrí por propiedad sobre todo el árbol
   versionado; **no** barrí proyectos consumidores ya instalados, cuyo `AGENTS.md` está congelado y
   **sigue diciendo la regla vieja** hasta que migren.
5. **Ningún guardián del banco cubre `agents/*.md`, `templates/requirements-README.md.tpl` ni las
   skills.** `H-1`, `H-2`, `N-1`, `N-2` y `N-3` **no los caza ninguna máquina**: sólo se ven
   leyendo. Esta clase de defecto seguirá dependiendo de una lectura por propiedad en cada cambio.
6. **No acredita seguridad.** No soy el auditor y no emito `Seguridad:`.
7. **No propuse ampliar la política** ni la evalué fuera de la coherencia entre sedes.

---

# Tercera vuelta — verificación acotada de la reparación de `N-1`…`N-4`

- **Árbol medido:** mismo worktree, commit **`a34e18c`**, árbol limpio. Diff `8427e7a..a34e18c`, 7 archivos, +114/−11.
- **Alcance:** sólo esta reparación y sus casos afectados. No re-verifiqué lo acreditado sobre `c65ce65` ni `9d1d0a5`.

## Veredicto: PENDIENTE — un impedimento, y es uno solo

`N-1`, `N-2`, `N-3` y `N-4` están **reparados**. Los cuatro falsos positivos están **bien
descartados**. Las gemelas y los intactos **cuadran con la medida declarada, al byte**.

**El impedimento es el hallazgo que el desarrollador dejó abierto — y su clase no es `instrumento`.**
Al clasificarlo descubrí que no es sólo «la forma de `H-7` en otra entrada»: esa misma frase
**reabre `H-5`**, el defecto que detenía la migración entera. Es `contrato`, y bloquea.

## El impedimento · `skills/arnes-upgrade/SKILL.md:583` — `contrato`, bloquea

**Corrección de cita, antes de nada:** la frase está en la línea **583**, no en la 576, y pertenece
a la entrada **`### Hacia 1.32.0`** (línea 530), **no a la de 1.33.0**. Ocurrencia única, verificada.

> `templates/requirements-README.md.tpl` … y **las definiciones de los tres agentes que la aplican**
> — `analista-requerimientos` …, `qa-tester` …, `auditor-seguridad` …. Si personalizaste alguno,
> **el merge a tres vías te lo marcará: conserva tu texto y añade lo nuevo, que es aditivo.**

El desarrollador vio **una** de las dos cosas que esta frase hace mal, y por eso propuso
`instrumento`. Hace **tres**:

1. **La forma de `H-7`** — prescribe la acción («conserva tu texto y añade lo nuevo») sobre lo que
   el merge marca como conflicto. Eso solo sería `instrumento`.
2. **Afirma algo falso sobre lo construido:** que **el merge a tres vías marcará** una definición de
   agente personalizada. **No puede.** Es exactamente lo que `H-5` estableció y esta misma versión
   escribió en la entrada de 1.34.0: los agentes **los provee el plugin**, el proyecto no tiene copia
   propia y `arnes-init` no deja base suya en `.arnes/plantillas-origen/` (verificado: **0** agentes).
   Peor: los lista bajo *«lo que además llega **por plantilla**»*, y por plantilla no llegan.
3. **Reabre `H-5`, que es lo que lo hace bloqueante.** `## Migraciones conocidas` es acumulativa:
   un proyecto anterior a 1.32.0 que migre a 1.34.0 lee **esta** entrada y la de 1.34.0 **en la misma
   corrida**, y dicen lo contrario — «el merge te lo marcará» frente a «**no los clasifiques, no los
   busques y no los pongas en el plan**». Si gana la de 1.32.0, el agente intenta clasificar una
   definición de agente, no hay base, sale `UNKNOWN` y —con `H-6` ya reparado— **se detiene la
   corrida completa**. Es el fallo que `H-5` existe para evitar, alcanzable otra vez, y justo para
   **los proyectos más antiguos**, que son los que más necesitan esta migración.

**Y la contradicción no es sólo con la entrada nueva: es con el encabezado de la propia skill.** La
línea **10** ya dice *«Los hooks, **los agentes** y las skills viven **en el plugin** y se actualizan
solos»*. Barrido por propiedad sobre toda la skill: **tres sedes hablan de migrar agentes — la 10 y
la 1237-1240 dicen lo correcto, la 579-583 dice lo contrario.** Ocurrencia única, confirmada.

**Por qué lo clasifico `contrato` y no `instrumento`:** la clase se decide por si el documento
afirma algo falso sobre lo construido, y lo afirma. Su consecuencia no se queda en el instrumento:
**detiene la migración de un consumidor**. No lo arreglé, conforme a la instrucción.

**Y no es «de otra ventana».** El defecto vive en una entrada anterior, pero lo que queda
incompleto es **la reparación de `H-5` que esta versión entrega**: una promesa que una entrada
hermana desmiente, leída en la misma corrida, no está cumplida. Mover la ventana no la cumple;
tampoco la cumple documentarla.

## 1 · `N-4`, las tres comprobaciones por separado

### (1) El caso F ya no llega a `INTACTO` sin preguntar — **SÍ, reparado**

Ejercido otra vez sobre copias temporales, con la plantilla real de `v1.2.0` como base y proyecto:

| Lectura | Resultado |
|---|---|
| Clasificando **por número** (lectura hostil) | `INTACTO` → aplicar sin preguntar — el defecto viejo |
| Clasificando **la sección identificada** (lectura conforme) | la sección gestionada «Cambios de requerimientos» existe en el proyecto bajo **0** números y en la base bajo **0** → «no existía en la base y sí en el destino» = **`NUEVO` → Añadir** |
| Párrafo posterior a la tabla (eje base ↔ hoy) | base §9 ≠ hoy §9 → **no es `INTACTO`** → `NUEVO` |
| Blockquote previo a la tabla | nombra el caso y su desenlace: *«Identificar por título y contenido da `NUEVO` —Añadir—, que es lo correcto»* |

**Doy el caso F por reparado** porque la respuesta correcta está enunciada **tres veces**, dos de
ellas pegadas a la tabla (inmediatamente antes y después), y una **nombra el caso, el desenlace
erróneo y el correcto**. La lectura que aún falla exige haber «identificado por título y contenido»
y luego **descartar la identificación** para clasificar por número — no es una lectura de buena fe.

**Observación, sin elevar a hallazgo, porque el riesgo es futuro:** el paso que el texto declara
**obligatorio** («manda sobre la tabla») compara **proyecto ↔ base**, y en el caso F esas dos **sí**
son la misma sección, así que **ese paso pasa**. El eje que falla es **base ↔ hoy**, y va nombrado
sólo en la prosa que rodea la tabla. Hoy no bloquea porque la prosa está; **si alguien recorta esa
prosa en un cambio futuro, `N-4` vuelve** sin que nada lo señale.

### (2) Enunciada por propiedad, no como lista de versiones — **SÍ**

El blockquote abre por la propiedad —*«el arnés ha renumerado y retitulado secciones de `AGENTS.md`
a lo largo de su historia»*, con su razón: *«una lista de tags envejece; la propiedad no»*— y marca
el caso explícitamente como **«Caso real, como ejemplo y no como definición»**. No hay lista de
tags. Conforme a cómo este proyecto exige enunciar un control.

### (3) `UNKNOWN` conserva el tratamiento canónico — **SÍ, en las tres sedes**

- Canónica (línea 43-50): *«`UNKNOWN`, paras **y el documento se conserva**»*.
- Entrada 1.34.0: *«se detiene, **no se aplica nada** y el documento **se conserva intacto**»*.
- Fila de la tabla: *«Terminal … te DETIENES y NO se aplica NADA de toda la corrida, ni siquiera lo
  que salió `SAFE`»*.

**Y la doctrina se reutilizó, no se reinventó:** la entrada cita literalmente la regla de la línea
852 y añade *«Esto no es una regla nueva —es la de arriba, reutilizada»*; la sección canónica añade
*«Esta regla vale para **toda** entrada de migración, también las de abajo: ninguna la re-inventa»*.
Esa última frase es la que habría evitado el defecto de origen.

## 2 · `N-1`, `N-2`, `N-3`

| Id | Estado | Evidencia |
|---|---|---|
| `N-1` | **reparado, y gemelo** | Corregido en `templates/requirements-README.md.tpl` **y** en `requirements/README.md`. Medido: **6 líneas, 468 B, `sha256 ecf04195…eec2` idéntico** en las dos sedes — coincide exactamente con lo declarado |
| `N-2` | **reparado** | `arnes-close`: «devuélvelo para el write-back … **A quién depende de la vía**», con **«Lo bloqueante no cambia: sin el write-back no se entrega, venga de quien venga»**. Conserva la obligación al cambiar el rol |
| `N-3` | **reparado** | `analista`: añade la condición **y** la exclusión expresa («NO lo uses para el write-back de una reparación ya contratada»), e incluye el REQ nuevo nacido de una reparación. `desarrollador`: gana la vía de reparación **con la frontera de escalada** dentro del propio `description` |

### El `description` de `qa-tester` sin tocar — argumento **contrastado y aceptado**, con un matiz

**La conclusión es correcta: no hay que tocarlo.** Verificado contra la propiedad que barro: el
`description` de `qa-tester` **no atribuye ninguna fase ni ningún write-back a ningún otro rol**;
describe sólo la fase propia. Nada que corregir.

**Pero la razón que se ofrece no es la que sostiene la conclusión.** *«QA siempre va después del
dev, así que no hay misrouting posible»* **no discrimina**: el `auditor-seguridad` también va
después del dev y su `description` **sí** hubo que repararlo. Si ese argumento valiera, habría
exonerado al auditor. Lo que de verdad exonera a `qa-tester` es que **su texto no reclama el
write-back para nadie**, y por eso no puede desviarlo. Lo anoto porque un argumento que no
discrimina, reutilizado en el próximo barrido, **absolvería a una sede culpable**. No es hallazgo:
el producto está bien.

## 3 · Los cuatro falsos positivos — **bien descartados los cuatro**

| Sede (línea real) | Motivo alegado | Mi verificación |
|---|---|---|
| `agents/qa-tester.md:24` | criterio deficiente | **Correcto.** Un criterio intesteable o vago exige **decidir qué promete el REQ**; eso es decisión de requisitos → analista |
| `agents/qa-tester.md:44` | cambio DE FONDO | **Correcto.** §9 reserva el cambio de fondo al analista, con ADR. La frase ya dice «eso es un cambio DE FONDO» |
| `templates/requirements-README.md.tpl:84, :100, :103` | rigor y sensibilidad | **Correcto**, y además **coherente con la propia reparación**: el punto 2 de §6 restituyó ese procedimiento nombrando al analista. *Matiz:* estas tres líneas son la **superficie de la segunda contradicción ya declarada abierta** (un REQ nuevo de vías 2 o 3 no tiene rol asignado). Bien descartadas como defecto de write-back; siguen siendo el síntoma del hueco conocido |
| `skills/arnes-upgrade/SKILL.md:567` (citada 560) | Definition of Ready | **Correcto.** La DoR es del analista por su propia definición de agente |

**Ninguno mal descartado y ninguno mal corregido:** ninguna de las cuatro reparaciones tocó una
regla que estuviera bien.

## 4 · La comprobación conjunta — despacho, ejecución, cierre y actualización

Barrido final por propiedad sobre las cuatro superficies. **Dicen lo mismo.**

| Superficie | Sede | Resultado |
|---|---|---|
| **Despacho** | los cuatro `description` | analista **condicionado + exclusión expresa**; desarrollador **con la vía y la frontera**; auditor **sin depender de quién lo despache**; QA sin atribución ajena |
| **Ejecución** | `AGENTS.md` §6/§9, plantilla, cuerpos de los agentes | coherentes; §6 gemelo al byte |
| **Cierre** | `skills/arnes-close/SKILL.md` | «depende de la vía», obligación conservada |
| **Actualización** | `skills/arnes-upgrade/SKILL.md` | coherente **salvo la entrada de 1.32.0** — el impedimento de arriba |

Sedes residuales que atribuyen el write-back a rol fijo: **ninguna**. Los dos únicos aciertos del
barrido son `agents/desarrollador.md:3` (le concede el write-back, correcto) y
`skills/arnes-upgrade/SKILL.md:1222` (narra el cambio: «el `qa-tester` **deja de leer** que el
write-back es siempre del analista» — descripción, no regla).

## 5 · Gemelas e intactos — contrastados, no aceptados

| Afirmación del desarrollador | Mi medida |
|---|---|
| `N-1`: 6 líneas, 468 B, `sha256 ecf04195…` | **6 líneas, 468 B, `ecf04195b67b…eec2`** — coincide, y `cmp` sin diferencias |
| §6: 57 líneas, 4701 B, `sha256 b2c79314…`, sin cambios | **57 líneas, 4701 B, `b2c79314b489…b54a`** en las dos sedes, `cmp` limpio |
| `A`, `B` y la exclusión intactos | `git diff 8427e7a..a34e18c` devuelve **0 líneas** en `registro-seguridad.md`, `AGENTS.md`, `templates/AGENTS.md.tpl`, `autoalojamiento.md`, `agents/qa-tester.md` y `agents/auditor-seguridad.md` |
| Exclusión del experimento | §13 con `sha256 6e206361…50fc3`, **idéntico a la base `f387b1c`**; `referencia/` **ausente** |

## 6 · Quality gates y banco

**Corridos por mí sobre `a34e18c`**, no reutilizados: **gates §7 4/4** y banco
`'44-*' '45-*' '46-*'` → **103 PASS · 0 FAIL · 1 SKIP** (el `SKIP` sólo-Windows). Idénticos a lo
declarado. No corrí el banco entero, no lo amplié y no construí herramientas.

## 7 · Nota de método — tres citas con deriva de línea

No cambian la sustancia, pero quien re-derive este informe desde el disco no las encontraría:
`583` (citada `576`) y su entrada es **`Hacia 1.32.0`**, no 1.33.0; `567` (citada `560`).
`templates/requirements-README.md.tpl:84/100/103` sí son exactas.

## 8 · Hallazgos abiertos tras esta vuelta

| Id | Sede | Clase | Bloquea |
|---|---|---|---|
| N-5 | `skills/arnes-upgrade/SKILL.md:583` (entrada `Hacia 1.32.0`) | **`contrato`** | **sí** |

`N-1`…`N-4`: **cerrados**. `H-1`…`H-8` y las dos observaciones: cerrados en vueltas anteriores.
Las dos contradicciones declaradas abiertas siguen como las verifiqué en la segunda vuelta.
No corregí nada.

## 9 · Qué NO acredita esta tercera vuelta

1. **No he migrado ningún proyecto.** El caso F es, otra vez, **la instrucción aplicada a mano sobre
   copias temporales** de artefactos reales (base congelada y la plantilla del tag `v1.2.0`). **No es
   una corrida de `arnes-upgrade` observada.** `N-5` también está medido por lectura cruzada de dos
   entradas de la misma skill, no ejecutando la migración.
2. **No re-verifiqué lo acreditado sobre `c65ce65` ni `9d1d0a5`**, por el acotamiento del encargo.
   Mi acreditación de esas cabezas sigue siendo la de las vueltas anteriores, con sus límites.
3. **No acredita la publicación**, que sigue detenida, ni me pronuncio sobre ella.
4. **No acredita el banco completo** (~73 secciones sin correr aquí) ni `REQ-024`/`023`/`026`.
5. **Ningún guardián cubre `agents/*.md`, las plantillas de `requirements` ni las skills.** `N-1`…`N-5`
   **no los caza ninguna máquina**: se ven leyendo. `N-5` es la prueba de que esa clase de defecto
   sobrevive a un barrido cuidadoso cuando vive en una entrada que nadie consideró en alcance.
6. **No acredita seguridad.** No soy el auditor y no emito `Seguridad:`.

---

# Cuarta vuelta — verificación acotada de la corrección de `N-5`

- **Árbol medido:** mismo worktree, commit **`2e72934`**, árbol limpio. Diff `c39736c..2e72934`: 2 archivos, +73/−7; sustantivo sólo `skills/arnes-upgrade/SKILL.md` **576-593**.
- **Alcance:** únicamente esa corrección y su interacción con `H-5`. **No re-verifiqué `a34e18c` ni lo anterior.** `N-4` queda registrado y fuera de alcance.

## Método — sin ambigüedad, y separado

En **esta** vuelta hice **lectura cruzada de las instrucciones**, más mediciones mecánicas sobre el
disco (`git diff`, `cmp`, `sha256sum`, recuentos) y la corrida de las puertas y de una sección del
banco.

**NO ejecuté `arnes-upgrade`. NO migré ningún proyecto, ni real ni copiado. En esta vuelta no hice
ejercicio sobre copias temporales** — el de las vueltas anteriores (casos A-F) sigue siendo lo que
era y no se extiende a esta cabeza. Las tres cosas son distintas y no las mezclo.

## Veredicto: LISTO para la siguiente validación

**`H-5` está completo.** La vía que yo establecí —la acumulatividad de `## Migraciones conocidas`—
**está cerrada**, y lo verifiqué recorriendo las entradas yo mismo, no aceptando el recuento.

Queda **un residuo de clase `instrumento`**, que **no bloquea** y que registro abajo con su dueño
implícito: la frase que dice «aquí no se repite» **sí repite**.

## 1 · ¿Está `H-5` completo? — SÍ

**La vía que yo medí era ésta:** un proyecto anterior a 1.32.0 lee la entrada `Hacia 1.32.0` y la
`Hacia 1.34.0` **en la misma corrida**; la primera le decía que *«el merge a tres vías te lo
marcará»* para una definición de agente; intentar clasificarla no encuentra base, sale `UNKNOWN`, y
—con `H-6` ya reparado— **se detiene la corrida entera**.

**Está cerrada.** La entrada `Hacia 1.32.0` ya no manda clasificar nada: dice lo contrario, y con la
misma dirección que la de 1.34.0.

- La frase causante **ha desaparecido del archivo entero**: `grep 'añade lo nuevo, que es aditivo'`
  → **0 ocurrencias**. No se matizó: se retiró.
- El bullet nuevo (`:581`) abre por la conclusión: «**NO llegan por plantilla y NO entran en esta
  migración**».
- Y cierra la vía en seco (`:589`): «**No las clasifiques ni las busques en
  `.arnes/plantillas-origen/`**: no hay base suya que recuperar, y pedirle al merge que las
  clasifique sólo produce un `UNKNOWN`, que detiene la corrida entera». La consecuencia que yo medí
  está escrita **en el sitio donde alguien la provocaría**.

**Recuento propio, no el suyo.** Conté las entradas y barrí los tres temas yo:

- `## Migraciones conocidas` = líneas **188-1304**, **21 entradas** (`Hacia 1.16.0` … `Hacia 1.34.0`).
  **Mi recuento coincide con el suyo.**
- **Tema agentes** — sedes que enuncian una regla sobre migrarlos: **`:10`** («los agentes … viven
  **en el plugin** y se actualizan solos»), **`:581-593`** (1.32.0) y **`:1247-1251`** (1.34.0).
  **Las tres dicen lo mismo. 0 contradicciones.** Descartadas por asunto distinto, y verificadas una
  a una: `:15` (los agentes *leen* archivos), `:862` (referencias `§N` **por número**, que es
  doctrina de `N-4`, no de migrar agentes) y `:1230` (narra el cambio de 1.34.0).
- **Tema `UNKNOWN`** — 24 menciones. Todas dicen *detiene / no se aplica nada / terminal*, incluida
  la nueva de `:589`. **0 contradicciones.**
- **Tema personalizaciones** — todas dicen *no se toca / no se pisa / conflicto*, incluidas las
  nuevas de `:580` y `:591`, coherentes con `:889`, `:891`, `:1046` y `:1285`. **0 contradicciones.**

## 2 · La forma de la salida — referencia **y** transcripción, no sólo referencia

Ésta es la única reserva, y la presento tal cual.

**Lo que sí es referencia, y es correcto:** hay **dos punteros**, y ambos cargan peso —
«**como dice «Por qué existe» al principio de esta skill**» y «**el tratamiento vigente de los
agentes distribuidos está en la entrada `Hacia 1.34.0`**: se aplica **ése**».

**Lo que no cuadra:** la frase sigue «**y aquí no se repite**», y la oración siguiente **repite** la
regla operativa —no clasificarlas, no hay base, `UNKNOWN` detiene la corrida—. De modo que la sedes
que enuncian esa regla pasan de **una** (1.34.0) a **dos** (1.34.0 y 1.32.0), no a «dos correctas y
una referencia». **«Aquí no se repite» es falso sobre el propio párrafo en que está escrito.**

### R-1 · `skills/arnes-upgrade/SKILL.md:586-589` — clase `instrumento`. **No bloquea.**

- **Por qué no lo subo de clase:** nadie ve, decide ni cobra distinto. Las dos sedes **coinciden
  hoy**, y lo que se duplica son **hechos estables** —dónde viven los agentes y qué hace un
  `UNKNOWN`—, no el tratamiento mutable. El riesgo de deriva es real pero menor que el de `N-5`,
  que duplicaba una instrucción que **podía** cambiar y cambió.
- **Por qué lo abro igual:** es **la misma forma** que produjo `N-5` —una regla enunciada en dos
  entradas de la misma skill— y la frase que debería protegernos de ella **afirma lo contrario de lo
  que hace**. Si alguien confía en «aquí no se repite» para editar sólo la entrada de 1.34.0, la de
  1.32.0 queda atrás en silencio: exactamente `N-5` otra vez.
- **Y no recomiendo retirar la repetición**, porque lo repetido es el **suelo de seguridad** («no las
  clasifiques») y quitarlo dejaría al lector de 1.32.0 sin freno local. Lo que no es cierto es la
  frase que niega la repetición. **No lo arreglé**, conforme a la instrucción.

## 3 · Las dos mitades, contrastadas por separado

**Mitad A — las dos plantillas: la afirmación es VERDADERA.** Verificado en disco:
`.arnes/plantillas-origen/` contiene **`AGENTS.md.tpl`** y **`requirements-README.md.tpl`**, así que
**sí tienen base** y **sí entran** en el merge a tres vías. Y el tratamiento que les da (`:579-580`)
—«saldrán `MODIFICADO`, y eso es **conflicto** — tu texto **no se toca** y la decisión es tuya»—
coincide con la forma corregida de `H-7` y con `:1285`. **Confirmo: la forma de `H-7` también queda
corregida ahí**, porque la frase que prescribía la acción ya no existe en el archivo.

**Mitad B — las definiciones de agente: la afirmación es VERDADERA.** `.arnes/plantillas-origen/`
contiene **0** agentes; `arnes-init` copia sólo `.tpl`; los agentes los provee el plugin. Ni llegan
por plantilla ni pueden entrar en un merge que no puede clasificarlas.

**La separación de los dos grupos es el arreglo correcto**, y es lo que hacía falta: estaban
revueltos en un bullet que les aplicaba a los cuatro el mismo tratamiento, y sólo dos lo admitían.

## 4 · El proyecto que personalizó una definición de agente

**Lo que el texto dice** (`:590-593`): «esta migración **no la toca, no la sobrescribe y no la
actualiza** — se queda exactamente como la dejaste. Lo que **no** va a pasar es que el merge te
avise de ella, así que compararla con la definición nueva del plugin queda **a tu cargo**».

**Y es verdad**, verificado: los agentes quedan fuera del plan de migración, así que ninguna fase la
toca; y como no entra en el plan, el merge no puede avisar de ella. Barrido de toda la skill en
busca de un paso que escriba en `.claude/agents/`: **ninguno**.

**Está dicho, no prometido**, que es la forma correcta: declara la limitación —el usuario se queda
solo ante la comparación— en vez de vender una cobertura que no existe. Es justo lo contrario del
defecto original, que **prometía** un aviso que el merge no podía dar.

## 5 · Los intactos — contrastados, no aceptados

`git diff c39736c..2e72934` devuelve **0 líneas** en los **once** archivos que no debían moverse:
`AGENTS.md`, `templates/AGENTS.md.tpl`, `registro-seguridad.md`, `autoalojamiento.md`, los **cuatro**
agentes, `templates/requirements-README.md.tpl`, `requirements/README.md` y `arnes-close/SKILL.md`.

Y lo que el propietario dejó expresamente fuera de alcance, medido aparte para poder afirmarlo:

| Pieza | Medida |
|---|---|
| `N-4`: paso obligatorio + prosa que rodea la tabla (entrada 1.34.0) | **44 líneas, 4278 B, `cmp` sin diferencias** frente a `c39736c` — **intacta byte a byte** |
| Sección canónica «Clasificación» | `sha256 b06bc7cd…f40f9`, **idéntico** en las dos cabezas |
| §13 de `AGENTS.md` | `sha256 6e206361…50fc3`, **idéntico a la base `f387b1c`** |
| `referencia/` | **ausente** |

## 6 · Puertas y banco — corridos por mí sobre `2e72934`

**Gates §7: 4/4.** Banco con filtro `'45-*'` (arrastra `46`): **89 PASS · 0 FAIL · 1 SKIP**, el
`SKIP` sólo-Windows. **Coinciden exactamente con lo declarado.** No corrí el banco entero, no lo
amplié, no añadí herramientas ni criterios.

## 7 · Citas verificadas

Las mías, comprobadas contra el disco en esta cabeza: bullet de las plantillas **`:576`**; bullet de
las definiciones de agente **`:581`**, que termina en **`:593`**; puntero y frase «aquí no se
repite» **`:586`**; cierre de la vía **`:589`**. Entrada `Hacia 1.32.0` = **`:530`-`:633`** (la
siguiente, `Hacia 1.32.1`, abre en `:634`). Sección `## Migraciones conocidas` = **`:188`-`:1304`**.

## 8 · Hallazgos tras esta vuelta

| Id | Sede | Clase | Bloquea |
|---|---|---|---|
| R-1 | `skills/arnes-upgrade/SKILL.md:586-589` — «aquí no se repite» y a continuación repite | `instrumento` | **no** |

**`N-5`: cerrado.** `N-1`…`N-4`, `H-1`…`H-8` y las dos observaciones: cerrados en vueltas
anteriores. Las dos contradicciones declaradas abiertas siguen como las verifiqué en la segunda
vuelta. No cerré ningún hallazgo yo, no corregí nada y no comiteé.

## 9 · Qué NO acredita esta cuarta vuelta

1. **Lectura cruzada, no ejecución.** `H-5` está verificado **leyendo** las 21 entradas y el
   encabezado y comprobando que no se desmienten. **No ejecuté `arnes-upgrade` ni migré ningún
   proyecto**, y en esta vuelta **tampoco hice ejercicio sobre copias temporales**. Que las
   instrucciones ya no se contradigan **no acredita** que una corrida real se comporte así.
2. **No re-verifiqué `a34e18c` ni lo anterior**, por el acotamiento. Mi acreditación de esas cabezas
   es la de las vueltas previas, con sus límites.
3. **`N-4` está fuera de alcance**: sólo medí que **no se movió**, no lo revalidé. La observación
   que dejé en la tercera vuelta —el paso obligatorio compara proyecto ↔ base y el eje que falla es
   base ↔ hoy— **sigue en pie y sin verificar de nuevo**.
4. **No acredita la publicación**, que sigue sin autorizarse, ni me pronuncio sobre ella.
5. **No acredita el banco completo** (sólo `'45-*'` con su arrastre) ni `REQ-024`/`023`/`026`.
6. **Ningún guardián cubre las skills, los agentes ni las plantillas de `requirements`.** `R-1`,
   como `N-1`…`N-5`, **no lo caza ninguna máquina**: la única defensa de esta clase de defecto
   sigue siendo que alguien lea por propiedad, y por eso R-1 importa aunque no bloquee.
7. **No acredita seguridad.** No soy el auditor y no emito `Seguridad:`.

---

# Quinta vuelta — revisión acotada de la autorización por documento

- **Árbol medido:** mismo worktree, commit **`5101ece`**, árbol limpio. Diff `b2323f1..5101ece`: 11 archivos, +509/−36.
- **Alcance:** lo encargado y nada más. Es la única revisión de esta entrega.

## Método — sin ambigüedad, y separado en tres

1. **EJECUTADO:** las **tres** quality gates del manifiesto; el banco con filtro `'44-*' '45-*' '46-*'`;
   la re-derivación completa de su prueba de despacho (construcción de los dos proyectos temporales
   y `comprobar-autorizacion.sh` sobre cada uno); **dos casos de opt-out que él no probó**; y las
   mediciones mecánicas (`git diff`, `cmp`, `sha256sum`, `grep`, recuentos).
2. **LEÍDO / APLICADO A MANO:** la clasificación de los casos de la tabla de vías y el contraste de
   las instrucciones entre sedes.
3. **NO OCURRIÓ:** **no desplegué ningún agente**, **no ejecuté `arnes-upgrade`** y **no migré ningún
   proyecto**. Ningún proyecto consumidor fue tocado.

## Veredicto: PENDIENTE — dos impedimentos nuevos, presentados junto a los suyos

La implementación del diseño es **correcta en lo principal**: `SEC-093` llega a quien actúa, el
anclaje en `guard-completado` está retirado, las prohibiciones del propietario se respetan **las
seis**, y su prueba es **re-derivable: la reproduje y da exactamente sus cifras**.

Pero **la comprobación de autorización falla ABIERTA en un caso que medí**, y la frase que remedia
`SEC-094` admite una lectura que **estrecha** la obligación. Los dos son de texto y acotados.

---

## I-1 · La comprobación resuelve AUTORIZADA sobre un opt-out parcial — `contrato`. **Bloquea.**

**Medido, no razonado.** Construí dos proyectos desde `templates/AGENTS.md.tpl` y corrí su propio
script:

| Proyecto | Cita de autorización | Tabla de vías | Resuelve | rc |
|---|---|---|---|---|
| **opt-out total** (lo que la nota manda: «borra esta cita **y la tabla**») | ausente | ausente | `NO-AUTORIZADA` | **1** |
| **opt-out parcial** (borra sólo la cita) | **ausente** | presente | **`AUTORIZADA`** | **0** |

**Esperado:** borrar la cita **es** el acto de no declarar la vía; §6 promete que «**la ausencia de
la declaración no habilita nada**».
**Observado:** habilita. El proyecto queda autorizado contra su propia decisión.

**Y la evidencia sobre la que dispara es circular.** La forma (b) casa con esta fila:

> `| **Reparación con causa, alcance y contrato claros** | desarrollador → QA. **Sin comisión de analista** — sólo si este proyecto **autoriza** la vía (cita de arriba) …`

La fila **no afirma** autorización: la **condiciona a una cita que ya no existe**. Tomarla como una
forma que «responde que sí» es usar como prueba algo que remite a la prueba que falta.

**Segunda mitad, del mismo defecto:** la forma (b) casa **también con la prosa de la propia
comprobación** (línea 109 del documento generado: *«…o la tabla de vías de esta sección con la fila
«Sin comisión de analista»»*). Es decir, **un documento que sólo describa la comprobación, sin
autorizar nada, resuelve `AUTORIZADA`.** Lo verifiqué en la corrida: la forma (b) reportó **dos**
coincidencias, una de ellas esa.

**Dónde está y dónde no.** La **prosa de §6 es correcta por propiedad** —pregunta *«¿este documento
declara la vía y la autoriza?»*, dice que las dos formas son «**su evidencia, no su definición**» y
manda al analista si «la respuesta no es clara»—, así que un lector cuidadoso resuelve bien el
opt-out parcial. **Lo que está mal es (a)** nombrar la fila como una forma que responde que sí, ahora
que la fila es condicional, **y (b)** que el script la implemente como **disyunción**, produciendo el
fail-open medido. Y el script no es un detalle: es la evidencia que el informe ofrece como
re-derivable, y su `rc` es lo que la tabla de resultados publica como «Resuelve».

**Por qué bloquea:** el diseño entero se justifica por **fallar hacia el analista**. Un caso medido
en que falla hacia la vía contradice la promesa central. **No lo arreglé.**

> **Disposición del propietario sobre `I-1` (2026-09-14) — nota de la coordinadora, el veredicto de arriba no se
> toca.** El script `comprobar-autorizacion.sh` **se conserva como evidencia histórica, identificado como no válido
> para acreditar autorización**, sin repararlo ni usarlo como mecanismo del producto (cabecera del propio script y
> README). **El estado de `I-1` lo determina QA**, responsable del hallazgo, en la revisión final; **no se cierra por
> exclusión**.

## I-2 · La frase que remedia `SEC-094` admite una lectura que estrecha la obligación — `contrato`. **Bloquea.**

`AGENTS.md:187-189` y su gemela en la plantilla:

> interviene seguridad **siempre que** el **efecto** del cambio alcance algo que este proyecto
> declare crítico (§6) o una protección del arnés, **y siempre que** el **rigor efectivo** del REQ
> sea `critico` …

«**Siempre que** A, **y siempre que** B» admite dos lecturas en español, y **cambian la protección**:

- **Disyuntiva** (dos disparadores suficientes) — que es lo que se quiso: efecto **o** rigor. Correcta.
- **Conjuntiva** («siempre que» = *con tal de que*, ambas necesarias) — efecto **y** rigor. Y esa
  lectura es **más estrecha que el anclaje que `SEC-094` vino a retirar**: antes bastaba
  `rigor = critico`; con la conjuntiva haría falta además que el efecto alcance un crítico. Sería
  una **regresión**, no una remediación.

**El caso discriminante existe y su prueba no lo ejerció.** Un REQ **que ya existe** declarado
`Rigor: estandar` —y §6 dice que los REQ existentes «conservan sus clasificaciones»— cuya reparación
toca dinero: disyuntiva → **entra seguridad**; conjuntiva → **no entra**. Su caso `B-2` usó
`REQ-001`, que es `critico` / `sí`, **donde las dos lecturas coinciden**, así que la ambigüedad no
se tocó.

**Lo que sí está bien, y lo separo:** el núcleo de `SEC-094` —que la enumeración limite la
obligación— **está remediado**: la fila 3 va por propiedad («efecto que alcance un criterio de
`critico` de este proyecto o una protección del arnés») con los ejemplos **declaradamente no
exhaustivos** y la frase «no acotan la propiedad». Y `agents/auditor-seguridad.md:39` lo enuncia
**sin ambigüedad**: «*Si el efecto de un cambio alcanza algo que el proyecto declare crítico … o una
protección del arnés, **entras**, esté o no en la enumeración y venga por la vía que venga*».
**Es un conector en una sede, no un problema de diseño** — pero la sede es `AGENTS.md` §6, que es la
que lee la coordinadora, y es la sede sobre la que `SEC-094` se abrió.

---

## Lo que verifiqué y está BIEN

### (1) La comprobación de autorización, incluida la coordinadora — presente en las dos direcciones

- **§14 A gana el punto (5)** —«si el encargo **OMITE una fase** … ¿el `AGENTS.md` **DE ESTE
  PROYECTO** la autoriza expresamente?»— y el párrafo que se lo asigna a ella («**es ella quien
  decide el despacho, así que es ella quien comprueba el permiso: tenerlo escrito en la definición
  de un agente no lo concede**»), con registro obligatorio de **qué archivo leyó y qué resolvió**.
  **Y llega a la plantilla:** el bloque `A.` es **idéntico byte a byte** en las dos sedes
  (**15 líneas, 1272 B**).
- **Dirección 1 — sin declaración no habilita:** dicho en §6 («**La ausencia de la declaración no
  habilita nada**… el agente trae la **capacidad**, el documento da el **permiso**»), en §14 A, en
  `desarrollador.md`, `analista-requerimientos.md`, `qa-tester.md`, `auditor-seguridad.md`,
  `arnes-close` y `autoalojamiento.md`. **Medido:** proyecto A (plantilla de `f387b1c`) → `rc 1`.
- **Dirección 2 — ante contradicción tampoco:** «o que otra parte del **mismo documento** exija el
  analista para ese cambio **sin resolver expresamente** la contradicción» → analista. Y §6 **resuelve
  expresamente** la suya con el «todo REQ pasa por analista…» del autoalojamiento.
- **Y el write-back nunca queda sin dueño:** §9 y los cuatro agentes dicen que sin declaración
  **vuelve al analista**. Es la mitad que faltaba para que «fallar hacia el analista» sea cierto.

### (2) `SEC-093` — llega a quien actúa, y el pronombre está resuelto

- `agents/desarrollador.md:37-43` contiene ahora **`REQ NUEVO`**, **`Rigor:`** y
  **`Sensible a seguridad:`** (antes: ninguno de los tres), con la frontera dicha como frontera:
  «**es una frontera de rol, no un control mecánico**», y el motivo: sin esos campos «el disparador
  del `auditor-seguridad` **nace sin sujeto**». Hay además casilla en su checklist de entrega.
- El **pronombre ambiguo** de §6 —«es **él** quien define el contrato inicial», cuyo antecedente más
  próximo era el auditor— **desapareció de las dos sedes**: ahora nombra al sujeto y **excluye a los
  otros dos** («es el `analista-requerimientos`, **y no el `desarrollador` ni el
  `auditor-seguridad`**»).

### (3) `SEC-094` — el núcleo remediado y el anclaje retirado

- **`guard-completado` retirado como criterio:** `grep 'es lo que la máquina ya calcula'` → **0** en
  las dos sedes. Sustituido por el párrafo que explica **por qué** no sirve: puerta de **cierre**, no
  de despacho, y **inerte** sin `jq` o sin `.arnes/config.json`. Repetido en `auditor-seguridad.md`.
- **La colisión de filas resuelta, y con el caso que lo destapó:** «Si un cambio casa con más de una
  fila, manda la **MÁS RESTRICTIVA**… la reparación de un **bug de cobro** … fila 2 … y además toca
  dinero —fila 3—, y la vía que se aplica es la de la **fila 3**». Ejercido: con la enumeración
  anterior el bug de cobro terminaba en QA; con la fila 3 por propiedad, casa y manda la 3.
- Reserva: `I-2`, arriba.

### (4) La prueba práctica — re-derivable, y su frase es exacta

**La re-derivé entera** con el método que dejó escrito, y da **sus cifras**:

| Comprobación | Suyo | Mío |
|---|---|---|
| Proyecto A (plantilla `f387b1c`) | `NO-AUTORIZADA`, rc 1 | **`NO-AUTORIZADA`, rc 1** |
| Proyecto B (plantilla de esta cabeza) | `AUTORIZADA`, rc 0, formas en `:95` y `:102` | **`AUTORIZADA`, rc 0, `:95` y `:102`** |
| `grep -c "Sin comisión de analista"` en `f387b1c` | 0 | **0** |
| `grep -c "^| Naturaleza del cambio | Vía |"` en A | 0 | **0** |
| `grep -c "reparación"` en A | 0 | **0** |

**La frase de honestidad es exacta:** «*aquí no hay ningún «despacho ejecutado»: hay rutas
resueltas*». La declara **antes** de los resultados, separa `EJECUTADO` / `LEÍDO` / `LO QUE NO
OCURRIÓ`, titula la columna «**Ruta resuelta**» y la repite en «No acredita ningún despacho real».

**Un único término suelto, y no la contradice:** la línea 95 dice «Los tres mismos cambios …
**corridos** contra los dos proyectos». Leída sola sugiere ejecución. **No lo cuento como
contradicción** porque la frontera está declarada en el párrafo inmediatamente anterior, la columna
dice «Ruta resuelta» y la lista de «no acredita» lo repite. Lo dejo anotado por precisión.

**La corrección del salto de línea no ensanchó el reconocedor de más:** se limitó a **juntar la
frase** para que la forma (a) casara. `grep 'autoriza la vía proporcional de reparación'` la
encuentra **en una sola línea** en las dos sedes (`AGENTS.md:140`, `tpl:95`). No se añadió ninguna
tercera forma, ni comodines, ni normalización. **Lo que sí resultó demasiado ancho es la forma (b),
y eso es `I-1`** — no por la corrección del salto de línea, sino porque la fila dejó de ser
autónoma al volverse condicional.

### (5) Las prohibiciones del propietario — las seis respetadas

| Prohibición | Verificación |
|---|---|
| Sin claves de manifiesto | Ninguna sede de manifiesto en el diff; ninguna clave nueva en JSON |
| Sin mecanismos de versiones | Barrido del diff: **0** líneas con `arnes_version`, «a partir de la versión», «si tu versión» |
| Sin tocar el clasificador de migraciones | `skills/arnes-upgrade/SKILL.md` **no está en el diff** |
| `SEC-095`/`SEC-096` no cerrados | Declarado **dos veces** en el informe (`:137`, `:194`), con el motivo: **una** composición, sin copia propia de agentes y sin `arnes-upgrade` |
| Limitación de agentes personalizados explícita | §6 «Tres límites», punto 1, **en las dos sedes**, incluido el remate: «y si el coordinador tampoco la tiene (§14, límite (b)), **no la hace nadie**» |
| Ninguna afirmación de compatibilidad universal ni de coste cero | Barrido: **0**. Y van **negadas por escrito**: punto 2 «**NO está demostrada** la compatibilidad con definiciones de agente anteriores» y punto 3 «**No se afirma que cueste cero**» |

### (6) Las dos sedes que tocó de más — tocarlas era correcto, y están bien

- **`skills/arnes-close/SKILL.md:29-34`:** el cierre comprueba trazabilidad y no-deriva y **decide a
  quién devolver** un write-back ausente. Si no llevara la condición, diría lo contrario que §9 en
  un proyecto sin declaración. **Correcto tocarla**, y está bien: añade la condición y **conserva lo
  bloqueante** («sin el write-back no se entrega, **venga de quien venga**»).
- **`docs/gobernanza/autoalojamiento.md:167-183`:** es la **premisa** — §6 se declara transcripción de
  esa enmienda, y dejarla con la fila 3 enumerada y sin la condición habría dejado viva la premisa de
  la que `SEC-094` se deduce. **Correcto tocarla.** Recibe las tres cosas: fila 3 **por propiedad**,
  «manda la MÁS RESTRICTIVA», `guard-completado` descartado como criterio, y la declaración de que la
  vía «sólo rige donde el `AGENTS.md` del proyecto la declara». **Ninguna de las dos introduce
  afirmación nueva.**

### Gemelas y evidencia mecánica

- **§14 A:** 15 líneas, **1272 B**, `cmp` limpio — idénticas.
- **§6 (tabla → fin):** 100 líneas; **una sola divergencia**, en el límite 1 («**un** proyecto» /
  «**este** proyecto»), del mismo tipo editorial que «ArnesJuan» / «este proyecto». Nada más.
- **Divergencia deliberada confirmada:** el blockquote de autorización difiere a propósito —
  `AGENTS.md` cita la enmienda y resuelve la contradicción; la plantilla trae la **nota de opt-out**.
- **§13** sigue con `sha256 6e206361…50fc3`, **idéntica a la base `f387b1c`**.
- **Quality gates: 3/3**, leídas del manifiesto y ejecutadas una a una. **Confirmo su corrección: son
  tres**, no cuatro — la tercera agrupa `plugin.json` y `marketplace.json`. Yo también las venía
  contando mal en las vueltas anteriores de este informe.
- **Banco `'44-*' '45-*' '46-*'`: 103 PASS · 0 FAIL · 1 SKIP** (sólo-Windows), corrido por mí.

---

## Sus seis impedimentos: cinco confirmados, uno AMPLIADO

| # | Suyo | Mi verificación |
|---|---|---|
| 1 | `arnes-upgrade` (`:1226`, `:1231`) describe la vía **sin** la condición | **CONFIRMADO y AMPLIADO.** `grep` de la condición en toda la skill → **0**. **Y lleva un segundo defecto que él no declaró:** esa entrada describe la fila 3 con la **enumeración vieja** («*cambio que toca hooks, protecciones, firmas, permisos, instalación, migración o publicación*»), que es **la forma exacta de `SEC-094`**. Un proyecto migrado por ahí hereda **las dos** promesas retiradas |
| 2 | `SEC-093`/`094` escritos, sin validar | **Parcialmente resuelto por esta revisión:** yo los validé — `SEC-093` **remediado**; `SEC-094` **remediado en su núcleo** con la reserva `I-2`. Sigue sin revisión del `auditor-seguridad`, que no es mía |
| 3 | `SEC-095`/`096` no cerrados, una sola composición | **CONFIRMADO.** Declarado dos veces, con la mitad no ejercida nombrada (copia propia de agentes) |
| 4 | `SEC-097` pendiente de decisión del propietario | **CONFIRMADO.** No se tocó |
| 5 | `docs/ESTADO.md` § «⏸ RETOMAR AQUÍ» desfasado | **CONFIRMADO y AMPLIADO.** No sólo dice que no se vuelve a tocar `AGENTS.md`: declara el candidato **«@ `27bb49c`, DETENIDO y NO ADOPTADO»** y que el propietario **«no autoriza reparaciones»** — y la cabeza es `5101ece`, con esta reparación hecha. Está **más** desfasado de lo que él declara. **Hizo bien en no tocarlo** (bloque de la coordinadora) **y en no dejarlo en silencio** |
| 6 | Nada medido sobre despacho real | **CONFIRMADO**, y lo repito en mi propio «no acredita» |

## Hallazgos abiertos tras esta vuelta

| Id | Sede | Clase | Bloquea |
|---|---|---|---|
| I-1 | `AGENTS.md` §6 «Cómo se comprueba la autorización» (forma (b)) + `docs/arnes/via-proporcional-prueba-despacho/comprobar-autorizacion.sh` | `contrato` | **sí** |
| I-2 | `AGENTS.md:187-189` y su gemela en `templates/AGENTS.md.tpl` | `contrato` | **sí** |

No cerré ningún hallazgo, no corregí nada y no comiteé.

## Qué NO acredita esta quinta vuelta

1. **No desplegué ningún agente.** Que las instrucciones digan lo correcto **no acredita** que un
   agente real haga la comprobación, ni con qué profundidad. Es el límite que la propia entrega
   declara, y lo suscribo.
2. **No ejecuté `arnes-upgrade` ni migré ningún proyecto**, ni real ni copiado. Los proyectos
   `proy-antiguo`, `proy-proporcional` y los dos de opt-out son **copias temporales** sobre las que
   corrí un script y leí instrucciones.
3. **`I-1` está medido sobre el script**, que es una transcripción de la mitad mecánica — **no** sobre
   un agente aplicando la prosa. La prosa de §6, aplicada por propiedad, resuelve bien ese caso; lo
   que falla es la forma (b) como evidencia y su implementación disyuntiva.
4. **No acredita la compatibilidad con agentes anteriores ni el coste**, que la entrega declara
   expresamente no demostrados. No los medí.
5. **No acredita nada de `arnes-upgrade`**, que quedó fuera por prohibición y arrastra el impedimento 1.
6. **No corrí el banco entero** (sólo `'44-*' '45-*' '46-*'`), ni `REQ-024`/`023`/`026`.
7. **No acredita seguridad.** No soy el auditor y no emito `Seguridad:`. `SEC-093`…`SEC-097` siguen
   siendo suyos: yo verifiqué la **remediación de texto** de dos de ellos, no su cierre.
8. **No me pronuncio sobre la publicación**, que sigue sin autorizarse.

---

# Validación final — delta `b3efa23..2b56cb4`

- **Árbol medido:** mismo worktree, commit **`2b56cb4`**, árbol limpio.
- **Objeto:** el delta de dos commits (`f141511`, `2b56cb4`): 11 archivos, +133/−20.
- **No re-verifiqué nada anterior a `b3efa23`.** Mi 5.ª vuelta (sobre `5101ece`/`b3efa23`) sigue en pie con sus límites.

## Método — separado, como siempre

**EJECUTADO:** las **tres** quality gates del manifiesto; el banco `'44-*' '45-*' '46-*'`; el
re-ejercicio del **caso F** sobre copias temporales con la plantilla real de `v1.2.0`; y las
mediciones mecánicas (`git show`, `diff`, `cmp`, `sha256sum`, `grep`, localización de entradas).
**LEÍDO:** el delta completo y el contraste entre sedes.
**NO OCURRIÓ:** no desplegué ningún agente, no ejecuté `arnes-upgrade`, no migré ningún proyecto,
no toqué ningún proyecto consumidor. **No repetí los ensayos funcionales** de
`evidencia/prueba-despacho-2026-09-14`: los **reutilizo como declarados** y **no inspeccioné** ese
worktree — lo digo porque no puedo acreditar lo que no miré.

## Veredicto: FAVORABLE CON RESERVA

**No hay bloqueo.** `I-2` está **resuelto y verificado**; la separación de `SEC-097` es **correcta y
no debilita el caso F**; la precisión de «contrato claro» está **idéntica en las tres sedes
normativas** y cubre por sí sola mi caso discriminante.

**La reserva son dos hallazgos `instrumento`, ninguno bloqueante:** `I-1`, que **reclasifico y dejo
ABIERTO** —no se cierra por exclusión—, y `I-3`, nuevo, una cita que apunta a la entrada equivocada.

---

## 1 · `I-2` — RESUELTO

Texto final, idéntico en las dos gemelas:

> interviene seguridad cuando el **efecto** del cambio alcance algo que este proyecto declare crítico
> (§6) o una protección del arnés, **o** cuando las reglas vigentes de rigor y sensibilidad la
> exijan …. **Basta una de las dos condiciones; no se exige que coincidan.**

**La lectura estrecha ya no cabe.** La conjunción se sustituyó por **`o`** y, por si el conector
solo no bastara, una frase **cierra la interpretación** de forma explícita. `grep 'y siempre que el
**rigor efectivo**'` → **0** en `AGENTS.md` y **0** en la plantilla.

**Mi caso discriminante, ejercido por lectura** — REQ existente `Rigor: estandar` cuya reparación
toca dinero:

| Condición | Resultado |
|---|---|
| Efecto alcanza algo que el proyecto declara crítico (dinero) | **se cumple** → **entra seguridad** |
| Reglas de rigor y sensibilidad (`estandar`, `no`) | no se cumple |
| «Basta una de las dos» | → **entra seguridad** |

**Y queda cubierto dos veces,** porque `f141511` ataca el mismo caso por el otro lado: esa cabecera
**no tiene el contrato claro**, así que la fila 2 no aplica y la coordinadora pide al analista
**sólo esa decisión** antes de continuar. La redundancia aquí es correcta: son dos preguntas
distintas —quién revisa, y si el REQ está bien clasificado— y las dos se responden.

## 2 · `SEC-097` separado — CORRECTO, y el caso F sigue cerrado

**La retirada es exacta.** La sección canónica «Clasificación» quedó **restaurada byte a byte** a
`a34e18c^`: `sha256 4507acfc…5045c` idéntico. La rama `sec-097/clasificador-por-titulo-y-contenido`
existe y **conserva el párrafo** (`grep` → 1).

**El caso F sigue cerrado SÓLO con la entrada, y lo medí.** El bloque de identificación de «Hacia
1.34.0» es **idéntico al que ya validé en `c39736c`** salvo **una sola frase** (la de la cita), y
conserva las **tres** afirmaciones que cerraban el caso — ninguna de las cuales vivía en el párrafo
canónico retirado:

1. El paso de identificación por **contenido y título**, que «**manda sobre la tabla**».
2. El **blockquote previo a la tabla**, que nombra el caso (`## 9.` fue «Convenciones de trabajo»)
   y su desenlace: «Identificar por título y contenido da **`NUEVO`**».
3. El **párrafo posterior a la tabla**, con el eje que de verdad decide el caso F: «si el `## N.` de
   la base no es la misma sección que el `## N.` de hoy, **no es `INTACTO`**».

**Re-ejercido sobre copias temporales** con la plantilla real de `v1.2.0`: proyecto §9 «Convenciones
de trabajo», base §9 igual, hoy §9 «Cambios de requerimientos»; la sección de hoy existe en el
proyecto bajo **0** números y en la base bajo **0** → **`NUEVO` → Añadir**. Correcto.

**Conclusión: retirar el párrafo canónico no quitó nada de lo que cerraba el caso F.** Era
duplicación, y por eso su retirada es inocua — que es justo lo contrario de lo que habría pasado si
el cierre hubiera dependido de él.

## 3 · `I-1` — RECLASIFICADO a `instrumento`, y **ABIERTO**

Lo determino yo, como corresponde, y lo separo en las dos mitades que tenía.

**Mitad A — el script.** La disposición del propietario está aplicada y verificada: cabecera propia
del script («**EVIDENCIA HISTÓRICA — NO VÁLIDO PARA ACREDITAR AUTORIZACIÓN**… **NO es mecanismo del
producto, NO se repara, NO se usa para decidir nada**») y aviso al principio del README. **Un
artefacto que ya no acredita nada no puede acreditar en falso**, y el `rc` que yo medí deja de ser
evidencia de autorización. **Esta mitad queda neutralizada por retirada de alcance — no reparada, y
lo digo así a propósito:** documentar no es remediar; lo que cambia es qué promete el artefacto, no
su comportamiento.

**Mitad B — la prosa del producto, que sigue viva.** El delta **no tocó** el párrafo «Cómo se
comprueba la autorización». Sigue nombrando como forma que «responde que sí» a la fila «Sin comisión
de analista», y esa fila **ya no afirma autorización**: la condiciona a «la cita de arriba». Además,
la cadena aparece **en la prosa de la propia comprobación**. Esa imprecisión está en `AGENTS.md` §6,
en la plantilla y en `agents/desarrollador.md` — producto, no script.

**Por qué ya no bloquea.** El fail-open que medí exigía tratar las dos formas como una **disyunción
mecánica**, y el único artefacto que lo hacía está retirado. La norma pregunta por **propiedad**
—*«¿este documento declara la vía proporcional y la autoriza para este proyecto?»*—, declara las dos
formas «**su evidencia, no su definición**», y su cláusula de fallo cubre «**que la respuesta no sea
clara**». Un opt-out parcial es exactamente eso: la declaración no está y la fila remite a una cita
inexistente. **Una lectura conforme falla hacia el analista.** Nadie ve, decide ni cobra distinto.

**Por qué NO lo cierro.** Porque la imprecisión sigue escrita, y es la misma forma que produjo el
fail-open medido: quien trate la lista de formas como una **lista de comprobación** lo reproduce.
Cerrarlo sería cerrarlo por exclusión del artefacto, que es justo lo que la disposición prohíbe.

**Qué lo cerraría** —lo dejo escrito para que la deuda tenga contenido y no sólo dueño—: que la
forma (b) deje de figurar como evidencia afirmativa, **o** que la fila vuelva a ser autónoma (que
afirme la autorización en vez de remitir a la cita). Es un cambio de una línea en tres sedes.
**Dueño:** quien toque el párrafo de autorización de §6. **No lo arreglé.**

## 4 · `I-3` — NUEVO · la cita reanclada apunta a la entrada equivocada — `instrumento`. No bloquea.

`skills/arnes-upgrade/SKILL.md:1261` dice ahora:

> Esto no es una regla nueva —es la de la entrada **«Hacia 1.31.0»** (`## 14.`: «el número está
> tomado: `UNKNOWN`»), reutilizada aquí para esta migración—

**Medido:**

| Comprobación | Resultado |
|---|---|
| ¿Dónde vive «el número está tomado»? | línea **857**, dentro de la entrada **`### Hacia 1.34.0`** (`:748`) |
| ¿`Hacia 1.31.0` (`:399`-`:522`) la menciona? | **0** ocurrencias de `## 14.` y de «el número está tomado» |
| ¿La doctrina **preexiste** a este trabajo? | **SÍ** — está en `f387b1c`, línea 847, también bajo `Hacia 1.34.0` |

**Lo sustantivo es cierto y lo separo del defecto:** «no es una regla nueva» es **verdad** —la
doctrina precede a todo este trabajo—, así que la justificación de la separación de `SEC-097` se
sostiene. **Lo que es falso es el puntero:** quien lo siga hasta «Hacia 1.31.0» no encuentra nada.
La redacción exacta sería «es la de **esta misma entrada**», porque el ancla está 400 líneas más
arriba dentro de ella.

**Clase `instrumento` y no bloquea** porque la regla está **enunciada entera en su sitio** y el caso
F queda cerrado por tres vías independientes del puntero: nadie decide distinto. **Pero lo abro**
porque es la **tercera vez** que una referencia cruzada de esta misma skill es hallazgo —`N-5`,
`R-1`, ahora `I-3`—, y esa reincidencia es el dato, no la anécdota: en este archivo, la pieza que
falla no es la regla, es el puntero que la ata.

## 5 · La precisión de «contrato claro» (`f141511`) — CORRECTA

**Idéntica en las tres sedes normativas**, medido: **14 líneas, 1234 B, `sha256 df1f1d78…ac1bb`** en
`AGENTS.md`, `templates/AGENTS.md.tpl` y `docs/gobernanza/autoalojamiento.md`. No hay tres
redacciones que puedan derivar: hay una, transcrita.

**Hace lo que el propietario pidió, y no más:**

- **Define** «contrato claro» incluyendo que `Rigor:`, `Sensible a seguridad:` y las revisiones
  exigidas sean coherentes con el **efecto**, con el ejemplo nombrado —dinero sobre un REQ
  `estandar`/`no`/`n/a`— y el motivo: «la cabecera promete **menos revisión** que la que el efecto
  exige».
- **Acota la devolución:** «**únicamente esa decisión y su actualización documental**… **No se repite
  el análisis completo**».
- **No da facultades nuevas:** «ni el desarrollador ni QA corrigen esos campos por su cuenta, y el
  auditor conserva la suya de subir el rigor». Y las **referencias** lo respetan: `desarrollador`
  («**no la corrijas tú y no sigas como si encajara**»), `qa-tester` («tampoco corriges tú… **ni
  escribes `Seguridad: n/a` para que el hueco no se vea**» — es **hallazgo**, se devuelve),
  `analista` («entregas esa decisión y su actualización documental, **y nada más**»).
- **No altera la vía cuando el contrato sí es claro:** «una reparación ordinaria correctamente
  clasificada **sigue siendo desarrollador → QA sin analista**».
- **§14 A (5)** gana la segunda pregunta, y **llega a la plantilla**: el bloque `A.` es **idéntico
  byte a byte** en las dos sedes.
- **`arnes-upgrade`** recibe la definición **y** pasa su resumen de la fila 3 de la **enumeración
  vieja** a la propiedad («un criterio de `critico` del proyecto o una protección del arnés
  (ejemplos declaradamente no exhaustivos)»). **Esto cierra la mitad `SEC-094` de la ampliación que
  hice a su impedimento 1** en la 5.ª vuelta. *(La otra mitad —la condición de autorización ausente
  en esa entrada— sigue abierta y es suya.)*

## 6 · Mecánica

| Comprobación | Resultado |
|---|---|
| Quality gates del manifiesto | **3/3**, leídas y ejecutadas una a una |
| Banco `'44-*' '45-*' '46-*'` | **103 PASS · 0 FAIL · 1 SKIP** (sólo-Windows), corrido por mí |
| §13 de `AGENTS.md` | `sha256 6e206361…50fc3`, **idéntica a la base `f387b1c`** |
| Gemelas §14 A | **idénticas byte a byte** |
| Gemelas §6 (tabla → fin), 115 líneas | **una sola divergencia**, la editorial ya conocida del límite 1 («un» / «este» proyecto) |
| «contrato claro» en tres sedes | **idéntica**, `sha256 df1f1d78…` |
| Sección canónica «Clasificación» | **restaurada** a `a34e18c^`, `sha256 4507acfc…` |
| Mi veredicto de `I-1` en este informe | **intacto**; la nota del propietario va **después**, atribuida y fechada |

## 7 · Hallazgos abiertos

| Id | Sede | Clase | Bloquea |
|---|---|---|---|
| I-1 | `AGENTS.md` §6 «Cómo se comprueba la autorización» (forma (b)), su gemela y `agents/desarrollador.md` | `instrumento` *(reclasificado desde `contrato`)* | **no** |
| I-3 | `skills/arnes-upgrade/SKILL.md:1261` | `instrumento` | **no** |

`I-2`: **resuelto**. No cerré `SEC-093`…`SEC-097` —son del auditor, que va después de mí—, no
corregí nada y no comiteé.

## 8 · Qué NO acredita esta validación

1. **No desplegué ningún agente.** Que las instrucciones digan lo correcto no acredita que un agente
   real haga la comprobación ni con qué profundidad. **Y no verifiqué los ensayos funcionales de
   `evidencia/prueba-despacho-2026-09-14`:** los reutilizo **como declarados**, sin haber mirado ese
   worktree. Con su propia reserva declarada: **`CON-1c` es ambiguo y no concluye**, y **`CON-4` se
   conserva como evidencia de coste sin concluir que sus hallazgos fueran irrelevantes ni que su
   clasificación fuera correcta** — no me pronuncio sobre ninguna de las dos cosas.
2. **No ejecuté `arnes-upgrade` ni migré ningún proyecto.** El caso F es, otra vez, **la instrucción
   aplicada a mano sobre copias temporales** de artefactos reales. `I-3` está medido por
   localización de líneas y entradas en el archivo, no por corrida.
3. **No acredita la retirada de `SEC-097` más allá de lo medido:** verifiqué que la sección canónica
   volvió a su estado anterior, que la rama conserva el párrafo y que **el caso F no dependía de
   él**. **No** evalué si el clasificador debe llevar esa doctrina —eso es `SEC-097`, decisión del
   propietario, y sigue abierta.
4. **No acredita `SEC-093`…`SEC-097`.** Verifiqué **remediaciones de texto**; el cierre es del
   `auditor-seguridad`, que aún no ha revisado esta entrega.
5. **No acredita compatibilidad con agentes anteriores ni coste**, que la propia entrega declara no
   demostrados y yo no medí.
6. **No corrí el banco entero** (sólo `'44-*' '45-*' '46-*'`) ni revisé `REQ-024`/`023`/`026`.
7. **No me pronuncio sobre la publicación.**
