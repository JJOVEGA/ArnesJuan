# REQ-024 · Verificación de la CORRECCIÓN ESTRUCTURAL de `CA-13` — veredicto del `qa-tester`

**Base:** `/home/juan/dev/ArnesJuan-1.34-reparaciones`, rama `feat/1.34-cierre-alcance` @ **`587cbb0`**, árbol limpio.
**Fecha:** 2026-09-11.
**Vuelta:** **3 del bucle `analista-requerimientos` ↔ `qa-tester`** sobre el write-back de `QA-024-19`
(la 1 fue `QA-024-33`, la 2 la corrección de `QA-024-33`, ésta la 3). **NO gasta vuelta del contador
dev↔QA, que sigue en 5** — medido: `git diff a05994f HEAD -- hooks tools .github` sale **vacío**; ni
hubo entrega del desarrollador ni volvió el REQ a él. El presupuesto ordinario de **dos** vueltas de
`docs/gobernanza/autoalojamiento.md` § «Enmienda» **queda agotado**, y esta vuelta la autorizó
expresamente el propietario **por encima** de él.

**Veredicto:** **la corrección NO alcanza.** Queda **una** contradicción, medida, en el punto que el
propio analista trajo como corregido. Por instrucción del propietario —*«agotado el presupuesto no se
aprueba por agotamiento… lo que toca es una lista consolidada, no otra vuelta»*— **no pido otra
vuelta**: la lista consolidada va en §6.

**Reutilización declarada.** El código NO cambió desde `a05994f` (diff vacío), así que **no repetí**
las 56 celdas, ni las 41 formas del lector, ni el banco. Sólo ejercí lo necesario para las dudas de
esta comisión: **4 instrumentos, 66 celdas nuevas** sobre el hook real, todas con control en la misma
corrida. Instrumentos y salidas: `docs/qa/evidencia-req-024-ca13-587cbb0/`.

**Quality gates (§7):** las tres en **verde** (`bash -n` de `hooks/*.sh` y `tools/*.sh`;
`hooks/hooks.json`; `plugin.json` + `marketplace.json`). Árbol limpio, sin comitear.

---

## 1. Las cuatro comprobaciones de la comisión

### (1) ¿Remite en vez de afirmar, y a la sede correcta? — **PASA, con la excepción de §3**

- El titular ya **no** dice «el fail-closed deja de ser mudo». Declara que `CA-13` responde de **una**
  cosa —que disparador y lector reconozcan el mismo conjunto, y qué ocurre con el **aviso**— y remite:
  el **cierre** a `CA-01`, la **medibilidad** a `REQ-023`.
- **Las remisiones apuntan donde dicen**, verificado abriendo cada una:
  - `CA-01` es, en efecto, la sede de la **dirección de la ausencia** (tabla por campo). Intacto: el
    commit `587cbb0` **no toca una sola línea de `CA-01`** (`git show 587cbb0 -- requirements/REQ-024.md`
    no contiene ninguna línea de ese criterio).
  - `REQ-023` contrata en su `CA-…` (`requirements/REQ-023.md:40-41`) el **DENY citando esa línea**
    con el carácter en forma **imprimible**. Es la sede correcta de la medibilidad.
  - `docs/seguridad/registro-seguridad.md:6994-7055` es **exactamente** la entrada `SEC-078`
    (`:6994` = su título, `:7055` = su última línea, `:7056` ya es la sección 3).
  - `:7003-7012` es **exactamente** la tabla de veredictos del auditor (`:7003` = fila de cabecera,
    `:7012` = última fila, el control positivo).
  - `AGENTS.md` §13 (`:356`) es la celda que declara la vía abierta «tampoco 1.34.0».
- **Excepción:** en `(ii)` punto **(3)** queda **una** afirmación sobre una consecuencia ajena, y es
  falsa. Es `QA-024-35` (§3).

### (2) ¿La contradicción con `CA-01` resuelta, y por atribución? — **PASA**

`CA-13 (ii)` retira la frase «el cierre no pierde nada por ello: sigue denegando por ausencia» y
**no la sustituye por otra sobre el cierre**: deja escrito qué se prometía, por qué era falso y dónde
se contrata de verdad. Lo mismo en `(v)` con «el cierre la resuelve por ausencia, que es fail-closed».
`CA-01` **no se tocó** porque su tabla ya era verdadera. Comprobado en las tres sedes vivas que
`QA-024-34` señalaba, ahora `:853`, `:888` y la **heredada** `:1270-1274`: ninguna afirma ya una
consecuencia del cierre. **Ningún criterio del REQ dice dos cosas del mismo hecho sobre el rigor.**

### (3) ¿El homóglifo explícito, con su hallazgo NOMBRADO? — **PASA**

Lo **ejercí** contra el hook real, y además en los **dos estados** de `campos.ausencia_exige`, que el
criterio no declara (`salida-1.txt`, `salida-2.txt`):

| forma en la clave `Seguridad:` | `ligero` | `estandar` | `critico` | llave |
|---|---|---|---|---|
| `Ѕeguridad:` (U+0405) | **ALLOW (cierra)** | **ALLOW (cierra)** | DENY | `false` y `true`, idéntico |
| `Segurida:` (truncada) | **ALLOW (cierra)** | **ALLOW (cierra)** | DENY | `false` |
| `Segurídad:` (desfase) | **ALLOW (cierra)** | **ALLOW (cierra)** | DENY | `false` |
| control: clave limpia, `Seguridad:` ausente | ALLOW | ALLOW | DENY | `false` y `true` |

`CA-13 (v)` dice literal que ambas **CIERRAN el REQ** en `estandar` y en `ligero`, **DENY sólo en
`critico`**, y que **«eso no es fail-closed»**. **Verdadero, y más robusto de lo que afirma:** aguanta
también con la llave encendida. La vía va **nombrada** como `SEC-078`, no disuelta en el enlace, con
clase, dueño, forzador y vencimiento **citados** por ruta y rango. `SEC-078` **existe** y **dice lo
que se le atribuye**: `instrumento`, severidad alta, **abierto**; dueños `auditor-seguridad` (la
entrada, hecha) y `desarrollador` + `analista-requerimientos` (la vía); forzador el de `REQ-023`
§ «Fuera de alcance», declarado **disparado**; vencimiento la ventana **1.35.0** que decida el
propietario.

### (4) ¿`ZWSP`/`BOM` atribuidos al mecanismo que los trata? — **PASA**

`CA-13 (v)(1)` los atribuye a la **guarda de medibilidad** (`REQ-023`), que **deniega citando la
línea**, **también en `ligero`**. Medido, las tres formas en los tres rigores, 9 de 9 **DENY**
(`salida-1.txt` bloque D), y el motivo **cita la línea en forma imprimible** (`salida-4.txt`):

> `…declara el campo 'Seguridad:' con algo INSERTADO DENTRO DE LA CLAVE… La clave, con sus bytes ajenos
> en hexadecimal: «Seguri\xe2\x80\x8bdad:»… y NUNCA permite por AUSENCIA del campo que ese caracter borro.`

El reparto de `(v)` en **dos ramas que en `ligero` deciden al revés** —medibilidad **DENY**, ausencia
**ALLOW**— es exacto.

---

## 2. La comprobación que iba en dirección contraria — **medio PASA**

El analista trae que el punto `(3)` contrataba un aviso **sin condición** cuando la máquina lo escribe
**condicionado al rigor**. **Confirmado ejerciendo** (`salida-1.txt` bloque A), texto literal de los
dos avisos de `guard-completado.sh:287-292`:

| aviso de desfase | ¿lleva «si el rigor efectivo es critico»? |
|---|---|
| `QA:` (`qa: aprobado`) | **NO** — «…y el REQ no podra cerrarse por ausencia de veredicto de QA.» |
| `Seguridad:` (`seguridad: aprobado`) | **SÍ** — «…y, **si el rigor efectivo es critico**, el REQ no podra cerrarse…» |

Era, en efecto, **un criterio más laxo que lo construido**, y esa mitad queda corregida. **Pero no
quedó exacto en las dos direcciones:** la corrección introdujo una afirmación nueva que es falsa en el
otro eje. Es `QA-024-35`.

---

## 3. `QA-024-35` — `contrato`, **BLOQUEA**

**Título.** `CA-13 (ii)(3)` certifica como correcto un aviso que promete un cierre que la máquina no
tiene: la condición de `QA:` no es el **rigor** sino la **llave** `campos.ausencia_exige`, apagada por
defecto, y el criterio declara que ahí «no hay condición».

**Sede.** `requirements/REQ-024.md:812-818` (`CA-13 (ii)`, punto **(3)** y su paréntesis de
corrección). Sede única: la transcripción del Historial no cuenta como sede viva.

**Lo que el criterio afirma, literal:**

> «(3) nombra la **consecuencia sin prometer más cierre del que el cierre hace para ESA clave** —…y el
> REQ no podrá cerrarse por ausencia de ese veredicto **allí donde el cierre exija ese campo**, conjunto
> que **no es el mismo para las dos claves** y cuya sede es `CA-01` y no este criterio, de modo que el
> aviso **lleva la condición cuando la hay y la omite cuando no la hay**—»

**Pasos de reproducción** (`docs/qa/evidencia-req-024-ca13-587cbb0/2-eje-de-la-llave.sh`):

1. Proyecto con `.arnes/config.json` **sin** `campos.ausencia_exige`, o con `false` (**el defecto del
   arnés, y el estado de ESTE repositorio**: `.arnes/config.json` lo declara `false`).
2. `Edit` sobre un REQ escribiendo `qa: aprobado` (minúscula). → **ALLOW + aviso**, que dice, sin
   condición alguna: *«el REQ no podra cerrarse por ausencia de veredicto de QA»*.
3. Cerrar un REQ cuya cabecera **no declara** `QA:` (`Rigor: ligero`, `estandar` y `critico`).

**Esperado** (lo que el criterio certifica): la consecuencia anunciada por el aviso se cumple, porque
para `QA:` «no hay condición».

**Observado** (hook real, `salida-2.txt`):

| `campos.ausencia_exige` | `QA:` ausente, `ligero` | `estandar` | `critico` |
|---|---|---|---|
| **`false` (por defecto)** | **ALLOW — el REQ CIERRA** | **ALLOW — CIERRA** | **ALLOW — CIERRA** |
| `true` | DENY nombrando `QA:` | DENY | DENY |

Y con la línea escrita `qa:` en minúscula (el caso exacto del aviso), llave `false`: **ALLOW en los
tres rigores**. Con `true`: DENY en los tres.

**Por qué es `contrato` y no matiz de redacción.**

1. La glosa «**lleva la condición cuando la hay y la omite cuando no la hay**» es **medidamente
   falsa**: para `Seguridad:` es cierta —su única condición es el rigor, y el aviso la lleva; medido,
   la llave **no** cambia su veredicto en ningún rigor—, pero para `QA:` **la condición la hay** —es la
   llave— **y el aviso la omite**. El criterio la certifica correcta.
2. Es **la misma forma de defecto** que `QA-024-33` (pertenencia) y `QA-024-34` (consecuencia), ahora
   en la **condición**: se barrió el eje señalado —el rigor— y no la **clase** —«qué condiciona el
   cierre de ESTA clave»—. Es exactamente lo que la regla **(6)** que este mismo REQ acaba de escribir
   prohíbe: *«cuando la celda nombra una CONSECUENCIA, nombra el MECANISMO que la resuelve y no atribuye
   a uno solo lo que resuelven dos»*.
3. **El REQ trata ese eje como obligatorio en sus demás criterios y sólo aquí lo omite.** `CA-01`
   rotula su tabla «campo ausente, **llave encendida**»; `CA-12 (b)` exige «deniega **en los dos
   estados de la llave**»; `CA-05` mide los dos estados. `CA-13` menciona la llave **una sola vez**
   (`:913`) y es justo para **abstenerse** de comparar la tabla de `CA-01` con la del auditor «porque
   `CA-01` enuncia la suya con la exigencia encendida y la del auditor no declara ese estado». El
   criterio **sabe** que `CA-01` está condicionado a la llave, y aun así deriva de `CA-01` que para
   `QA:` «no hay condición».
4. **Efecto concreto, y va en la dirección que tranquiliza.** Un operador escribe `qa: aprobado`,
   recibe un aviso que le dice que el REQ **no podrá cerrarse**, y el REQ **cierra** — en el estado por
   defecto de todo proyecto instalado. Es la familia de `SEC-079`: una promesa medida falsa hacia el
   lado que abre, con el agravante de que el criterio ahora la **acredita**.

**Severidad:** alta. **Clase:** `contrato` (el REQ afirma algo falso sobre lo construido). **Bloquea.**

**No lo reescribo** (`AGENTS.md` §9: el write-back es del `analista-requerimientos`). Las salidas
posibles, con su coste, van en §6.

---

## 4. Lo que barrí yo, sede por sede, sin fiarme de la lista del analista

El analista declara **once** sedes. Comprobé que el commit `587cbb0` toca **sólo** `CHANGELOG.md`,
`docs/arnes/coste-de-comision/00-metodo-y-base.md` y `requirements/REQ-024.md`, con **once hunks** de
contenido en el REQ más el del Historial. Barrí por **propiedad** —`fail-closed`, `por ausencia`,
`sigue denegando`, `pasa sin aviso`, y toda frase que atribuya una **consecuencia** a un **mecanismo**—
y **fuera** del REQ, que es donde el analista no tocó nada:

| sede barrida | resultado |
|---|---|
| `REQ-024` `CA-13` entero (`:775-960`) | corregido salvo `(ii)(3)` → `QA-024-35` |
| `REQ-024` `CA-01` (`:36-60`) | **intacto y verdadero**; 0 líneas en el diff |
| `REQ-024` § capitalización (`:1035-1045`) | corregido: «esa medición dice **eso** y no que el cierre sea fail-closed para toda forma: **es de `critico`**» — verdadero, medido |
| `REQ-024` celda **heredada** `CA-12 (ii)(B)(b)` (`:1258-1290`) | corregida; **medida oración por oración** en §5 |
| `REQ-024` reglas de la celda (ahora **seis**) | la (6) es doctrina correcta y medida |
| `REQ-024` variante **(A)** histórica (`:1231-1240`) | marcada histórica y «no se transcribe» — correcto |
| `requirements/` (los demás REQ) | sin sede viva de esta falsedad |
| `AGENTS.md` §13 y `templates/AGENTS.md.tpl` | sin sede viva; la fila destino de `SEC-084` sigue siendo la corta |
| `skills/arnes-upgrade/SKILL.md` | sus `fail-closed` son de otra cosa (presupuesto de 64 KiB, `arnes-paralelo`) |
| `docs/seguridad/registro-seguridad.md` `SEC-078` | existe y dice lo atribuido |
| **`hooks/guard-completado.sh:277-280`** | **SEDE VIVA NUEVA, no nombrada por nadie** → `QA-024-36` |
| **`tests/…/42-disparador-y-lector.sh:54` y `:81`** | **SEDE VIVA NUEVA, no nombrada por nadie** → `QA-024-37` |
| `hooks/lib.sh:2206-2208` | ya nombrado por la coordinadora; **no** lo toco |

### `QA-024-36` — `instrumento`, **no bloquea**

`hooks/guard-completado.sh:277-280`, comentario de código: *«el REQ sigue sin ese campo y el cierre lo
deniega por ausencia: el camino fail-closed ya era correcto… Un fail-closed silencioso es un defecto de
diagnostico.»* Es **la misma falsedad** de `hooks/lib.sh:2206-2208` —la condición de rigor y la de la
llave, ausentes— en **otro archivo**, y nadie la había nombrado. Dueño: `desarrollador`. Va con su
gemela, en su propio ciclo: es un comentario, nadie ve, decide ni cobra distinto por él.

### `QA-024-37` — `instrumento`, **no bloquea**

`tests/escenarios/hooks/secciones/42-disparador-y-lector.sh`: el comentario `:54-56` («El cierre ya era
fail-closed… la ausencia deniega») y, sobre todo, **el rótulo que el banco publica** en `:81`
—`"42/B QA-024-19 el cierre sigue fail-closed: la minuscula no gobierna"`— generalizan sobre un fixture
que es `Sensible a seguridad: sí` → rigor **`critico`**, el único nivel donde eso es cierto. **El caso
mide bien y su control positivo es correcto**; lo que promete de más es la etiqueta, que es lo que un
lector del banco ve. No incumple `CA-13 (v)` —la forma medida es clase `desfase`, no clase `no`—, así
que es deuda de instrumento y no un fallo del criterio. Dueño: `desarrollador`.

---

## 5. `SEC-084` — **la mitad de TEXTO ya es transcribible.** Medida, no leída

La mitad de texto de `SEC-084` es la celda heredada cuyo **texto lo fija** `REQ-024 § CA-12 (ii) (B)
salida (b)` y cuyo destino son `AGENTS.md:353` y `templates/AGENTS.md.tpl:311` — hoy, las dos, todavía
la fila corta (`| Seguridad no firma lo que QA no ha validado (salvo `Seguridad: preventiva`) | §6 | …`).
La **medí oración por oración contra el hook real, en los dos estados de la llave**
(`3-celda-heredada-sec084.sh`, `salida-3.txt`):

| oración de la celda | medición | llave `false` | llave `true` |
|---|---|---|---|
| «tampoco cuando la línea `QA:` no llega a declararse: … se deniega nombrando el campo» | `QA:` ausente + `Seguridad: aprobado` | **DENY** nombrando `QA:` | **DENY** |
| ídem, vía del **comentario** `<!-- QA: aprobado -->` | | **DENY** | **DENY** |
| control negativo: `QA: pendiente` | | DENY (otro motivo) | DENY |
| **control positivo:** `QA: aprobado` | | **ALLOW** | **ALLOW** |
| «una línea que … el lector no [lee], esta guarda **NO la juzga**» | firma `seguridad:` minúscula | **ALLOW** | **ALLOW** |
| ídem, clase `no` | firma `Ѕeguridad:` (U+0405) | **ALLOW** | **ALLOW** |
| «si el lector la clasifica como **desfase** … el arnés **AVISA sin denegar**» | | ALLOW + aviso | ALLOW + aviso |
| «parte … las coge la guarda de **medibilidad**, que **deniega citando la línea**» | ZWSP/BOM/blanco | DENY citando | DENY citando |
| «el resto llega al cierre … donde manda la dirección de la ausencia, que **no siempre deniega** — medido: una clave con un **homóglifo** deja **CERRAR** el REQ por debajo de `critico`» | | **cierra** en `ligero`/`estandar` | **cierra** |
| «su clase, su dueño y su vencimiento viven en `docs/seguridad/registro-seguridad.md`» | `SEC-078` | existe, con los cuatro | — |

**Veredicto: sí, ya es transcribible.** La celda es **verdadera leída sola** en las dos direcciones y
en los dos estados de la llave, sus dos ramas están nombradas con el mecanismo que las resuelve, la vía
abierta va nombrada y no presentada como cerrada, y las seis reglas se cumplen.

**Con una salvedad nombrada, que NO impide transcribir:** la última oración remite a «la propiedad que
sí se garantiza está en `REQ-024 CA-13`», y `CA-13` tiene hoy `QA-024-35` abierto. **El defecto de
`QA-024-35` no viaja en el texto de la celda** —la celda no afirma nada sobre el **contenido del
aviso**, que es donde está—, así que la transcripción no propaga la falsedad. Si el propietario prefiere
no remitir a un criterio con un `contrato` abierto, eso es decisión suya, no una objeción de medición.

La transcripción sigue siendo del `desarrollador` con el gate humano de `CA-12 (ii)`.

---

## 6. LISTA CONSOLIDADA — lo que queda, su efecto y la decisión necesaria

No pido otra vuelta. Esto es lo que queda abierto de este bucle, con lo que hace falta para cerrarlo.

| # | qué | clase | efecto si se deja | decisión necesaria |
|---|---|---|---|---|
| 1 | **`QA-024-35`** — `CA-13 (ii)(3)` certifica un aviso que promete un cierre que la máquina no tiene con la llave apagada (el defecto) | `contrato` · **bloquea** | El REQ acredita como correcto un diagnóstico falso hacia el lado que **abre**, y esa acreditación es lo que un auditor lee al firmar. Familia de `SEC-079` | **Del propietario.** Tres salidas, y **no** son equivalentes (abajo) |
| 2 | `QA-024-36` — `hooks/guard-completado.sh:277-280`, la misma falsedad en un comentario | `instrumento` | Nadie ve, decide ni cobra distinto. Pero es la **tercera** sede del mismo texto | Cola, dueño `desarrollador`, con su gemela `lib.sh:2206-2208` |
| 3 | `QA-024-37` — el **rótulo publicado** del caso `42/B` generaliza sobre un fixture `critico` | `instrumento` | El banco publica una etiqueta más ancha que lo que su fixture mide | Cola, dueño `desarrollador` |
| 4 | `SEC-084` mitad de texto | `contrato` (ajeno a este bucle) | — | **Desbloqueado por esta verificación:** ya es transcribible. Falta la transcripción del `desarrollador` + gate humano de `CA-12 (ii)` |

### Las tres salidas de `QA-024-35`, con su coste

- **(a) Corregir el criterio** (`analista-requerimientos`, sin tocar código): que `(3)` **no afirme**
  qué condiciona el cierre y **remita**, como ya hace el titular — p. ej. exigir que el aviso no
  prometa más cierre del que su sede (`CA-01`) sostiene **en el estado de la llave del proyecto**, sin
  declarar desde aquí cuál es. Es **una vuelta más del bucle ya agotado**, y deja el **mensaje real**
  igual de inexacto: el criterio dejaría de acreditarlo, pero nadie lo arreglaría.
- **(b) Corregir el mensaje** (`desarrollador`, toca `hooks/guard-completado.sh:288`): que el aviso de
  `QA:` lleve su condición, como ya la lleva el de `Seguridad:`. Cierra el defecto **de raíz** y deja
  `(3)` verdadero tal como está escrito. Coste: **gasta vuelta dev↔QA** (sería la 6), entrega de
  código, y `CA-13 (iii)` obliga a acreditar que **ninguna decisión se mueve** — es un cambio de
  **texto de mensaje**, así que esa acreditación es barata, pero existe.
- **(c) Declararlo residual** con dueño, forzador y vencimiento. **Lo desaconsejo y lo digo con su
  motivo:** un `contrato` no se declara residual por definición (`AGENTS.md` §6), y el forzador natural
  —«cuando alguien encienda la llave»— se arma al revés: **hoy**, con la llave apagada, es cuando el
  mensaje miente.

**Mi recomendación, que no es la decisión:** **(b)**, y **(a) en el mismo acto** si el analista quiere
que `(3)` no vuelva a hablar del cierre. (b) sola deja `(3)` verdadero; (a) sola deja el mensaje
mintiendo. Es la misma lección que este ciclo lleva cobrando: *documentar no es remediar*.

---

## 7. Estado de los dos hallazgos que esta comisión podía cerrar

- **`QA-024-34` — CIERRA.** Sus **dos** extremos están remediados y verificados **ejerciendo**:
  (a) `CA-13` ya no llama `fail-closed` a la clase, y dice explícitamente que **no lo es**, con la
  conducta medida (`ALLOW` en `estandar` y `ligero`, `DENY` sólo en `critico`); (b) el reparto «por
  ausencia» se parte en las **dos** ramas que en `ligero` deciden al revés, cada una con su mecanismo
  nombrado. Las **tres** sedes vivas que abrió, corregidas, incluida la **heredada**. Se retira del
  campo.
- **`QA-024-19` — NO CIERRA.** El código lo tengo acreditado desde la vuelta 5 y no se ha movido; sus
  otras condiciones siguen cumplidas. Pero su write-back **es** `CA-13`, y `CA-13 (ii)` es justamente
  el criterio que contrata **el aviso** que resuelve `QA-024-19` — el `ALLOW` + aviso que sustituyó al
  `DENY` que el hallazgo pedía al nacer. Uno de los **cuatro** elementos que ese criterio contrata del
  aviso, el **(3)**, describe mal el mensaje. Un write-back que afirma algo falso **sobre la cosa que
  contrata** no cierra el hallazgo que lo exigía. **El motivo está acotado: es UN elemento de UN punto,
  no los cuatro**, y no se le imputa ningún defecto nuevo al código.
