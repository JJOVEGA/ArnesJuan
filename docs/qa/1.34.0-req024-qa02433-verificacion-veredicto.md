# QA — verificación de la CORRECCIÓN de `QA-024-33` (`REQ-024`, `CA-13` y sus siete sedes)

**Worktree:** `/home/juan/dev/ArnesJuan-1.34-reparaciones` · **rama** `feat/1.34-cierre-alcance`
· **commit verificado** `f18dd45` · árbol limpio · 2026-09-11.
**Objeto:** la SEGUNDA entrega del write-back de `QA-024-19` (`7154769..f18dd45`), que dice corregir
la frontera falsa que abrí como `QA-024-33`.
**Veredicto:** **`QA-024-33` CIERRA.** **`QA-024-19` NO CIERRA.** Nace **`QA-024-34`** (`contrato`).

---

## 0. El contador — declarado, no dejado al lector

**Esta verificación es la VUELTA 2 del bucle `analista`↔QA sobre este write-back, y NO gasta vuelta
del contador dev↔QA, que sigue en 5.**

Aplico el mismo criterio que ya apliqué —el contador se lleva **por el trabajo, no por la etiqueta**—
y lo digo entero, en sus dos mitades, porque la mitad cómoda sola sería exactamente el renombre que
este proyecto prohíbe:

1. **No gasta vuelta dev↔QA.** Medido: `git diff a05994f f18dd45 -- hooks tests` → **salida vacía**.
   No hay entrega del `desarrollador`, el REQ no volvió a él, y el árbol de código es byte a byte el
   que ya acredité entero en la vuelta 5.
2. **Sí es una vuelta, y la numero.** Es la **segunda** entrega del `analista-requerimientos` sobre
   **el mismo** write-back, y la primera quedó desmentida por medición. Eso es, por definición, una
   vuelta de retrabajo: llamarla «verificación acotada» o «cierre de la vuelta anterior» sería
   esconderla. `QA-024-33` abrió la vuelta 1 de ese bucle; ésta es la vuelta 2, y `QA-024-34` abre
   la **vuelta 3**.

**Observación para el propietario, no hallazgo, y ya escalada:** ese bucle `analista`↔QA **no tiene
tope en `AGENTS.md` §6**, porque el tope está escrito sobre dev↔QA. Va camino de su tercera vuelta
sin que ninguna puerta lo acote. Si debe tenerlo, es decisión suya; yo sólo mantengo el número
auditable desde el disco.

---

## 1. Lo que verifiqué EJERCIENDO, que es lo que faltó la vez anterior

`QA-024-33` nació de afirmar una propiedad «por construcción» sin ejercer la construcción. No repetí
el error: **no leí el criterio contra el código, le pasé formas al lector y miré qué clasifica**, y
después ejercí **el hook real** en los dos actos.

- **Instrumento 1 — el lector.** `docs/qa/evidencia-req-024-qa02433-f18dd45/ejerce2.sh`: carga
  `hooks/lib.sh` e invoca `arnes_declara_clave "<cabecera>" "<clave>"` con la clave variada y todo lo
  demás fijo; publica además el plegado de la clave sola (`arnes_norm_campo`) para contrastar la
  **glosa** del criterio. **41 formas.** Salida íntegra en `salida-lector-41-formas.txt`.
- **Instrumento 2 — el hook.** `…/e2e.sh`: proyecto de prueba propio (manifiesto con
  `quality_gates:["true"]`), `hooks/guard-completado.sh` alimentado por stdin con un `PreToolUse`
  real, en el acto de **FIRMAR** (¿hay `systemMessage`?) y en el de **CIERRE** (¿`deny`?, ¿con qué
  motivo?). **14 + 12 celdas.** Salida íntegra en `salida-hook-e2e.txt`.
- **Determinismo:** repetido; mismo resultado. Ningún caso flaky.
- **Código sin cambios** desde `a05994f`, así que **no repetí las 56 celdas ni el banco entero**.
  **Gates §7 en verde** (las corrí igual, porque el veredicto se firma sobre un árbol): `bash -n`
  sobre los 10 `hooks/*.sh` y `tools/*.sh` → OK; `jq -e .` sobre `hooks/hooks.json`,
  `.claude-plugin/plugin.json` y `.claude-plugin/marketplace.json` → OK.

---

## 2. Comprobación 1 — ¿la frontera es ahora VERDADERA? **SÍ.** Medida en los dos bordes

El criterio enuncia ahora la frontera sobre la **clasificación del lector**: queda fuera del aviso
«exactamente la línea que el lector clasifica **`no`** —ni la lee como el campo, ni su plegado
devuelve la clave—».

### 2.1 Los cinco ejemplos que el criterio declara: **5 de 5 correctos**

| forma | `ARNES_DECLARA` | ¿avisa el hook real? | lo que dice `CA-13 (v)` |
|---|---|---|---|
| `Ѕeguridad:` (`U+0405`) | `no` | **no** | «pasa sin aviso» — **correcto** |
| `Segurida:` (truncada) | `no` | **no** | «pasa sin aviso» — **correcto** |
| `Segurídad:` NFC | `desfase` | **sí** | «sí avisa» — **correcto** |
| `Segurídad:` NFD (`i`+`U+0301`) | `desfase` | **sí** | «sí avisa» — **correcto** |
| `Séguridad:` NFC **y** NFD | `desfase` | **sí** | «sí avisa» — **correcto** |
| `QÁ:` NFC **y** NFD | `desfase` | **sí** | «sí avisa» — **correcto** |

### 2.2 Las que busqué yo: **36 formas más, y la glosa aguanta 41 de 41**

En **todas** las formas ejercidas se cumple, sin excepción: `desfase` ⟺ el plegado devuelve la clave
y la línea no se lee literal; `no` ⟺ el plegado **no** la devuelve. La glosa del criterio describe
lo que el código hace.

**Avisan** (clase `desfase`), y varias desmentirían cualquier reformulación «lingüística»:
`Segúridad:`, `SegurIdad:`, `SEGURÍDAD:` (mayúscula acentuada), `Següridad:` **NFC y NFD**
(diéresis), `Segurîdad:`, `Segurĩdad:`, `_Segurídad_:` y `**Segurídad**:` (acento **+** marcado),
` Segurídad :` (acento + blancos), `Segur idad:` (blanco de más), `Qá:`, `QÀ:`, `QÄ:`, `_QÁ_:`.

**No avisan** (clase `no`): `Seguriñad:` y `Seguridañ:` (**la `ñ` no se pliega** — el plegado cubre
las cinco vocales, no la eñe), `Segurīdad:` (macrón `U+0304`), `Seguri̇dad:` (`U+0307`),
`Segurıdad:` (i sin punto `U+0131`), `Sеguridad:` (`е` cirílica `U+0435`), `Seguridadd:`,
`Seguri<ZWSP>dad:`, `<BOM>Seguridad:`, `QÅ:` (anillo), `QĀ:` (macrón NFC), `QA`+`U+030A`.

La `ñ` y la diéresis son el par que más me interesaba y salen **en lados opuestos**: `Següridad:`
avisa (la diéresis **sí** está en `arnes_pliega_ortografia`) y `Seguriñad:` no (la `ñ` **no**). Un
criterio enunciado por «letra acentuada» habría vuelto a fallar; el enunciado por la clasificación
del lector los coloca a los dos bien **sin nombrarlos**.

### 2.3 El disparador, ejercido end-to-end: **aviso ⟺ `desfase`, 13 de 13**

Sobre `hooks/guard-completado.sh` real: **8 de 8** formas `desfase` producen `systemMessage`
citando la línea y **ninguna deniega**; **5 de 5** formas `no` producen **silencio**. El disparador
del aviso es literalmente `[ "$qa_decl" = 'desfase' ]` / `[ "$seg_decl" = 'desfase' ]`
(`guard-completado.sh:286,289`), sin ninguna cadena intermedia: la frase del criterio «el aviso se
enuncia sobre lo que el lector clasifica como `desfase`» es **verdadera y es la única vía**.

**Conclusión de la comprobación 1: la frontera quedó verdadera.** No encontré ninguna forma que el
criterio deje en el lado equivocado **del aviso**.

---

## 3. Comprobación 2 — ¿`CA-13 (ii)` dejó de enumerar? **SÍ**, y no quedó lista equivalente

*Antes:* «el mismo plegado … **(blancos, marcado, caja)**» — tres elementos, sin marca
`no exhaustivo` ni puntero: la forma **(a)** que `requirements/README.md:310-316` prohíbe.

*Ahora* (`:820-825`): **«Los pasos de ese plegado NO se enumeran aquí, y no por brevedad sino por
defecto medido (`QA-024-33`)»**, con (1) el **sitio único** nombrado —`arnes_norm_campo`,
`hooks/lib.sh`—, (2) «el caso los **deriva ejerciéndolo**» y (3) los ejemplos marcados **no
exhaustivos**, ya con el cuarto paso dentro. Es la forma correcta del README, y el criterio dice
**por qué** la usa citando el hallazgo.

**Barrido de listas equivalentes**, por la cadena y por la propiedad
(`blancos, marcado` · `marcado, caja` · `blancos y marcado` · `marcado y caja`): **no queda ninguna
en `requirements/`**. Los dos restos son ajenos y los verifico uno a uno:
- `hooks/lib.sh:2201` — comentario de código que lista los **cuatro** pasos. Completo y correcto; no
  es criterio.
- `hooks/guard-completado.sh:287,290` — el texto del aviso dice «el marcado de Markdown sí se tolera
  pero la CAJA no» y no menciona los acentos. **Sigo sin abrir hallazgo**, por la misma razón que la
  vez anterior y por coherencia: no afirma exhaustividad, es un mensaje y no un contrato ni un
  disparador. Lo repito aquí sólo para que quien redacte no lo tome por sitio único del plegado.

---

## 4. Comprobación 3 — la celda de superficie heredada (`CA-12 (ii)`, salida (b))

**Cierta en lo que `QA-024-33` señalaba, y por eso ese hallazgo cierra:** el reparto ya no se hace
por «se ha sustituido u omitido una letra» sino por «**QUÉ le ocurre lo decide el LECTOR y no la
vista**», con las dos ramas nombradas por su clase. La regla **(5)** que se añade a esa sección
—«cuando la celda reparte la conducta en dos ramas, el criterio del reparto es la clasificación del
lector, nunca una noción que la máquina no computa»— es la doctrina correcta y nace medida.

**Completa, no.** Su última oración es la mitad de `QA-024-34` (§5), y es **la peor de las dos
sedes** porque viaja a `AGENTS.md` §13, a `templates/AGENTS.md.tpl` y de ahí a todo proyecto.

---

## 5. `QA-024-34` — **NUEVO** · clase **`contrato`** · bloquea

### La frontera quedó bien trazada y mal CONSECUENCIADA: lo que el criterio dice que le pasa a la clase `no` es falso, medido, y en las dos direcciones

Corregí la pertenencia a la clase y no miré qué se afirma **de** la clase. El criterio dice, textual:

> `REQ-024.md:863-864`, `CA-13 (v)`: «… **pasa sin aviso**, y el cierre la resuelve **por ausencia**,
> que es **fail-closed** y es (iii).»
> `REQ-024.md:1192-1193`, **celda HEREDADA**: «… pasa **SIN** aviso y **el cierre resuelve por
> ausencia**».
> `REQ-024.md:831`, `CA-13 (ii)`: «**El cierre no pierde nada por ello:** sigue **denegando por
> ausencia**».

**«Pasa sin aviso» es verdad** (§2.3). Lo que sigue **no**, y se cae por sus **dos** extremos.
Medido con el hook real (`salida-hook-e2e.txt`), un REQ con `QA: aprobado`,
`Hallazgos abiertos: (ninguno)` y el veredicto de seguridad escrito con la clave variada:

| forma (toda la tabla es clase **`no`**) | `Rigor: critico` | `Rigor: estandar` | `Rigor: ligero` | cómo lo resuelve el cierre |
|---|---|---|---|---|
| `Ѕeguridad:` (`U+0405`) | DENY | **ALLOW — CIERRA** | **ALLOW — CIERRA** | **por ausencia** ✓ |
| `Segurida:` (truncada) | DENY | — | **ALLOW — CIERRA** | **por ausencia** ✓ |
| `QĀ:` (macrón) | DENY | — | — | **por ausencia** ✓ |
| `Seguri<ZWSP>dad:` | DENY | DENY | **DENY** | **«algo INSERTADO DENTRO DE LA CLAVE»** ✗ |
| `<BOM>Seguridad:` | DENY | — | **DENY** | **«algo INSERTADO DENTRO DE LA CLAVE»** ✗ |
| `Segur idad:` (blanco de más) | DENY | — | **DENY** | **«algo INSERTADO DENTRO DE LA CLAVE»** ✗ |

**(a) «que es fail-closed» es falso, y falso hacia el lado que PROMETE DE MÁS.** Dos miembros
medidos de la clase —el homóglifo y la truncada— **cierran el REQ** en `estandar` y en `ligero`.
Quien lea `CA-13 (v)` aislado (que es como su propio titular manda leerlo, `REQ-023 CA-10`) concluye
que una clave con homóglifo **nunca** deja cerrar. Cierra.

**Y no hace falta salir del documento para saberlo: `CA-01` de este mismo REQ lo declara
medidamente falso**, en negrita y con su tabla — «**"No abre" NO es "deja de cerrar"**» y «`Seguridad:`
… **ALLOW por debajo de ese nivel**, igual que con su valor declarado». `CA-13 (v)` reintroduce, dos
criterios más abajo, exactamente la falsedad que `CA-01` se quitó de encima. **Dos criterios del
mismo REQ dicen cosas opuestas sobre el mismo hecho.**

**(b) «el cierre la resuelve por ausencia» es falso para el otro subconjunto.** Las tres formas con
algo insertado en la clave **no** las resuelve la ausencia: las coge la guarda de medibilidad y
deniega **citando la línea**. Y esa distinción **no es terminológica en este repositorio**: está
escrita, con su motivo medido, en el propio código —`hooks/lib.sh`, § «LO QUE NO TOCA»: «**NO deniega
por AUSENCIA**: publica que la cabecera no se puede medir … Un `deny` por "el campo falta" **sería un
`allow` con otro nombre** en cuanto el rigor declarado fuese `ligero`»— y en `AGENTS.md` §13. La
tabla de arriba **lo mide**: en `ligero`, «por ausencia» **abre** y «no medible» **cierra**. Son dos
ramas con veredicto opuesto, y el criterio las llama a las dos por el nombre de una.

**Lo mismo, un párrafo antes, sobre la clase `desfase`** (`:831`): «el cierre … sigue **denegando**
por ausencia». Medido: `Segurídad: aprobado` con `Rigor: ligero` → **ALLOW**. La frase de cabecera
—«el cierre no pierde nada por ello»— es cierta; la sub-oración que la explica, no.

### Por qué bloquea, y por qué NO es un matiz de redacción

Es el mismo argumento que sostuvo `QA-024-33`, con el objeto cambiado — que es, literalmente, la
forma de reincidencia que este proyecto tiene medida:

1. **`CA-13 (v)` prescribe el caso del banco**: «lo conforme es medirla y **publicarla como frontera
   abierta, con su control en la misma corrida**». Un `desarrollador` que escriba ese caso tomando
   `Seguri<ZWSP>dad:` como instancia de la clase obtiene un `DENY` con un motivo que el criterio no
   contempla; si toma el homóglifo y varía el rigor, obtiene un **ALLOW** donde el criterio promete
   fail-closed. Las dos salidas vuelven a ser malas: acreditar por medición falsa, o «arreglar» algo
   que no está roto.
2. **La sede heredada viaja.** `:1192-1193` es el texto destinado a `AGENTS.md` §13 y a
   `templates/AGENTS.md.tpl`. Llevaría a **todo proyecto** una frase que describe mal su propia
   puerta, en la dirección de creer que una clave corrompida «sólo» cae a ausencia.
3. **Es la clase `SEC-079` otra vez**: una promesa medida falsa cuya excepción no coincide con lo que
   la máquina hace. Este repositorio lleva nueve eslabones pagándola.

*Resultado esperado:* que lo que se afirma **de** la clase `no` se enuncie con la misma disciplina
con que ya se enuncia su **pertenencia** — por lo que el código hace, derivable ejerciéndolo, sin
prometer un `DENY` que depende del rigor efectivo y sin llamar «ausencia» a la rama de
**medibilidad**, que en `ligero` decide lo contrario.
*Resultado observado:* «por ausencia, que es fail-closed», falso por sus dos extremos, en **tres**
sedes vivas (`:831`, `:863-864` y la **heredada** `:1192-1193`) más su transcripción en el Historial
(`:1755`).

*Clase:* **`contrato`** — el REQ afirma algo falso sobre lo construido, medido, y una de las sedes es
superficie heredada. *Dueño:* `analista-requerimientos`. *Forzador:* el cierre de `QA-024-19`.
*Lo que NO pido:* **el código está bien y no se toca.** Control medido en la misma corrida: con la
clave **limpia** y el campo realmente ausente, `ligero` → ALLOW y `critico` → DENY — **idéntico**.
Ninguna decisión se mueve; `CA-13 (iii)` se cumple. El arreglo es **de texto, en tres sedes**.

---

## 6. `QA-024-33` — **CIERRA**

Su `Esperado:` era «que la clase declarada frontera se enuncie por la **propiedad que el código
decide** … de modo que su pertenencia sea derivable ejerciendo el lector, con el sitio único citado».
**Cumplido y verificado ejerciendo**, no leyendo:

| lo que pedí | estado |
|---|---|
| la frontera enunciada por la clasificación del lector, no por «letra» | **CUMPLIDO** — 41 formas, 41 aciertos |
| los ejemplos **en los dos sentidos** y marcados no exhaustivos | **CUMPLIDO** — 5 de 5 correctos |
| `CA-13 (ii)` deja de enumerar el plegado; sitio único + derivación | **CUMPLIDO** — y sin lista equivalente en otro sitio |
| las 3 sedes vivas que nombré + el Historial | **CUMPLIDO** — el analista barrió por propiedad y encontró **7**, y su lista **la comprobé yo**: las 7 están y ninguna conserva la frase |
| la fila del Historial desmentida, conservada y marcada | **CUMPLIDO** — «FILA HISTÓRICA» con sus tres afirmaciones nombradas una a una |

**Y le acredito la causa raíz escrita**, que es lo que impide la tercera instancia: «una propiedad que
se afirma "por construcción" se comprueba **EJERCIENDO** la construcción». Es la regla correcta. El
defecto que abro en §5 existe porque **ni él ni yo** la aplicamos a la **segunda mitad** de la misma
oración; no porque la regla sea mala.

---

## 7. `QA-024-19` — **NO CIERRA.** Clase `contrato`, sin cambio

Exactamente qué falta, para que el próximo pase sea quirúrgico:

| condición de cierre | estado |
|---|---|
| `:815` deja de afirmar algo falso | **CUMPLIDA** (vuelta anterior) |
| existe criterio del comportamiento nuevo (`CA-13`) | **CUMPLIDA** |
| contrata lo entregado (`ALLOW` + aviso), no el `DENY` que el hallazgo pedía | **CUMPLIDA** |
| la **propiedad** por propiedad y no por lista | **CUMPLIDA** |
| la **frontera** — **pertenencia** a la clase | **CUMPLIDA** — `QA-024-33` cierra |
| la **frontera** — **consecuencia** de la clase | **NO CUMPLIDA** — `QA-024-34` |

**Falta una sola oración, en tres sedes, sin tocar `hooks/`.** No cierro `QA-024-19` porque cerrarlo
aprobaría un REQ que describe un cierre que la máquina no tiene — la deriva de `AGENTS.md` §9, que
es lo que este hallazgo vino a impedir.

---

## 8. Comprobación 4 — barrido por propiedad, y no por la lista del analista

Barrí por la familia entera («pasa sin aviso» · «no avisa» · «queda mudo» · «por ausencia» ·
«fail-closed» · «por construcción» · «se sustituye u omite una letra») sobre `requirements/`,
`AGENTS.md`, `templates/`, `CHANGELOG.md`, `docs/seguridad/`, `skills/`, `hooks/` y `tests/`.

- **Las 7 sedes que declara el analista: comprobadas una a una, las 7 corregidas.** No me fié de su
  lista: la verifiqué y además busqué fuera de ella.
- **Lo que encontré de más son las tres sedes de `QA-024-34`** (§5) — y nótese que son **las mismas
  oraciones** que él corrigió, en su **segunda mitad**. Su barrido fue por la familia
  «sustituye u omite una letra»; el defecto vivía en la cláusula siguiente.
- **`:967`** («El cierre no se movió y sigue siendo fail-closed») **no lo abro**: está dentro del
  párrafo de la capitalización, ceñido a su medición de 19/19 celdas en `critico`, y `QA-024-34`
  cubre la clase con su formulación general. Lo dejo escrito para que quien corrija §5 lo revise en
  el mismo pase y no produzca una cuarta vuelta por una frase vecina.
- **`AGENTS.md:356`** (la celda de medibilidad) **no afirma nada falso** sobre esto: ya declara que
  el homóglifo «**permite, y permite por ausencia**». Es, de hecho, la sede que **contradice** a
  `CA-13 (v)`.

---

## 9. Fuera de alcance — dicho, no tocado

- **`SEC-084`** (no es mío). Se me pidió decir dos cosas:
  1. **Su mitad de texto sigue AUSENTE.** `AGENTS.md:353` y `templates/AGENTS.md.tpl:311` siguen
     prometiendo la invariante del orden **sin declarar su alcance**. El write-back **fija** la
     redacción; la **transcripción** es del `desarrollador` con el gate humano de `CA-12 (ii)`.
  2. **¿Ya sería seguro transcribirla? NO.** La celda mejoró —el reparto por «letra» desapareció—
     pero su última oración es la sede heredada de `QA-024-34`: transcribirla hoy llevaría a
     `AGENTS.md` §13, a la plantilla y a **todo proyecto** una frase que dice «el cierre resuelve por
     ausencia» sobre un conjunto en el que una parte se resuelve por **medibilidad**, con veredicto
     opuesto en `ligero`. **La respuesta cambia de «no, por la frase de la letra» a «no, por la frase
     de la ausencia»: el motivo es otro, el veredicto es el mismo.**
- **La corrección de la coordinadora en `CHANGELOG.md` (`:128`): forma CORRECTA, con una mejora
  disponible y ninguna ambigüedad material.** Es lo que corresponde: anota en su sitio, **no
  reescribe**, se atribuye la afirmación («es mía y la corrijo aquí»), nombra el mecanismo con ruta y
  línea, remite a la entrada que lleva la medición y dice **por qué** no se borra —«una bitácora
  corregida en silencio deja de servir para saber qué se creyó y cuándo»—. Es la misma convención que
  la «FILA HISTÓRICA» del analista y la variante (A) de la celda heredada, lo cual importa: **tres
  correcciones el mismo día con la misma forma** es una convención, no una improvisación.
  **La mejora:** el analista puso su marcador **antes** del texto anulado; la coordinadora lo puso
  **después**. Quien lee de arriba abajo encuentra la frase falsa antes que su corrección. No es
  ambiguo —el bloque está pegado y en la misma sección `##`, así que la rotación no puede separarlos—
  pero un rótulo de una línea por delante cerraría también esa ventana.
- **`CA-12 (ii)` como criterio, `CA-03`, los veinte `instrumento` abiertos, `QA-024-32`, el
  instrumento de rendimiento (reapertura prohibida por el propietario), otros REQ y mejoras:** no
  tocados.

---

## 10. Evidencia y método — en disco, no en la conversación

- **Árbol:** `feat/1.34-cierre-alcance` @ **`f18dd45`**, limpio. Código sin cambios desde `a05994f`
  (`git diff a05994f f18dd45 -- hooks tests` → vacío).
- **Delta verificado:** `git diff 7154769 f18dd45 -- requirements/REQ-024.md`.
- **Instrumentos y salidas** (re-derivables sin preguntar, con la base y el método dentro):
  - `docs/qa/evidencia-req-024-qa02433-f18dd45/ejerce2.sh` → `salida-lector-41-formas.txt` (41 formas
    contra `arnes_declara_clave`, con el plegado de la clave al lado).
  - `docs/qa/evidencia-req-024-qa02433-f18dd45/e2e.sh` → `salida-hook-e2e.txt` (hook real: 14 celdas
    del acto de FIRMAR, 12 del de CIERRE con su motivo, incluidos los controles de clave limpia).
- **Gates §7:** en verde (§1).
