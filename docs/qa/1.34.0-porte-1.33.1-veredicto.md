# QA acotada al porte de `v1.33.1` — PR #48 · veredicto y hallazgos

**Veredicto: `con-hallazgos`.** **Vuelta 1 de 3 del ciclo dev↔QA del PORTE** (unidad de trabajo:
los cuatro commits `f05bd79`, `e3acbbc`, `d1b3cc3`, `406f7e9`, que ningún QA había validado antes).
**No consume vuelta de `REQ-016` ni de `REQ-024`**, cuyos contadores están agotados y cuyo trabajo
**no** se revalida aquí. *Si la coordinadora decide que el porte pertenece al ciclo de `REQ-024`,
entonces no queda vuelta y esto es una escalada al propietario: esa decisión no es mía.*

**Nada se cierra con este documento.** No cierro `QA-017-33`, ni `SEC-056`, ni `SEC-084`, ni
`SEC-085`, ni `QA-016-04`. No firmo `Seguridad:` y no marco nada `completado`. No toqué código.

---

## 0. Método, versión base y cómo re-derivar cada cifra

- **Árbol medido:** worktree `/home/juan/dev/ArnesJuan-1.34-reparaciones`, rama
  `feat/1.34-reparaciones-astra`, cabeza **`406f7e9`**, `git status --porcelain` **vacío** al empezar.
- **Líneas base del par fail-before**, las tres ejercidas de verdad:
  - `ARNES_HOOKS_DIR=/home/juan/dev/ArnesJuan/hooks` — base del PR (`1dfe31b`), **sin** el porte.
  - `ARNES_HOOKS_DIR=/home/juan/.claude/plugins/cache/arnes-juan/arnes-juan/1.33.0/hooks` — la
    1.33.0 **publicada**, la que tenía los dos defectos en producción.
  - `ARNES_HOOKS_DIR=/home/juan/.claude/plugins/cache/arnes-juan/arnes-juan/1.33.1/hooks` — la
    1.33.1 **publicada**, que es lo que el porte dice traer.
- **Reutilicé** la evidencia de la coordinadora (`/home/juan/dev/ArnesJuan/docs/arnes/porte-1.33.1-verificacion/`)
  **tras verificarla**: la matriz de rigor, las cuatro formas de `Seguridad:` decorada y el cuadre
  1064 → 1079 **cuadran** con mis mediciones independientes. No las repito.
- **Sondas propias** (harness que ejerce `guard-completado.sh` real sobre un proyecto efímero, sin
  reimplementar ningún lector): `1.34.0-porte-1.33.1-falsacion/harness.sh`, `sonda-rigor.sh`,
  `sonda-seguridad.sh`, `sonda-seguridad-2.sh`, `comprueba-exhaustividad.py`. Salidas crudas en la
  misma carpeta, numeradas.
- **Máquina de medición:** WSL2, **12 núcleos**, `ARNES_JOBS=6`. **No es el runner** (4 vCPU).
  `loadavg` publicado en `09-loadavg-corrida-1.txt` (0,09 antes → 3,37 después).
  **Ninguna cifra de reloj de este documento se presenta como rendimiento acreditado.**

## 1. Quality gates (`AGENTS.md` §7)

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | **OK** |
| `jq -e . hooks/hooks.json` | **OK** |
| `jq -e .` sobre `plugin.json` y `marketplace.json` | **OK** |
| Banco completo, **corrida 1** | **1071 PASS · 0 FAIL · 8 SKIP · rc 0** · reloj 103,5 s (user 330,8 / sys 92,5 / 408 % CPU) |
| Banco completo, **corrida 2** | **1072 PASS · 0 FAIL · 7 SKIP · rc 0** · reloj 90,6 s (user 283,7 / sys 76,8 / 397 % CPU) |

`1071 + 8 = 1072 + 7 = 1079 = CASOS_ESPERADOS`. **El cuadre sale exacto en las dos corridas, y 0 FAIL
en las dos.** La segunda corrida **no se lanzó buscando verde** —la primera ya estaba verde—, sino para
medir dispersión; y midió algo (§2 bis). Las tres puertas del manifiesto
`.arnes/config.json` son **idénticas** a las de `AGENTS.md` §7.
**Integridad de dependencias: N/A** — no hay gestor de paquetes con lockfile (Bash + jq).

**Los 8 SKIP, uno por uno y sin causa común** (`08-banco-corrida-1-resumen.txt`): ruta Windows sin
`cygpath`; `REQ-017 CA-05 (i)` y `(ii)` (acreditación de fail-before, no puerta de PR);
`REQ-017 CA-08 (ii)` (el techo cae DENTRO del recorrido observado); `REQ-021 CA-08 (iii)` (la mitad
de procesos no es medible en este instrumento); `REQ-023 CA-12` (precondición de atribuibilidad no
se cumple); y **`REQ-023 CA-09 (iii)` × 2** (`arnes_norm_clave` y `arnes_campo_linea`: dispersión de
la relación 0,267× y 0,863× ≥ el margen contra el techo 1,000×).
**Los dos últimos son el criterio que ya dio `FAIL → SKIP → PASS → PASS` sobre código idéntico.**
Aquí salieron **SKIP** en **las dos** corridas, no `FAIL`. **El porte no los causa —no toca esos
criterios— y tampoco los declaro falsos:** quedan anotados como no concluyentes, con su dispersión.

### 1 bis. Dispersión entre las dos corridas — un criterio ALTERNA, y no es el que se me advirtió

Comparando veredicto por veredicto (`13-dispersion-corrida-1-vs-2.txt`), **de 1079 casos cambia de
categoría exactamente uno**:

> `REQ-017 CA-08 (ii) una cabecera de 200 líneas: el reloj no sube más de 1,25× el de v1.32.1`
> · **corrida 1: SKIP** («*el techo cae DENTRO del recorrido observado [1.161×, 1.434×]: el
> instrumento no distingue el factor que vigila*») · **corrida 2: PASS** («*máx(r) 1.206× ≤ techo …
> recorrido 1.147×*»).

Mismo commit, mismo árbol, misma máquina. **No lo trato como pase ni como fallo: es flaky**, y la
evidencia de la dispersión queda en disco. Lo apunto porque **no es el criterio que se me advirtió**
(`REQ-023 CA-09 (iii)`, que aquí fue SKIP estable en las dos): es una **instancia nueva** de la misma
clase, sobre un criterio distinto. Y la corrida 2 fue **más rápida con más carga inicial**
(90,6 s con `loadavg` 0,25 frente a 103,5 s con 0,09), o sea **caché caliente** — razón de más para
**no** tomar la segunda corrida como la buena. Ver `QA-P48-05`.

---

## 2. Punto 1 — Rigor con matiz

**Método:** deduzco el rigor **efectivo por conducta de la puerta**, no leyendo variables: dos sondas
por celda (`QA:pendiente`+`Seg:pendiente` → allow sólo si `ligero`; `QA:aprobado`+`Seg:pendiente` →
deny sólo si `critico`). **27 valores de `Rigor:` × 2 de sensibilidad = 54 celdas**, contra el porte
y contra la base. Salidas: `01-rigor-matriz-porte.txt`, `02-rigor-matriz-base.txt`.

**(a) El camino habitual NO cambió, y está medido, no supuesto.** Las **9** formas sin paréntesis
—`ligero`/`estandar`/`critico`, `LIGERO`, `Ligero`, ` ligero `, `**ligero**`, `_ligero_`,
`` `ligero` ``— dan **el mismo rigor efectivo en porte y en base**, en las dos sensibilidades. Igual
`basura`, `ligero estandar` y `ligero, estandar`. **18 de 18 celdas idénticas.**

**(b) El arreglo D16 hace lo que dice.** `critico (por suelo)`, `critico (R-011, 2026-09-07)` y
`**critico** (por suelo)` con `Sensible: no` pasan de **`estandar` (base) a `critico` (porte)`**:
la puerta deja de cerrar sin auditoría. Reproduce lo que la coordinadora midió.

**(c) HALLAZGO — el arreglo también RELAJA, en una dirección que nada declara.**
`ligero (local)`, `ligero (D8, 2026-09-01)`, `ligero(sin espacio)`, `ligero ()` y `ligero (critico)`
pasan de **`estandar` (base) a `ligero` (porte)`**, y `ligero` es el único nivel que **no exige
`QA: aprobado`** para cerrar. Es decir: **un REQ no sensible con `Rigor: ligero (<matiz>)` cierra hoy
con `QA: pendiente` y `Seguridad: pendiente` donde la base lo denegaba.** Ver `QA-P48-01` abajo.

**(d) El residual que se me pidió juzgar — `basura` → `estandar` en un REQ no sensible: NO lo abro
como hallazgo.** Motivos, y son tres: el porte **no lo toca** (idéntico en base y porte, 2 de 2
celdas); está **documentado en el código** («*valor no reconocido: se ignora y se cae al
comportamiento de siempre*»); y **ya tiene sede y clase**: es `QA-016-04` (`contrato`), abierto en
`PENDING_APPROVAL.md` §D16. Abrir un duplicado no añade señal y sí ruido. **Queda como
observación con su riesgo nombrado:** un `Rigor: critcio` mal teclado en un REQ **no sensible** cae a
`estandar` en silencio, y entonces —palabras del propio auditor en `R-027`— «*el brazo `critico` que
acabo de acreditar NO SE EJECUTA*». **Exposición medida en este repositorio: 0 de 27** (los 27 REQ
declaran `Rigor: critico` limpio y `Sensible a seguridad: sí`, que impone `critico` como suelo).

## 3. Punto 2 — Cambio real de `Seguridad:`

**Método:** 33 sondas sobre la puerta real, ejerciendo el disparador en **las dos direcciones** y en
los bordes. Salidas `03`–`06`.

**Lo que el porte CIERRA, y es más de lo que el banco afirma:**

| Sonda | base | porte |
|---|---|---|
| `Edit` que sustituye **sólo el valor** (`old='pendiente'`), clave **limpia** | **allow** | **deny** |
| ídem con `replace_all: true` | **allow** | **deny** |
| ídem con el documento en **CRLF** | **allow** | **deny** |
| `Seguridad : aprobado` (espacio antes de los dos puntos) | **allow** | **deny** |

**Esto importa y lo digo aparte:** `SEC-084` se describió como «*toda forma de la clave que el lector
acepta y el disparador no reconoce*», es decir la clave **decorada**. Lo medido es que el fail-open
**también alcanzaba la clave limpia** siempre que el `Edit` sustituyera sólo el valor —el fragmento
entrante no contiene la cadena `Seguridad:`—, que es **la forma más natural de firmar con `Edit`**.
La sección 41 sólo cubre la variante **decorada** con `replace_all`. El porte cierra las dos.

**Lo que el porte ABRE a propósito, y es el falso positivo que costó una reescritura:**

| Sonda | base | porte |
|---|---|---|
| `Edit` de prosa del **cuerpo** que menciona `Seguridad: aprobado` | **deny** | **allow** |
| `Write` del documento entero con la **cabecera igual** y el cuerpo distinto | **deny** | **allow** |
| `Edit` con `old_string` inexistente cuyo `new` trae la firma **ya vigente en disco** | **deny** | **allow** |

**El cambio de conducta declarado se ejerce y se cumple:** cambiar **sólo el paréntesis de
evidencia** (`aprobado` → `aprobado (R-029)`) sobre un `QA: pendiente` **deniega** en el porte.
«*Cambiar la evidencia también renueva la firma*» es cierto, y es más estricto.

**Bordes, todos conformes y ninguno abierto por el porte:**
- **REQ nuevo, sin valor previo en disco:** `Write` con `Seguridad: aprobado` + `QA: pendiente` →
  **deny**; sin línea `QA:` → **deny** (fail-closed, `SEC-083`); con `QA: aprobado` → **allow**.
- **Variantes del conjunto conocido:** `APROBADO`, `**aprobado**`, `aprobado` con espacio final,
  `Seguridad : aprobado` → **deny** (el lector normaliza y el disparador lo sigue).
- **Precedencia de duplicados:** con dos líneas `Seguridad:` gana **la última**, y el disparador
  decide sobre la misma; cambiar la primera cuando la última dice `pendiente` → **allow** (correcto).
- **`<!-- Seguridad: aprobado -->`** y `Seguridad:` **sólo en el cuerpo** → **allow**: un campo
  comentado o fuera de cabecera no declara. Coherente con `AGENTS.md` §13.
- **Idempotencia:** la misma edición dos veces da el mismo veredicto.
- **Interacción con la guarda de medibilidad:** con un **BOM** en la clave `QA:` del disco, la firma
  **deniega nombrando el carácter insertado** en el porte igual que en la base. El
  `arnes_seguridad_cabecera "$disk"` nuevo **no pisa** `ARNES_OCULTA` (lo comprobé porque el orden
  de llamada lo hacía plausible): el diagnóstico sobrevive.
- **`Write` que BORRA la línea `Seguridad:`** teniendo `aprobado` en disco → **allow** en las dos
  versiones. Retirar una firma no es firmar; conducta **preexistente**, no del porte.

**No encontré ninguna vía por la que el porte permita firmar donde la base denegaba con motivo.**

## 4. Punto 3 — Los 15 casos de regresión

**Cuadre:** `CASOS_ESPERADOS_SECCION=15` = 15 `check`/`check_motivo` contados a mano;
`CASOS_ESPERADOS` 1064 → **1079** = +15; corrida real 1071 PASS + 8 SKIP = **1079**. **Cuadra.**

**No son tautológicos, y el par lo prueba** (`07-failbefore-41-*.txt`):

| Línea base | Resultado de la sección 41 |
|---|---|
| base del PR (`1dfe31b`, sin porte) | **5 PASS · 10 FAIL** |
| 1.33.0 **publicada** | **5 PASS · 10 FAIL** (idéntico) |
| 1.33.1 **publicada** | **14 PASS · 1 FAIL** |
| porte (`406f7e9`) | **15 PASS · 0 FAIL** |

**10 de 15 casos discriminan**, en las dos direcciones (deny→allow y allow→deny) y para los dos
arreglos, así que cada arreglo tiene su caso `deny` y su `allow` que de verdad muerden. El único
`FAIL` contra la 1.33.1 publicada es «*ausencia QA conserva cierre fail-closed de 1.34*», y es
**correcto**: esa mitad es el arreglo de `SEC-083`, que vive sólo en la línea de 1.34.0.

**Los 5 que NO discriminan, nombrados** —pasan también contra la base, así que son controles de
no-regresión y no acreditan el arreglo—: «desconocido conserva derivacion heredada», «preventiva
decorada permitida», «QA y seguridad aprobados en MultiEdit», «cambiar evidencia de firma requiere
QA» y «QA previo permite firma decorada». El último **no llega a ejecutar el código nuevo** (con
`QA: aprobado` no hay cruce). Es información, no defecto.

**Piso de `CA-18`: los tres controles de máquina pasan.** `5 + 0 + 15 = 20` (suma exacta),
`20 ≤ 33` líneas, y está declarado — y el banco verde incluye esa máquina. **Observación, no
hallazgo:** el término «bloque indivisible mayor (líneas 13-27)» es discutible, porque la línea 21
está en blanco y 13-20 (fixture `REQ-943`) y 22-27 (fixture `REQ-944`) son **dos** bloques
autocontenidos; el mayor de verdad son 8 líneas y el piso saldría 13. **No lo abro porque no compra
conformidad**: el archivo tiene 33 líneas y lo gobierna `N = 400`, así que `piso × k` no interviene.
`CA-18` sólo convierte en regresión el piso inflado **«para caber»**. Dueño: `desarrollador`, deuda
de una línea.

## 5. Punto 4 — `QA-017-33`, `SEC-056` y la exhaustividad del índice

**`QA-017-33`: correcto.** Sigue `instrumento`, dueño `desarrollador`, en el campo de `REQ-017`.
La cuarta sede está **bien citada**: `docs/arnes/req-017-ca-03-modo-de-medicion/04-evidencia-del-intercalado.md`
**líneas 131-133** contienen literalmente el texto que `docs/qa/1.34.0.md` §4 les atribuye
(verificado carácter a carácter). *Observación:* el campo de `REQ-017` sigue enumerando **dos**
sedes mientras `docs/qa/1.34.0.md` §4 enumera **tres**; el `CHANGELOG` declara que la ampliación es
sólo de `docs/qa/`, así que es coherente, pero la enumeración del campo queda incompleta y sin marca
de «no exhaustivo» ni puntero al sitio único. Dueño: `analista-requerimientos`.

**`SEC-056` y `SEC-085`: nada cerrado en silencio.** `SEC-056` sigue `instrumento` · **abierto** ·
dueño `auditor-seguridad`, con nota de mantenimiento que declara aplicada **sólo** la remediación de
**sede**. `SEC-085` sigue `contrato` · **abierto**. `SEC-084` sigue `contrato` · **abierto** en el
índice y en el campo de `REQ-024`. *Observación:* la sección «Ubicación» de `SEC-056` sigue diciendo
«*el índice es §2 de la entrada `## Revisión R-016`*», que ya es falso; lo corrige la nota del final
del propio hallazgo.

**La exhaustividad, con el resultado partido en dos porque las dos mitades no dicen lo mismo:**

**(a) Contra los campos `Hallazgos abiertos:` de `requirements/` —el test que se me pidió—: PASA, y
cuadra con la cifra publicada.** Medido mecánicamente (`comprueba-exhaustividad.py`,
`10-exhaustividad-indice.txt`): **22** IDs de clase bloqueante declarados en campos, **22 de 22
presentes** en el índice, **0 ausencias**. Y el índice tiene **32** filas con estado bloqueante, de
las que **22** están en un campo — que es **exactamente** lo que `docs/ESTADO.md:60` publica
(«**32**, de los que **22** los ve la puerta»). El sello interno de la tabla (`R-028`) **coincide**
con la última revisión del documento (`R-028`), así que su regla fail-closed **no se dispara**.
**Verificado el número, no aceptado por firma.**

**(b) Contra su PROPIA declaración —«la lista exhaustiva de los hallazgos de clase `usuario/dinero`
y `contrato` de este proyecto»—: NO PASA.** `QA-016-04` es de clase **`contrato`** por su propia
cabecera en dos sedes (`PENDING_APPROVAL.md:790` y `docs/qa/1.34.0.md:4132`), está **vivo** en la
cola «## Pendientes» (`D16`, entrada 15 de 16), **no está en ningún campo** —su propia entrada lo
dice: «*hoy no tiene sede en ningún campo, y sin sede ninguna puerta lo mide*»— y **no está en el
índice**. Es **el mismo patrón** que el índice ya reconoce en `SEC-075` («*estaba en ninguna de las
dos sedes: es el contraejemplo de `SEC-085`*») — pero `SEC-075` **sí** figura y `QA-016-04` no.
Ver `QA-P48-02`.

**(c) Tres celdas de la tercera columna son falsas hoy, y son PREEXISTENTES.** El porte movió la
tabla **verbatim** (comparado contra `1dfe31b`: las tres celdas son idénticas byte a byte), así que
**no las introdujo**. Medido contra el árbol:

| Fila | Dice | El árbol dice |
|---|---|---|
| `SEC-014` (`:48`) | «sigue declarado en `REQ-013` — **re-medida el 2026-09-10**: sigue ahí … y por eso `REQ-013` no puede cerrar» | el campo de `REQ-013` **NO** lo declara |
| `SEC-055` (`:74`) | «— **debe declararse en `REQ-019`**» | el campo de `REQ-019` **YA** lo declara |
| `SEC-083` (`:82`) | «sigue declarado en `REQ-024`» *(con «subsanado en R-028 §3» detrás, contradiciéndose)* | el campo de `REQ-024` **NO** lo declara |

Y `R-028` §5 arrastra lo mismo: cita el índice como «`R-016` §2» (ya mudado) y cuenta «**1** que
debería estar en un campo (`SEC-055` → `REQ-019`)». Ver `QA-P48-03`.

---

## 6. Hallazgos abiertos por esta validación

### `QA-P48-01` — `contrato` · severidad media · dueño `analista-requerimientos`
**El porte cambia la conducta de la puerta de cierre para toda la familia `Rigor: <nivel>
(<matiz>)`, en las DOS direcciones, y ningún criterio de aceptación de `requirements/` lo
describe.** La dirección que importa es la que **abre**: `ligero (<matiz>)` baja de `estandar` a
`ligero`, y `ligero` es el único nivel exento de `QA: aprobado`.

- **Reproducción.** REQ con `Sensible a seguridad: no`, `QA: pendiente`, `Seguridad: pendiente`,
  `Rigor: ligero (local)`; `Edit` de `Estado: en-revisión` → `Estado: completado`.
- **Esperado** (base del PR y 1.33.0 publicada): **deny** (rigor efectivo `estandar`).
- **Observado** (porte): **allow** (rigor efectivo `ligero`). **Cierra sin validación de QA.**
- **Alcance medido:** 12 de 54 celdas cambian de veredicto entre base y porte; 5 de ellas en la
  dirección que abre (`01-` / `02-rigor-matriz-*.txt`).
- **Por qué `contrato` y no `instrumento`:** no es que la conducta nueva sea incorrecta —un humano
  que lee `ligero (local)` lee `ligero`, y `AGENTS.md:343` («salvo `Rigor: ligero`») queda **más**
  verdadera con el porte que sin él—. Es que **el código de la puerta de cierre hace algo que
  ningún requerimiento describe**, y eso es la deriva de `AGENTS.md` §9, cuya única salida es el
  **write-back**. `contrato` es la clase que bloquea *hasta* ese write-back, que es el efecto
  correcto. **No pido que se revierta el código.**
- **Y hay una segunda mitad que el write-back tiene que barrer, porque «corregir la promesa
  completa» es la regla:** el porte deja **tres transcripciones** de la conducta vieja que ahora son
  falsas o medio falsas —`PENDING_APPROVAL.md` §D16 (su bloque medido dice
  `Rigor: critico (por suelo)` + `Sensible: no` → **ALLOW**, y hoy es **DENY**),
  `PENDING_APPROVAL.md` §D17 (su hecho medido —`_Seguridad_: aprobado` firma sobre `QA: pendiente`—
  hoy **deniega**), y `docs/qa/1.34.0.md:4128` («`Rigor:` **falla ABIERTO**»), cuya reproducción
  concreta ya no reproduce aunque el caso `basura` siga cayendo a `estandar`—. **Son dos entradas
  vivas de la cola que el propietario va a leer para decidir**, y describen un árbol que ya no
  existe. Esto no es cosmética: `D16` pide **elegir la sede de `QA-016-04`** sobre una medición
  que el porte desmintió en parte.
- **Exposición en este repositorio: 0 de 27** REQ. **En un proyecto consumidor, desconocida y no
  medible desde aquí** — es el mismo argumento con el que `D16` y `D17` se escalaron.
- **Cierra cuando:** exista criterio de aceptación que describa la regla del paréntesis en `Rigor:`
  (las dos direcciones), y las tres transcripciones digan lo que el árbol tiene. **No cierra por
  documentar el residuo.**

### `QA-P48-02` — `contrato` · severidad media · dueño `auditor-seguridad`
**El «Índice de hallazgos de clase bloqueante» se declara «sitio único de la lista exhaustiva» de
los hallazgos `usuario/dinero` y `contrato` «de este proyecto», y omite `QA-016-04`
(`contrato`, vivo, sin sede en ningún campo).**

- **Reproducción.** `grep -n 'QA-016-04' docs/seguridad/registro-seguridad.md` → **0 líneas**.
  `grep -n 'QA-016-04' PENDING_APPROVAL.md docs/qa/1.34.0.md` → cabeceras que declaran `contrato`.
- **Esperado:** una fila en el índice, con su estado.  **Observado:** ausente.
- **Por qué es el defecto que `SEC-085` nombra, y no otro:** el índice **sí** incluye `SEC-075` por
  ser exactamente este caso —`contrato` que no cuelga de ningún REQ—, y la reconstrucción de
  `e3acbbc` se hizo **después** de abrir `SEC-085`. Una lista que se declara exhaustiva y omite un
  ejemplar de la clase que la motivó no está exhaustiva.
- **Candidatos adicionales, NO acreditados y por eso separados:** `QA-016-02`, `QA-024-01`,
  `QA-024-02`, `QA-024-03`, `QA-024-13`, `QA-024-14` declaran `contrato` en su propia cabecera, no
  están en ningún campo, no están en el índice y **no encontré marca de cierre** para ellos — pero
  mi detección de cierre es un `grep` con ventana y **no basta para afirmarlo**. Es trabajo del
  auditor confirmar su estado uno por uno. **No los cuento y no muevo ninguna cifra.**
- **Efecto sobre las cifras publicadas: NINGUNO hoy.** El **32/22** de `docs/ESTADO.md` cuadra fila
  por fila con el índice, y las cifras de `R-028` §5 son de otro commit. Este hallazgo dice que la
  **promesa de exhaustividad** es falsa, no que el recuento esté mal.

### `QA-P48-03` — `instrumento` · severidad baja · dueño `auditor-seguridad`
**Tres celdas de la columna «Declarado en campo `Hallazgos abiertos:` de» afirman lo contrario de lo
que el árbol tiene** (tabla en §5(c)), y una de ellas (`SEC-014`) cita una **re-medición fechada el
2026-09-10** que el árbol desmiente. Más dos punteros desfasados en `R-028` §5 («el índice
reconstruido de `R-016` §2») y en `SEC-056` §Ubicación.

- **Preexistente en la base del PR** (`1dfe31b`), movido **verbatim** por `e3acbbc`: **el porte no lo
  introdujo**.
- **`instrumento` y NO bloquea:** el defecto está en el control (el registro), no cambia ninguna
  cifra —los tres hallazgos están correctamente contados por su **estado**— y no hay REQ que afirme
  nada falso por ellas. **Riesgo concreto:** quien lea la celda de `SEC-014` puede mantener
  `REQ-013` bloqueado por un motivo que ya no existe, y quien lea la de `SEC-055` puede volver a
  despachar un write-back ya hecho.

### `QA-P48-04` — `instrumento` · severidad media · dueño `desarrollador`
**Dos de los cuatro commits del PR no actualizan `CHANGELOG.md`, contra `AGENTS.md` §8, y son
precisamente los dos que cambian el mecanismo.**

- **Reproducción.** `git show --stat d1b3cc3` y `git show --stat 406f7e9` → ni una línea de
  `CHANGELOG.md`. Los otros dos (`f05bd79`, `e3acbbc`) sí lo tocan.
- **Consecuencia medida:** `git diff 1dfe31b..406f7e9 -- CHANGELOG.md` contiene **dos** entradas, y
  **ninguna** describe el porte del arreglo de rigor ni el de la firma. La bitácora de 1.34.0 no
  dice que la puerta de cierre cambió de conducta.
- **Y el control existe y no actuó:** `.githooks/pre-commit` rechaza cualquier commit sin
  `CHANGELOG.md` en el índice, y `core.hooksPath` está en `.githooks`. O se usó `--no-verify` o el
  `hooksPath` no estaba puesto en ese momento. **Ése es el defecto de clase `instrumento`**: un
  control del proyecto que se puede saltar sin que quede rastro.
- **Se arregla antes de fusionar**, y sin reescribir la historia: basta la entrada que falta en el
  commit que la coordinadora ya tiene previsto.

### `QA-P48-05` — `instrumento` · severidad baja · dueño `desarrollador` · **fuera de los cuatro puntos**
**`REQ-017 CA-08 (ii)` («una cabecera de 200 líneas») alterna `SKIP` ↔ `PASS` entre dos corridas del
mismo commit, en la misma máquina.** Evidencia y números en §1 bis y
`13-dispersion-corrida-1-vs-2.txt`. **No lo causa el porte** —no toca ese criterio— y **no bloquea
nada**: el cuadre y el `0 FAIL` se sostienen en las dos corridas. Lo registro porque un criterio no
determinista no es evidencia válida, y porque **amplía la clase ya conocida** («los criterios de techo
de reloj no discriminan hoy») a un criterio que no estaba en esa lista. **Siguiente paso:** el
`desarrollador` decide si el margen de convergencia de `CA-08 (ii)` se ensancha o si el criterio pasa
a abstenerse siempre; **no es trabajo de este PR**.

## 7. Lo que esta validación NO acredita

- **Rendimiento.** Ninguna cifra de reloj de aquí es representativa del runner (12 núcleos contra 4
  vCPU, `ARNES_JOBS=6`, `loadavg` publicado). **No hay «cumple» de rendimiento en este documento.**
- **`REQ-023 CA-09 (iii)` ni `CA-12`.** Salieron **SKIP**; no los declaro ni verdes ni rojos.
- **La revisión de seguridad.** No firmo `Seguridad:`. Va después y **sobre este mismo commit**.
- **Ningún REQ.** No validé `REQ-016`, `REQ-017`, `REQ-023`, `REQ-024` ni `REQ-026`: sólo el porte.
- **El estado de los seis candidatos de `QA-P48-02`.** Enumerados, no acreditados.
