# QA acotada del PR #48 — commit `6e3bb90` · **vuelta 1 de 2**

**Veredicto: `aprobado` con una observación no bloqueante.**
**Cabeza cubierta: `6e3bb901acd8bc5e29f2ea6afd8b7bbe0ef32939`** (rama `feat/1.34-reparaciones-astra`,
worktree `/home/juan/dev/ArnesJuan-1.34-reparaciones`, árbol limpio al empezar y al entregar salvo
los archivos de esta carpeta y `CHANGELOG.md`).
**Vuelta 1 de 2** del presupuesto dev↔QA acordado para esta reparación.
**Alcance:** `6e3bb90` y sus dependencias. **No** es una revisión del historial ni del resto de #48.
Fecha: 2026-09-11. Agente: `qa-tester` (Opus 5).

> **Lo aprobado en `v1.33.2` no acredita nada de aquí.** Toda la evidencia que sostiene este
> veredicto se midió **sobre este árbol**, con sondas propias escritas para esta validación
> (`30-qa-sonda-puerta.sh`, `31-qa-sonda-puerta-ausencia-exige.sh`), no reutilizando las del parche.
> De `v1.33.2` se reutilizó el **método** (medir por la puerta y no por un atajo), no ningún resultado.

---

## 1. La afirmación central: la guarda **no atraviesa** el camino de la ausencia — **CIERTA, medida por dos vías**

**(a) Estructural.** En `hooks/lib.sh`, `arnes_rigor_efectivo` resuelve la ausencia y **retorna**
antes de llegar a la guarda del matiz:

```
if [ -z "$declarado" ]; then
  ARNES_RIGOR_AUSENTE=1
  arnes_resuelve_ausencia "$ARNES_CLAVE_RIGOR" ''
  if [ -n "$ARNES_AUSENCIA_APLICA" ]; then ARNES_RIGOR="$ARNES_AUSENCIA_APLICA"; return 0; fi
  ARNES_RIGOR="$heredado"
  return 0
fi
```
La guarda `if [ "${ARNES_RIGOR_MATIZ:-0}" = "1" ]` está **~50 líneas por debajo** de esos dos
`return 0`, y no hay ninguna otra entrada a ese tramo: `arnes_rigor_efectivo` tiene **un solo
llamador en el mecanismo** (`arnes_campos_normaliza`, `lib.sh:2164`), que a su vez es el único
camino de `guard-completado.sh` y de `estado-derivado.sh` (`arnes_campos_req`). No hay segundo
lector que pueda entrar por otro sitio.

**(b) Conductual, por la PUERTA.** Se ejercitó el camino de la ausencia con la exigencia
**apagada** (4 celdas) y **encendida** (`campos.ausencia_exige: true`, 11 celdas), contra la cabeza
y contra su padre `2f7c821` (el árbol **sin** la guarda). **Las 15 celdas dan el mismo veredicto en
las dos versiones** — evidencia en `34-qa-ausencia-exige-head-vs-padre.txt` y bloque `E` de
`32-…-head-…txt` / `33-…-padre-…txt`. Si la guarda tocara ese camino, alguna celda se movería.

**Dicho como corresponde, y sin matices:** **la ausencia del campo `Rigor:` no pasa por esta guarda.**
Quien lea el arreglo no debe suponer que cubre ese camino: lo resuelve `ADR-009`
(`arnes_resuelve_ausencia`), aparte y antes. Las secciones 39 y 40 del banco, que son las que
contratan la ausencia, **no cambian ni un veredicto**, y eso es coherente y no un descuido.

## 2. Conducta **por la PUERTA**, con las exenciones ejercidas — **PASA**

Sonda propia sobre `guard-completado.sh` (no sobre el lector): 31 celdas, `Edit` real que lleva
`Estado: en-revisión` → `Estado: completado`. Salida íntegra en `32-qa-puerta-head-6e3bb90.txt`.

| caso (REQ **no** sensible, `QA: pendiente`) | padre `2f7c821` | **cabeza `6e3bb90`** |
|---|---|---|
| `Rigor: ligero` | allow | **allow** (la exención se conserva) |
| `Rigor: ligero (local)` | **allow** ← el fail-open | **deny** |
| `ligero (lo que sea)` · `ligero ()` · `ligero (a) (b)` · `ligero (a (b))` | allow | **deny** |
| `LIGERO (local)` · `Ligero (local)` · `ligero(local)` · `  ligero (local)  ` · `ligero  (local)` | allow | **deny** |

Con `QA: aprobado` y `Seguridad: pendiente`: `ligero (local)` → allow, `estandar (x)` → allow,
`critico (por suelo)` → **deny** (la corrección de v1.33.1 **se conserva**); con seguridad aprobada
→ allow. Con `Sensible a seguridad: sí` el suelo manda en las tres combinaciones.

**Par fail-before / pass-after sobre esta rama** (`35-qa-diff-padre-head.txt`): de 31 celdas
comparadas, **10 cambian y las 10 son `allow` → `deny`**; **0 celdas pasan de `deny` a `allow`**.
Una guarda que sólo estrecha es exactamente lo que se contrató, y aquí está medido y no argumentado.
(La línea base `1.33.1` publicada da el mismo cuadro que el padre en las celdas de `ligero (…)`; el
fail-before pertinente para **este** commit es el padre, porque es el único árbol que tiene la
maquinaria de `ADR-009` con la que hay que comparar.)

**Variantes de entrada sobre el conjunto conocido, todas ejercidas:** capitalización (`LIGERO`,
`Ligero`), espacios sobrantes (delante, detrás, dobles), sin espacio antes del paréntesis, valor
vacío (`Rigor:` con nada), valor ausente, valor inventado, paréntesis vacío, doble paréntesis,
anidado, sin cerrar y con texto detrás del cierre. **Ninguna alcanza `ligero`**, es decir: **ninguna
forma con `(` regala la exención de QA por ninguna vía**. Repetición/idempotencia: alternando el
mismo par de REQ cuatro veces el veredicto no varía (bloque `F`) — descarta el arrastre de
`ARNES_RIGOR_MATIZ` entre lecturas, que era el riesgo obvio de usar una variable global.

## 3. La rama **no perdió nada suyo** — **PASA**

`6e3bb90` toca 8 archivos y **borra 18 líneas en total**; las 18 se auditaron una por una: el `case`
del desenvoltorio (sustituido por el que además marca el matiz), los **dos** `case` de derivación a
mano (sustituidos por la variable `heredado`, mismo valor), la frase absoluta del contrato en el
README y su plantilla, los tres literales de contabilidad y los **dos** nombres de caso corregidos.
No hay ni una eliminación colateral.

Los otros trabajos de la rama están **fuera del conjunto de archivos tocados**, así que no pudieron
perderse, y se comprobó que siguen en la cabeza: el porte de 1.33.1 (`d1b3cc3`, con los casos
`SEC084…` corriendo en verde en el banco), el piso de la sección 41 (`406f7e9`, recalculado no
borrado), el índice de hallazgos (`e3acbbc` → `docs/seguridad/registro-seguridad.md`, **no tocado**),
la enmienda de política (`2f7c821` → `AGENTS.md` y `docs/gobernanza/autoalojamiento.md`, **no
tocados**) y `D16`/`D17` (`209ad39` → `PENDING_APPROVAL.md`, **no tocado**).

## 4. Contabilidad — **PASA, con la mitad que `CA-18` no comprueba re-derivada a mano**

- Sección 41: `CASOS_ESPERADOS_SECCION=26`, e **invocaciones `check` contadas en el archivo: 26**.
- `CASOS_ESPERADOS` 1079 → **1090**. Suma de los `CASOS_ESPERADOS_SECCION` de todas las secciones:
  1091, **menos 1** que no es una sección sino un literal **generado dentro** de
  `37-coste-del-escaner-5` (línea 486, fixture sintético) → **1090**. Cuadre exacto.
- Banco: `1082 PASS + 8 SKIP = 1090`, `rc 0` — **verificado leyendo la corrida en disco, no repetido**.
  Los 8 SKIP traen cada uno su motivo declarado; ninguno es del rigor ni del matiz.
- `PISO_AUTONOMO_SECCION` 20 → **64**. `CA-18` comprueba la **aritmética**; la otra mitad —que el
  término mayor sea de verdad el mayor— se **re-derivó a mano** (`36-qa-piso-y-contabilidad.txt`):
  bloques de 59 (líneas 23-81), 17, 12, 8 y 3 → el mayor **es** 59; preámbulo **5** (el primer caso
  arranca en la línea 6); maquinaria propia definida en el archivo: **0**. **5 + 0 + 59 = 64.**

## 5. Las cinco sedes del contrato: **ciertas aquí** — **PASA**

El enunciado copiado de `v1.33.2` se verificó **contra la conducta medida en esta rama**, donde la
lógica de ausencia cambia el cuadro:

| lo que la sede promete | medido en esta rama |
|---|---|
| matiz **bien formado** conserva `estandar` y `critico`, sujeto al suelo | `estandar (x)`→estandar, `critico (por suelo)`→critico ✔ |
| `ligero` **con** matiz → `estandar` si no sensible, `critico` si lo es | ✔ (deny con QA pendiente; `critico` con `Sensible: sí`) |
| `ligero` **sin** matiz conserva su comportamiento | ✔ allow |
| paréntesis que **no cierra al final** no es matiz → derivación heredada | ✔ (`ligero (local`, `critico (x) y`) |
| valor desconocido → derivación heredada | ✔ (`inventado`, `inventado (x)`) |

No queda ninguna sede con la promesa absoluta: el único resto de la frase vieja está en
`.arnes/plantillas-origen/requirements-README.md.tpl`, que es el **snapshot congelado del origen de
la migración** y cuyo descarte está motivado por escrito (y `v1.33.2` también lo dejó intacto).
**Y la frontera de la ausencia se dice explícitamente** en las tres sedes que pueden inducir a error
(`lib.sh`, sección 41, `run.sh`), que era el riesgo real de este porte.

## 6. Quality gates — **en verde**

- `for f in hooks/*.sh tools/*.sh; do bash -n "$f"; done` → sin errores.
- `jq -e . hooks/hooks.json` · `.claude-plugin/plugin.json` · `.claude-plugin/marketplace.json` → válidos.
- Banco completo: en disco, `rc 0`, sin FAIL (no repetido; ver §4).

**Rendimiento: no evaluado y no acreditado.** Esta máquina no es el runner
(`loadavg 0.75 0.94 0.60` al arrancar la validación). No se presenta ninguna cifra de reloj como
evidencia, y las dos comparaciones de coste que el banco declara no concluyentes siguen así.

---

## Hallazgos

**Ninguno bloqueante.** Una observación, abierta como deuda con dueño:

### `QA-P48-02` — clase **`instrumento`** (no bloquea)
**`SEC-087` se cita en el código y en el banco de esta rama, y en esta rama no tiene sede.**

- **Reproducción:** `grep -rn "SEC-087" .` en el worktree → aparece en `hooks/lib.sh:2444`, en
  `tests/…/41-estabilizacion-firmas-y-rigor.sh:8,26` y en `CHANGELOG.md`. En
  `docs/seguridad/registro-seguridad.md` de **esta rama** la numeración termina en `SEC-086`, y
  ningún REQ lo declara en su campo `Hallazgos abiertos:`. Igual para `SEC-088` y `QA-1332-01`.
- **Esperado:** un hallazgo abierto de clase `contrato` citado por el código tiene sede en el
  registro de esta línea y, si corresponde, en el `Hallazgos abiertos:` de un REQ, que es lo único
  que `guard-completado` lee para decidir si bloquea.
- **Observado:** sus sedes viven en la línea del parche
  (`/home/juan/dev/ArnesJuan-1.33.2/docs/seguridad/registro-seguridad.md` y
  `docs/qa/1.33.2-vuelta-2.md`). En **esta** línea la referencia queda colgando y el hallazgo es
  **invisible para la puerta**.
- **Por qué `instrumento` y no `contrato`:** ningún REQ de esta rama afirma algo falso sobre lo
  construido —el contrato quedó corregido, §5—, y lo que falta es el **registro** de un hallazgo del
  propio arnés, no una conducta del producto. **No se reclasifica ni se cierra `SEC-087`, `SEC-088`
  ni `QA-1332-01`**, que están fuera de mi encargo.
- **Dueño y forzador:** quien reconcilie #48 con la línea del parche, **antes de fusionar a `main`**
  (el momento en que las dos líneas se juntan es el forzador natural y no hace falta inventar otro).
  `docs/seguridad/` es del `auditor-seguridad`; se le señala en su turno, que va detrás de éste.

### Observación sin clase (no es hallazgo; se anota para no perderla)
El `deny` de un `Rigor: ligero (local)` dice *«el veredicto de QA es 'pendiente' (se requiere
`QA: aprobado`)»* — es **correcto y diagnosticable**, pero **no menciona que el matiz elevó el rigor
de `ligero` a `estandar`**, que es justo lo que la persona no espera. El contrato lo explica
(`requirements/README.md`), así que no queda nadie sin respuesta; enriquecer el mensaje es **mejora,
otra ventana, y no se abre hallazgo por ella** — se deja escrito aquí para quien la recoja.

---

## Lo que **no** se validó (dicho, no supuesto)

- El **resto de #48** y su historial: fuera del encargo.
- La **vía del paréntesis sin cerrar** (`SEC-087`): preexistente, registrada, otra reparación. Se
  comprobó únicamente que **ninguna sede promete lo contrario**, y eso pasa.
- **Rendimiento / coste**: no acreditado (§6). Esta máquina no es el runner.
- **CI**: corriendo al cerrar esta validación; su verde **no está incorporado** a este veredicto.
  Un rojo del banco en Linux obliga a revisar este `aprobado`.
- **Seguridad**: pendiente. Este REQ es `Sensible a seguridad: sí` por la política de
  autoalojamiento, así que **`QA: aprobado` no cierra nada**: falta `Seguridad: aprobado` y el gate
  humano de fusión (`AGENTS.md` §6). Se aprueba ahora, sin esperar al auditor, porque el auditor no
  puede firmar antes que QA.

## Evidencia en disco (toda re-derivable sin preguntar)

Base de todas las mediciones: `6e3bb901acd8bc5e29f2ea6afd8b7bbe0ef32939`; línea base del par:
`2f7c821`. En `docs/qa/1.34.0-porte-1.33.1-falsacion/`:

- `30-qa-sonda-puerta.sh` — la sonda (recibe `HOOKS_DIR`; monta proyecto propio; mide la **puerta**).
- `31-qa-sonda-puerta-ausencia-exige.sh` — la misma con `campos.ausencia_exige: true`.
- `32-qa-puerta-head-6e3bb90.txt` · `33-qa-puerta-padre-2f7c821.txt` — las 31 celdas en cada árbol.
- `34-qa-ausencia-exige-head-vs-padre.txt` — el camino de la ausencia, idéntico en los dos.
- `35-qa-diff-padre-head.txt` — el diff celda a celda: 10 `allow`→`deny`, **0** `deny`→`allow`.
- `36-qa-piso-y-contabilidad.txt` — la re-derivación a mano del piso y del cuadre.
