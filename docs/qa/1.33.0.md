# QA y medición — ventana 1.33.0

> **Qué es esto.** El registro de medición de la ventana 1.33.0. Los hallazgos por REQ viven en
> `docs/qa/1.33.0-hallazgos-*.md`; aquí van el coste de cada comisión y las mediciones de la ventana.

## El tema de la ventana

**Preguntas de estado, no preguntas de vía.** La ventana 1.32.1 produjo, sin buscarlo, cuatro
instancias del mismo defecto de forma en cuatro sitios con cuatro dueños distintos: los cinco casos de
banco vacíos, el barrido de migración, el control de datos de cliente y el guardián del intérprete.
Interrogar al mecanismo tiene una vía nueva cada vez; interrogar a la propiedad no envejece. El detalle
está en `docs/PLAN.md`.

**Y abre por las palancas de coste, no por los REQ del núcleo** (decisión del propietario, 2026-09-07).
El banco lo corre **cada** comisión que mide, decenas de veces: bajarlo de 92 s a ~40 s abarata toda la
ventana antes de escribir una línea del núcleo.

## Libro de comisiones — se anota al volver cada una, nunca al cerrar

Tres reglas nuevas, decididas midiendo el ciclo anterior: **tope de ~12 criterios por REQ**, **working
set declarado al empezar** (qué se lee y qué **no**), y **presupuesto desde la mediana medida**.

| # | REQ | Fase | Agente | Modelo | Tokens | Reloj | Presupuesto | Notas |
|---|---|---|---|---:|---:|---:|---|---|
| A | REQ-017 | análisis | `analista-requerimientos` | Opus | **83 775** | 6 min 33 s | 80 000 | 18 usos; **9 criterios**, bajo el tope. Working set declarado **y cumplido**, con una desviación de 32 líneas confesada |
| B | REQ-017 | implementación | `desarrollador` | Opus | **207 960** | 48 min 41 s | 200 000 | 81 usos; **se pasó** (104 %). Cociente 3,95 → **1,90**: la lectura pasa de cuadrática a lineal. **Escaló una contradicción de CA-05 en vez de resolverla solo** |
| C | REQ-017 | write-back (CA-05) | `analista-requerimientos` | Opus | **61 929** | 6 min 16 s | 60 000 | 11 usos. Encontró un **error de aritmética** en la propuesta del desarrollador **y en la de la coordinadora**, y se negó a poner `en-revisión` porque su cambio crea delta de implementación |

**La lección de CA-05, que vale para todo criterio de coste que venga después.** Una línea base contra
un **tag congelado** parece rigor y es una puerta que **mide una vez y luego envejece hacia el lado que
abre**: el banco crece a propósito, así que la igualdad de inventario contra v1.32.1 será falsa en la
primera sección que añada 1.34.0, y entonces alguien la re-fija **corriendo el propio banco que el
archivo dice validar** — una copia regenerada desde el objeto bajo prueba no valida nada.

De ahí la distinción que el criterio ahora declara y que antes nadie declaraba: **una comprobación
contra línea base congelada es acreditación de fail-before, no puerta permanente.** Lo permanente tiene
que ser **auto-anclado** — CA-03, el orden de crecimiento, que no depende de ningún tag, cuesta
milisegundos y habría visto H-07.

**Y el error de aritmética, anotado porque lo cometimos dos:** la corrida heredada de 76 s servía a la
igualdad de inventario **y** a la razón de reloj. Contarla como ahorro en las dos mitades daba ~45 s;
el número real de la propuesta era ~107 s — **por encima de los 92 s que el REQ venía a arreglar**. Dos
agentes y la coordinadora dieron por buena una resta sin comprobarla.

**Tres sondas suyas midieron cero antes de medir algo, y las tres salieron a la luz porque CA-06 les
exigía guarda.** Sin ella habrían salido **verdes**: (1) `jq --arg` con más de 128 KB revienta contra
`MAX_ARG_STRLEN`, deja el JSON vacío, el hook responde en 0,1 s y **la curva de coste salía plana y
perfecta**; (2) `git show` no preserva el bit de ejecución, así que el canario del corredor hijo no
arrancaba y el caso salía SKIP —fail-closed, dirección correcta— **por un motivo que no era el suyo**;
(3) `$BASHPID` dentro de `$( )` otra vez, el mismo que envenenó la ventana anterior.

El criterio que obliga a la sonda a delatarse **se pagó a sí mismo en su primera comisión**. Y las
tres son de la misma familia que todo lo demás de estas dos ventanas: **una medición en verde que no
acreditaba nada**.

## La magnitud que no miente, y por qué importa más que el arreglo

REQ-017 nace de que **`CA-08` de REQ-016 se cumplía**: fijaba el coste en **procesos por llamada**
—4 = 4, medido correctamente— mientras el reloj se multiplicaba por **10**. Una magnitud correcta,
medible sin ruido, y **ortogonal a lo que se degradó**.

El analista descartó las dos salidas obvias y dio la tercera:

| Magnitud | Por qué no basta |
|---|---|
| **Procesos por llamada** | Ortogonal a la degradación real: no ve un 10× de reloj |
| **Tiempo absoluto** (`≤ 40 s`) | Depende de la máquina, del runner del CI y de la **carga**. Esta ventana midió una sonda que sobrevivió **3 h 41 min** a su comisión y envenenó la línea base de otro agente, que concluyó «dentro del ruido» con toda lógica. Un criterio que se falsea con `nice` no es un criterio |
| **Orden de crecimiento** (elegida) | **Doblar la entrada y comparar el cociente**: lineal ≈ 2, cuadrático ≈ 4. La velocidad de la máquina **se cancela algebraicamente**. Estadístico: el **mínimo** de k repeticiones, nunca la media — la carga sólo puede **añadir** tiempo, así que el mínimo es el estimador robusto contra el ruido que la media absorbe |

Complemento: donde hace falta un absoluto, **razón contra una línea base medida en la misma corrida**
(«≤ 0,25× lo que cuesta el árbol heredado, ahora, aquí, con el mismo inventario»), porque un runner
lento sube numerador y denominador.

**Y la parte que sobrevive al arreglo:** CA-07 lleva a `requirements/README.md` y a la plantilla
heredable una **cuarta forma prohibida** de criterio — «fijar la magnitud equivocada»— con su caso
medido. Sin ese write-back, esto se arregla una vez y reaparece a la tercera ventana. Es el mismo
patrón que `ADR-002` y `SEC-025`: **una comprobación que fija la magnitud equivocada envejece hacia el
lado que abre**.

---

## Mediciones del `desarrollador` — REQ-017 (2026-09-07)

Máquina: WSL2, 12 núcleos, bash 5.3.9. Todas las razones se midieron **en la misma corrida** que su
línea base, materializada desde su tag con `git show` (y `chmod +x`: `git show` escribe el
contenido, no el modo).

### El arreglo, en una sentencia

`hooks/lib.sh`, primera sentencia de `arnes_sin_cita`:

```
antes:  case "${l%$'\r'}" in *$'\r'*)      # eliminación de sufijo CON PATRÓN: O(n) intentos de O(n)
ahora:  case "$l" in *$'\r'?*)             # «un CR con al menos un carácter detrás»
```

Equivalencia **por construcción**, no por casuística: el único CR que la eliminación podía retirar
es el final, y sólo si está al final; así que «tras quitar un CR final queda un CR» y «existe un CR
en una posición que no es la última» son la misma proposición. La guarda **no se movió**: sigue
siendo la primera sentencia del único escáner (restricción anti-deriva de REQ-016).

### Las magnitudes

| Medida | Antes (v1.32.1) | Después | Techo contratado |
|---|---|---|---|
| CA-03 · cociente de duplicación de `arnes_sin_cita` (70 000 → 140 000 B, k=20, mín. de 3) | **3,95** | **1,90** | ≤ 2,6 |
| CA-04 · camino de campo contra v1.32.0 (140 000 B sin CR) | **7,9×** | **1,27×** | ≤ 2,0× |
| CA-05 · sección 32 aislada, `ARNES_JOBS=1` | **76,19 s** | **9,60 s** → **0,125×** | ≤ 0,25× |
| CA-08 (i) · procesos por llamada, REQ de 6 líneas / cabecera de 200 | 5 / 5 | **5 / 5** | 0 añadidos |
| CA-08 (ii) · reloj, REQ de 6 líneas | 0,0991 s | 0,0989 s → **0,998×** | ≤ 1,25× |
| CA-08 (ii) · reloj, cabecera de 200 líneas | 0,1475 s | 0,1476 s → **1,000×** | ≤ 1,25× |
| Banco completo (44 secciones, `JOBS=6`) | **95,66 s** | **45,14 s** | — |
| CA-02 · inventario ordenado caso→veredicto | 828 casos | **idéntico byte a byte** | — |

Sobre el 1,17× que la primera vuelta dio en CA-08 (ii) con 200 líneas: era **ruido**. Repetido
alternando el orden de los dos árboles, el mínimo de seis series da 1,012×. Una sola medición de
una razón no es una razón.

### CA-09 · la pared de los 60 s (medición, no movimiento — SEC-030 sigue abierto)

Sonda: un `Write` cuya cabecera es **una sola línea** de longitud creciente. El orden de
crecimiento se mide **restando el coste fijo de arranque** (~0,21 s): sin restarlo, a 96/192 KB el
arranque todavía pesa tanto como el escaneo y el cociente sale ~1,3 sobre un camino que es
cuadrático — la sonda colocaba la pared en 4,25 MB donde está en 1,5.

| Árbol | Orden medido | 60 s en | Comprobado aparte a 256/512/1024 KB |
|---|---|---|---|
| este árbol | 2,01 | **≈ 1,60 MB** | 1,49 MB (27,05 s a 1 MB) |
| v1.32.1 | 2,00 | **≈ 0,94 MB** | 0,86 MB (81,43 s a 1 MB) |
| v1.32.0 | ~1 | **no medible en este rango** | 0,61 s a 1 MB: el coste apenas depende del tamaño |

**Los dos árboles siguen siendo cuadráticos en este camino**, tal como REQ-017 anticipa: lo que
queda cuadrático es `arnes_norm_clave` (`${1//$'\r'/}` y las eliminaciones con patrón que hace
después), preexistente y fuera de alcance. Este arreglo **sube** la pared de 0,94 a 1,60 MB.

Discrepancias con las cifras de referencia del REQ, y su causa probable: v1.32.1 ≈ 0,99 MB (REQ)
frente a 0,94 medido aquí — la misma cifra. v1.32.0 ≈ 1,56 MB (REQ) frente a «no medible» aquí: en
v1.32.0 **no existe** `arnes_sin_cita` y su camino de cabecera es casi plano en Linux; el 1,56 MB
sólo se explica por otra plataforma (MSYS) o por otro punto del camino.

### Lo que costó la propia medición

Dos sondas mías midieron cero antes de medir algo, y las dos por el mismo motivo — **el
instrumento, no el código**:

1. `jq --arg` con más de 128 KB (`MAX_ARG_STRLEN`) muere, deja el JSON **vacío**, el hook no recibe
   nada y responde en 0,1 s. La curva de CA-09 salía plana y perfecta. Es la lección que ya estaba
   escrita en `emite_bash` del corredor, y la volví a pagar. El contenido va por **stdin**, y la
   sonda **aborta si el JSON sale vacío**.
2. El árbol heredado extraído con `git show` **no lleva bit de ejecución**, así que el canario del
   corredor hijo no arrancaba y la corrida heredada de CA-05 salía «sin casos». El veredicto era
   SKIP —fail-closed, dirección correcta— pero **por un motivo que no era el suyo**.
3. Y un error de bash mío: `$BASHPID` **dentro de `$( )`** es el PID del subshell de la
   sustitución, no el de la sección. Cada corrida escribía su salida en un archivo con otro nombre
   y el inventario leía uno vacío.

Las tres se detectaron porque la sonda tenía guarda («una corrida que no produjo casos no es una
corrida rápida: es una que no midió»). Sin esas guardas, las tres habrían salido **verdes**.

### Desviación que escalo, y es la más importante: CA-05 cuesta más que la regresión que certifica

Implementado **tal como está contratado**, y por eso lo escribo en vez de resolverlo yo.

CA-05 exige correr la sección 32 **aislada, con `ARNES_JOBS=1`, contra los dos árboles y en la misma
corrida**, y el mínimo de **3** relojes de este árbol. El denominador —la corrida heredada— cuesta
**76 s**, porque es literalmente el defecto corriendo. Coste total de la sección 37/2: **~120 s**
(3 × 9,6 + 76 + ~13 de CA-08). El corredor despacha 44 secciones con `JOBS=6` y las 37 salen al
final del glob, así que el banco pasa de **45,14 s a 144,76 s** (medido, en verde).

Es decir: **la puerta requerida de `main` queda más lenta (145 s) que la regresión que este REQ
arregla (92 s)**, y de forma permanente, porque la línea base es un tag fijo. Contradice la Historia
del propio REQ-017 («para no pagar 92 s donde pagaba 39 s»), así que no lo decido yo.

Lo que ya hice para abaratarlo **sin tocar el criterio**: una sola corrida heredada en vez de tres.
Es la lectura literal («el mínimo de 3 relojes **de este árbol**») y el hueco que abre lo cierra el
margen: el techo es 0,25× y lo medido es 0,125×, así que la línea base tendría que inflarse **al
doble** para que un verde fuera falso, y la carga sólo puede añadir tiempo al numerador también.
Tres corridas heredadas costarían 150 s más para cerrar un hueco que el margen ya cierra.

**La alternativa que propongo al analista, si se decide bajar el coste** (no la adopto por mi cuenta:
debilitaría un criterio, que es el pecado que esta ventana persigue):

- **(ii) el reloj** no necesita el valor del denominador, sólo una **cota inferior**. Basta correr la
  heredada bajo `timeout` de `4 × mín(este)` ≈ 39 s: si el `timeout` salta, entonces
  `heredada > 4 × este` y el cociente es `< 0,25` — demostrado, no estimado, y en la dirección
  cerrada. Ahorra ~37 s.
- **(i) el inventario** es lo que obliga a que la heredada **termine**. Se puede cerrar contra el
  inventario **registrado** del tag (un archivo versionado junto al banco, comparado byte a byte)
  en vez de contra una corrida viva. Pierde «la misma corrida» —que para un inventario **no aporta
  nada**: un inventario no depende de la velocidad de la máquina— y ahorra los 76 s enteros.
- Con las dos, la sección 37/2 baja de ~120 s a **~45 s** y el banco vuelve a ~50 s, sin que ningún
  techo se relaje.

Mientras no se decida, la sección se paga entera por defecto —una puerta que no se ejecuta no
mide— y `ARNES_COSTE_RUTA_CRITICA=0` la apaga **diciéndolo**: sus dos casos emiten **SKIP con ese
motivo**, nunca PASS.

### Desviación menor declarada: la línea base de CA-04

CA-04 pide medir «`arnes_sin_cita` de este árbol y **la del tag v1.32.0**». En v1.32.0 **no existe**
`arnes_sin_cita`: la noción de cita nace en 1.32.1 (REQ-016). Lo comparable es **la misma boca** —la
función que recibe una línea cruda de cabecera y devuelve un campo—: `arnes_norm_clave` en v1.32.0
frente a `arnes_campo_linea` en este árbol, que es `arnes_sin_cita` **más** `arnes_norm_clave`. Es
además la lectura **estricta**: mide el coste que la guarda añadió, incluyéndose a sí misma (1,27×
frente al 0,21× que daría comparar sólo `arnes_sin_cita`). El techo contratado (≤ 2,0×) se mantiene.

El «49×» que CA-04 cita como valor de hoy **no se reproduce en Linux**: aquí el árbol enfermo da
**7,9×** sobre la misma línea base. No afecta al criterio —lo que se contrata es el techo, y el caso
**falla** contra v1.32.1 y **pasa** contra este árbol, que es lo que CA-04 exige—, pero conviene que
quede escrito: una razón medida en otra plataforma no es transportable, y por eso el criterio es una
razón y no un número de segundos.

### Lo que NO hice, y por qué

- **`requirements/REQ-017.md` no se toca** (instrucción de la coordinadora: es del analista). Quedan
  pendientes de su mano: `Estado: en-progreso` → `en-revisión`, y la entrada de **Historial de
  cambios** que CA-09 exige con las cifras de la tabla de arriba (≈ 1,60 MB este árbol · ≈ 0,94 MB
  v1.32.1 · v1.32.0 no medible en este rango).
- **`.arnes/config.json`, `.claude-plugin/`, `.github/` y `docs/ESTADO.md`**: fuera de mi comisión.
  El banco nuevo **no** necesita cambios de CI: el corredor descubre las secciones con un glob.

---

## Mediciones del `desarrollador` — REQ-017, delta de CA-05 + H-08 (2026-09-07)

Misma máquina que arriba: WSL2, 12 núcleos, bash 5.3.9, `timeout` de uutils 0.8.0.

### El defecto invertido y el plazo derivado

`ARNES_COSTE_RUTA_CRITICA` pasa a **apagada por defecto** y se enciende con `=1`. La corrida
heredada ya no se mide: se **acota** con `timeout` de **4 × mín(este árbol)**, derivado en la misma
corrida y **después** del numerador, en segundos y **redondeado hacia arriba** —redondear abajo
probaría una desigualdad más floja que la contratada—:

```
plazo47=$(( (u_este47 * 4 + 999999) / 1000000 ))    # µs -> s, techo
ARNES_JOBS=1 ARNES_HOOKS_DIR="$HER47/hooks" \
  "$TIMEOUT47" -k 5 "$plazo47" bash "$BANCO47" secciones/32-huecos-auditoria-r001.sh > "$OUT_HER47" 2>&1
```

Sin `--foreground`, `timeout` pone al hijo en **su propio grupo de procesos** y señala al grupo
entero: el corredor heredado muere con sus subshells. Comprobado aparte con un nieto de 8 s bajo un
plazo de 2 s — el nieto no sobrevive. Sin esa propiedad, cada corrida encendida dejaría **76 s** de
sección 32 huérfana envenenando el reloj de la siguiente, que es exactamente el fallo de 3 h 41 min
que CA-06 existe para no repetir.

**El vencimiento es PASS, no SKIP.** Es la única lectura correcta: si el plazo vence,
`heredada > 4 × este` y el cociente contratado (≤ 0,25×) queda **demostrado**. Lo que se deja de
conocer es el *valor* de la razón, que CA-05 no pide. Un SKIP ahí convertiría el hallazgo en
silencio. Los SKIP que quedan son los que CA-06 nombra: sin tag, sin `timeout`, sin corredor, o con
el numerador **bajo el suelo de 50 ms** (guarda nueva: un numerador dentro del ruido produce un
plazo dentro del ruido, y la cota se apoyaría en él). Y uno más, del mismo linaje: si la heredada
**termina sin producir un solo caso**, no se lee como «terminó ⇒ FAIL» sino como SKIP — *una cota
sobre una corrida que no midió no es una cota*.

**Fail-before / pass-after de la rama nueva**, las dos medidas:

| Vuelta | Qué se corrió | Resultado |
|---|---|---|
| pass-after | este árbol vs v1.32.1, `ARNES_COSTE_RUTA_CRITICA=1` | **PASS** — la heredada no terminó en **36 s** = 4 × 8,81 s |
| fail-before | `ARNES_HOOKS_DIR` := v1.32.1, o sea «este árbol» **es** el enfermo | **FAIL** — la heredada terminó dentro de **285 s** = 4 × 71,17 s (rc 0) |

La mitad (ii) pasa a ser **auto-anclada**: las 3 corridas cronometradas de este árbol producen casos
y dan el **mismo** inventario ordenado entre sí (40 casos). La igualdad contra el árbol heredado la
cierra **CA-02** sobre el banco entero, con el mismo comparador, y aquí ya no se duplica.

### Lo que le cuesta a la puerta requerida recuperar los tags (H-08)

Medido, no estimado, porque era la condición de la aprobación. «Sin tags» se reproduce copiando el
árbol **sin `.git`**: `mat47` no encuentra `refs/tags/v1.32.1` y toma la misma rama que el CI.

| Corrida | Casos | Reloj (`JOBS=6`) |
|---|---|---|
| Banco **sin tags** (= CI antes de H-08) | 833 PASS · 0 FAIL · **12 SKIP** | **45,85 s** |
| Banco **con tags** y 37/2 apagada (= CI después) | **842 PASS** · 0 FAIL · **3 SKIP** | **55,98 s** |

**+10,1 s (+22 %) compran 9 criterios que pasan de no medirse a medirse.** La simulación se valida
sola: el «sin tags» reproduce **exactamente** el `833 PASS, 0 FAIL, 12 SKIP` del PR #43.

Los 3 SKIP que quedan son los que deben quedar: uno de plataforma (`cygpath`, sólo Windows) y los
dos de CA-05, que citan el número acreditado y su fecha.

Coste del checkout, mínimo de 3 clones locales: **0,202 s** superficial y sin tags · **0,346 s**
completo (`.git` de 1,2 MB → 1,6 MB, 40 tags). **+0,14 s.** El paquete pesa 540 KiB: la historia
completa no es un coste que haya que optimizar, y por eso se eligió `fetch-depth: 0` antes que
`fetch-tags: true` — éste mantiene la profundidad 1 y deja la prueba colgando de las semánticas del
clon superficial, cuyo modo de fallo **es H-08**: medio funciona y se lee como verde.

Con la sección encendida a mano, 37/2 cuesta **76,1 s** (antes ~120 s) y apagada **12,5 s**.

### Lo que NO hice en este delta, y por qué

- **`Estado:` de REQ-017 sigue `en-progreso`** (instrucción de la coordinadora): lo mueve QA y lo
  firma el auditor, no yo.
- **La consecuencia 3 de H-08 sigue abierta**: un SKIP honesto agregado a un resultado global se
  sigue leyendo como verde. Que hoy en CI no quede casi ninguno es una propiedad del **entorno**, no
  del corredor. Es la palanca «¿esta prueba mide algo?», que ya está en 1.33.0 con este hallazgo
  como forzador medido.

---

## Validación de QA — REQ-017 (2026-09-07, vuelta 0 de 3, Opus)

Máquina: WSL2, 12 núcleos, bash **5.3.9(1)**, `timeout` de uutils 0.8.0, locale del entorno
`C.utf8`. Rama `cand/1.33.0` @ `1cd7be5`, árbol limpio salvo `docs/ESTADO.md`.

### Quality gates de `AGENTS.md` §7

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | **OK** |
| `jq -e . hooks/hooks.json` | **OK** |
| `jq -e .` sobre `plugin.json` y `marketplace.json` | **OK** |
| Banco completo (`tests/escenarios/hooks/run.sh`) | **842 PASS · 0 FAIL · 3 SKIP · rc 0**, en 51,47 / 48,02 / 47,31 s (3 vueltas) |
| Cola de `PENDING_APPROVAL.md` | 0 pendientes |
| Lockfile / manifiesto | **no aplica**: bash + jq, sin gestor de paquetes |

**Estabilidad del banco:** 3/3 vueltas con el mismo veredicto en los 845 casos. Fuera de la
sección 37 el inventario ordenado es **idéntico byte a byte** vuelta a vuelta; dentro de la 37
varía porque el *nombre* del caso lleva el número medido. Ningún caso flaky.

### Veredicto por criterio

| CA | Veredicto | Evidencia |
|---|---|---|
| CA-01 | **con hallazgo** | El banco pasa (214 entradas, idéntico byte a byte) pero hay contraejemplo reproducible fuera de su corpus → **QA-017-01** |
| CA-02 | **PASS** | Verificado por QA aparte del banco: banco entero menos la 37, hooks de este árbol vs los de v1.32.1 → **828 casos, `cmp` idéntico**, md5 `745dcd59…` |
| CA-03 | **PASS** | 6 corridas: 2,141 · 1,998 · 2,034 · 1,944 · 1,943 · 1,821 (techo 2,600). Fail-before sobre v1.32.1: 3,602–4,332, siempre por encima del techo. Separación robusta |
| CA-04 | **PASS** | 6 corridas: 1,199–1,387 (techo 2,000). Fail-before: 8,27–9,92× |
| CA-05 | **PASS con hallazgos** | Encendida a mano: (i) y (ii) PASS, la heredada no terminó en 39 s = 4 × 9,69 s. Pero la puerta se deja comprar → **QA-017-03** y **QA-017-04**. (iii) el número y la fecha están en el Historial |
| CA-06 | **PASS** | Las tres mitades pasan. La palanca `ARNES_COSTE_RUTA_CRITICA` se probó con **18 variantes** (vacía, `0`, `1`, ` 1 `, `SI`, `Sí`, `TRUE`, `on`, `yes`, `2`, `verdadero`, `1;rm`, tab…): **fail-closed en todas** y el valor no reconocido **se dice** en el motivo del SKIP |
| CA-07 | **PASS** | Forma (d) «fijar la magnitud equivocada» presente en `requirements/README.md` **y** en `templates/requirements-README.md.tpl`, con el caso medido (CA-08 de REQ-016) y la línea de la Definition of Ready en las dos |
| CA-08 | **PASS, con margen estrecho** | (i) 5 = 5 procesos en los dos casos y los dos árboles. (ii) 6 medidas: 1,081 · 1,158 · 1,217 · 1,204 · 1,032 · 0,984 contra techo 1,25× → **QA-017-06** |
| CA-09 | **PASS de criterio, sonda no reproducible** | La medición existe y cada cifra lleva su corrida, que es lo único que CA-09 exige. Pero la sonda no repite → **QA-017-05** |

### Los tres SKIP, uno a uno

1. `ruta estilo Windows con backslashes -> deny (sin cygpath)` — **honesto**. Leído el código
   (`04-windows-formas-del-manifiesto.sh:20-29`): el caso construye la ruta con `cygpath`, que
   sólo existe en MSYS; `type -P cygpath` no lo encuentra aquí y `uname -s` es `Linux`. **No
   esconde ningún caso ejecutable en Linux**, y el corredor lo cuenta en el cuadre a propósito.
2. y 3. `CA-05 (i)` y `(ii)` — **por diseño y con el motivo escrito**: `ARNES_COSTE_RUTA_CRITICA`
   apagada, citando el número acreditado y su fecha. Comprobado que la rama sí mide cuando se
   enciende: con `=1` las dos dan **PASS** en 84,29 s. No es un SKIP que oculte nada; es el que
   H-08 enseñó a escribir.

### Hallazgos

**QA-017-01 · `contrato` · CA-01 afirma una equivalencia que tiene contraejemplo reproducible.**
CA-01 contrata que, para **una línea cualquiera**, el estado publicado coincide **byte a byte**
con v1.32.1, y titula «El comportamiento no cambia». No es cierto.

*Reproducción* (locale UTF-8, bash 5.3.9), línea `\ \ 0xc3 \ CR ] CR` — y con **cualquier**
prefijo ASCII, incluidos `Modulo: `, `Estado: ` y `QA: `, probadas longitudes 0 a 40:

```
v1.32.1  -> cita=0 | cr=0 | crlinea=<>              <- NO ve el CR interior
este     -> cita=0 | cr=1 | crlinea=<…\r]\r>        <- lo ve
```

3/3 corridas, estable. *Causa medida*: en bash 5.3.9 bajo locale UTF-8, `${l%$'\r'}` sobre una
cadena con una secuencia multibyte inválida **no devuelve un sufijo**: devuelve basura, y
distinta en cada evaluación de la **misma** entrada dentro del **mismo** proceso —
`5c c3 5c 01 3f 01 0d` → `5c 7d ff c3 3f 57`, `5c 6c ff c3 3f 57`, `5c 72 ff c3 3f 57`…
Bajo `LC_ALL=C` es correcto y estable. La sentencia nueva no evalúa esa eliminación de sufijo,
así que es **determinista y correcta**.

*Consecuencia, y es buena*: REQ-017 **no sólo abarata — cierra un fallo en abierto de v1.32.1**
en la guarda que `AGENTS.md` §13 describe («un CR que no es el que la termina no deja pasar»).
Eso hay que declararlo, igual que se declaró que sube la pared de los 60 s.

*Lo que NO conseguí, y lo digo*: **no reproduje una decisión distinta del guardián**. Monté un
REQ-900 con esa secuencia en la cabecera y `guard-completado` **denegó con los dos árboles**: la
puerta recorre la cabecera **dos veces** (`ARNES_CR_INTERIOR` y `ARNES_ESTADO_CR`) y en v1.32.1
el segundo recorrido sí lo cazó. La divergencia está probada **en la función que CA-01 nombra**,
no en el veredicto de la puerta.

*Cierra con*: write-back del `analista-requerimientos` — CA-01 declara la clase de entrada en que
las dos implementaciones difieren y **en qué dirección**, o un criterio nuevo que recoja el
fallo-en-abierto cerrado. No lo reescribo yo (`AGENTS.md` §9).

**QA-017-02 · `instrumento` · el corpus de CA-01 no puede producir el contraejemplo.**
El generador de `37-…-1-escala.sh` sortea entre 8 fichas, **todas ASCII** (`CR`, `<!--`, `-->`,
`Estado: `, `x`, ` `, `<!`, `--`). Ninguna combinación produce un byte multibyte inválido, así
que el diferencial acredita la equivalencia sobre un corpus que **excluye por construcción** la
única clase conocida donde no la hay. Es la forma exacta que esta ventana persigue: un verde que
no acredita lo que su nombre dice. *Cierra con*: una ficha de byte inválido en el alfabeto del
corpus (dueño `desarrollador`).

**QA-017-03 · `instrumento` · CA-05 concede PASS a un numerador que falló.**
`corre47` **descarta el rc del corredor interno** y sólo exige ≥1 línea de veredicto; (ii) compara
las 3 corridas **entre sí**, y tres corridas igualmente rotas coinciden. Demostrado: con
`ARNES_HOOKS_DIR` apuntando a un `guard-completado.sh` que siempre permite, la sección 32 sale en
**14 FAIL, rc 1, 3,79 s**; el plazo cae a 16 s, la heredada vence y las dos mitades dan **PASS** —
incluida la que se llama «la comparación no se compra dejando de probar» (40 casos, mismo
inventario). *Cierra con*: exigir `rc = 0` (o 0 FAIL) en la corrida del numerador. Dueño
`desarrollador`; forzador: la próxima vez que CA-05 se encienda.

**QA-017-04 · `instrumento` · `rc=137` no prueba vencimiento, y el caso lo declara «DEMOSTRADO».**
Medido: matar al hijo **desde fuera** a los 0,6 s de un plazo de 60 s devuelve **137**; matar al
propio `timeout`, también 137. Sólo **124** prueba que el plazo venció. El caso acepta los dos y
emite «heredada > 4 × este, luego el cociente es ≤ 0,250× — DEMOSTRADO, no estimado». La corrida
heredada es el camino cuadrático —el que más memoria y forks gasta—, así que en un runner apretado
el OOM killer es su modo de muerte más probable, y produciría un verde falso etiquetado como
demostración. *Cierra con*: corroborar por reloj (`t1-t0 ≥ plazo`), que ya está a mano, o aceptar
sólo 124.

**QA-017-05 · `instrumento` · la sonda de CA-09 no repite, y la «divergencia declarada» es su
dispersión.** Seis corridas de **este árbol**: **1,08 · 1,32 · 1,78 · 2,64 · 2,65 · 3,98 MB**, con
orden medido **1,41 a 2,33**. El REQ acredita 1,60 MB / orden 2,01, y declara abierta una
divergencia con un 1,46 posterior. **No son dos series que discrepan: son dos extracciones de la
misma distribución**, y mi serie contiene ambas y más. v1.32.1 sí es estable (1,01–1,19 MB), así
que la dirección del beneficio se sostiene 6/6; **la magnitud escrita (~0,66 MB de margen ganado)
no**. *Causa medida*: `u0`, `ua` y `ub` son **una sola muestra cada uno** —contra la regla que la
propia sección enuncia dos pantallas más arriba, «el mínimo de k, nunca la media»—; `a` y `b` son
**diferencias de muestras únicas**, y la extrapolación lleva `p` en el **exponente**
(`MB = n·((60e6−u0)/b)^(1/p)`), de modo que un `p` de 1,41 frente a 2,01 mueve la pared un factor
~2,5. Se ve además la bimodalidad: `ub` alterna entre ~0,91 s y ~1,01 s y de ahí salen los dos
grupos de pared. Y `u0` osciló **0,10–0,21 s** según la corrida fuera aislada o dentro del banco
con `JOBS=6`: se resta un arranque que no es el de la medición. *Dueño*: **SEC-030**, que es de
quien es la pared. **No se cierra aquí** y **no bloquea**.

**QA-017-06 · `instrumento` · CA-08 (ii) roza el techo en la puerta requerida de `main`.**
Seis medidas: 1,081 · 1,158 · 1,217 · 1,204 · 1,032 · 0,984 contra techo **1,25×**. El máximo
observado queda a un **2,6 %** del techo. El propio desarrollador midió que **alternando el orden
de los dos árboles** el mínimo de seis series da 1,012×: la varianza es del **procedimiento** —los
dos árboles se miden en bloque y no intercalados—, no del arreglo. Un runner cargado puede poner
en rojo la puerta de `main` sin que haya regresión. *Cierra con*: intercalar las series. No se
toca el techo: es `operativo` y su dirección admitida es **bajar**.

**QA-017-07 · `instrumento` para REQ-017, y escalado al `auditor-seguridad`** — **un lector de
guardián que no es determinista**, preexistente y **fuera del alcance de este REQ.**
`arnes_norm_clave` —idéntica en v1.32.1 y en este árbol— hace `${1//$'\r'/}` y varias
eliminaciones con patrón. Bajo locale UTF-8, con una línea de cabecera que **empieza** por una
secuencia multibyte inválida, devuelve **una clave distinta en cada llamada con la misma entrada**:
`\xc3\x5cQA: aprobado` → `clave=<,Ø?d>`, `<ۉØ?d>`, `<ØXØ?d>`, `<"��?d>`, `<���?d>`… en seis
llamadas consecutivas del mismo proceso. Bajo `LC_ALL=C` es estable. Hoy la basura no se parece a
ninguna clave conocida, así que el efecto observado es fail-closed (la línea no declara campo);
pero **un lector no determinista en una puerta no es una propiedad que se pueda dejar al azar**.
REQ-017 **reduce** la exposición —quita una de las dos operaciones con patrón por línea— y no la
introduce: por eso va con dueño propio, como se hizo con SEC-030, y **no** reabre este REQ.

### Lo que se miró y NO encontró nada

- **La equivalencia de la sentencia, atacada de cinco maneras.** (1) Exhaustivo hasta longitud 3
  sobre un alfabeto de 21 símbolos con **todos los metacaracteres de glob** (`* ? [ ] \ ! ^ - ( )
  | +`), tab, espacio y bytes altos: **9 724 entradas, 0 divergencias**. (2) Aleatorio de 40 000
  cadenas de hasta 8 símbolos, bajo **2 locales × 5 combinaciones de `shopt`** (`extglob`,
  `nocasematch`, `globasciiranges`, `nocaseglob`, `nullglob`, `failglob`): **0 divergencias bajo
  `LC_ALL=C` en las cinco**. (3) Fronteras a escala: CR en las posiciones 0, 1, 2, 65 535 y
  100 000 con colas de 0, 1 y 2 caracteres. (4) Cadenas de 1, 2, 3, 4 y 10 CR consecutivos al
  final, con y sin prefijo — que es donde la eliminación de sufijo quita **uno**. (5) Barrido de
  los **255 bytes** posibles después de un CR, y de `0xc3 + <byte> + CR`, y de `<byte> + 0xc3 + CR`.
  La única familia divergente es la de **QA-017-01**, y ahí gana la implementación nueva.
- **El diff de código.** Contra v1.32.1, todo `hooks/` y `tools/` cambia en **una sola línea**
  (`diff` sin comentarios de `lib.sh`: 1 línea; `git diff --stat` sobre `hooks/` y `tools/`: sólo
  `lib.sh`). No hay nada más que validar en el producto, y eso es lo que hace creíble a CA-02.
- **La guarda no se movió.** Sigue siendo la primera sentencia de `arnes_sin_cita`, y
  `arnes_campo_linea` sigue siendo la única boca (`grep` de las cuatro bocas: `arnes-lectura.sh`
  y `arnes-paralelo.sh` siguen entrando por `arnes_campo_linea`). La restricción anti-deriva de
  REQ-016 se cumple.
- **El redondeo del plazo de CA-05 va en la dirección segura.** `⌈4u⌉` alarga el plazo, así que el
  vencimiento prueba una desigualdad **más fuerte** que la contratada. Correcto tal como se razonó.
- **El suelo de 50 ms del numerador de CA-05 es inalcanzable por abajo en la práctica** (la
  sección 32 de este árbol tarda ~9,7 s), así que no es por ahí por donde se cuela un plazo
  absurdo: se cuela por el numerador **rojo** de QA-017-03.
- **`timeout` sin `--foreground` mata al grupo entero**: tras encender CA-05 no quedó ni un
  proceso de la corrida heredada vivo. La propiedad que el desarrollador comprobó con un nieto se
  sostiene también con la sección real.
- **Mis propias sondas no sobrevivieron a esta comisión** (CA-06 aplicado a mí mismo): ningún
  `sleep`, `timeout` ni corrida de banco huérfana al terminar. El `sleep 900` que hay en la
  máquina es de otro shell de la sesión, no de esta validación.

### Sobre el endurecimiento de CA-09 por el analista

**Sigue siendo cumplible y no me dejó sin poder firmar.** Lo que CA-09 exige es la **medición y su
procedencia**, no un valor; el Historial ya nombra la corrida de cada cifra («la corrida de Linux
del 2026-09-07, la del delta de implementación»). El endurecimiento hizo justo lo que decía que
haría: **al obligar a que cada número lleve su corrida, dejó ver que el número no repite** — que
es QA-017-05. Un criterio que se paga a sí mismo en su primera validación.

### Coste de esta comisión

| | |
|---|---|
| Reloj | **30 min** (15:29 → 15:59) |
| Tokens declarados por el agente | ~**205 000** |
| Tokens con la corrección de subestimación (25–38 %) | **256 000 – 283 000** |


---

## Validación de QA — REQ-017 (2026-09-07, **vuelta 1 de 3**, Opus)

Máquina: WSL2, 12 núcleos, bash **5.3.9(1)**, `timeout` de uutils 0.8.0, locale del entorno
`C.UTF-8`. Rama `cand/1.33.0` @ `1792152` **más los cambios sin comitear en `tests/`**
(`README.md`, `run.sh`, `37-…-2-la-seccion-caliente.sh`). Árbol por lo demás limpio salvo
`docs/ESTADO.md`.

**Condiciones de medida, declaradas por corrida.** Corrí en paralelo con **una** comisión de
analista, que piensa y no mide. `load` al empezar cada bloque va anotado junto a su cifra; los dos
rojos de CA-08 (ii) se tomaron con `load` **0,91** y **4,04** —el más bajo de los dos dio el peor
número—, así que la carga **no** explica el hallazgo.

### Quality gates de `AGENTS.md` §7

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | **OK** |
| `bash -n` sobre `run.sh` y las 44 secciones | **OK** |
| `jq -e . hooks/hooks.json` | **OK** |
| `jq -e .` sobre `plugin.json` y `marketplace.json` | **OK** |
| Banco completo | **844 PASS · 0 FAIL · 3 SKIP · rc 0** en 3 vueltas (60,2 / 56,0 / 56,0 s) — y **1 FAIL** en una cuarta (abajo) |
| Cuadre total | 847 = 844 + 3, con `CASOS_ESPERADOS=847` y `CASOS_ESPERADOS_SECCION=10` en 37/2 |
| Cola de `PENDING_APPROVAL.md` | **0 pendientes** |
| Lockfile / manifiesto | **no aplica**: bash + jq, sin gestor de paquetes |

**El banco NO es estable: 1 de 4 vueltas completas salió en rojo.** El rojo es
`REQ-017 CA-08 (ii) un REQ real de 6 líneas` con **1,443×** contra un techo de 1,250×, con `load`
0,91 al arrancar. Detalle en **QA-017-06**, que sigue abierto.

### Veredicto por criterio — los diez

| CA | Veredicto | Evidencia |
|---|---|---|
| **CA-01** | **FALLA** | La partición dominio/fuera está trazada por la **propiedad equivocada**: 104–106 de 106 entradas multibyte inválidas caen **DENTRO** del dominio (la heredada es determinista sobre ellas con k=6) y **6–7 de ellas divergen byte a byte**. 4/4 corridas. Y la implementación que el criterio nuevo exige —corpus con la clase, clasificación por corrida, FALLA si el corpus la perdió, SKIP si la máquina no tiene el defecto— **no existe**: el corpus sigue siendo 100 % ASCII → **QA-017-01**, **QA-017-02** |
| **CA-02** | **PASA** | Mismo banco, hooks de este árbol contra los de v1.32.1: **828 casos idénticos byte a byte**, `md5 31400a13e34f50bacd6b60c55a3bbc9c`. Cuadre por archivo cerrado, ningún PASS→SKIP. El cambio de texto de tres mensajes **no lo rompe** (razonado abajo) |
| **CA-03** | **PASA** | 4 corridas: 1,810 · 1,977 · 1,989 · 2,011 (techo **2,600**). Fail-before sobre v1.32.1: 3,410 · 3,562 · 4,233 · 4,483 — siempre por encima. Separación robusta |
| **CA-04** | **PASA** | 4 corridas: 1,179 · 1,239 · 1,256 · 1,333 (techo **2,000**). Fail-before: 9,207 · 9,930 · 9,958 · 10,424× |
| **CA-05** | **PASA (i)(ii)(iii)** | Encendida a mano: la heredada no terminó en 39 s = 4 × 9,66 s **y el reloj lo corrobora (39,17 s)**; (ii) las 3 corridas del numerador en verde (rc 0, 0 FAIL) y mismo inventario entre sí (40 casos); (iii) el número y su fecha están en el Historial. Fail-before reproducido de extremo a extremo: con `ARNES_HOOKS_DIR` := v1.32.1 la heredada **terminó** en 77,12 s dentro de 289 s → **FAIL**. Queda **QA-017-09**, que no lo hace fallar |
| **CA-06** | **PASA** | Las causas nuevas de SKIP verificadas de extremo a extremo (no sólo en sintético). Palanca fail-closed y **diagnosticable**. Ninguna sonda mía sobrevivió a la comisión |
| **CA-07** | **PASA** | Forma (d) presente y **byte a byte idéntica** en `requirements/README.md` y en `templates/requirements-README.md.tpl` (el único delta entre los dos archivos en esa zona es el título y la nota de adopción), con el caso medido de CA-08 de REQ-016, la regla por propiedad y la línea de la Definition of Ready |
| **CA-08** | **FALLA** | El **procedimiento contratado no se implementó**: siguen siendo **3** series por árbol (no 6) y **en bloque** (no intercaladas). Y la varianza que el procedimiento venía a quitar **salió en rojo dos veces**: 1,252× y 1,443× sobre un techo de 1,250× → **QA-017-06** |
| **CA-09** | **PASA** | El Historial ya lleva medición, procedencia y **dispersión** (6 corridas por árbol y rango) y **no acredita magnitud**. La **dirección** contratada la verifiqué yo, pareada dentro de cada corrida: **6/6**. Queda **QA-017-10**, que no lo hace fallar |
| **CA-10** | **FALLA** | **No tiene ni un caso en el banco**: nace sin validar y sigue sin validar → **QA-017-08**. Y tal como está escrito su sujeto es una moneda al aire: en 3 de 4 corridas **cero** entradas cayeron fuera del dominio, así que emitiría SKIP y **nunca PASS** (parte de **QA-017-01**) |

**Global: `con-hallazgos`.** Tres criterios de diez no pasan.

### Lo que la vuelta 0 dejó abierto, comprobado uno a uno

| Hallazgo | Estado | Cómo lo comprobé |
|---|---|---|
| QA-017-01 (`contrato`) | **SIGUE ABIERTO** | El write-back existe, pero el criterio reescrito **vuelve a ser falso**. Medido abajo |
| QA-017-02 (`instrumento`) | **SIGUE ABIERTO** | El corpus sigue sin la clase, y ahora faltan además las tres decisiones que CA-01 le exige |
| QA-017-03 (`instrumento`) | **CERRADO** | Reproducido de extremo a extremo |
| QA-017-04 (`instrumento`) | **CERRADO** | Reproducido de extremo a extremo, matando al hijo desde fuera |
| QA-017-05 (`instrumento`) | **CERRADO para REQ-017** | La magnitud está retirada del REQ, del criterio y del tablero; la dirección se sostiene 6/6 en mis corridas pareadas. El residual (arreglar la sonda) es de **SEC-030** |
| QA-017-06 (`instrumento`) | **SIGUE ABIERTO, y peor** | Dos rojos reproducidos |
| QA-017-07 (`instrumento`) | **SIGUE ABIERTO** | Escalado al `auditor-seguridad`, fuera de alcance por decisión trazada. No lo re-medí |

#### QA-017-03, cerrado: el sello que siempre permite ya no compra la comparación

Reproducción exacta del contraejemplo de la vuelta 0: `hooks/` copiado con
`guard-completado.sh` sustituido por un sello que imprime `allow` y sale 0.

```
corrida interna de la sección 32 con el sello:  rc=1 · 40 casos · 14 FAIL · 3,73 s
                        (contra ~9,6 s y rc 0 de una corrida de verdad)
```

Antes, esas dos mitades daban **PASS**. Ahora, con la palanca encendida:

```
SKIP  REQ-017 CA-05 (i)   la corrida 1 de este árbol no midió: terminó EN ROJO
                          (rc=1; 14 FAIL de 40 casos): … así que mide MENOS
SKIP  REQ-017 CA-05 (ii)  (mismo motivo)
```

El motivo **viaja hasta el veredicto** con el rc y el recuento dentro, que es lo que hace la
diferencia entre un fail-closed y un fail-closed **diagnosticable**.

#### QA-017-04, cerrado: la muerte por señal a los 0,6 s ya no es «DEMOSTRADO»

Con la palanca encendida y un vigía externo que espera al proceso `timeout` real y mata a **su
hijo** a los 0,6 s del plazo:

```
QA: timeout(pid 2440316) hijo=2440317 — kill -9 al hijo a los 0,6 s del plazo
SKIP  REQ-017 CA-05 (i)  la corrida heredada murió por la señal 9 a los 0.70s de un plazo
                         de 37s: sólo el vencimiento (rc=124) prueba que el plazo se agotó…
```

Antes: `PASS … ≤ 0,250× — DEMOSTRADO, no estimado`. **Nombra la señal**, que es lo que pide CA-06.

#### Y no hay sobre-corrección: los dos positivos siguen decidiendo lo que decidían

| Escenario | Antes de la vuelta 1 | Ahora | Medido |
|---|---|---|---|
| Positivo real (la heredada agota el plazo) | PASS | **PASS** | 3 corridas: plazos 35/37/39 s, reloj 35,16 / … / 39,17 s ≥ plazo. `load` 0,64–1,4 |
| La heredada **termina limpia** dentro del plazo | FAIL | **FAIL** | `ARNES_HOOKS_DIR` := v1.32.1: terminó en **77,12 s** dentro de **289 s** (rc 0) → FAIL. 314,6 s de corrida, `load` 1,40 |
| Numerador en verde (3 corridas) | PASS | **PASS** | rc 0, 0 FAIL, mismo inventario (40 casos) |

El arreglo **no** convirtió el criterio en algo que ya no puede fallar: el `rc = 0` del numerador
no se tragó el FAIL del fail-before, porque el numerador contra v1.32.1 sale en verde.

### Los hallazgos de esta vuelta

#### QA-017-01 · `contrato` · **el dominio de CA-01 está dibujado por la propiedad equivocada, y la clase divergente cae dentro**

El write-back acotó CA-01 al **dominio en que la heredada es determinista**, con este argumento:
«la igualdad byte a byte no la cumple ni v1.32.1 consigo misma». **Medido, ese argumento no se
sostiene sobre el estado que el propio criterio nombra.**

Corpus de **106** entradas con secuencia multibyte inválida (`c3`, `c35c`, `e2`, `f0`, `c3a9`,
`ff`, `c35c0d5d` × colas `0d`, `0d5d0d`, `5d0d`, `0d0d`, vacía × prefijos ``, `Estado: `, `QA: `,
más la línea exacta de la vuelta 0). Para cada entrada, **k = 6** evaluaciones de la heredada en el
**mismo proceso**, comparando el estado completo que CA-01 nombra —`ARNES_LINEA`, `ARNES_CITA`,
`ARNES_CR`, `ARNES_CR_LINEA`—:

| Corrida | Dentro del dominio | Fuera | **Dentro Y divergente de este árbol** |
|---|---|---|---|
| 1 | 104 | 2 | **6** |
| 2 | 106 | 0 | **7** |
| 3 | 106 | 0 | **7** |
| 4 | 106 | 0 | **7** |

CA-01 exige **0**. Ejemplo (entrada `c3 5c 0d 5d 0d`, dentro del dominio en 4/4 corridas):

```
heredada  cita=0|cr=0|L=c35c0d5d0d|CRL=<vacío>
este      cita=0|cr=1|L=c35c0d5d0d|CRL=c35c5c725d5c72
```

**La causa, y es lo que hay que retener:** la clase en que los dos árboles divergen no es «la
heredada no repite», es «**la heredada no es invariante al locale**» — 7 de 106 entradas dan una
decisión distinta bajo `LC_ALL=C` que bajo UTF-8. El no determinismo es un fenómeno **distinto y
más raro** (0–2 de 106) y **no coincide** con el conjunto divergente. Dibujar el dominio por el
determinismo mete la clase divergente **dentro** de CA-01, donde el criterio exige una igualdad que
no hay, y deja a **CA-10 sin sujeto**: en 3 de 4 corridas, **cero** entradas cayeron fuera, así que
el criterio que existe para cubrir «lo de fuera» habría dicho SKIP y **nunca PASS**.

Lo que la vuelta 0 midió sí es cierto —`${l%$CR}` devuelve un valor intermedio distinto en cada
evaluación—, pero ese valor **no siempre llega al estado publicado**: la rama que se toma suele ser
la misma, y entonces la heredada **repite** aunque por dentro sea basura. El criterio se escribió
sobre lo que se midió (el intermedio) y contrata sobre lo que se publica (el estado). No es un
matiz: es la diferencia entre un dominio que excluye la clase divergente y uno que la contiene.

*Reproducción:* corpus y sonda en el registro de esta comisión; 4 corridas, `load` 0,6–2,1.
*Esperado:* 0 entradas dentro del dominio con estado distinto. *Observado:* 6–7 de 106, 4/4.
*Cierra con:* write-back del `analista-requerimientos` que trace la partición por la propiedad que
**sí** separa —invariancia de locale de la heredada, o directamente «la heredada es correcta»—, o
que declare la dirección también dentro. **No lo reescribo yo** (`AGENTS.md` §9).

#### QA-017-02 · `instrumento` · **el corpus sigue sin la clase, y las tres decisiones nuevas de CA-01 no existen**

El generador de `37-…-1-escala.sh` sigue sorteando entre las **8 fichas ASCII** de la vuelta 0
(`CR`, `<!--`, `-->`, `Estado: `, `x`, ` `, `<!`, `--`) y las fronteras nombradas siguen siendo
todas ASCII: **ni un byte ≥ 0x80** en el corpus. Además CA-01 exige ahora tres cosas que **no
están escritas en ninguna parte del banco**: (1) clasificar cada entrada dentro/fuera **en la
corrida**, con k ≥ 3; (2) **FALLAR** si el corpus perdió la clase; (3) **SKIP citando bash, locale
y número de entradas clasificadas** si es la máquina la que no tiene el defecto. `grep` de
`dominio|multibyte|LC_ALL` sobre las dos secciones 37: **cero coincidencias**.
*Dueño:* `desarrollador`. *Forzador:* el mismo diferencial de CA-01.

#### QA-017-06 · `instrumento` · **CA-08 (ii) ya no roza el techo: lo cruza, y en la puerta requerida de `main`**

El procedimiento que el write-back contrató —**≥ 6 series por árbol, intercaladas a, b, a, b**—
**no se implementó**: `mide47` sigue tomando **3** series por árbol y el bucle sigue midiendo
**este** entero y luego **heredado** entero. `grep` de `intercal|alterna` en la sección: cero.

**26 medidas mías hoy, 2 en rojo:**

| Modo de corrida | Medidas | Razones de `REQ-100` (6 líneas) | Rojos |
|---|---|---|---|
| Banco completo, `JOBS=6` (**el modo de la puerta requerida**) | 4 | 1,061 · **1,169** · **1,245** · **1,443** | **1 de 4** |
| Sección 37/2 aislada | 9 | 0,821 – 1,010 | 0 |
| Sección 37/2 aislada, caso de 200 líneas | 9 | 0,897 – **1,252** | **1 de 9** |

Rango global observado: **0,821 – 1,443** contra un techo de **1,250**. Y el patrón importa: en el
modo en que corre la puerta de `main` las razones son **sistemáticamente más altas** (1,06–1,44)
que en aislamiento (0,82–1,01), porque el banco despacha con `JOBS=6` y los dos árboles se miden en
bloque, uno detrás de otro, con vecinos distintos. La vuelta 0 lo dejó a un 2,6 % del techo; hoy lo
cruza. *Esperado:* ninguna corrida en rojo sin regresión. *Observado:* **1 de 4 corridas completas
del banco sale roja**, con `load` 0,91 al arrancar. *Cierra con:* implementar las series
intercaladas y ≥ 6 por árbol. **El techo no se toca**.

#### QA-017-08 · `instrumento` · **CA-10 no tiene ni un caso**

`grep -rn 'CA-10' tests/` sólo encuentra los CA-10 de **otros** REQ (REQ-009, REQ-014, el canario de
sección). No hay diferencial fuera del dominio, no hay evaluación bajo `LC_ALL=C`, no hay registro
del incumplimiento de la heredada. El criterio **nace sin validar por construcción y sigue así**.

Lo que sí pude comprobar **a mano** —y conviene que quede, porque es el fondo del criterio y es
cierto—: sobre las 106 entradas, **este árbol** es determinista en las 6 evaluaciones (**0**
violaciones), su decisión es **la misma** bajo `LC_ALL=C` que bajo UTF-8 (**0** violaciones), y la
heredada **incumple** la invariancia de locale en **7 de 106** y el determinismo en 0–2. El
*fail-before* de CA-10 existe; lo que no existe es el caso que lo mida.
*Dueño:* `desarrollador`, **después** de que el write-back de QA-017-01 fije la partición: hoy el
caso no se puede escribir bien porque el conjunto que debe recorrer está mal definido.

#### QA-017-09 · `instrumento` · **el denominador de CA-05 conserva el agujero que se cerró en el numerador**

El arreglo preguntó «¿esta corrida midió?» **sólo al numerador**. Sobre el denominador,
`veredicto47` da **PASS** en cuanto `rc = 124` y el reloj corrobora, **sin mirar si la heredada
produjo un solo caso** — el `valida47` sólo se consulta en la rama que **no** venció:

```
A) heredada que NO produce ni un caso pero agota el plazo (se cuelga)
   PASS  ataque-A  … ≤ 0,250× — DEMOSTRADO, no estimado
C) heredada cuyo archivo de salida NI SIQUIERA EXISTE, y agota el plazo
   PASS  ataque-C  … ≤ 0,250× — DEMOSTRADO, no estimado
```

No es hipotético y hay precedente **en esta misma ventana**: el árbol heredado extraído con
`git show` **sin bit de ejecución** hacía que el canario del corredor hijo no arrancara y la
corrida heredada saliera «sin casos» (registro del `desarrollador`, más arriba). Entonces
**terminaba** y el código lo mandaba a SKIP; si en vez de terminar se **bloquea** —un hook
esperando en `stdin`, un cerrojo, un `read` sin `</dev/null`— el plazo vence, `timeout` devuelve
124, el reloj corrobora, y una corrida que no midió nada se publica como **demostración**. Es
exactamente la familia de QA-017-03, un lado más allá.
*Esperado:* SKIP con motivo. *Observado:* PASS «DEMOSTRADO». *Cierra con:* exigir también en la
rama del vencimiento que la heredada haya producido al menos un caso —o que su salida exista y no
esté vacía—, que es un `[ -s ]` y un `grep -c`. **No hace fallar a CA-05**, que no contrata que la
heredada mida (por definición no termina); por eso es deuda con dueño y no bloqueo.

#### QA-017-10 · `instrumento` · **el caso de banco de CA-09 no comprueba la dirección que CA-09 contrata**

CA-09 ya no acredita magnitud y contrata **la dirección**: en cada corrida, este árbol ≥ v1.32.1 en
**esa misma** corrida. El caso del banco hace lo contrario de las dos cosas: **publica una
magnitud** (`case "$este37" in *MB*) → PASS`) y **no compara** con la heredada, que mide al lado y
sólo imprime. Una corrida en la que este árbol saliera **por debajo** de v1.32.1 seguiría dando
PASS. Y sigue midiendo **una sola** vez por árbol, contra las **6** que el criterio pide.

**No hace fallar a CA-09**, porque lo que CA-09 exige es que la medición y su dispersión estén
**en el Historial**, y están; pero la única propiedad que el criterio contrata **no tiene puerta**.
*Dueño:* `desarrollador`. *Forzador:* el primer arreglo de la sonda que haga **SEC-030**.

### CA-02 y el cambio de texto de tres mensajes — la decisión, dicha en voz alta

El `desarrollador` avisó de que su arreglo cambia el **texto** de tres mensajes de 37/2 y de que
`inventario.sh` mete el texto en la identidad del caso. Es cierto: `inventario.sh` toma
`^  (PASS|FAIL|SKIP)  (.*)$` entero y sólo normaliza `[0-9]+ms` → `Nms`. **El texto es identidad.**

**Y aun así CA-02 no se rompe, y no porque el criterio hable de nombres — habla de la línea entera.**
Se sostiene porque el **eje** de la comparación de CA-02 no es «banco de ayer contra banco de hoy»,
sino **hooks de antes contra hooks de después sobre el mismo banco**. Lo dice el propio REQ, sin
margen de lectura, en CA-05: «la igualdad de inventario **contra el árbol heredado** la contrata
**CA-02**, sobre el banco entero». Sobre ese eje los dos lados corren **la misma** sección 37/2 y
producen **los mismos** textos, y el resultado es el de arriba: **828 casos idénticos byte a byte**.

Un diff contra una corrida **anterior** enseñaría, además de esas tres líneas, los **dos casos
nuevos** de 37/2 (8 → 10) y las cuatro cifras medidas que cambian en cada vuelta. Eso es
**evolución del banco**, no un cambio de veredicto — y leerlo como incumplimiento de CA-02 sería
adoptar exactamente la línea base congelada que este REQ argumenta en tres sitios que **envejece
hacia el lado que abre** (`ADR-002`, `SEC-025`, la lección de CA-05). **No abro hallazgo.**

Los 19 casos `REQ-017 CA-0…` quedan **fuera** de la comparación y hay que decir por qué: sus
nombres llevan **el número medido**, así que no pueden ser idénticos entre dos corridas ni siquiera
del mismo árbol. Es la misma exclusión que hizo la vuelta 0 (828 casos, allí y aquí).

### CA-10, ¿cambio menor o de fondo? Mi lectura: **de fondo, y pide ADR**

El analista se apoyó en el precedente de la pared de los 60 s. **Ese precedente no transporta**, y
la diferencia la nombra la propia comisión: aquella se declaró como **medida** y se dio a **otro
dueño** (`SEC-030`), de modo que no añadió ninguna obligación a este REQ ni ningún criterio suyo
podía fallar por ella. **CA-10 se contrata como criterio**: añade una puerta que tiene que pasar
para cerrar, añade trabajo en el banco y —esto es lo decisivo— **cambia el significado de CA-01**,
que es el criterio donde este REQ define qué quiere decir «equivalencia». `AGENTS.md` §9 llama de
fondo justamente a lo que «cambia el alcance, la decisión base o el **significado** del REQ».

Y hay un argumento medido encima del formal: la decisión nueva —*dónde* se traza la frontera del
dominio— **es la que salió mal**, y salió mal en silencio, porque se tomó dentro de un write-back
de cambio menor donde nadie tenía que justificar la elección de la propiedad. Una decisión de ese
tamaño, tomada mal, es exactamente lo que un ADR existe para hacer visible y reversible.

*Recomendación:* un ADR que registre (a) que la equivalencia de REQ-017 pasa a afirmarse sobre un
dominio en vez de sobre toda entrada, (b) **qué propiedad** define ese dominio y por qué ésa
separa la clase divergente, y (c) qué se contrata fuera. Es del `analista-requerimientos`; yo no
lo escribo.

### Los SKIP, uno a uno

1. `ruta estilo Windows con backslashes -> deny (sin cygpath)` — **honesto y de plataforma**.
   `type -P cygpath` no lo encuentra y `uname -s` es `Linux`. No esconde ningún caso ejecutable
   aquí, y el corredor lo cuenta en el cuadre a propósito.
2. y 3. `REQ-017 CA-05 (i)` y `(ii)` — **por diseño**, con `ARNES_COSTE_RUTA_CRITICA` apagada,
   citando el número acreditado (`0,125× — 9,60 s frente a 76,19 s`) y su fecha. Comprobado que la
   rama **sí mide** cuando se enciende: 4 corridas encendidas hoy, con PASS, SKIP y FAIL según lo
   que pasara de verdad.

No hay ningún SKIP nuevo, y ninguno de los tres tapa un caso que aquí pudiera correr.

### Lo que miré y NO encontró nada

- **El positivo real no se rompió por el arreglo.** Tres corridas encendidas: el reloj corrobora el
  plazo con margen (39,17 s ≥ 39 s; 35,16 s ≥ 35 s), y el sentido del redondeo `⌈4u⌉` sigue siendo
  el seguro. `ut_her47` se toma **antes** de lanzar `timeout`, así que incluye el arranque y no
  puede quedar por debajo del plazo por un artefacto de medida.
- **El `rc = 0` del numerador es equivalente a «0 FAIL».** `run.sh` termina con
  `[ "$FAIL" -eq 0 ] && [ "$PROBLEMAS" -eq 0 ]`, así que el criterio («rc 0 **y** 0 FAIL») y el
  código (sólo el rc) deciden igual. El recuento de FAIL sí viaja en el mensaje, que es donde hace
  falta.
- **`valida47` con las cinco entradas frontera** —rc ilegible, salida inexistente, 0 casos, 40 PASS
  con rc 1, 14 FAIL con rc 1— rechaza las cinco. La guarda `num47` está escrita **una a una** y no
  concatenada, que es el error que el propio comentario documenta.
- **Una heredada que termina EN ROJO dentro del plazo da SKIP, no FAIL**, y es la dirección
  correcta: una corrida roja termina **antes**, así que hace parecer a la heredada **más rápida** —
  un FAIL ahí sería un rojo falso.
- **La palanca `ARNES_COSTE_RUTA_CRITICA`, 14 variantes re-probadas de extremo a extremo** (vacía,
  `0`, `no`, `NO`, `off`, `2`, `SI`, ` 1 `, `TRUE`, `yes`, `verdadero`, `1;rm -rf /`, `<TAB>1<TAB>`,
  `sí`): **apagan** las cinco del vocabulario de apagado; **encienden** `SI`, ` 1 `, `TRUE`, `yes`,
  `<TAB>1<TAB>` y `sí` (espacios y tabuladores recortados, mayúsculas y tilde normalizadas); y las
  tres que no reconoce —`2`, `verdadero`, `1;rm -rf /`— **no encienden y lo dicen** en el motivo del
  SKIP (`no se reconoce`). Fail-closed **y** diagnosticable en las tres. Sin regresión respecto a
  las 18 de la vuelta 0, y el valor con metacaracteres no llega a ninguna shell.
- **Ninguna sonda mía sobrevivió a la comisión** (CA-06 aplicado a mí mismo): sin `run.sh`, sin
  `timeout`, sin corridas de sección huérfanas al cerrar.
- **El campo `Archivos:` cubre lo tocado**: los tres archivos con cambios sin comitear
  (`tests/escenarios/hooks/README.md`, `run.sh`, `37-…-2.sh`) están declarados. Sin deriva de mapa.
- **`docs/qa/1.33.0.md` no lo escribe nadie más ahora mismo**: la comisión de analista trabaja en
  `requirements/REQ-020.md`, que este REQ no declara.

### Coste de esta comisión

| | |
|---|---|
| Reloj | **1 h 35 min** (16:33 → 18:08) |
| Tokens declarados por el agente | ~**175 000** |
| Tokens con la corrección de subestimación (25–38 %) | **219 000 – 242 000** |
| Reloj de máquina gastado en medir | ~**55 min** de corridas de banco y de sección (4 bancos completos, 6 secciones aisladas, 4 encendidas, 1 fail-before de 315 s, 12 corridas de la sonda de pared) |

### Forma del criterio deficiente (anotado aquí, y sólo aquí)

**QA-017-01 no encaja en ninguna de las tres formas nombradas** por `requirements/README.md`
§ «Cómo se escribe un criterio que no se desmiente» —no enumera un conjunto sin marca, no fija un
número sin declararlo operativo o de contrato, no exige igualdad donde tocaría un techo—. Su forma
es otra: **acota el universo por una propiedad que no separa la clase que el criterio quiere
excluir**, de modo que el contraejemplo queda **dentro** del dominio declarado y el criterio
hermano se queda **sin sujeto**. Es prima de la forma (d) —fijar la magnitud equivocada—, pero
sobre el **conjunto** en vez de sobre la magnitud: *acotar por la propiedad equivocada*. Si al
`analista-requerimientos` le parece que aguanta como quinta forma, tiene su caso medido aquí;
decidirlo no es mío. Va escrito en este registro y **no** en el paréntesis de la clase del REQ,
que es lo que lee `guard-completado`.
