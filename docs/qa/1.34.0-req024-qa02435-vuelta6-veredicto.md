# `QA-024-35` · verificación acotada de la corrección — **VUELTA 6** del bucle `dev↔QA`

**Veredicto: la corrección PASA.** `QA-024-35` **CIERRA** y `QA-024-19` **CIERRA con él**.
Abiertos **dos** hallazgos nuevos de clase `contrato` (`QA-024-38`, `QA-024-39`) que **no**
imputan defecto a lo entregado en esta vuelta: uno es una **tercera sede** de la misma promesa
falsa que nadie había señalado y que era falsa **antes** de este commit, y el otro es el
**write-back ausente** de la segunda sede que el desarrollador corrigió.

| qué | valor |
|---|---|
| worktree | `/home/juan/dev/ArnesJuan-1.34-reparaciones` |
| rama · commit | `feat/1.34-cierre-alcance` · **`0440cfd`** (árbol limpio) |
| «antes» contra el que se mide | **`929043d`**, materializado desde el repositorio |
| vuelta | **6** del bucle `dev↔QA` de `REQ-024`, autorizada **expresamente** por el propietario. **No se renombra.** El bucle `analista↔QA` sigue en **3** y esta vuelta no lo gasta |
| plataforma | Linux 6.18 (WSL2), `bash` 5, `jq` |
| instrumentos y salidas | `docs/qa/evidencia-req-024-qa02435-0440cfd/` |

**Instrumento propio, no el del desarrollador.** `qa-fidelidad.sh` no comprueba que el texto
contenga una cadena: **empareja, en la misma celda, lo que el aviso PROMETE con lo que el cierre
HACE** sobre un REQ en ese mismo estado. Un instrumento que sólo casara cadenas daría verde ante
una promesa que siguiera siendo falsa.

---

## 1. ¿El mensaje es ahora fiel en las combinaciones relevantes? — **SÍ, 48 de 48**

`2` estados de `campos.ausencia_exige` **+ un tercero que el desarrollador no midió** (el
manifiesto **sin la clave `campos`**, que es como está todo proyecto que no la declara) × `4`
combinaciones de rigor —incluida `Sensible a seguridad: si` con `Rigor: ligero`, para ejercer el
**suelo** y no sólo el valor declarado— × las **dos claves** × las **dos vías**.

| vía | clave | celdas | resultado |
|---|---|---|---|
| campo **AUSENTE** para el lector (`qa:` / `seguridad:` en minúscula) | `QA:` | 12 | **12 FIEL** |
| ídem | `Seguridad:` | 12 | **12 FIEL** |
| valor **FUERA DE VOCABULARIO** | `QA:` | 12 | **12 FIEL** |
| ídem | `Seguridad:` | 12 | **12 FIEL** |

**Los dos ejes son distintos, y queda medido — no aceptado.**

- `QA:` ausente → el eje es **la llave**: apagada (y sin la clave `campos`), el REQ **CIERRA** en
  los tres rigores; encendida, **DENY** en los tres. El aviso resuelve el estado **real** de la
  llave («Aqui esta APAGADA / ENCENDIDA») y **acierta en las 12 celdas**.
- `Seguridad:` ausente → el eje es **el rigor**: DENY sólo en `critico`, **idéntico en los tres
  estados de la llave** (la llave **no** mueve su veredicto en ningún nivel: 24 celdas).
- Sub-afirmación del mensaje nuevo verificada aparte: «*encendida, el cierre se deniega **nombrando
  `QA:`** en los tres niveles de rigor*» → los tres motivos de denegación nombran literalmente
  `el campo 'QA:'` (`bordes-llave.txt`).

**La segunda sede (`guard-completado.sh:270`, valor fuera de vocabulario) también es fiel.** Su
condición es el **rigor**, el texto nuevo la nombra («*Si el rigor efectivo NO es 'ligero'*») y el
emparejamiento sale exacto en las 12 celdas, incluido el caso que la delata: `Rigor: ligero` →
**ALLOW**, el REQ **cierra** con un valor que no es ninguno de los tres.

**Los dos mensajes de `Seguridad:` NO se tocaron — comprobado byte a byte, dos veces.** (a) las dos
líneas `arnes_aviso` de `Seguridad:` son **idénticas** entre `929043d` y `0440cfd`
(`md5 43712023…` en ambas); (b) los **24 textos** que esos avisos emiten en las 12 combinaciones
salen **idénticos** entre las dos vueltas del instrumento.

### Lo que intenté para romperlo, y no se rompió

| ataque | resultado |
|---|---|
| manifiesto **sin la clave `campos`** | aviso dice `APAGADA`, cierre **ALLOW**: de acuerdo |
| `campos.ausencia_exige` con **tipo inválido** (`"true"` como cadena) | aviso dice `APAGADA`, cierre **ALLOW**: de acuerdo (fail-open **preexistente** del lector, no de este arreglo; el aviso y la puerta leen la **misma** variable, así que no pueden discrepar por construcción) |
| llave **encendida** + `Rigor: ligero` | aviso dice `ENCENDIDA`, cierre **DENY**: de acuerdo |
| la **edición** que dispara el aviso, ¿se permite? | **ALLOW** en las 12 combinaciones, también con `Seguridad: aprobado` ya en disco: la frase «*ninguna guarda juzga esta edicion*» es **cierta** |
| ¿promete **de menos**? (el reverso que pide el encargo) | no: no hay ninguna celda en que el aviso anuncie cierre y la puerta deniegue **por ese campo** |
| el calificativo «*…cerrarse **sin veredicto de QA***» ¿aguanta otras causas? | sí: con la llave apagada el REQ **no** cierra si el rigor efectivo es `critico` y falta `Seguridad:`, si hay un hallazgo `usuario/dinero`, o si la cola tiene una pendiente — y el aviso **no** lo promete, porque acota su promesa a la ausencia de QA |

---

## 2. ¿Las decisiones de cierre no cambiaron? — **NINGUNA, ni un `rc`**

**72 celdas** de decisión medidas con el **mismo instrumento** contra los **dos** árboles de hooks
(6 sujetos × 3 estados del manifiesto × 4 combinaciones de rigor). `diff` de la columna de
decisión y del código de salida: **vacío**. Todas salen `rc=0` antes y después — lo que importa
porque el arreglo mete una **sustitución de comando dentro del texto** de un aviso.

Lo único que cambia son **24 de 48** textos: los 12 de `QA:` ausente y los 12 de `QA:` fuera de
vocabulario. Los 24 de `Seguridad:`, idénticos. Y el **control de silencio** (clave limpia con
valor válido) sigue sin emitir aviso en las 12 combinaciones.

---

## 3. ¿Se activó alguna protección? — **NO. Cero ramas nuevas**

- **Diff del commit sobre `hooks/`:** 1 solo archivo (`guard-completado.sh`), **2 líneas añadidas
  y 2 borradas que no son comentario**, y las cuatro son la **misma** llamada `arnes_aviso`. Todo
  lo demás del diff es comentario.
- **Diff estructural** (ramas `if`/`elif`/`case`/`return`/`arnes_deny`/`arnes_allow`, sin
  comentarios): **212 líneas antes, 212 después, `diff` vacío**.
- **Manifiesto intacto:** `campos.ausencia_exige` sigue `false`; `git diff 929043d..0440cfd --
  .arnes/config.json templates/arnes-config.json.tpl` sale **vacío**.
- **Que la ausencia se perdone con la llave apagada sigue ocurriendo**, medido: las 12 celdas de
  `QA:` ausente con llave apagada (y sin `campos`) siguen dando **ALLOW**, exactamente como antes.
  El arreglo no la dejó de perdonar; hace que el aviso **lo diga**.
- `hooks/lib.sh` y `tools/` no se tocaron. Todo el delta de código desde el árbol `a05994f` que
  acredité en la vuelta 5 es **este** commit.

---

## 4. Banco y puertas — **con una limitación material, y no se acredita rendimiento**

| puerta | resultado |
|---|---|
| §7 · `bash -n` de `hooks/*.sh` y `tools/*.sh` (10 archivos) | **OK** |
| §7 · `jq -e . hooks/hooks.json` | **OK** |
| §7 · `jq -e .` de `plugin.json` y `marketplace.json` | **OK** |
| sección 43 dentro del banco entero | **52 / 52 PASS** |
| cuadre total | **1157 + 1 + 9 = 1167** ✓ (`CASOS_ESPERADOS` correcto) |
| **banco entero** | **1157 PASS · 1 FAIL · 9 SKIP**, `rc=1` |
| fail-before de la 43 contra `929043d` | **40 PASS · 12 FAIL**, y los 12 rojos son **exactamente** los bloques A y B de `QA:` — replicado |

**El banco NO salió como el desarrollador declaró (`1160 PASS · 0 FAIL · 7 SKIP`), y la
discrepancia se deja escrita en vez de repetirse hasta el verde.** Los tres casos de diferencia
están **todos** en la familia del **reloj**:

- **FAIL** `REQ-023 CA-09 (ii) el reloj de la ruta crítica no pasa de 1,25× la línea base v1.33.0`
  → **1.337× > 1.250×** (296046µs sobre 221289µs).
- **+2 SKIP** `REQ-017 CA-08 (ii)` (la sonda no converge / el techo cae dentro del recorrido).

**Atribuibilidad, medida y no supuesta:** una corrida **pareada** de esa sección, en la misma
sesión y máquina, contra el árbol candidato **y** contra los hooks de `929043d`, dio **6 PASS ·
0 FAIL en los dos**. Eso establece que el resultado **no es atribuible a `0440cfd`** — el mismo
instrumento da lo mismo sobre los dos árboles — y **NO retira el FAIL**: repetir hasta el verde no
desmiente una medición roja.

**Por eso este artefacto NO acredita rendimiento.** Las condiciones estaban declaradas **no
representativas** (otra cuenta del propietario trabajando en la misma máquina), así que la familia
del reloj queda **NO CONCLUYENTE** y su FAIL, **registrado como evidencia**. No abro hallazgo por
ello: la investigación del instrumento de rendimiento está **prohibida por el propietario** y
queda **fuera del alcance** de esta comisión. **Queda como dato para el propietario**: la
afirmación «banco entero en verde» de `0440cfd` **no es reproducible** en esta máquina y **no la
acredito**. Lo que sí acredito es lo atribuible al cambio: 52/52 de la sección 43, cuadre exacto y
cero movimientos de decisión.

---

## 5. `QA-024-35` **CIERRA**, y `QA-024-19` **CIERRA con él**

**`QA-024-35` cierra.** Su objeto era la glosa de `CA-13 (ii)(3)` —«*el aviso lleva la condición
cuando la hay y la omite cuando no la hay*»—, que estaba medida falsa porque para `QA:` la
condición **existía** (la llave) y el aviso **la omitía**. Hoy la lleva, y la resuelve.

**`(3)` pasa a ser verdadero TAL COMO ESTÁ ESCRITO**, que era el argumento de la salida (b).
Elemento por elemento, contra la medición:

| elemento de `(3)` | medido |
|---|---|
| «nombra la consecuencia **sin prometer más cierre del que el cierre hace para ESA clave**» | ✔ 48/48 celdas: promesa = hecho |
| «**ninguna guarda juzga esa firma**» | ✔ la edición que dispara el aviso sale **ALLOW** 12/12, también con `Seguridad: aprobado` en disco |
| «el REQ no podrá cerrarse por ausencia de ese veredicto **allí donde el cierre exija ese campo**» | ✔ `QA:` → donde exige = llave encendida (DENY 3/3 rigores); `Seguridad:` → donde exige = rigor `critico` |
| «**conjunto que no es el mismo para las dos claves**» | ✔ llave vs rigor, y la llave **no** mueve `Seguridad:` en ningún nivel (24 celdas) |
| «el aviso **lleva la condición cuando la hay y la omite cuando no la hay**» | ✔ las dos claves **tienen** condición y las dos la llevan |

*Observación de forma, no hallazgo:* la segunda mitad de esa glosa —«**y la omite cuando no la
hay**»— es **vacuamente verdadera**: en el dominio que `(ii)` contrata no existe ningún aviso **sin**
condición, así que esa mitad no tiene instancia. No es ninguna de las tres formas que
`requirements/README.md` prohíbe (enumeración · número · igualdad) y **no bloquea**; se deja
anotado por si el analista quiere que el criterio no prometa sobre un conjunto vacío.

**`QA-024-19` cierra.** Su write-back **es** `CA-13`, y lo único que lo mantenía abierto —lo dejé
escrito en la vuelta 3 del bucle `analista↔QA`— era que `(ii)(3)` **describía mal el mensaje**. Sus
otras cinco condiciones de cierre siguen acreditadas y **sobre el mismo árbol**: `git diff
a05994f..0440cfd -- hooks tools .github` es **exactamente** este commit, así que nada de lo que
acredité se ha movido.

---

## 6. Lo que abro — dos hallazgos nuevos, ninguno imputable a esta vuelta

### `QA-024-38` — `contrato` · la **TERCERA sede** de la misma promesa falsa, en la superficie **heredada**

**Dónde:** `AGENTS.md:363` y `templates/AGENTS.md.tpl:321`, bloque «**Un hook que avisa sin
decidir**» — la sede que `REQ-024 CA-13 (ii)` **cita por nombre** como la definición del aviso de
valor fuera de vocabulario.

**Qué dice, sin condición:** «*…el arnés lo dice en ese momento con un mensaje a la persona y **no
deniega** la edición: **ese REQ no podrá cerrarse** y, sin el aviso, nadie lo sabría hasta el
cierre.*»

**Reproducción** (`probe-tercera-sede.sh`, hook real, REQ con `Hallazgos abiertos: (ninguno)`):

| llave | rigor | `QA:` fuera de vocab. | `Seguridad:` fuera de vocab. | ¿la frase es cierta? |
|---|---|---|---|---|
| `false` | `ligero` | **ALLOW** | **ALLOW** | **falsa para las dos claves** |
| `false` | `estandar` | DENY | **ALLOW** | **falsa para `Seguridad:`** |
| `false` | `critico` | DENY | DENY | cierta |
| `true` | `ligero` | **ALLOW** | **ALLOW** | **falsa para las dos claves** |
| `true` | `estandar` | DENY | **ALLOW** | **falsa para `Seguridad:`** |
| `true` | `critico` | DENY | DENY | cierta |

**Esperado vs observado:** esperado, que un REQ con un veredicto fuera del vocabulario no pueda
cerrarse; observado, que **cierra** en **4 de 6** celdas.

**Severidad alta y por qué es `contrato`, no `instrumento`:** no es una prueba ni un control, es la
**superficie normativa que todo proyecto hereda** por `arnes-upgrade`; el error apunta hacia el lado
que **TRANQUILIZA** —alguien lee «no podrá cerrarse» y el REQ cierra—, que es la familia de
`SEC-079`; y **después de este commit la máquina contradice a su propia documentación**: el hook ya
dice «*Si el rigor efectivo NO es 'ligero'…*» donde `AGENTS.md` §13 sigue diciéndolo sin condición.
**Era falsa antes de `0440cfd`** y nadie la había nombrado — verificado: no aparece en ningún
hallazgo, REQ ni entrada del registro de seguridad.

**Dueño:** `desarrollador` (arreglo de **texto** en dos sedes). Por tocar `AGENTS.md` §13 y su
plantilla entra por la **misma vía de gate humano** que `CA-12 (ii)`.

### `QA-024-39` — `contrato` · la segunda sede se corrigió **sin contrato**

El aviso de **valor fuera de vocabulario** cambió de comportamiento (el texto ahora lleva su
condición) y **ningún punto de `CA-13` lo certifica**: `(ii)` contrata el aviso de **desfase**, y la
única mención del de vocabulario está en el «Dado que» de `(i)`, que contrata que **ENTRE**, no lo
que **diga**. Comprobado abriendo `CA-13` entero y `REQ-003` Bloque B.

**Juzgo el apunte del desarrollador y coincido con su hecho, no con su conclusión.** Tiene razón en
que su corrección «no contradice nada»; pero `AGENTS.md` §9 no pide ausencia de contradicción, pide
**write-back**: un comportamiento aceptado que ningún criterio describe es **deriva**, y un hallazgo
no se cierra por quedar resuelto sólo en el código. Que el banco lo fije (43/B, 6 casos) protege
contra la **regresión**, no sustituye al **contrato**. **Falta contrato, y es del analista.**

**Aviso de alcance, porque la decisión NO es mía:** el dueño natural de ese criterio es **`REQ-003`
Bloque B** (el REQ que creó el aviso de vocabulario), no `REQ-024` — y `REQ-003` está
**`completado`**, así que contratarlo ahí lo **reabre** (§9, REGLA DE ESTADO). Alternativa: alojarlo
en `REQ-024 CA-13` como punto nuevo. **Es una decisión de alcance del propietario**, y con el bucle
`analista↔QA` en 3 sobre un presupuesto de 2 conviene decidirla antes de despachar nada.
**Recomendación (no decisión):** `QA-024-38` y `QA-024-39` comparten causa y **pueden viajar en un
solo write-back**, que es la ruta más barata para ese presupuesto.

---

## 7. Observación medida que NO abro, porque `REQ-003` está **fuera de mi alcance**

`REQ-003 CA-13` (REQ **`completado`**) dice: «*Dado un `Edit` que a la vez escribe un valor fuera
del vocabulario y deja la cabecera en el estado terminal … Entonces el hook responde **DENY** por la
puerta de cierre*» — **sin condición**. Medido con el escenario **exacto** del criterio
(`observacion-req003-ca13.txt`): `ligero` → **ALLOW**; `estandar` → DENY; `critico` → DENY. El
criterio es **falso en `ligero`**.

No abro hallazgo: «otros REQ» está declarado fuera del alcance de esta comisión. **Lo dejo medido y
nombrado para el propietario**, porque callar una falsedad medida por respetar un límite de alcance
sería el error contrario.

---

## 8. Lo que NO hice

- No repetí las 56 celdas del disparador, ni las 41 formas, ni las 66 de `CA-13`: el código no se
  movió en esa superficie y esa evidencia sigue válida (vueltas 3 y 5).
- No toqué `CA-12 (ii)`, `CA-03`, `QA-024-36`/`37` ni su gemela de `lib.sh:2206-2208`, `REQ-026` ni
  la transcripción de `SEC-084`.
- No investigué el instrumento de rendimiento: **prohibido por el propietario**.
- No toqué código de la app, ni `Estado:`, ni `Seguridad:`, ni comiteé nada.
