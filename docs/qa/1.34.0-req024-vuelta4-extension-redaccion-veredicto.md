# `REQ-024` · vuelta 4, pase de QA nº 3 — validación ACOTADA del delta de redacción `f612b84..d82c127`

> **Contador §6.** Ésta es la **vuelta 4** de `REQ-024`, autorizada **expresamente por el propietario
> el 2026-09-11** por encima del tope de tres de `AGENTS.md` §6. El contador **no se reinicia y no se
> renombra**: no es un «tramo», ni un «ajuste», ni una «revisión acotada». Dentro de esa vuelta 4 este
> es el **tercer pase de QA** (el 1.º sobre la fase del desarrollador, el 2.º sobre el write-back del
> analista en `ab3e4cb`, este 3.º sobre la extensión de redacción en `d82c127`).

## Veredicto

**`QA: con-hallazgos`** — y las dos mitades por separado, porque no dicen lo mismo:

- **El delta CUMPLE.** Las **cinco** cosas que el propietario exigió que el texto dijera están dichas
  en las catorce sedes; la **decisión, el techo, la unanimidad, el presupuesto de intentos y los `rc`
  NO cambiaron** (verificado por cuatro vías independientes, ninguna reutilizada); **ningún resultado
  antes bloqueante pasó a verde** (reproducido de extremo a extremo). **`QA-024-25` y `QA-024-26` se
  CIERRAN** con la remediación verificada.
- **El REQ sigue `con-hallazgos`**, y no por este delta: `QA-024-19` y `SEC-084` (**`contrato`**)
  siguen abiertos, `CA-12 (ii)` sigue **sin implementar** y `CA-03` sin acreditar. `Estado:` se queda
  **`bloqueado`** (`D14`) y `Seguridad:` **`pendiente`** — no los toco.

**Tres hallazgos nuevos, los tres `instrumento` y ninguno bloqueante:** `QA-024-29`, `QA-024-30`,
`QA-024-31` (§6). Ninguno afecta a lo que el delta imprime ni a lo que decide.

## Versión base y método

| | |
|---|---|
| **Worktree** | `/home/juan/dev/ArnesJuan-1.34-reparaciones` (los otros tres no se tocaron) |
| **Rama / cabeza** | `feat/1.34-reparaciones-astra` · **`d82c127`**, árbol limpio; **sin comitear** |
| **Delta validado** | `f612b84..d82c127` — 8 archivos, 465 inserciones |
| **Máquina** | WSL2 `linux-gnu-x86_64-bash5.3`, 12 núcleos |
| **Escritura** | sólo `docs/qa/` y, en `requirements/REQ-024.md`, **únicamente** `QA:`, `Hallazgos abiertos:` y una fila del Historial |
| **Scratchpad con los artefactos ejecutables** | `/tmp/claude-1000/-home-juan-dev-ArnesJuan/23a8ee5a-8587-4923-8f15-1fc9bf03aaf2/scratchpad/` (`rejilla.sh`, `dec-f612b84.sh`, `dec-d82c127.sh`, `flags.awk`, `banco-qa-1.txt`, `inj3700-qa.txt`) |

### Qué REUTILICÉ y qué REPRODUJE, con el motivo

| medición | origen | por qué |
|---|---|---|
| `rc 1` en 17/17 y `rc 0` en 8/8 (vuelta 2) | **reutilizada** | el delta no toca ninguna rama de decisión, y eso lo demuestro aquí de nuevo por cuatro vías; sin duda concreta no se re-mide |
| `FAIL` 10/10 con `+3700`, `PASS` del runner sobre `f2b74d9` | **reutilizadas** | ídem |
| Diff estructural, constantes, líneas de condición | **REPRODUCIDAS con mi propio filtro** | el propietario pidió verificar, no aceptar |
| Rejilla del decisor | **REPRODUCIDA y AMPLIADA a 16 entradas** | la de 11 es del desarrollador; añado vacío, cero, negativo, no numérico, una sola repetición y el borde mixto |
| `rc` extremo a extremo con `+3700` | **REPRODUCIDO** (1 corrida) | es *la* mitad que bloquea: no se toma de un informe |
| Banco completo | **REPRODUCIDO** (1 corrida) | para tener número propio del reparto `PASS`/`SKIP` |
| Sección `37/2` aislada ×3 | **REPRODUCIDA** | duda concreta: el `PASS → SKIP` de `REQ-017 CA-03` |
| Autoprueba del corredor | **REPRODUCIDA** | el delta mueve `PISO_AUTONOMO_SECCION`, que es un valor **con efecto** |

## 1. Las cinco exigencias del propietario, una por una

### (1) La atribución absoluta, sustituida por descripción fiel — **CUMPLE**

Barrido **por propiedad** en todo el árbol (`grep -rn "no lo puso ah"`, `"es regresión, no ruido"`):
en **sede viva** no queda ninguna instancia afirmativa. Las que quedan son de tres clases legítimas:

- **refutación explícita** — `40/7:258`, `37/5:273`, `tests/…/README.md:414`, `REQ-024:1557`;
- **contraejemplo doctrinal** — `requirements/README.md:413`, bajo el rótulo **«Mal:»**;
- **registro de evidencia** — `docs/qa/…`, `docs/arnes/…/demostraciones/…`, `CHANGELOG.md`.

Y **la única sede viva afirmativa que sobrevive está NOMBRADA por el propio desarrollador y por el
analista, y está fuera de mi alcance y del suyo**: `requirements/REQ-017.md:118`, el criterio
**hermano**, sigue diciendo «excedido en **todas**, así que no lo puso ahí el vecino». No la corrijo
(otro REQ, y el propietario decide el write-back) y **no la cuento contra este delta**: lo que
`QA-024-25` abrió es el criterio de `REQ-024` y el programa, y ahí la afirmación **dejó de estar
escrita**. Queda como **escalación al propietario**, ya declarada en el Historial del REQ.

Lo que el programa imprime hoy, comprobado **ejecutando** `veredicto07` (no leyendo el archivo):

```
  FAIL  …  mín(r) 1.339× > techo en TODAS las repeticiones: LA MEDICIÓN EXCEDE EL TECHO Y NO SE
  ACREDITA CUMPLIMIENTO. Esto NO afirma que haya regresión del CÓDIGO ni descarta el RUIDO: …
```

Y **bloquea sin atribuir**: el mismo mensaje dice «Bloquea igual —una puerta que no puede acreditar
no deja pasar—», y el contador subió (`FAIL=1`) en mi ejecución directa.

### (2) El «suelo» como dato diagnóstico estimado, sin lógica nueva — **CUMPLE**

- **Rótulo publicado:** `suelo estimado 1.303× (DIAGNÓSTICO de esta corrida: no es umbral y no
  gobierna ninguna rama; derivado del recorrido intra-corrida, subestima si la dispersión que manda es
  la de entre corridas)`. Verificado **en la salida real**, no en el código.
- **No gobierna nada, medido otra vez:** `suelo` (el de detección) aparece en **dos** sitios
  ejecutables de `40/7` — `:290` donde se calcula y `:306` donde se interpola — y en **ninguna**
  condición: `grep -nE '(\[|\[\[|if|elif|case|test).*suelo'` sobre líneas no comentadas devuelve
  **vacío**. Las otras dos apariciones de la palabra (`:238`, `:387`) son el **suelo de 50 ms** del
  reloj, que es otro concepto y ya gobernaba antes del delta.
- **Sin lógica nueva:** el diff estructural (§2) es **vacío** salvo la línea del piso. No se añadió
  ninguna rama para justificar el nombre, que es lo que el propietario prohibió expresamente.

### (3) Techo, unanimidad, intentos y códigos de salida intactos; nada bloqueante pasado a verde — **CUMPLE**

Cuatro vías **independientes**, todas ejecutadas por mí:

1. **Diff estructural propio** (comentarios fuera, cada cadena → `"S"`, blancos normalizados): **vacío
   en los dos archivos salvo la línea `PISO_AUTONOMO_SECCION`**. Ni una condición, umbral, operación,
   contador ni `rc`.
2. **Constantes:** `SER07=30 K07=8 KRAZ07=4 TECHO07=1250 FACTOR07=1280 INTENTOS07=3` y
   `SER47=6 K47=4 KRAZ47=4 TECHO47=1250 FACTOR47=2000` — diff **vacío**.
3. **Líneas de condición y contador** con filtro **más ancho** que el del desarrollador (82 y 89
   líneas): la **única** diferencia en cada archivo es el **contenido de la cadena** del `FAIL`. El
   `FAIL=$((FAIL+1))` de esa misma línea no cambió.
4. **Rejilla EJECUTABLE de 16 entradas** sobre `resuelve07`/`cierre07` extraídos de **ambas**
   versiones: **decisión y cierre idénticos en las 16**. Incluye lo que la de 11 no tenía:

| entrada | `RES07` | `cierre07(1)` | `cierre07(3)` |
|---|---|---|---|
| **`1.250×` borde exacto** | `pass` | emitir | emitir |
| **`1.251×`** | `fail` | emitir | emitir |
| `1.249×` | `pass` | emitir | emitir |
| unánime `1.300×` | `fail` | emitir | emitir |
| mezcla `1.000×`/`1.300×` | `inconcluso` | **reintentar** | **sin-acreditar** |
| borde mixto `1.250×`/`1.251×` | `inconcluso` | reintentar | sin-acreditar |
| una sola repetición `1.251×` / `1.250×` | `fail` / `pass` | emitir | emitir |
| no converge · bajo suelo 50 ms | `inconcluso` | reintentar | sin-acreditar |
| **falta un número · cero · negativo · no numérico** | `inconcluso` | reintentar | sin-acreditar |
| **sin ninguna repetición** | `inconcluso` | reintentar | sin-acreditar |

Y **el `rc` que de verdad bloquea, reproducido**: copia de `hooks/` con
`for ((_qa=0;_qa<3700;_qa++)); do :; done` tras el shebang de `guard-completado.sh`,
`ARNES_HOOKS_DIR=<copia> bash tests/escenarios/hooks/run.sh <sección 40/7>` →
**`FAIL  … mín(r) 1.381× > techo …`, `Resultado: 5 PASS, 1 FAIL`, `rc 1`**. `hooks/` del repo
**intacto** (`git diff --name-only -- hooks/` vacío). El camino conforme también es mío: en el banco
completo el caso real dio **`PASS  … máx(r) 1.232× <= techo`** con **`rc 0`**.

**Quinta vía, que el desarrollador no reclamó y que a mí sí me preocupaba:** añadir texto **dentro
del cuerpo de funciones** puede mover los flags de la **invariante 1** del corredor (`eje`/`ver`/`gua`
de `run.sh:1008`), y un `gua` encendido por accidente **exime** a una función de la guarda de salida
vacía — un fail-open silencioso. Reproduje ese mismo `awk` sobre las dos versiones de los dos
archivos: **los tres flags son idénticos función por función**. No se relajó nada.

### (4) La calibración de `1,28×`, conservada como recalibrada y no universal — **CUMPLE**

`FACTOR07` **no se toca** (sigue `1280`), y el comentario declara: la calibración de `QA-024-21`
**quedó SUSTITUIDA** (el `+1500` vale `1,177×`, **bajo** el techo; el caso de `1,28×` es `+3700`);
`1,280` se conserva como **magnitud sintética de entrada** de una demostración, «no un umbral que
nadie aplique»; y **no es una constante universal** — «la dispersión ENTRE corridas de un árbol
CONFORME (`1,095×–1,490×`) **SE SOLAPA** con él». En el contrato, viñeta propia en `REQ-024:564-575`.

Dos cosas quedan escritas **arriba** de su corrección y las nombro para que nadie las descubra dos
veces, ninguna es hallazgo: la derivación vieja del suelo (`1,250 × 1,021 = 1,276`, `FACTOR07:146-148`)
sigue en pie **seguida** de su refutación, que es la forma que este repositorio usa a propósito (un
registro no se reescribe encima); y el **nombre** del caso mantiene «la magnitud de los rojos REALES
de CI» con la precisión sólo en comentario — correcto por dos motivos: la frase es **verdadera** (es
la magnitud **observada**, no una atribución) y renombrar un caso **rompe el inventario** que `CA-12`
compara.

### (5) El falso rechazo sobre un candidato conforme, como limitación abierta — **CUMPLE**

Está **en el mensaje que la puerta imprime** —«Un FALSO RECHAZO sobre un candidato CONFORME es por
tanto una limitación ABIERTA de este instrumento, **y esta redacción NO la resuelve**»—, en el
hermano `37/5` («sigue siendo una limitación ABIERTA»), en `tests/…/README.md:421-424`, en el
**criterio** con viñeta propia («La LIMITACIÓN MATERIAL que esta redacción NO resuelve…», `REQ-024:576-585`, con
`1 de 12` y `4 de 12`), en `requirements/README.md:421-423` y en la **nota 1** de «Qué queda SIN VERIFICAR».
No es una nota al pie: es una viñeta del criterio, como el propietario pidió.

**Y no es documentación en lugar de remediación**, que era la trampa: la afirmación falsa **dejó de
estar escrita** (§1) *además* de quedar declarado el residuo. Las dos cosas, no una.

## 2. El `PASS → SKIP` de `REQ-017 CA-03` — mi juicio

**Lo que la evidencia sostiene:**

1. El archivo del caso (`secciones/37-coste-del-escaner-2-las-razones.sh`) tiene **cero cambios** en
   el delta (`git diff --name-only` vacío para él), y las secciones corren **cada una en su subshell**:
   no hay estado compartido por el que el delta pueda alcanzarlo.
2. El caso es una **sonda de reloj que se abstiene por contrato** cuando el techo cae **dentro** de su
   banda (`banda37`, `37/2:250-278`). `SKIP` es una **salida diseñada**, no un fallo del test.
3. **Basculó solo, delante de mí, sin el delta en juego:** corrí **sólo esa sección** tres veces
   seguidas sobre el árbol actual → **`PASS`, `PASS`, `SKIP`** (el `SKIP` con «el techo `2.600×` cae
   DENTRO de la banda `[0.994×, 3.106×]`»). Los dos archivos modificados **no se cargan** en esa
   corrida. 1 de 3.
4. **El reparto `PASS`/`SKIP` del banco es una propiedad de cada corrida, y lo medí más ancho que
   ellos:** mi corrida del banco completo sobre `d82c127` dio **`1083 PASS · 0 FAIL · 12 SKIP`, `rc 0`**,
   frente a `1087·8` (antes) y `1086·9` (después) del desarrollador. El **total cuadra en 1095 en las
   tres** — la diferencia que yo observo sobre el **mismo** árbol es de **3 casos**, el triple del
   desplazamiento que el delta supuestamente «causó».
5. La dirección es **abstención, nunca verde**: `FAIL` siguió en **0** y `rc` en **0**; y la abstención
   de este caso está **documentada de antes** en el propio repositorio
   (`docs/arnes/req-017-ca-03-modo-de-medicion/05-corridas-crudas.txt:39`,
   `docs/qa/evidencia-req-017-writeback-f4a5f1f/abstencion-sin-maquina.txt:3`).

**Lo que la evidencia NO sostiene, y lo digo porque la simetría es la regla:** no he demostrado que
aquella corrida concreta abstuviera *por* variabilidad. La variabilidad **no desmiente** un resultado
en **ninguna** de sus dos direcciones: ni el `SKIP` nuevo prueba un defecto del delta, ni el `PASS`
anterior lo descartaba. Lo que sí está demostrado es que **el mecanismo por el que el delta lo causaría
no existe** (1 y 2) y que **el efecto se produce sin el delta** (3 y 4).

**Conclusión:** el `PASS → SKIP` **no es hallazgo de este delta** y **no cambia el veredicto**. La
**tasa de abstención** de `REQ-017 CA-03` y su cota son asunto de `REQ-017`, **fuera de mi alcance**
(y la investigación general de la variabilidad está **excluida** por el propietario). Lo dejo anotado
como dato para ese dueño: **1 de 3** en corridas aisladas de la sección y **2 de 2** casos de `CA-03`
abstenidos en mi corrida del banco.

## 3. `QA-024-25` — **CERRADO** por quien lo abrió

Pedía que el criterio **dejara de afirmar** que la unanimidad excluye el ruido y **dijera lo que el
instrumento garantiza**. Verificado:

- `REQ-024 CA-07 (ii)` tiene ahora párrafo propio: «**La unanimidad es NECESARIA y NO SUFICIENTE, y el
  veredicto NO atribuye la causa: bloquea sin afirmar que ha demostrado una regresión del código ni
  descartado el ruido**», con la medición de las 12 corridas y la magnitud (`1,095×–1,490×` frente al
  `recorrido` intra-corrida).
- La tercera salida se renombró de `FAIL` **por regresión** a `FAIL` por **techo excedido**; la viñeta
  de la salida de (ii) y la de «los dos `FAIL`» dicen que **ninguno atribuye la causa**.
- El **sitio único** (`requirements/README.md`) recibió la doctrina con su par **Mal/Bien**.
- Los **dos mensajes** que el programa imprime ya no atribuyen (verificado ejecutándolos).
- Lo que el hallazgo prohibía expresamente **no pasó**: el techo sigue `1250`, la unanimidad sigue
  contratada, no hay `continue-on-error` y ningún bloqueo se volvió verde.

## 4. `QA-024-26` — **CERRADO** por quien lo abrió

Daba **dos** salidas; se tomó la primera, íntegra: el criterio **deja de presentar el suelo como cota
de lo que la corrida puede probar** y lo declara **dato diagnóstico estimado que se publica y no
gobierna**, con sus **dos** limitaciones (subestima, y no impide afirmar por debajo de sí mismo). El
rótulo publicado lo dice en el propio mensaje. **Y la contradicción que medí se disuelve**: lo que
`inj1500-01` hacía —publicar `suelo 1.296×` y **afirmar** una regresión con `mín(r) 1.262×`— ya no
puede ocurrir, porque el `FAIL` **no afirma** haber probado nada; dice que la medición excede el techo
y que no se acredita cumplimiento. La segunda salida («que el caso deje de afirmar por debajo de él»)
queda **declarada como no tomada**, con dueño (`desarrollador` con el propietario) en el propio
criterio — y eso es correcto: no era condición del cierre, era la alternativa.

## 5. Lo que NO reabro, y por qué va escrito

**«La puerta no aprueba lo que no puede resolver»** (`REQ-024:572-573`) sobrevive al rewrite y es la
**forma espejo** de `QA-024-25`, en el lado del `PASS`: si la dispersión que manda es la de **entre**
corridas, una corrida puede ser unánimemente conforme sin que el instrumento resuelva el factor que
vigila. **No lo abro como hallazgo**, y las tres razones son de método: (a) es **inferencia**, no
medición — las 12 corridas eran de un árbol **conforme**, así que **no medí ningún falso `PASS`**;
(b) la frase es **verdadera** bajo la definición que el propio criterio da de «resolver» (el techo
dentro del recorrido → nunca `PASS`), y eso lo verifiqué en la rejilla; (c) el criterio **ya declara**
dos líneas antes que «**no está demostrado que la puerta detecte una regresión de `1,28×`**» y escala
la **brecha** techo↔suelo como decisión de alcance **PENDIENTE** del propietario. Queda anotado aquí
para que ese gate lo tenga delante, no como vuelta nueva.

## 6. Hallazgos NUEVOS — los tres `instrumento`, ninguno bloquea

### `QA-024-29` — `instrumento` · dueño `desarrollador` · **no bloquea**

**El barrido de la promesa hermana quedó a medias en `37/5`, y la mitad que falta es la que el hallazgo
refutó.** La premisa de la que salía la atribución —«la decisión **no depende del ruido**»— se corrigió
añadiéndole **«DENTRO de la corrida»** en tres de sus cuatro sedes vivas (`40/7:266`, `40/7:292`,
`37/5:257`) y **no** en la cuarta: `37/5:336` sigue diciendo «son la evidencia de que la decisión **no
depende del ruido**, no el adorno del `SKIP`» — que es **la misma frase** que en `40/7:292` sí se
calificó. Y `37/5:254` la enuncia sin calificar («el veredicto sólo se emite si **NO depende del
ruido**»), aunque su definición calificada llega tres líneas después.

- **Reproducción:** `grep -rn "depende del ruido" tests/escenarios/hooks/ requirements/README.md requirements/REQ-024.md`
- **Esperado:** las cuatro sedes con la calificación, como pide «corregir la promesa completa».
- **Observado:** una sin calificar (`37/5:336`) y una calificada sólo por la frase siguiente (`:254`).
- **Severidad: baja.** Son **comentarios**: nadie los ve, no gobiernan nada y los **mensajes
  impresos** son correctos (`37/5:341` dice explícitamente «la unanimidad acota la dispersión DENTRO de
  esta corrida y NO la de ENTRE corridas»). Por eso es `instrumento` y **no** `contrato`: el REQ no
  afirma nada falso y nadie ve, decide ni cobra distinto. Entra en la cola, no en el cierre.

### `QA-024-30` — `instrumento` · dueño `analista-requerimientos` · **no bloquea**

**El delta tocó dos archivos que `Archivos:` de `REQ-024` no declara, y ese campo es la entrada de
`tools/arnes-paralelo.sh`.** Medido, archivo por archivo del delta: declarados sólo
`tests/escenarios/hooks/README.md` y `…/40-ausencia-que-abre-7-…sh`; **no declarados**
`tests/escenarios/hooks/secciones/37-coste-del-escaner-5-el-camino-normal.sh` y
`requirements/README.md` (además de `CHANGELOG.md`, `requirements/REQ-024.md` y dos `docs/arnes/…`,
que son bitácora). Los dos primeros importan: `37/5` es **código de banco de otro criterio**
(`REQ-017 CA-08 (ii)`) y `requirements/README.md` es el **sitio único** que comparten muchos REQ —
justo los dos candidatos a colisión. Un `disjunto` de `arnes-paralelo.sh` calculado sobre este campo
sería **falso**, y `AGENTS.md` §6 nombra el conflicto de fusión y el trabajo perdido como la
consecuencia medida.

- **Reproducción:** `for f in $(git diff --name-only f612b84..d82c127); do grep -q -- "$f" <(sed -n '4p' requirements/REQ-024.md) || echo "NO declarado $f"; done`
- **Es la misma familia que `QA-024-24`**, que cerré en la vuelta 2 porque `Archivos:` **sí** declaraba
  `40/7`. Ahora el alcance real creció y el campo no lo siguió. Lo abro de nuevo, medido, en vez de
  reabrir el cerrado.

### `QA-024-31` — `instrumento` · dueño `desarrollador` · **no bloquea**

**La demostración (3) del artefacto de evidencia no se puede re-derivar con el método que ella misma
declara.** Dice: «**72** líneas en `40/7` y **86** en `37/5` que contienen
`if`/`elif`/`case`/`while`/`return`/`exit` o `PASS=$…`/`FAIL=$…`/`RES07=`: diff **vacío** en ambos».
Aplicando ese filtro literal obtengo **85/88** y **100/100** líneas con **5** y **2** líneas
**distintas**; quitando además los comentarios, **81** y **92** con **2** distintas en cada uno. El
diff **no puede** ser vacío con ese filtro: la línea del `FAIL` contiene `FAIL=$((FAIL+1))`, así que
el filtro la captura y su cadena **sí** cambió (es el objeto de la comisión). Sólo queda vacío si se
normalizan las cadenas, que es el método de la demostración **(1)**, no de la (3).

- **Reproducción:** `pat='if|elif|case|while|return|exit|PASS=\$|FAIL=\$|RES07='` y
  `diff <(git show f612b84:<F> | grep -E "$pat") <(git show d82c127:<F> | grep -E "$pat")`
- **Lo que NO se sigue de este hallazgo, y por eso no bloquea ni cambia el veredicto:** **la propiedad
  que la demostración (3) afirma es VERDADERA**, y la verifiqué por otras cuatro vías (§1.3). El
  defecto está en **la prueba**, no en el producto: `AGENTS.md` §14 B.7 pide que el método permita
  re-derivar la cifra «sin preguntar», y aquí no lo permite. Es exactamente lo que la clase
  `instrumento` nombra.

## 7. Quality gates — corridas por mí sobre `d82c127`

| puerta | resultado |
|---|---|
| `bash -n` de `hooks/*.sh` y `tools/*.sh` | **OK** |
| `bash -n` de todo el banco (`run.sh`, `autoprueba`, 40+ secciones) | **OK** |
| `jq -e .` de `hooks.json`, `plugin.json`, `marketplace.json` | **OK** |
| `autoprueba-corredor.sh` (incluye las 7 de `CA-18`, que es lo que el piso mueve) | **106 PASS, 0 FAIL** |
| Banco completo | **1083 PASS · 0 FAIL · 12 SKIP · `rc 0`** (2 m 24 s) |
| Banco, sección `40/7` con `+3700` | **1 FAIL · `rc 1`** — el bloqueo sigue bloqueando |

**Contabilidad de `REQ-014 CA-18`, que es el único valor **con efecto** que el delta mueve** —
comprobada a mano además de por la autoprueba: `37/5` piso `21+100+346 = 467` ✔, archivo **523**
líneas, techo `max(400, 467×1,25) = 583` ✔; `40/7` piso `23+73+389 = 485` ✔, archivo **485** líneas,
techo `606` ✔. Los tres términos suman, el piso cabe y ningún archivo excede su techo.

## 8. Lo que NO hice

No toqué `tests/`, `hooks/`, `tools/`, `requirements/README.md`, `REQ-017.md`, ningún otro REQ ni los
otros tres worktrees. **No comiteé.** No toqué `Estado:` (sigue `bloqueado`) ni `Seguridad:` (sigue
`pendiente`, es del auditor). No abrí ni cerré `QA-024-19`, `QA-024-22/23/27/28` ni `SEC-084`. No
investigué la variabilidad del rendimiento (excluido por el propietario). No subí el techo, no relajé
la unanimidad y no propuse `continue-on-error`.

**Y una nota de forma sobre lo que remedié:** el texto corregido **no tiene guardián**. Las
demostraciones de los dos archivos comprueban el **veredicto** (`sed -nE 's/^  (PASS|FAIL|SKIP)  .*/\1/p'`)
y **nunca el contenido del mensaje**, así que reintroducir mañana «es regresión, no ruido» dejaría el
banco entero en verde. Eso **no es hallazgo nuevo**: es literalmente `QA-024-27`, que sigue **abierto**
con dueño `desarrollador`. Lo anoto porque es donde caería la regresión de esta misma corrección.

## 9. Qué escribí en la cabecera, y el cuadre de que el REQ NO se volvió cerrable

Sólo tres líneas del REQ, verificado con `git diff -U0` (hunks en `@@ -7 +7 @@`, `@@ -9 +9 @@` y la
fila añadida al final):

- **`QA:`** → `con-hallazgos` con el veredicto de la **vuelta 4**, su tercer pase, la cabeza
  `d82c127`, el cierre de `QA-024-25/26`, la apertura de `QA-024-29/30/31` y el puntero a este
  artefacto. **No** escribo `aprobado`: sería falso mientras `QA-024-19`, `SEC-084`, `CA-12 (ii)` y
  `CA-03` sigan como están.
- **`Hallazgos abiertos:`** → se **retiran** `QA-024-20`, `QA-024-21`, `QA-024-24` (cerrados en el
  segundo pase, que el analista dejó escritos a propósito) y `QA-024-25`, `QA-024-26` (cerrados aquí);
  **entran** `QA-024-29`, `QA-024-30`, `QA-024-31`.
- **Una fila del Historial**, la mía.

**Cuadre del campo, medido:** `total=21 · contrato=2 · instrumento=19 · sin-clase=0`. Los dos
`contrato` son `QA-024-19` y `SEC-084`, **así que `guard-completado` sigue denegando el cierre por esa
vía**, y además por `Estado: bloqueado`, por `Seguridad: pendiente` en un REQ `critico` y por las
**16** entradas de `PENDING_APPROVAL.md`. **Cuatro vías independientes, y cualquiera basta**: retirar
cinco hallazgos cerrados no vuelve cerrable este REQ. `tools/arnes-lectura.sh` —el mismo lector que la
puerta— lee la cabecera nueva **sin valor anómalo**.

**Divulgación de forma (`AGENTS.md` §13, «la invariante manda sobre cualquier preferencia»):** los dos
campos de cabecera se escribieron con la **herramienta de edición**, que es donde la puerta mide.
La **fila del Historial** se añadió por **consola** —es una sola línea de ~4 KB generada de una
plantilla— y lo digo en vez de callarlo: el Historial **no** es un campo que ninguna invariante mida,
y la palabra del estado terminal no aparece en el comando. Los campos que sí se miden no pasaron por
ahí.
