# QA — ventana 1.34.0

Registro de hallazgos del `qa-tester`. Continúa la serie `QA-017-nn` de `docs/qa/1.33.0.md`
(último usado allí: **QA-017-15**).

---

## REQ-017 · commit `19b1822` (rama `rel/registro-1.33.0`)

> **Cuenta de vueltas, corregida por la coordinadora el 2026-09-08.** Las vueltas 0/1/2 pertenecen
> al ciclo de **1.33.0**, que cerró (`completado`, QA y seguridad aprobados, publicado). El REQ se
> reabrió por `AGENTS.md` §9 porque **cambió el criterio**, no porque QA hallara un defecto: si el
> contador sobreviviera a una reapertura de §9, la REGLA DE ESTADO —«re-recorre el ciclo»— sería
> inoperante para todo REQ que hubiera gastado su tope. Ésta es la **vuelta 0 del ciclo nuevo**, y
> la re-validación de `538c266` es la **vuelta 1 de 3**. Lectura de la coordinadora, revocable por
> el propietario.

**Alcance de esta validación:** el write-back de **CA-08 (ii)** del 2026-09-08 —la resolución se
comprueba sobre la **razón**, con `k` derivado midiendo— y **sólo** lo que ese cambio toca. No se
revalidan los otros nueve criterios: el commit no toca `hooks/`, y los diez pasaron en la vuelta 2
sobre código idéntico.

**Fecha:** 2026-09-08 · **Modelo:** Opus (política de autoalojamiento, `AGENTS.md` §5).

### Cifras de la corrida (medidas por QA en esta máquina)

| Qué | Resultado | Cómo |
|---|---|---|
| Banco completo | **881 PASS · 0 FAIL · 5 SKIP**, `rc=0`, total **886** | `bash tests/escenarios/hooks/run.sh` |
| Cuadre del total | **886 = `CASOS_ESPERADOS`** | `run.sh:1303` |
| Sección `37/5` aislada | **9 PASS · 0 FAIL**, 44,6 s de reloj | `run.sh secciones/37-coste-del-escaner-5-*.sh` |
| `bash -n` de `hooks/*.sh` y `tools/*.sh` | pasa | `AGENTS.md` §7 |
| `jq -e .` de `hooks.json`, `plugin.json`, `marketplace.json` | pasa | `AGENTS.md` §7 |
| `bash -n` de las 50 secciones y de `tests/util/*.sh` | pasa | añadido por QA |

Las cifras del banco **coinciden** con las que midió la coordinadora sobre este mismo árbol. La
diferencia contra el `882 PASS · 4 SKIP` que informó el `desarrollador` es el caso
`REQ-017 CA-09 … los rangos de las dos pasadas SE SOLAPAN`, que emite SKIP o PASS según el ruido de
la corrida. **No es un defecto**: es un veredicto no determinista por diseño (la dispersión de esa
sonda es de `SEC-030`). El total es **886 en las dos**.

### Forma de los criterios (comprobado ANTES de probar nada)

`requirements/README.md` § «Cómo se escribe un criterio que no se desmiente» prohíbe tres formas.
**CA-08 no cae en ninguna** y se deja escrito para que no haya que volver a mirarlo:

- **enumeración** — CA-08 no enumera ningún conjunto que el código reconozca. No aplica.
- **número** — los cinco números de CA-08 declaran su tipo: `1,25×` **operativo** (dirección bajar),
  `≥ 6` series **operativo** (sube), `k` **operativo** (sube, y se deriva midiendo), `2×` del par (b)
  **operativo** (baja), techo de coste **operativo** (baja); y la **unanimidad de las k** se declara
  **de contrato** por nombre. Conforme a `REQ-012 CA-03`.
- **igualdad** — las dos mitades de coste están escritas como techo con dirección: (i) «no más de 0
  procesos añadidos … **menos** es conforme», (ii) «no más de 1,25×». Conforme a `REQ-012 CA-04`.

---

## Veredicto por criterio

| Criterio / cláusula | Veredicto | Evidencia |
|---|---|---|
| CA-08 (ii) — regla de emisión: PASS si `máx(r) ≤ techo`, FAIL si `mín(r) > techo`, SKIP si el techo cae dentro | **PASS** | `37/5:312-318`; salida real de la corrida |
| CA-08 (ii) — unanimidad de las k, sin mayoría / promedio / «mejor de k» | **PASS** | falsado con 6 entradas sintéticas (abajo) |
| CA-08 (ii) — publica las k razones, el recorrido y el techo | **PASS** en los tres caminos de decisión · **parcial** en la abstención por no-convergencia | `37/5:311`; QA-017-17 |
| CA-08 (ii) — par discriminante (a) **ejecutado**, no argumentado | **PASS** | `37/5:385-394`, caso verde en la corrida |
| CA-08 (ii) — par discriminante (b) **ejecutado**, no argumentado | **PASS** | `37/5:395-412`, caso verde en la corrida |
| CA-08 (ii) — el techo `≤ 1,25×` sigue intacto | **PASS** | `TECHO47=1250` idéntico antes y después |
| CA-08 — convergencia por brazo, **mismo umbral** | **PASS** | equivalencia lógica verificada |
| CA-08 — convergencia por brazo, **mismo SKIP** | **FAIL** en `19b1822` → **PASS** en `538c266` | **QA-017-16**, cerrado |
| `REQ-014 CA-18` — el piso declarado mide el bloque real | **PASS** (verificado a mano sobre `538c266`) | fronteras 123-449, mutación del caso nuevo |
| CA-08 — techo de coste **declarado**, escrito como techo y nunca como igualdad | **PASS** | `37/5:165-176`, Historial, README, CHANGELOG |
| CA-08 — techo de coste **derivado en la misma corrida en que se derive k** | **no verificado / no se cumple** | **QA-017-21** |
| CA-08 (ii) — `k` **derivado midiendo** y no elegido | **no verificable** | **QA-017-20** |
| CA-08 (i) — 0 procesos añadidos | **PASS** (no tocado por el commit) | «5 procesos frente a 5» ×2 |
| `Archivos:` declara lo que el commit toca, sin decoración | **PASS** en decoración · **FAIL** en exactitud | **QA-017-22** |
| Quality gates de `AGENTS.md` §7 | **PASS** | tabla de arriba |

### Lo que sí quedó probado, y cómo (postura de falsación)

La función que decide (`veredicto08_47`, `37/5:283-319`) se extrajo a un banco de ataque y se
ejerció con entradas sintéticas. **La unanimidad aguanta:**

| Entrada | Esperado por CA-08 | Obtenido |
|---|---|---|
| 3 de 4 razones bajo el techo, 1 encima (1,300×) | SKIP — no hay mayoría | `SKIP … [1.000×, 1.300×]` |
| 3 de 4 con regresión real de 2×, 1 limpia | SKIP — no hay «mejor de k» ni promedio | `SKIP … [1.000×, 2.000×]` |
| 4 de 4 con regresión de 2× | FAIL | `FAIL  mín(r) 2.000× > techo en TODAS` |
| 4 de 4 limpias | PASS | `PASS  máx(r) 1.000× <= techo` |
| borde exacto `rmax = techo` | PASS (`≤`) | `PASS  máx(r) 1.250× <= techo` |
| `rmin = techo`, `rmax = techo + 0,001×` | SKIP | `SKIP … [1.250×, 1.251×]` |

El par discriminante **ejerce la función que decide los casos reales** (`37/5:340` y `:387`), no una
copia suya, y el `PISO_AUTONOMO_SECCION` lo deja escrito. Eso es lo que hace que el par valga: es la
diferencia entre acreditar la guarda y acreditar un simulacro de la guarda.

---

## Hallazgos

### QA-017-16 — el SKIP por no-convergencia dejó de citar «la razón que sí obtuvo», y tres artefactos siguen diciendo que no cambió · **clase: `contrato`** · **CERRADO el 2026-09-08 en `538c266`**

**Severidad:** media. **Bloqueaba el cierre** (`AGENTS.md` §6). **Cerrado** — evidencia al final de
esta entrada.

CA-08 contrata, para la abstención por convergencia de brazo:

> … emite **SKIP citando las dos razones de convergencia y la razón que sí obtuvo**, nunca PASS y
> nunca FAIL (CA-06). […] **la primera se mantiene tal cual —mismo umbral, mismo SKIP—**.
> (`requirements/REQ-017.md:80`)

**El umbral sí se mantiene.** El código pasó de `[ "$ce" -gt "$TECHO47" ] || [ "$ch" -gt "$TECHO47" ]`
a `CONV47=max(ce,ch); [ "$CONV47" -gt "$TECHO47" ]` — equivalente, mismo `TECHO47=1250`.

**El SKIP no.** El mensaje perdió el tercer elemento. Antes (`19b1822^`):

```
… no puede distinguir una regresión de su ruido. La razón que sí obtuvo es 1.020×, y sobre eso no se firma
```

Ahora (`37/5:275`):

```
… no puede distinguir una regresión de su ruido, y sobre eso no se firma
```

**Causa mecánica:** la refactorización movió el cálculo de la razón (`R47`, `37/5:278`) a **después**
de la comprobación de convergencia, que retorna antes (`:276`). En ese camino `R47` nunca se calcula,
así que no hay nada que citar.

**Pasos de reproducción**

```bash
# entrada: el brazo "este" no converge (1,400×)
veredicto08_47 X si 1000000:1400000:1000000:1020000
```

**Esperado** (CA-08): SKIP citando 1,400× (este), 1,020× (heredada) **y la razón obtenida**.
**Observado:** SKIP citando 1,400×, 1,020× y el techo. **La razón obtenida no aparece.**

**Por qué es `contrato` y no `instrumento`.** Tres artefactos afirman hoy algo falso sobre lo
construido, y dos de ellos los **escribió este mismo commit**:

1. `requirements/REQ-017.md:80` — «la primera se mantiene tal cual —mismo umbral, mismo SKIP—».
2. `tests/escenarios/hooks/README.md:325-326` — «**SKIP citando las dos convergencias y la razón que
   sí obtuvo**, nunca PASS y **nunca FAIL**». Documentación que los proyectos heredan.
3. `CHANGELOG.md`, entrada del 2026-09-08 — «La convergencia por brazo **se queda como estaba**
   —mismo umbral, mismo SKIP—».

**Ningún caso del banco lo habría visto**, y ése es el segundo defecto: la autoprueba de la cláusula
(`37/5:349-366`) reduce cada salida a la **palabra** del veredicto con
`sed -nE 's/^  (PASS|FAIL|SKIP)  .*/\1/p'` y **descarta el mensaje entero**. Lo mismo vale para la
publicación de las k razones: **nada en el banco comprueba el contenido de ninguno de los dos
mensajes**, sólo el veredicto. Una guarda cuyo contrato es *lo que dice* está verificada sólo por
*lo que decide*.

**Por qué no se cierra relajando el criterio.** `REQ-012 CA-06` lo prohíbe por nombre: «Dado un
criterio que el código **no** cumple, Cuando se descubre, Entonces **no** se relaja el criterio para
que encaje: se abre el hallazgo contra el código». La vía de write-back **no** está disponible aquí;
la corrección es del código.

**Dueño:** `desarrollador`.

---

### Cierre de QA-017-16 — verificado por QA sobre `538c266` (vuelta 1 de 3 del ciclo nuevo)

El `desarrollador` calcula la razón **antes** de abstenerse y la publica en `MOT47`, dejando `R47`
vacío; y añade el caso `REQ-017 CA-08 (ii) el SKIP por no convergencia cita las TRES cifras`, que
llama a `razon08_47` —la función que decide, no una copia— **sin subshell**.

**Lo que QA verificó, con su medición y no con la del desarrollador:**

**1. El caso lee el CONTENIDO del mensaje y nombra cuál cifra falta.** Fail-before ejecutado contra
el `razon08_47` de `19b1822` con el bloque de aserciones de hoy:

```
FAIL  … al mensaje le falta: la-razón-que-sí-obtuvo(1.600×) — salió <… y sobre eso no se firma>
```

Pass-after sobre el árbol de hoy: **PASS**, publicando el mensaje entero. No compara la palabra del
veredicto: compara las cifras dentro del texto.

**2. Las tres cifras son distintas entre sí y del techo, y cada una se detecta por separado.** La
entrada `1000000:1400000:625000:637500` produce **1,400× (este) · 1,020× (heredada) · 1,600×
(obtenida)** contra el techo **1,250×** — cuatro valores distintos, ninguno subcadena de otro. Y no
se dio por bueno leyendo: se probó con **mutación** del código, cinco mutantes y cinco detecciones:

| Mutante | Lo que detecta el caso |
|---|---|
| quita `$x` del mensaje | `FAIL → la-convergencia-de-este(1.400×)` |
| quita `$ce` del mensaje | `FAIL → la-convergencia-de-la-heredada(1.020×)` |
| quita `$rob` del mensaje | `FAIL → la-razón-que-sí-obtuvo(1.600×)` |
| `R47` deja de quedar vacío | `FAIL → R47-no-quedó-vacío(<1600>)` |
| la llamada se envuelve en `$( )` | `FAIL →` las tres cifras a la vez |

Ninguna cifra se da por presente por casar con otra: cada una tiene su mutante y su nombre propio.
El quinto mutante es el que importa para la clase: **envolver la llamada en un subshell rompe el
caso en voz alta**, así que el «sin subshell» no es una convención escrita en un comentario, es
fail-closed.

**3. `R47` sigue vacío en la abstención y el llamador no cambia.** `razon08_47` retorna antes de
`R47=$(( ue * 1000 / uh ))`; el caso lo asegura con `[ -z "$R47" ]` y el cuarto mutante demuestra
que esa aserción muerde. `veredicto08_47:300` —`[ -n "$R47" ] || { … SKIP …; return 0; }`— **no lo
toca el commit**. La llamada sin subshell deja `MOT47`/`CONV47` globales, pero nada aguas abajo los
lee sin reiniciarlos: `razon08_47` los reinicia en su primera línea. Comprobado además en contexto:
la sección da **10 PASS · 0 FAIL** y el par discriminante que va justo detrás se comporta igual que
antes.

**4. El mensaje es byte a byte el de `19b1822^`.** Comprobado con `cmp` por QA, reconstruyendo el
mensaje del árbol anterior con los mismos valores. **Idénticos.**

**5. Los tres artefactos que el hallazgo señalaba como falsos vuelven a ser verdaderos y NO se
tocaron** (`REQ-017.md:80`, `tests/escenarios/hooks/README.md:325-326`, la entrada de `19b1822` del
`CHANGELOG.md`). Ésa es la forma correcta de cerrar un `contrato`: se hace verdadero el código, no
más laxo el texto — `REQ-012 CA-06`.

**Lo que este cierre NO alcanza:** **QA-017-17 sigue abierto** (la abstención descarta las razones
de las demás repeticiones). El arreglo recupera la tercera cifra de **esta** repetición; no publica
las de las otras.

**Cuadre:** `CASOS_ESPERADOS` 886 → **887**; `CASOS_ESPERADOS_SECCION` de `37/5` 9 → **10**. Banco
completo sobre `538c266` (medido por la coordinadora): **882 PASS · 0 FAIL · 5 SKIP**, `rc=0`,
total **887**. Quality gates de `AGENTS.md` §7 re-corridas por QA: **en verde**.

---

### El piso de `REQ-014 CA-18` pasó de 411 a 448 — comprobado por QA, **justificado**

No se abre hallazgo. Se deja la evidencia escrita porque el piso es la única entrada de `CA-18` que
**ninguna máquina puede verificar**, y porque mover un techo sin causa es exactamente lo que ese
criterio existe para impedir.

**El piso es una MEDICIÓN, no una perilla, y el desarrollador no tenía discreción para dejarlo.**
`autoprueba-corredor.sh` exige `términos declarados == piso declarado`. Con el bloque en 327 líneas,
mantener `411` habría exigido declarar el tercer término como `290`, es decir **declarar un tamaño
falso**. Lo que la máquina **no** puede comprobar es si `327` es el tamaño real: eso se verificó a
mano, así:

**(a) Las fronteras, medidas.** Línea **123** = `# ---------- CA-08 · LAS DOS MAGNITUDES DEL CAMINO
NORMAL ----------`; línea **449** = el `fi` que cierra el par discriminante positivo; línea 450 en
blanco; línea **451** abre la cola de CA-06 (`# La sonda de procesos se comprueba A SÍ MISMA`).
`449 − 123 + 1 = **327**`. La cola de CA-06 (451-504) queda **fuera** del bloque, y es correcto: no
llama a `razon08_47` ni a `veredicto08_47`, y podría vivir en otro archivo.

**(b) El crecimiento es exactamente lo añadido, y todo lo añadido cae dentro del bloque.** Los tres
hunks del commit: `@@ -13,8 +13,8 @@` neto **0** (dentro del preámbulo de 21, que no cambia de
tamaño), `@@ -271,8 +271,15 @@` neto **+7** (dentro del bloque), `@@ -367,6 +374,36 @@` neto **+30**
(dentro del bloque). `290 + 37 = **327**`; archivo `467 + 37 = **504**`. **No se movió nada al
interior del bloque para inflarlo.** Preámbulo (21) y maquinaria duplicada (100) intactos.

**(c) Las 30 líneas nuevas son indivisibles, y está MEDIDO, no argumentado.** El quinto mutante de
arriba lo demuestra: envuelta en `$( )`, la aserción de `R47` muere con el subshell y el caso
**FALLA nombrando las tres cifras**. El caso no puede vivir en otro archivo de sección —`razon08_47`
está aquí— ni dentro de un subshell. Viaja con la función que decide, luego pertenece al bloque
dondequiera que se le coloque físicamente.

**(d) La aritmética del techo y su comprobación por máquina.** `techo = ceil(piso × 1,25)`:
`ceil(448 × 1,25) = **560**` (antes `ceil(411 × 1,25) = 514`). `autoprueba-corredor.sh` da **12
casos de CA-18 en PASS**, incluidos «los términos SUMAN el valor declarado», «el piso declarado cabe
en el archivo» y «ningún archivo excede `max(N, piso × k)`» con `N=400`, `k=1,25`.

**Anotado sin abrir hallazgo, porque es de `REQ-014` y no de éste:** el tercer término del piso —el
tamaño del bloque indivisible— **no lo verifica ninguna máquina**. `CA-18` comprueba legibilidad,
suma, `piso ≤ líneas` y `líneas ≤ techo`; las cuatro pasarían igual con un tercer término inflado.
Va a `docs/PENDIENTES.md`.

**Y el dato que la coordinadora pidió que se dijera:** el archivo queda en **504** y **ya cabía bajo
el techo anterior de 514**. El cambio **no compró margen para esta entrega**; deja el techo puesto
donde la fórmula lo pone con el bloque real.

---

### QA-017-17 — la abstención por no-convergencia tira las razones ya calculadas: una regresión de 3 de 4 no deja rastro · **clase: `instrumento`**

**Severidad:** baja. No bloquea.

`veredicto08_47` recorre las repeticiones en orden y, en cuanto una no produce razón, emite SKIP y
**retorna** (`37/5:300`), descartando las razones de las repeticiones ya procesadas y sin llegar a
las siguientes. CA-08 dice «el caso **publica las k razones, su recorrido y el techo**».

**Reproducción y evidencia**

```
veredicto08_47 B3 si 1000000:1400000:1000000:1010000 \
                     2000000:2020000:1000000:1010000 \
                     2000000:2020000:1000000:1010000 \
                     2000000:2020000:1000000:1010000
→ SKIP  B3  repetición 1 de 4: la sonda NO convergió: …
```

Las repeticiones 2, 3 y 4 valen **2,000×** cada una —una regresión al doble, muy por encima del
techo— y **no aparecen en ninguna parte de la salida**. El caso se abstiene, que es correcto, pero
lo hace **mudo**: quien lea el banco no sabe que había tres razones al doble del techo. Es
exactamente el modo en que una regresión real se vuelve invisible detrás de una abstención
legítima.

**Dueño:** `desarrollador`. **Forzador:** la primera corrida de la puerta requerida en que una
repetición no converja (medido: ocurre; los dos rojos de `hooks-en-linux` tenían brazos a 1,232× y
1,249× contra 1,250×). **Vencimiento:** el cierre de `SEC-030` o la ventana 1.35.0, lo que llegue
antes.

---

### QA-017-18 — la función que aplica la unanimidad **de contrato** no comprueba que llegaron `k` repeticiones · **clase: `instrumento`**

**Severidad:** media (latente). No bloquea: no es alcanzable por el llamador de hoy.

CA-08 declara la unanimidad de las **k** repeticiones **de contrato**, y nombra `k = 1` como la
sonda ciega que produjo los dos rojos de CI: «`k = 1` —lo que había— **no tiene recorrido**».
`veredicto08_47` **no conoce `KRAZ47`**: sólo exige `[ "$n" -ge 1 ]` (`37/5:306`).

**Reproducción**

```
veredicto08_47 A1 si 1000000:1010000:1000000:1010000
→ PASS  A1  máx(r) 1.000× <= techo en las 1 razones: 1.000× · recorrido 1.000× · techo 1.250× · …
```

**Esperado:** que la función que aplica un contrato de unanimidad sobre `k` se niegue a firmar con
menos de `k`. **Observado:** firma un **PASS** sobre una sola razón, publicando literalmente
«1 razones … recorrido 1.000×» — el recorrido tautológico de un punto.

**Alcanzabilidad hoy: no.** El bucle de recolección (`37/5:212-224`) empuja siempre `KRAZ47`
entradas y mete `:::` cuando una repetición falla, lo que lleva el caso a SKIP. Por eso es
`instrumento` y no bloquea. Pero la guarda depende de que **el llamador** se acuerde, y el propio
archivo tiene escrito el motivo por el que eso no basta (`37/5:136`: «una disciplina que depende de
que quien llama se acuerde no es una disciplina»).

**Dueño:** `desarrollador`. **Forzador:** cualquier cambio en el bucle de recolección de `37/5` o su
mudanza a `tests/util/` (**REQ-021**). **Vencimiento:** el cierre de REQ-021.

---

### QA-017-19 — el parser de la repetición acepta registros con el número de campos equivocado y emite veredicto · **clase: `instrumento`**

**Severidad:** baja (latente). No bloquea.

`razon08_47` (`37/5:263`) parte el registro `ue:ue2:uh:uh2` con expansiones de prefijo/sufijo y
**no comprueba cuántos campos había**. `num47` sólo valida que los cuatro trozos resultantes sean
dígitos, y esos trozos existen aunque el registro tenga 2 o 6 campos.

| Entrada | Campos | Esperado | Observado |
|---|---|---|---|
| `1000000:1010000` | 2 | rechazo | `PASS  0.990×` (`uh` y `uh2` se rellenan con el mismo valor) |
| `1000000:1010000:1000000:1010000:999:999` | 6 | rechazo | `PASS  1.000×` (`uh2=999` → convergencia de la heredada calculada sobre basura) |
| `99999999999999999:1010000:1000000:1010000` | 4 | rechazo o abstención | `FAIL  7766279631.452×` — desbordamiento silencioso de int64 en `ue*1000` |

El tercero **no es alcanzable** (1e17 µs son ~3 170 años) y se anota sólo porque el camino que
produce un **FAIL** —un rojo en la puerta requerida de `main`— no valida su rango. Los dos primeros
sí describen una clase real: si algún día un campo de `sonda-reloj.sh` trajera un `:` o quedara
vacío de otra forma, el caso emitiría un veredicto en vez de abstenerse. Lo correcto está a una
línea: contar los campos.

También queda anotado que un **segundo mínimo menor que el mínimo** —físicamente imposible, señal
de sonda rota— pasa la comprobación de convergencia sin decir nada (da `ce = 0`).

**Dueño:** `desarrollador`. **Forzador:** cualquier cambio de formato en el registro de
`tests/util/sonda-reloj.sh` (**REQ-021**). **Vencimiento:** el cierre de REQ-021.

---

### QA-017-20 — la derivación de `k` no tiene registro auditable: las 56 repeticiones existen sólo como prosa · **clase: `instrumento`**

**Severidad:** media. No bloquea, pero **es la cifra sobre la que descansa toda la guarda**.

CA-08 manda que `k` «**se deriva midiendo** (`REQ-012 CA-03`), no se elige». El `desarrollador`
declara 56 repeticiones válidas de 56 intentadas en tres entornos y la saturación del recorrido
(`1,268× de 1,268×`, `1,262× de 1,262×`, y `1,178× de 1,262×` con `k = 3`).

**Ese dato no está en ninguna parte del repositorio.** Las tres únicas apariciones son **prosa que
repite las mismas cuatro cifras**:

- `tests/escenarios/hooks/secciones/37-coste-del-escaner-5-el-camino-normal.sh:150-156` (comentario);
- `tests/escenarios/hooks/README.md:341-344`;
- `requirements/REQ-017.md:463` (Historial).

No hay tabla de las 56 razones, ni el arnés de medición, ni las cuatro series, ni las ventanas.
**QA no puede auditar `k = 4` sin re-derivarlo** (56 repeticiones ≈ 30+ min de reloj), que es lo
que el encargo excluye por coste. Lo que se puede afirmar es que el **argumento** es correcto y que
el criterio de selección está bien elegido —el CHANGELOG descarta explícitamente «el menor k con el
recorrido bajo el techo» porque el recorrido es no decreciente en k y lo cumpliría `k = 1`—; lo que
**no** se puede afirmar es que las cifras sean las medidas.

**Este REQ ya pagó exactamente esta factura.** Historial del 2026-09-07: «Los **1,25–3,40×** los
declaró el `desarrollador` al implementar; **QA midió 1,154×–2,523×** y el suelo real está por
debajo del declarado». Y la lección quedó escrita: «un colchón declarado de más es cómo un residual
aceptado se vuelve un rojo sorpresa». La forma se repite; esta vez sin posibilidad de comprobarla.

**Dueño:** `desarrollador`. **Forzador:** la primera vez que haya que **subir** `k` —lo que CA-08
admite explícitamente («operativo: se sube con la medición»)—, porque sin la muestra no se puede
saber si el `k` nuevo satura ni desde dónde. **Vencimiento:** el primer cambio de `k`, o la ventana
1.35.0.

---

### QA-017-21 — el techo de coste no se derivó «en la misma corrida en que se derive k» · **clase: `instrumento`**

**Severidad:** baja. No bloquea.

CA-08 (`REQ-017.md:94`): el techo de coste «se deriva **en la misma corrida en que se derive k**».

Las dos mediciones son **dos campañas distintas**, y así lo describe el propio Historial:

- `k` — 56 repeticiones del par intercalado en **tres entornos** (ociosa, con 5 vecinos, 2 CPU con 6
  procesos encima), con un arnés de medición aparte;
- el coste — **banco entero** con `JOBS=6`, mínimo de 3 vueltas, 49,19 s sin la guarda y 77,19 s con
  ella.

No pueden ser la misma corrida: la primera necesita 56 repeticiones del par, la segunda necesita el
banco completo dos veces con y sin la guarda. **La exigencia tal como está escrita no es
satisfacible** — que es, por su nombre, la clase que este mismo REQ diagnostica dos veces
(«criterio derivado sin comprobar su factibilidad», `REQ-017.md:86`).

**Lo que sí se verificó, y pasa.** El techo **es un techo y muerde**, y su aritmética es
recalculable sin volver a medir:

- unidad declarada por el `desarrollador`: una repetición añadida cuesta ≈ 4,1 s (REQ de 6 líneas) +
  6,0 s (cabecera de 200) = **10,1 s**;
- con `k = 4` se añaden 3 repeticiones → ≈ 30,3 s previstos, **28,0 s medidos** → **0,569×**;
- con `k = 5` serían 4 → ≈ 37,3–40,4 s → **0,759×–0,821×**, **por encima del techo 0,750×**.

Es decir: **el techo se cruza en el valor de `k` inmediatamente superior al elegido**. No es holgura
puesta para que quepa el 0,569×; es una cota que restringe. Está escrita como techo, con dirección
admitida (**bajar**) y tipo (**operativo**), en cuatro sedes coherentes entre sí.

**Lo que queda anotado sobre la sede.** CA-08 dice «techo declarado» sin fijar dónde. El
`desarrollador` lo declaró en el comentario de `37/5:165-176`, en el Historial del REQ, en el README
del banco y en el CHANGELOG — **no en el cuerpo de CA-08**. Es conforme a la letra del criterio; se
anota porque el techo `≤ 1,25×` sí vive en el cuerpo, y la asimetría hace que el techo de coste sea
el único número de CA-08 que no se encuentra leyendo CA-08.

**Dueño:** `analista-requerimientos` (la exigencia insatisfacible es del texto). **Forzador:** el
primer cambio de `k`. **Vencimiento:** el primer cambio de `k`, o la ventana 1.35.0.

---

### QA-017-22 — `Archivos:` declara dos rutas que no existen, y no declara las dos reales · **clase: `instrumento`**

**Severidad:** baja. No bloquea. **Preexistente**; el commit editó esa línea sin corregirlo.

```
Archivos: … tests/escenarios/hooks/secciones/37-coste-del-escaner-1-escala.sh,
             tests/escenarios/hooks/secciones/37-coste-del-escaner-2-la-seccion-caliente.sh, …
```

Ninguna de las dos existe. Las secciones reales son:

```
tests/escenarios/hooks/secciones/37-coste-del-escaner-1-el-dominio.sh
tests/escenarios/hooks/secciones/37-coste-del-escaner-2-las-razones.sh
```

Es la clase de `SEC-020` —un campo `Archivos:` que declara rutas inexistentes es un mapa que no
protege lo que dice proteger— llegando por otra vía: **nombres caducados**, no decoración de
Markdown. La decoración sí está bien: el campo no lleva un solo carácter de marcado (verificado).

**Hoy NO produce un `disjunto` en falso**, y se comprobó en vez de suponerlo:

```
tools/arnes-paralelo.sh REQ-017 REQ-014 → colisiona
tools/arnes-paralelo.sh REQ-017 REQ-021 → colisiona
```

REQ-014 declara el directorio entero `tests/escenarios/hooks/` y REQ-021 declara **los mismos dos
nombres fantasma**, de modo que los pares colisionan igual. Ésa es la razón de la clase
`instrumento`, y es la misma disposición que se dio a **QA-017-14** en la vuelta 2.

**Anotado, sin abrir hallazgo:** el commit toca `CHANGELOG.md` y `requirements/REQ-017.md`, y el
campo no declara ninguno de los dos. La convención del repositorio está dividida (4 de 11 REQ
abiertos declaran `CHANGELOG.md`; REQ-014 y REQ-021 declaran su propio archivo de REQ, REQ-017 no),
así que no se cuenta como incumplimiento: se señala para que el `analista-requerimientos` fije la
convención en `requirements/README.md`, que hoy no dice nada de los dos artefactos que **todo**
commit toca.

**Dueño:** `analista-requerimientos`. **Forzador:** el primer REQ que declare `37/1` o `37/2` por su
**nombre real** — a partir de ahí el `disjunto` en falso es real. **Vencimiento:** el cierre de
`SEC-020` o de REQ-021.

---

## Resumen de clases

| Id | Clase | ¿Bloquea el cierre? | Dueño |
|---|---|---|---|
| QA-017-16 | `contrato` | **CERRADO** en `538c266` (2026-09-08) | `desarrollador` |
| QA-017-17 | `instrumento` | No | `desarrollador` |
| QA-017-18 | `instrumento` | No | `desarrollador` |
| QA-017-19 | `instrumento` | No | `desarrollador` |
| QA-017-20 | `instrumento` | No | `desarrollador` |
| QA-017-21 | `instrumento` | No | `analista-requerimientos` |
| QA-017-22 | `instrumento` | No | `analista-requerimientos` |

## Lo que NO se midió, dicho expresamente

1. **La derivación de `k` no se reprodujo.** 56 repeticiones, 30+ min de reloj; excluido por el
   encargo. Se auditó el **razonamiento** (correcto y con el criterio de selección bien elegido) y
   su **registro** (inexistente — QA-017-20).
2. **El techo de coste no se re-midió.** No se corrió el banco con y sin la guarda, tres vueltas
   cada uno, con `JOBS=6`. Se verificó la **coherencia aritmética** del modelo publicado y que el
   techo **muerde** en `k = 5`. La cifra `+28,0 s = 0,569×` queda **acreditada por el
   `desarrollador`, no verificada por QA**.
3. **El comportamiento en el CI real (`hooks-en-linux`) no se observó.** Todo lo medido aquí es
   local. El `desarrollador` declara él mismo que la emulación de 2 CPU **no** equivale al runner
   que decide la puerta, y ese límite se acepta tal como está declarado. **Si `k = 4` basta en el
   CI real está SIN MEDIR**, y sólo lo dirán las corridas siguientes de la puerta requerida.
4. **No hay prueba de carga ni umbral de rendimiento** en `AGENTS.md` que aplique a este REQ más
   allá del techo de coste. No procede.
5. **No se revalidaron los otros nueve criterios de REQ-017.** El commit no toca `hooks/`.
6. **No se revisó nada fuera de REQ-017.**

---

## REQ-026 · commit `2a91c82` (rama `rel/registro-1.33.0`)

Vuelta **0 de 3**. Objeto bajo prueba: `hooks/rotar-artefactos.sh` (reconocedor de entradas por
propiedad, puntero fuera de la tabla, fail-closed de estructura ambigua) y
`hooks/estado-derivado.sh` (tercera rama del bloque derivado). El riesgo que gobierna la revisión:
**este hook ya escribe dentro de `requirements/`**, donde la cabecera y los criterios son el
contrato.

### Quality gates (`AGENTS.md` §7)

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | **PASA** |
| `jq -e . hooks/hooks.json` | **PASA** |
| `jq -e .` sobre `plugin.json` + `marketplace.json` | **PASA** |
| `jq -e . .arnes/config.json` | **PASA** |
| Rotación sigue apagada (`activo:false`, `artefactos:[]`) | **CONFIRMADO** |

**Fail-before reproducido por QA, no aceptado por declaración.** Sección 28/3 contra los hooks de
`c59fd83` vía `ARNES_HOOKS_DIR`: **16 FAIL / 3 PASS**, y los tres que pasan son exactamente
`CA-07`, `CA-09` y `CA-10`, tal como el desarrollador declaró. `CA-06` falla con
`esperado=iguales-20 obtenido=iguales-0`: la mitad «byte a byte» pasa y la mitad «SÍ se movió»
falla — **el motivo correcto**, el discriminante no es vacuo.

### Veredicto por criterio

| CA | Resultado | Evidencia |
|---|---|---|
| CA-01 | **PASA** | 26→10 en doc, 16 al archivo (copia de `REQ-007`) |
| CA-02 | **PASA** | cabecera y separadora permanecen; movidas = `N − conservar` exacto |
| CA-03 | **PASA** | marca/cabecera/separadora pegadas; **dos bloques con columnas distintas** verificados |
| CA-04 | **PASA** | puntero **antes** de la cabecera, filas conservadas pegadas a la separadora |
| CA-05 | **FALLA** (condición NUL) | multiconjunto exacto en 5 REQ reales; **se pierden 5 filas y 17.697 B** con un NUL — QA-026-01 |
| CA-06 | **PASA** | fuera de la sección byte a byte en 5 REQ reales; tabla de criterios intacta |
| CA-07 | **PASA** | conducta conservada (pasa en ambas versiones, por diseño) |
| CA-08 | **FALLA** | las 4 formas declaradas fail-closed con textos distintos; **existe una quinta** — QA-026-02 |
| CA-09 | **PASA** | tabla sin filas → avisa «sin entrada reconocible», no rota |
| CA-10 | **PASA** | bajo umbral: byte a byte igual y **sin** aviso (`REQ-001`, 14.064 B) |
| CA-11 | **PASA** | archiva la más antigua, conserva la más reciente; **espejo `nuevo-primero` invierte exacto** |
| CA-12 | **PASA** | no anida `historial/historial`; el archivo no entra como origen |
| CA-13/14/16/17 | **FUERA DE ALCANCE** | gate humano; no validados por encargo |
| CA-15 | **CONFORME con límite declarado** | método auditado, no re-medido — QA-026-05 |

### Hallazgos

#### QA-026-01 (`usuario/dinero`) — un byte NUL hace que el rotador ESCRIBA MEDIO DOCUMENTO encima del REQ, en silencio

`hooks/rotar-artefactos.sh:263` lee el documento con la forma **cruda**:

```bash
texto=''; IFS= read -r -d '' texto < "$f"
```

`read -d ''` se detiene en el **primer NUL** y devuelve 0. El llamador cree tener el documento
entero, tiene la mitad, y el paso 7 escribe esa mitad **encima del original**.

**El arnés ya tiene el lector que evita esto y este hook no lo usa.** `arnes_lee_archivo`
(`hooks/lib.sh:1120`) nació de **SEC-002 / R-001** por este mismo fallo y devuelve `ARNES_TEXTO=''`
+ rc 1 —«no se devuelve la mitad de un documento»—. Su **único** llamador hoy es
`guard-completado.sh:150`, que ante un NUL **deniega** («una puerta que no puede medir no deja
pasar»). El rotador, que además **escribe**, se quedó con la forma cruda en `:86` y `:263`.

**Reproducción** (copia en `/tmp`, nunca el REQ real):
1. `cp requirements/REQ-007.md $P/requirements/REQ-007.md`.
2. Insertar un `\x00` al inicio de una fila de datos situada a **>16.384 B** del encabezado de la
   historia (así la sección **truncada** sigue por encima del umbral). Medido: NUL a 23.082 B.
3. Manifiesto con `activo:true`, `glob:"requirements/REQ-*.md"`,
   `seccion:"## Historial de cambios"`, `conservar_entradas:10`, `umbral_bytes:16384`.
4. Ejecutar `hooks/stop.sh` con el evento `Stop`.

**Esperado:** no tocar el archivo y avisar — no se puede leer el documento entero, luego no se
puede reconstruir (es la regla que ya aplica `guard-completado`).
**Observado:** `rc=0`, **stderr vacío**, y:

| | antes | después |
|---|---:|---:|
| bytes del REQ | 158.598 | 140.901 (**−17.697**) |
| filas de historia | 27 | 10 en doc + 12 en archivo = **22** |

**Cinco filas desaparecen de los dos lados** y el documento queda cortado a mitad de fila
(`| 2026-09-07 | fila con NUL `). Viola `CA-05` («cero filas perdidas»), la Historia del REQ («sin
que se pierda una sola fila») y la invariante que el propio archivo declara en su cabecera:
**«NUNCA BORRA … se prefiere un archivo grande a un archivo perdido»**.

**Sobre la atribución, con precisión.** La lectura cruda es **anterior** a este commit. Lo que
cambia con `REQ-026` es la **alcanzabilidad**: medido contra `c59fd83`, una sección en forma de
tabla daba **cero entradas** y la función retornaba **antes** del paso 7, así que sobre el corpus
de `requirements/` esta vía **no se podía alcanzar**; con los hooks de `2a91c82` se alcanza en cada
rotación de un REQ. Un latente inofensivo se convierte en un destructor silencioso del contrato,
que es exactamente el riesgo que este REQ tenía que no tener.

**Mitigantes medidos, que acotan pero no cierran:** la rotación está apagada y `artefactos` está
vacío; y hoy `## Historial de cambios` es la **última** sección de **los 26** REQ, así que un NUL
posterior a la historia no tiene texto detrás que destruir (verificado: con el NUL antes del
encabezado el hook avisa y no escribe; con la sección truncada por debajo del umbral, no rota).
Ninguno de los dos es una propiedad que el mecanismo garantice.

**Arreglo sugerido** (del `desarrollador`, no de QA): usar `arnes_lee_archivo` en `:86` y `:263` y
tratar el rc 1 como el resto de fail-closed de esta función — no tocar nada, avisar con texto
propio y contador propio, salir 0. Dueño: **`desarrollador`**. **Bloquea el cierre.**

#### QA-026-02 (`usuario/dinero`) — la quinta forma: DOS tablas en la sección; ni es ambigua para el hook, ni avisa, y deja filas bajo la cabecera equivocada

`CA-08` enumera **dos** casos y los marca **«no exhaustivos»**, y el desarrollador implementó
cuatro. La propiedad que manda es la del criterio: *no poder decir sin ambigüedad cuáles son las
filas de datos*. **Existe una quinta forma que la incumple y NO se detecta.**

**Causa raíz, en una línea:** la pasada 3a hace `break` en la **primera fila de datos**
(`hooks/rotar-artefactos.sh`, estado 2, rama `'|'*`), de modo que **nada de lo que viene después de
esa fila se valida jamás**. Una segunda tabla —o una segunda separadora— posterior entra al
troceado como **filas de datos**.

**Reproducción:** sección `## Historial de cambios` con dos tablas, `conservar_entradas:2`,
`umbral_bytes:300`:

```
| Fecha | Cambio | Causa |
|---|---|---|
| 2026-01-01 | fila A … |
| 2026-01-02 | fila B … |

| Fecha | OTRA COLUMNA | Y OTRA MAS |
|---|---|---|
| 2026-02-01 | fila C … |
| 2026-02-02 | fila D … |
```

**Esperado:** fail-closed — no archivar nada y avisar (misma razón que las otras cuatro formas:
aquí adivinar mal mueve el contrato de un REQ).
**Observado:** `rc=0`, **stderr vacío**, rota, y el **origen** queda:

```
| Fecha | Cambio | Causa |
|---|---|---|
| 2026-02-01 | fila C … |
| 2026-02-02 | fila D … |
```

Las filas C y D pertenecían a la **segunda** tabla; su cabecera y su separadora se **archivaron
como filas de datos**, y las filas conservadas quedan **bajo las columnas de otra tabla**. El
lector del REQ lee `Cambio` y `Causa` donde el documento decía `OTRA COLUMNA` y `Y OTRA MAS`: el
dato queda **reetiquetado en silencio**. En la variante sin línea en blanco entre las dos tablas el
origen se queda además con **dos separadoras seguidas**, y el bloque archivado termina con
`| Fecha | OTRA |` haciéndose pasar por historia.

`CA-05` se conserva en esta forma (el multiconjunto de filas no se pierde), y eso es justo lo que
la hace peligrosa: **ninguna comprobación de pérdida la ve**.

**Variante menor, misma causa raíz:** una fila separadora colocada **después** de la primera fila
de datos se archiva como fila de datos, sin aviso (verificado).

**Cobertura del banco: cero.** Los cuatro casos `CA-08` de
`tests/escenarios/hooks/secciones/28-rotacion-seccion-3-la-tabla.sh:149-166` están parametrizados
sobre las cuatro formas declaradas; no hay ningún caso con dos tablas.

Dueño: **`desarrollador`**. **Bloquea el cierre.**

#### QA-026-03 (`instrumento`) — `conservar_entradas` negativo mata el hook de parada y el bloque derivado no se escribe

Con `conservar_entradas: -1`, `hooks/rotar-artefactos.sh:452` explota:

```
rotar-artefactos.sh: line 452: ent[$i]: unbound variable
```

`set -u` + índice fuera de rango. El hook sale con **`rc=1`** y, medido, **el bloque derivado de
`docs/ESTADO.md` no se escribe** (control con `conservar_entradas: 1`: sí se escribe). Un error de
tecleo en el manifiesto deja la continuidad congelada dejando sólo un error de bash en un stderr
que —por el argumento del propio proyecto— no sobrevive a la sesión.

El guardián existente sólo cubre lo **no numérico** (`[ "$total" -gt "$conservar" ] 2>/dev/null`,
verificado: con `"diez"` el archivo queda intacto); un negativo lo atraviesa.

**No es una regresión de este commit:** reproducido igual contra `c59fd83` (allí `:311`) con una
sección en forma de lista. Fuera del alcance de `REQ-026`. Dueño: **`desarrollador`**, deuda
técnica. **No bloquea.**

#### QA-026-04 (`instrumento`) — `_doc_artefactos` describe un mecanismo que ya no existe

`.arnes/config.json:48` y `templates/arnes-config.json.tpl:53` siguen diciendo que las entradas son
«líneas que empiezan por `- `, `* `, `### ` o `N. `, con sus continuaciones». El código ya reconoce
además las filas de datos de la tabla que **es** la sección. Un proyecto que instale el arnés y lea
la plantilla concluirá que la historia de sus REQ **no** se rota.

Declarado como pendiente por el desarrollador; **nadie puede arreglarlo hoy**: el manifiesto es
gate humano (`AGENTS.md` §6). **Forzador que se arma solo:** el arreglo viaja con `CA-13`, que
toca exactamente esas claves. Por la regla de acumulación del propietario (2026-09-08) se
**registra y no abre REQ ni entra en la ventana**. Dueño: **`desarrollador`** + autorización del
propietario. **No bloquea.**

#### QA-026-05 (`instrumento`) — el techo de `CA-15` es defendible; su REGISTRO no permite verificarlo

Auditado el **método**, no re-medida la campaña (excluido por encargo).

**Lo que está bien, y no es poco.** El techo se **deriva** de una medición publicada con sus cinco
condiciones; el margen sale de una **dispersión medida** (`máx(activo) − mín(apagado)` =
187.110 − 56.434 = 130.676 µs) y no de un gusto; el redondeo declara su paso (5 ms → 135.000 µs, y
la aritmética cuadra: `ceil(130.676/5000)×5000`); y el desarrollador declara **en voz alta** los
tres límites del techo, incluido que la condición (i) se cumple **en plataforma y no en host**.
Eso es más de lo que traían los cuatro techos anteriores de esta clase.

**Los dos defectos del registro:**
1. **El juego de validación no dice qué estadístico son sus cifras.** `CA-15(v)` define el coste
   atribuido como **«la diferencia de las medianas de 5 paradas»**, y la validación se publica como
   `186.215 − 62.903 = 123.312 µs`, sin etiquetar si son medianas o extremos —el juego base sí
   distingue ambos—. Como `CA-15` exige literalmente que **«su mediana no supere ese techo»**, el
   criterio **no se puede verificar contra su propio registro**. Si son extremos, la comprobación
   es más estricta que la contratada (no infla el verde), pero entonces la cifra publicada no es la
   magnitud que el criterio define.
2. **No hay evidencia de que la regla del margen se fijara ANTES de ver el juego de validación.**
   El registro admite que un techo en la mediana pelada (121.192) lo habría incumplido el segundo
   juego (123.312) — dato que sólo se conoce **después** de medirlo. Con el orden sin declarar,
   «techo derivado» y «número ajustado hasta que cupiera» son **indistinguibles desde el
   registro**, aunque la regla elegida sea legítima y pre-declarable.

Dueño: **`analista-requerimientos`** (etiquetar los estadísticos y declarar el orden en `CA-15`).
**No bloquea.**

#### QA-026-06 (`instrumento`) — la línea del bloque derivado está construida y probada, pero ningún criterio la contrata

**El cambio en `hooks/estado-derivado.sh` NO es alcance tomado por su cuenta.** Contra el **REQ
vigente**, que es mi fuente de verdad, está declarado dos veces en la cabecera: `Módulo:` dice
«`estado-derivado.sh`, la línea del bloque derivado» y `Archivos:` lista
`hooks/estado-derivado.sh`. El motivo dado —el stderr de una parada no sobrevive a la sesión, y
`QA-102`/`QA-109` impusieron las otras dos ramas por ese mismo argumento— es además el correcto, y
la tercera rama es aditiva y coherente con sus hermanas.

Lo que falta es el contrato: **`CA-08` sólo exige avisar «por stderr»**. La línea del bloque
derivado es comportamiento aceptado que **ningún criterio describe**, y el banco ya lo comprueba
(`CA-10` verifica que **no** aparezca por debajo del umbral). Es deriva menor en el sentido de
`AGENTS.md` §9: pide **write-back** a `CA-08`. Dueño: **`analista-requerimientos`**. **No bloquea.**

### Resumen de clases

| Id | Clase | ¿Bloquea el cierre? | Dueño |
|---|---|---|---|
| QA-026-01 | `usuario/dinero` | **Sí** | `desarrollador` |
| QA-026-02 | `usuario/dinero` | **Sí** | `desarrollador` |
| QA-026-03 | `instrumento` | No | `desarrollador` |
| QA-026-04 | `instrumento` | No | `desarrollador` + propietario |
| QA-026-05 | `instrumento` | No | `analista-requerimientos` |
| QA-026-06 | `instrumento` | No | `analista-requerimientos` |

**Forma de criterio deficiente detectada:** ninguna. `CA-08` marca su enumeración como **«no
exhaustivos»** y apunta a la propiedad, `CA-15` declara sus cifras como **operativas** con la
dirección admitida, y `CA-13` distingue lo operativo de lo de contrato. Los criterios de este REQ
están escritos en la forma que `requirements/README.md` exige; **el hallazgo QA-026-02 existe
precisamente porque `CA-08` está bien escrito** — la implementación cubre la enumeración y no la
propiedad.

### Lo que NO se midió, dicho expresamente

1. **El banco completo no se re-corrió.** Se aceptan las cifras de la coordinadora
   (`902 PASS · 0 FAIL · 4 SKIP`, `rc=0`, 906). QA sólo corrió la **sección 28/3** contra
   `c59fd83` para reproducir el fail-before.
2. **La campaña de `CA-15` no se re-midió** (25 artefactos, dos juegos de 5 paradas). Se auditó
   **método y registro**; las cifras quedan **acreditadas por el `desarrollador`, no verificadas
   por QA**.
3. **El techo de `CA-15` en el runner de `hooks-en-linux` está SIN MEDIR**, y el propio
   desarrollador lo declara. Nada de lo medido aquí viene del CI.
4. **`CA-13`, `CA-14`, `CA-16` y `CA-17` no se validaron**: no se implementaron por diseño (gate
   humano) y quedaron fuera del encargo. **No cuentan como fallos.**
5. **La rotación no se encendió sobre el repositorio real.** Todo se probó sobre **copias en
   `/tmp`** con manifiestos sintéticos. Ningún REQ real se rotó.
6. **No hay prueba de carga ni umbral de concurrencia aplicable** en `AGENTS.md` más allá del techo
   de `CA-15`. No procede.
7. **Dos paradas simultáneas sobre el mismo REQ en forma de tabla no se probaron.** `REQ-015` cubre
   la carrera de temporales y el commit no la toca, pero la rama de tabla es nueva y su
   comportamiento bajo concurrencia queda **sin medir**.
8. **La rotación de artefacto ENTERO** (`arnes_rotar_uno`, `:86`) no se ejercitó; sólo se observó
   que comparte la lectura cruda de QA-026-01.
9. **No se revisó nada fuera de `REQ-026`.**

---

## REQ-026 · re-validación vuelta 1 · commit `931218b`

Sólo el **delta**. Quality gates re-corridas: las 4 pasan. Banco no re-corrido (cifras de la
coordinadora: `912 PASS · 0 FAIL · 4 SKIP`, 916, `rc=0`). Rotación sigue apagada; todo probado
sobre **copias en `/tmp`**.

### QA-026-01 — **CERRADO** (verificado por QA, no leído del commit)

`hooks/rotar-artefactos.sh:92` y `:297` usan ya `arnes_lee_archivo`. Medido por mí:

| Caso | Antes (vuelta 0) | Ahora |
|---|---|---|
| NUL **después** del inicio de la sección (copia de `REQ-007`) | −17.697 B, 5 filas perdidas, stderr **vacío** | 158.598 → **158.598 B**, no rota, avisa, `rc=0` |
| NUL **antes** de la sección | salía por otra rama, sin tocar nada | **también avisa**, con el mismo texto |
| Contención: `REQ-013` con NUL + `REQ-007`/`REQ-019` sanos, **misma parada** | — | `REQ-013` intacto y **citado en el aviso**; los otros dos **rotan** |

El aviso cita `SEC-002/R-001` y da salida accionable. **Verifiqué el detalle de método que él
declara:** con el NUL *antes* de la sección el hook sale por la rama «no contiene la sección» sin
tocar nada, así que esa reproducción **no muestra el defecto** — el defecto exige el NUL después
del inicio de la sección. Su fixture lo fija correctamente. Nota a favor del arreglo: al mover la
lectura fiable **antes** de la detección de sección, **las dos posiciones** quedan cubiertas, que
es más de lo que el fixture exige.

### QA-026-02 — **CERRADO**, y el ajuste de fixture es legítimo

La validación vive ya dentro del bucle de troceado y recorre la sección entera. Medido, con y sin
línea en blanco entre las dos tablas — **las dos** quedan byte a byte iguales y avisan:

| Variante | Resultado | Motivo publicado |
|---|---|---|
| dos tablas **con** línea en blanco | intacto, avisa | «hay lineas que no son filas de datos entre dos filas de datos…» |
| dos tablas **sin** línea en blanco | intacto, avisa | «hay otra fila separadora entre las filas de datos…» |

**Sobre el ajuste de fixture (quitar la línea en blanco): conserva lo que `-02` contrataba.** Lo
que `QA-026-02` contrataba es *dos tablas en la sección no rotan en silencio*, y el fixture
ajustado sigue teniendo dos tablas y sigue exigiendo fail-closed + aviso. Lo que se retira es la
fijación de **cuál de dos diagnósticos verdaderos se ve primero**, que es sobre-especificación: un
reordenamiento de las comprobaciones rompería un caso verde sin que hubiera regresión de conducta.
Y la variante con línea en blanco no queda sin cubrir — es la sexta forma, con casos propios.
**Verifiqué las dos variantes directamente**, que es más fuerte que cualquiera de los dos fixtures.

### Séptima forma: **no la encontré**. Dónde busqué, y el argumento que cierra el hueco

Doce formas probadas, ninguna rota en silencio nada que dañe:

| Forma | Resultado |
|---|---|
| estorbo (prosa) **entre** filas | fail-closed + aviso |
| comentario HTML entre filas | fail-closed + aviso |
| separadora como **última** línea | fail-closed + aviso |
| tres tablas · separadora indentada | fail-closed + aviso |
| sección **sin salto final** | rota bien, multiconjunto OK, fuera byte a byte |
| fila que es sólo `\|` · fila vacía `\| \|` | rota bien — en GFM **son** filas de datos, tratarlas así es correcto |
| lista antes de la tabla, `conservar=0` | rota como entrada de lista (CA-07), tabla intacta |
| tabla indentada con **TAB** en el preámbulo | preámbulo; la tabla real se reconoce; íntegro |

**El argumento que cierra la clase donde vivían la quinta y la sexta.** Las dos nacieron de una
segunda tabla cuya separadora no se veía. Auditado el predicado `arnes_rot_es_separadora`: acepta
**exactamente** el conjunto de caracteres que GFM admite en una fila delimitadora (`-`, `:`, `|`,
espacios/tabs) y **exige** al menos un `-`. Luego **no existe fila que GFM lea como separadora y el
hook lea como dato** — la dirección peligrosa está cerrada. Los desacuerdos van todos al lado
seguro (`|- - -|`: el hook dice separadora → fail-closed; GFM dice fila de datos). Verificado sobre
9 formas de separadora.

**Regresión sobre corpus real: ninguna.** 10 REQ reales (`007/001/013/019/023/014/021/017/012/016`)
en copia: 8 rotan con multiconjunto exacto y todo fuera de la sección byte a byte; los 2 que no
rotan (`REQ-001` 14.064 B, `REQ-016` 15.969 B) están **bajo el umbral** y no emiten aviso — `CA-10`
correcto. **Ningún fail-closed falso.**

### El residual declarado: inocuidad **VERIFICADA**

Una segunda tabla indentada (o prosa) **tras la última fila** rota sin avisar. Inspeccioné el
destino en tres configuraciones:

| Config | Dónde acaba el estorbo | ¿Línea intrusa entre dos filas de datos? |
|---|---|---|
| `nuevo-al-final`, `conservar=2` | se queda en el documento, tras la última conservada | **No** |
| `nuevo-primero`, `conservar=2` | viaja al archivo, **al final** del bloque | **No** |
| `nuevo-al-final`, `conservar=0` | todo al archivo, estorbo **al final** del bloque | **No** |

Multiconjunto exacto en las tres. **No hace daño:** `CA-03`, `CA-04` y `CA-05` se conservan, y la
conducta es la que `CA-07` ya sanciona (la continuación viaja con su entrada). **No es hallazgo de
producto.** El banco ya lo prueba (`28/3:326-330`, «control: una continuacion TRAS la ultima fila
no corta nada»), así que **no pido ningún caso nuevo** y no hace falta partir la sección en una
parte 4.

### QA-026-04 (v) — **CERRADO**: las cuatro ramas publican

Probadas las cuatro en una sola parada, cada una con sección **por encima** del umbral (mi primer
intento midió mal: la sección ambigua quedaba **bajo** el umbral y salía antes — fixture mío, no
defecto):

| Rama | Línea en el bloque derivado | Ejemplo citado |
|---|---|---|
| sin la sección declarada | sí, contador propio | `REQ-981.md` → `## Historial de cambios` |
| sin entradas reconocibles | sí, contador propio | `REQ-982.md` → `## Historial de cambios` |
| estructura ambigua | sí, contador propio | `REQ-983.md` → `## Historial de cambios` |
| lectura no fiable | sí, contador propio | `REQ-984.md` |

`CA-10` confirmado: el REQ bajo umbral (`REQ-985`) **no** aparece en ninguna línea `Rotación:` —
sólo como fila del inventario de REQ.

**La línea del NUL lleva sólo el nombre del archivo, y es lo correcto: no oculta información, evita
afirmar lo que no se ha medido.** Esa rama ocurre **antes** de saber si el documento contiene la
sección declarada; imprimir `→ ## Historial de cambios` publicaría la **expectativa del
manifiesto** como si fuera un hecho medido. Es la misma regla que este proyecto aplica en el bloque
derivado cuando dice `sin datos` y **nunca 0**. Y no se pierde nada operativo: el nombre del
archivo más el texto propio identifican el archivo y la acción (quitar el NUL, arreglar permisos).

### QA-026-05 — **CERRADO**: mis dos defectos del registro

1. **Estadístico nombrado.** «Todas las cifras son **medianas de 5 paradas**, y el coste atribuido
   es la **diferencia de esas dos medianas**» — exactamente la magnitud de `CA-15 (v)`. Aritmética
   re-verificada por mí: base `187.903 − 59.022 = 128.881`; envolvente
   `193.486 − 57.427 = 136.059`; redondeo 5 ms → **140.000**; validación
   `186.010 − 60.066 = 125.944 ≤ 140.000`, holgura **10,0 %**. Todo cuadra.
2. **Orden declarado y hecho verificable.** La regla del margen se escribió **antes de medir**, en
   la cabecera del arnés de medición, y los dos juegos corrieron en **invocaciones separadas** con
   el techo ya derivado e impreso antes de `val`. Eso es una garantía **estructural**, no una
   promesa: el juego de validación no pudo mover el número.

**Y hay que acreditarle lo que no estaba obligado a escribir:** admite por escrito que en la vuelta
anterior la regla «se eligió teniendo el número de validación delante»; declara que la subida
135.000 → 140.000 **no** se atribuye a los arreglos, porque las cuatro medianas medidas
(121.192–128.881 µs) caen dentro de la dispersión del instrumento y **el coste de los arreglos no
se aisló**; y limita lo afirmable a **camino, no reloj** — en régimen estacionario la comprobación
por fila no se ejecuta porque la función sale en la comparación de tamaño antes de trocear
(verificado: el paso 2 precede al troceado, y mis casos bajo umbral no hacen trabajo ni avisan).

### QA-026-06 — **CERRADO**

`CA-08 (v)` es ahora el criterio contra el que medirse, y contrata **más** de lo que yo pedí: aviso
**simétrico** en las cuatro ramas, con contador y texto distintivo, y el motivo escrito («un canal
condicionado a un segundo suceso no es un canal»). Verificado arriba. El analista **rechazó** la
asimetría del desarrollador, que era la decisión correcta.

### QA-026-07 (`instrumento`) — NUEVO: el único hueco que rota sin avisar no está contratado

Un estorbo **tras la última fila** rota sin aviso (inocuidad verificada arriba). No incumple
`CA-08 (i)`: el hook **sí** identifica esa línea, como continuación de la última entrada, que es la
regla de `CA-07`. Pero **ningún criterio lo dice**, así que hoy la frontera entre «continuación
legítima» y «estructura no reconocida» queda a interpretación del lector, y es justo la frontera
donde este REQ falló dos veces. Merece **una cláusula** en `CA-08 (ii)` o en `CA-07`: *una línea que
no es fila de datos y no tiene ninguna fila detrás no corta tabla y viaja con su entrada*. El
desarrollador ya dice que esto es del analista, y coincido. Dueño: **`analista-requerimientos`**.
**No bloquea.**

### QA-026-03 y QA-026-04 — **siguen abiertos**, sin cambio

- **QA-026-03** (`instrumento`): `:517-520` siguen sin guardián para un `conservar` **negativo**.
  Pre-existente, fuera del alcance de este REQ, ya en `docs/PENDIENTES.md`.
- **QA-026-04** (`instrumento`): `_doc_artefactos` sigue obsoleto en `.arnes/config.json` **y** en
  `templates/arnes-config.json.tpl` (1 ocurrencia cada uno). **Nadie puede arreglarlo hoy**: gate
  humano. Forzador: viaja con `CA-13`.

### Resumen de clases — re-validación

| Id | Clase | Estado | Dueño |
|---|---|---|---|
| QA-026-01 | `usuario/dinero` | **CERRADO** en `ade924e` (2026-09-09) | `desarrollador` |
| QA-026-02 | `usuario/dinero` | **CERRADO** en `ade924e`/`931218b` (2026-09-09) | `desarrollador` |
| QA-026-03 | `instrumento` | Abierto — no bloquea | `desarrollador` |
| QA-026-04 | `instrumento` | Abierto — no bloquea | `desarrollador` + propietario |
| QA-026-05 | `instrumento` | **CERRADO** en `931218b` (2026-09-09) | `analista-requerimientos` |
| QA-026-06 | `instrumento` | **CERRADO** en `6b07f88` (2026-09-09) | `analista-requerimientos` |
| QA-026-07 | `instrumento` | Abierto — no bloquea | `analista-requerimientos` |

**Ningún hallazgo abierto es `usuario/dinero` ni `contrato`.** El mecanismo queda acreditado:
`QA: aprobado` con el alcance declarado en la cabecera. El **cierre** no depende de esto —
`CA-13`, `CA-14`, `CA-16` y `CA-17` no están implementados y la cola tiene una entrada abierta.

### Lo que NO se midió en la re-validación

1. **Banco completo no re-corrido** (cifras de la coordinadora). Tampoco re-corrí `28/3` contra
   `6b07f88` para reproducir el fail-before de la sexta forma: **el fail-before de la sexta forma
   está acreditado por el `desarrollador`, no verificado por QA.**
2. **Campaña de `CA-15` no re-medida.** Audité estadístico, aritmética y orden; las cifras siguen
   **acreditadas por el desarrollador, no verificadas por QA**.
3. **Techo de `CA-15` en el runner de `hooks-en-linux`: sigue SIN MEDIR.**
4. **`CA-13`, `CA-14`, `CA-16`, `CA-17` no validados** — no implementados, fuera de ventana.
5. **Concurrencia sobre la rama de tabla: sigue sin medir** (dos paradas simultáneas sobre el mismo
   REQ). Arrastrado de la vuelta 0.
6. **La rotación de artefacto entero (`arnes_rotar_uno`, `:86`) sólo se verificó en que ya usa
   `arnes_lee_archivo`;** su conducta completa no se ejercitó.
7. **No busqué una octava forma** más allá de las doce probadas; la ausencia de séptima es
   **evidencia acotada, no demostración**.

---

## REQ-026 · re-validación vuelta 2 · commits `8248e58` (CA-18) y `43bfd47` (manifiesto)

Sólo el **delta**: `CA-18` y el ajuste del manifiesto. Banco no re-corrido (cifra de la
coordinadora sobre `43bfd47`: `920 PASS · 0 FAIL · 4 SKIP`, 924, `rc=0`). Rotación **no**
encendida; todo sobre copias en `/tmp`. `requirements/historial/` no existe al terminar.

> **Aviso de método sobre mi propia medición.** Mis dos primeras sondas de la carrera y de `(iv)`
> apuntaban a `$REPO/hooks` con `$REPO` **vacío**, así que invocaban `/hooks/stop.sh`: el hook
> **no se ejecutaba** y el resultado «no rota, no avisa» era un artefacto de la sonda, no conducta
> del código. Lo detecté al volcar el stderr crudo (`No such file or directory`). **Todas las
> cifras de abajo son de las corridas rehechas con ruta absoluta y con un guardián
> `[ -f "$H/stop.sh" ]` en la sonda.** Los brazos contra `1fe975f` siempre fueron válidos (ruta
> real), y por eso el defecto sí aparecía en ellos. Es la clase de fallo que este proyecto ya
> tiene medida —una sonda que no mide lo que dice— y queda escrita aquí para no repetirla.

### (a) El par discriminante de `(vi)`: **verificado, y el control NO es vacuo**

Fail-before corrido por mí, sección 4 contra `1fe975f` con `ARNES_HOOKS_DIR`: **5 de 8 FAIL**,
exactamente lo declarado. Los 3 que pasan son los **dos controles obligatorios** y el de `(iv)` —
los controles deben pasar a los dos lados o no controlan nada. El caso que discrimina falla así:

| Caso | contra `1fe975f` |
|---|---|
| `el 'Seguridad: aprobado' escrito en medio NO vuelve a 'pendiente'` | `esperado=1-0 obtenido=0-1` |

**Reproducción independiente mía** (fixture propio de 1.200 filas, escritor ajeno a los 150 ms):

| | `Seguridad:` tras la parada | filas en el doc | rotó | aviso | línea derivada |
|---|---|---:|---|---|---|
| `1fe975f`, 3/3 | **`pendiente`** (pisado) | 10 | sí | **no** | **no** |
| HEAD, 3/3 | **`aprobado`** (sobrevive) | 1.200 | no | **sí** | **sí** |

La actualización perdida de `SEC-067` se reproduce 3/3 en el código viejo, **en silencio**, y queda
cerrada 3/3 en HEAD, con aviso por stderr **y** línea propia en el bloque derivado.

**El control no es vacuo, y se ve en lo que afirma:** exige `10-1190-si-silencio` — 10 filas en el
documento, **1.190 en el archivo**, el destino legible como tabla y **sin** aviso de caducada. Un
fixture que se hubiera quedado bajo el umbral daría `1200-0-no-silencio` y fallaría. Verificado
directamente: el mismo fixture sin carrera **rota de verdad**.

Y `(vii)` completo, las tres ramas medidas por mí en HEAD:

| Configuración | rota | aviso caducada | aviso sin-constancia | línea derivada |
|---|---|---|---|---|
| con carrera, `estado_derivado: true` | no | **sí** | no | **sí** |
| sin carrera, `estado_derivado: true` | **sí** | no | no | no |
| sin carrera, `estado_derivado: false` | **no** | no | **sí** | no (canal apagado) |

**El descarte del cerrojo es correcto, y su argumento se sostiene.** `CA-18 (i)` incluye
expresamente *«una persona»*, y un cerrojo **sólo obliga a quien lo respeta**: el editor de una
persona no lo toma, luego habría dado garantía sobre las rotaciones y **ninguna sobre el caso que
se midió** — que es justamente el que se midió. Añadir un cerrojo huérfano habría metido un modo de
fallo peor (un documento que no vuelve a rotar nunca) y obligado a decidir cuándo está rancio, que
es adivinar. La relectura no obliga a nadie, no espera a nadie (`CA-18 (v)(b)`) y cuesta una
lectura del archivo que se está a punto de reescribir. **Acreditado.**

### (b) `(iv)` es de conformidad y no discrimina: **aceptable, con la condición dicha**

Corrí el caso 3 veces contra HEAD (8/8 PASS las tres, 24 ejecuciones, **cero flakiness**; `(iv)`
hace 3 vueltas internas, o sea 9 rondas concurrentes sin una anomalía). Y **medí la intermitencia
yo**, fuera del banco:

| Versión | dos bloques | filas duplicadas | `CA-05` roto | vueltas |
|---|---:|---:|---:|---:|
| `1fe975f` | 2 | 2 | 2 | 20 |
| `1fe975f` (corrida previa) | 3 | 3 | 3 | 15 |
| **HEAD** | **0** | **0** | **0** | **20** |

Frecuencia del defecto en el código viejo: **~10–20 %**, coherente con su «1 de cada 5 / 1 de cada
10». HEAD: **0 de 20** (más 0 de 9 en las corridas del banco).

**Juicio.** Un caso de conformidad sobre una rama `usuario/dinero` es **débil**, y hay que decirlo
con número: a una tasa del 10–20 % por ronda, tres rondas detectan una regresión con probabilidad
**27–49 %**. Es una moneda al aire. Lo acepto **por tres razones concretas, no por deferencia**:
1. el fail-before está **medido y registrado fuera del banco con su frecuencia**, que es el sitio
   correcto para una medición intermitente;
2. `SEC-030` ya prohíbe casos intermitentes en este banco (dispersión del instrumento ~3,7×), y un
   caso con 27–49 % de poder daría rojos que no son regresiones — el remedio sería peor;
3. el mecanismo que da `(iv)` **no es un camino de código independiente**: es el mismo testigo que
   los casos de `(vi)` y el de `(ii)` cubren **de forma determinista**.

La razón 3 tiene una excepción, y es exactamente el punto (d): la rama `elif [ -e "$destino" ]` —el
destino que no existía al leer y existe al publicar— es la que sostiene `(iv)` y **no tiene ningún
caso**. Conformidad débil **más** rama sin cubrir es lo que registro en `QA-026-09`.

### (c) El residuo de la ventana de publicar: satisface `(i)` **como límite declarado**, no literalmente

Leído literalmente, `CA-18 (i)` dice que una escritura posterior a la lectura y anterior a la
publicación **no se publica**; una escritura que caiga entre el testigo y el `mv` **sí** se pierde,
luego `(i)` no se cumple en ese subintervalo. **No lo cuento como hallazgo**, y el motivo no es
indulgencia:
- la ventana pasa de **295–312 ms** (medidos aquí; 355–361 ms el auditor) a **dos `mv` y un
  `printf`** — del orden de microsegundos: cuatro o cinco órdenes de magnitud;
- es **irreducible en shell**: no existe un «renombra-si-no-ha-cambiado» atómico en POSIX, y
  `CA-18 (v)` delega el mecanismo en el desarrollador;
- y **está declarado en el REQ**, en su propia sección («El mecanismo de `CA-18`, y sus dos residuos
  declarados»), no escondido en un comentario.

Lo que sí anoto: `(i)` está **redactado en absoluto** («ninguna publicación puede derivarse de una
lectura que ya no describe el disco») mientras la implementación tiene una excepción declarada en
otro sitio. Quien lea sólo `(i)` concluirá algo falso — la misma clase de defecto que `QA-026-04`.
La tolerancia pertenece **dentro** de `(i)`. Va en `QA-026-10`.

### (d) La rama del archivo de historia, implementada y sin caso: **ausencia justificada**

Verifiqué el argumento en el código, no en su palabra. El documento se lee en el **paso 0** y se
comprueba tras ~300 ms de troceado: ventana grande, caso de reloj fiable (24/24 en mis corridas).
El **destino** se lee en el **paso 6**, y su comprobación llega tras armar el bloque y una sonda
`grep`: la ventana es de microsegundos, **el mismo residuo de (c)**. Un caso de reloj ahí sería
intermitente de verdad, y «no envío casos intermitentes» es coherente con `SEC-030`.

La otra mitad —que la vigencia del destino **no se pueda comprobar** (NUL ⇒ no se publica)— **sí**
tiene caso determinista, y lo vi fallar contra `1fe975f` (`CA-18 (ii)`). Así que de las dos mitades,
la comprobable está cubierta y la de reloj está declarada. **Acepto la ausencia**, y la registro en
`QA-026-09` para que no se pudra en silencio: «implementada y sin probar» sobre una rama
`usuario/dinero` es el estado que hay que poder ver.

### El coste: **acredito la abstención**

Los hechos, y ninguno es cómodo para él: **encontró su propia regresión** en la única línea de su
cambio que no era camino de fallo —guardar el testigo al leer copiaba el documento entero por
archivo y por parada, también donde no se rotaba—, la midió (**140.583 µs contra 128.742 =
+11.841 µs, +9,2 %**, **por encima** del techo de 140.000 µs) y la corrigió **de diseño**,
reconstruyendo el testigo en el punto de publicación.

**Acredito la abstención posterior por dos razones:**
1. **La evidencia que la invalida es interna a la medición**: el brazo de control —el **mismo código
   viejo, sin cambios**— se movió un **34 %** entre corridas (93.110–141.277 µs). Cuando el control
   se mueve un 34 %, una señal de +9,2 % no es resoluble, y una comparación absoluta hoy
   **condenaría también al código que era conforme**. Abstenerse es lo que exige `CA-15 (ii)`
   (máquina en reposo), no una salida.
2. **La afirmación que de verdad importa no necesita reloj**: «cero fuera del camino de rotación»
   es una propiedad de **camino**, y la verifiqué leyendo el código — el paso 2 sale por la
   comparación de tamaño **antes** de trocear y mucho antes del paso 6b, así que en régimen
   estacionario el testigo no se ejecuta. Eso se comprueba sin cronómetro y se cumple.

Y **no re-derivar el techo es la decisión conservadora correcta**: re-derivarlo en una máquina que
no está en reposo sólo podía **subirlo**, que es la única dirección que `CA-15` no admite sin
medición válida. **No abro hallazgo de producto por falta de medición.** Sí dejo la deuda con dueño
y forzador en `QA-026-11`, porque «no medido» sin dueño es «olvidado».

### `QA-026-04` — **CERRADO** con mi evidencia (era `contrato` tras `R-021`)

Comprobé **el límite**, no sólo el texto:

| Comprobación | Resultado |
|---|---|
| `git diff --numstat 8248e58 43bfd47` | **1 línea** en `.arnes/config.json`, **1 línea** en la plantilla |
| `rotacion.activo` | sigue **`false`** |
| `rotacion.artefactos` | sigue **`[]`** (eso es `CA-13`, no autorizado) |
| `_doc_artefactos` en los dos archivos | **byte a byte idéntico** (2.813 caracteres, mismo hash) |
| plantilla con marcadores sustituidos | **JSON válido**, `activo: false`, `artefactos: []` |

Y el texto describe **las cinco ramas** que el código tiene hoy, verificado leyéndolo: sin la
sección declarada · sin entradas reconocibles · estructura ambigua **en toda la extensión** (con las
tres sub-formas) · documento o archivo de historia **no legibles enteros** · el documento **cambió
entre leerlo y publicarlo**. Más el acople de `(vii)` con `estado_derivado.activo: false`, el
puntero **fuera** de la tabla y la definición de entrada **por propiedad** y no por lista de
prefijos. **El defecto que abrió el hallazgo está cerrado en los dos archivos.**

### Hallazgos nuevos de la vuelta 2

#### QA-026-08 (`instrumento`) — ninguna puerta comprueba la plantilla del manifiesto

`jq -e . templates/arnes-config.json.tpl` **falla**, y falla igual en `8248e58`: la plantilla lleva
`{{ARNES_VERSION}}`, `{{CODIGO_APP_GLOBS}}` y `{{QUALITY_GATES_JSON}}` y **por diseño** no es JSON
válido. Lo acredité sustituyéndolos: el resultado **sí** es JSON válido.

**Sí falta un gate de `AGENTS.md` §7**, y el argumento es que la plantilla es **lo que heredan todos
los proyectos** por `arnes-init`/`arnes-upgrade`: un `.tpl` malformado rompe la instalación de cada
proyecto nuevo, y hoy **nada lo comprueba** — las cuatro puertas miran `.arnes/config.json`,
`hooks.json`, `plugin.json` y `marketplace.json`. La forma del gate es la que ya usé: sustituir los
marcadores por valores de relleno y pasar `jq -e .`. **Pre-existente, no lo introduce este commit.**
Dueño: **`desarrollador`**; la lista de gates vive en el manifiesto, luego el alta es **gate
humano**. **No bloquea.**

#### QA-026-09 (`instrumento`) — `(iv)`: conformidad débil sobre una rama `usuario/dinero`, y su rama de soporte sin caso

Dos mitades de la misma carencia, ambas aceptadas arriba con su motivo, ambas registradas para que
se vean: (1) el caso de `(iv)` detecta una regresión con **27–49 %** de probabilidad a la tasa que
medí (10–20 %); (2) la rama `elif [ -e "$destino" ]`, que es la que sostiene `(iv)`, **no tiene
ningún caso** y su ventana no admite uno determinista. Lo que cerraría la brecha sin casos
intermitentes es una prueba **sin reloj** —inyectando el cambio del destino entre la lectura y la
comprobación por un punto de control del propio código— y eso es diseño de banco, no un caso más.
Dueño: **`desarrollador`**. **No bloquea.**

#### QA-026-10 (`instrumento`) — dos write-backs a `CA-18` para el analista

1. **`(iv)` ya no está «modelado, no medido».** El criterio declara ese estado de evidencia y dice
   que el par de `(vi)` «lo convierte en medido o lo desmiente». Está **medido**: por el
   desarrollador y, **de forma independiente, por mí** — `1fe975f` 2/20 y 3/15 con dos bloques,
   filas duplicadas y `CA-05` roto; HEAD **0/20**. Dejar «no medido» dice «no sabemos» cuando sí se
   sabe, y la frecuencia es lo más valioso del dato.
2. **`(i)` necesita su tolerancia dentro.** Está redactado en absoluto y la implementación tiene un
   residuo de microsegundos declarado en otra sección; quien lea sólo `(i)` concluirá algo falso.

Dueño: **`analista-requerimientos`**. **No bloquea.**

#### QA-026-11 (`instrumento`) — el techo de `CA-15` queda sin verificar contra el código de hoy

El residual de coste tras la corrección está **declarado no medido** y el techo **no se re-derivó**;
acredito ambas decisiones arriba. Queda, dicho como deuda y no como número: **no hay medición válida
del coste del código actual contra el techo de 140.000 µs.** **Forzador que no depende de nadie:**
la próxima medición de `CA-15` que se haga en máquina en reposo —la que exija `CA-13` al declarar el
artefacto, o la primera que se corra en el runner— resuelve esto o lo desmiente. Dueño:
**`desarrollador`**. **No bloquea.**

### Resumen de clases — vuelta 2

| Id | Clase | Estado | Dueño |
|---|---|---|---|
| QA-026-01 | `usuario/dinero` | **CERRADO** (vuelta 1) | `desarrollador` |
| QA-026-02 | `usuario/dinero` | **CERRADO** (vuelta 1) | `desarrollador` |
| QA-026-03 | `instrumento` | Abierto — no bloquea | `desarrollador` |
| QA-026-04 | `contrato` (tras `R-021`) | **CERRADO** en `43bfd47` (2026-09-09) | `desarrollador` + propietario |
| QA-026-05 | `instrumento` | **CERRADO** (vuelta 1) | `analista-requerimientos` |
| QA-026-06 | `instrumento` | **CERRADO** (vuelta 1) | `analista-requerimientos` |
| QA-026-07 | `instrumento` | Abierto — no bloquea | `analista-requerimientos` |
| QA-026-08 | `instrumento` | Abierto — no bloquea | `desarrollador` + propietario |
| QA-026-09 | `instrumento` | Abierto — no bloquea | `desarrollador` |
| QA-026-10 | `instrumento` | Abierto — no bloquea | `analista-requerimientos` |
| QA-026-11 | `instrumento` | Abierto — no bloquea | `desarrollador` |

**`CA-18` queda acreditado** en sus siete partes salvo el residuo de `(i)`, que es un límite
declarado e irreducible en shell. **Ningún hallazgo abierto es `usuario/dinero` ni `contrato`**: el
único `contrato` de la ventana (`QA-026-04`) cierra aquí. `QA: aprobado` sobre el mecanismo.

### Lo que NO se midió en la vuelta 2

1. **Banco completo no re-corrido** (cifra de la coordinadora). Corrí **sólo la parte 4**: tres
   veces contra HEAD y una contra `1fe975f`.
2. **El residual de coste del testigo no se re-midió** — excluido por encargo, y la sonda del
   desarrollador no converge. **El techo de `CA-15` sigue sin verificar contra el código de hoy**
   (`QA-026-11`).
3. **El techo de `CA-15` en el runner de `hooks-en-linux`: sigue SIN MEDIR** (arrastrado).
4. **La rama `elif [ -e "$destino" ]` no se ejercitó**: su ventana es de microsegundos y no supe
   provocarla sin un caso intermitente (`QA-026-09`).
5. **El residuo de la ventana de publicar no se intentó explotar.** Habría exigido ganar una carrera
   de microsegundos; su existencia se acepta **por lectura del código**, no por medición.
6. **`CA-13`, `CA-14`, `CA-16`, `CA-17` no validados** — sin implementar por diseño, fuera de
   ventana.
7. **No validé nada de `REQ-023`, `REQ-024` ni `ADR-008`**, ni el resto de `1aa050a`: fuera del
   encargo y con un `analista` vivo en esos archivos.

---

## REQ-023 · commit `e406202` (rama `rel/registro-1.33.0` @ `802b47d`)

**Alcance:** los 12 criterios de `REQ-023`, la mitad (1) de la remediación de `SEC-047`
(severidad crítica, vencimiento el cierre de 1.34.0). Vuelta **1 de 3** del ciclo dev↔QA de
este REQ.

**Fecha:** 2026-09-09 · **Modelo:** Opus (política de autoalojamiento, `AGENTS.md` §5).

### Quality gates

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | PASA |
| `jq -e . hooks/hooks.json` | PASA |
| `jq -e . .claude-plugin/plugin.json` y `marketplace.json` | PASA |
| Banco completo | **no re-corrido**: la coordinadora aportó dos corridas sobre `e406202` (`960 PASS · 0 FAIL · 5 SKIP`, `rc=0`, total 965). No es evidencia mía y no la reclamo como tal. |

**Instalación estricta de dependencias / lockfile:** no aplica — el proyecto no usa gestor de
paquetes con lockfile (Bash + jq + awk).

### Qué reproduje yo, y con qué instrumento

Banco de pruebas propio, independiente del banco del proyecto: la línea base heredada se
materializó con `git archive 4f647c7 hooks` (verificado: ese árbol **no** contiene
`ARNES_CLAVES`), y cada caso se emite como un `Write` de `PreToolUse` real contra
`guard-completado.sh`, con el mismo proyecto efímero para las dos versiones. Todos los
números de abajo son míos.

#### 1. El dominio de `CA-02`, derivado midiendo (no leído del REQ)

Procedimiento del criterio: campo declarado en su valor **más restrictivo** contra campo
**ausente**; pertenece a la clase si el segundo **abre**. Medido contra la línea base:

| Campo | con el campo | sin el campo | en el dominio |
|---|---|---|---|
| `QA` | deny | allow | **sí** |
| `Seguridad` | deny | **deny** | no |
| `Sensible a seguridad` | deny | allow | **sí** |
| `Hallazgos abiertos` | deny | allow | **sí** |
| `Rigor` | deny | allow | **sí** |
| `Estado` | deny | allow | **sí** |

**Dominio = 5 de 6**, y el excluido es `Seguridad` (su ausencia sigue denegando en rigor
efectivo `critico`). Coincide con lo que el REQ anota en su Historial, y la quinta clave **es**
`Estado` — la que el puntero medido falso de `CA-01` dejaba fuera. La anti-vacuidad tiene caso
propio en el banco (`39-…-1-la-puerta.sh:212/214` FALLA con dominio vacío, y `:210` FALLA si el
lector declara una clave a la que la sección no da valor restrictivo): un dominio colapsado
**no puede dar PASS**.

#### 2. Fail-before / pass-after y los cuatro controles

| Caso | base (`4f647c7`) | `e406202` | esperado |
|---|---|---|---|
| `SEC-047` fila 1 — BOM + `Sensible a seguridad: sí`, `Rigor: ligero` | allow | **deny** | allow→deny ✔ |
| `SEC-047` fila 2 — U+200B + `Hallazgos abiertos:` con hallazgo `contrato` | allow | **deny** | allow→deny ✔ |
| invisible sobre `QA` | allow | deny | ✔ |
| invisible sobre `Sensible a seguridad` | allow | deny | ✔ |
| invisible sobre `Hallazgos abiertos` (`0xc3` suelto) | allow | deny | ✔ |
| invisible sobre `Rigor` | allow | deny | ✔ |
| invisible sobre `Estado` | allow | deny | ✔ |
| todo verde + BOM (deniega por medibilidad, no por veredicto) | allow | deny | ✔ |
| **CTRL** misma cabecera **sin** el carácter | deny | deny | **motivo idéntico** ✔ |
| **CTRL** todo verde sin carácter | allow | allow | ✔ |
| **CTRL** `CA-11` comentado **sin** espacio (`<!--Rigor: ligero-->`) | allow | allow | ✔ |
| **CTRL** `Módulo:` / `Versión destino:` no ASCII | allow | allow | ✔ |

Los 12 reproducen. El motivo del control «sin el carácter» es **byte a byte el mismo** en las
dos versiones (`md5` idéntico), así que el `deny` de los ocho primeros lo causa la guarda y no
otra rama.

#### 3. El motivo es imprimible y diagnosticable

`«\xef\xbb\xbfSensible a seguridad:»` dentro del motivo, con la clave limpia nombrada aparte y
el puntero a `tools/arnes-lectura.sh`. `CA-01` cumplido en este eje.

#### 4. `LC_ALL=C` es `local` de verdad

Tras ejecutar la guarda: `LC_ALL` **no existe** como variable en el ámbito global
(`declare -p` falla), y `${#s}` sobre `"áé"` sigue devolviendo **2** (no 4). No hay fuga. Y
extremo a extremo el veredicto **y** el `md5` del motivo son idénticos bajo `LC_ALL=C` y
`LC_ALL=C.UTF-8`.

#### 5. `CA-04` — 0 divergencias, medidas por mí

Corpus propio: cabeceras de `requirements/*.md` + toda línea con `:` del glob de secciones del
banco = **3 687 líneas con campo**. Los dos lectores (base y `e406202`) emiten clave, valor y
`ARNES_CLAVE_DECORADA` **idénticos**: `diff -q` vacío. **0 divergencias.**
Suelos de anti-vacuidad sobre las cabeceras **reales** (`requirements/*.md` + plantillas, 382
líneas con campo): clave no ASCII **89**, valor no ASCII **125**, clave decorada **59** — los
tres muy por encima del suelo de 1. Y **0 falsos positivos**: la guarda no dispara ni una vez
sobre las 382 cabeceras reales del árbol. `tools/arnes-lectura.sh` sigue saliendo `rc=0` sobre
el árbol real.

#### 6. `CA-07` — el informe no calla, con fail-before

Sobre un REQ con BOM en `Estado:` (el peor caso, el que caía en el descarte «archivo sin
Estado»): la base responde **«Ningún valor anómalo … los 1 REQ»** con **`rc=0`**; `e406202`
responde `VALORES QUE LA MÁQUINA NO LEE COMO ESTÁN ESCRITOS (1)`, con
`escrito: «\xef\xbb\xbfEstado:»`, `se lee: <cabecera no medible>`, la consecuencia («la puerta
de cierre DENIEGA») y **`rc=1`**.

#### 7. `CA-09` — coste

- **(i) 0 procesos añadidos:** verificado por lectura de las cuatro funciones nuevas — ni una
  sustitución de comandos, ni un externo: sólo expansión de parámetros, `case`, `printf -v` y
  llamadas a funciones bash.
- **(ii) ruta crítica:** medido por mí con `tests/util/sonda-reloj.sh` sobre el **camino de
  lectura de cabecera entero** (`arnes_campos_req` + `arnes_estado_cabecera` sobre el texto de
  `requirements/REQ-023.md`, k=100, r=6, mínimo de k): base **1 717 592 µs** contra
  **1 915 874 µs** → **1,116×**. Es un sujeto **más estrecho** que la ruta crítica del banco
  (donde el desarrollador midió 1,002×), y aun así queda **por debajo** del techo de 1,25×.
- **(iii) cociente de duplicación:** ver `QA-023-05`. **No concluyente.**

#### 8. `CA-12` — la cola no se movió

`arnes_cola_pendientes` y `arnes_sin_cita` **no aparecen en el diff** (0 líneas tocadas), y
sobre cuatro corpus de cola (entrada normal · `### Real <!-- nota -->` + `### Migrar A --> B` ·
rango sin cerrar · cola vacía) el conteo y el `rc` son **idénticos**: `1/0 · 0/0 · 0/0 · 0/0`.
(i) y (ii) cumplidos. (iii) **no** — ver `QA-023-03`.

### Veredicto por criterio

| Criterio | Veredicto | Base |
|---|---|---|
| `CA-01` | **FALLA** | `QA-023-01`: la clase sigue abierta para toda entrada que pertenezca al alfabeto derivado —señaladamente `U+0020`— y por sustitución del espacio interno (NBSP, TAB). El resto del criterio (motivo imprimible, nunca por ausencia, cita de la línea) **pasa**. |
| `CA-02` | **PASA en lo que mide, FALLA por arrastre** | Dominio 5/6 derivado midiendo (verificado), anti-vacuidad con caso propio, deniega con todo en verde. Pero la cláusula «cualquier campo del dominio … entonces la puerta deniega» falla para la subclase de `QA-023-01`. |
| `CA-03` | **FALLA** | `QA-023-01` (una entrada del universo declarado, con el procedimiento declarado, **no** deniega: el propio criterio dice que entonces «el hallazgo es contra el código») + `QA-023-02` (el sorteo no ejerce el universo que el criterio declara). Las tres familias declaradas **sí** pasan, y la anti-vacuidad de `CA-03` tiene sus dos casos. |
| `CA-04` | **PASA** | 0 divergencias sobre 3 687 líneas con campo; tres suelos cumplidos con holgura; 0 falsos positivos sobre las 382 cabeceras reales. |
| `CA-05` | **PASA** (con límite de entorno) | Veredicto y motivo idénticos bajo `C` y `C.UTF-8`; `LC_ALL=C` es `local` y no fuga. Límite: en esta máquina sólo existen `C` y `C.utf8` — ver «lo que quedó sin medir». |
| `CA-06` | **PASA** | El conjunto de claves baja de **2** transcripciones (dos despachos disjuntos) a **1** (`ARNES_CLAVES`); el alfabeto se **deriva** con escape de `]`, `-`, `^`; el despacho **ya no decide** quién es campo (`arnes_en_vocab "$ARNES_CLAVE" "$ARNES_CLAVES" \|\| continue` **antes** del `case`); la guarda se publica en **una** boca (`arnes_campo_linea`) y no en `arnes_norm_clave`; `arnes_estado_cabecera` deriva su clave de la constante. El fail-closed de `Archivos:` con un invisible se verificó: `colisiona (sin declarar)`. |
| `CA-07` | **PASA** | Fail-before/pass-after reproducido, con el caso peor (el invisible sobre `Estado:`). |
| `CA-08` | **NO ACREDITADO — y no es hallazgo** | Los casos (i), (ii) y (iii) existen y pasan, pero el banco **no materializa `hooks/` desde un tag**. El propio criterio declara la precondición (`SEC-048`, gate humano abierto) y dice que hasta entonces no se puede medir; el Historial lo declara así. Confirmado: el REQ **lo dice**, no lo finge. |
| `CA-09 (i)` | **PASA** | 0 procesos, por lectura. |
| `CA-09 (ii)` | **PASA por MEDICIÓN, no por la cota** | 1,116× medido por mí sobre un sujeto más estrecho que la ruta crítica; 1,002× el desarrollador sobre la ruta crítica. Techo 1,25×. La cota algebraica **ya no es una demostración** — ver abajo. |
| `CA-09 (iii)` | **NO CONCLUYENTE** | `QA-023-05`. |
| `CA-10` | **PENDIENTE, declarado como tal** | Falta la fila de `AGENTS.md` §13 / `templates/AGENTS.md.tpl` y el apartado de `skills/arnes-upgrade/SKILL.md`. El Historial lo declara **no tocado** por precondición ajena (otra comisión tiene esos archivos). Verificado: está declarado pendiente, no hecho. No lo enruto yo. |
| `CA-11` | **PASA** | `<!--Rigor: ligero-->` y `<!--Estado: completado -->` (sin espacio) deciden **igual** en las dos versiones: la guarda **no** dispara. La elección del sitio de publicación es lo que lo consigue. |
| `CA-12 (i)` | **PASA** | 0 transcripciones nuevas de la noción de cita: `arnes_sin_cita` no se toca. |
| `CA-12 (ii)` | **PASA** | Conteo y `rc` idénticos sobre cuatro corpus; por construcción, `arnes_cola_pendientes` no llama al lector de cabecera. |
| `CA-12 (iii)` | **FALLA** | `QA-023-03`: hay una superficie que comparte la propiedad, que este REQ no corrige, y que **no** está nombrada en «Fuera de alcance». |

### ¿Sigue en pie la cota algebraica de `CA-09 (ii)`?

**No como demostración; sí como conclusión, porque la sustituye el número.**

La cota exige tres premisas. La segunda (medir `r` a longitudes de cabecera real) se cumple. Las
otras dos **no**: la guarda **no queda contenida** en la función medida —la publicación añade una
comparación de enteros por línea en `arnes_campo_linea` y la pertenencia de `CA-06` una llamada
a `arnes_en_vocab` por línea en `arnes_campos_req`— y **sí añade llamadas** al camino (dos, las
dos builtins). Con coste fuera de `T_f`, la desigualdad `razón_ruta = (T_f·r + T_resto)/(T_f +
T_resto) ≤ r` deja de valer: el término `ΔT` de fuera no está acotado por `r`.

Lo que la sostiene es la medición, y con margen que no depende de la álgebra: **1,116×** en el
sujeto más estrecho que pude construir (el camino de cabecera entero, aislado del arranque del
proceso y de `jq`) contra un techo de **1,25×**. El desarrollador declaró esta rotura en el
Historial en vez de taparla, con la premisa nombrada. **No es hallazgo**: el criterio exige una
medición y la medición cumple; la cota era sólo el argumento de factibilidad. Lo que sí queda
dicho es que **la próxima comisión no puede reutilizar la cota** — está rota, no debilitada.

### ¿Hay una cuarta derivación?

**No.** Inventario completo de quien lee el conjunto de claves del **texto** de `hooks/lib.sh`,
buscado por `sed`/`awk`/`grep` en todo el árbol (`tests/`, `tools/`, `hooks/`, `skills/`,
`templates/`, `.github/`): son **tres**, las tres ya nombradas por el desarrollador —
`tools/arnes-lectura.sh:69-72` (re-apuntada a la constante),
`36-cabecera-comentario-html-2-los-lectores.sh:228` y
`36-cabecera-comentario-html-3-comentar-retira.sh:33`. La cuarta referencia que existe
(`36-…-4-el-informe-y-los-textos.sh:78`) extrae el **cuerpo de `arnes_sin_cita`**, no las
claves, y no se ve afectada. Pero **una de las tres se estrechó**: ver `QA-023-04`.

### Hallazgos

#### `QA-023-01` · clase `usuario/dinero` · severidad **crítica**

**El alfabeto de las claves contiene el espacio, así que la guarda falla en abierto para toda
entrada que pertenezca a ese alfabeto — y `SEC-047` fila 1 y fila 2 siguen cerrando un REQ que
nadie validó y nadie auditó.**

Raíz. `ARNES_CLAVES_ALFA` se **deriva** de las seis claves, y dos de ellas son multi-palabra
(`Sensible a seguridad`, `Hallazgos abiertos`), así que el alfabeto incluye `0x20`:

```
ALFA = [QASeguridansbl HzotRE]
hex  =  51 41 53 65 67 75 72 69 64 61 6e 73 62 6c 20 48 7a 6f 74 52 45
                                                  ^^ el espacio
```

La guarda limpia la clave de todo lo **ajeno** al alfabeto y pregunta si lo que queda **es** una
clave. Si lo insertado **pertenece** al alfabeto, la limpieza no lo retira y la reconstrucción
falla; y si lo insertado **sustituye** al espacio interno, la limpieza lo borra y **no repone el
espacio**, con el mismo resultado. En los dos casos `arnes_en_vocab` dice «no» y la guarda
**calla**: la línea se resuelve como **ausencia** del campo, que es justo lo que la puerta
perdona.

Pasos de reproducción (proyecto efímero con `estados.completado = completado`, `quality_gates:
["true"]`, cola vacía; en disco `Estado: en-revisión`; se emite un `Write` de `PreToolUse`
contra `hooks/guard-completado.sh`):

| Documento entrante (cabecera) | esperado | base `4f647c7` | `e406202` |
|---|---|---|---|
| `Estado: completado` · `Sensible a` **`·· `** `seguridad: sí` · `Rigor: ligero` · `QA: pendiente` · `Seguridad: pendiente` (dos espacios: **inserción de `U+0020`**) | **deny** | allow | **allow** ✗ |
| `Estado: completado` · `Rigor: ligero` · `Hallazgos`**`·· `**`abiertos: SEC-9 (contrato)` | **deny** | allow | **allow** ✗ |
| `Est`**`·`**`ado: completado` · `Rigor: estandar` · `QA: pendiente` | **deny** | allow | **allow** ✗ |
| `Sensible a`**`<U+00A0>`**`seguridad: sí` + `Rigor: ligero` + los dos veredictos `pendiente` (NBSP **en vez del** espacio) | **deny** | allow | **allow** ✗ |
| `Sensible a`**`<TAB>`**`seguridad: sí`, ídem | **deny** | allow | **allow** ✗ |
| `Hallazgos`**`<U+00A0>`**`abiertos: SEC-9 (contrato)`, `Rigor: ligero` | **deny** | allow | **allow** ✗ |
| **CONTROL** mismo documento con `U+FEFF` delante de `Sensible a seguridad` | deny | allow | **deny** ✔ |
| **CONTROL** mismo documento con `Ω` insertado (del pool reservado de `CA-03`) | deny | allow | **deny** ✔ |

Resultado esperado: `DENY` por medibilidad. Observado: **`ALLOW`** — un REQ `Rigor: ligero`
sobre `Sensible a seguridad` (que impone el suelo `critico`) cierra a `completado` con
`QA: pendiente` **y** `Seguridad: pendiente`. Es **literalmente la fila 1 medida de `SEC-047`**,
con un carácter distinto, y la fila 2 con el hallazgo `contrato` que debía bloquear.

Por qué es de esta clase y no `instrumento`: el defecto **no está en el control ni en la
prueba, está en el producto** — el mecanismo que `arnes-upgrade` lleva a todos los proyectos
consumidores. Su efecto es que alguien decide distinto: un proyecto lee `completado` en un REQ
`critico` que nadie validó. La tabla de `requirements/README.md` reserva `instrumento` para
defectos «sin efecto en el producto», y aquí el producto **es** la guarda.

Y por qué no lo tapa el «fuera de alcance» del homóglifo: ese apartado se justifica con una
premisa que aquí es **medida falsa** — «la clave resultante no contiene nada ajeno al alfabeto».
Con NBSP o TAB **sí** contiene algo ajeno, la guarda lo ve, entra en la sustitución y aun así
abre; y el caso del **espacio insertado** no es sustitución en absoluto: es exactamente el
**procedimiento de inyección que `CA-03` fija**, con una entrada del **universo que `CA-03`
declara**. Además, un doble espacio y un NBSP **no se ven en el diff**, que es el rasgo por el
que la clase existe.

Dueño: `desarrollador`. **Bloquea el cierre.**

#### `QA-023-02` · clase `contrato` · severidad **alta**

**El sorteo de la entrada RESERVADA de `CA-03` no ejerce el universo que el criterio declara, y
deniega por construcción.**

`CA-03` declara: «**Universo:** **cualquier** punto de código, sin restricción previa». Lo
construido (`39-caracter-invisible-2-la-clase-y-el-corpus.sh:122-131`) sortea de
`RES93_POOL`, **doce** puntos de código imprimibles escogidos a mano
(`Ω Ж 漢 😀 U+FE0F U+E0061 U+2028 U+2029 ☃ 𝔄 ᅟ ·`), y su propio comentario lo dice: «se
sortea entre los imprimibles de tres planos». Los doce están **fuera** del alfabeto derivado,
así que las ocho tiradas **deniegan por construcción**: el sorteo no aporta poder de falsación
sobre ocho tiradas ni sobre ochenta.

Que esto no es un detalle está **medido**: si el pool contuviera `U+0020` —que pertenece al
universo declarado— el caso habría **fallado**, y es exactamente lo que `QA-023-01` demuestra.
El pool no es la misma tabla que usa la guarda, pero está **seleccionado para no intersecarla**,
que produce la misma tautología un paso más allá — la forma **(d)** que `CA-03` existe para
impedir.

Y hay una tensión que el criterio no resuelve y que el write-back tiene que decidir: bajo el
universo literal («cualquier punto de código») más la exigencia «DENY en **todas**», el criterio
**no es satisfacible** — insertar `a` en `QA` da `QaA`, que ninguna persona lee como `QA:` y que
correctamente no deniega. La salida no es estrechar el pool en silencio.

Resultado esperado: el caso publica un universo que ejerce, o el criterio declara la restricción
con su motivo. Observado: el criterio declara un universo y el caso ejerce otro.
Dueño: `analista-requerimientos` (write-back del criterio) + `desarrollador` (el caso).
**Bloquea hasta el write-back.**

#### `QA-023-03` · clase `contrato` · severidad **media**

**`CA-12 (iii)` incumplido: existe una superficie que comparte la propiedad, que este REQ no
corrige, y que no está nombrada en «Fuera de alcance».**

Un carácter invisible delante del `## ` que **termina** la cabecera impide que el escáner
reconozca el fin de la cabecera, así que una línea del **cuerpo** pasa a leerse como veredicto de
cabecera. Reproducido con control, en las dos versiones:

| Documento | base | `e406202` |
|---|---|---|
| cabecera con `QA: pendiente`, `U+FEFF` delante de `## Historia`, y `QA: aprobado` en el cuerpo | allow | **allow** |
| lo mismo **sin** línea de veredicto en el cuerpo | deny | deny |
| lo mismo **sin** el `U+FEFF` (con la línea en el cuerpo) | deny | deny |

Los dos controles aíslan el mecanismo: lo que abre es el invisible **más** la línea del cuerpo.
Es la misma propiedad que `CA-01` —una regla de descarte del lector de cabecera que un carácter
que nadie ve desplaza, y que se resuelve del lado que **abre**— y derriba de paso la invariante
de `AGENTS.md` §13 «los campos valen sólo en la cabecera». **No es una regresión de este commit**
(la base decide igual) y **no** se exige que este REQ la cierre: lo que `CA-12 (iii)` exige es que
quede **nombrada** con dueño, ventana y forzador, junto al homóglifo y al NUL/UTF-16. No está.

Dueño: `analista-requerimientos` (nombrarla en «Fuera de alcance»; el arreglo, a quien decida su
ventana). **Bloquea hasta el write-back.**

#### `QA-023-04` · clase `instrumento` · severidad **media**

**Una de las tres derivaciones del conjunto de claves se estrechó de 6 a 5 en silencio, y perdió
justo `Estado`.**

`36-cabecera-comentario-html-2-los-lectores.sh:228` deriva las claves con dos patrones `sed`
sobre el **texto** de `hooks/lib.sh`; el primero casa `case "$ARNES_CLAVE" in 'X')` con la clave
**entre comillas simples**. Los dos brazos de `arnes_estado_cabecera` pasaron a
`case "$ARNES_CLAVE" in "$ARNES_CLAVE_ESTADO")`, así que el patrón ya no casa. Medido con el
mismo `sed` sobre los dos árboles:

```
base 4f647c7 : n=6  [Estado|Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad]
e406202      : n=5  [Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad]
```

Ninguna prueba falló, porque la sección sólo comprueba `[ -n "$CLAVES36" ]`: la anti-vacuidad
distingue «vacío» de «no vacío», no «completo» de «incompleto». El resultado es un guardián que
**envejeció hacia el lado que abre** —la clase de `SEC-050`— dentro del banco, y sobre la clave
que decide el cierre. `tools/arnes-lectura.sh` sí se re-apuntó a la constante y deriva **6**.

Resultado esperado: 6 claves, o un FAIL. Observado: 5 y verde.
Dueño: `desarrollador`. **No bloquea** (deuda con dueño; el forzador natural es `REQ-024`, que va
a unificar esos brazos y se encontrará el mismo `sed`).

#### `QA-023-05` · clase `instrumento` · severidad **media**

**`CA-09 (iii)` no es comparable: el cociente de duplicación depende de la FORMA de la línea, no
sólo del par de longitudes que el criterio obliga a publicar. Medido, el techo de 2,2 se supera —
y se supera igual en la versión heredada.**

`tests/util/sonda-reloj.sh`, sujetos **intercalados en la misma invocación**
(`--sujeto-a`/`--sujeto-b`), k=200, r=6, mínimo de k, sujeto `arnes_norm_clave`, par de
longitudes de línea **1 015 → 2 015**:

| Forma de la línea | base `4f647c7` | `e406202` | techo |
|---|---|---|---|
| se dobla la **CLAVE** (`Sensible a <a…> seguridad: sí`) | **2,412** | **2,364** | 2,2 |
| se dobla el **VALOR** (`Rigor: <a…>`) | 1,845 | 1,826 | 2,2 |

Dos lecturas del mismo criterio, con el **mismo** par publicado, dan 2,36 y 1,83. El criterio ya
aprendió una vez que «2,2 sin decir entre qué dos longitudes no es comparable con nada»; le falta
el segundo parámetro: **dónde** se dobla. Con la forma de clave larga el techo **no se cumple**.

Por qué es `instrumento` y no `contrato` ni un hallazgo contra el código: el producto **no se
degrada** — en la misma forma, la versión publicada es marginalmente **más barata** que la
heredada (2,364 contra 2,412), así que la guarda no aporta orden de crecimiento; el exceso es
**heredado** y el régimen realista (claves de ≤ 20 bytes, donde la guarda **retorna temprano** sin
ninguna sustitución) queda holgadamente por debajo. El defecto está en el criterio y en el caso
que publica una sola forma como si zanjara el techo. **No bloquea.**

Dueño: `desarrollador` (el caso y la forma medida) + `analista-requerimientos` (el parámetro que
falta en el criterio).

### Resumen de clases

| Hallazgo | Clase | Bloquea |
|---|---|---|
| `QA-023-01` | `usuario/dinero` | **sí** |
| `QA-023-02` | `contrato` | **sí** |
| `QA-023-03` | `contrato` | **sí** |
| `QA-023-04` | `instrumento` | no |
| `QA-023-05` | `instrumento` | no |

**Forma del criterio mal formado** (se anota aquí y **sólo** aquí, nunca dentro del paréntesis de
la clase en `Hallazgos abiertos:`):

- `QA-023-02` → forma **enumeración**: `CA-03` declara un universo «sin restricción previa» y lo
  construido enumera doce entradas escogidas a mano, sin la marca de conjunto no exhaustivo ni
  puntero al sitio único.
- `QA-023-05` → forma **número**: `CA-09 (iii)` fija 2,2 declarándolo **operativo** pero sin el
  parámetro que determina su valor, así que dos mediciones conformes se contradicen.
- `QA-023-03` → forma **enumeración**: la lista de «Fuera de alcance» se presenta como el
  inventario de la superficie que comparte la propiedad, y le falta una entrada medida.

### Lo que NO se midió, dicho expresamente

1. **No re-corrí el banco.** Las cifras `960/0/5` son de la coordinadora, sobre `e406202`. No son
   evidencia mía. En particular **no** verifiqué el caso no determinista `REQ-017 CA-09`.
2. **`CA-08` en su letra queda sin medir por precondición ajena** (`SEC-048`, gate humano
   abierto): no existe fail-before contra `hooks/` materializados **desde un tag**. Mi
   fail-before usa `git archive 4f647c7`, que es el commit anterior de esta rama y **no** un tag
   firmado: acredita el delta, no la inmutabilidad del árbol de referencia.
3. **`CA-05` sólo se ejerció sobre dos locales**, `C` y `C.utf8`: son los únicos instalados en esta
   máquina (`locale -a`). El eje que el criterio nombra —byte contra carácter— **sí** queda
   ejercido; un locale con **colación** distinta (`es_ES.UTF-8`) **no**, y `bash` degradó a `C` con
   un aviso en vez de fallar. Nada de esto se midió en Windows/MSYS, que es de donde sale el BOM.
4. **`CA-06` en su eje de los cuatro lectores:** comparé `hooks/lib.sh` base contra publicado
   (3 687 líneas, 0 divergencias) y verifiqué el fail-closed de `Archivos:` en
   `tools/arnes-paralelo.sh`, pero **no** comparé `hooks/campos-req.awk` contra `hooks/lib.sh` por
   mi cuenta: eso lo mide el banco (`39-…-3:157`) y lo doy por su cuenta, no por la mía.
5. **`CA-09 (i)` no se midió con la sonda de procesos**, se acredita por **lectura** de las cuatro
   funciones nuevas.
6. **`CA-10` no se validó**: sin implementar por precondición ajena, y sus archivos están en manos
   de otra comisión.
7. **No amplié a otros REQ.** Lo que vi fuera del encargo va a `docs/PENDIENTES.md` en una línea.
8. **Rendimiento como NFR de usuarios concurrentes: no aplica** — el arnés no tiene usuarios
   concurrentes; los umbrales de este proyecto son de reloj y de procesos, y están en `CA-09`.

---

## REQ-023 · re-validación vuelta 1 de 3 · **árbol de trabajo** sobre `5305de9` (rama `rel/registro-1.33.0`)

**Alcance:** los cinco hallazgos abiertos de la vuelta anterior (`e406202`), la **acreditación
fail-before / pass-after** de los casos nuevos y reescritos de la sección 39, y el **cuadre
independiente** de la partición de esa sección en cinco partes.

**Fecha:** 2026-09-09 · **Modelo:** Opus (política de autoalojamiento, `AGENTS.md` §5).

> **El árbol validado está SUCIO a propósito.** Lo que se valida es el **árbol de trabajo**, no un
> commit: `5305de9` + la partición de la sección 39 sin comitear (`39-…-2-la-clase.sh` renombrada,
> `39-…-5-el-corpus.sh` nueva, `run.sh`, `README.md` del banco y `docs/qa/1.34.0-req023-particion-39.md`).
> Toda cifra de abajo es **mía**, medida en esta máquina (WSL2, sin `cygpath`), y ninguna se
> hereda del informe del `desarrollador` ni de la coordinadora.

### Cifras de la corrida (medidas por QA)

| Puerta | Resultado |
|---|---|
| `for f in hooks/*.sh tools/*.sh; do bash -n "$f"; done` | **rc 0** |
| `jq -e . hooks/hooks.json` | **rc 0** |
| `jq -e . .claude-plugin/{plugin,marketplace}.json` | **rc 0** |
| `bash -n` sobre `secciones/*.sh` | **rc 0** |
| Banco completo, corrida 1 | **962 PASS · 0 FAIL · 4 SKIP · rc 0** (1 min 5 s) |
| Banco completo, corrida 2 | **962 PASS · 0 FAIL · 4 SKIP · rc 0** (42 s) |
| `autoprueba-corredor.sh` | **106 PASS · 0 FAIL · rc 0** |
| `tools/arnes-lectura.sh .` sobre el árbol real | **rc 0**, 27 REQ, ninguna anomalía falsa |
| Cola de `PENDING_APPROVAL.md` | 0 pendientes |

**Instalación estricta de dependencias / lockfile:** no aplica — Bash + `jq` 1.8.2 + GNU awk 5.3.2,
sin gestor de paquetes.

**Flakiness: no observada en esta ventana.** Dos corridas, **la LISTA de SKIP idéntica** (no el
total: la lista, que es lo que discrimina) — `cygpath`, `REQ-017 CA-05 (i)`, `REQ-017 CA-05 (ii)`,
`REQ-021 CA-08 (iii)`. `REQ-017 CA-08 (ii)` **no** skipeó en ninguna de mis dos corridas. El
inventario normalizado de las dos corridas es **idéntico**; lo único que cambia dentro de un SKIP es
un número medido, que el oráculo normaliza.

### 1. Fail-before / pass-after — acreditado, caso por caso

Instrumento: el mecanismo que el propio banco declara (`ARNES_HOOKS_DIR`), con `ARNES_SEM_39`
fijando la semilla para que las dos corridas sorteen lo mismo. Árboles: `git worktree add --detach`
(nunca se movió el árbol de trabajo) y `git archive v1.33.0 hooks`.

#### (a) Los dos casos NUEVOS de `CA-03` (sorteo estratificado), contra `29b06eb^` = `95764db`

`ARNES_SEM_39=508269595`, sección 39 completa, mismos archivos de sección en las dos corridas:

| Caso | contra `95764db` (fail-before) | contra el árbol (pass-after) |
|---|---|---|
| `CA-03 rama 1: entrada AJENA al alfabeto (E1) por P1 o P2 DENIEGA` | **FAIL** — permitió `<P2:f0b4ae98>` | **PASS** (2 tiradas) |
| `CA-03 rama 2: el BLANCO duplicado (P3) o insertado (E2) DENIEGA` | **FAIL** — permitieron `<P1:20> <P3:20> <P3:20> <P3:20> <P3:20>` | **PASS** (5 tiradas) |
| resto de la sección 39 (40 casos) | PASS | PASS |
| **total** | **40 PASS · 2 FAIL · rc 1** | **42 PASS · 0 FAIL · rc 0** |

Los dos casos **discriminan**, y discriminan por el mecanismo correcto: la rama 1 cae por un punto
de código de cuatro bytes que **sustituye** al blanco interno (`P2`) y la rama 2 por el blanco
**insertado** (`P1:20`) y **duplicado** (`P3:20`) — que son exactamente las dos formas de
`QA-023-01`. Ningún caso pasa en las dos versiones.

#### (b) Los cuatro casos marcados «(era ALLOW)» de `CA-08`, contra el TAG `v1.33.0`

`ARNES_HOOKS_DIR` = `git archive v1.33.0 hooks` (verificado: ese árbol **no** define `ARNES_CLAVES`).
Parte 1 de la sección 39:

| Caso | contra el tag | contra el árbol |
|---|---|---|
| `CA-08` fila 1 — BOM + `Sensible a seguridad: sí` + `Rigor: ligero` **(era ALLOW)** | **FAIL** | PASS |
| `CA-08` fila 2 — U+200B + `Hallazgos abiertos:` con hallazgo `contrato` **(era ALLOW)** | **FAIL** | PASS |
| `CA-01` el carácter sobre la clave del **ESTADO** **(era ALLOW)** | **FAIL** | PASS |
| `boca Edit`: el BOM vive en DISCO **(era ALLOW)** | **FAIL** | PASS |
| `CA-01` el motivo nombra el byte en hexadecimal | **FAIL** | PASS |
| `CA-02` todo en verde + BOM en `QA:` deniega igual | **FAIL** | PASS |
| `CA-02` el dominio derivado (base: **0 campos, 0 abren** → FAIL por anti-vacuidad) | **FAIL** ×2 | PASS |
| `boca fragmento` | **FAIL** | PASS |
| `CA-05` invariancia al locale | **FAIL** | PASS |
| **CTRL** `CA-08 (ii)` misma cabecera **sin** el carácter (×2) y `(iii)` control positivo | **PASS** | PASS |
| **CTRL** las cuatro de `CA-11` (la frontera con lo que una regla contratada sí retira) | **PASS** | PASS |
| **CTRL** `CA-04` `Módulo:`/`Versión destino:` y la clave decorada | **PASS** | PASS |
| **total parte 1** | **11 PASS · 10 FAIL · rc 1** | **21 PASS · 0 FAIL** |

**«Ningún caso marcado “(era ALLOW)” pasa contra la línea base»: verificado, los cuatro fallan.** Y
los controles que deben decidir **igual** en las dos versiones —los tres de `CA-08 (ii)/(iii)`, los
cuatro de `CA-11`, los dos de `CA-04`— pasan en las dos: el rojo lo produce la guarda y no otra rama.

**Novedad respecto de la vuelta anterior:** el banco **ya materializa la línea base desde un TAG**
(`mat9x v1.33.0` en las partes 2, 3, 4 y 5), con verificación de contenido por `hash-object` contra
los OID de `ls-tree`. La vuelta 1 lo daba por no medible. La reserva que queda es la que `SEC-048`
nombra —un tag del propio repositorio no es una referencia inmutable acreditada— y **no** es de este
REQ.

#### (c) La anti-tautología de `CA-03`, PROVOCADA y no leída

`CA-03` exige que el caso **aborte con SKIP y su motivo —nunca PASS—** si una rama de DENY se queda
sin tirada. Provocado sobre una **copia** de la sección (`ARNES_SECCIONES_DIR`), sustituyendo la
referencia de la línea base por una inexistente:

```
tiradas por rama: 1(E1→DENY)=0 · 2(blanco→DENY)=0 · 3(…)=0 · descartadas por no abrir en la heredada=8
SKIP  REQ-023 CA-03 rama 1: … esa rama se quedó sin ninguna tirada que abriera en la heredada …
```

**La cláusula existe y no da verde por vacío.** Pero al ejercerla salió `QA-023-06` (abajo).

Y provocado el otro extremo —sección apuntada a los hooks de `v1.33.0`, que **no** definen
`ARNES_CLAVES`—, la anti-vacuidad de los estratos **falla en vez de pasar**:
`FAIL … los estratos no se derivaron de la constante (claves=0 alfabeto=0)`. **No es vacua.**

### 2. La partición de la sección 39 — cuadre verificado de forma independiente

Método (reproducible): `git worktree add --detach <tmp> 5305de9` (cuatro partes, **mismos** hooks
que el árbol) → `run.sh` en cada árbol → `inventario.sh` de las dos salidas → `diff`.

| Magnitud | Antes (4 partes) | Después (5 partes) |
|---|---:|---:|
| Líneas del inventario (= casos ejecutados) | **966** | **966** |
| `CASOS_ESPERADOS` de `run.sh` | 966 | 966 |
| Casos de `REQ-023` en la corrida | **42** | **42** |
| Reparto de la parte partida | **10** | **7 + 3** |
| `Resultado` | 962 PASS · 0 FAIL · 4 SKIP | 962 PASS · 0 FAIL · 4 SKIP |

**Diff crudo del inventario: 4 líneas, todas volátiles y todas explicadas** — las dos del sorteo
(sólo la semilla y el reparto de tiradas: `565916959` → `508269595`, `3/4` → `2/5`) y las dos de
`CA-04` (sólo los conteos del corpus, que crece con el preámbulo duplicado: `5184` → `5207`,
`1866` → `1874`, `2176` → `2180`, `3201` → `3220`). **Normalizando esos volátiles, los dos
inventarios salen idénticos: 966 = 966, `diff` vacío.**

**Y el reparto se comprobó por NOMBRE, no por total:** los 7 casos de la parte 2 nueva son
literalmente los 7 primeros de la parte 2 vieja, y los 3 de la parte 5 los 3 de `CA-04` que estaban
al final de la parte 2. **Ningún identificador de caso desapareció, cambió de texto ni se fusionó.**
El cuadre que el `desarrollador` declara en `docs/qa/1.34.0-req023-particion-39.md` es **correcto**.

### 3. Los tres bloqueantes de la vuelta anterior

#### `QA-023-01` (`usuario/dinero`, crítica) — **CERRADO**, con mi evidencia y tras intentar romperlo

Banco propio (independiente del banco del proyecto): un `Write` de `PreToolUse` real contra
`guard-completado.sh`, proyecto efímero (`estados.completado = completado`, `quality_gates: ["true"]`,
cola vacía, en disco `Estado: en-revisión`), las **dos** versiones sobre el mismo proyecto.
**20 formas de la clase, todas DENIEGAN hoy; 13 de ellas eran `allow`:**

| # | forma | base `95764db` | árbol |
|---|---|---|---|
| A01 | `Sensible a`·`··`·`seguridad` (U+0020 **insertado**) | allow | **deny** |
| A02 | `Sensible a`⟨NBSP⟩`seguridad` (**sustituye** al blanco) | allow | **deny** |
| A03 | `Sensible a`⟨TAB⟩`seguridad` | allow | **deny** |
| A04 | `Sensible a`⟨U+200B⟩`seguridad` | allow | **deny** |
| A05 | tres espacios | allow | **deny** |
| A08 | `Sensi ble a seguridad` (blanco **dentro de una palabra**) | allow | **deny** |
| A09/A10/A11 | `Hallazgos abiertos` con doble espacio · NBSP · TAB | allow | **deny** |
| A12 | `Est ado` | allow | **deny** |
| A15 | `Q A` (clave de **una** palabra) | allow | **deny** |
| A19/A20 | el blanco **retirado**: `Sensibleaseguridad`, `Hallazgosabiertos` | allow | **deny** |
| A06/A07/A13/A14/A16/A17/A18 | espacio+TAB, TAB interno, BOM, ZWSP final, blanco al borde | deny | **deny** |

**Y el motivo diagnostica.** `«Sensible\x20a\x20\x20seguridad:»` para el doble espacio y
`«Sensible\x20a\xc2\xa0seguridad:»` para el NBSP: los blancos salen en hexadecimal **sólo** cuando
alguno de ellos **es** lo insertado, que es la mitad sin la cual el motivo diría exactamente la
palabra que la persona cree haber escrito.

**La tercera forma: la busqué y no la encontré dentro de la familia del blanco.** Dónde busqué:
inserción, sustitución y duplicación del blanco en las **seis** claves; blanco al borde (sangría, que
es tolerancia y sigue gobernando); blanco retirado; blancos múltiples; TAB, NBSP y ZWSP en los tres
procedimientos; el blanco en una clave de **una** palabra (`Q A`, `Est ado`). El argumento que cierra
el hueco: el alfabeto se **deriva** de las claves, sus únicos elementos no-letra son el blanco, y la
comparación ahora retira **lo ajeno** y **todos los blancos** antes de preguntar si lo que queda es
una clave **sin sus blancos** — con lo que las tres operaciones posibles sobre un blanco (poner,
quitar, cambiar) caen con la **misma** pregunta. Lo que queda fuera es sustituir una **letra**, que
es el homóglifo y está declarado fuera de alcance (y medido: ver `QA-023-07`).

#### `QA-023-02` (`contrato`) — **CERRADO**

- **Write-back: existe.** `requirements/REQ-023.md`, Historial 2026-09-09: «Write-back de `QA-023-02`:
  `CA-03` deja de exigir “DENY en todas” y pasa a exigir EL VEREDICTO DE CADA RAMA…».
- **El sorteo ya no es un pool tecleado.** Los dos estratos se derivan **en la corrida** de
  `ARNES_CLAVES` leída del `lib.sh` bajo prueba —no de `ARNES_CLAVES_ALFA`, para no heredar el error
  de la guarda—: medido, `6` claves y alfabeto de `21`. E1 = complemento; E2 = el alfabeto. El
  universo del sorteo es cualquier punto de código de `1..1114111` menos LF, CR y los sustitutos, con
  su motivo escrito.
- **Publica la semilla y las tiradas por rama** (`ARNES_SEM_39` reproduce: verificado, la semilla
  `508269595` da `2/5/1` en corridas independientes).
- **Puede fallar, y falla:** los dos casos rojos contra `95764db` (§1.a).
- **Aborta con SKIP y no con PASS** cuando una rama se queda sin tirada: provocado (§1.c).

Matiz que anoto y **no** convierto en hallazgo: la única entrada de **E2 garantizada** por las cuatro
tiradas fijas es el **blanco**; una entrada de E2 que no sea blanco sólo entra por las cuatro tiradas
libres. El criterio pide «no menos de una entrada de cada uno de dos estratos» y el blanco **es** una
entrada del alfabeto, así que conforma; y la rama 3 no exige veredicto, luego no se pierde poder de
falsación.

#### `QA-023-03` (`contrato`) — **CERRADO**

- **Write-back: existe.** Historial 2026-09-09: «Write-back de `QA-023-03`: la vía del invisible
  delante del `## ` que TERMINA la cabecera queda NOMBRADA en “Fuera de alcance”…».
- **La comprobación de `CA-12 (iii)` es contar, y cuentan 3 = 3.** En «Fuera de alcance» están
  nombradas las tres: el **homóglifo** (dueños, ventana 1.35.0, clase), el **NUL / UTF-16LE** (dueño
  `desarrollador`, ventana 1.35.0, clase `instrumento`) y el **invisible delante del `## `** (con
  dueño, ventana, forzador y la medición de sus dos controles).

### Veredicto por criterio (vuelta 2)

| Criterio | Veredicto | Base |
|---|---|---|
| `CA-01` | **PASA** | La subclase que la hacía fallar está cerrada: 20 formas, 13 volteadas allow→deny; motivo imprimible y diagnosticable; nunca por ausencia. |
| `CA-02` | **PASA** | Dominio derivado **5 de 6** en cada corrida (`[QA · Sensible a seguridad · Hallazgos abiertos · Rigor · Estado]`, fuera `Seguridad`); anti-vacuidad con caso propio que **falla** con dominio vacío (medido contra `v1.33.0`: «0 campos, 0 abren» → FAIL); deniega con todo en verde. Ya sin el arrastre de `QA-023-01`. |
| `CA-03` | **PASA** | §1.a y §1.c. |
| `CA-04` | **PASA en su letra · el CASO no discrimina** | 0 divergencias sobre el corpus y los 27 REQ, y 0 falsos positivos sobre los 74 documentos reales del árbol (medido por mí). Pero la comparación **no puede ver** la guarda: `QA-023-08`. |
| `CA-05` | **PASA** (con límite de entorno) | Veredicto y motivo idénticos bajo `C` y `C.utf8`; y el caso **falla** contra `v1.33.0`. Sigue sin medirse un locale con colación distinta ni Windows/MSYS. |
| `CA-06` | **PASA** | Una sola constante (`ARNES_CLAVES`), alfabeto derivado con `]` primero y `^`/`-` al final (verificado leyendo `_arnes_deriva_alfabeto`), mapa «clave sin blancos → clave» también derivado, publicación en **una** boca (`arnes_campo_linea`, después de `arnes_sin_cita`) y no en la llamada cruda; `Archivos:` fuera de la constante con su fail-closed verificado. |
| `CA-07` | **PASA** | `rc 0` y ninguna anomalía falsa sobre el árbol real; los cuatro casos del informe fallan contra `v1.33.0`. |
| `CA-08` | **PASA, y ahora sí desde un TAG** | Los cuatro «(era ALLOW)» fallan contra `v1.33.0` (§1.b); el banco materializa desde el tag con verificación de OID. Reserva: `SEC-048`, ajena a este REQ. |
| `CA-09 (i)` | **PASA** | El caso publica 5 procesos contra 5 de la línea base: 0 añadidos, medido en la corrida. |
| `CA-09 (ii)` | **PASA por MEDICIÓN** | 1,053× y 1,072× en mis dos corridas (techo 1,25×), sujetos intercalados. La cota algebraica sigue rota como demostración (declarado en el Historial); la sostiene el número. |
| `CA-09 (iii)` | **PASA en el caso · el criterio sigue incompleto** | `QA-023-05`, todavía abierto. |
| `CA-10` | **PENDIENTE, declarado** | Sin implementar por precondición ajena. No lo enruto yo. |
| `CA-11` | **PASA** | Las cuatro formas comentadas y la ausencia deciden **igual** en las dos versiones, y los cuatro casos pasan **también** contra `v1.33.0`: la frontera no se movió. |
| `CA-12 (i)/(ii)` | **PASA** | `arnes_sin_cita` y `arnes_cola_pendientes` no se tocan; el caso compara 7 formas del corpus contra `v1.33.0`. |
| `CA-12 (iii)` | **PASA** | 3 = 3 (arriba). |

### Hallazgos

#### `QA-023-04` · `instrumento` · media — **SIGUE ABIERTO, sin cambio**

La derivación por `sed` de `36-cabecera-comentario-html-2-los-lectores.sh:228` sigue dando **5**
claves y sigue perdiendo justo `Estado`. Medido hoy con el mismo `sed` sobre los tres árboles:

```
v1.33.0 : n=6  [Estado|Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad]
95764db : n=5  [Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad]
árbol   : n=5  [Hallazgos abiertos|QA|Rigor|Seguridad|Sensible a seguridad]
```

Dueño: `desarrollador`. Forzador natural: `REQ-024`. **No bloquea.**

#### `QA-023-05` · `instrumento` · media — **SIGUE ABIERTO**, y se refuerza; retiro mi número anterior

Sin write-back: `CA-09 (iii)` sigue publicando el **par** de longitudes y no la **forma** de la
línea, que es el segundo parámetro que decide el valor. Re-medido hoy con
`tests/util/sonda-reloj.sh` (k=200, r=3, mínimo de k, sujeto `arnes_norm_clave`, par 1000→2000):

| Forma que se dobla | base `95764db` | árbol | techo |
|---|---|---|---|
| clave tipo **título** (la que el caso mide) | 2,084 | **2,069** | 2,2 |
| clave `Sensible a <a…> seguridad` | **2,181** | 2,008 | 2,2 |
| **valor** `Rigor: <a…>` | 1,815 | 1,689 | 2,2 |

**Cuatro mediciones conformes del mismo criterio, con el mismo par publicado, van de 1,689 a 2,181** —
un 29 % de dispersión, y la peor a **99,1 %** del techo. El coste absoluto de la forma «valor» es
~85 ms contra ~280 ms de las formas de clave: es la **forma**, no la longitud.
**Y retiro por honestidad el «el techo se supera» de mi vuelta anterior:** aquellos 2,412/2,364 se
tomaron con otra construcción (par 1015→2015, sujetos intercalados) y **no se reproducen** hoy. Eso
no debilita el hallazgo: lo confirma, porque el defecto es exactamente que dos mediciones conformes
no son comparables. Dueño: `analista-requerimientos` (el parámetro que falta) + `desarrollador` (que
el caso publique la forma). **No bloquea.**

#### `QA-023-06` · `instrumento` · media — NUEVO: **la abstención por SKIP MATA la sección; la cláusula anti-tautología de `CA-03` nunca se había ejecutado**

Toda ruta de SKIP de las secciones de este REQ hace `SKIP=$((SKIP+1))`, y **el corredor no define
`SKIP`** antes de ejecutar una sección (`run.sh:23` inicializa `PASS=0; FAIL=0`; `SKIP=0` aparece en
`:1241`, en la fase de **agregación**, después). Con `set -u` eso es fatal: la sección muere en la
**primera** línea de SKIP que emite. El conteo de SKIP lo hace el corredor **leyendo el texto**
(`/^  SKIP /`), así que el incremento además no sirve para nada (`04-windows-…:29` lo hace bien: sólo
`echo`).

Reproducción (copia de las secciones en `ARNES_SECCIONES_DIR`, con la referencia de la línea base
sustituida por una inexistente para forzar la abstención):

```
SKIP  REQ-023 CA-03 rama 1: … esa rama se quedó sin ninguna tirada que abriera en la heredada …
39-caracter-invisible-2-la-clase.sh: line 288: SKIP: unbound variable
ABORT: la sección 39-caracter-invisible-2-la-clase.sh murió a mitad (código de salida 1)…
Resultado: 21 PASS, 0 FAIL      ← y se habían impreso 6 líneas FAIL
```

**Esperado:** las dos ramas emiten SKIP con su motivo y la sección llega al final.
**Observado:** la rama 1 emite su SKIP, la sección muere, la rama 2 **no reporta ni PASS ni SKIP**, y
la sección entera se descarta del cuadre.

Alcance medido: **las cuatro** partes de la sección 39 que tienen ruta de SKIP mueren
(`-2-la-clase.sh:288`, `-3-…:235`, `-4-el-coste.sh:172`, `-5-el-corpus.sh:172`); en el árbol hay
**14** instancias del patrón, **12** en secciones de este REQ y 2 pre-existentes en la 28. El defecto
es **anterior** a esta vuelta (existe idéntico en `e406202`, el árbol que aprobé en lo demás: es un
fallo mío de la vuelta 1 tanto como del `desarrollador`).

**Por qué es `instrumento` y no bloquea:** el producto (`hooks/`) no cambia; y **no es un fail-open** —
el corredor detecta la muerte, lo dice y devuelve `rc 1`. Lo que se pierde es (a) que la cláusula que
`CA-03` eleva a criterio —«aborta con SKIP y su motivo, nunca PASS»— sea **ejecutable**, y (b) que la
línea `Resultado:` sea veraz cuando coincidan una abstención y una regresión: publicó `0 FAIL` con
seis FAIL en pantalla. Dueño: `desarrollador`.

#### `QA-023-07` · `contrato` · media — NUEVO: **el «Fuera de alcance» del homóglifo sigue apoyado en la premisa que `QA-023-01` midió FALSA, y contradice a `CA-03` en el mismo documento**

`CA-03` corrigió el motivo por el que el homóglifo queda fuera y lo dice con estas palabras: *«el
motivo va corregido, porque el anterior estaba medido falso: no es que “la clave resultante no
contenga nada ajeno al alfabeto” —sí lo contiene, y la guarda lo **ve**—, es que retirar lo ajeno no
repone lo sustituido»* (`requirements/REQ-023.md:169-172`). Pero el apartado **«Fuera de alcance»**
sigue diciendo lo contrario, literal: *«la clave resultante no contiene nada ajeno al alfabeto de las
claves —es **otra** clave—, así que el mecanismo de CA-03 calla»* (`:973-975`).

Medido ejecutando el lector del árbol sobre `Еstado` (`Е` = U+0415 cirílica, bytes `d095`):

```
clave cruda        = <Еstado>   bytes = d095 73 74 61 64 6f
retirado lo AJENO  = <stado>    ¿contenía algo ajeno? SÍ
sin blancos        = <stado>
reconstruye una clave: NO  -> la guarda calla
¿es OTRA clave del lector? NO
```

Las **dos** cláusulas de la premisa son falsas: la clave **sí** contiene algo ajeno (la guarda lo ve
y lo retira) y **no** es otra clave. La conclusión —la guarda calla— es cierta, por el motivo que
`CA-03` ya escribió. Verificado además de punta a punta: `Еstado: completado` y `Ｅstado: completado`
salen `allow` en las dos versiones (el homóglifo está fuera de alcance, como se declara).

**Por qué `contrato` y no una errata:** es el apartado que fija la **frontera de alcance**, y quien
audite si el homóglifo es realmente inalcanzable siguiendo ese puntero concluirá que la guarda no ve
el carácter — y sí lo ve. Es la falta que `SEC-050` mide y la que este mismo REQ documenta **dos
veces** en `CA-01` (un puntero medido falso, corregido dos veces). El texto correcto ya existe
literal en `CA-03`: el write-back es trasladarlo.
Dueño: `analista-requerimientos`. **Bloquea hasta el write-back.**

#### `QA-023-08` · `instrumento` · media — NUEVO: **el caso de `CA-04` no puede fallar en el eje del que `CA-04` habla: ninguna de sus dos mitades compara la guarda**

`CA-04` existe para que la guarda «no se convierta en una prohibición de escribir en español». Su
caso (`39-…-5-el-corpus.sh`) compara las dos versiones con un evaluador que imprime:

- mitad (1) «campo a campo»: `CAMPO\t$ARNES_CLAVE\t$ARNES_VALOR\t$ARNES_CLAVE_DECORADA` (`:132`);
- mitad (2) «decisión a decisión»: los cinco campos, el estado, su cita, el rango abierto, el CR
  interior y la sensibilidad dudosa (`:123-129`).

**En ninguna de las dos aparece `ARNES_OCULTA`**, que es la única variable por la que la guarda puede
cambiar una decisión. Y `CA-06` exige que la guarda sea **observacional** —que no cambie ningún valor
de campo—, así que la mitad (1) **no puede fallar por construcción** para esta guarda: mide la
propiedad de `CA-06` y publica el resultado como *«las dos versiones resuelven IGUAL las 5207 líneas
del corpus»*. La mitad (2) publica *«los 27 REQ del árbol deciden IGUAL»*, y tampoco mira la guarda.

Medido con el **mismo** lector real de las dos versiones sobre **20 306** líneas con `:` de todos los
`.md`, `.tpl`, `.sh` y `.txt` del árbol, imprimiendo `ARNES_OCULTA`: **131 líneas** en las que la
guarda **dispara hoy y no disparaba en `95764db`** — cero de ellas visibles para el caso de `CA-04`.
Tres formas reproducibles, extremo a extremo contra la puerta:

| cabecera | base | árbol | motivo de hoy |
|---|---|---|---|
| `> Estado: en-revisión` | allow | **deny** | `«\x3e\x20Estado:»` |
| `- Estado: en-revisión` | allow | **deny** | `«\x2d\x20Estado:»` |
| `1. Rigor: ligero` | allow | **deny** | `«\x31\x2e\x20Rigor:»` |

Y **un documento real de este repositorio** cuya lectura de cabecera cambia de opinión:
`docs/gobernanza/autoalojamiento.md` línea 3, `> **Estado: ACTIVO desde 2026-09-05.** …` →
base `oculta=0`, árbol `oculta=1|clave=Estado`.

**Lo que NO es:** no es un fail-open, y **no es un incumplimiento de la letra de `CA-04`** — su corpus
son las secciones más las cabeceras de los REQ del árbol, y ahí **medí yo mismo 0 divergencias sobre
los 74 documentos reales** (`requirements/`, `templates/`, `docs/`, `PENDING_APPROVAL.md`,
`AGENTS.md`, `CHANGELOG.md`), con el único disparo el de `docs/gobernanza/`, que **ninguna puerta
lee** porque no está en `requirements_dir`. Tampoco estrecha ninguna tolerancia contratada: `>`, `-`
y `1.` nunca fueron decoración admitida, y bajo `CA-01` una línea que una persona lee como campo y la
máquina no debe **denegar** — la conducta nueva es la conforme y la vieja era el lado que abre.

**Lo que sí es:** el único criterio que vigila la **sobre-denegación** está certificado por una
comparación **ciega a la guarda**, y publica un verde con las cifras 5207/27 que no respaldan lo que
su nombre afirma. Es la misma familia que `QA-023-02` —un caso que pasa en las dos versiones no mide
nada— un eje más allá. La vía conforme es que el evaluador imprima `ARNES_OCULTA` (y su clave) en las
dos mitades, y que las divergencias esperadas, si se aceptan, queden **declaradas**.
Dueño: `desarrollador` (el caso) + `analista-requerimientos` si la conducta nueva se acepta como
criterio. **No bloquea.**

#### `QA-023-09` · `instrumento` · baja — NUEVO: **la clave escrita con otra capitalización se resuelve como AUSENCIA y abre, igual en las dos versiones**

Variantes de entrada sobre el conjunto conocido de claves (mi paso obligado de falsación):

| cabecera | base | árbol | esperado por el contrato |
|---|---|---|---|
| `sensible a seguridad: sí` + `Rigor: ligero` + los dos veredictos `pendiente` | allow | **allow** | fuera de `CA-01` |
| `estado: completado` / `ESTADO: completado` | allow | allow | fuera de `CA-01` |
| `Sensible a seguridad: quiza` (valor inválido) | deny | deny | deny ✔ |
| `QA: ok` (valor inválido) | deny | deny | deny ✔ |
| `QA:   aprobado  ` (blancos sobrantes en el valor) | deny | deny | deny ✔ |
| cabecera vacía · documento vacío · un solo `\n` | allow | allow | ausencia perdonada ✔ |

Una persona lee `sensible a seguridad: sí` como el campo y la máquina lo resuelve como **ausencia**,
que es el lado que abre. **No lo reclamo como incumplimiento de `CA-12 (iii)`** y digo por qué: la
clase de este REQ es la del carácter que **nadie ve en el diff**, y una capitalización distinta **se
ve**; además `CA-01` acota su propiedad a «la clave **tal como la escribe la plantilla**». Queda
anotado para que exista por escrito y con dueño, no para bloquear.
Dueño: `analista-requerimientos` (decidir si entra en la superficie de `REQ-024`, que es quien cambia
la semántica de la ausencia). **No bloquea.**

### Resumen de clases — vuelta 2

| Hallazgo | Clase | Bloquea | Estado |
|---|---|---|---|
| `QA-023-01` | `usuario/dinero` | — | **CERRADO** |
| `QA-023-02` | `contrato` | — | **CERRADO** |
| `QA-023-03` | `contrato` | — | **CERRADO** |
| `QA-023-04` | `instrumento` | no | abierto, sin cambio |
| `QA-023-05` | `instrumento` | no | abierto, reforzado |
| `QA-023-06` | `instrumento` | no | **nuevo** |
| `QA-023-07` | `contrato` | **sí** | **nuevo** |
| `QA-023-08` | `instrumento` | no | **nuevo** |
| `QA-023-09` | `instrumento` | no | **nuevo** |

**Forma del criterio mal formado** (se anota aquí y **sólo** aquí, nunca dentro del paréntesis de la
clase en `Hallazgos abiertos:`):

- `QA-023-05` → forma **número**: `CA-09 (iii)` fija 2,2 como **operativo** sin el segundo parámetro
  que determina su valor (la forma de la línea), así que cuatro mediciones conformes se contradicen.
- `QA-023-07` → forma **enumeración**: el apartado de alcance describe la superficie del homóglifo con
  una premisa que el propio `CA-03` declara medida falsa, y el documento se desmiente a sí mismo.

**Vueltas del ciclo dev↔QA de `REQ-023`: 2 de 3 consumidas** (`AGENTS.md` §6: el contador es por REQ
y **no** se reinicia con cada hallazgo nuevo). Queda **una**. Con los tres bloqueantes anteriores
cerrados y un único bloqueante nuevo cuyo write-back es trasladar un texto que ya existe literal en
`CA-03`, la vuelta 3 debería alcanzar; si no, la salida es el residual declarado o `bloqueado` con
escalada, no una cuarta vuelta.

### Lo que NO acredito, dicho expresamente

1. **`CA-10` no se validó**: sigue sin implementar por precondición ajena (`AGENTS.md` §13,
   `templates/AGENTS.md.tpl`, `skills/arnes-upgrade/SKILL.md` están en manos de otra comisión).
2. **`CA-08` en la mitad que `SEC-048` nombra**: el fail-before ya se toma desde un **tag**
   (`v1.33.0`), pero un tag del propio repositorio no es una referencia inmutable **acreditada**; ese
   gate es humano y **no** es de este REQ. Acredito el delta, no la inmutabilidad del árbol de
   referencia.
3. **`CA-05` sólo sobre dos locales**, `C` y `C.utf8` (los únicos de esta máquina). Un locale con
   **colación** distinta no se ejerció, y **nada** se midió en Windows/MSYS, que es de donde sale el
   BOM y donde viven los proyectos consumidores.
4. **`CA-09 (i)` no lo medí yo con sonda de procesos**: lo acredito por el caso del banco (que publica
   5 contra 5 en la corrida) y por lectura; no conté procesos por mi cuenta.
5. **`CA-06` en su eje de los cuatro lectores**: no comparé `hooks/campos-req.awk` contra
   `hooks/lib.sh` por mi cuenta; lo mide el caso `39-…-3` (33 documentos, 6 con un invisible) y lo doy
   por su cuenta, no por la mía.
6. **El homóglifo y el NUL/UTF-16 no se cerraron ni se pidió cerrarlos**: están declarados fuera de
   alcance con dueño y ventana. Mi verde **no** acredita la clase, que es lo que `CA-12 (iii)` exige
   no maquillar.
7. **Rendimiento con usuarios concurrentes: no aplica** — el arnés no los tiene. Los umbrales de este
   proyecto son de reloj y de procesos y viven en `CA-09`; los medí y los publico arriba. No hay prueba
   de carga que reportar y no la finjo.
8. **Dos paradas simultáneas del hook de continuidad**: no las ejercité; están fuera de este REQ.
9. **No firmo `Seguridad:`**, no toco el `Estado:` de `REQ-023`, no hice write-back yo, y no toqué
   `REQ-024` ni los hallazgos de `REQ-026`.

**Evidencia en disco (método y versión base para re-derivar sin preguntar):**
`docs/qa/1.34.0-req023-vuelta2-metodo.md`.

## REQ-023 · re-validación **vuelta 3 de 3 — la última** · `hooks/` y `tests/` en **`6ad9752`** (rama `rel/registro-1.33.0`)

**Fecha:** 2026-09-09 · **Agente:** `qa-tester` (Opus, política de autoalojamiento) · **Árbol:**
`hooks/` y `tests/` tal como quedan en **`6ad9752`**, más el write-back del analista a
`requirements/REQ-023.md`, sin comitear. **Método y evidencia re-derivable:**
`docs/qa/1.34.0-req023-vuelta3-qa-metodo.md`.

**La base cambió a mitad de la comisión y se dice:** el encargo la fijaba en el árbol de trabajo sobre
`5305de9`, y a las ~19:00 aterrizó `6ad9752`, que comiteó **exactamente los bytes que ya estaban ahí**
(las cinco secciones `39-*`, la 36, `run.sh`, el índice, `REQ-024`, `REQ-019` y `PENDING_APPROVAL.md`).
**`hooks/` no entró en ese commit y no se tocó en toda la comisión**, así que ninguna medición cambia
de sujeto — pero el veredicto se da contra `6ad9752`, no contra `5305de9`.

**Vueltas del ciclo dev↔QA de `REQ-023`: 3 de 3 CONSUMIDAS** (`AGENTS.md` §6: el contador es por REQ
y **no** se reinicia con cada hallazgo nuevo). **No hay vuelta 4.**

### Cifras de la corrida (medidas por QA, tres corridas del banco)

```
A   958 PASS ·  1 FAIL · 7 SKIP   (total 966 ✔)
B   954 PASS ·  4 FAIL · 8 SKIP   (total 966 ✔)   rc=1
C   958 PASS ·  1 FAIL · 7 SKIP   (total 966 ✔)   rc=1
D   958 PASS ·  0 FAIL · 8 SKIP   (total 966 ✔)   rc=0   <-- VERDE
E   959 PASS ·  1 FAIL · 6 SKIP   (total 966 ✔)   rc=1
F   957 PASS ·  1 FAIL · 8 SKIP   (total 966 ✔)   rc=1   <-- la del ÁRBOL ENTREGADO
      (6 corridas: 5 rojas, 1 verde; total 966 y REQ-023 en 40/0/2 en las SEIS)
      (E se corrió DESPUÉS de escribir REQ-023.md, CHANGELOG.md y docs/qa/, que están DENTRO
       del corpus del propio banco; su FAIL es `REQ-021 CA-03 (c)`, de reloj y ajeno)

Y la corrida que MANDA no es ninguna de las mías — es el CI, y sale ROJA por un caso de REQ-023:
CI `hooks-en-linux` sobre 6ad9752 (PR #45)   955 PASS ·  1 FAIL · 10 SKIP   (total 966 ✔)
  FAIL  REQ-023 CA-09 (iii) arnes_campo_linea
        mediana 2.365× > techo 2.200×  (margen 0.165× > dispersión 0.023×)
casos de REQ-023: 42 · 40 PASS · 0 FAIL · 2 SKIP — IDÉNTICO en las tres
autoprueba del corredor: 106 PASS · 0 FAIL
quality gates de AGENTS.md §7 (las tres del manifiesto): rc 0 · rc 0 · rc 0
sintaxis de las 57 secciones: rc 0     ·     tools/arnes-lectura.sh . : rc 0
cola de PENDING_APPROVAL.md: 4 pendientes (D1..D4) -> ningún REQ puede cerrarse
worktrees huérfanos: ninguno (el de esta comisión se retiró)
```

**La cola NO está en 0**, contra lo que el encargo daba por comprobado: el archivo se reescribió a
mitad de esta comisión (18:56:54 → `0`; 19:00:33 reescritura; 19:06:25 → `4`) y su contenido es
**exactamente el de `HEAD`**, con `D1`–`D4` dentro. **No es un defecto del lector** —30 lecturas
consecutivas dan 4, con el `mtime` y el hash intactos— y está medido en el método, §1. Consecuencia:
`guard-completado` deniega hoy el cierre de **cualquier** REQ, y `D1` es la entrada que espera este
veredicto.

**El banco está ROJO en la puerta que decide, y el rojo ES de REQ-023 — se dice antes que nada, y
corrige lo que yo mismo había escrito con mis cinco corridas locales delante.** En este host el banco
no es reproducible (**4 de 5** corridas rojas, la `D` verde) y **ningún FAIL local es de REQ-023**;
pero en el **CI**, que es la puerta **requerida y estricta** de `main`, `CA-09 (iii)` sobre
`arnes_campo_linea` **FALLA**: mediana **2,365×** contra el techo **2,200×**, con **margen 0,165× >
dispersión 0,023×**. El detalle y lo que decide está en `QA-023-10`, y el efecto sobre mis propias
cifras en `QA-023-14`. **Ningún FAIL local es de REQ-023** — los seis son de reloj y de sondas ajenas (`QA-023-14`). La **lista** de SKIP
—nunca el total— y los FAIL elemento por elemento están en el método, §1.

### Veredicto por criterio (vuelta 3)

**10 pasan · 1 no acreditado (`CA-09 (iii)`) · 1 NO CUMPLE (`CA-10`).** Tabla completa con la base de
cada uno en `docs/qa/1.34.0-req023-vuelta3-qa-metodo.md` §8. Lo que cambia respecto de la vuelta 2:

- `CA-04` pasa a **PASA y el caso DISCRIMINA** — antes pasaba «en su letra» con el caso ciego.
- `CA-09 (iii)` pasa de «PASA en el caso · el criterio incompleto» a **NO ACREDITADO, y el caso está
  en ROJO en la puerta que decide**: el criterio quedó bien formado y el caso quedó midiendo la magnitud
  que el criterio retiró — y en el CI, donde la máquina está quieta, ese caso **resuelve y FALLA**
  (`QA-023-10`). No es regresión del REQ: falla contra el techo retirado, que la base heredada incumple
  **más**.
- `CA-10` pasa de «PENDIENTE, declarado» a **NO CUMPLE**: su precondición ha **vencido** y sigue sin
  escribirse. Esto es nuevo, y es lo único que bloquea.

### La reconciliación de `QA-023-05`, que era el primer encargo

**Clase: `contrato`.** Confirmo la subida del analista y **corrijo mi propio veredicto de la vuelta 2**
(`docs/qa/1.34.0.md:2002` y `:2011`, que decían `instrumento`). Mi argumento de entonces —«el producto
no se degrada»— responde a la pregunta de `usuario/dinero`, no a la de `contrato`, que es «¿el REQ
afirma algo falso sobre lo construido?»; y lo afirmaba. **Forma del criterio mal formado: «número»**
(se anota aquí y sólo aquí, nunca dentro del paréntesis de la clase del campo).

**Y CERRADO por el write-back**, así que **se retira del campo**. El write-back cumple las cuatro
cosas que se le pedían: la magnitud pasa al **delta** que este REQ introduce, el número se declara
**operativo** con dirección **hacia abajo**, el **cuarto parámetro** (la línea base, el tag) es
obligatorio y sin él la relación **no acredita**, y la brecha que queda en el instrumento está
**nombrada por escrito en el propio REQ** — de modo que el documento ya no afirma nada falso. La
**prueba de falsación está escrita literalmente** dos veces (`requirements/REQ-023.md:474-478` y
`:501-522`): *«si las medianas emparejadas hubieran salido 1,05, el techo seguiría siendo 1,00 y lo que
habría es un hallazgo contra el código, no una reformulación»*. Verifiqué además su **aritmética**:
`0,950−0,435 = 0,515`, `1,211−0,829 = 0,382`, `1−0,856 = 0,144`, `1−0,906 = 0,094`. Las cuatro cuadran.

Su residual **no desaparece con el cierre**: se abre con ID propio y clase propia (`QA-023-10` y
`QA-023-12`).

**Y el cierre es CONDICIONAL, dicho en la única forma que sirve — con la condición medida.** El
write-back vive **sólo en el árbol de trabajo**: verificado por mí, `git show
6ad9752:requirements/REQ-023.md | grep -c 'relación emparejada'` → **0**, y el mismo conteo sobre el
archivo local → **8**. Contra el árbol **comiteado**, `CA-09 (iii)` sigue siendo el criterio viejo —el
techo absoluto de 2,2 medido insatisfacible— y el CI está **rojo contra ese mismo techo**. Por tanto:
**retiro `QA-023-05` del campo porque el write-back existe y es correcto, y esa retirada vale si y sólo
si el write-back se comitea.** Si `6ad9752` se fusionara sin él, `QA-023-05` vuelve a ser `contrato`
abierto y hay que reponerlo en el campo. No es una formalidad: es la diferencia entre un hallazgo
cerrado y un hallazgo cerrado en un archivo que nadie fusionó.

### Los cuatro `instrumento` del `desarrollador`: RETIRADOS del campo

| Hallazgo | Estado | Verificado por mí |
|---|---|---|
| `QA-023-04` | **CERRADO** | `. hooks/lib.sh; echo "$ARNES_CLAVES"` → **6 claves con `Estado` dentro**; la derivación de la sección 36 lee la **constante**, no el texto del `case`. Fail-closed escrito para hooks < 1.34.0 |
| `QA-023-06` | **CERRADO en su alcance** | `grep -Frn` → **2** ocurrencias, **0** en las cinco secciones `39-*` (eran 15). El mecanismo de muerte por `set -u` **ejecutado**, no leído. Residual → `QA-023-11` |
| `QA-023-08` | **CERRADO** | **doble** par discriminante propio: sobre-denegación sintética → la mitad (2) **FALLA** nombrando `REQ-001.md`; guarda neutralizada → el **cuarto suelo** de anti-vacuidad **FALLA** |
| `QA-023-05` | **CERRADO** (mitad de contrato) | arriba |

**La abstención sobre `28-rotacion-seccion-{1,4}` fue la correcta**, y lo digo con el motivo: esos dos
archivos están en el `Archivos:` de `REQ-026`, que tiene `QA: aprobado` dado **sobre otro árbol**;
editarlos habría invalidado ese veredicto y producido la colisión que `tools/arnes-paralelo.sh` existe
para evitar. Fue correcta **en el procedimiento** e incompleta **en la materia**, así que el residual va
a `QA-023-11` con dueño y forzador — no a una nota.

### Hallazgos

#### `QA-023-10` · `instrumento` · severidad **crítica** — NUEVO: **el caso del banco mide la magnitud que `CA-09 (iii)` RETIRÓ; en una máquina quieta resuelve, y resuelve en ROJO contra el techo retirado — la puerta requerida y estricta de `main` está roja por esto**

**Pasos de reproducción.** `bash tests/escenarios/hooks/run.sh` y leer las dos líneas de
`REQ-023 CA-09 (iii)`; después leer `CA-09 (iii)` en `requirements/REQ-023.md:386-395`.

| | el criterio desde el write-back | el caso (`39-…-4-el-coste.sh`) |
|---|---|---|
| magnitud | **relación emparejada** candidata/base, dentro de cada toma, una tanda intercalada al nivel de `n` | **cociente absoluto** de la candidata, sola |
| techo | **1,00×** | **2,200×**, que el criterio **ya no contrata** |
| parámetros | **cuatro** (par, `k`, forma, **tag**); «sin los cuatro, la relación **no acredita** (iii)» | **tres**; **ningún tag** |
| línea base | se materializa desde un tag; si no, **SKIP** | **no se materializa ninguna** |

**Esperado:** que el caso publique la relación emparejada con sus cuatro parámetros, o que **SKIP** con
ellos. **Observado en local, en las cinco corridas:**
`SKIP … dispersión 0.554× (de 1.898× a 2.452×) >= margen 0.112× contra el techo 2.200× (mediana 2.312×)`
— tres parámetros y la magnitud retirada.

**Y observado en el CI, que es lo que vuelve esto crítico.** `hooks-en-linux` sobre `6ad9752` (PR #45),
**955 PASS · 1 FAIL · 10 SKIP**, total cuadrando en 966:

```
FAIL  REQ-023 CA-09 (iii) arnes_campo_linea …
      mediana 2.365× > techo 2.200×  (margen 0.165× > dispersión 0.023×)
```

| | dispersión | margen | veredicto |
|---|---|---|---|
| **CI** (máquina quieta) | **0,023×** | 0,165× | **FAIL** — pudo afirmar |
| local, corrida A | 0,245× | 0,112× | SKIP |
| local, corrida E | 0,788× | 0,396× | SKIP |

La dispersión del CI es entre **10× y 34×** más estrecha que la mía. **Verificado por mí, no heredado:**
el archivo del caso es **byte a byte idéntico** entre `6ad9752` y el árbol local
(`git diff --stat 6ad9752 -- …39-caracter-invisible-4-el-coste.sh` → vacío), y el techo que codifica es
literal (`:271`, `coc94 "…" "$fn94" 2200`). **La diferencia no es de código: es el ruido de la
máquina.** `arnes_norm_clave` siguió en SKIP incluso allí.

**Tres consecuencias, y ninguna se cancela con otra:**

1. **La puerta requerida y estricta de `main` está roja por un caso de este REQ.** Eso **no** lo decide
   la clase de este hallazgo: lo decide `AGENTS.md` §7. Mientras el caso siga apuntado a la magnitud
   retirada, el gate humano de fusión **no puede pasar**, y ése no es mío.
2. **El rojo NO es una regresión de `REQ-023`, y esto sí lo acredito.** El FAIL es contra el techo
   **absoluto de 2,2**, que el write-back **retiró por estar medido insatisfacible**: en ese **mismo
   sujeto** la base heredada `v1.33.0` paga mediana **2,446** —**peor** que el 2,365 que el CI marca— y
   la candidata queda por debajo de la base en **9 de 10** pares
   (`docs/qa/1.34.0-req023-vuelta3-metodo.md` §11.3–§11.4). Es decir: el caso pone en rojo a la
   candidata por una propiedad que la **línea base incumple más**. *Limitación honesta:* ese 2,446 se
   midió en un host **ruidoso** y no es afirmable como nivel; lo robusto es la **relación**, y la
   relación favorece a la candidata.
3. **Mis cuatro «0 FAIL» locales eran la abstención disparándose por ruido.** La cláusula del margen
   cumplió su mitad —no dio un **PASS falso**— y a la vez mantuvo **invisible un FAIL real** que una
   máquina quieta produce. Eso es materia de `QA-023-12`, y lo agravo allí.

**La clase, decidida con el dato del CI delante y no antes de tenerlo.**

- **`instrumento`, y `AGENTS.md` §6 lo nombra por su nombre:** «un defecto **del propio arnés** —un
  lector de umbral, un guardián, **una prueba**— es `instrumento` y va a deuda técnica con dueño». El
  caso del banco **es una prueba**. Y la sección añade la advertencia que aquí aplica literalmente: «un
  defecto de instrumento que bloquea una función de negocio es cómo un REQ pasa semanas abierto».
- **No `usuario/dinero`:** el producto no cambia. El coste real de la guarda lo mide `CA-09 (ii)`, que
  **pasa** (0,920× contra techo 1,25×), y la relación emparejada la deja **por debajo** de la base en 9
  de 10 pares. Nadie ve, decide ni cobra distinto **sobre lo que la guarda hace**; lo que hay es un
  número mal apuntado en un control.
- **No `contrato` — con una CONDICIÓN que hay que leer, porque hoy no se cumple.** En el árbol que
  valido, el REQ **nombra la brecha** por escrito y con dueño, así que no afirma nada falso. Pero el
  `REQ-023.md` que está **comiteado** en `6ad9752` lleva todavía el criterio **viejo**: verificado por
  mí, **0** apariciones de «relación emparejada» en `git show 6ad9752:requirements/REQ-023.md` y **8**
  en el archivo local pendiente. **Contra el árbol comiteado, la mitad de `contrato` de `QA-023-05` NO
  está cerrada**, porque el write-back no ha aterrizado. Mi cierre de `QA-023-05` es por tanto
  **condicional: vale si el write-back se comitea, y sólo entonces.** Si se fusionara `6ad9752` sin él,
  el repositorio quedaría afirmando un techo medido insatisfacible **y** con el CI rojo contra ese
  mismo techo — y este hallazgo volvería a ser `contrato`.

**No bloquea el campo `Hallazgos abiertos:`** (es `instrumento`), **y sí bloquea el gate humano de
fusión** (§7: el banco entero en verde en CI). Las dos cosas son verdad a la vez y no se sustituyen.
*Dueños:* `desarrollador` (re-apuntar el caso a la relación emparejada con la base materializada desde
el tag, y publicar los **cuatro** parámetros) + `qa-tester` (verificarlo).
*Forzador — ya no es hipotético, está disparado:* el CI de `6ad9752` en rojo. Y el segundo, que sigue
vivo: que alguien lea el SKIP de `(iii)` como si hablara del techo que el criterio contrata hoy.

#### `QA-023-11` · `instrumento` · severidad **alta** — NUEVO: **la abstención que MATA la sección sigue viva detrás de un umbral de RELOJ, y enrojece la puerta requerida de `main` por un motivo que no es el suyo**

**Pasos de reproducción.**

```
grep -Frn 'SKIP=$((SKIP+1))' tests/escenarios/hooks/secciones/
  28-rotacion-seccion-1-la-historia.sh:149      # detrás de: plataforma sin enlaces simbólicos
  28-rotacion-seccion-4-la-carrera.sh:87        # detrás de: CAR_DUR < 170000 us  (CAR_MIN = 150000+20000)
```

Mecanismo, **ejecutado**: `run.sh:17` es `set -uo pipefail`, `run.sh:23` define `PASS`/`FAIL` y **no**
`SKIP`, y las secciones van con `source` dentro de `( )`. Con `SKIP` sin definir,
`SKIP=$((SKIP+1))` levanta `SKIP: unbound variable` y **mata el subshell**: todos los casos
posteriores de la sección se pierden.

**Esperado:** `echo "  SKIP …"` sin incrementar nada, que es la convención que los propios ayudantes
del corredor ya usan (`run.sh:783`, `:808`). **Observado:** la sección muere.

**Reachability, y es lo que sube la severidad.** `28/1:149` sólo dispara en una plataforma sin enlaces
simbólicos (Windows/MSYS). `28/4:87` dispara cuando la **parada dura menos de 170 ms**, es decir en la
máquina **rápida**, no en la cargada — y el arnés está **abaratando esa misma ruta a propósito**
(`REQ-017` la dejó en 0,125× de `v1.32.1`). En mi corrida A la carrera **sí** se provocó (`CA-18 (vi)`
en PASS), así que hoy no detonó; el margen es el que separa la parada de 170 ms. Como control de la
plausibilidad: en la misma corrida A un suelo hermano de **150 ms** se falló por **147 616 µs**
(`REQ-021 CA-03 (c)`), o sea que los umbrales de esa magnitud **se están rozando hoy** en este host.

**Atenuante medida:** la detonación es **ruidosa y con nombre** — el cuadre por archivo
(`run.sh:1272-1289`) emite `ABORT: la sección <base> declara N casos y ejecutó M` y el total
(`:1342-1345`) compara contra `CASOS_ESPERADOS=966`. No es un fail-open silencioso.

**No bloquea `REQ-023`** (`AGENTS.md` §6: un defecto del propio arnés es `instrumento` y no puede ser
condición para cerrar), y **la superficie no es de este REQ**: los dos archivos son de `REQ-026`.
*Dueño:* `desarrollador`, en el ciclo de `REQ-026` o en uno propio — **no** en éste, y **no** editando
esos archivos sin reabrir `REQ-026`, cuyo `QA: aprobado` se dio sobre otro árbol.
*Forzador medido:* que la parada baje de **170 ms** en cualquier host, o la primera corrida en
Windows/MSYS.

#### `QA-023-12` · `instrumento` · severidad **alta** — NUEVO: **la dispersión es el RANGO, y eso hace dos cosas: invalida la vía conforme que el criterio pone primera, y ENMASCARA un FAIL real en cuanto la máquina tiene ruido**

**Pasos de reproducción.** Leer el estadístico en `39-…-4-el-coste.sh:218-262`: `disp` se calcula de
`lo` y `hi`, o sea es el **rango** (`hi − lo`). Después leer la vía conforme que `CA-09 (iii)` declara
(`requirements/REQ-023.md:511-517`): «**más tomas**, `k` mayor o un par mayor …, o un host menos
cargado».

**El argumento, que es aritmético y no de opinión:** el rango es **monótono no decreciente** en el
número de tomas de una corrida —el máximo no baja y el mínimo no sube al añadir muestras—, así que
«más tomas» **no puede** bajar la dispersión; en general la **sube**. Con la serie emparejada ya medida
(rango 0,515 contra margen 0,144 en `arnes_norm_clave`), añadir tomas aleja la afirmabilidad en vez de
acercarla. Las otras tres vías **sí** funcionan: `k` mayor baja el ruido **por toma**, un par mayor
**aumenta el margen**, y un host menos cargado baja las dos cosas.

**Esperado:** que la vía declarada primera sea una que pueda funcionar, o que el criterio (o el caso)
**diga qué estadístico de dispersión decide**. **Observado:** ni el criterio ni el caso lo nombran; el
caso elige el rango y el criterio propone una vía que el rango anula.

**Y la segunda mitad, que es la grave y está MEDIDA en el CI — no la deduje, me la trajo la
comparación.** El rango es una medida de **ruido del host**, no de reproducibilidad del sujeto, así que
el veredicto de un caso de coste pasa a depender de **cuánto ruido tenga la máquina**:

| host | dispersión (`arnes_campo_linea`) | margen | veredicto |
|---|---|---|---|
| **CI, máquina quieta** | **0,023×** | 0,165× | **FAIL** |
| local, corrida A | 0,245× | 0,112× | SKIP |
| local, corrida E | 0,788× | 0,396× | SKIP |
| local, corrida E (`arnes_norm_clave`) | 1,396× | 0,046× | SKIP |

**Mis cuatro «0 FAIL» locales, y los dos del propietario y los tres del `desarrollador`, no eran un
verde: eran la abstención disparándose por ruido sobre un FAIL que existía.** La cláusula del margen
cumple su mitad buena —**nunca da un PASS falso**, y eso es lo que se diseñó— y paga un precio que
nadie había escrito: **oculta el rojo justo en las máquinas donde se trabaja**, y sólo lo revela en la
puerta donde ya es tarde. Un instrumento cuyo silencio se lee como verde es la familia de defecto que
`REQ-020` existe para cazar, un eje más allá: aquí no responde verde ante la ausencia de datos,
responde **«no sé»** ante la presencia de ruido, y **«no sé» se contó como «bien»** nueve veces
seguidas entre tres personas.

**Por qué `instrumento` y no `contrato`:** el criterio **no fija** el estadístico de dispersión —sí fija
el de posición («el mínimo de k, nunca la media»; la mediana para la serie)—, así que «más tomas» es
alcanzable eligiendo un estadístico robusto, y eso es **latitud del `desarrollador`**, no una
afirmación falsa del REQ. **Si un write-back futuro fija «el rango», este hallazgo sube a `contrato`**,
porque entonces la vía declarada sería falsa sin escapatoria. **No bloquea** el campo — pero su segunda
mitad es lo que hizo invisible el rojo del CI durante nueve corridas, así que la severidad sube a
**alta**.
*Dueños:* `desarrollador` (el estadístico del caso, y la decisión de si la abstención debe publicar
además el veredicto que **habría** dado sin la cláusula) + `analista-requerimientos` (si el criterio
debe fijar el estadístico). *Forzador:* disparado ya — el rojo del CI que nueve corridas locales no
vieron.

#### `QA-023-13` · `instrumento` · severidad **media** — NUEVO por encargo del propio REQ: **la SUPERLINEALIDAD del camino heredado de lectura de cabecera**

Se abre con ID porque el apartado «Fuera de alcance» de `requirements/REQ-023.md` lo pide
literalmente: *«**Quien verifica** puede abrirla con su ID en el registro que le corresponde; el
analista no le asigna uno»*. **No es un hallazgo mío de medición**: nace de la medición del
`desarrollador` (`docs/qa/1.34.0-req023-vuelta3-metodo.md` §11) y yo **verifiqué su aritmética y su
clasificación**, no la remedí.

**Contenido.** `arnes_norm_clave` —y `arnes_campo_linea`, que lo llama— crecen **más que linealmente**
con la longitud de línea, por el recorte de blancos y los `${l%%:*}`. Con par 1000→2000, `k`=200, forma
«clave» y en la misma tanda, la base `v1.33.0` paga medianas **2,492** y **2,446** frente a **2,295** y
**2,237** de la candidata: **la paga el camino heredado, no la guarda de este REQ** (la guarda añade
1,206× a n=1000 y 1,108× a n=2000, o sea **diluye** el cociente). Limitación que el propio medidor
declara y que no se maquilla: **ninguna** de las cuatro series es afirmable en ese host (dispersión ≥
margen), así que lo medido es la **relación**, no el nivel.

**Clase `instrumento` confirmada por mí, por propiedad:** es coste de un lector del propio arnés y
nadie ve, decide ni cobra distinto. **Y verifiqué contando que NO entra en las tres superficies de
`CA-12 (iii)`** —la clase de allí es «una línea de cabecera que la máquina **no puede medir**» y esto es
el **coste** de un camino que mide perfectamente— así que `CA-12 (iii)` sigue en **3 = 3** y **pasa**.
**No bloquea.**
*Dueños:* `desarrollador` (el arreglo, **descrito y no aplicado**, §11.6) + `analista-requerimientos`
(el criterio, si se decide contratarlo). *Ventana:* **1.35.0 propuesta — la decide el propietario**, y
yo **no** la fijo. *Forzador (el del REQ, conservado):* que algún texto de este REQ o de la superficie
heredada afirme **sin condición** que el camino de lectura de cabecera crece **linealmente**, o que un
criterio de coste del arnés vuelva a contratar un **nivel absoluto** sobre ese camino como si fuera el
delta de la versión que lo declara.

#### `QA-023-14` · `instrumento` · severidad **alta** — NUEVO: **el banco no es reproducible en este host: 5 de 6 corridas rojas por reloj, ninguna por REQ-023 — y el «0 FAIL» que yo mismo publiqué ya no vale en CI**

**Pasos de reproducción.** `bash tests/escenarios/hooks/run.sh`, seis veces en este host; y leer el CI de `6ad9752`.

| corrida | resultado | FAIL |
|---|---|---|
| **CI `hooks-en-linux`** sobre `6ad9752` | 955 · **1** · 10 · **la puerta requerida** | **`REQ-023 CA-09 (iii)` `arnes_campo_linea`**: mediana **2,365×** > techo **2,200×** (margen 0,165× > dispersión 0,023×) → ver `QA-023-10` |
| **F (árbol entregado, local)** | 957 · **1** · 8 · rc 1 | `DEV v3 heredoc ~300 KB`, **4 344 ms**, de reloj |
| E | 959 · **1** · 6 · rc 1 | `REQ-021 CA-03 (c)`, de reloj |
| D | 958 · **0** · 8 · rc 0 | ninguno |
| A | 958 · **1** · 7 | `REQ-021 CA-03 (c)`: el sensible base mide **147 616 µs** y no llega a **150 000 µs** |
| B | 954 · **4** · 8 | `DEV v3 heredoc CITADO ~300 KB` **4 466 ms** (techo 4 000, veredicto `allow` correcto) · `REQ-021 CA-03` **a=2550** fuera de [1600,2400] y **b=1303** fuera de [800,1200] · `REQ-021 CA-10` y `CA-10.2` en **cascada** del anterior |
| C | 958 · **1** · 7 | `DEV v3 heredoc CITADO ~300 KB` **4 357 ms** |

**Esperado:** 0 FAIL (el propietario midió `960·0·6` y `959·0·7`; el `desarrollador`, `962·0·4` tres
veces). **Observado:** 1, 4, 1, **0** y 1 FAIL en cinco corridas locales — el verde existe, la
reproducibilidad no.

**Y la corrección que me llegó a mitad de comisión, que retiro sin discutir porque está medida: «0 FAIL»
YA NO ES CIERTO EN CI.** `hooks-en-linux` sobre `6ad9752` da **955 · 1 · 10** y su FAIL **es de
REQ-023** (`CA-09 (iii)` sobre `arnes_campo_linea`). Eso **no** cambia la clase de este hallazgo —los
seis FAIL locales siguen siendo de reloj y ajenos— pero sí cambia dos cosas que había escrito y que
ahora dicen lo contrario de la verdad: (a) que ningún FAIL fuera de REQ-023 **sólo vale en local**; y
(b) que mi «flaky» describe **mi host**, no el árbol. En la máquina quieta del CI el banco **resuelve**,
y resuelve en rojo por `QA-023-10`. **Tratamiento: `flaky`, no regresión** — un test no
determinista no es evidencia válida en ninguna de las dos direcciones, y el veredicto `allow` de
`DEV v3` es **correcto** en las dos veces que su reloj se desbocó.

**Lo que sí queda acreditado dentro de esto:** el **total cuadra en 966** en las cinco corridas locales
**y también en el CI**, así que no se creó ni se perdió ningún caso en ninguna parte; y los **42 casos
de REQ-023** salen **40 PASS · 0 FAIL · 2 SKIP** en las cinco locales. **Ninguno de los seis FAIL
LOCALES toca REQ-023** — el del CI sí, y va en `QA-023-10`.

**La mitad ya conocida y la mitad nueva, separadas:** el `DEV v3` es la oscilación que el
`desarrollador` dejó en cola y que el propietario ya había medido en 4 835 ms — **no es nuevo**, lo
confirmo con dos números más. Lo **nuevo** es `REQ-021 CA-03`/`CA-10`/`CA-10.2`: la calibración de
`sonda-reloj.sh` deja de distinguir el sujeto sensible del insensible y **arrastra tres casos**.

**No bloquea `REQ-023`** (`AGENTS.md` §6, `instrumento`; y ningún caso del REQ falla **en local**),
**pero sí es precondición del gate humano de fusión** (`AGENTS.md` §7: el banco entero en verde en CI).
**No certifico esa puerta** —hoy está **roja**, por `QA-023-10`— y la decisión de fusionar no es mía.
*Dueño:* `desarrollador`, en el ciclo de `REQ-021` o en uno propio.
*Forzador:* la primera corrida de CI que ponga roja la puerta requerida de `main` por uno de estos seis.

#### `QA-023-15` · **`usuario/dinero`** · severidad **alta** — NUEVO: **`CA-10` no está implementado, su precondición declarada ha VENCIDO, y lo que falta es justo lo que los proyectos consumidores heredan**

**Este es el único hallazgo que bloquea.**

**Pasos de reproducción.**

```
grep -n "no se puede medir\|invisible\|medibilidad" AGENTS.md templates/AGENTS.md.tpl
  AGENTS.md:349                 -> la fila define la invariante por el RETORNO DE CARRO
  templates/AGENTS.md.tpl:314   -> idéntica
grep -n "sin validación ni auditoría\|no cerrarían hoy\|REQ-023\|SEC-047" skills/arnes-upgrade/SKILL.md
  -> (vacío)
git status --short AGENTS.md templates/AGENTS.md.tpl skills/arnes-upgrade/SKILL.md
  -> (vacío: ninguno de los tres se ha tocado)
```

**Esperado (`CA-10`, literal).** Que `AGENTS.md` §13 y **la misma fila** de `templates/AGENTS.md.tpl`
escriban la invariante **por propiedad** —«una línea de la cabecera que la máquina no puede medir no
deja cerrar»— con el retorno de carro como **instancia** y no como definición; y que
`skills/arnes-upgrade/SKILL.md` diga, **sin eufemismo**, que las versiones afectadas **pudieron cerrar
un REQ `critico` sin validación ni auditoría**, enunciando **antes de ofrecer ningún comando** la
pregunta por estado («cuáles de mis REQ en estado terminal **no cerrarían hoy**»).

**Observado.** Nada de eso existe. Los tres archivos están sin tocar y la fila sigue nombrando el CR
como la definición.

**Por qué esto ya no es «pendiente por precondición ajena», que es como lo registré en la vuelta 2.**
El Historial del REQ (`requirements/REQ-023.md:1519`) declaró la precondición con nombre:
*«`AGENTS.md` y `templates/AGENTS.md.tpl` los está escribiendo **otra comisión viva** (entregados en
`6dd3f8a`)»*. Esa comisión **cerró**: `6dd3f8a` está en la historia y `REQ-027` quedó `completado` en
`95764db`. Y los tres archivos **no** están en `codigo_app.globs`
(`hooks/*`, `tools/*`, `.github/*`, `.arnes/config.json`, `.claude-plugin/*`), así que ningún guardián
impide escribirlos. La precondición **ha vencido** y el criterio sigue sin cumplirse: eso es la
**deriva** que `AGENTS.md` §9 prohíbe dejar en silencio. El Historial dice «se enrutan» y **no nombra
destino**: ni un REQ, ni una ventana, ni un dueño. Un criterio que sigue en la lista de criterios y no
está hecho no es un pendiente declarado; es un criterio incumplido.

**Por qué la clase es `usuario/dinero` y no `instrumento`, que es la pregunta que decide si bloquea.**
La pregunta es «¿puede alguien ver, decidir o cobrar distinto por esto?», y la respuesta es sí, dos
veces, y ninguna de las dos es sobre el instrumento:

1. **Un proyecto consumidor decide distinto sobre su propio corpus.** Lo único que el REQ dice que un
   proyecto hereda «y que exige acción humana» es precisamente **la auditoría de `CA-10`**
   (`requirements/REQ-023.md:1326`), y el REQ deja escrito que la vulnerabilidad es latente aquí pero
   que *«lo que sí hereda un proyecto consumidor es la **pregunta** (CA-10), porque su corpus no lo ha
   barrido nadie»* (`:746`). Sin el apartado de migración, el dueño de un proyecto consumidor **no se
   enterará** de que pudo cerrar un REQ `critico` sin validación ni auditoría, y por tanto **no barrerá
   sus REQ en estado terminal**. La decisión que cambia es suya, no la de un guardián.
2. **Lo que su coordinadora lee dejará de describir lo que la máquina hace.** Con 1.34.0 la puerta
   **deniega** por una propiedad más ancha que el CR —lo he medido: 9 de mis 20 formas voltean
   `allow`→`deny`, entre ellas un BOM, un NBSP, un TAB, un blanco duplicado y un emoji dentro de la
   clave—, mientras la fila que ese proyecto tiene delante sigue hablando **sólo** del retorno de
   carro. Y `AGENTS.md` §14.D deja escrito el agravante: *«un proyecto ya instalado tiene su `AGENTS.md`
   congelado: hasta que `arnes-upgrade` migre este bloque, sus coordinadoras no tienen estas reglas»*.
   El resultado es un cierre denegado por un motivo que **ningún documento suyo nombra**, que es la
   fricción que `CA-04` y `AGENTS.md` §13 dicen que acaba con alguien apagando el guard.

No lo clasifico `contrato` porque el REQ **no** afirma que `CA-10` esté hecho —el Historial dice que
no—: el defecto no es una afirmación falsa, es un **entregable que falta y cuyo efecto sale del
repositorio**.

*Dueños:* `desarrollador` (la fila de `AGENTS.md` §13, la misma de `templates/AGENTS.md.tpl` y el
apartado de `skills/arnes-upgrade/SKILL.md`; los tres están en el `Archivos:` de este REQ) +
`analista-requerimientos` **sólo si** se decide que `CA-10` salga del alcance de `REQ-023`, y entonces
con su ADR y su REQ destino, porque sacar un criterio del alcance es cambio **de fondo**
(`AGENTS.md` §9) y no una línea de Historial.

**Y lo que NO propongo, porque no es mío:** ni escribo esos tres archivos (soy QA), ni saco `CA-10` del
alcance (eso es write-back del analista), ni decido la ventana.

### Resumen de clases — vuelta 3

| Hallazgo | Clase | Bloquea | Estado |
|---|---|---|---|
| `QA-023-01` | `usuario/dinero` | — | **CERRADO** (vuelta 2; re-verificado hoy: forma 02 de la batería) |
| `QA-023-02` | `contrato` | — | **CERRADO** (vuelta 2) |
| `QA-023-03` | `contrato` | — | **CERRADO** (vuelta 2; su superficie queda nombrada en «Fuera de alcance») |
| `QA-023-04` | `instrumento` | no | **CERRADO** y retirado del campo |
| `QA-023-05` | `contrato` | — | **CERRADO** por el write-back y retirado del campo, **CONDICIONADO a que el write-back se comitee** (hoy sólo vive en el árbol de trabajo: 0 apariciones en `6ad9752`, 8 en local); **clase reconciliada a `contrato`** |
| `QA-023-06` | `instrumento` | no | **CERRADO** en su alcance y retirado del campo; residual → `QA-023-11` |
| `QA-023-07` | `contrato` | — | **CERRADO** (vuelta 2 / write-back) |
| `QA-023-08` | `instrumento` | no | **CERRADO** y retirado del campo |
| `QA-023-09` | `instrumento` | no | **CERRADO** por decisión escrita (retirado por el analista) |
| `QA-023-10` | `instrumento` | no el campo · **sí el gate de fusión** | **nuevo** · severidad **crítica**: pone ROJA la puerta requerida de `main` |
| `QA-023-11` | `instrumento` | no | **nuevo** |
| `QA-023-12` | `instrumento` | no | **nuevo** |
| `QA-023-13` | `instrumento` | no | **nuevo** (abierto por encargo del propio REQ) |
| `QA-023-14` | `instrumento` | no | **nuevo** |
| `QA-023-15` | **`usuario/dinero`** | **sí** | **nuevo** |

**Forma del criterio mal formado** (se anota aquí y **sólo** aquí, nunca dentro del paréntesis de la
clase en `Hallazgos abiertos:`, que es lo que lee `guard-completado`):

- `QA-023-05` → forma **número**. **Cerrada**: el write-back del 2026-09-09 corrigió la magnitud, dejó
  el número declarado **operativo** con dirección hacia abajo y añadió el cuarto parámetro.
- **Ningún hallazgo nuevo de esta vuelta es un criterio mal formado.** Se comprobó expresamente contra
  las cuatro formas de `requirements/README.md`: `CA-09 (iii)` reformulado no cae en (a) —cita el sitio
  único de las claves—, ni en (b) —declara `operativo` con dirección—, ni en (c) —es techo, no
  igualdad—, ni en (d) —mide el **delta** que este REQ introduce, que es justo la corrección—.
  `QA-023-12` roza (b) pero no lo es: lo que falta no es el tipo de un número, es el **estadístico** de
  una dispersión, y el criterio no lo fija a propósito.

### Sobre la pregunta «¿es `CA-09 (iii)` un criterio incumplible disfrazado?» — decidida, porque se me encargó

**No lo es, y el motivo importa.** Un criterio que **abstiene con motivo** en vez de fingir un número es
la conducta que este banco contrata en todas sus sondas (`REQ-021`), y el `SKIP` de `(iii)` es
**conforme**: publica sus parámetros y sus números y no afirma nada. Existe además **solución conforme
medida** —las medianas emparejadas 0,856 y 0,906, con la candidata por debajo de la base en 9 de 10
pares— y una **falsación escrita** que impide que el techo se haya elegido para que la medida quepa. Lo
que hoy falta es **resolución del instrumento**, no satisfacibilidad del criterio, y de las cuatro vías
que el criterio declara para ganarla **tres funcionan** (`QA-023-12` retira la cuarta).

**Lo que sí es, y no lo maquillo: un criterio que no acredita nada hoy, y un caso que en la puerta que
decide sale ROJO midiendo otra cosa.** Abstiene en 5 de 5 corridas locales por ruido del host, y en el
CI **resuelve en FAIL contra el techo que el criterio ya retiró** (`QA-023-10`). Así que no lo cuento
como aprobado, no lo cuento como fallado del producto, y lo digo en la línea `QA:`.

**Y la lección que me llevo sobre mi propio método, escrita porque es la parte útil:** yo leí cinco
«0 FAIL de REQ-023» y los conté como evidencia de que el caso estaba bien. Eran **abstenciones**. Una
abstención repetida no es un verde acumulado — es la misma falta de dato cinco veces, y la máquina
quieta del CI la resolvió a la primera. Comparar la **dispersión entre hosts** era el paso que faltaba,
y no lo di yo.

### Lo que NO acredito, dicho expresamente

Los diez puntos están en `docs/qa/1.34.0-req023-vuelta3-qa-metodo.md` §9. Los tres que más pesan:
**(1)** `CA-09 (iii)` — no acredito ni el techo ni su alcanzabilidad; **(2)** `CA-10` — no acredito
nada, no existe; **(3)** **el banco en verde de `AGENTS.md` §7 — NO lo certifico, y hoy está ROJO**: 4 de
5 corridas locales rojas por reloj, y el **CI** —la puerta requerida y estricta— en **FAIL** por
`CA-09 (iii)`, que es de este REQ (`QA-023-10`). Tampoco acredito que ese rojo desaparezca re-apuntando
el caso: no lo he medido, porque no escribo pruebas.

**Evidencia en disco (método y versión base para re-derivar sin preguntar):**
`docs/qa/1.34.0-req023-vuelta3-qa-metodo.md`.

---

## REQ-023 — revalidación ACOTADA de la extensión excepcional (`808f9ca`)

**Fecha:** 2026-09-09 · **Agente:** `qa-tester` (Opus) · **Árbol validado:** `808f9ca`
**Línea base:** tag `v1.33.0` · **Juez de coste:** CI `hooks-en-linux` **run 34427423326** (PR #45)
**Método y números completos:** `docs/qa/1.34.0-req023-revalidacion-acotada-metodo.md`

**Qué es.** `REQ-023` está `bloqueado` por decisión del propietario tras agotarse las **tres**
vueltas dev↔QA. **No es una cuarta vuelta** y no reinicia el contador: valida **sólo** la extensión
de alcance cerrado que el propietario autorizó — `CA-10`, la prueba de `CA-09 (iii)` y las clases de
`QA-023-12` y `QA-023-15`. Los otros diez criterios conservan el veredicto de la vuelta 3.

### La distinción que ordena todo este apartado

**«Prueba corregida» y «criterio acreditado» son dos cosas, y aquí salen con veredictos distintos.**
La prueba de `CA-09 (iii)` **está corregida**: mide la relación emparejada contra `v1.33.0` con los
cuatro parámetros, y ya no el cociente absoluto contra 2,2. El criterio **NO está acreditado**: los
dos sujetos **abstienen** en el CI, que es la máquina más limpia de que dispongo. Un `SKIP` no
acredita, y el **1 PASS de 10** local tampoco — salió de un rango que cayó bajo el margen por suerte
de la corrida, y una afirmación que depende del sorteo del ruido es **flaky**, no evidencia.

### `CA-10` — **PASA**

Reproducido, no leído: el par por cláusulas **7 de 26 → 26 de 26** con el script del
`desarrollador`; las dos filas **byte a byte idénticas** (md5 `721abd51…`, 1274 bytes) y **una sola**
fila por archivo, así que la del CR fue **sustituida** y no acompañada; las dos vías `awk`
**ejecutadas** contra la puerta real, con el control limpio abriendo y `QA-: aprobado` denegando
mientras el barrido no lo ve; la afirmación de completitud de versiones comprobada sobre **los 41
tags publicados** (0 en todos), no sobre la muestra de 7. Las nueve exigencias de la letra de
`CA-10` pasan una por una (tabla en §1.6 del método).

### `QA-023-15` · **`usuario/dinero`** — **CERRADO**

**Se retira, y no por el texto: por los dos brazos que le daban la clase.** (1) El consumidor ahora
**sí** se entera —el apartado «Hacia 1.34.0» existe, con la frase sin eufemismo, la pregunta por
estado **antes** de cualquier comando y la confesión de que ningún comando la responde— y las dos
herramientas que publica **funcionan como dice**: verifiqué su punto ciego declarado ejecutándolas.
(2) La fila que su coordinadora lee **sí** describe lo que la máquina hace, y lo comprobé
ejerciéndolo: la puerta deniega un guion **ASCII** que ninguna lista de invisibles contiene, que es
la clase «reconstrucción de la clave» que la fila anuncia. El `.tpl` lleva la **misma** fila, así que
llega al proyecto que nace y al que migra. Residual → `QA-023-16`.

### `QA-023-10` · `instrumento` · severidad crítica — **CERRADO**

El caso ya mide la magnitud contratada y **el CI está en 0 FAIL** (957 PASS · 0 FAIL · 9 SKIP,
cuadre 966 — el mismo total que las 955+1+10 de `6ad9752`: **nada se retiró**).

**Y el rojo que este hallazgo reportaba era un ROJO FALSO**, medido por el propio caso nuevo, que
publica los absolutos como control en la misma tanda: la **base `v1.33.0`** paga cocientes
**2,571 · 2,519 · 2,642** (`arnes_campo_linea`) y **2,676 · 2,659** (`arnes_norm_clave`) contra el
techo retirado de 2,200×. El `FAIL` de `2.365× > 2.200×` condenaba una propiedad que **el código
heredado ya pagaba más caro**. Re-apuntar el caso no compró verde: retiró un rojo que no acreditaba
nada.

### `QA-023-12` · `instrumento` · severidad **alta → media** — **SE MANTIENE, misma clase**

**Su primera mitad sigue viva y sin tocar:** el estadístico de dispersión continúa siendo el
**rango** (`disp=$(( hi - lo ))`, `39-…-4-el-coste.sh:312`). El `desarrollador` re-apuntó la
**magnitud** y no el estadístico, así que la vía que `CA-09 (iii)` declara **primera** —«más
tomas»— sigue siendo aritméticamente incapaz de funcionar: el rango es **monótono no decreciente**
en el número de muestras. El CI lo confirma con 3 tomas: rango 0,229 en `arnes_norm_clave` por
**una** toma en 1,138.

**Su segunda mitad se disuelve, y por eso baja la severidad.** Decía que la abstención «oculta el
rojo justo en las máquinas donde se trabaja». Ese rojo era **falso** (arriba, `QA-023-10`): la
abstención no ocultaba un defecto, ocultaba una medición inválida. El **mecanismo** que describía
sigue siendo real —un estadístico que crece con las tomas hace de la abstención el resultado por
defecto, y una abstención se lee como verde—, y ahora abstiene **también en el CI**, en los dos
sujetos. Se mantiene por la primera mitad y por el mecanismo; **media** y no alta porque el daño
concreto que justificaba «alta» no existía.

**Clase `instrumento` confirmada:** el criterio fija el estadístico de **posición** (mediana de la
relación) y **no** el de **dispersión**, así que elegir uno robusto es latitud del `desarrollador`,
no una afirmación falsa del REQ. **Sigue en pie la condición de ascenso escrita en la vuelta 3:** si
un write-back futuro fija «el rango», sube a `contrato`. **No bloquea.**
*Dueños:* `desarrollador` (el estadístico) + `analista-requerimientos` (si el criterio debe fijarlo).
*Forzador:* la próxima vez que se intente acreditar `CA-09 (iii)`. *Ventana:* **1.35.0 propuesta.**

### `QA-023-16` · `instrumento` · severidad **baja** — NUEVO: **la entrada de migración no dice que la fila de `§13` quedó vieja, y es la única entrada de la skill que cambia una fila de `§13` sin decirlo**

**Pasos de reproducción.**
```
awk 'NR>=738' skills/arnes-upgrade/SKILL.md | grep -n '§13'     -> vacío
grep -n '`AGENTS.md` §13' skills/arnes-upgrade/SKILL.md          -> 5 entradas anteriores sí lo anuncian
```
**Esperado:** la fórmula establecida por cinco precedentes de esta misma skill («`AGENTS.md` §13:
fila nueva» / «dos filas nuevas» / «fila “…”»), para que quien migra sepa que **su** fila quedó
vieja. **Observado:** la entrada «Hacia 1.34.0» no menciona `§13` en ninguna línea.

**Por qué NO bloquea y por qué NO es `CA-10` incumplido:** la **sustancia** sí llega —el bullet «Qué
cambia en el código, y qué no» reenuncia la invariante entera, con la reconstrucción de la clave y
las tres fronteras—, así que el operador **aprende lo que la máquina hace**; lo que le falta es el
puntero de que debe reescribir su propia fila. Y `CA-10` **no exige** ese puntero: exige que la fila
esté escrita por propiedad en los dos archivos (lo está) y que el apartado diga lo que dice (lo
dice). Nadie ve, decide ni cobra distinto por el puntero ausente.
*Dueño:* `desarrollador`. *Forzador:* la próxima entrada de migración que toque `§13`.
*Ventana:* **1.35.0 propuesta.**

### `QA-023-17` · `instrumento` · severidad **media** — NUEVO: **la afirmabilidad de `CA-09 (iii)` empeora cuanto MEJOR se porta el código, y diverge en el resultado deseado**

**Pasos de reproducción.** Leer la cláusula de afirmación de `CA-09 (iii)` («el techo sólo se puede
AFIRMAR con margen mayor que la reproducibilidad demostrada en la misma corrida») y aplicarla a los
dos veredictos del CI run 34427423326.

**El argumento es aritmético, no de opinión.** La condición es
`dispersión < margen`, con `margen = 1,000 − mediana`:

| sujeto | mediana | margen | rango (CI, 3 tomas, k=200) | reducción necesaria |
|---|---|---|---|---|
| `arnes_campo_linea` | 0,952 | **0,048** | 0,055 | ×1,15 — al alcance |
| `arnes_norm_clave` | 0,986 | **0,014** | 0,229 | **×16** — fuera de alcance |

La **mediana no es un parámetro de diseño**: es la realidad medida de cuánto mejor es la candidata
que la base. Para `arnes_norm_clave` vale **0,986**, o sea «prácticamente igual» — que es
**exactamente lo que el criterio contrata** («no empeora»). Entonces `margen > dispersión` pide
resolver una diferencia **más pequeña que el efecto real**: con mediana 0,986 hace falta
reproducibilidad mejor del **1,4 %** sobre una **relación de relaciones**, que compone cuatro
cronometrajes y multiplica el ruido relativo por ≈2 → **<0,7 % por cronometraje**. La consecuencia,
dicha sin adorno: **un código que no cambia nada el orden de crecimiento —el resultado deseado— es
el más difícil de acreditar**, y la exigencia diverge cuando la mediana tiende a 1,000.

**Esperado:** que la cláusula de afirmación tenga una salida que no dependa de que la realidad se
aparte del techo — por ejemplo un **par mayor** obligatorio (que baja la mediana y **fabrica**
margen), o un umbral de afirmación **relativo a la resolución del instrumento** en vez de a la
distancia al techo. **Observado:** la única vía que ataca el margen (par mayor) está declarada como
una de tres alternativas, y las otras dos no lo atacan.

**Por qué `instrumento` y no `contrato`:** el REQ **no afirma** que `(iii)` esté acreditado —el campo
`QA:` dice lo contrario— ni afirma nada falso sobre lo construido; y es coste de un lector del propio
arnés, así que nadie ve, decide ni cobra distinto. **No bloquea.**
*Dueño:* `analista-requerimientos` (la forma de la cláusula) + `desarrollador` (los parámetros).
*Ventana:* **1.35.0 propuesta — la decide el propietario.**

### Qué evidencia falta EXACTAMENTE para acreditar `CA-09 (iii)`

No «más medición»: **par mayor, `k` mayor y un estadístico robusto**, que es la vía que el propio
criterio declara conforme. Protocolo concreto: par **2000→4000**, `k`=**600**, **5** tomas,
dispersión por **rango intercuartílico o MAD** en vez del rango, forma «clave» contratada, contra
`v1.33.0`, **en el CI**. Coste estimado ≈2–3 min del job (hoy 1 m 33 s). Con eso
`arnes_campo_linea` acredita con holgura; `arnes_norm_clave` tiene una posibilidad real **sólo**
gracias al par mayor, porque ahí el problema no es el ruido, es que **no hay margen**.

**Y las tres prohibiciones del propietario no hacen falta para nada de esto:** no se sube el techo,
no se cambia la forma contratada y no se retira ninguna prueba. Verificado sobre el árbol: techo
`1000` por mil = **1,000×**, nota de falsación intacta (`39-…-4-el-coste.sh:236-237`),
`CASOS_ESPERADOS_SECCION=4` **antes y ahora**, y cuadre del banco **966 en las dos** corridas de CI.

### Resumen de clases — revalidación acotada

| Hallazgo | Clase | Bloquea | Estado |
|---|---|---|---|
| `QA-023-10` | `instrumento` | no | **CERRADO** — CI 0 FAIL, y el rojo era falso |
| `QA-023-11` | `instrumento` | no | abierto (no revalidado: fuera de alcance) |
| `QA-023-12` | `instrumento` | no | **abierto**, misma clase, severidad **alta→media** |
| `QA-023-13` | `instrumento` | no | abierto (no revalidado: fuera de alcance) |
| `QA-023-14` | `instrumento` | no | abierto (no revalidado: fuera de alcance) |
| `QA-023-15` | `usuario/dinero` | — | **CERRADO** — `CA-10` cumplido y los dos brazos comprobados |
| `QA-023-16` | `instrumento` | no | **nuevo** |
| `QA-023-17` | `instrumento` | no | **nuevo** |

**Ninguno de los abiertos es `usuario/dinero` ni `contrato`.** El bloqueante desapareció.

### Defecto concreto que justifique otra ronda de desarrollo: **ninguno**

Lo busqué donde correspondía —la puerta con fixtures propios, las dos vías `awk` ejecutadas, el
cuadre de casos, los 41 tags, el CI— y no encontré ningún defecto del producto. Lo que queda es
**evidencia pendiente** (`CA-09 (iii)` sin acreditar) y **deuda de instrumento con dueño y ventana**,
no trabajo de una ronda. `QA-023-17` es un hallazgo contra el **criterio**, no contra el código, y su
dueño es el analista.

---

## REQ-023 · `SEC-079` — revisión **ACOTADA al cambio** de la mitad de texto · commit **`1154417`**

**Árbol validado: el COMITEADO `1154417`**, en `git worktree --detach`, **no** el de trabajo (tenía
WIP vivo de otro agente en `REQ-024`). Padre `89440e9` como línea base A/B. Método, comandos y
cifras enteras: **`docs/qa/1.34.0-sec079-fila-metodo.md`**.

Alcance por instrucción del propietario: la cláusula nueva de `CA-10` y su control negativo, las dos
filas, la asimetría fila/`CA-01`, el conteo de `CA-12 (iii)`, el CI y mi veredicto. **No** se
revalidó `REQ-023` entero. **`CA-09 (iii)` sigue sin acreditar** y no se re-midió.

### Lo que PASA, medido

| | Comprobación | Resultado |
|---|---|---|
| B | dos filas byte a byte idénticas | `md5 1f4384ed…`, **2287 B**, `cmp` sin diferencias |
| B | **una sola** fila por archivo, la anterior **sustituida** | 1 y 1 · delta **1/1** por sede |
| B | promesa absoluta vieja **borrada** | `grep` = **0** en las dos sedes |
| B | la `Е` del ejemplo es **cirílica** (`d0 95` = U+0415) | sí, en las **dos** apariciones; **cero** `E` latinas en el ejemplo |
| B | cita por **ID** y no por línea | `R-024`, `SEC-078`, `SEC-079` presentes; **cero** citas `:69xx`/`:70xx` — **correcto**: en el proyecto que nace del template esas líneas no existen, y `CA-10 (ii)` pide los **números de hallazgo**, no líneas |
| A | el **control negativo DISCRIMINA** | R0 (fila anterior) **incumple** (iii) · R1 (+«no exhaustiva») **sigue incumpliendo** · R1b (+puntero) **sigue incumpliendo** · R2 (fila nueva) cumple en las oraciones 2–3 |
| B | la fila dice verdad sobre el **homóglifo** | ejecutado: `Sensible а seguridad: sí` + `Rigor: ligero` + veredictos `pendiente` → **ALLOW**; `Еstado: completado` → **ALLOW** con salida vacía (permite **por ausencia**) |
| E | `CA-12 (iii)` cuenta **tres** | 9 ítems antes y después; las tres superficies presentes 1 vez cada una; `CA-01`, `CA-03`, `CA-11`, `CA-12` **byte a byte idénticos**; **12** criterios antes y después |
| F | quality gates de `AGENTS.md` §7 | las **tres** en verde sobre `1154417` |
| F | cuadre del banco | **966** en las tres corridas (padre, este árbol, CI) |

### `QA-023-18` · **`contrato`** · severidad **media** — la promesa absoluta se borró de la oración 2 y **sigue en la oración 1**, que es la que se lee

**Pasos.** `sed -n '349p' AGENTS.md`, y leer **oración a oración** como `CA-10 (iii)` prescribe.

**Oración 1, literal:** «Una línea de la cabecera que la máquina **no puede medir** no deja cerrar —
la propiedad es el **estado**, no el carácter: […] una persona lee ahí un campo y la máquina no lo
lee como ese campo».
**Oración 6 de la MISMA fila:** «la **sustitución de una letra de la clave por un homóglifo**
(`Еstado`…) **permite, y permite por ausencia** […]; **ninguna** versión lo deniega, **tampoco
1.34.0**».

**Esperado:** ninguna oración promete denegación o no-permisividad sin su condición — es la letra de
`CA-10 (iii)`, «se lee **oración a oración** y **basta una**».
**Observado:** el homóglifo satisface **exactamente** la caracterización de la oración 1 —«una
persona lee ahí un campo y la máquina no lo lee como ese campo», que es la propiedad de `CA-01`, y
la oración dice **«la propiedad es el estado, no el carácter»**— y sin embargo **permite**, medido
(`ALLOW`, sonda `probe-e2e.sh` casos 4 y 5). Aplicado literalmente (iii), **R2 incumple por su
oración 1**.

**La ambigüedad, dicha entera, porque es el defecto:** la misma oración 1 ofrece **dos**
caracterizaciones incompatibles de la clase — «la línea **lleva dentro algo**» (inserción: excluye al
homóglifo, que **sustituye**) y «una persona lee ahí un campo y la máquina no lo lee como ese campo»
(estado: **incluye** al homóglifo) — y zanja a favor de la segunda con «la propiedad es el estado, no
el carácter». La acotación que la salva llega en la **oración 3**, a ~700 bytes, después de la
oración que un lector de tabla toma **como** la invariante: la columna «Invariante» de `§13` se
escanea por su encabezado, y ése es el uso para el que la tabla existe.

**Lo que NO es, para que nadie lo lea de más:** no reabre ningún fail-open, no reintroduce la frase
borrada, y la acotación explícita y el nombramiento de la vía **sí están** en la celda. La condición
del propietario está cumplida **en sustancia**. Lo que falla es que la **promesa principal** —la
oración titular, palabra por palabra la misma que antes del write-back— **no se acotó**, y es
justamente la forma con la que el defecto de `SEC-079` puede volver.

**Arreglo (del analista, en `CA-10`, y del `desarrollador` en las dos sedes):** una subordinada en la
oración 1 —«…no deja cerrar **dentro de lo que la guarda alcanza (abajo)**»— o mover ahí la
acotación. Es un pase de redacción, no trabajo.

### `QA-023-19` · **`contrato`** · severidad **media** — `CA-10` punto 1, y la fila que lo transcribe, describen una reconstrucción que el código **no hace**

**Pasos.** Sonda `probe-clave-oculta.sh` (llama a `_arnes_clave_oculta`) y `probe-e2e.sh` (ejecuta
`hooks/guard-completado.sh` con su JSON real), las dos sobre `wt-1154417`.

**Texto contratado** (`CA-10` punto 1, la fila en las dos sedes, y la entrada del Historial):
«retirado de la clave lo ajeno al alfabeto de las claves, **repuesto un blanco en el sitio de la
retirada y colapsados los repetidos**, **si lo que queda es una clave del lector** […] se deniega
[…]; si esa reconstrucción **no** devuelve ninguna clave, la guarda **calla** y la puerta resuelve
por **ausencia**».

**Código** (`hooks/lib.sh:1841-1843`): retira lo ajeno al alfabeto **y TODOS los blancos**, y
compara contra la clave **sin sus blancos**. Su propio comentario (`:1826-1828`) lo dice así. En
`hooks/` y `tools/` **no existe** ninguna reposición de blanco ni colapso de repetidos.

**Esperado vs observado** — tres divergencias medidas, y una es la instancia que la propia fila
nombra primero:

| Entrada | Lo que dice el texto | Lo que hace la máquina |
|---|---|---|
| `Sensibleaseguridad` (blanco **borrado**) | nada se retiró → no reconstruye → **calla y permite por ausencia** | **DENY** (`oculta=1`, clave `Sensible a seguridad`; e2e caso 3) |
| `Est\xc2\xa0ado` (NBSP **dentro** de la palabra) | → `Est ado`, que no es clave → **calla y permite** | **DENY** (clave `Estado`) |
| `\xef\xbb\xbfEstado` (**BOM**, «el que PowerShell añade al redirigir», nombrado en la misma fila) | → ` Estado`, que no es clave → **calla y permite** | **DENY** (clave `Estado`) |

Ni con la lectura estricta («hay que reponer el blanco») ni con la benévola («se prueba también con
el blanco repuesto») el texto reconstruye nada en el **primer** caso: la divergencia no depende de
cómo se lea.

**Y la celda se contradice consigo misma:** su oración 12 —heredada, y **correcta**— dice «retirado
de la clave lo ajeno al alfabeto de las claves **y sus blancos**, si lo que queda **es** una clave del
lector, alguien insertó algo dentro». Dos descripciones **no equivalentes** de la misma guarda en la
misma celda; un lector no puede saber cuál rige.

**Dirección del error, dicha porque cambia la urgencia:** el texto describe **MENOS** cobertura de la
que la guarda tiene. **No** reabre nada y **no** es la falta de `SEC-079` otra vez. Pero es
literalmente «el REQ afirma algo falso sobre lo construido», que es la definición de `contrato`, y se
cierra con el mismo write-back que `QA-023-18` — un solo pase: `CA-10` punto 1, las dos filas y la
entrada del Historial que lo repite.

**Arreglo:** escribir la reconstrucción **como es** —«retirado lo ajeno al alfabeto **y todos los
blancos**, si lo que queda es una clave del lector **sin sus blancos**, deniega»—, que además ya
está escrita bien en la oración 12 de la propia fila y en `CA-03`.

### `QA-023-20` · `instrumento` · severidad **media** — **ninguna** sección del banco lee las dos filas: la identidad entre sedes se acredita a mano y nada caza una divergencia futura

**Pasos.** `grep -rln "AGENTS" tests/ hooks/ tools/` sobre `1154417`.
**Observado:** los únicos casos que abren esos archivos son
`36-cabecera-comentario-html-4-el-informe-y-los-textos.sh:183-187` (comprueba un texto de `REQ-016`)
y `34-arnes-paralelo.sh:25,79` (usa la ruta como cadena). **Nada** lee la fila de `§13`, ni su
identidad byte a byte entre sedes, ni la presencia del nombre de la vía, ni la ausencia de la
promesa absoluta.
**Consecuencias, las dos:** (a) por esta vía el cambio **no puede** mover un veredicto del banco —es
la mitad de la respuesta al punto F, y es una respuesta **por construcción**, no por suerte—; y (b)
`CA-10` es hoy el único criterio de este REQ cuya comprobación es **manual**, así que una divergencia
futura entre `AGENTS.md` y `templates/AGENTS.md.tpl` —la cuarta forma que el propio `CA-10` punto 4
declara como incumplimiento— **no la caza nada**.
**Dueño:** `desarrollador`; es exactamente el caso de banco de `CA-10` que ya está enrutado y que
esta comisión tiene prohibido escribir. **No bloquea.**

### `QA-023-21` · `instrumento` · severidad **media** — la promesa absoluta **sobrevive en el mensaje de la puerta**, que es la sede que una persona lee en el momento de decidir

**Pasos.** `grep -n "NUNCA permite por AUSENCIA\|Nunca permite por AUSENCIA" hooks/guard-completado.sh`
sobre `1154417`.
**Observado:** las dos ramas de denegación siguen diciendo, **sin condición**, lo mismo que se borró
de `AGENTS.md`: `:363` «…y **NUNCA** permite por AUSENCIA del campo que ese caracter borro» y `:392`
«**Nunca** permite por AUSENCIA del campo que ese caracter borro».
**Por qué es `instrumento` y no más:** la **decisión** de la puerta no cambia por esto —es el texto
del propio control—, y el mensaje sólo se emite **cuando ya denegó**, o sea en el caso en que su
afirmación local es cierta. Lo que es falso es su alcance **general**, y quien lo lee es la misma
persona a la que `SEC-079` protegía.
**Dueño:** `desarrollador` (es `hooks/`, y no lo toco). **Y va nombrado aquí para el
`auditor-seguridad`:** es la misma clase de `SEC-079` en una **tercera sede** que su hallazgo no
enumeró; si él juzga que un mensaje de puerta es una promesa de cobertura hacia la persona, subir la
clase es su prerrogativa y no la mía.

### El FAIL del CI y el FAIL local: ninguno es hallazgo de esta revisión

- **CI (`hooks-en-linux`, run `34431267106`, 954 · 1 · 11):** el FAIL es
  `REQ-017 CA-03 fail-before` (2.329× bajo el techo 2,600×). Mide el tag **`v1.32.1`**, un árbol
  **inmutable**; y el **mismo caso** en mi máquina dio **4.667×** sobre el padre y **3.562×** sobre
  este commit —mismos sujetos, 1,1× de deriva entre dos corridas—. Es la clase de `SEC-080` («un
  umbral de la puerta requerida cuyo veredicto lo decide el ruido de la máquina»). **Escalado por la
  coordinadora; fuera de mi alcance.** Confirmo por mi vía que **no** lo mueve el cambio de texto.
- **Local (957 · 1 · 8):** `DEV v3 heredoc CITADO ~300 KB`, 4450 ms sobre el techo holgado de
  4000 ms, con veredicto `allow` **correcto**. Repetido aislado **5/5 PASS** a 3224–3970 ms: el peor
  caso pasa por **30 ms**. **Flaky ya registrado** en este documento (corridas B y C: 4466 y
  4357 ms). No lo abro de nuevo.

### El único efecto medible del cambio sobre el banco, y va hacia el lado bueno

El censo del corpus de `CA-04` sube (5336→**5356** líneas con campo; 33→**34** donde la guarda
dispara) porque ese corpus es `cat requirements/*.md` **entero**
(`39-…-5-el-corpus.sh:175-179`) y `REQ-023.md` creció ~137 líneas. Pasando por el **evaluador del
propio banco** sólo las líneas añadidas: `RESUMEN 42 21 29 41 1` — la que dispara es una línea del
**cuerpo** del Historial con `` `QA-: pendiente` `` (guion ASCII, reconstruye la clave `QA`). El
cuerpo **no** se restringe (`CA-01`, tercera frontera) y el caso siguiente sigue midiendo **0
divergencias** con la guarda **sin disparar en ninguno de los 27 REQ del árbol**. El suelo de
anti-vacuidad queda **más** satisfecho, no menos.

### La asimetría fila / `CA-01`: **honesta**, con una condición

Acotar la **fila** y no `CA-01` es correcto y no es la coartada de `requirements/README.md`
§ «Y el reverso», por tres razones **verificables en el árbol** y no por el motivo escrito:

1. **La exigencia no se relajó, y se puede comprobar:** `CA-01` es **byte a byte idéntico**
   (`md5 4d0d1495…`) al del padre, igual que `CA-03`, `CA-11` y `CA-12`. Nada bajó de nivel.
2. **El incumplimiento queda VIVO y nombrado en tres sitios que se cuentan:** `CA-11` («sigue
   **incumpliendo CA-01**, y se corrige en el código, nunca ensanchando esta frontera»),
   `CA-12 (iii)` (las **tres** superficies, contadas: 3/3) y § «Fuera de alcance» (9 ítems, dueño y
   ventana propuesta). Relajar `CA-01` habría hecho desaparecer el problema **por definición**;
   acotar la fila lo deja **medible y contable**, y de hecho **lo publica**: la fila ahora dice en
   voz alta que la vía **permite** y que «**ninguna** versión lo deniega, **tampoco 1.34.0**».
3. **Los dos textos tienen lectores y usos distintos, y eso es comprobable, no retórico:** un
   criterio se lee para **construir**; la fila de `§13` se lee para **decidir si auditar los REQ ya
   cerrados**. Un criterio optimista no engaña a nadie —el REQ está `bloqueado` y su incumplimiento
   está escrito—; una fila optimista sí, y a un tercero que no participó.

**La condición:** esa honestidad vive en la celda **completa**. Mientras la **oración titular** siga
prometiendo sin condición (`QA-023-18`), la fila conserva una lectura —la única que hace quien
escanea la columna «Invariante»— en la que el problema **sí** desapareció. Por eso `QA-023-18` es
`contrato` y no una nota de estilo: es el punto exacto por donde «acotar la fila» se convertiría en
la coartada que hoy **no** es.

### Resumen de clases — revisión acotada de `SEC-079`

| Hallazgo | Clase | Bloquea | Estado |
|---|---|---|---|
| `QA-023-18` | **`contrato`** | **sí, hasta el write-back** | **nuevo** |
| `QA-023-19` | **`contrato`** | **sí, hasta el write-back** | **nuevo** |
| `QA-023-20` | `instrumento` | no | **nuevo** — dueño `desarrollador` |
| `QA-023-21` | `instrumento` | no | **nuevo** — dueño `desarrollador`, avisado al auditor |
| `SEC-079` | `contrato` | sí | **abierto** — no lo cierro yo, por condición del propietario |
| `QA-023-11/12/13/14/16/17`, `SEC-078`, `SEC-080` | `instrumento` | no | abiertos, sin revalidar (fuera de alcance) |

**Un solo pase de write-back cierra los dos `contrato`**, y es el mismo texto: `CA-10` punto 1 y su
oración titular, transcritas a las dos sedes. Ninguno de los cuatro exige una ronda de desarrollo de
código: **cero** hallazgos contra `hooks/`, y la guarda hace **exactamente** lo que `CA-03` y la
oración 12 de la fila describen — medido entrada por entrada.

### Mi veredicto, y la cuenta que el analista dejó abierta

El analista escribió que mi `QA: aprobado` sobre `808f9ca` **no cubre** la cláusula nueva de `CA-10`
y no tocó el campo, «porque desfirmar por otro es la misma falta que firmar por otro». **Correcto, y
lo resuelvo yo:** extiendo el veredicto a la cláusula nueva y el resultado es que **no pasa**. El
campo pasa a **`QA: con-hallazgos` sobre `1154417`**, conservando en el propio campo qué sigue
acreditado sobre `808f9ca` —11 de 12 criterios, con `CA-09 (iii)` sin acreditar— y qué no lo está.
Lo que valía sigue valiendo; lo nuevo no se firma.

---

## REQ-023 · `SEC-079` — revalidación ACOTADA **vuelta 2 del texto** · commit **`b5a29d9`**

**Árbol validado:** el **comiteado `b5a29d9`**, en `git worktree --detach`, **no** el de trabajo (el
`desarrollador` tenía WIP vivo en `tests/…/37-coste-del-escaner-2-las-razones.sh`). **Fecha:**
2026-09-09. **Alcance del encargo:** `QA-023-18`, `QA-023-19`, `CA-12 (ii)` y si `SEC-079` queda listo
para la firma del auditor. **No es una cuarta vuelta** y no reinicia el contador.
**Método y evidencia completos:** `docs/qa/1.34.0-req023-sec079-vuelta2-metodo.md`.

### Señales automáticas, sobre el árbol validado

| Señal | Resultado |
|---|---|
| Las 3 quality gates de `AGENTS.md` §7 | **las 3 OK** |
| Banco completo local (rc 0) | **988 PASS · 0 FAIL · 8 SKIP** (996 casos, sin descuadre) |
| CI `hooks-en-linux` (puerta requerida, PR #45, run `34436151034`) | **pass** — 985 PASS · 0 FAIL · 11 SKIP (996 casos) |

El **único FAIL** de la vuelta anterior (`REQ-023 CA-12`) ya no existe. El desvío local↔CI es de **3
casos** de la familia **`SEC-080`** (umbrales de reloj cuyo veredicto lo decide el ruido de la
máquina): **0 FAIL en las dos** y el mismo total. El SKIP de `CA-12` es **byte a byte el mismo** en
las dos corridas → determinista, no flaky.

### Veredicto por hallazgo

| Hallazgo | Clase | Veredicto |
|---|---|---|
| **`QA-023-18`** | `contrato` | **SATISFECHO — se retira** |
| **`QA-023-19`** | `contrato` | **SATISFECHO — se retira** |
| `CA-12 (ii)` (el SKIP) | — | **abstención LEGÍTIMA**, con par discriminante |
| **`QA-023-22`** | `instrumento` | **nuevo** — dueño `desarrollador` |
| `SEC-079` | `contrato` | **listo para la firma del auditor** — sigue abierto: no lo cierro yo |

### `QA-023-18` — se retira

La titular ahora lleva su condición **dentro**: «no deja cerrar **dentro de lo que la guarda alcanza
(acotado en esta misma celda; hay vías medidas que hoy PERMITEN)**». Es la subordinada que `CA-10`
punto 1 declara suficiente, con el puntero a la acotación de la propia celda, y **juzgada aislada es
verdadera**: no promete denegación universal y además publica que hay vías que permiten. Ya no se
contradice con la oración 6.

Barrido de **(iii)** enumerando las **8** oraciones (tabla completa en el método): **ninguna** promete
denegación o no-permisividad sin su condición. Dejo explícito el único punto discutible —la oración 2,
«Se deniega citando…»— y por qué no la cuento: `CA-10` exige el aislamiento **sólo** para la titular,
y la 2 describe la **forma** de la denegación, no su alcance.

**Control negativo (iv): discrimina CUATRO candidatas** — (a) `89440e9` `721abd51`/1274 B
**incumple**; (b) (a)+«no exhaustivas» **incumple**; (c) `1154417` `1f4384ed`/2287 B **incumple** por
su titular; (d) `b5a29d9` `3ead3bd3`/2545 B **cumple**. Separa (c) de (d), que era la candidata que
este write-back tenía que distinguir.

Y el delta `1154417`→`b5a29d9` es **aditivo**: dos cambios (la subordinada y la oración 5), **8
oraciones antes y 8 después**. Ninguno de los dos defectos se arregló **borrando** la oración
incómoda, que es la salida que `CA-10` punto 4 prohíbe por su nombre.

### `QA-023-19` — se retira

Reproducidas **todas** las medidas del `desarrollador`: fragmento **165 B** / `md5`
**`596a237f…`**, **2 + 2 + 1** apariciones, **0** restos de la redacción vieja en las tres sedes, las
dos sedes **idénticas byte a byte** (`3ead3bd3…`, **2545 B**, `cmp` sin diferencias). «Ninguna con una
letra distinta» comprobado **contando la cadena**: el total de «retirado de la clave» por sede es
2/2/1, igual que el del fragmento exacto.

**Punto (v), ejecutando la puerta real:** las tres entradas que divergieron dan **DENY** —el blanco
borrado (`Sensibleaseguridad`), el NBSP dentro (`Est\xc2\xa0ado`) y el BOM (`\xef\xbb\xbfEstado`)—,
con el byte en forma **imprimible**. Y el texto de la celda **deriva DENY para las tres** (derivación
paso a paso en el método), en **las dos** apariciones, que son el mismo fragmento. La oración 6 además
es **verdadera medida ejecutando**: el homóglifo da **allow** en las dos formas.

**¿«Fragmento, no oración» relaja algo? No — es más estrecha**, y coincido con el analista por razón
medible: «frases equivalentes» admite cualquier paráfrasis, y una paráfrasis es exactamente lo que
produjo `QA-023-19`; la identidad byte a byte no admite ninguna. Que la apódosis quede libre no afloja
nada, porque el mecanismo vive entero en la prótasis y la consecuencia queda clavada aparte por **(v)**.
La letra anterior era además **insatisfacible** sin borrar una de las dos apariciones.

### `CA-12 (ii)` — el SKIP es LEGÍTIMO, no evasión

Cuatro vías independientes:

1. **La precondición es la correcta:** el cuerpo de `arnes_cola_pendientes` **y el de cada función que
   llama**, con el **mismo** extractor en las dos versiones. Es la letra de `CA-12 (ii)`.
2. **La enumeración es exhaustiva HOY, comprobado mecánicamente** (no leído): los únicos tokens
   `arnes_*` del cuerpo son su propio nombre y `arnes_lee_archivo`; la única otra dependencia es la
   **variable** `ARNES_TEXTO`, que fija `arnes_lee_archivo`, cuyo cuerpo es **idéntico**.
3. **Las huellas reproducen exactamente:** `arnes_cola_pendientes` `a060be56`/3883 B contra
   `008683e6`/1103 B (**difiere**), `arnes_lee_archivo` `e9fedaa8`/408 B (**idéntico**). Que la
   **compartida** sea idéntica es lo que hace honesta la atribución a `REQ-024`.
4. **El SKIP publica lo suficiente para reconstruirlo:** qué difiere, las cuatro huellas con sus
   bytes, y las formas movidas **elemento por elemento** — `rango-sin-cerrar` `0|rc=0`→`|rc=1` y
   `cierre-huerfano` `0`→`1`, **2 de 7**, las otras 5 idénticas. Son **exactamente** las dos que
   `REQ-024 CA-08`/`CA-09` declaran cambiar.

**EL PAR DISCRIMINANTE — sin esto un SKIP no discrimina.** Con hooks sintéticos = `v1.33.0` sin tocar
**+ una redefinición de `arnes_cola_pendientes` añadida al final** (el extractor toma la **primera**
definición, así que la precondición ve los cuerpos **idénticos** y la conducta viene de **fuera**),
inyectados con `ARNES_HOOKS_DIR`:

| Corrida | Cuerpos | Conteos | Veredicto |
|---|---|---|---|
| `b5a29d9` contra `v1.33.0` | DIFIEREN | 2 de 7 movidas | **SKIP** |
| `v1.33.0` contra sí mismo | idénticos | 7 de 7 iguales | **PASS** |
| **sintético** | **idénticos** | **7 de 7 movidas** | **FAIL** |

**Las tres ramas están vivas.** El SKIP no es una salida incondicional: es la rama del par de árboles
que ya no permite atribuir. Un FAIL ahí habría medido el defecto de `REQ-024`, no de este REQ.

### Falsación adicional — sin fail-open nuevo

Variantes sobre el conjunto conocido de claves: mayúsculas (`ESTADO:`), espacio antes del `:`, sin
espacio tras el `:`, blanco doble, ZWSP, cabecera vacía, hallazgo sin clase. El discriminante fue
repetir cada variante con **`QA: pendiente`**: las de capitalización y blancos **deniegan**, o sea el
campo **sí** se resuelve y su `allow` inicial era correcto (veredictos en verde), no un fail-open.
Blanco doble, ZWSP y hallazgo sin clase **deniegan**. **No apareció ningún fail-open fuera del ya
declarado** (`SEC-078`, el homóglifo, fuera de alcance con su reparto).

> **Instrumento corregido a mitad de camino, y se dice.** La primera tanda corrió en un proyecto
> sintético **sin `hooks/` ni `tools/`**: las tres quality gates fallaban y **todo** salía `deny` por
> un motivo ajeno al que se medía — un falso «la puerta cierra». Se repobló el proyecto y se repitió
> todo. Los `deny` de medibilidad de `(v)` no estaban afectados (citan `INSERTADO DENTRO DE LA CLAVE`,
> que se evalúa antes de las gates), pero se re-corrieron igual.

### `QA-023-22` (`instrumento`) — nuevo

La precondición de `CA-12 (ii)` **fija a mano** la lista que compara
(`for fn93 in arnes_cola_pendientes arnes_lee_archivo`, `39-…-3-…sh:282`) en vez de **derivarla** del
cuerpo, y nada en el banco vigila que siga completa. Si esa función gana una tercera llamada, la
precondición compararía dos cuerpos idénticos y podría emitir **PASS** mientras el tercero difiere.
Segunda mitad medida: el extractor toma la **primera** definición a columna cero, así que una
redefinición posterior le es invisible (así se construyó el par discriminante); hoy `lib.sh` no tiene
ninguna duplicada, así que **no hay defecto vivo**.

**`instrumento` y no `contrato`** porque el REQ **no** afirma nada falso: dice «hoy sólo
`arnes_lee_archivo`» y hoy es verdad, comprobado mecánicamente. Nadie ve, decide ni cobra distinto por
esto; el defecto está en la **prueba**. Dueño **`desarrollador`**; forzador **que se arma solo** —la
primera llamada nueva en ese cuerpo, y `REQ-024` ya edita esa función—; vencimiento propuesto
**1.35.0** (lo decide el propietario).

### `SEC-079` — **listo para la firma del auditor**

Los dos `contrato` que yo abrí se retiran, `CA-10` se cumple en sus cuatro puntos y en sus cinco
comprobaciones (i)–(v), las tres quality gates están en verde, el banco da **0 FAIL** y la puerta
requerida de `main` **pasa sobre `b5a29d9`**. No queda ningún hallazgo `usuario/dinero` ni `contrato`
**mío** abierto. **`SEC-079` sigue abierto porque su dueño es el auditor**: no lo cierro, no firmo
`Seguridad:` y no toco `Estado:` — el REQ sigue `bloqueado` y su desbloqueo es del propietario.

### Cierre de clases de esta vuelta

| Hallazgo | Clase | ¿Bloquea el cierre? | Estado tras esta vuelta |
|---|---|---|---|
| `QA-023-18` | `contrato` | — | **RETIRADO** (satisfecho, verificado) |
| `QA-023-19` | `contrato` | — | **RETIRADO** (satisfecho, verificado) |
| `QA-023-22` | `instrumento` | no | **nuevo** — dueño `desarrollador` |
| `SEC-079` | `contrato` | **sí** | abierto — del auditor, listo para firma |
| `QA-023-11/12/13/14/16/17`, `QA-023-20/21`, `SEC-078`, `SEC-080` | `instrumento` | no | abiertos, fuera de alcance |

### Addendum — `HEAD` avanzó a `48b7808` durante esta revisión, y por qué el veredicto sigue en pie

Mientras corría esta validación, el `desarrollador` comiteó **`48b7808`** («Mi diagnostico de k=1 era
falso en su mecanismo…»), que además **arrastró al commit** este artefacto de QA y el método, todavía
en curso. Se dice porque cambia el árbol al que apunta `HEAD`, no lo que se midió:

- **Lo validado sigue siendo `b5a29d9`**, y por eso el veredicto lo nombra. La validación se hizo en
  `git worktree --detach`, así que ningún commit ajeno pudo contaminarla.
- **`48b7808` no toca nada de lo que aquí se acredita.** Comprobado:
  `git diff --stat b5a29d9 48b7808 -- AGENTS.md templates/AGENTS.md.tpl skills/arnes-upgrade/SKILL.md
  hooks/ tools/ tests/…/39-caracter-invisible-3-…sh` → **sin salida**, es decir **byte a byte
  idénticos**. Lo que cambia es `tests/…/37-coste-del-escaner-2-las-razones.sh` (el WIP vivo del dev,
  fuera de mi encargo) y evidencia en `docs/arnes/req-017-ca-03-modo-de-medicion/`.
- **Consecuencia:** para la superficie en alcance, el veredicto sobre `b5a29d9` vale igual sobre
  `48b7808`. La puerta requerida del PR, en cambio, está en verde sobre **`b5a29d9`**: si se fusiona
  `48b7808`, el CI tiene que volver a pasar sobre él —cosa que no depende de este REQ, porque el rojo
  o verde de `37-*` es de `REQ-017`—.

---

## REQ-024 · vuelta 1 de QA · commit **`d54761e`** (rama `rel/registro-1.33.0`)

**Árbol validado:** `d54761e`. **HEAD avanzó a `3e49b98`** durante la validación (comisión ajena de
`REQ-017`): verificado que **toda la superficie de `REQ-024` es byte a byte idéntica** entre
`d54761e`, `3e49b98` y el árbol de trabajo sobre el que corrí el banco (`git diff` sobre `hooks/`,
`tools/`, `.arnes/config.json`, `templates/`, `AGENTS.md`, `docs/decisions/`,
`skills/arnes-upgrade/SKILL.md`, `tests/…/run.sh`, `tests/…/31-*`, `tests/…/40-*`,
`requirements/REQ-016.md` → **0 líneas**), así que el veredicto sobre `d54761e` vale igual sobre
`3e49b98`. **Fecha:** 2026-09-09. **Vuelta 1 de 3** del bucle dev↔QA de este REQ.
**Alcance:** los **10** criterios implementados. **`CA-04` está fuera por instrucción** (gate humano
**previo**, escalado como `D7`): no lo pruebo, no lo exijo y no lo acredito.
**Método y evidencia primaria completos: `docs/qa/1.34.0-req024-qa-metodo.md`** (base `d54761e`).

### Señales automáticas, reproducidas

| Señal | Resultado | ¿coincide con lo publicado? |
|---|---|---|
| Las 3 quality gates de `AGENTS.md` §7 | **las 3 OK** | sí |
| Banco completo, corrida 1 (rc 0, sin `ABORT`) | **1004 PASS · 0 FAIL · 8 SKIP** — cuadre **1012** | **cuadre sí; 1 caso desviado, reconciliado** |
| Banco completo, corrida 2 (mismo árbol) | **1004 · 0 · 8** — cuadre 1012 | estable entre corridas |
| Reloj / `loadavg` | **63,2 s** con 0,19 → 2,45 · **72,3 s** con 4,04 → 3,93 | compatible con los 65 s / 0,82 → 3,05 del dev |
| Autoprueba del corredor | **106 PASS · 0 FAIL** | sí |
| `fail-before` secciones 31 + 40/1 + 40/2 vs `v1.33.0` | **44 PASS · 9 FAIL · 2 SKIP** | **sí, exacto** |
| `pass-after` de esas tres | **55 · 0 · 0** | sí |
| `fail-before` de `CA-06` vs `6537e83:…/SKILL.md` | **3 PASS · 13 FAIL** | **sí, exacto** |
| `pass-after` de `CA-06` | **16 · 0** | sí |
| Inventario `6537e83` → `d54761e` | **+18 identidades, 0 suprimidas** (comprobado por identidad con `comm -23`) | **sin pérdidas: confirmado** |

**El desvío de un caso está reconciliado y no es regresión:** `REQ-017 CA-08 (ii)` sale **SKIP** en
mis dos corridas y **PASS** en la del dev. Es el caso cuyo techo cae dentro del recorrido observado
y cuyo veredicto lo decide la carga del host; el propio dev lo declaró
(`docs/qa/1.34.0-req024-ca06-metodo.md:196-198`). 1005 − 1 = 1004 y 7 + 1 = 8. **Lo trato como
`flaky` declarado —ni pase ni fallo— y es de `REQ-017`.** Ningún SKIP de los 8 es de `REQ-024`.

> **Aviso de método, y me costó una vuelta a mí mismo:** materializar la línea base con
> `git archive v1.33.0 hooks` **sin `tools`** produce **15 FAIL · 3 SKIP** en vez de 9 · 2, y seis de
> esos FAIL son del instrumento (`rc=127` del informe ausente). Estas secciones necesitan **`hooks`
> y `tools`**, que es lo que `MAT24_RUTAS='hooks tools'` declara dentro del banco.

### Veredicto por criterio

| Criterio | Veredicto | Cómo lo verifiqué (además del banco) |
|---|---|---|
| **CA-01** — la ausencia no se resuelve del lado que abre | **PASA**, con la glosa desmentida (`QA-024-01`) | ejecutando la puerta con la llave `true`: los 5 campos medibles **DENY** o gobiernan nombrando el campo; `Estado` n/a. Y 8 casos borde (vacío, blancos, comentado, minúscula, valor no reconocido) **todos DENY** |
| **CA-02** — un solo sitio, con su discriminante | **PASA** | tabla `ARNES_AUSENCIA` verificada fila a fila contra `ADR-009`; mutación del banco PASS. Frontera honesta declarada por el ADR: la fila de `Seguridad` **declara y no decide** |
| **CA-03** — ningún puntero queda falso | **NO ACREDITADO** (`QA-024-02`) | salida (ii) elegida **por medición** (correcto y declarado), pero de sus tres obligaciones sólo 1 está hecha: `REQ-016` sigue `completado` y su `CA-11` sigue citando `hooks/guard-completado.sh`. El caso del banco **lo dice él mismo** |
| **CA-05** — quien no activa nada no nota nada | **PASA** | con un manifiesto **sin el bloque `campos`**: 5 fixtures × 2 versiones, **decisión idéntica**. Llave apagada en **las dos sedes** |
| **CA-06** — la nota de migración | **PASA** | (i)–(iv) más el negativo; los **tres** PASS-antes-y-después **falsados por inyección** (ver abajo). Límite: `QA-024-05` |
| **CA-07** — el coste, cuatro vías | **PASA en esta máquina, NO CONCLUYENTE como techo** | (i) 2/2 y 5/5 → 0 añadidos; (ii) 1.080× < 1.250×; (iii) mediana 2.042× (MAD 0.081×); (iv) mediana 2.019× (MAD 0.068×), los dos con su par. El juez del techo es el CI, y su sonda **abstuvo** por suelo en mi `fail-before` |
| **CA-08** — la cola que no se puede contar no cuenta cero | **PASA** | los **tres** lectores sobre el rango sin cerrar: puerta DENY citando la **línea 3**, parada **rc 0** + `sin datos` (y el texto humano de fuera de los marcadores intacto), informe **rc 1** + `sin datos` + «la puerta DENIEGA» + **sin** «Ningún valor anómalo». `v1.33.0`: ALLOW y `0`. Más 13 formas propias. Ancho ≠ criterio: `QA-024-07` |
| **CA-09** — un cierre sin apertura no retira nada | **PASA** | `### Migrar A --> B` cuenta **1** hoy y **0** en `v1.33.0`, en los tres lectores; las dos no-regresiones (plantilla → 0, anotación cerrada en su línea → lo de siempre) reproducidas |
| **CA-10** — el grano se decide en un ADR | **PASA** | `ADR-010` mantiene; `hooks/lib.sh:1191` y `:1653` lo citan nombrando la forma medida; el banco mide la divergencia entre los dos lectores |
| **CA-11** — ni una transcripción más, ningún valor movido | **PASA** | `hooks/campos-req.awk` emite valores **crudos** y `tools/arnes-paralelo.sh` no lee rigor: la dirección no puede mover ningún **valor**; sólo el derivado, y sólo con la llave. (iii) no afirma cierre — pero enumera (ver `QA-024-06`) |

**9 de los 10 pasan. `CA-03` no está acreditado.**

### Los tres PASS de `CA-06` que pasan ANTES y DESPUÉS: ninguno es tautología, y lo probé rompiéndolos

| Caso | Antes | Después | Inyección | Resultado |
|---|---|---|---|---|
| `(ii) orden en el apartado` | PASS (2 < 3) | PASS | un bloque de comando **al principio** del apartado | **FAIL** «propiedad en la viñeta 3, primer comando en la 1 (de 21)» |
| `(iii) los N barridos POR VÍA declaran las tres cosas` | PASS (**1**) | PASS (**2**) | quitar `interroga una VÍA, no la propiedad` del **segundo** barrido | **FAIL** nombrando la viñeta **y** el defecto |
| `completitud: N de N «exhaustiv» negadas` | PASS (3/3) | PASS (5/5) | escribir «Lo que NO cambia (**lista exhaustiva**)» | **FAIL** «5 negadas de 6» |

Los tres son **falsables** y **publican su denominador**; su PASS-antes es un **hecho sobre el
documento anterior** (el texto de `REQ-023` ya cumplía esas propiedades), no vacuidad. Y el par que
el `desarrollador` declaró es real y **complementario**, verificado por construcción: la inyección
que pone en rojo el caso **del apartado** deja el **del sub-bloque** en PASS, y el `fail-before` del
sub-bloque (viñeta 0/0) deja el del apartado en PASS. **Ninguno de los dos sobra.**

### Hallazgos

#### `QA-024-01` · `contrato` · severidad **media** — «los VEREDICTOS DENIEGAN nombrando el campo que falta» es FALSO para `Rigor: ligero`, y está escrito **sin condición** en tres sedes que un proyecto hereda

**Reproducción** (manifiesto de sonda con `campos.ausencia_exige: true`; cabecera con
`Estado: en-revisión` y el `Edit` que escribe `completado`):

```
# REQ-940
Estado: en-revisión
Seguridad: aprobado
Sensible a seguridad: no
Hallazgos abiertos: (ninguno)
Rigor: ligero
```

| Variante | Esperado por el texto heredado | **Observado** |
|---|---|---|
| la de arriba, **`QA:` omitido**, llave `true` | DENY nombrando `QA:` | **ALLOW**, sin diagnóstico ninguno |
| la misma con `Rigor: estandar` | DENY | DENY nombrando `'QA:'` ✔ |
| `Rigor: ligero` + `QA`/`HALL` ausentes, llave `true` | DENY por los dos | DENY, **sólo por `Hallazgos abiertos:`** |

**Causa:** la resolución de la ausencia de `QA` vive **dentro** del corto-circuito
`if [ "$rigor" != "ligero" ]` (`hooks/guard-completado.sh:410`, llamada en `:421`); la de
`Hallazgos abiertos` vive **fuera** (`:551`). Con la llave encendida y `Rigor: ligero`, la clase del
hallazgo deniega y el veredicto de QA no.

**Qué desmiente, con cita:**
- `.arnes/config.json:36` y `templates/arnes-config.json.tpl:36` (`_doc`, las **dos** sedes):
  «los VEREDICTOS y la clase del hallazgo **DENIEGAN** nombrando el campo que falta» y «**Todo** REQ
  heredado que omita uno de esos cuatro campos **deja de cerrar** hasta que lo declare». La segunda
  frase es **medidamente falsa**.
- `skills/arnes-upgrade/SKILL.md` (bloque de la vía nueva): «si a un REQ en estado terminal le falta
  `QA:` o `Hallazgos abiertos:`, con la llave encendida la puerta **DENIEGA nombrando el campo que
  falta**». Falsa para `ligero`.
- `REQ-024 CA-01`, la glosa: «**no abre** — o **gobierna** …, o **deniega** nombrando … el campo que
  falta». Hay una **tercera** salida observada: no gobierna, no deniega, y no abre **porque a ese
  nivel el campo no gobernaba nada**.

**Qué NO es, dicho para que la clase no se lea de más:** no es un bypass nuevo. Bajo `ligero`, un
`QA:` **declarado** con el valor más restrictivo (`con-hallazgos`) tampoco gatilla (**medido:
ALLOW**), así que la ausencia **no es más permisiva que la presencia** y la clase derivada de `CA-01`
sigue **vacía** bajo el procedimiento de derivación de `REQ-023 CA-02`. Y **no es alcanzable en este
repositorio**: `rigor efectivo: critico 27 · estandar 0 · ligero 0`. Lo que está roto es la
**promesa**, y es la forma exacta de `SEC-047` y `SEC-079` — texto heredado que promete de más — en
superficie **nueva**, que es por lo que clasifico `contrato` y no `instrumento`: un propietario que
lea el `_doc` para decidir si encender la llave audita su corpus creyendo que los cuatro campos ya
deniegan, y para sus REQ `ligero` no es cierto.

**Dueños:** `analista-requerimientos` (la glosa de `CA-01` y, si aplica, `CA-06 (iv)`) y
`desarrollador` (las tres sedes de texto). **La decisión de diseño no es mía:** o se acota la frase,
o se saca la resolución de `QA` del corto-circuito. Las dos son defendibles y la segunda cambia
veredictos.

#### `QA-024-02` · `contrato` · severidad **alta** — `CA-03` no está acreditado: el puntero de `REQ-016 CA-11` sigue falso, `REQ-016` sigue `completado`, y la deuda no existe para la puerta

`CA-03` admite dos salidas y prohíbe la tercera («dejar el puntero citando un archivo donde falte
algún campo de la clase»). La implementación tomó la **(ii)**, **y la tomó midiendo, no invocándola**
— eso lo acredito: `ADR-009` §Alternativas lo argumenta porque `hooks/lib.sh` lo cargan **siete**
puntos de entrada y sólo uno es la puerta, y **lo comprobé ejecutando**: sin el renglón de
`hooks/estado-derivado.sh`, el tablero publicaría `estandar` donde la puerta dice `critico`.

Pero la salida (ii) exige **tres** cosas y sólo una está hecha:

| Obligación de `CA-03 (ii)` | Estado | Evidencia |
|---|---|---|
| el sitio único es otro | **hecha** | `ARNES_AUSENCIA`, `hooks/lib.sh:1873` |
| **`REQ-016` vuelve a `en-progreso`** | **NO hecha** | `requirements/REQ-016.md:2` → `Estado: completado` |
| **su `CA-11` reescrito con el puntero real** | **NO hecha** | `requirements/REQ-016.md:152-153` → «lo decide **un solo sitio**, `hooks/guard-completado.sh`, y ahí vive la lista exhaustiva» |

**Así que el árbol está hoy exactamente en la tercera salida que `CA-03` prohíbe**, y el puntero que
un auditor seguiría le dejaría fuera los **2** campos que retiran el **suelo** y el **nivel** — que
es el defecto que `SEC-050` midió.

Dos cosas más, y la segunda es la que corrige esta validación:
1. La entrada del Historial de `REQ-024` del 2026-09-09 afirma «**`CA-03` resuelve por su salida
   (ii)**». No lo resuelve: ejecuta un tercio de ella. El caso del banco es **más honesto que el
   REQ** («ESTE CASO PUBLICA LA MEDICIÓN Y NO ACREDITA CA-03»).
2. La deuda está **nombrada con dueño** (`analista-requerimientos`) en el Historial y en `ADR-009`,
   pero **no estaba en ningún `Hallazgos abiertos:`**, así que `guard-completado` no podía verla.
   Este hallazgo la pone ahí.

**Dueño:** `analista-requerimientos` — write-back de `REQ-016` (que además lo devuelve a
`en-progreso` y le hace re-recorrer el ciclo, `AGENTS.md` §9) y corregir la afirmación del Historial
de `REQ-024`.

#### `QA-024-03` · `contrato` · severidad **media** — el manifiesto propio se cambió **antes** de su gate humano, y `ADR-009` sigue declarando ese gate «pendiente»

- `AGENTS.md` §6: el pipeline **se detiene** antes de «cambiar el ruleset, el workflow de CI o el
  manifiesto `.arnes/config.json` **de este repo**».
- `docs/decisions/ADR-009-…:3`: «`Estado: propuesta` (**gate humano pendiente**: toca
  `.arnes/config.json` y la superficie heredada)».
- Medido: `git diff v1.33.0 -- .arnes/config.json` muestra el bloque `campos` **ya en el árbol**
  desde `119e853`. `PENDING_APPROVAL.md` **no tiene ninguna entrada** para él: la única, `D7`, es de
  `CA-04`/`AGENTS.md`.
- Y `REQ-024` lo afirma de sí mismo: «Los dos ADR pasan a ser la **fase 0 de la implementación**: se
  escriben **antes de tocar código**, **con su aprobación humana**». Los dos siguen en `propuesta`.

**Riesgo vivo: ninguno** — la llave nace `false` en las dos sedes (verificado) y un manifiesto sin el
bloque decide idéntico a `v1.33.0` (medido, 5 fixtures). El defecto es de **orden de gate**, en el
repositorio cuyo producto **es** el orden de los gates: el `desarrollador` respetó el gate para
`AGENTS.md` —se detuvo y abrió `D7`, y eso es exactamente lo correcto— y no lo aplicó al manifiesto,
que §6 nombra en la **misma** viñeta.

**Dueño:** coordinadora (llevarlo a `PENDING_APPROVAL.md` junto con `D7`, o traer la decisión de que
la delegación lo cubría) + `analista-requerimientos` si el write-back toca el texto de «fase 0».
**No lo resuelvo yo y no firmo alrededor de él.**

#### `QA-024-04` · `instrumento` · severidad **media** — `CA-07 (ii)` publica su medición en el **nombre** del caso y con `µs`: el oráculo del inventario no la normaliza y el inventario deja de ser comparable

**Reproducción decisiva, sin depender del árbol:**

```
printf '  PASS  caso sin separador 69779µs sobre 64569µs y 500ms y 1.5×\n  PASS  caso con separador  69779µs sobre 64569µs y 500ms y 1.5×\n' > /tmp/s.txt
bash tests/escenarios/hooks/inventario.sh /tmp/s.txt
PASS	caso con separador  Nµs sobre Nµs y Nms y N×
PASS	caso sin separador 69779µs sobre 64569µs y Nms y N×
```

**Causa exacta, las dos mitades:**
1. la línea **PASS** de `tests/…/40-ausencia-que-abre-2-migracion-y-punteros.sh:329` (y la FAIL de
   `:331`) **no lleva el doble espacio** que `inventario.sh` usa para separar nombre de evidencia
   —las dos líneas **SKIP** de `:312` y `:325` sí lo llevan—, así que toda la medición cae en la
   posición de **identidad** (`todo=0`);
2. ahí `inventario.sh` sólo normaliza si el numeral lleva decimal o una unidad `[A-Za-z]` **pegada**,
   y `µ` (0xC2 0xB5 bajo `LC_ALL=C`) **no** es `[A-Za-z]`. `500ms` sí se normaliza; `69779µs` no.

**Consecuencia medida:** dos corridas del **mismo** árbol dan **1004 · 0 · 8** las dos y un `diff`
de inventario de **22 líneas**, una de ellas esta. `REQ-014 CA-12` escribe «las tres salen
**idénticas** … `cmp` limpio», y hoy no lo son. El caso guardián «CA-12 (b) no queda ninguna magnitud
sin normalizar (unidad pegada o tirada larga)» **pasa**: su fixture no cubre `µ` en posición de
nombre. De las 14 líneas del inventario con `µs`, ésta es la **única** con dígitos crudos (la otra,
`REQ-021 CA-03 (a.3) cond. 5`, es un `1µs` literal del nombre y es identidad correcta).

**Dueño `desarrollador`:** el doble espacio en `:329`/`:331` es de este REQ; ensanchar
`sig ~ /[A-Za-z]/` para cubrir `µ` es de `REQ-014` y va aparte.

#### `QA-024-05` · `instrumento` · severidad **baja** — el universal «**cada** barrido por vía» de `CA-06 (iii)` se mide sobre un conjunto reconocido por una **cadena literal**

El contador es `grep -E 'Barrido POR VÍA|barrido POR VÍA'` sobre el apartado aplanado. Inyecté un
tercer barrido escrito de otra forma y con **cero** de las cuatro declaraciones:

```
- **(3) Otro barrido por via, sin ninguna declaracion:** grep -r QA requirements/
```

→ **PASS  REQ-024 CA-06 (iii) los 2 barridos POR VÍA del apartado declaran las tres cosas…**

El caso **no** es tautología (probado: quitar una declaración del barrido nuevo lo pone en FAIL
nombrando la viñeta), y publica su denominador — pero su «cada» sólo alcanza a los barridos que usan
la frase, y ésa es la misma falta de forma que `SEC-050`, aquí dentro del instrumento. La sección
deriva el **dominio de claves** del código (excelente) y reconoce los **barridos** por literal.
**Dueño `desarrollador`.**

#### `QA-024-06` · `instrumento` · severidad **baja** — la forma de `SEC-081` reaparece **dos veces** en las frases nuevas de la skill: enumeraciones cerradas sin marca ni puntero

**Lo primero, porque es lo que se me pidió confirmar:** el `desarrollador` **no escribió** la fila
del rigor y **no cerró** `SEC-081`. **No existe una cuarta instancia de la fila.** Correcto: su
forzador es `CA-04`, detenido en `D7`.

Lo que **no** puedo confirmar es que ninguna frase nueva reproduzca la **forma**. Reproducen dos:

**(a)** `skills/arnes-upgrade/SKILL.md`, párrafo del residual: «con la llave apagada tu proyecto
**sigue expuesto** a las **tres vías** —comentar, borrar, no escribir—». Medido con la llave
**apagada**, sobre la puerta real y con `QA: pendiente` en la cabecera:

| Vía que produce el MISMO estado ausente | Veredicto |
|---|---|
| comentar · borrar · no escribir | ALLOW (las tres) |
| **otra capitalización**: `qa: pendiente` | **ALLOW** — control `QA: pendiente` → **DENY** |
| **letra sustituida**: `QÁ: pendiente` | **ALLOW** |

Al menos **cinco**, no tres. Las dos últimas ya tienen dueño (`QA-023-09`, `SEC-078`), así que **no
son vía nueva**: lo que falla es el **número cerrado**, sin `no exhaustiv` y sin puntero al sitio
único de las vías (`docs/seguridad/registro-seguridad.md`), justo en el párrafo que existe **para no
maquillar**. Y la propia skill, unas viñetas antes, dice que el homóglifo «permite **por ausencia**
del campo que el homóglifo borró» — es decir el documento se contradice a sí mismo a 80 líneas.

**(b)** mismo archivo, viñeta de la cola: «**Lo que NO cambia:** [dos formas]», sin marca de no
exhaustivo (sí con puntero a `ADR-010`). El banco mide **7** formas de la cola, de las que **5** no
cambian; el texto nombra 2.

**Forma de las dos:** `enumeración`. **Dueño `desarrollador`**, con `SEC-081` como causa. **No
bloquea** y no reabre nada de negocio: es deuda de instrumento en su propio ciclo.

#### `QA-024-07` · `instrumento` · severidad **baja** — `CA-08` está implementado **más ancho** que su criterio y que la nota de migración

Medido: `## Pendientes` con **una** entrada, luego `## Resueltas`, y detrás un `<!--` **sin cerrar**
→ `rc=1`, cola `sin datos`, la puerta **DENIEGA** (en `v1.33.0`: `0` con `rc=0`). Es decir: un rango
sin cerrar **fuera** de `## Pendientes` también deja la cola sin medir y **bloquea todo cierre del
proyecto**.

La dirección es la correcta y está **declarada en el código** («SE MIRA AL FINAL DEL ARCHIVO Y NO
SÓLO DENTRO DE `## Pendientes`, a propósito … lo que no se pudo leer no se puede acotar»). Lo que no
lo dice es el contrato: `CA-08` lo enuncia «Dado un `PENDING_APPROVAL.md` **cuya sección
`## Pendientes` contiene** un rango…» y la skill dice «un rango … **dentro de la sección**». Un
proyecto que reciba el DENY y lea la nota buscará sólo dentro de `## Pendientes`. **Write-back
pequeño y sin ADR** (es un ensanchamiento hacia el lado que cierra): **dueño
`analista-requerimientos`** (el criterio) y **`desarrollador`** (la frase de la skill).

#### `QA-024-08` · `instrumento` · severidad **media** — la cola sigue contando `0` con `rc=0` sobre entradas **visibles** cuando el encabezado no se reconoce

| Fixture | hoy (`d54761e`) | `v1.33.0` |
|---|---|---|
| **`## pendientes`** en minúscula + `### D1` + `### D2` | `rc=0` cola **0** | `rc=0` cola **0** |
| `##Pendientes` sin espacio + `### D1` | cola 0 | cola 0 |
| `#### D1` / `###D1` / `  ### D1` (sangrada) | cola 0 | cola 0 |

**Preexistente y no regresión** —decide igual en las dos versiones—, y por eso **no cuenta vuelta**.
Se abre porque es la **misma familia** que `SEC-051` parte B, en la **misma función** que este REQ
acaba de reescribir, y porque los controles de `CA-08`/`CA-09` contratan «la **igualdad** entre lo
visible y lo contado»: aquí hay **2 visibles y 0 contadas**, y los **tres** canales coinciden en el
número equivocado — que es el agravante que `REQ-024` nombra en su propia § del bloque B. `CA-08` no
lo cubre (aquí la cola **sí** se puede medir) y `CA-09` tampoco (su propiedad es el cierre huérfano).
**Dueño `desarrollador`**, con ventana; la salida limpia es la misma que la del REQ: enunciarlo por
**propiedad** («lo visible se cuenta») en vez de por la forma del encabezado.

#### `QA-024-09` · `instrumento` · severidad **baja** — el `Archivos:` de `REQ-024` no declara tres rutas que la implementación escribió

| Ruta escrita | ¿en `Archivos:`? | ¿desviación declarada? |
|---|---|---|
| `hooks/estado-derivado.sh` | no | **sí**, Historial + comentario del código → conforme |
| `tests/…/39-caracter-invisible-4-el-coste.sh` (+6, deuda preexistente enrutada) | no | sólo en `docs/qa/REQ-024-dev-metodo.md:139` |
| `tests/…/40-ausencia-que-abre-3-los-textos-heredados.sh` (**archivo nuevo**, 204 líneas) | no | sólo en `docs/qa/1.34.0-req024-ca06-metodo.md:93` |

El campo es la **única** autorización para despachar en paralelo (`AGENTS.md` §6, `REQ-016` **H-05**):
incompleto, produce un `disjunto` que no cubre lo que de verdad se escribe. Las dos últimas no tienen
entrada en el Historial, que es la regla que el propio `REQ-024` se escribió para este caso. **Dueño
`analista-requerimientos`.**

#### `QA-024-10` · `instrumento` · severidad **baja** — la línea explicativa del informe queda incompleta con la llave encendida

`tools/arnes-lectura.sh:302-304` imprime, fijo: «El rigor efectivo es DERIVADO:
`Sensible a seguridad: sí` impone `critico` … Si un REQ que crees crítico sale `estandar`, su campo
de sensibilidad no se está leyendo como crees». Con la llave encendida hay una **segunda** vía —la
**ausencia** gobierna— y el informe no la nombra: medido, `REQ-950` sin `Sensible` ni `Rigor` sale
`critico 1` sin decir por qué. La puerta **sí** lo dice en su motivo (verificado). Y es justo la
superficie que la skill manda usar para medir el coste de activar. **Dueño `desarrollador`, baja.**

### Resumen de clases

| Hallazgo | Clase | ¿bloquea el cierre? |
|---|---|---|
| `QA-024-01` (la promesa del `_doc` falsa bajo `ligero`) | `contrato` | **sí**, hasta el write-back |
| `QA-024-02` (`CA-03` sin acreditar, `REQ-016` con puntero falso) | `contrato` | **sí**, hasta el write-back |
| `QA-024-03` (manifiesto cambiado antes de su gate) | `contrato` | **sí**, hasta que el humano resuelva |
| `QA-024-04` · `QA-024-05` · `QA-024-06` · `QA-024-07` · `QA-024-08` · `QA-024-09` · `QA-024-10` | `instrumento` | no — deuda con dueño |

**Y lo que no depende de mis hallazgos:** `REQ-024` no puede cerrar de todas formas — `CA-04` está
detenido en su gate (`D7`), la cola tiene **5** entradas y `Seguridad:` está `pendiente`.

### Lo que NO se midió, dicho expresamente

1. **`CA-04`**: fuera de encargo. No probado, no exigido, no acreditado. Sólo verifiqué que sus
   cuatro sedes siguen **intactas** (el único cambio de `AGENTS.md` frente a `v1.33.0` en esa zona es
   la fila de `SEC-079`, autorizada por `D5`, más el bloque §14 de `REQ-027`).
2. **El CI `hooks-en-linux`**: no lo corrí. **Ningún techo de reloj de esta validación acredita nada
   más allá de esta máquina**, y tres SKIP de la familia `SEC-080` desvían local↔CI por diseño.
3. **Rendimiento bajo carga o concurrencia**: `AGENTS.md` **no fija** umbral de usuarios concurrentes
   ni de latencia objetivo para este proyecto (no hay usuarios finales), así que **no hay prueba de
   carga y queda pendiente de acordarlo con el humano** si alguna vez tiene sentido aquí. Los cuatro
   techos de `CA-07` los mide el banco y en esta máquina son **no concluyentes**.
4. **La inmutabilidad del árbol de referencia**: `SEC-048` sigue abierto; todo `fail-before` supone
   que `v1.33.0` es el tag que dice ser.
5. **Seguridad**: no firmo `Seguridad:` ni la sustituyo. El `auditor-seguridad` va después de mí.
6. **`REQ-023 CA-12`** y **`SEC-081` como hallazgo del auditor**: fuera de mi encargo. De `CA-12`
   sólo constato que su SKIP es **determinista** (mismo texto en mis dos corridas); de `SEC-081`,
   que no se creó su cuarta instancia y que **no lo cierro yo**.
7. **La documentación de usuario final** (`docs/usuario/`): **no la escribo en esta vuelta**. Con tres
   hallazgos `contrato` abiertos sobre la nota de migración y sobre la promesa de la llave,
   documentar el flujo sería documentar algo que va a cambiar.

---

## `REQ-016` (re-recorrido de `CA-03 (ii)`) + `REQ-024` (vuelta 2 de 3) — commit **`a57eecc`**

> **Comisión conjunta.** Los dos REQ comparten `docs/qa/1.34.0.md`, `requirements/REQ-016.md` y
> `requirements/REQ-024.md`; separarlas daría tres conflictos de fusión. Corrida con **Opus** por la
> política de autoalojamiento, bajo delegación de decisión del propietario por 24 h.
>
> **Toda la evidencia con su método y su base está en
> `docs/qa/1.34.0-req016-req024-vuelta2-evidencia.md`** (§14.B.7). Aquí van los veredictos.

### Señales automáticas

Tres quality gates de `AGENTS.md` §7 en **verde**. Banco **1017 PASS · 0 FAIL · 7 SKIP** (`rc=0`,
cuadre **1024**), autoprueba **106 PASS · 0 FAIL**, reloj 63,73 s con `loadavg` 0,02 → 2,40.
**CI `hooks-en-linux` PASS sobre `a57eecc`** (1m42s, run `34448005449`) — al abrir la comisión
estaba `pending` y se releyó al cerrar. Los 7 SKIP están en la evidencia, cada uno con su motivo;
**ninguno nuevo**, y **un SKIP repetido no es un verde acumulado**: `REQ-017 CA-08 (ii)` sigue con
el techo **dentro** del recorrido observado y **no acredita nada en ninguna dirección**.

### `REQ-016 CA-11` — **VALIDADO**, comprobado ejecutando y no leyendo

| Mitad del criterio | Resultado | Evidencia |
|---|---|---|
| El sitio único es `hooks/lib.sh`, nombrado **por propiedad** | **pasa** | `ARNES_AUSENCIA` (constante, vía `_arnes_deriva_ausencia`) y `arnes_ausencia` (consulta) viven **las dos** en `hooks/lib.sh`, verificado por archivo |
| Los **cuatro campos de la clase** declaran ahí su dirección | **pasa** | `QA`→`deniega`, `Sensible a seguridad`→`gobierna:si`, `Hallazgos abiertos`→`deniega`, `Rigor`→`gobierna:critico`; los 6 del lector están en la tabla, **0 sin dirección** |
| **Equivalencia comentado ≡ borrado**, en los **dos** estados de la llave | **pasa** | **20 de 20** pares (C/D) equivalen, perfiles `blando` y `critico` × llave `false`/`true` |
| La excepción de `Seguridad:` en rigor efectivo `critico` | **pasa** | comentado **DENY** y borrado **DENY**, con la llave apagada **y** encendida: no se relaja |
| El puntero viejo ya no se cita | **pasa** | `CA-11` declara «ya no es `hooks/guard-completado.sh`» y da el motivo medido (ahí no viven `Sensible a seguridad:` ni `Rigor:`) |

**El puntero nuevo es CIERTO.** Y lo es en el sentido fuerte que `CA-11` pide: no sólo la lista
exhaustiva vive ahí, sino que la tabla **decide** — el propio banco lo prueba por mutación
(`REQ-024 CA-02 mutada SOLO la tabla, los DOS vuelven a abrir`) y yo lo reproduje con control
positivo sobre `QA`. **`CA-11` no cita líneas**, así que no nace envejecido.

**Cuatro pares de `Estado` que NO son hallazgo, y se dice porque mi primera lectura los contó
mal:** la edición que la puerta juzga toma su `old_string` de la línea `Estado:`; borrada esa
línea no hay transición que juzgar, que es lo que la tabla declara con `n/a`. Además `Estado`
queda **fuera del `Dado` de `CA-11`**, cuyo sujeto son los campos de **veredicto**.

### `REQ-024` — los tres `contrato`: **los tres se RETIRAN**

| Hallazgo | Veredicto | Por qué |
|---|---|---|
| **`QA-024-01`** | **se retira** | El código está arreglado **y medido**: la omisión de `QA:` con `Rigor: ligero` **DENIEGA nombrando el campo** (banco, cinco casos `(i)`–`(v)` en verde). Y la glosa de `CA-01` ya **no promete lo falso**: pasa a **tres** vías y su tabla dice, campo a campo, cuál gobierna y cuál deniega. **Verifiqué la fila que el analista dejó sin verificar** —`Seguridad:` «DENY si el rigor efectivo es `critico`; ALLOW por debajo, igual que con su valor declarado»— y **es cierta en las seis combinaciones** medidas. El contrato ya no afirma nada falso. *(La falsabilidad de la vía (c) es otra cosa y sale abajo como `QA-024-11`.)* |
| **`QA-024-02`** | **se retira** | La cuenta es correcta: `CA-03` **ELIGE** su salida (ii), la premisa **no** cuenta como obligación, y de las **tres** obligaciones están hechas **2**. Verifiqué las tres contra el árbol: (1) `REQ-016.md:2` dice `en-progreso` ✔; (2) el `CA-11` reescrito tiene el puntero real ✔ (§1 de la evidencia); (3) el re-recorrido es **esta comisión** y sigue **pendiente** porque el auditor va después. La cuenta vive **en tabla dentro de `CA-03`**, que es donde el lector la busca. |
| **`QA-024-03`** | **se retira** | El REQ **ya no afirma** que el gate se cumplió. Verifiqué los tres hechos que ahora declara, uno por uno: `ADR-009` y `ADR-010` en `Estado: propuesta` con el gate pendiente **en su línea 3** ✔; el bloque `campos` en el árbol desde **`119e853`** (2026-09-09 21:39) **sin** entrada previa en la cola —la única, `D7`, es de `CA-04`— ✔; `campos.ausencia_exige` en **`false`** en las **dos** sedes ✔. Escalado como **`D8`** (`PENDING_APPROVAL.md:169`). **Mi trabajo NO era validar el gate** —es del propietario— sino que el texto diga la verdad sobre su estado, y la dice. |

### `QA-024-11` · `instrumento` · severidad **media** — la vía **(c)** de `CA-01` no es falsable hoy

**Reproducción.** `bash tests/escenarios/hooks/run.sh tests/escenarios/hooks/secciones/40-ausencia-que-abre-1-la-clase-derivada.sh`

**Esperado** (`CA-01`): la vía (c) «sólo es conforme si el caso demuestra **en la misma corrida**
la **igualdad** con el valor declarado», y `CA-01` asigna `Seguridad:` a «**(b) / (c)**».

**Observado.** Ningún caso publica esa igualdad, y **la vía (c) no se ejercita en absoluto**:
`OTROS24['Seguridad']`
(`tests/escenarios/hooks/secciones/40-ausencia-que-abre-1-la-clase-derivada.sh:151-154`) fija
`Rigor: critico`, así que el único campo al que (c) aplica se mide **sólo donde aplica (b)**. La
salida publicada nombra **dos** vías y no tres: «*ninguna de las 5 claves medidas ABRE al faltar
(**gobiernan o deniegan** nombrando el campo)*».

**Medido por QA, porque el banco no lo hace:** la igualdad **existe en el producto** — con
`Seguridad:` declarado `pendiente` frente a **ausente**, el veredicto es idéntico en `ligero`,
`estandar` y `critico`, con la llave apagada y encendida (6 de 6). **Luego el producto es correcto
y la tabla de `CA-01` dice la verdad; lo que falta es el caso.** Por eso es `instrumento` y no
`contrato`: el REQ no afirma nada falso, y `CA-01` **ya previó** este reparto («si el caso
implementado no la publica para esa vía, es trabajo de implementación y lo verifica el
`qa-tester`»).

**Consecuencia que hay que conservar: `CA-01` está acreditado en su parte medida y la rama (c)
queda SIN acreditar.** *Dueño:* `desarrollador`. *Vía conforme:* un fixture de `Seguridad` por
**debajo** de `critico` que publique la comparación declarado/ausente en la misma corrida —**no**
mover la fila de `CA-01` ni ensanchar la (c).

### `QA-024-12` · `instrumento` · severidad **media** — la entrada `Seguridad` del sitio único es **código muerto**

**Reproducción.** `grep -n 'arnes_resuelve_ausencia "' hooks/*.sh` → **cuatro** llamadas: `QA` y
`Hallazgos abiertos` (`hooks/guard-completado.sh:433`, `:565`), `Sensible a seguridad` y `Rigor`
(`hooks/lib.sh:1532`, `:2377`). **Ninguna** para `ARNES_CLAVE_SEG`.

**Esperado.** `hooks/lib.sh` afirma sobre `arnes_resuelve_ausencia`: «**EL UNICO SITIO QUE
DECIDE.** Los tres lectores que resuelven ausencia —la puerta, el informe y el bloque derivado—
pasan por aqui y por ningun otro sitio».

**Observado.** Para `Seguridad` eso es **falso**. La tabla declara `deniega` y **nadie la
consulta**: mutada a `gobierna:aprobado` (mutación **verificada** leyendo `ARNES_AUSENCIA_DIR`), un
REQ `critico` sin `Seguridad:` sigue dando **DENY**; la **misma** mutación sobre `QA` pasa de DENY
a **ALLOW** (control positivo, así que la técnica sí discrimina). Quien decide es
`hooks/guard-completado.sh:497` — `[ "$rigor" = "critico" ] && vered+=("Seguridad:|…")` —, que no
mira la tabla. Y la dirección declarada es **incondicional** (`deniega`) mientras la conducta real
es **condicional al rigor**.

**Por qué `instrumento` y no `usuario/dinero` ni `contrato`.** Nadie ve, decide ni cobra distinto:
la conducta de hoy es la correcta y **está descrita con exactitud** en la tabla de `CA-01` y en el
`_doc` del manifiesto («*`Seguridad:` tambien DENIEGA, pero su veredicto solo se lee cuando el
rigor efectivo es `critico`*»). El engaño, además, va del lado **conservador**: quien lea la tabla
creerá que la puerta es **más** estricta de lo que es. Es una **trampa latente**, no un fallo en
abierto: el día que alguien enrute `Seguridad` por la tabla confiando en el comentario, o añada un
lector que la consulte, la dirección incondicional **denegaría por debajo de `critico`**.

*Dueño:* `desarrollador`. *Vías conformes (elige una, no las dos):* (a) enrutar `Seguridad` por
`arnes_resuelve_ausencia` y declarar su dirección **condicionada al rigor**; o (b) dejar la
conducta como está y **acotar el comentario** de `hooks/lib.sh` para que no prometa por
`Seguridad`, más un caso que sujete la elección. *Forzador medido:* el propio banco, si algún día
un caso deriva la clase **desde la tabla** en vez de desde los llamadores.

### `QA-024-13` · **`contrato`** · severidad **media** — un título de sección que es falso en sus dos mitades

**Reproducción.** `sed -n '678p;9p' requirements/REQ-024.md`

**Observado.** `requirements/REQ-024.md:678` titula «*Por qué `Hallazgos abiertos:` está en
`(ninguno)`, con **tres** hallazgos encima*». Las dos mitades son falsas hoy: el campo **no** está
en `(ninguno)` y **no** lleva tres, lleva **DIEZ** (`QA-024-01`…`QA-024-10`, tres de ellos
`contrato`). **El cuerpo de la sección sigue siendo correcto** — explica por qué `SEC-047`,
`SEC-050` y `SEC-051`, que son el **origen** del REQ, no se cargan en su propio campo.

**Por qué `contrato` y no `instrumento`.** No es un control ni una prueba: es el **texto del
contrato**, y afirma algo falso sobre un campo que **la máquina lee para decidir el cierre**. Un
lector que se detenga en el título concluye que `guard-completado` ve `(ninguno)` cuando ve diez,
tres de los cuales **bloquean**. La detectó el `analista-requerimientos` y no la corrigió citando
«corregir sin ampliar»; la clase la decido yo, y esto es exactamente lo que la clase `contrato`
existe para no dejar pasar.

*Vía conforme:* retitular por la **propiedad** que el cuerpo defiende —el reparto entre el origen
del REQ y los hallazgos contra él— **sin** volver a escribir un número que sube con cada hallazgo
(forma **(b)** de `requirements/README.md`, la de fijar un número sin declararlo operativo o de
contrato). *Dueño:* `analista-requerimientos`.

### `QA-024-14` · **`contrato`** · severidad **baja** — la celda de evidencia de `CA-03` fila 3 queda vencida por mi propio veredicto

**Observado.** La tabla de `CA-03` (`requirements/REQ-024.md:218-222`) justifica la fila 3
—**PENDIENTE**— con: «`REQ-016` conserva `QA: aprobado` y `Seguridad: aprobado` **del
2026-09-07**». En cuanto escribo mi veredicto, la **mitad `QA:` de esa celda es falsa**: `REQ-016`
pasa a `QA: aprobado (2026-09-10, a57eecc)`.

**La columna `Estado` sigue siendo VERDADERA** —la obligación 3 **no** está cumplida, porque
`Seguridad:` sigue siendo del 2026-09-07 y el auditor va después de mí—, así que **la cuenta 2 de
3 no cambia** y `CA-03` **sigue sin acreditar**. Lo único vencido es la evidencia citada.

Se abre en vez de callarse porque es precisamente la deriva que `AGENTS.md` §9 persigue: un REQ
que describe el árbol de ayer. Y se abre **contra mi propio acto**, que es la única forma honesta
de cerrarlo. *Vía conforme:* una celda — dejar la evidencia en el veredicto que **falta**
(`Seguridad:` del 2026-09-07 sobre un `CA-11` distinto del vigente) en vez de en los dos.
*Dueño:* `analista-requerimientos`. **Se cierra sola** cuando el auditor renueve `Seguridad:` y la
obligación 3 pase a hecha.

### `QA-024-15` · `instrumento` · severidad **baja** — el valor malformado de la llave no lo contrata nadie

**Reproducción.** Tabla completa de once variantes en la evidencia §5.

**Observado.** Con `campos.ausencia_exige` en `"true"`, `"TRUE"`, `1`, `[]`, `{}` o `" true"`, la
puerta **no activa** la exigencia (ALLOW) pero **sí avisa nombrando `campos.ausencia_exige`**,
porque la validación de tipos de `arnes_parse_manifest` lo recoge. `null` y el bloque ausente dan
ALLOW **en silencio**, que es el defecto documentado. Y `campos` como **cadena** da **DENY** con
aviso: **fail-closed** ante un manifiesto no indexable, que es la dirección correcta.

**La conducta es defendible y es diagnosticable** —cae del lado que no activa, que es lo que
`CA-05` contrata, y lo dice—. Lo que falta es el **contrato**: ninguno de los once criterios de
`REQ-024` cubre el valor malformado de la llave, así que **nada en el banco la sujeta** contra una
regresión al ALLOW mudo. Y el ALLOW mudo era exactamente el defecto anotado al planificar la
implementación («la llave como cadena `"true"` cae a ALLOW sin aviso»), hoy **medio cerrado**: ya
no es mudo. *Dueño:* `analista-requerimientos` (criterio) + `desarrollador` (caso).

### `QA-016-01` · `instrumento` · severidad **media** — el control del puntero quedó desfasado y su PASS afirma algo falso

**Reproducción.** `bash tests/escenarios/hooks/run.sh tests/escenarios/hooks/secciones/40-ausencia-que-abre-2-migracion-y-punteros.sh`

**Observado.** El caso «`REQ-024 CA-03` control del puntero» (`40/2:192`) **no lee `CA-11`**:
codifica el puntero viejo como **literal** (`SITIO40["$k40"] = 'guard-completado.sh'`,
`:187-192`). De ahí dos consecuencias.

1. **Su mensaje de PASS es falso desde `a57eecc`.** Publica «*siguiendo `REQ-016 CA-11` al pie de
   la letra…*» cuando `CA-11` **ya no dice eso**, y termina afirmando «*el write-back es del
   analista y **no se ha hecho***» — **se hizo** el 2026-09-10, y es lo que yo acabo de validar.
   El caso también «ordena» lo ya cumplido: «*`REQ-016` vuelve a `en-progreso` y su `CA-11` se
   reescribe*».
2. **Sigue en verde por construcción**, porque mide contra un literal congelado. Es un control que
   **demuestra un defecto que el write-back acaba de corregir**, y por eso hacer cierto el
   criterio no exigió código y el CI lo ve pasar.

**Mi decisión, que la comisión me delegó: NO sigue midiendo la propiedad que nombra, y no se
conserva como está.** Un caso cuyo PASS afirma que un trabajo está sin hacer envenena la salida
del banco justo para quien la lee buscando el estado. Vale como **control histórico** sólo si se
**re-etiqueta** diciendo lo que es —la medición de `SEC-050` sobre el puntero de `v1.33.0`, con su
fecha y su commit—, y entonces **no puede seguir hablando en presente** del write-back. La otra
salida conforme es **derivar el sitio de `CA-11`** en vez de codificarlo, con lo que el caso pasa a
vigilar que el puntero **siga** siendo cierto — que es lo que `CA-02` declara **de contrato**
(«cambiarlo exige ADR y write-back en los criterios que lo nombran») y hoy **nada vigila**.

**Por qué `instrumento`:** el defecto está en la **prueba**, no en el producto; nadie ve, decide ni
cobra distinto. **Y no bloquea `REQ-016`** — atacar guardianes es valioso pero no puede ser
condición para cerrar. *Dueño:* `desarrollador`. *Forzador medido:* el propio `CA-02`, la próxima
vez que el sitio único se mueva; hoy ningún caso lo notaría.

### Resumen de clases — esta comisión

| Hallazgo | REQ | Clase | ¿Bloquea? |
|---|---|---|---|
| `QA-024-01`, `QA-024-02`, `QA-024-03` | 024 | **retirados** | — |
| `QA-024-11` | 024 | `instrumento` | no |
| `QA-024-12` | 024 | `instrumento` | no |
| **`QA-024-13`** | 024 | **`contrato`** | **sí** |
| **`QA-024-14`** | 024 | **`contrato`** | **sí** (una celda; se cierra con la firma del auditor) |
| `QA-024-15` | 024 | `instrumento` | no |
| `QA-016-01` | 016 | `instrumento` | no |
| **`QA-016-02`** | 016 | **`contrato`** | **sí** (se cierra con la firma del auditor) |

**`REQ-016`: `CA-11` queda VALIDADO y el re-recorrido puede pasar a seguridad** —es la última de
las tres obligaciones de `REQ-024 CA-03 (ii)`—, **pero el REQ no cierra**, y no por su criterio:
`QA-024-02` se retira con razón y en su lugar entra **`QA-016-02`**, porque retirarlo a secas
dejaba el cierre **medidamente abierto** sobre un `Seguridad:` del 2026-09-07 emitido sobre otro
`CA-11`. `H-07` y `QA-016-01` son `instrumento` y no bloquean.

**`REQ-024`: dos `contrato` nuevos, los dos de una línea o una celda.** Los tres que traía se
retiran; ninguno de los nuevos toca el mecanismo, y **`CA-03` sigue sin acreditar** hasta que el
auditor renueve `Seguridad:` de `REQ-016`.

### Lo que NO se midió, dicho expresamente

1. **No encendí la llave** en ninguna sede del repositorio, ni presupuse `D8`.
2. **No firmo `Seguridad:`** de ningún REQ — el auditor va después (`AGENTS.md` §6).
3. **`CA-04` de `REQ-024`:** fuera de alcance por gate humano previo (`D7`).
4. **No marqué nada `completado`:** la cola tiene **7** entradas en «## Pendientes» y
   `guard-completado` denegaría igual.
5. **Rendimiento:** sin umbral de negocio en `AGENTS.md`, **no hay prueba de carga**; pendiente de
   acordarlo. `REQ-024 CA-07 (ii)` sale **no concluyente** en esta máquina.
6. **Concurrencia sobre la puerta:** no se ejercitó (`ADR-008` sigue en `propuesta`).
7. **`docs/usuario/`:** no la escribo. Con `QA-024-13` y `QA-024-14` abiertos, documentar sería
   documentar algo que va a cambiar.

### `QA-016-02` · **`contrato`** · severidad **alta** — retirar `QA-024-02` de `REQ-016` deja el cierre ABIERTO sobre una firma de seguridad vencida

**Lo encontré atacando mi propio veredicto, y es un fail-open que yo habría creado.** `QA-024-02`
era el **único** hallazgo bloqueante de `REQ-016`. Al retirarlo con razón —su contenido, la cuenta
de `CA-03`, quedó corregido— el campo se queda con dos `instrumento`, y entonces la puerta ya no
tiene por dónde retener el REQ: su `Seguridad: aprobado (R-009, 2026-09-07)` **está presente**, y
`guard-completado` **no puede saber que se emitió sobre otro `CA-11`**.

**Reproducción** (fixtures en un temporal, `requirements_dir` redirigido, **cola vacía a
propósito** para aislar el efecto del campo; `hooks/`, `tools/` y `templates/` hermanos
materializados o salen 102 FAIL de artefacto con `rc=127`):

| Fixture | Campo `Hallazgos abiertos:` | Veredicto de la puerta |
|---|---|---|
| **A** — `REQ-016` como lo deja esta comisión | `H-07 (instrumento), QA-016-01 (instrumento)` | **ALLOW** |
| **B** — control | `H-07 (instrumento), X-99 (contrato)` | **DENY**, nombrando la clase |
| **C** — sin hallazgos | `(ninguno)` | **ALLOW** |

**El control B es lo que hace concluyente el ALLOW de A:** la puerta **sí** bloquea ante un
`contrato`, así que el ALLOW de A no es un instrumento que no mide — es la decisión real.

**Esperado.** `REQ-024 CA-03 (ii)` exige **tres** obligaciones, y la tercera —el re-recorrido— no
está cumplida: yo renuevo `QA:`, pero `Seguridad:` sigue siendo del **2026-09-07**, emitido sobre
un criterio que el write-back cambió. Mientras eso sea así, `REQ-016` **no debe poder cerrarse**.

**Observado.** Puede. Hoy lo tapa la **cola de 7 entradas** (`tools/arnes-lectura.sh`: «*ningún REQ
puede cerrarse*»), y por eso el riesgo no es vivo **hoy** — pero la cola es transitoria y la firma
vencida no: el día que el propietario resuelva las 7, `REQ-016` cierra con una auditoría que nunca
vio el `CA-11` vigente. Es la familia exacta de `SEC-047`: un cierre que se gana porque a la puerta
le **falta** una declaración, no porque alguien la haya firmado.

**Por qué `contrato` y no `instrumento`.** El defecto no está en la prueba: está en lo que el
**REQ afirma**. Su cabecera declara `Seguridad: aprobado` a secas, y eso es falso respecto del
criterio vigente —lo dice el propio `Estado:` del REQ, en prosa que **la máquina no lee**—. Y
bloquea por diseño: es el único mecanismo que sostiene la obligación 3 hasta que exista la firma.

**Cómo se cierra, y no lo hago yo.** *(a)* El `auditor-seguridad` renueva `Seguridad:` sobre este
árbol —es el camino normal, y ya puede: `QA:` está en `aprobado`, que es su precondición de
`AGENTS.md` §6—; o *(b)* si eso se demora, el `analista-requerimientos` deja constancia en el
**campo** y no sólo en la prosa. **Lo que no vale es dejar el campo sin nada bloqueante:** ésa es
la deriva. *Dueño:* `auditor-seguridad` (vía normal). **Se cierra solo** con su firma.

**Nota de método, porque la barandilla me frenó a mí:** el primer intento de esta medición lo
**denegó el `guard-completado` de la 1.33.0 instalada** —mi comando escribía en `requirements/` y
mencionaba el estado terminal—. Es la conducta correcta descrita en `AGENTS.md` §13, y se rodeó
por la vía conforme: fixtures **fuera** de `requirements/`, con `requirements_dir` redirigido en el
manifiesto de prueba. No se tocó ningún REQ real para medir esto.

---

## `REQ-024` · **vuelta 3 de 3 — la última** · base **`9ba3b1f`** (rama `rel/registro-1.33.0`)

> Corrida con **Opus** por la política de autoalojamiento, bajo delegación del propietario por 24 h.
> **Toda la evidencia, con su método, sus mutaciones verificadas y sus cifras elemento por elemento,
> está en `docs/qa/1.34.0-req024-vuelta3-metodo.md`** (`AGENTS.md` §14.B.7). Aquí van los veredictos y
> los hallazgos.

### Señales automáticas, y la que NO está verde

Tres quality gates de `AGENTS.md` §7 en **verde**. Banco en **tres** corridas: `1058/0/6`, `1057/0/7`,
`1057/0/7` — **0 FAIL en las tres** y **`PASS + SKIP = 1064`** en las tres, cuadrado **por lista** y no
por total (`loadavg` 0,29 · 1,80 · 3,20). De los siete SKIP posibles, **seis** son estables y **uno**
flota (`REQ-023 CA-09 (iii)` sobre `arnes_campo_linea`, 2 de 3); los dos de esa familia son de **reloj**
y su residual está en `SEC-080`. **Un SKIP repetido no es un verde acumulado.**

**Y una cuarta corrida, DESPUÉS de escribir mi veredicto en la cabecera** —porque `40/1` y `40/5`
leen `requirements/REQ-024.md` y mi edición podía romperlos—: **1056 PASS · 0 FAIL · 8 SKIP**, cuadre
**1064** otra vez y **0 FAIL**. El octavo SKIP es **`REQ-024 CA-07 (iii)`**, que no había abstenido
antes: **los que flotan son TRES y no dos**. Repetido **7 veces aislado**, dio **7 PASS** con
**mediana de medianas 2,061× y MAD 0,019×** contra el techo **2,200×** — así que `CA-07 (iii)`
**pasa**, y su abstención es un miembro **nuevo** de la familia de reloj de `SEC-080`, no un fallo:
**10 de 11 observaciones PASS y la undécima una abstención**, que es la dirección conforme.

**El CI `hooks-en-linux` de la PR 45 está `pending` sobre `9ba3b1f`** (run `34465439360`, leído con
`gh pr checks 45`). El último verde acreditado es de `a57eecc`, y entre las dos bases hay **ocho**
commits sobre los que la puerta requerida **no ha corrido**. **No se cita el verde anterior como si
fuera éste.**

### Veredicto por criterio

| Criterio | Veredicto | Cómo se comprobó |
|---|---|---|
| `CA-01` | **pasa** | Banco: M=6 claves del lector (1 `n/a`, 5 medidas), clase derivada **vacía**, y discriminante contra `v1.33.0` que **abre en 4 de 5** |
| `CA-02` | **pasa** | Banco: mutada **sólo** la tabla, los **dos** sitios vuelven a abrir; discriminante (i)+(ii) en la misma corrida. Corroborado por mi propia mutación sobre `Seguridad` (§10 de la evidencia) |
| `CA-03` | **NO acreditado** (sin cambio desde la vuelta 2) | Su obligación 3 sigue **ejecutada y no acreditada**: `REQ-016` conserva `Seguridad: con-hallazgos` con `SEC-082 (contrato)` abierto. No es falta de medición: es falta de una firma que no es mía |
| `CA-04` | **no validado, y no exigido** | Detenido en su gate humano `D7`, fuera de alcance por instrucción |
| `CA-05` | **pasa** | 40 celdas, 27 idénticas y 13 divergentes **todas de ALLOW a DENY**; las 10 del **cierre** sin migrar deciden idéntico. **Anti-vacuidad reproducida**: con la base apuntando al propio árbol da **0 PASS · 2 FAIL · 5 SKIP** |
| `CA-06` | **pasa**, incluido `(v)` | Sus 5 casos verdes y el caso de las promesas **discrimina** (forma conocida inyectada → FAIL). La viñeta de `(v)` es **verdadera leída sola**, verificada ejecutando |
| `CA-07` | **pasa** | (i) 0 procesos añadidos (2/2 y 5/5); (ii) **1,093×** contra techo 1,250×; (iii) mediana **2,047×**; (iv) mediana **1,962×** |
| `CA-08` | **pasa** | Los **tres** lectores, verificados ejecutando: puerta DENY citando el rango **y la línea con su texto**; informe rc 1 con «NO puede afirmar “Ningún valor anómalo”»; parada rc 0 y celda «sin datos» |
| `CA-09` | **pasa** | Cierre huérfano cuenta **1** en los tres lectores; los dos controles de no-regresión siguen contando **0** |
| `CA-10` | **pasa** | El caso que **mide** la diferencia entre los dos lectores sobre la misma forma existe y está verde |
| `CA-11` | **pasa** | (i) 0 transcripciones nuevas; (ii) los **cuatro** lectores devuelven lo mismo que `v1.33.0` sobre los 31 REQ; (iii) sin artefacto que declare la clase cerrada |
| **`CA-12`** | **PARCIAL — `(i)` acreditado, `(ii)` NO implementado** | `(i)`: los tres controles en los dos estados de la llave, la salida (b) sobre ausente **y** comentada por una rama distinta, el fail-before con cuatro ALLOW contra `v1.33.0` y la asimetría por acto. `(ii)`: la fila de `AGENTS.md` §13 y su gemela **no** se transcribieron, **no hay caso de banco** que mida texto heredado y **el gate que el criterio nombra no está en la cola** (`QA-024-16`) |

### Hallazgos

#### `QA-024-16` · `instrumento` · severidad **media** — `CA-12 (ii)` no está implementado, y su fila queda verdadera por la salida elegida, no por construcción

**Reproducción.** `grep -n 'Seguridad no firma lo que QA no ha validado' AGENTS.md templates/AGENTS.md.tpl`
→ `AGENTS.md:346` y `templates/AGENTS.md.tpl:311`, las dos con la celda **antigua**.
`grep -rho 'REQ-024 CA-12[^"]*' tests/escenarios/hooks/secciones/` → **11** casos, **ninguno** sobre
texto heredado. `grep -n 'fila\|AGENTS' PENDING_APPROVAL.md` dentro de `D12` → nada.

**Esperado.** `REQ-024` § «La fila de `AGENTS.md` §13 de `CA-12`: el texto exacto» prescribe para la
**salida (b)** —la tomada en el código— la celda «*Seguridad no firma lo que QA no ha validado, tampoco
cuando la línea `QA:` no llega a declararse: la ausencia se trata como no validado y se deniega
nombrando el campo*», dice que **no se interpreta**, y `(B)` aplica «cuando la remediación (1) entre»
— ya entró. `CA-12 (ii)` exige además el **control en las dos direcciones en la misma corrida**.

**Observado.** Nada de eso existe: ni la fila en las dos sedes, ni el caso, ni la entrada de cola.

**Por qué `instrumento` y no `contrato`.** Bajo la salida (b) la guarda deniega en los **dos** estados
de la llave (medido: 11 variantes de valor, evidencia §7), así que la fila vieja resulta **verdadera
leída sola** y **hoy nadie ve, decide ni cobra distinto**. Lo que falta es una **prueba** y una
transcripción, que es literalmente lo que `AGENTS.md` §6 clasifica como `instrumento`. **Pero eso no
salva el criterio:** `CA-12` no está cumplido, y eso va en el veredicto por criterio de arriba — no en
la clase del hallazgo.

**Y el riesgo no es hipotético, está fechado.** `D12` sigue **sin firma** y ofrece al propietario
«pedir la salida (a)». Si la firma cae de ese lado, la fila queda **falsa el mismo día en las dos sedes
heredadas y ninguna prueba lo mide** — la forma exacta de `SEC-079`.

*Dueños:* `desarrollador` (transcribir la fila en las **dos** sedes y escribir el caso con su
fail-before) y **coordinadora** (abrir la entrada de gate que el criterio nombra). *Forzador medido:*
la resolución de `D12`. *Vencimiento:* el cierre de **1.34.0**.

#### `QA-024-17` · `instrumento` · severidad **media** — el extractor de actos de `CA-12` envejece EN SILENCIO por su mitad de CAMPO

**Reproducción — dos construcciones, las dos con conducta idéntica y las dos dejando `40/6` en 10 PASS.**

**(E1) Una comilla.** En una copia de `hooks/`, `guard-completado.sh:334` pasa de
`grep -q 'Seguridad:' <<< "$nuevo"` a `grep -q "Seguridad:" <<< "$nuevo"`. El extractor captura la
clave sólo del primer literal **entre comillas simples**, así que la guarda deja de reconocerse y sus
**3** ramas caen al cubo `sin-transicion/-`.
*Observado:* actos derivados **3 → 2** (`sin-transicion/-` de 8 a **11**), actos ejercidos 2, y la
sección **10 PASS · 0 FAIL · 0 SKIP**. Queda **justo en el suelo** `≥2`, así que nada se pone rojo, y
la identidad del acto de firmar se pierde sin traza.

**(E2) Un acto nuevo con el fail-open de `SEC-083` dentro.** Inyectada, **antes de la frontera**, una
rama de denegación con guarda de campo escrita `case "$nuevo" in *"Rigor: ligero"*)` —forma que el
extractor no lee— y condición `[ -n "$qa" ] && [ "$qa" != "aprobado" ]`. **Conducta medida del acto
inyectado: `QA:` declarado → DENY; `QA:` ausente → ALLOW; `QA:` comentado → ALLOW.** Es la conducta que
`CA-12` prohíbe, en otro acto.
*Observado:* actos derivados **3 de 37 ramas** (`sin-transicion/-` de 8 a **9**), actos ejercidos **2**,
suelo cumplido, y la sección **10 PASS · 0 FAIL · 0 SKIP**. **El acto nuevo no se ejerce nunca.**

**Esperado.** El preámbulo de la sección promete: «**Y FALLA RUIDOSAMENTE, NUNCA EN SILENCIO** —es lo
único que se puede prometer de un extractor que va a envejecer—».

**Observado.** Es **cierto para la mitad FRONTERA** —renombrados `done_norm` y `estado_done`, la
sección da **5 PASS · 0 FAIL · 5 SKIP** con el motivo correcto, reproducido— y **falso para la mitad
CAMPO**. Además, **el límite que la sección declara subestima la mitad**: dice «un tercer acto sin
guarda de campo **y después de la frontera**», y la absorción ocurre en **las dos zonas**, porque
`sin-transicion/-` y `transicion/-` no son actos, son **cubos**.

**Por qué `instrumento`.** El defecto está en el **extractor**, que es un instrumento; `ADR-011`
§ Consecuencias (−) sólo promete que «se rompe», sin decir cómo, y `CA-12` declara la lista como
**cota inferior**. Así que **ni el REQ ni el ADR afirman nada falso**: la frase falsa vive dentro del
archivo de pruebas. Nadie ve, decide ni cobra distinto.

*Dueño:* `desarrollador`. *Vías conformes (elegir una):* (a) reconocer la guarda de campo por
**propiedad** y no por la forma `grep -q '<clave>:'`, abstiniéndose con SKIP cuando no pueda
identificarla; (b) **acotar el preámbulo** para que prometa ruido sólo de la frontera y publique la
cuenta de ramas **no atribuidas a ninguna guarda** como denominador visible; o (c) fijar la cuenta de
ramas y de actos contra un valor esperado, de modo que **cualquier** cambio de forma se ponga rojo en
vez de deslizarse. *Forzador medido:* la propia E1 — una comilla, la conducta intacta y el aparato
mudo. *Vencimiento:* 1.35.0.

#### `QA-024-18` · `instrumento` · severidad **media** — `emite_edit` da verde por NO EJERCER, y es un emisor compartido por 22 secciones

**Reproducción.** `run.sh:99-105` emite `old_string:"x"`. Acto de firmar, cabecera sin `QA:`, árbol
limpio:

| título del fixture | `old_string` | veredicto |
|---|---|---|
| `omite-QA` | `x` | **DENY** — el acto se ejerce |
| `omite-QA (fixture)` | `x` | **ALLOW** — el acto **no** se ejerce |
| `omite-QA (fixture)` | `ZZ-no-esta-ZZ` | **DENY** — con centinela vuelve a ejercerse |

**Observado.** Si el fixture contiene una `x`, la edición **se reconstruye**, el `new_string` cae a
mitad de línea, la cabecera no declara el campo y el veredicto sale **ALLOW**. Ese ALLOW no significa
«permitido»: significa **«no medido»**, y **un fixture que deja de ejercer su acto no se pone rojo, se
pone verde**.

**El denominador de la superficie latente, elemento por elemento** (base `9ba3b1f`): **122 usos** de
`emite_edit` en **22 secciones** — `01`(4) `02`(2) `03`(6) `04`(3) `05`(10) `08`(7) `10`(1) `11`(9)
`12`(4) `13`(15) `14`(6) `15`(15) `16`(7) `17`(4) `18`(6) `19`(2) `26`(1) `27`(11) `30`(2) `34`(2)
`36-…-5`(1) `39-…-1`(1) — más **dos** usos inline del mismo `old_string:"x"` en `40/4:160` y
`40/5:169`. **El centinela existe sólo en `40/6`.**

**La afirmación de que `40/4` y `40/5` hoy no están afectadas: VERIFICADA de forma independiente.**
Reconstruidas las **10** cabeceras de `40/5` con el formato real de `pon05` y la de `40/4`:
**0 ocurrencias de `x` en las 11**. (Mi primera reconstrucción metió la palabra «fixture» en el título
y dio 1 `x` en las diez; se descartó por no reproducir el formato real. Es el mismo error que el
centinela existe para atrapar.)

**Por qué `instrumento` y no más, dicho con el riesgo dentro.** Hoy **ninguna** celda está afectada, así
que nadie ve, decide ni cobra distinto: es el emisor de un banco de pruebas. Lo que lo pone en `media`
y no en `baja` es que **el riesgo es de todo fixture futuro de 22 secciones** y su síntoma es un
**verde**, no un rojo: la clase de defecto que `REQ-020` existe para cazar. La vía conforme es
**centinela en `emite_edit`** —una vez, para las 122— y no un centinela por sección. *Dueño:*
`desarrollador`. *Forzador:* la primera `x` que entre en un fixture de cualquiera de las 22 secciones.
*Vencimiento:* 1.35.0.

#### `QA-024-19` · `instrumento` · severidad **baja** — la clave del veredicto en minúscula evade el DISPARADOR de la guarda del orden

**Reproducción.** Cabecera **sin** línea `QA:`; edición que escribe **`seguridad: aprobado`** (clave en
minúscula), los dos estados de la llave. **Esperado:** el mismo DENY que con `Seguridad: aprobado`
(medido DENY/DENY). **Observado: ALLOW / ALLOW.**

**Causa medida.** El disparador es `grep -q 'Seguridad:' <<< "$nuevo"`
(`hooks/guard-completado.sh:334`), **sensible a mayúsculas**: el acto **no se entra**, y el lector
tampoco reconoce la clave, así que `$seg` queda vacío. **No es la ausencia resuelta del lado que abre:
es el acto que no se reconoce**, un mecanismo anterior a toda resolución de ausencia — y por eso ni
`CA-01` ni `CA-12` lo alcanzan.

**Dirección del daño, entera.** El **cierre sigue fail-closed**: un REQ `critico` cuyo único veredicto
sea `seguridad: aprobado` **no cierra**, porque el campo se lee como ausente. Lo que se pierde es (a) el
guardián del **orden** sobre esa edición y (b) **el aviso** — el «hook que avisa sin decidir» de
`AGENTS.md` §13 tampoco dispara, porque no hay campo que juzgar. Un auditor puede escribir una firma
que **nadie mide y nada comenta**.

**Y una frase del REQ que esto acota, dicha sin reescribirla.** `REQ-024` § «Una INSTANCIA medida que
entra por `CA-01`» afirma que, cerrada la dirección de la ausencia, «la vía se cierra **por
construcción**». **Para el acto de CIERRE es cierto y lo medí.** Para el acto de **FIRMAR** no lo es.
No se reescribe aquí: el write-back es del `analista-requerimientos`, y por eso no se declara `contrato`
— la frase acota una vía de `QA-023-09`, que ya está registrada como `instrumento`, y la corrección es
de una línea en una nota que no contrata nada.

*Clase:* `instrumento` (`AGENTS.md` §6: «un defecto del propio arnés —un lector de umbral, **un
guardián**, una prueba»). *Dueño:* `desarrollador`. *Forzador:* la primera firma escrita con la clave en
minúscula que nadie note. *Vencimiento:* 1.35.0.

#### `QA-024-05` — se REFINA con medición, y sigue abierto como `instrumento`

Lo abrí como conjunto abierto reconocido por cadena literal; ahora está **medido en tres sedes**:

1. **`CA-05 (ii)`**: reescrito el párrafo declarativo de `CA-05` **diciendo lo mismo** («omite la
   línea `QA:`», «deja de permitirse y pasa a denegarse», «tanto con la llave encendida como con la
   apagada», «casillas»), la sección pasa a **`6 PASS · 1 FAIL`** y nombra el patrón que falla:
   `/no declara.*QA:/`. **4 de los 6 patrones de la huella son redacción, no semántica.**
2. **`CA-06` promesas**: la forma **conocida** inyectada en el apartado → **FAIL** (discrimina); una
   forma **nueva** que dice lo mismo → **PASS**, y el denominador sigue diciendo «**2** promesas»
   habiendo **3**. **El instrumento no puede avisar de que su clase creció.**
3. **`CA-05 (iii)`**: **no medido** — la mutación de la skill no tomó efecto y esa corrida se
   descartó. Se declara por construcción, no por medición.

*Sin cambio de clase ni de dueño.* Lo que se añade es que la vía conforme incluye **publicar como
denominador lo NO reconocido**, no sólo lo reconocido.

### Hallazgos RETIRADOS en esta vuelta, con lo que queda después

| Hallazgo | Estado | Verificación |
|---|---|---|
| `QA-024-12` (declarado `contrato` en el campo, `instrumento` en este registro) | **RETIRADO** | `guard-completado.sh:546` enruta `Seguridad` por `arnes_resuelve_ausencia`; mutada **sólo** la tabla, el veredicto **se mueve** (DENY→ALLOW con la llave encendida) — control positivo propio, más 5 casos de banco en `40/4`. **Y la trampa latente NO se activó**: `estandar` y `ligero` sin `Seguridad:` siguen dando ALLOW en los dos estados. *Residual declarado:* la tabla dice `deniega` a secas y la conducta es condicional al rigor — `instrumento`, dueño `desarrollador`, forzador: un segundo llamador fuera de la rama de `critico`; vencimiento 1.35.0 |
| `QA-024-13` (`contrato`) | **RETIRADO** | El encabezado se retituló **por propiedad y sin número** (`requirements/REQ-024.md:912`), con su fila de Historial del 2026-09-10 |
| `QA-024-14` (`contrato`) | **RETIRADO** | La celda de evidencia de `CA-03` fila 3 cita ahora **literalmente** lo que hay en `requirements/REQ-016.md:7-8` (`QA: aprobado … a57eecc` y `Seguridad: con-hallazgos … R-026`) |
| **`SEC-083`** (`contrato`) | **NO se retira** | Dos motivos: (a) retirar un hallazgo de **seguridad** es acto del `auditor-seguridad`, no de QA; (b) su remediación **(2)** está escrita «*mientras (1) no exista*» y **(1) ya existe**, así que hay que releerla contra el texto **más estricto** de `CA-12 (ii)` — que **no está transcrito** (`QA-024-16`). Estado verificado: **(1) hecha y medida**, **(3) hecha y medida**, **(2) pendiente** |

**Y la comprobación que la vuelta anterior enseñó — qué queda DESPUÉS de retirar.** Con los tres
retirados quedan **nueve `instrumento`** heredados (`QA-024-04`…`-11` y `-15`) más los **cuatro
nuevos** de esta vuelta, es decir **trece `instrumento` + `SEC-083 (contrato)`**: **un solo
bloqueante**. Si
además se retirara `SEC-083`, desaparecería lo **único** que representa la fila de `AGENTS.md` §13 que
falta, y el cierre pasaría a depender sólo de la cola. Es exactamente el patrón que ya costó un ALLOW
sobre una firma vencida. **Por eso se queda.**

### Resumen de clases de esta vuelta

| Hallazgo | REQ | Clase | ¿Bloquea? |
|---|---|---|---|
| `QA-024-16` | 024 | `instrumento` | no — **pero `CA-12` no está cumplido** |
| `QA-024-17` | 024 | `instrumento` | no |
| `QA-024-18` | 024 | `instrumento` | no |
| `QA-024-19` | 024 | `instrumento` | no |
| `SEC-083` (heredado) | 024 | **`contrato`** | **sí** |

**Ningún hallazgo nuevo de esta vuelta bloquea el cierre.** Lo que impide `QA: aprobado` es otra cosa y
va dicha sin adornos: **`CA-12 (ii)` es un criterio del REQ vigente y no está implementado**, y `CA-03`
sigue sin acreditar por una firma que no es mía. Aprobar con eso sería aprobar contra un REQ que
describe más de lo construido, que es deriva del lado contrario (`AGENTS.md` §9).

### Recomendación sobre el tope de vueltas

Agotada la tercera vuelta, `AGENTS.md` §6 da dos salidas terminales y **ninguna es mía**. Mi
recomendación, con su motivo:

**Cerrar con residual declarado NO es conforme hoy**, porque lo que queda **no** es sólo residual: hay
un **criterio contratado sin implementar** (`CA-12 (ii)`) y un `contrato` abierto (`SEC-083`) cuya
remediación (2) es justo ese criterio. Cerrar así dejaría la fila que **todo proyecto hereda** sin la
condición que el propio REQ le exige, y sin ninguna prueba que la sujete.

**Recomiendo `Estado: bloqueado` con escalada al propietario**, con un motivo que es de **decisión** y
no de trabajo: la fila de `AGENTS.md` §13 **no puede escribirse** hasta que `D12` diga si la salida es
la **(a)** o la **(b)**, porque la redacción es **distinta en cada una** y el propio REQ prescribe dos
textos alternativos. El trabajo pendiente es de **una fila en dos sedes más un caso de banco** —horas,
no días—, pero **está aguas abajo de una firma humana que no ha llegado**. Esa es la definición de
bloqueado, no de residual.
