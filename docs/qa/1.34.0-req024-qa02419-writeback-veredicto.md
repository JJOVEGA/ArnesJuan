# QA — verificación del WRITE-BACK de `QA-024-19` (`REQ-024`, `CA-13` y las sedes de la promesa)

**Worktree:** `/home/juan/dev/ArnesJuan-1.34-reparaciones` · **rama** `feat/1.34-cierre-alcance`
· **commit verificado** `7154769` · árbol limpio · 2026-09-11.
**Objeto:** el write-back del `analista-requerimientos` (`21c52c6..7154769`), que mi vuelta 5
declaró que era lo único que faltaba para cerrar `QA-024-19`.
**Veredicto:** **`QA-024-19` NO CIERRA.** Sigue `contrato`. Nace **`QA-024-33`** (`contrato`).

---

## 0. Número de vuelta — declarado, no dejado al lector

**Esta verificación NO gasta vuelta del contador dev↔QA. Se registra como cierre de la VUELTA 5.**

El criterio es el que ya apliqué —y que fue correcto— al negarme a llamar «acotada» a la vuelta 5:
**el contador se lleva por el trabajo, no por la etiqueta.** Aplicado aquí, por sus dos hechos
medidos:

1. **No hay entrega del `desarrollador` ni cambio de código.** Medido:
   `git diff a05994f HEAD -- hooks tests` → **salida vacía**. El árbol que valido es, byte a byte
   en hooks y tests, el mismo que ya acredité entero en la vuelta 5.
2. **El trabajo verificado es del `analista-requerimientos`, y es la primera entrega de ese
   write-back**, no la re-entrega de algo que ya había entregado con defecto. Mi propia vuelta 5
   escribió que lo que impedía cerrar `QA-024-19` **«no es un defecto de este entregable»**.

Un ciclo dev↔QA se gasta cuando el REQ vuelve al **desarrollador** por un defecto de lo que
entregó. Aquí ni volvió ni había defecto suyo. Llamar «vuelta 6» a esto inflaría el contador con
trabajo que no es el que el tope acota; llamarlo «tramo» para esconder una vuelta real sería el
error inverso. Ninguno de los dos aplica: **es la mitad pendiente de la vuelta 5, ejecutada por
otro rol sobre la cara de texto.**

**Observación para el propietario, no hallazgo.** A partir de hoy `REQ-024` tiene **dos** bucles de
retrabajo vivos y sólo uno tiene tope: el dev↔QA (5 de 3, con autorización expresa) y el
**analista↔QA sobre el write-back**, que `AGENTS.md` §6 no acota porque el tope está escrito sobre
dev↔QA. `QA-024-33` abre la **primera** vuelta de ese segundo bucle. Si el propietario quiere que
ese bucle también tenga tope, es decisión suya y no la invento yo; lo dejo dicho para que el
contador sea auditable desde el disco y no dependa de que alguien lo recuerde.

---

## 1. Lo que SÍ cumple — acreditado, y no se re-mide

### Vía (b) de mi vuelta 5: `:815` queda **cierta**. **PASA.**

*Antes* (`:815`): «`CA-01` … contrata la **dirección** … Cerrada la dirección, la vía se cierra
**por construcción**» — **sin acto**, que es lo que la hacía falsa para el acto de FIRMAR.

*Ahora* (`:920-948`): la afirmación queda **acotada al acto de CIERRE** —«para ese acto, **y sólo
para ese**»— con la medición que la sostiene (19 de 19 celdas idénticas contra `v1.33.2`, la base y
el candidato), y un párrafo nuevo dice literalmente que **para el acto de FIRMAR la frase era falsa
y que esta reparación no la vuelve verdadera: la hace hablar**. Es exactamente lo que el hallazgo
pedía, y además sin convertir la corrección en un descargo.

**Y el titular acompaña**, que es donde los barridos anteriores se quedaban cortos: el **título**
de la sección pasó de «entra por `CA-01` **sin criterio nuevo**» a «entra por `CA-01` **en el acto
de CIERRE**, y en el acto de **FIRMAR** necesitó criterio propio (`CA-13`)». Leído **aislado**
(`REQ-023 CA-10`) ya no afirma nada falso.

### Vía (a), parte 1: `CA-13` contrata **lo que se entrega**, no lo que el hallazgo pedía al nacer. **PASA.**

`QA-024-19` nació pidiendo *«el mismo `DENY`»*. `CA-13 (ii)` contrata **`ALLOW` + aviso** y dice
**«y NO deniega»** de forma explícita, con las dos razones medidas (ensanchar el lector movería
decisiones y estrecharía `REQ-016 CA-04`; una errata de caja convertida en bloqueo produce la
fricción que acaba con el guard apagado). **No promete en ninguna parte el `DENY` que no ocurre.**
El cambio del `Esperado:` del propio hallazgo queda declarado **dentro** del criterio y en su
titular, no en una nota al pie.

### Comprobación 1 — ¿propiedad o lista? **PASA en lo principal.**

- **El conjunto se deriva ejerciendo el lector**, no se enumera: «conjunto cuya pertenencia decide
  el **código**, cuyo **sitio único** es el propio lector (`arnes_declara_clave` →
  `arnes_campo_linea` → `arnes_norm_clave`, `hooks/lib.sh`) y que el caso **deriva ejerciéndolo**,
  **nunca** de una lista escrita en el banco, en este criterio ni en ningún otro documento».
  No enumera `_Seguridad_`, `SeGuRiDaD` ni la minúscula como conjunto cerrado; los ejemplos van
  marcados **no exhaustivos**. Una forma que el lector acepte mañana **entra sola**.
- **Las cifras entran como evidencia fechada, no como umbral.** Verificado uno por uno: (iii)
  «*(Medición fechada del 2026-09-11, que es evidencia y **no** el umbral: 37 … 19 …)*»; (iv)
  «*(Medición fechada del 2026-09-11: 18 vecinas, 0 falsos positivos.)*»; (vi) «*(Medición fechada
  del 2026-09-11: `11 → 9`, `10 → 9` y `8 → 8`)*». **Ninguna de mis tres cifras quedó convertida en
  techo.**
- **Todo número declara si es operativo o de contrato, con su dirección.** Barrido completo del
  bloque: `no más de 1` aviso (**operativo**, hacia la agregación); cero celdas movidas (**de
  contrato**); cero falsos positivos (**de contrato**); `no menos de 6` vecinas (**operativo**,
  hacia arriba); `no más de 0` procesos añadidos (**operativo**, hacia menos); suelos `no menos de
  8` / `no menos de 6` (**operativos**, hacia arriba). **Cero números desnudos.** Y el coste va como
  **techo con dirección**, nunca como igualdad.

*(La excepción a este «pasa» está en `QA-024-33` §A: hay **una** enumeración mal formada, y es
justamente la que produce el defecto de §2.)*

### El campo `Archivos:` — **PASA**, y no es un detalle

Gana `tests/escenarios/hooks/secciones/42-disparador-y-lector.sh`, sede del caso de `CA-13`.
Verificado además que el campo sigue **sin decoración** (0 backticks, 0 subrayados en la línea):
con el fail-open de `SEC-020` abierto, un `Archivos:` decorado haría que `arnes-paralelo.sh`
respondiera `disjunto` sobre un mapa corrompido. No ocurre.

### Quality gates de §7 — **en verde**, y digo por qué las corrí

Las corrí aunque esto sea verificación de texto, porque el veredicto se firma sobre un árbol:
`bash -n` sobre todo `hooks/*.sh` y `tools/*.sh` → OK; `jq -e .` sobre `hooks/hooks.json`,
`.claude-plugin/plugin.json` y `.claude-plugin/marketplace.json` → OK.
**No repetí mis 56 celdas ni el banco entero**, y el motivo está medido: `git diff a05994f HEAD --
hooks tests` sale **vacío**, así que la acreditación de la vuelta 5 sigue vigente sobre este árbol.

---

## 2. `QA-024-33` — **NUEVO** · clase **`contrato`** · bloquea

### El criterio nuevo afirma algo falso sobre lo construido: la clase que declara «frontera» **sí avisa** en parte, y está medido

Es el mismo defecto de forma que este REQ persigue, en la frase que lo persigue. La comprobación 2
que se me pidió era verificar **el razonamiento**, no que el texto estuviera. El texto está. **El
razonamiento no se sostiene.**

#### A) La causa mecánica: `CA-13 (ii)` **enumera** el plegado, y lo enumera **incompleto**

`requirements/REQ-024.md:808-809`:

> «el **mismo plegado que el arnés ya aplica a los valores** (blancos, marcado, caja) sí devuelve
> la clave»

El plegado real es `arnes_norm_campo` (`hooks/lib.sh:1439-1464`) y tiene **cuatro** pasos, no tres:

| paso | código | ¿está en la enumeración del criterio? |
|---|---|---|
| blancos (espacio y tabulador) | `lib.sh:1460` | sí |
| marcado de Markdown | `arnes_desenvuelve`, `lib.sh:1461` | sí |
| **ortografía — acentos NFC y NFD** | **`arnes_pliega_ortografia`, `lib.sh:1462`** | **NO** |
| caja | `${v,,}`, `lib.sh:1463` | sí |

Además de estar incompleta, la enumeración **está mal formada** por `requirements/README.md:310-316`:
es un conjunto cuya pertenencia decide el código, enumerado con **tres** elementos concretos, **sin
la marca `no exhaustivo` y sin puntero al sitio único** en esa frase. Es la forma (a) que el README
prohíbe por nombre. El propio `CA-13 (i)` la hace bien dos párrafos antes; `(ii)` la hace mal.

#### B) La consecuencia: la clase frontera de `CA-13 (v)` es **falsa**, y lo es en la dirección que produce un arreglo equivocado

De esa enumeración incompleta el analista deriva que la clasificación `desfase` «**repone el
plegado** pero **no repone letras**» y que por tanto la frontera queda excluida **por
construcción**. **El plegado sí repone letras**: `arnes_pliega_ortografia` mapea cada vocal
acentuada a su vocal base, en las dos formas de guardado (NFC precompuesta y NFD con diacrítico
combinante). Una clave con **una letra sustituida** por su acentuada **cae dentro del plegado** y
por tanto **sí produce aviso**.

**Medido ejerciendo el lector** (no leído a ojo), sobre `hooks/lib.sh` @ `7154769`, script en
`/tmp/.../scratchpad/ejerce.sh`, método: cargar `hooks/lib.sh` e invocar
`arnes_declara_clave "<cabecera>" "Seguridad"` con la clave variada y todo lo demás fijo.

| clave escrita en la cabecera | `ARNES_DECLARA` | ¿avisa? | lo que dice `CA-13 (v)` |
|---|---|---|---|
| `Seguridad:` (limpia) | `lee` | no | correcto (control) |
| `_Seguridad_:` (marcado) | `lee` | no | correcto (control) |
| `seguridad:` (minúscula) | `desfase` | **sí** | correcto |
| `SEGURIDAD:` / `SeGuRiDaD:` | `desfase` | **sí** | correcto |
| `Ѕeguridad:` (homóglifo `U+0405`) | `no` | no | correcto — frontera real |
| `Segurida:` (letra omitida) | `no` | no | correcto — frontera real |
| `Segurxdad:` (letra sustituida por otra latina) | `no` | no | correcto — frontera real |
| **`Segurídad:`** (`i` → `í`, NFC) | **`desfase`** | **SÍ AVISA** | **«pasa sin aviso» — FALSO** |
| **`Segurídad:`** (`i` + `U+0301`, NFD) | **`desfase`** | **SÍ AVISA** | **«pasa sin aviso» — FALSO** |
| **`Séguridad:`** (`e` → `é`) | **`desfase`** | **SÍ AVISA** | **«pasa sin aviso» — FALSO** |
| **`Segúridad:`** (`u` → `ú`) | **`desfase`** | **SÍ AVISA** | **«pasa sin aviso» — FALSO** |

**Y no es una lectura forzada del término «letra»: es el vocabulario del propio proyecto.**
`docs/qa/1.34.0.md:3343` y `docs/qa/1.34.0-req024-qa-metodo.md:434` rotulan `QÁ:` como
«**letra sustituida**». Ejercido contra este mismo árbol: `arnes_declara_clave` sobre `QÁ:` devuelve
**`desfase`** → **avisa**. La clase que `CA-13 (v)` declara «pasa sin aviso» contiene, con el
nombre que este repositorio ya le daba, un miembro que avisa.

*Resultado esperado:* que la clase declarada frontera se enuncie por la **propiedad que el código
decide** —lo que el plegado **no** repone— de modo que su pertenencia sea derivable ejerciendo el
lector, con el sitio único citado.
*Resultado observado:* se enuncia por la palabra «letra», que **no** es la frontera del código: el
plegado repone unas sustituciones de letra (acentos) y no otras (homóglifo, latina distinta,
omisión). Cuatro miembros medidos de la clase declarada la desmienten.

#### Por qué bloquea, y no es un matiz de redacción

`CA-13 (v)` no se limita a describir: **prescribe cómo debe comportarse el caso del banco** —«Un
caso que midiera esta forma y la diera por conforme **incumpliría** este criterio: lo conforme es
medirla y **publicarla como frontera abierta, con su control en la misma corrida**»—. Un
`desarrollador` que escriba ese caso tomando `Segurídad:` o `QÁ:` como instancia de la clase
nombrada obtiene un **aviso** donde el criterio le dice que debe haber silencio, y tiene dos
salidas, las dos malas:

- publicar como «frontera abierta» algo que **sí** avisa —el criterio queda acreditado por una
  medición falsa—; o
- «arreglar» el código para que esa forma deje de avisar, lo que obliga a tocar
  `arnes_pliega_ortografia`, que es **compartido con el lector de valores**. Eso **mueve
  decisiones** y estrecha la tolerancia que `REQ-016 CA-04` contrata — exactamente lo que
  `CA-13 (iii)` prohíbe como incumplimiento.

Es decir: el criterio, tal como está escrito, tiene un camino mecánico hacia el defecto que él
mismo declara prohibido. Eso es `contrato`, no `instrumento`.

#### Comprobación 3 — las sedes. **Tres vivas, y la peor es superficie heredada**

Barrido **por propiedad** (familia «sustituye u omite una letra» + «por construcción» + «deja de
haber hueco»), sobre todo el árbol y no sobre la lista del analista:

| # | sede | línea | estado |
|---|---|---|---|
| 1 | `CA-13 (v)`, dentro del criterio | `REQ-024.md:850-851` | **falsa** — clase desmentida por 4 medidas |
| 2 | párrafo nuevo de `:815` («sigue habiendo una frontera que no avisa») | `REQ-024.md:945-946` | **falsa** — misma clase, misma forma |
| 3 | **celda de `AGENTS.md` §13 que `CA-12 (ii)` prescribe — SUPERFICIE HEREDADA** | `REQ-024.md:1159-1160` | **falsa** — «o pasa SIN aviso cuando se ha sustituido u omitido una letra de la clave» |
| 4 | fila del Historial que transcribe `(v)` | `REQ-024.md:1712` | transcripción de la misma frase |

La **(3)** es la que obliga a no cerrar aunque las otras se vieran menores: es el texto destinado a
`AGENTS.md` §13 y a `templates/AGENTS.md.tpl`, y de ahí **a todo proyecto que instale el arnés**.
Una frontera mal trazada en superficie heredada es la forma exacta en que este proyecto lleva
`SEC-079`/`SEC-081` pagando: una promesa cuya excepción no coincide con lo que la máquina hace.

**La celda heredada, en su otra mitad, SÍ está bien**, y lo acredito porque era el punto 3 del
encargo: la afirmación de que la mención del hallazgo «**desaparece** porque deja de haber hueco»
**ya no está**. Ahora dice que **no desaparece: cambia de hallazgo** —el hueco que se cerró era el
de `SEC-083`; sigue el de la entrada al acto (`SEC-084`/`QA-024-19`), «estrechado hasta el borde
del lector y no cerrado»—. Eso es correcto y está medido. Y la variante **(A)** queda marcada
**histórica y no vigente**, con su motivo (su celda diría hoy que la guarda «no deniega», y
deniega). Las «tres reglas» pasan a **cuatro**, con la (4) sobre el disparador.
**El defecto de esta sede es sólo la frase de la frontera.** Corregirla no exige rehacer la celda.

*Clase:* **`contrato`** — el REQ afirma algo falso sobre lo construido, medido, en tres sedes vivas
y una de ellas heredada. *Dueño:* `analista-requerimientos`. *Forzador:* el propio cierre de
`QA-024-19`, que no puede firmarse mientras esto siga escrito.
*Lo que NO pido:* no pido ensanchar ni estrechar el lector, y el arreglo **no** debe tocar
`hooks/`. Es una corrección de texto en tres sedes.

---

## 3. `QA-024-19` — **NO CIERRA.** Clase `contrato`, sin cambio

Mi vuelta 5 condicionó el cierre a **dos** write-backs, y el primero decía, literalmente, que
faltaba un criterio «sobre el aviso nuevo, **su propiedad ni su frontera**». Balance:

| condición de cierre que yo fijé | estado |
|---|---|
| 2. `:815` deja de afirmar algo falso | **CUMPLIDA** |
| 1a. existe criterio del comportamiento nuevo | **CUMPLIDA** — `CA-13` |
| 1b. contrata lo entregado (`ALLOW` + aviso), no el `DENY` que pedía el hallazgo | **CUMPLIDA** |
| 1c. la **propiedad** enunciada por propiedad y no por lista | **CUMPLIDA** |
| **1d. la FRONTERA** | **NO CUMPLIDA** — `QA-024-33`: la clase declarada es falsa, medida |

La frontera estaba **nombrada en mi condición de cierre**, no añadida ahora. Cerrar `QA-024-19`
dejando la frontera mal trazada sería aprobar un REQ que describe un comportamiento que la máquina
no tiene, que es la deriva de `AGENTS.md` §9 — la misma que el hallazgo vino a impedir.

**Lo que sí cambia respecto de la vuelta 5, y queda escrito para que el próximo pase sea
quirúrgico:** el motivo de que `QA-024-19` siga abierto **ya no es la ausencia del write-back**.
Es **una frase** del write-back, en **tres** sedes, con el arreglo acotado y sin tocar código.

---

## 4. Fuera de alcance — dicho, no tocado

- **`SEC-084`:** no es mío. Dejo constancia, porque se me pidió, de que **su mitad de texto sigue
  AUSENTE**: `AGENTS.md:353` y `templates/AGENTS.md.tpl:311` siguen prometiendo la invariante del
  orden **sin declarar su alcance**. El write-back **fija la redacción** de esa celda pero **no la
  transcribe** —la transcripción es del `desarrollador` con el gate humano de `CA-12 (ii)`—, y el
  propio analista lo declara así en su fila de Historial, lo cual es honesto y lo acredito.
  *Consecuencia de `QA-024-33` sobre esto:* esa redacción **no debe transcribirse tal como está**,
  porque llevaría la frase falsa a la superficie heredada.
- **`CA-12 (ii)`, `CA-03`, los `instrumento` abiertos, el instrumento de rendimiento** (cuya
  reapertura el propietario **prohibió**), otros REQ y mejoras: **no tocados**.

### `QA-024-32` — información sobre el hallazgo, no defecto del write-back

Su forzador era este write-back. **No saltó** en las seis ediciones del analista, y él declaró la
explicación como **plausible y no verificada**: los hooks que gobiernan la sesión son los de la
instalación estable `v1.33.x`, donde la rama del aviso de desfase todavía no existe. **Coincide con
lo que observo yo**: mis ediciones sobre la cabecera de `REQ-024` en esta sesión tampoco lo
dispararon. Eso **refuerza** la explicación del analista y la mantiene como no verificada —para
verificarla haría falta ejercer el hook del árbol candidato, no el instalado—. **Lo anoto y no lo
arreglo**, conforme al encargo. `QA-024-32` sigue `instrumento`, no bloquea, sin cambio.

### Observación menor, sin hallazgo propio

El aviso del código (`hooks/guard-completado.sh:287,290`) dice «el marcado de Markdown sí se tolera
pero la CAJA no» y tampoco menciona los acentos. **No es falso** —no afirma exhaustividad y es un
mensaje, no un criterio ni un disparador—, así que **no abro hallazgo**. Lo dejo escrito porque, si
el analista corrige la frontera, conviene que quien redacte no lo tome como sitio único del plegado:
el sitio único es `arnes_norm_campo`.

---

## 5. Evidencia y método

- **Árbol:** `feat/1.34-cierre-alcance` @ `7154769`, limpio. **Código sin cambios** desde `a05994f`:
  `git diff a05994f HEAD -- hooks tests` → vacío.
- **Delta verificado:** `git diff 21c52c6 7154769 -- requirements/REQ-024.md` (8 hunks, +193/-20).
- **Medición del lector:** script en el scratchpad de la sesión, reproducible en tres líneas —
  `. hooks/lib.sh` y `arnes_declara_clave "<cabecera con la clave variada>" "Seguridad"`, leyendo
  `ARNES_DECLARA`. 13 formas ejercidas, 2 de control (`lee`), 3 de desfase conocido, 3 de frontera
  real, **4 que desmienten la clase declarada** (NFC y NFD), más `QÁ:` con el rótulo del propio
  repositorio. **Determinista**: repetido, mismo resultado; ningún resultado flaky.
- **Gates §7:** en verde (detalle en §1).
