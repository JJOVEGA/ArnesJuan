# REQ-023 · vuelta 3 de 3 (qa-tester) — método y evidencia para re-derivar sin preguntar

**Versión base:** `hooks/` y `tests/` tal como quedan en **`6ad9752`** (rama `rel/registro-1.33.0`),
más el write-back del analista a `requirements/REQ-023.md`, que sigue **sin comitear**.

**Y la base cambió a mitad de la comisión, dicho porque afecta a cómo se lee todo lo de abajo.** El
encargo la fijaba en el árbol de trabajo sobre `5305de9`. A las **~19:00** aterrizó `6ad9752`
(«Sección 39 partida, índice cuadrado y la ventana de REQ-019 al día»), que **comiteó exactamente los
bytes que ya estaban en ese árbol de trabajo**: las cinco secciones `39-*`, la sección 36, `run.sh`, el
README del banco, `requirements/README.md`, `REQ-024.md`, `REQ-019.md`, `PENDING_APPROVAL.md` y dos
documentos de `docs/qa/`. **`hooks/` no entró en ese commit y no se ha tocado en toda la comisión**, así
que las medidas siguen valiendo: lo que validé es byte a byte lo que hoy está en `6ad9752`, y
`git status` lo confirma —ninguna modificación local en `hooks/` ni en `tests/`—. Las corridas `A`, `B`
y `C` se tomaron antes del commit sobre esos mismos bytes; la `D`, después.
**Fecha:** 2026-09-09 · **Agente:** `qa-tester` (Opus, política de autoalojamiento, `AGENTS.md` §5).
**Máquina:** WSL2 Linux 6.18.33.2, `bash` 5.3.9, `jq` 1.8.2, GNU awk 5.3.2, **sin `cygpath`**,
locales disponibles **sólo** `C`, `C.utf8`, `POSIX` (`locale -a`).
**Cifras operativas:** son la medición de este día en esta máquina y se rehacen midiendo.

Este archivo existe porque, sin él, la auditoría de seguridad, el propietario y el write-back del
analista tendrían que **re-medir** para cumplir algo ya escrito (`AGENTS.md` §14.B.7). El del
`desarrollador` de esta misma vuelta es `docs/qa/1.34.0-req023-vuelta3-metodo.md`; **no se toca**,
porque `CA-09 (iii)` lo cita por sección.

## 0. Árboles usados, y por qué ninguno movió el árbol de trabajo

Nada de `git stash`, `reset --hard`, `clean -f`, `checkout .` ni `restore .` (`git.prohibidos`).

```
git worktree add --detach <tmp>/wt 5305de9        # (5305de9) copia limpia para los pares discriminantes
git archive v1.33.0 hooks | tar -x -C <tmp>/hooks-v1330 --strip-components=1   # LINEA BASE HEREDADA
git archive v1.33.0 tools hooks | tar -x -C <tmp>/tools-v1330                  # el informe heredado (CA-07)
cp -r hooks <tmp>/hooks-real ; cp -r hooks <tmp>/hooks-sobre ; cp -r hooks <tmp>/hooks-muda
```

Comprobación de que `v1.33.0` es de verdad la heredada: `grep -c ARNES_CLAVES <tmp>/hooks-v1330/lib.sh`
→ **0** (la constante de CA-06 no existe ahí). El worktree se **retira** al terminar
(`git worktree remove`); las copias de `hooks/` viven en el scratchpad y no en el repositorio.

Las secciones sucias del árbol se copian al worktree porque `REPO92` de la sección 39/5 se deriva de
`SEC_DIR` (`${SEC_DIR%/}/../../../..`, `39/5:32`) y **no** de `HOOKS_DIR`: fuera de un repo con `.git`,
`mat92` no encuentra el tag y las dos mitades **se abstienen** — el par no discriminaría por otra razón.

## 1. Puertas de esta comisión (cifras PROPIAS, no heredadas del informe del dev)

```
for f in hooks/*.sh tools/*.sh; do bash -n "$f" || exit 1; done              # rc 0
jq -e . hooks/hooks.json >/dev/null                                         # rc 0
jq -e . .claude-plugin/plugin.json && jq -e . .claude-plugin/marketplace.json  # rc 0
for f in tests/escenarios/hooks/secciones/*.sh; do bash -n "$f"; done       # rc 0 (57 secciones)
bash tests/escenarios/hooks/autoprueba-corredor.sh                          # 106 PASS · 0 FAIL
bash tools/arnes-lectura.sh .                                               # rc 0, ningún valor anómalo
jq -r '.quality_gates[]' .arnes/config.json                                 # las TRES de arriba, idénticas a AGENTS.md §7
```

**Y la cola de aprobaciones NO está en 0, contra lo que decía el encargo — medido, y explicado en vez
de reclamado.** `tools/arnes-lectura.sh .` dio **`0 pendiente(s)`** a las **18:56:54** y
**`4 pendiente(s)`** a las **19:06:25** del mismo día. **No es un defecto del lector:** el archivo se
reescribió a las **19:00:33** (`stat -c %y`) y su contenido volvió a ser **exactamente el de `HEAD`**
(`git hash-object PENDING_APPROVAL.md` == `git rev-parse HEAD:PENDING_APPROVAL.md` ==
`666554578b9867c20d23b65a951f5d55f4ed557a`), que ya traía las cuatro entradas `D1`–`D4`. Comprobado
además con **30 lecturas** consecutivas —10 en reposo y 20 intercaladas con la autoprueba corriendo—:
**4 en las treinta**, y `PENDING_APPROVAL.md` con el `mtime` y el hash intactos al terminar. Es decir:
la cola cambió **debajo de esta comisión**, no el lector.

**Consecuencia operativa, y es la segunda razón independiente por la que hoy no cierra nada:** con 4
entradas en «Pendientes», `guard-completado` **deniega el cierre de cualquier REQ** (`AGENTS.md` §6), y
el propio informe lo dice así: «*4 pendiente(s) — ningún REQ puede cerrarse*». `D1` es precisamente la
entrada que **espera este veredicto**, y `D4` es la que ya propone la ventana 1.35.0 de la
superlinealidad — que por eso **no fijo yo**.

### El banco: TRES corridas, y NO está en verde en este host

```
bash tests/escenarios/hooks/run.sh   (A)   958 PASS ·  1 FAIL · 7 SKIP   (total 966 ✔)
bash tests/escenarios/hooks/run.sh   (B)   954 PASS ·  4 FAIL · 8 SKIP   (total 966 ✔)  rc=1
bash tests/escenarios/hooks/run.sh   (C)   958 PASS ·  1 FAIL · 7 SKIP   (total 966 ✔)  rc=1
bash tests/escenarios/hooks/run.sh   (D)   958 PASS ·  0 FAIL · 8 SKIP   (total 966 ✔)  rc=0  <-- VERDE
bash tests/escenarios/hooks/run.sh   (E)   959 PASS ·  1 FAIL · 6 SKIP   (total 966 ✔)  rc=1
bash tests/escenarios/hooks/run.sh   (F)   957 PASS ·  1 FAIL · 8 SKIP   (total 966 ✔)  rc=1  <-- ÁRBOL ENTREGADO
```

**Seis corridas locales: 5 rojas y 1 verde, el total cuadrando en 966 las seis, y los 42 casos de
REQ-023 en `40 PASS · 0 FAIL · 2 SKIP` las seis.** Los FAIL locales son siempre de reloj y siempre
ajenos a este REQ: `DEV v3 heredoc ~300 KB` (4 466 · 4 357 · 4 344 ms contra techo holgado 4 000, con
veredicto `allow` **correcto** las tres veces) y la familia `REQ-021 CA-03`/`CA-10`/`CA-10.2`. La `F`
es la del árbol que se entrega, tomada después de la última edición de `requirements/REQ-023.md`.

**Y la corrida que decide no es ninguna de las cinco: es el CI, y sale ROJA por un caso de este REQ.**
`hooks-en-linux` sobre **`6ad9752`** (PR #45): **955 PASS · 1 FAIL · 10 SKIP**, total cuadrando en 966.

```
FAIL  REQ-023 CA-09 (iii) arnes_campo_linea …
      mediana 2.365× > techo 2.200×   (margen 0.165× > dispersión 0.023×)
```

Lo verifiqué en lo que se puede verificar sin el CI: el archivo del caso es **byte a byte idéntico**
entre `6ad9752` y el árbol local (`git diff --stat 6ad9752 -- …-4-el-coste.sh` → vacío), el techo está
**codificado literal** (`:271`, `coc94 "…" "$fn94" 2200`), y el criterio que viajó en `6ad9752` es el
**viejo** (`git show 6ad9752:requirements/REQ-023.md | grep -c 'relación emparejada'` → **0**; el
archivo local → **8**). **La diferencia entre el rojo del CI y mis cinco abstenciones no es de código:
es el ruido del host** — dispersión **0,023×** allí contra 0,245× (A) y 0,788× (E) aquí, entre 10× y
34× más ancha. Consecuencias en `QA-023-10` (la puerta requerida roja) y `QA-023-12` (la abstención que
enmascaró el rojo durante nueve corridas entre tres personas).

**La corrida `D` es la que cuenta como cifra de entrega, y va la cuarta a propósito:** se tomó
**después** de escribir `requirements/REQ-023.md`, `CHANGELOG.md` y los dos documentos de
`docs/qa/`, que están **dentro del corpus del propio banco** (la sección 39/5 lee
`$REPO92/requirements/*.md` y el glob de secciones). Sin ella las cifras publicadas serían de un árbol
anterior al que se entrega. **Y sale en verde**, que es lo que vuelve `QA-023-14` un **flaky** y no una
regresión: el árbol entregado **puede** dar 0 FAIL; lo que no puede es darlo de forma reproducible.
Su lista de SKIP son los 6 estables más las **dos** de la familia `REQ-017 CA-08 (ii)` (REQ real de 6
líneas + cabecera de 200), que es la que oscila.

**El total cuadra en 966 las tres veces**: no se creó ni se perdió ningún caso. Y los casos de
REQ-023 son **idénticos en las tres corridas**: **42 casos · 40 PASS · 0 FAIL · 2 SKIP**, siendo los
dos SKIP los de `CA-09 (iii)` (§5). **Ningún FAIL de ninguna corrida es de REQ-023.**

**Los FAIL, elemento por elemento** (`QA-023-14`):

| corrida | caso | número |
|---|---|---|
| A | `REQ-021 CA-03 (c)` el tamaño de cada mitad se DERIVA del suelo | el sensible base mide **147 616 µs** y no llega a **150 000 µs** |
| B | `DEV v3: heredoc CITADO de ~300 KB -> allow y barato` | **4 466 ms** (techo holgado 4 000), veredicto `allow` **correcto** |
| B | `REQ-021 CA-03` la calibración distingue sensible de insensible | **a=2550** fuera de la banda [1600, 2400] · b=1303 fuera de [800, 1200] |
| B | `REQ-021 CA-10` el juez | cascada del anterior: se esperaba `<PASS SKIP…>` y salió `<FAIL SKIP…>` |
| B | `REQ-021 CA-10.2` vivos>0 | cascada del anterior: «la calibración real se rechazó» |
| C | `DEV v3: heredoc CITADO de ~300 KB` | **4 357 ms** (techo holgado 4 000), veredicto `allow` **correcto** |

**La LISTA de SKIP, no el total** (`docs/ESTADO.md` ya corrigió que el total engaña). Presentes en las
**tres**: `ruta estilo Windows con backslashes` (sin `cygpath`, permanente en este host) ·
`REQ-017 CA-05 (i)` · `REQ-017 CA-05 (ii)` · `REQ-021 CA-08 (iii)` · **`REQ-023 CA-09 (iii)`
`arnes_norm_clave`** · **`REQ-023 CA-09 (iii)` `arnes_campo_linea`** = **6**. Varía la familia
`REQ-017 CA-08 (ii)` (la sonda que no converge): **1** en A (REQ real de 6 líneas), **2** en B (6
líneas + cabecera de 200), **1** en C (cabecera de 200). Es la oscilación por reloj que su propio
motivo declara.

## 2. `QA-023-04` — CERRADO, verificado ejecutando el lector

El hallazgo era que la derivación por `sed` de `36-…-2-los-lectores.sh:228` leía el **texto** de los
brazos `case` y se estrechó de 6 claves a 5, perdiendo justo `Estado`.

```
bash -c '. hooks/lib.sh; printf "%s\n" "$ARNES_CLAVES"'
  -> QA|Seguridad|Sensible a seguridad|Hallazgos abiertos|Rigor|Estado      (n = 6, con `Estado`)
```

La derivación **ya no lee código**: `CLAVES36="$( . "$HOOKS_DIR/lib.sh"; printf '%s' "${ARNES_CLAVES:-}" )"`.
Estrecharse deja de ser posible porque la constante **es** el conjunto, no una huella suya. Su
fail-closed está escrito: con hooks anteriores a 1.34.0 la constante no existe, `CLAVES36` sale vacío,
la cosecha da 0 cabeceras y el caso **FALLA** nombrando el motivo (`REQ-016 CA-02`, rama `else`).
**Un proceso** (el subshell) contra los tres de antes (`sed`, `sort`, `paste`).

## 3. `QA-023-06` — CERRADO EN SU ALCANCE, y el mecanismo probado

**El mecanismo, ejecutado y no leído.** `run.sh:17` es `set -uo pipefail`; `run.sh:23` define
`PASS=0; FAIL=0` y **no** `SKIP`; las secciones se ejecutan con `source` dentro de `( )`
(`run.sh:1162-1176`). Reproducción mínima:

```
set -uo pipefail; PASS=0; FAIL=0
( set --; echo "  PASS caso-1"; SKIP=$((SKIP+1)); echo "  PASS caso-2" )
  ->   PASS caso-1
       línea 3: SKIP: unbound variable
       rc-subshell=1        # el caso-2 NUNCA se emite
( set --; SKIP=0; echo "  PASS caso-1"; SKIP=$((SKIP+1)); echo "  PASS caso-2" ) -> los dos, rc 0
```

Es decir: la abstención **mata la sección** en ese punto y se pierden todos sus casos posteriores.

**Conteo, verificado con `grep -F` (fijo, sin regex) sobre el directorio de secciones:**

```
grep -Frn 'SKIP=$((SKIP+1))' tests/escenarios/hooks/secciones/
  28-rotacion-seccion-1-la-historia.sh:149
  28-rotacion-seccion-4-la-carrera.sh:87
  -> 2 archivos, 2 ocurrencias      (eran 15; 13 retiradas, las 13 de REQ-023)
```

**Cero** quedan en las cinco secciones `39-*`. La abstención del `desarrollador` sobre las **dos**
restantes es **procedimentalmente correcta**: los dos archivos están en el `Archivos:` de `REQ-026`,
que tiene `QA: aprobado` dado sobre **otro árbol**, y editarlos aquí habría invalidado ese veredicto y
producido la colisión que `tools/arnes-paralelo.sh` existe para evitar. **Y materialmente incompleta**:
el residual queda vivo y va a `QA-023-11` con dueño y forzador, no enterrado en una nota.

**Por qué el corredor no lo esconde:** el cuadre por archivo (`run.sh:1272-1289`) emite
`ABORT: la sección <base> declara N casos y ejecutó M` y suma `PROBLEMAS`; el cuadre total
(`:1342-1345`) compara contra `CASOS_ESPERADOS=966`. La detonación es **ruidosa y con nombre**, no
silenciosa — eso baja la severidad, no la existencia.

## 4. `QA-023-08` — CERRADO, con DOBLE par discriminante medido por mí

El hallazgo era que las dos mitades del caso de `CA-04` no imprimían `ARNES_OCULTA`, de modo que la
única prueba contra la **sobre-denegación** era ciega a la guarda.

**Pass-after** (hooks reales, sección 39/5 sola, dentro del worktree):

```
PASS  CA-04 anti-vacuidad: 3084 líneas con campo, 1714 clave no ASCII, 1878 valor no ASCII,
      2346 clave decorada y 14 donde la GUARDA DISPARA
PASS  CA-04 campo a campo: IGUAL en las 3084 (clave, valor, decorada) — la GUARDA difiere en 14
PASS  CA-04 decisión a decisión: los 27 REQ deciden IGUAL, y la GUARDA no dispara en ninguno
      Resultado: 3 PASS, 0 FAIL
```

*(El corpus sale más pequeño que en la corrida completa —3 084 contra 5 322 líneas— porque el glob
sólo ve la sección copiada; el suelo de anti-vacuidad se cumple igual y las dos mitades comparan lo
mismo.)*

**Par 1 — sobre-denegación sintética.** En una copia de `hooks/` se retira el atajo «si ya **es** una
clave, aquí no hay nada» (`hooks/lib.sh:1840`, `arnes_en_vocab … && return 0`), con lo que la guarda
dispara también sobre toda clave legítima:

| evaluador | guarda dispara | campo a campo | decisión a decisión | `Resultado:` |
|---|---|---|---|---|
| hooks reales | 14 | PASS | PASS | **3 PASS, 0 FAIL** |
| hooks con sobre-denegación | **164** | PASS | **FAIL** (`27 de 27 REQ deciden distinto, el primero REQ-001.md`) | **2 PASS, 1 FAIL** |

**Par 2 — el cuarto suelo de anti-vacuidad, que es la mitad nueva.** En otra copia se neutraliza la
guarda (`_arnes_clave_oculta() { return 0; … }`):

```
FAIL  CA-04 anti-vacuidad: el corpus no llega a los suelos (campo=3084 clave-no-ascii=1714
      valor-no-ascii=1878 decorada=2346 guarda-dispara=0): la equivalencia sería cierta por vacío
```

Las **dos** mitades acreditativas fallan cuando deben. La mitad (1) sigue **sin** exigir identidad en
las columnas de la guarda y eso es correcto y declarado: exigirla sería insatisfacible —una guarda que
no cambia ninguna decisión no existe—; lo que la vuelve capaz de fallar es el suelo, y lo que caza la
sobre-denegación sobre un REQ real es la mitad (2).

## 5. `CA-09 (iii)` — lo que el CASO mide hoy, contra lo que el CRITERIO contrata desde el write-back

Línea literal del banco, idéntica en las tres corridas salvo los números:

```
SKIP  REQ-023 CA-09 (iii) arnes_norm_clave …  no se puede AFIRMAR el techo: dispersión 0.554×
      (de 1.898× a 2.452×) >= margen 0.112× contra el techo 2.200× (mediana 2.312×) · cociente de
      duplicación (1000→2000 bytes, k=200, forma «clave»: …; 3 tomas: t1=… t2=… t3=…)
```

**Cuadro de la divergencia, leído del criterio y del caso:**

| | `CA-09 (iii)` desde el write-back del 2026-09-09 | el caso `39-…-4-el-coste.sh` |
|---|---|---|
| magnitud | **relación emparejada** candidata/base, dentro de cada toma, en **una** tanda intercalada al nivel de `n` | **cociente absoluto** de la candidata, sola |
| techo | **1,00×** (operativo, hacia abajo) | **2,200×** — techo que el criterio **ya no contrata** |
| parámetros publicados | **cuatro**: par, `k`, forma **y línea base (el tag)** — «sin los cuatro, la relación **no acredita** (iii)» | **tres**: par, `k`, forma. **Ningún tag** |
| línea base | se **materializa desde un tag**; si no se puede, **SKIP** | **no se materializa ninguna** |
| cocientes absolutos | **control publicado y NO acreditativo** | son *el* número juzgado |

Por tanto **`CA-09 (iii)` no está ejercido por el banco**: sus dos SKIP abstienen sobre la magnitud que
el criterio retiró. Es `QA-023-10`. El criterio **sí** está bien formado (§6) y el REQ **no** afirma lo
contrario: nombra la brecha por escrito en la sección de `Hallazgos abiertos:`.

**El estadístico de dispersión, leído del código:** `39-…-4-el-coste.sh:218-262` calcula `lo`/`hi`
sobre las tomas y `disp` de ahí — es el **RANGO** (`hi − lo`), no una medida robusta. El rango es
**monótono no decreciente** en el número de tomas dentro de una corrida, así que **«más tomas» no puede
bajarlo**: de las cuatro vías conformes que el criterio declara para llegar a afirmar el techo, ésa es
**contraproducente**. Es `QA-023-12`. Aritmética del criterio verificada por mí sobre §11.4 del dev:
`0,950 − 0,435 = 0,515` y `1,211 − 0,829 = 0,382`; márgenes `1 − 0,856 = 0,144` y `1 − 0,906 = 0,094`.
Las cuatro cifras que el REQ publica **cuadran**.

## 6. La reconciliación de la CLASE de `QA-023-05`

- **Clase: `contrato`.** Confirmo la subida del analista y **corrijo mi propio veredicto de la vuelta
  2** (`docs/qa/1.34.0.md:2002`, `:2011`), que decía `instrumento` con el argumento «el producto no se
  degrada». Ese argumento contesta a la pregunta de `usuario/dinero`, **no** a la de `contrato`, que es
  «¿el REQ afirma algo falso sobre lo construido?». Y lo afirmaba: `CA-09 (iii)` contrataba un techo
  absoluto de 2,2 sobre una magnitud que la **línea base heredada ya pagaba** (medianas 2,492 y 2,446),
  de modo que ningún código de este REQ podía satisfacerlo y dos lecturas conformes daban **veredictos
  opuestos**. Además `requirements/README.md` § «Cómo se escribe un criterio que no se desmiente» no
  deja margen: un criterio mal formado «el QA lo reporta como hallazgo de clase **`contrato`** contra el
  REQ». Su **forma** es **número** (se anota aquí y en `docs/qa/1.34.0.md`, nunca dentro del paréntesis
  de la clase del campo).
- **Y CERRADO.** El write-back existe y es correcto: la magnitud pasa al **delta**, el número se declara
  **operativo** con dirección hacia abajo, el **cuarto parámetro** es obligatorio, la brecha del
  instrumento queda **nombrada** en el propio REQ, y la **prueba de falsación está escrita literalmente**
  —«si las medianas emparejadas hubieran salido **1,05**, el techo seguiría siendo **1,00** y lo que
  habría es un **hallazgo contra el código**, no una reformulación» (`requirements/REQ-023.md:474-478`,
  repetida en la factibilidad `:501-522`)—, que es lo que impide que el techo se haya elegido para que
  la medida quepa. Se retira del campo y su residual **no desaparece**: se abre como `QA-023-10` y
  `QA-023-12`, con clase y dueño.

## 7. Los 12 criterios, verificados uno por uno

**Batería de falsación propia, extremo a extremo contra `guard-completado.sh` real**, 20 formas, las
**dos** versiones en la misma sesión, proyecto de prueba con el manifiesto mínimo y el fixture en disco
en estado **no terminal** (el `Write` es la transición; sin eso el hook no juzga nada y todo sale
`allow` — error mío al primer intento, corregido y anotado para que nadie lo repita):

| # | forma | base `v1.33.0` | árbol | lo que exige el contrato |
|---|---|---|---|---|
| 00 | control: todo en verde, nada raro | allow | allow | CA-08 (iii) ✔ |
| 01 | BOM (`U+FEFF`) delante de `QA:` | allow | **deny** | CA-01 ✔ (volteado) |
| 02 | blanco **DUPLICADO**: `Sensible a  seguridad` | allow | **deny** | CA-03 rama 2 ✔ — `QA-023-01` **cerrado** |
| 03 | blanco **SUSTITUIDO** por `U+200B` | allow | **deny** | CA-03 rama 1 por P2 ✔ |
| 04 | blanco sustituido por **NBSP** (`U+00A0`) | allow | **deny** | CA-03 rama 1 por P2 ✔ |
| 05 | blanco interno = **TAB** | allow | **deny** | CA-01 ✔ |
| 06 | **tres** blancos en la clave | allow | **deny** | CA-03 rama 2 ✔ |
| 07 | **homóglifo** cirílico `Еstado` | allow | allow | **fuera de alcance declarado** ✔ (no es regresión) |
| 08 | capitalización `qa:`/`seguridad:` + `Rigor: ligero` | allow | allow | `QA-023-09`, decidido y enrutado a REQ-024 ✔ |
| 09 | `0xc3` suelto delante de `Rigor:` | allow | **deny** | CA-01 ✔ |
| 10 | documento **vacío** | allow | allow | CA-11: la ausencia se perdona ✔ |
| 11 | sólo un `\n` | allow | allow | CA-11 ✔ |
| 12 | clave **decorada** legítima `**QA:**` | allow | allow | CA-04 ✔ (no sobre-deniega) |
| 13 | `Módulo:` y `Versión destino:` | allow | allow | CA-04 ✔ (las dos claves no ASCII) |
| 14 | valor inválido `QA: ok` | deny | deny | invariante previa intacta ✔ |
| 15 | hallazgo **sin clase** | deny | deny | invariante previa intacta ✔ |
| 16 | **emoji** dentro de la clave: `Q🙂A:` | allow | **deny** | CA-03 rama 1 por P1 ✔ — falsación contra una lista de prohibidos |
| 17 | `U+2028` delante de `Rigor:` | allow | **deny** | CA-03 rama 1 ✔ |
| 18 | **CR interior** en la línea de `QA:` | deny | deny | REQ-016 CA-12: no se movió ✔ |
| 19 | `QaA:` (E2 no-blanco) | allow | allow | CA-03 rama 3: **no exige veredicto** ✔ |

**9 formas volteadas `allow` → `deny`; 11 idénticas en las dos versiones.** Ninguna sobre-denegación
sobre una cabecera legítima. El emoji y el `U+2028` son la prueba de que la guarda **no** es una lista
de prohibidos: no están en ninguna.

**`CA-05`, invariancia al locale, medida sobre las 20 formas y comparando veredicto Y MOTIVO:**
`LC_ALL=C` y `LC_ALL=C.utf8` producen salidas **byte a byte idénticas** (`diff` vacío). Límite
declarado: este host sólo tiene `C`, `C.utf8` y `POSIX`; **ningún locale con colación distinta**, y
**nada** en Windows/MSYS.

**`CA-07`, el informe, con fail-before contra el TAG.** Proyecto con un BOM delante de `QA:` en
`REQ-901`:

```
tools/arnes-lectura.sh (v1.33.0)  -> rc 0 · «Ningún valor anómalo: la máquina lee los 1 REQ como están escritos.»
tools/arnes-lectura.sh (árbol)    -> rc 1 · REQ-901  QA:
                                       escrito:  «\xef\xbb\xbfQA:»
                                       se lee:   <cabecera no medible>
                                       …«Una persona lee ahí `QA:` y la máquina NO lo lee como ese
                                       campo, así que la cabecera no se puede medir y la puerta de
                                       cierre DENIEGA. Reescribe esa línea dejando la clave limpia.»
```

Nombra el REQ, la línea, el byte en **hexadecimal** y la **consecuencia**, y sale ≠ 0. Es exactamente
el defecto H-03 de REQ-016 volteado.

**`CA-08`, fail-before completo contra el tag `v1.33.0`** (sección 39/1 sola, 21 casos):
**11 PASS, 10 FAIL** contra la base — fallan las **tres** filas «(era ALLOW)» y el motivo en
hexadecimal, la derivación del dominio de `CA-02` (que sale **vacía**, 0 campos), la boca `Edit` con el
BOM en disco, la boca del fragmento y `CA-05`; y **pasan en las dos versiones** los tres controles
(negativo ×2, positivo) y los cuatro de `CA-11`. Contra el árbol real la sección va **21/21 en verde**
en las tres corridas. Reserva declarada y ajena: `SEC-048` — acredito el **delta**, no la inmutabilidad
del árbol de referencia.

**`CA-12 (iii)`, contado contra «Fuera de alcance» y no leído del criterio.** Los nueve ítems del
apartado, clasificados por **propiedad**:

| ítem de «Fuera de alcance» | ¿es la clase de CA-01 —una línea de cabecera que la máquina no puede medir? | ¿en las tres de `CA-12 (iii)`? |
|---|---|---|
| semántica de la AUSENCIA (REQ-024) | no: es la **dirección** de la ausencia, no la medibilidad | no |
| **homóglifo** | **sí** | **sí (1)** |
| segundo lector / cola (`SEC-051`) | sólo su parte (C); tiene `SEC` propio y contrato en REQ-024 CA-08 | no — lectura conservada de la vuelta 2 |
| **NUL y UTF-16** | **sí** | **sí (2)** |
| **invisible delante del `## `** (`QA-023-03`) | **sí** | **sí (3)** |
| bloque derivado de `ESTADO.md` (residual `SEC-024`) | no: publica lo que la puerta no mide; no es el lector de la puerta | no |
| **superlinealidad heredada** (bullet NUEVO) | **no**: es **coste** de un camino que mide perfectamente | **no** — exclusión por propiedad **verificada** |
| ruleset y permisos (`SEC-048`) | no | no |
| ninguna llave nueva en el manifiesto | no | no |

**3 = 3.** La afirmación del analista es correcta: el bullet nuevo **no** entra en el recuento, y el
motivo es de propiedad, no de cupo. Ficha del bullet nuevo comprobada: dueños (`desarrollador` +
`analista-requerimientos`), clase (`instrumento`), forzador escrito, ventana **1.35.0 propuesta, «la
decide el propietario»**. El propio bullet dice «**Quien verifica** puede abrirla con su ID»: se abre
como `QA-023-13`.

**`CA-12 (i)/(ii)`:** el caso compara las 7 formas del corpus de colas contra `v1.33.0` y pasa en las
tres corridas; y por construcción `arnes_cola_pendientes` (`hooks/lib.sh:1152-1181`) no llama a
`arnes_sin_cita`, `arnes_campo_linea` ni `arnes_norm_clave`.

**`CA-10`: NO IMPLEMENTADO, y su precondición ha VENCIDO.** Medido leyendo los tres archivos:

```
grep -n "no se puede medir|invisible|medibilidad" AGENTS.md templates/AGENTS.md.tpl
  AGENTS.md:349          -> la fila sigue definiendo la invariante por el RETORNO DE CARRO
  templates/AGENTS.md.tpl:314 -> idéntica
grep -n "sin validación ni auditoría|no cerrarían hoy|REQ-023|SEC-047" skills/arnes-upgrade/SKILL.md
  -> (vacío)
git status --short AGENTS.md templates/AGENTS.md.tpl skills/arnes-upgrade/SKILL.md  -> (vacío)
```

Ninguno de los tres se ha tocado. `CA-10` exige que la invariante esté escrita **por propiedad** con el
CR como **instancia**, y que el apartado de migración diga «sin eufemismo» que las versiones afectadas
**pudieron cerrar un REQ `critico` sin validación ni auditoría»**. Nada de eso existe. La precondición
que el Historial declaró (`:1519`: «`AGENTS.md` y `templates/AGENTS.md.tpl` los está escribiendo **otra
comisión viva**, entregados en `6dd3f8a`») **ya no está**: `6dd3f8a` está fusionado y `REQ-027` cerró en
`95764db`. Y los tres archivos **no** están en `codigo_app.globs`
(`hooks/*`, `tools/*`, `.github/*`, `.arnes/config.json`, `.claude-plugin/*`), así que ningún guardián
impide escribirlos. Es `QA-023-15`, y es lo único que bloquea.

## 8. Veredicto por criterio (vuelta 3)

| Criterio | Veredicto | Base |
|---|---|---|
| `CA-01` | **PASA** | §7, formas 01·05·09·16·17 volteadas; motivo imprimible con el byte en hex |
| `CA-02` | **PASA** | Dominio derivado en cada corrida; contra la base sale **vacío** y el caso **FALLA** (§7, CA-08) |
| `CA-03` | **PASA** | Ramas 1 y 2 → `deny` medido (formas 02·03·04·06·16·17); rama 3 publicada y no juzgada (19); sorteo con semilla y reparto por rama publicados |
| `CA-04` | **PASA, y ahora el caso DISCRIMINA** | 0 divergencias; formas 12·13 no sobre-deniegan; y las **dos** mitades acreditativas fallan cuando deben (§4) — `QA-023-08` cerrado |
| `CA-05` | **PASA** (con límite de entorno) | Veredicto **y motivo** idénticos en `C` y `C.utf8` sobre 20 formas; falla contra la base. Sin locale de colación distinta ni Windows/MSYS |
| `CA-06` | **PASA** | `ARNES_CLAVES` es una sola constante de 6 claves, con `Estado` dentro; la publicación no viene de la llamada cruda de `:1880`; `Archivos:` fuera con su fail-closed |
| `CA-07` | **PASA** | §7: rc 1 con REQ, línea, hex y consecuencia; la base dice «Ningún valor anómalo» con rc 0 |
| `CA-08` | **PASA en su letra**, con la reserva de `SEC-048` | 21/21 en el árbol; 10 FAIL contra el tag `v1.33.0` con los controles pasando en las dos |
| `CA-09 (i)` | **PASA** | El caso publica **5 procesos contra 5** de la línea base: 0 añadidos, en la corrida |
| `CA-09 (ii)` | **PASA por MEDICIÓN** | **0,920×** en mi corrida A (techo 1,25×), sujetos intercalados, base `v1.33.0` |
| `CA-09 (iii)` | **NO ACREDITADO · y su caso ROJO en el CI** | Abstiene en 5 de 5 corridas locales (ruido del host) **y** mide la magnitud **retirada**; en el CI **resuelve y FALLA** contra el techo retirado (2,365× > 2,200×, dispersión 0,023×). No es regresión —la base paga 2,446 en ese sujeto— pero pone roja la puerta requerida: `QA-023-10` |
| `CA-10` | **NO CUMPLE** | Los tres archivos sin tocar y la precondición vencida: `QA-023-15` |
| `CA-11` | **PASA** | Formas 10·11 (ausencia perdonada) y los cuatro casos comentados, idénticos en las dos versiones |
| `CA-12 (i)/(ii)` | **PASA** | Corpus de 7 formas contra `v1.33.0`; y por construcción (`hooks/lib.sh:1152-1181`) |
| `CA-12 (iii)` | **PASA** | 3 = 3, contado por propiedad contra los nueve ítems del apartado (§7) |

**10 pasan · 1 no acreditado · 1 no cumple.**

## 9. Lo que NO acredito, dicho expresamente

1. **`CA-09 (iii)`**: no lo acredito de ninguna forma. Ni el techo, ni la relación emparejada, ni que
   sea alcanzable con el instrumento de hoy. Sólo acredito que el criterio está **bien formado** y que
   el caso mide **otra cosa** — y que en el CI ese caso **falla**. **No** acredito que re-apuntarlo
   ponga el CI en verde: no lo he medido, porque no escribo pruebas.
1.bis **El cierre de `QA-023-05` es CONDICIONAL**: vale si el write-back se comitea. Hoy vive sólo en
   el árbol de trabajo (0 apariciones de «relación emparejada» en `6ad9752`, 8 en local), y contra el
   árbol comiteado ese hallazgo **sigue abierto** como `contrato`.
2. **`CA-10`**: no acredito nada — no existe.
3. **El banco en verde (`AGENTS.md` §7)**: **NO lo certifico, y hoy está ROJO.** 4 de mis 5 corridas
   locales salen rojas por reloj, y el **CI** —la puerta requerida y **estricta** de `main`— sale en
   **FAIL** por `CA-09 (iii)`, que es de este REQ. Un verde de 1 sobre 5 en un host ruidoso no era una
   puerta; era la abstención. Ningún FAIL es de REQ-023, pero la puerta requerida de `main` se decide **en CI**, en otro
   host, y eso no lo he medido. El «banco entero en verde» sigue siendo precondición del gate humano de
   fusión y **no** la doy por cumplida.
4. **`CA-08` en la mitad que `SEC-048` nombra**: acredito el delta contra el tag, **no** la
   inmutabilidad del árbol de referencia.
5. **`CA-05` sólo en `C` y `C.utf8`**, los únicos de este host. Nada en Windows/MSYS, que es de donde
   sale el BOM y donde viven los proyectos consumidores.
6. **`CA-09 (i)`**: no conté procesos con sonda propia; lo doy por el caso del banco y por lectura.
7. **`CA-06` en su eje de los cuatro lectores**: no comparé `hooks/campos-req.awk` contra `hooks/lib.sh`
   por mi cuenta; lo mide el caso `39-…-3` y lo doy por su cuenta.
8. **El homóglifo, el NUL/UTF-16 y el invisible delante del `## `** no se cerraron ni se pidió
   cerrarlos: mi verde **no** cierra la clase.
9. **Rendimiento con usuarios concurrentes: no aplica** — el arnés no los tiene. Los umbrales de este
   proyecto son de reloj y de procesos, viven en `CA-09` y están arriba. No hay prueba de carga que
   reportar y no la finjo.
10. **No firmo `Seguridad:`** (va después de mí, `AGENTS.md` §6), **no toco `Estado:`**, **no hice
    write-back**, **no fijé ninguna ventana**, y **no escribí en `hooks/`, `tools/`, `.github/`,
    `.arnes/`, `.claude-plugin/` ni `tests/`**.

## 10. Limitaciones materiales de esta medición

- **Un host, un sistema operativo, con carga ajena variable y relojes altos hoy** (los `DEV v3` de
  4 466 y 4 357 ms, y el suelo de 150 ms fallado por 147 616 µs). Los pares discriminantes son de
  **veredicto**, no de reloj, así que no los afecta; las cifras de `CA-09 (ii)` sí.
- **Los pares discriminantes de §4 corren la sección 39/5 SOLA**, así que su corpus por glob es menor
  que en la corrida completa. Se declara porque los números no son comparables con los de la corrida
  entera.
- **Las tres corridas del banco se tomaron con esta sesión activa** (A en paralelo con lecturas; B y C
  en serie). No es un host en reposo, y por eso los FAIL de reloj se reportan como **flaky**, no como
  regresión.
- **`CA-09 (iii)` no se remidió por mí**: no tenía sentido medir con más precisión un número que el
  criterio ya no contrata. La medición de la relación emparejada es del `desarrollador`
  (`docs/qa/1.34.0-req023-vuelta3-metodo.md` §11) y yo sólo **verifiqué su aritmética**.
