# QA — candidata 1.31.0 (ciclo 2 del autoalojamiento)

**Rama:** `cand/1.31.0-mecanismos` · **Commit validado:** `40312c6`
**Guardián de la sesión:** instalación estable **1.30.3** (`6c1b58a`), que es también la
línea base de no-regresión.
**Vuelta:** 1 de 3 (tope por REQ de `AGENTS.md` §6).
**Plataforma:** Linux (WSL2). Presupuesto respetado: sondas con `timeout 30`, nada por
encima de 2× los umbrales declarados.

---

## 1. Quality gates

| Puerta | Resultado |
|---|---|
| `for f in hooks/*.sh tools/*.sh; do bash -n "$f"; done` | verde |
| `jq -e . hooks/hooks.json` | verde |
| `jq -e . .claude-plugin/plugin.json && .claude-plugin/marketplace.json` | verde (ambos `1.31.0`) |
| `jq -e . .arnes/config.json` | verde |
| `tools/arnes-lectura.sh` sobre este repositorio | 11 REQ, 0 anomalías |

## 2. Banco de escenarios

| Corrida | PASS | FAIL | SKIP | Cuadre |
|---|---|---|---|---|
| Candidata, banco tal como lo dejó el desarrollador | 561 | 0 | 1 | 562 = `CASOS_ESPERADOS` ✔ |
| Línea base 1.30.3 (`ARNES_HOOKS_DIR`), mismo banco | 442 | 119 | 1 | 562 ✔ |
| Candidata, banco **con los 7 casos que añade QA** | 563 | **5** | 1 | 569 = `CASOS_ESPERADOS` ✔ |

Los 5 FAIL de la tercera corrida son **exactamente** los hallazgos QA-101 (3 casos),
QA-102 (1) y QA-103 (1). No hay ningún otro rojo. Los 2 controles positivos que
acompañan a QA-101 (`git clean --dry-run`, `git stash list`) pasan: la sonda dispara.

El SKIP es el mismo de siempre y explica su condición en su línea (`ruta estilo Windows
con backslashes — sin cygpath: caso sólo de Windows`).

**Determinismo:** dos corridas completas de la candidata dieron el mismo resultado caso a
caso. No se observó ningún caso flaky.

## 3. Veredicto por REQ

| REQ | Veredicto | Hallazgos abiertos |
|---|---|---|
| REQ-002 veredicto fechado y caduco | **aprobado** | QA-106 (instrumento) |
| REQ-003 vocabulario único y aviso | **aprobado** | — |
| REQ-004 rotación de una sección | **con-hallazgos** | QA-102 (contrato) |
| REQ-005 `guard-git` | **con-hallazgos** | QA-101 (contrato) |
| REQ-006 celdas recortadas | **aprobado** | — |
| REQ-009 cola de aprobaciones | **aprobado** | — |
| REQ-010 acentos y forma Unicode | **aprobado** | QA-108 (instrumento, dueño analista) |
| REQ-007 (parcial, sigue `en-progreso`) | no se rinde en esta ventana | QA-103, QA-104, QA-105, QA-107 |

---

## 4. Hallazgos

### QA-101 — `git clean --force` y `git stash save` atraviesan la puerta nueva
**REQ-005 · clase `contrato` · severidad alta · dueño `desarrollador`**

**Reproducción**

```
$ printf 'git clean --force' | jq -Rs '{hook_event_name:"PreToolUse",tool_name:"Bash",
    cwd:env.CLAUDE_PROJECT_DIR,tool_input:{command:.}}' | hooks/guard-git.sh
(salida vacía)          -> allow
```

Igual con `git clean --force -d`, `git clean -d --force` y `git stash save "wip"`.
Control positivo en la misma corrida: `git clean -f`, `git clean -fd`, `git clean -xdf`,
`git stash push -u` → **DENY**. Control negativo: `git clean --dry-run`, `git stash list`
→ **allow**, como debe ser.

**Esperado vs observado**
`git clean --force` borra exactamente lo mismo que `git clean -f`, y `git stash save` es
el alias histórico —todavía funcional— de `git stash push`, que sí se deniega. Esperado:
DENY en los tres. Observado: ALLOW en los tres.

**Causa**
`arnes_casa_token` (`hooks/guard-git.sh`) compara los flags **cortos por letras** y salta
explícitamente todo argumento que empiece por `--`:

```
case "$a" in
  --*|-) continue ;;
```

La lista por defecto (`hooks/lib.sh:71`) declara `"clean -f"`, una sola ortografía. La
forma larga del mismo flag no está en ninguna regla, y el alias `save` tampoco.

**Por qué es `contrato`**
La Historia de REQ-005 afirma que «ningún agente —ni siquiera la sesión coordinadora—
pueda ejecutar por `Bash` un subcomando de git que destruye el árbol de trabajo». Eso es
falso sobre lo construido para tres formas ordinarias, no exóticas. Se cierra cuando el
código las cubra **o** cuando el REQ declare por escrito el límite de la regla (que sólo
alcanza las ortografías declaradas) y añada el criterio correspondiente.

**Casos de banco añadidos:** `QA-101 'git clean --force' -> deny`, `QA-101 'git clean
--force -d' -> deny`, `QA-101 'git stash save "wip"' -> deny`, más dos controles.

---

### QA-102 — la sección declarada que no existe no rota nada, pero tampoco lo dice
**REQ-004 CA-09 · clase `contrato` · severidad media · dueño `desarrollador`**

**Reproducción**

Proyecto con `rotacion.activo: true`, `umbral_bytes: 1000` y el artefacto
`{"glob":"requirements/REQ-*.md","seccion":"## Historial","conservar_entradas":20}`,
mientras la sección real del documento es `## Historial de cambios`; REQ de ~14 KB con
60 entradas. Se emite una parada (`hooks/stop.sh` con `{"hook_event_name":"Stop"}`).

**Esperado vs observado**

| | Esperado (CA-09) | Observado |
|---|---|---|
| El documento no cambia | sí | **sí** ✔ |
| `arnes_warn` que nombre el archivo y la sección declarada | sí | **stderr vacío** ✘ |
| El bloque derivado refleja «sección declarada no encontrada» | sí | **nada** ✘ |

**Causa**
`hooks/rotar-artefactos.sh`, tras partir el documento:

```
[ "$fase" -ge 1 ] || return 0            # el documento no tiene esa seccion: no se toca
```

Un `return 0` mudo, antes de cualquier aviso. `hooks/estado-derivado.sh` no tiene ninguna
referencia a la rotación, así que la segunda mitad del criterio tampoco puede cumplirse.

**Por qué importa**
Es el caso que la propia DoR midió y el criterio nombra: «un artefacto declarado que no
existe es un error de mapeo que hay que ver, no un acierto silencioso». Un proyecto que
escriba mal el nombre de la sección cree que rota y no rota, sin una sola señal.

**Caso de banco añadido:** `QA-102 CA-09: la seccion declarada no encontrada emite
arnes_warn`.

---

### QA-103 — el motivo del techo imprime el nombre de la variable, no su valor
**REQ-007 (SEC-006 parte b) · clase `instrumento` · severidad baja · dueño `desarrollador`**

**Reproducción**

```
$ python3 -c "print('echo \"'+'a'*70000+'\" > src/a.ts')" \
  | jq -Rs '{hook_event_name:"PreToolUse",tool_name:"Bash",cwd:env.CLAUDE_PROJECT_DIR,
             tool_input:{command:.}}' | hooks/guard-codigo.sh | jq -r '...Reason'
```

**Observado**, al final del motivo:

```
... hasta un maximo de $ARNES_BASH_MAX_MANIFIESTO bytes: por encima el analisis dejaria ...
```

**Esperado:** `hasta un maximo de 131072 bytes`.

**Causa:** el `$` va escapado (`\$ARNES_BASH_MAX_MANIFIESTO`) en `hooks/guard-codigo.sh:88`
y en `hooks/guard-completado.sh:54`. La decisión (DENY) es correcta; lo que falla es el
diagnóstico: el motivo ofrece una salida —subir el techo— sin decir hasta dónde.

**Caso de banco añadido:** `QA-103 el motivo del techo imprime el maximo en BYTES...`.

---

### QA-104 — el camino común de `Bash` paga un proceso más que en 1.30.3
**REQ-007 (SEC-005) · clase `contrato` · severidad media · dueño `desarrollador` + analista**

**Reproducción** — con un `jq` instrumentado primero en el `PATH` que cuenta invocaciones
y delega en el real:

| Entrada | Candidata | v1.30.3 |
|---|---|---|
| `Bash: ls -la` | **2** procesos `jq` | 1 |
| `Bash: npm run build` | **2** | 1 |
| `Bash: git status` | **2** | 1 |
| `Edit` fuera de los globs | 3 | 3 |

Reloj sobre 200 invocaciones de `guard.sh` con `ls -la`: candidata **26,0 ms/inv**,
v1.30.3 **16,1 ms/inv** → **+9,9 ms** en Linux/WSL2.

**Causa** — en `hooks/guard-codigo.sh`, `arnes_parse_manifest` se movió *delante* del
corte temprano:

```
+ arnes_parse_manifest
+ arnes_deny_manifiesto_roto ...
  [ -n "$objetivo" ] || return 0   # no es código de app -> permitir
- arnes_parse_manifest
```

Es **consecuencia directa y necesaria** de SEC-005 (CA-51): sin leer el manifiesto no se
puede distinguir «no es código protegido» de «no sé qué es código protegido». No es un
descuido, es un coste.

**Por qué es `contrato` y no sólo un dato**
Los criterios de esta ventana afirman repetidamente lo contrario: REQ-003 CA-22 («no añade
ningún proceso a esa ruta — 0 forks añadidos respecto a v1.30.3, medido con un `jq`
instrumentado en el `PATH`»), REQ-007 CA-44 y CA-48 («coste: 0 procesos nuevos/añadidos»)
y CA-50(d). Medido en conjunto, la candidata añade **uno** por cada comando `Bash`, que es
la llamada más frecuente que existe. En Linux cuesta 10 ms; en la plataforma del
desarrollador —donde `AGENTS.md` §2 cifra cada fork en **1,2–6 s**— ese mismo proceso es
el orden de magnitud que el arnés lleva tres versiones peleando.

No pido revertir SEC-005: pido que el coste **se declare** en un criterio (write-back del
analista) y, si se puede, que se pague sólo cuando haga falta. Mientras el REQ diga «0
procesos añadidos» y la medición diga «+1», hay deriva.

*(No se añade caso de banco: contar procesos requiere instrumentar el `PATH` y el banco no
tiene hoy ese armazón fuera de la sección de rendimiento.)*

---

### QA-105 — con el manifiesto roto, la salida que el propio motivo recomienda está denegada
**REQ-007 (SEC-005) · clase `instrumento` · severidad media · dueño `desarrollador`**

**Reproducción** — proyecto con `.arnes/config.json` = `{"agentes":{,}`:

| Llamada | Resultado |
|---|---|
| `Write` a `docs/notas.md` (ruta no protegida), coordinadora | DENY |
| `Write` a `.arnes/config.json` con `agent_type: desarrollador` | **DENY** |
| `Bash: printf "{}" > .arnes/config.json`, `desarrollador` | **DENY** |
| `Bash: ls -la` | allow |
| `Bash: node fix.mjs` | allow (vía de escape ya declarada en `AGENTS.md` §13) |

El motivo del DENY dice: *«Salidas: corrige el JSON (pruebalo con `jq -e .
.arnes/config.json`) o borra el archivo…»*. Ninguna de las dos se puede hacer desde la
sesión: las dos son escrituras y las dos están denegadas, para **todos** los agentes.

**Esperado vs observado**
El fail-closed es correcto y no lo discuto. Lo que falla es que el remedio que el propio
mensaje ofrece es inalcanzable por la vía que el mensaje sugiere, y el REQ no lo dice. Un
proyecto con una coma de más queda con todo `Edit`/`Write` bloqueado hasta que una persona
edite el archivo fuera de la sesión.

**Salidas posibles** (decisión del desarrollador, no mía): permitir la escritura de
`.arnes/config.json` al agente de código aun con el manifiesto roto —es el único archivo
cuya reparación devuelve la capacidad de medir—, o reescribir el motivo para que nombre la
vía real (arreglarlo fuera de la sesión / desde el editor).

---

### QA-106 — `veredictos.exigir_fecha` con un valor no booleano apaga la puerta en silencio
**REQ-002 · clase `instrumento` · severidad baja · dueño `desarrollador`**

**Reproducción** — REQ crítico con `QA: aprobado` y `Seguridad: aprobado` **sin fecha**,
listo para cerrar:

| `veredictos.exigir_fecha` | Veredicto | Aviso |
|---|---|---|
| `true` | DENY (correcto) | — |
| `false` / clave ausente | allow (correcto) | — |
| `"true"` (cadena) | **allow** | **ninguno** |
| `1` | **allow** | ninguno |
| `[]`, `null` | allow | ninguno |

Un proyecto que escriba `"exigir_fecha": "true"` cree que la exigencia está activa y no lo
está, sin una sola señal. El arnés ya resuelve bien este mismo caso en
`limites.bash_max_analisis`, que **avisa** cuando el valor no es un número JSON
(«una cadena como "999999" no lo es»). La asimetría es lo que sorprende.

Falla en abierto respecto de la intención del proyecto, pero **no** respecto de 1.30.3:
sin el opt-in la conducta es la de siempre, así que no reabre nada. Por eso `instrumento`.

---

### QA-107 — un número JSON en forma exponencial o decimal ni recorta ni avisa
**REQ-007 CA-54 · clase `instrumento` · severidad baja · dueño `desarrollador`**

| `limites.bash_max_analisis` | Techo efectivo | Aviso |
|---|---|---|
| `131073`, `4294967296`, `99999999999999999999` | 131072 (máximo) | sí, correcto |
| `"999999"`, `"131072"`, `true`, `[]` | defecto | sí, «no es un numero» |
| **`1e9`** (número JSON válido = 1 000 000 000) | defecto | **ninguno** |
| **`1.5`** | defecto | **ninguno** |

Comando de 70 KB → DENY en los cuatro últimos: **falla cerrado**, no hay riesgo. Lo que
falta es el aviso: `1e9` no es ni «supera el máximo» (no recorta a 131072) ni «no es un
número» (jq dice que sí lo es), y cae por el hueco entre las dos ramas.

---

### QA-108 — REQ-010 CA-11 afirma algo falso sobre la línea base
**REQ-010 · clase `instrumento` · severidad baja · dueño `analista-requerimientos`**

CA-11 dice que `Estado: en-revisión (2026-08-25, tras la ronda 3)` no se reporta
«exactamente igual que en **v1.30.3 publicada**: la normalización … del paréntesis ya
existía y este REQ no puede perderla».

**Medido** sobre el mismo fixture:

```
candidata 1.31.0 : no lo reporta            (correcto)
v1.30.3          : SÍ lo reporta -> «se lee: <en-revisión(2026-08-25,traslaronda3)>»
```

Lo construido hace lo correcto y el cambio **está** declarado — pero en **REQ-003 CA-16**,
no en REQ-010. Sólo la referencia a la línea base de CA-11 es falsa. Un control cuyo
«antes» está mal descrito no acredita lo que dice acreditar; de ahí `instrumento`.

---

## 5. Criterios verificados, uno por uno

### REQ-002 — **aprobado**
Bloque A (CA-01…CA-09): los nueve pasan. Apagado por defecto confirmado con las claves
ausentes y con `exigir_fecha:false` explícito. `exigir_fecha:true`: DENY sin paréntesis,
DENY con paréntesis sin fecha, ALLOW con la fecha en cualquier posición del paréntesis,
DENY para `01/09/2026`, `2026-9-1`, `20260901`, `2026-13-05` y además (probados de más)
`2026-00-10` y `2026-02-32`; en `critico` la fecha se exige a las dos firmas; `ligero` no
la pide; `QA: pendiente` deniega por el veredicto y no por la fecha.
Bloque B (CA-10…CA-15), probado con un repositorio git real y fechas de commit fijadas:
ALLOW con veredicto posterior, DENY nombrando ambas fechas con veredicto anterior, ALLOW
en el empate del mismo día, DENY nombrando el archivo con cambios sin comitear dentro de
los globs, ALLOW con el cambio fuera de los globs, DENY sin repositorio git.
Único hallazgo: QA-106 (instrumento).

### REQ-003 — **aprobado**
CA-01 y CA-06: `con-hallazgos` deja de ser anomalía en el informe y sigue sin cerrar un
REQ crítico; `vetado`, `preventiva`, `pendiente` y `n/a` siguen denegando. CA-03 y CA-08:
escribir `Seguridad: con-hallazgos` se permite. CA-04: verificado por lectura del fuente —
`QA_OK="|$ARNES_VOCAB_QA|"`, sin ninguna lista literal. CA-05, **prueba por mutación**: al
añadir `aprobadisimo` **sólo** a `ARNES_VOCAB_QA` en una copia del árbol, el aviso de la
puerta desaparece y el informe deja de reportarlo; al retirar la mutación, los dos vuelven.
Bloque B (CA-09…CA-15): aviso presente y con el campo, el valor normalizado y la lista de
válidos; sin aviso con un valor del vocabulario; sin aviso cuando el valor ya estaba en
disco y la edición toca otra sección; sin aviso dentro de `## Historial de cambios`; DENY
cuando la misma llamada cierra el REQ (el aviso no compite con la denegación); sin aviso
fuera de `requirements/`. CA-16…CA-18 verificados en §6 de este informe.

### REQ-004 — **con-hallazgos**
Pasan: CA-01 (apagada por defecto: dos paradas, origen byte a byte y sin crear el
directorio de archivo), CA-02 (20 entradas en el origen, 40 en `requirements/historial/`,
un solo puntero), CA-03 (todo salvo la sección, `diff` limpio), CA-04 (los seis campos de
cabecera intactos), CA-05 (dos paradas más: origen y destino con el mismo md5, un solo
puntero), CA-06 (200 entradas por debajo del umbral: nada se toca), CA-07/CA-08
(continuaciones indentadas: 20 en el origen y 40 en el destino, ninguna huérfana), CA-11
(CRLF, tres paradas: 40/40/40 en el destino y 20 en el origen; la fuga de 1.27.0 no
vuelve), CA-12 (destino sin permiso de escritura: origen con sus 60 entradas y **cero**
`.arnes.tmp` en el árbol), CA-13 (`archivo_dir: "../fuera"`: nada fuera, origen intacto),
CA-14 (enlace simbólico que sale del proyecto: nada escrito fuera), CA-16 (cinco
manifiestos malformados —sin `glob`, `seccion` vacía, `conservar_entradas` no numérico,
`artefactos` cadena, `conservar_entradas` negativo—: nada se toca, `stop.sh` sale 0 y el
bloque derivado se escribe igual). También la comparación exacta en las dos direcciones:
declarar `## Historial de cambios` sobre un documento cuya sección es `## Historial`
tampoco rota.
**Falla:** CA-09, segunda mitad → QA-102.

### REQ-005 — **con-hallazgos**
Pasan los 16 criterios tal como están escritos, más 34 formas adicionales que probé de mi
cuenta: los ALLOW (`git clean -n|-nd|-dn|--dry-run`, `stash list|show|show -p stash@{0}`,
`reset|reset --soft HEAD~1|reset archivo.ts`, `restore --staged x.ts|--staged .`,
`restore --source=HEAD~1 x.ts`, `checkout rama|-b rama|-- README.md`, `push`,
`push origin main`, `status`, `log`, `diff`, `add -A`, comillas simples y dobles, heredoc
citado, `github`, `gitk`, `mygit`, `legit`, `ls -la`) y los DENY (`clean -f|-fd|-df|-xdf|
-ffdx`, `reset --hard [HEAD~1]`, `checkout .`, `checkout -- .`, `checkout HEAD -- .`,
`restore .`, `restore --worktree .`, `stash`, `stash -u`, `stash --all`, `stash push -u`,
`cd sub && …`, `git -C proj …`, `(…)`, `x=$(…)`, `| tee log`, heredoc sin citar,
`git -c k=v`, `git --no-pager`, `GIT_DIR=x git`, `sudo git`, y el mismo comando desde el
`desarrollador`). **Ni un solo falso positivo** en 33 controles ALLOW: la puerta nueva no
estorba.
**Falla:** las formas largas y el alias → QA-101.

### REQ-006 — **aprobado**
Fixture de 6 REQ. CA-01: celda de 41 caracteres terminada en `…` para un `QA:` de 1 296
caracteres sin espacios. CA-02: la cadena completa no aparece en `docs/ESTADO.md`
(`grep -F` = 0). CA-03: `aprobado`, `con-hallazgos` y `SEC-121 (instrumento)` salen
intactos y sin `…`. CA-04: el valor de 40 sale intacto (len 40) y el de 41 recortado
(len 41 con `…`). CA-05: **todas** las filas, cabecera y separador incluidos, tienen 7
barras. CA-06: `SEC-1|SEC-2 (contrato)` sale como `sec-1¦sec-2(contrato)` conservando 6
celdas. CA-07: `REQ`, `Estado` y `Rigor` sin recortar. CA-08: valor con `ñ ó ü — …`
recortado por caracteres, archivo UTF-8 válido (`iconv` limpio), ningún byte partido.
CA-09: dos paradas seguidas → bloque **idéntico byte a byte**, sin segundo `…`. CA-10:
`Hallazgos abiertos:` vacío sigue mostrando `—`. CA-11/CA-12: la puerta lee el campo
entero (un `Hallazgos abiertos:` largo con clase `contrato` deniega el cierre).

### REQ-009 — **aprobado**
Los 13 escenarios probados dan **el mismo número en los tres lectores** (puerta, bloque
derivado e informe): CA-01 (una entrada con sus cuatro viñetas → 1 en los tres; en 1.30.3
el bloque derivado decía 4), CA-05 (0/1/3), CA-06 (ejemplo en cita antes de la sección),
CA-07 (ejemplo en comentario HTML), CA-08, CA-09 (cinco bajo `## Resueltas`), CA-10 (dos
bajo `## Notas`), CA-11 (`####` no cuenta), CA-12 (CRLF), CA-13 (`## Pendientes (2
abiertas)` → 1; `##Pendientes` sin espacio → 0), CA-14 (sin sección y **sin archivo** → 0
en los tres, sin error y sin bloquear). CA-02 verificado por lectura: `arnes_cola_pendientes`
aparece una vez en `lib.sh` y se invoca desde los otros tres, que no tienen ningún patrón
propio.

### REQ-010 — **aprobado**
CA-01 (`en-revision` sin tilde: la candidata no lo reporta y 1.30.3 sí), CA-03 (NFD
`en-revisio` + U+0301: mismo valor y mismo veredicto que NFC en los tres lectores),
CA-04 (`estándar` se lee `estandar`; `crítico` sigue leyéndose `critico`), CA-08 (los tres
controles negativos —`en revision`, `enrevision`, `revisión`— siguen reportándose),
CA-09 (`en-revisión-parcial` sigue siendo anomalía: comparación delimitada, no por
prefijo), CA-11 (`En-Revisión`, `**pendiente**` y el paréntesis de evidencia no se
reportan), CA-12 (`aprobado*` **conserva** el asterisco y no se lee `aprobado`;
`**sí**` se lee `sí`), CA-13 (el informe enseña el valor crudo **y** el normalizado),
CA-10 (la `ñ` descompuesta **no** se convierte en `n`: `año` en NFD sigue leyéndose `año`).
**Prueba diferencial obligatoria (lector bash vs `campos-req.awk`):** 39 cabeceras
—decoradas (`**Estado:**`, `__Estado:__`, `*Estado:*`, backticks, `Estado :`, sangrado con
espacio y con tabulador, `Estado:`+TAB), acentuadas en NFC y en NFD, con paréntesis de
evidencia, con barra vertical, con asterisco suelto, con clave asimétrica, con dos puntos
en el valor— alimentadas a **los dos** lectores y comparados los seis campos normalizados:
**0 diferencias**. No encontré ninguna cabecera donde discrepen.

### REQ-007 — criterios exigibles en esta ventana
Rendidos y **pasan**: bloque A completo (CA-01 tabulador, CA-02 `**Estado:**`, CA-03 los
tres sangrados, CA-04 las cuatro decoraciones, CA-05 y CA-06 los dos controles del
historial, CA-07 la clave decorada de `Hallazgos abiertos` con clase bloqueante, CA-08 los
tres campos con clave decorada, CA-09 la asimetría que nunca baja una exigencia);
CA-43/CA-44 (bytes de control C0: probé además `\002 \007 \013 \014 \016 \033 \037`, DENY
en todos; los tres controles de CA-44 correctos, incluido el Markdown con tabuladores y
saltos que sigue en ALLOW); CA-45/CA-46 (NUL); CA-47/CA-48 (barra duplicada y, de más,
**triple** barra → DENY; `//docs//` → ALLOW; el `desarrollador` con barra duplicada →
ALLOW); CA-49/CA-50 (enlace, enlace roto en 140 ms, archivo regular); CA-51/CA-52 (ver
abajo); CA-53 (los tres archivos del manifiesto y de `.claude-plugin/` sólo los edita el
`desarrollador`; `tests/` y `templates/` siguen abiertos; las plantillas **no** heredan los
globs); CA-54/CA-55 (techo con máximo 131072 y aviso; `-5`, `0`, `100` y la clave ausente
caen al defecto; el motivo imprime el techo vigente y no nombra ninguna ruta);
CA-56/CA-57 (REQ de 298 KB: 1 edición 210 ms, 101 ediciones 2 112 ms con DENY por
veredicto, 401 y 1 501 ediciones DENY por presupuesto en 211 y 314 ms; REQ de 3,1 MB ×
401 ediciones DENY en 310 ms — el caso que antes no respondía a los 30 s);
CA-34/CA-35 cubiertos por el banco sin FAIL.

**SEC-005 (CA-51/52), probado a fondo por exigencia del encargo:** manifiesto con JSON
roto, vacío, `null`, array, cadena JSON, número JSON, booleano JSON y truncado a la
mitad → **aviso propio del arnés + DENY** en los ocho, en las dos puertas. Manifiesto
válido → DENY normal **sin** ningún aviso. Manifiesto válido pero sin `codigo_app` →
allow en `guard-codigo`, sin aviso (fail-closed que no se dispara por incompleto, CA-52d).
Contaminación: con el manifiesto roto, `ARNES_AGENTE_CODIGO`, `ARNES_REQ_DIR`,
`ARNES_GLOBS` y el estado terminal quedan **vacíos** — ninguna variable se rellena con
campos de la entrada. Sin `jq` y sin manifiesto: inerte, como declara `AGENTS.md` §13.
Hallazgos asociados: QA-104 (coste) y QA-105 (recuperación).

**No se rinden en esta ventana, por ser de 1.32.0:** bloque B (CA-10…CA-13), bloque C
(CA-14…CA-33 salvo CA-34/CA-35, que sí se rinden como no-regresión) y CA-36…CA-39. No los
probé y no los cuento ni a favor ni en contra.

---

## 6. Datos, no hallazgos

Cosas que medí, que no incumplen ningún criterio vigente, y que dejo escritas para que
nadie las tenga que volver a medir.

1. **`Estado:` con paréntesis de evidencia en el informe.** La candidata deja de
   reportarlo; 1.30.3 lo reportaba. Es un cambio **querido** y declarado en REQ-003 CA-16.
   Sólo la referencia a la línea base de REQ-010 CA-11 está mal (→ QA-108).
2. **`Estado:** completado` (cierre de énfasis suelto tras los dos puntos) se lee
   `**completado` y no dispara la puerta. Los **dos** lectores coinciden, así que no hay
   divergencia; y no es ninguna de las cuatro formas que enumera REQ-007 CA-04. Queda como
   dato para el bloque B de 1.32.0.
3. **`- Estado: completado`** (la cabecera como viñeta) no se lee como estado en ninguno de
   los dos lectores. Misma familia que el punto 2 y que CA-10 del bloque B.
4. **`git worktree remove --force .`** → ALLOW. Está fuera de `git.prohibidos` por defecto,
   así que no es un incumplimiento; lo apunto porque destruye un árbol de trabajo y la
   lista lo podría querer.
5. **`{"veredictos":"x"}`** —JSON válido, un objeto, con una clave del tipo equivocado— se
   trata como manifiesto ilegible: DENY con el mensaje de SEC-005, que dice «no se puede
   leer como objeto JSON». El fail-closed es el correcto; el mensaje describe mal la causa.
6. **Latencia añadida en el camino común** (200 invocaciones, Linux/WSL2): `ls -la`
   26,0 ms/inv contra 16,1 ms/inv de 1.30.3; `git commit -m "mensaje normal"` 26,0 contra
   16,6. Dentro del «< 50 ms» que piden CA-44 y CA-50(d); el problema es el **proceso**, no
   el reloj (→ QA-104).

## 7. Rendimiento — qué está medido y qué no

`AGENTS.md` no fija un umbral de usuarios concurrentes ni de latencia de servicio para
este proyecto (no hay servicio: son hooks). Los umbrales que sí existen son los de los
propios REQ, y están medidos arriba: CA-56/CA-57, CA-34/CA-35 (por el banco), CA-44,
CA-50(d) y el conteo de procesos de QA-104.

Lo que **no** puedo declarar cumplido, y no lo declaro:

- El coste real en **Windows/MSYS**, que es la plataforma del desarrollador y donde
  `AGENTS.md` §2 cifra el fork en 1,2–6 s. Todo lo de este informe se midió en
  Linux/WSL2. El +1 proceso de QA-104 es, precisamente, la magnitud que cambia de
  significado al cambiar de plataforma. **Pendiente de medir allí antes de publicar.**
- El banco completo en Windows: `AGENTS.md` §7 exige correrlo entero al menos una vez en
  la plataforma del desarrollador antes de pedir la fusión. No consta que se haya hecho
  para esta candidata.

## 8. Cola de aprobaciones y visto bueno de seguridad

`PENDING_APPROVAL.md` no tiene entradas bajo `## Pendientes` (0 pendientes, según la misma
función que lee la puerta). Los siete REQ son `Sensible a seguridad: sí` y `Rigor: critico`,
así que ninguno cierra sin `Seguridad: aprobado`; el `auditor-seguridad` no puede firmar
antes que QA (`AGENTS.md` §6), y por eso este informe emite el veredicto `QA:` ahora, sin
esperarle. **No** marco ningún `Estado:` — el cierre no me corresponde y hay dos REQ con
hallazgos abiertos de clase bloqueante.

## 9. Vuelta 1 de 3

REQ-004 y REQ-005 vuelven al `desarrollador` con QA-102 y QA-101. Los otros cinco quedan
`QA: aprobado`. El contador de vueltas es **por REQ** y no se reinicia con cada hallazgo
nuevo (`AGENTS.md` §6).

---

# Re-validación, vuelta 1 del bucle (vuelta 2 de 3 del tope por REQ)

**Commit validado:** `22636cb` · **Rama:** `cand/1.31.0-mecanismos`
**Línea base de no-regresión:** instalación estable **1.30.3** (`6c1b58a`).
**Punto de comparación intermedio:** `40312c6`, la candidata que rindió la vuelta 1 — es el
par *fail-before* útil para todo lo que cambió entre vueltas.
**Alcance:** re-validación **acotada**. Vuelven REQ-004 y REQ-005; los cinco aprobados
(REQ-002, 003, 006, 009, 010) no se rinden otra vez, sólo se comprueba que no se movieron.

## 1. Quality gates

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | verde |
| `jq -e . hooks/hooks.json` | verde |
| `jq -e .` sobre `plugin.json` y `marketplace.json` | verde (ambos `1.31.0`) |
| `jq -e . .arnes/config.json` | verde |

## 2. Banco de escenarios — cifras propias y cuadre

| Corrida | PASS | FAIL | SKIP | Total | Cuadre |
|---|---|---|---|---|---|
| Candidata `22636cb`, banco del desarrollador — corrida 1 | 605 | 0 | 1 | 606 | = `CASOS_ESPERADOS` ✔ |
| Candidata, corrida 2 | 605 | 0 | 1 | 606 | ✔ |
| Candidata, corrida 3 | 605 | 0 | 1 | 606 | ✔ |
| Línea base 1.30.3 (`ARNES_HOOKS_DIR`) — corrida 1 | **457** | **148** | 1 | 606 | ✔ |
| Línea base 1.30.3 — corrida 2 | **458** | **147** | 1 | 606 | ✔ |
| Candidata, banco **con los 4 casos que añade QA en esta vuelta** | 609 | 0 | 1 | 610 | = `CASOS_ESPERADOS` ✔ |

**Determinismo de la candidata:** las tres corridas dan el **mismo veredicto caso a caso**
(comparación automática, no sólo del total). No hay ningún caso flaky *en la candidata*.

**La línea base sí flakea, y por eso mi cifra no coincide con la de la coordinadora**
(458/147/1). Las dos son ciertas; ver **QA-111**. La cifra de la **candidata** (605/0/1) sí
es reproducible y coincide con la que se me entregó.

## 3. Estado de los hallazgos de la vuelta 1

| Hallazgo | REQ | Clase | Estado tras `22636cb` | Evidencia |
|---|---|---|---|---|
| QA-101 | REQ-005 | contrato | **CERRADO** | §4.1 |
| QA-102 | REQ-004 | contrato | **CERRADO** | §4.2 |
| QA-103 | REQ-007 | instrumento | **CERRADO** | §4.3 |
| QA-104 | REQ-007 | contrato | **CERRADO** en el camino común | §4.4 |
| QA-105 | REQ-007 | instrumento | **CERRADO** | §4.5 |
| QA-106 | REQ-002 | instrumento | **CERRADO** | §4.6 |
| QA-107 | REQ-007 | instrumento | **CERRADO** | §4.6 |
| QA-108 | REQ-010 | instrumento | sigue **abierto** (dueño analista; no se re-midió: es un write-back) | vuelta 1 |

### 4.1 QA-101 — cerrado

Las cuatro formas que atravesaban la puerta ahora **DENY**, con los controles intactos:

```
git clean --force        DENY     git clean -n            ALLOW
git clean --force -d     DENY     git clean --dry-run     ALLOW
git clean -d --force     DENY     git clean -nd           ALLOW
git stash save "wip"     DENY     git stash list / show   ALLOW
```

Probado además, todo contra la candidata: `--force=true` y `--force=false` → DENY (el valor
pegado no importa, y `arnes_git_canon` corta por el `=`); `--forced` y `--force-with-lease`
→ ALLOW (la comparación de la larga es **entera**, no por prefijo); `-xdf`, `-ffdx`, `-n -f`,
`-d -f` → DENY; `-d -n`, `-F`, `--Force` → ALLOW (la comparación por letras es sensible a
mayúsculas). Opciones globales de git delante del subcomando: `git -c x=y clean -f`,
`git -c core.x=1 clean --force`, `git --git-dir=… clean --force`, `git -C proj clean --force`,
`git --no-pager stash save`, `git --work-tree=. clean -f`, `git -c a=b -C p reset --hard` →
**DENY los siete**. `stash`: `save`, `save wip`, `push`, `push --all`, `--all`, `-a`,
`--include-untracked`, `-u`, `pop`, `drop`, `clear` → DENY; `list`, `show`, `apply`,
`branch x`, `create`, `store x` → ALLOW.

**Límite declarado, confirmado y sin cambio:** un token de regla de **varias letras** sigue
exigiendo un mismo grupo. Con `git.prohibidos: ["clean -fd"]`, `git clean -fd` → DENY y
`git clean --force -d`, `git clean -f -d`, `git clean --force --directory` → **ALLOW**. Está
declarado en CA-21.1 y en el fuente; el mapeo por defecto usa `clean -f` y no lo alcanza.

**La equivalencia es simétrica**, como afirma el fuente: con `git.prohibidos: ["push --force"]`,
`git push --force origin main` **y** `git push -f origin main` → DENY, mientras
`git push --force-with-lease` y `git push origin main` siguen en ALLOW.

### 4.2 QA-102 — cerrado

Fixture propio (proyecto nuevo, `rotacion.activo: true`, `umbral_bytes: 1000`, artefacto con
`"seccion": "## Historial"` mientras el documento dice `## Historial de cambios`, REQ de 60
entradas), una parada real por `hooks/stop.sh`:

| CA-09 pide | Observado |
|---|---|
| El documento no cambia | **sí**, md5 idéntico ✔ |
| No se crea archivo de historia | **sí**, el directorio no existe ✔ |
| `arnes_warn` que nombre archivo y sección | **sí** ✔ |
| El bloque derivado lo refleja | **sí** ✔ |

El aviso: `rotacion: 'requirements/REQ-100.md' casa el artefacto declarado pero NO contiene
la seccion '## Historial'; …`. El bloque: `**Rotación:** 1 archivo(s) casan un artefacto
declarado pero **no contienen la sección declarada** (p. ej. 'REQ-100.md' → '## Historial')…`.

**Orden rotar→derivar, comprobado en lo que podía romper:** tres paradas seguidas dan un
bloque derivado **idéntico byte a byte**; siguen habiendo exactamente **2** marcadores; el
texto que escribió una persona **antes y después** de los marcadores sobrevive a las tres.
Con el nombre de sección corregido, la rotación ocurre (20 en el origen, 40 en
`requirements/historial/`, **un** puntero que además nombra las 20 que quedan), **no** hay
aviso y el bloque **no** trae la línea de Rotación; dos paradas más no mueven ni el origen ni
el destino (md5 iguales) ni añaden un segundo puntero.

### 4.3 QA-103 — cerrado

El motivo imprime `hasta un maximo de 131072 bytes` en **las dos** puertas
(`guard-codigo.sh` y `guard-completado.sh`), con un comando de 70 KB.

### 4.4 QA-104 — cerrado en el camino común

Medido con un `jq` instrumentado primero en el `PATH`, contando **invocaciones**
(no líneas del programa: la primera versión de mi sonda contaba líneas y daba 6 donde había 2).
La tabla es de **invocaciones totales** por llamada a `guard.sh`; las dos primeras son
siempre el preludio, que no cambia entre versiones.

| Entrada | Candidata `22636cb` | v1.30.3 | `40312c6` |
|---|---|---|---|
| `Bash: ls -la` | **2** | 2 | 3 |
| `Bash: npm run build` | **2** | 2 | 3 |
| `Bash: git status` | **3** | 2 | 3 |
| `Bash: git clean -fd` | 4 | 2 | 4 |
| `Edit` fuera de los globs | **3** | 4 | 4 |
| `Edit` dentro de los globs | 4 | 5 | 5 |

Descontando el preludio, son exactamente las cuatro cifras que se me entregaron: `ls -la` y
`npm run build` **1** (v1.30.3: 1; `40312c6`: 2), `git status` **2**, `Edit` fuera de los
globs **2** (v1.30.3: 3). **Confirmadas.** El camino común vuelve al coste de v1.30.3 y el
`Edit` queda por **debajo**. El `+1` de `git status` es `guard-git` leyendo el manifiesto,
que REQ-005 CA-27 declara ("como máximo 1 lectura por invocación").

**Reloj** (`ls -la`, n=200 por versión, con warm-up de 20, Linux/WSL2, mismas condiciones y
en la misma sesión):

| | ms/invocación |
|---|---|
| Candidata `22636cb` | **24,7** |
| v1.30.3 | 20,4 |
| `40312c6` | 30,3 |

La candidata recupera ~5,6 ms de los ~9,9 ms que `40312c6` había añadido, y queda **+4,3 ms**
sobre v1.30.3 **sin ningún proceso de más** en el camino común: lo que queda no es un fork,
es trabajo de shell (un archivo más que `source`ar y un programa `jq` mayor). Dentro del
`< 50 ms` de CA-44 y CA-50(d). Mis cifras absolutas no coinciden con las que se me
entregaron (23,1 → 19,0 contra 13,7), y no deberían: son otra corrida y otra carga. La
**dirección y el orden de magnitud** sí coinciden.

### 4.5 QA-105 — cerrado

Con `.arnes/config.json` = `{"agentes":{,}` (roto confirmado con `jq -e`):

| Llamada | Resultado |
|---|---|
| `Write` a `.arnes/config.json`, `desarrollador` | **ALLOW** ✔ |
| `Write` a `.arnes/config.json`, `qa-tester` / coordinadora | **ALLOW** ✔ (no filtra por agente, declarado) |
| `Bash: printf "{}" > .arnes/config.json` | **ALLOW** ✔ |
| `Bash: printf "{}" > .arnes/config.json; echo x > docs/n.md` | **DENY** ✔ (repara *y además* escribe: no es una reparación) |
| `Write` a `docs/notas.md` / `src/a.ts` | DENY ✔ |
| `Bash: echo x > docs/n.md` | DENY ✔ |
| `Bash: ls -la` | ALLOW ✔ |

**Y con el manifiesto sano vuelve a estar protegido:** con `.arnes/config.json` dentro de
`codigo_app.globs`, el `desarrollador` puede escribirlo y el `qa-tester` **no**. La salida
que el motivo recomienda ya es alcanzable, y la excepción no deja el archivo abierto.

### 4.6 QA-106 y QA-107 — cerrados

Toda clave con el tipo equivocado **cae al defecto y avisa**, y los controles callan:

| Clave / valor | ¿Avisa? |
|---|---|
| `veredictos.exigir_fecha` = `"true"`, `1`, `[]` | sí (las tres) |
| `veredictos.exigir_fecha` = `true` · clave ausente | **no** (controles) |
| `limites.bash_max_analisis` = `"999999"`, `1e9`, `1.5`, `-5`, `0` | sí (las cinco) |
| `limites.bash_max_analisis` = `131072` | **no** (control) |
| `codigo_app.globs` = cadena · `agentes.agente_codigo` = `7` · `git.prohibidos` = cadena | sí |

**`git.activo: "false"` (cadena) NO apaga la puerta**: avisa (`…se recibio "false"; se ignora
y manda el valor por defecto…`) y `git clean -fd` sigue en **DENY**. Con `false` booleano de
verdad, ALLOW. El fail-closed es visible y diagnosticable, que es lo que pedía el criterio.

## 5. Hallazgos nuevos de esta vuelta

### QA-110 — el «fin de opciones» y las equivalencias `-S`/`-W` mueven tres veredictos de DENY a ALLOW sin que ningún criterio los describa
**REQ-005 CA-21 · clase `contrato` · severidad baja · dueño `analista-requerimientos`**

**Reproducción** — 103 comandos pasados por `guard-git.sh` contra `40312c6` y contra la
candidata, comparando veredictos. De las **14** transiciones, 11 son `allow → deny` (el
arreglo de QA-101, esperado) y **3 son `deny → allow`**:

```
git clean -- -f        40312c6=DENY   22636cb=ALLOW
git reset -- --hard    40312c6=DENY   22636cb=ALLOW
git restore -S .       40312c6=DENY   22636cb=ALLOW
```

*(Canario del par: `git clean -fd` → DENY y `ls -la` → ALLOW en **las dos** versiones, antes
de cada tanda. La primera vez que monté esta comparación el `git archive` falló por estar en
otro directorio, el binario no existía, la salida vacía se leyó como ALLOW y las tres
transiciones **desaparecieron**; la tabla de arriba es la medición repetida con la extracción
verificada.)*

**Las tres son semánticamente correctas** — medido en un repositorio git desechable, no
razonado:

- `git reset -- --hard`: el árbol y el índice quedan **exactamente igual** (`rc=0`, ambos
  ficheros intactos). `--hard` ahí es un pathspec, no el flag.
- `git clean -- -f`: con la configuración por defecto git **se niega**
  (`fatal: clean.requireForce is true and -f not given`); con `clean.requireForce=false`
  borra **sólo** el archivo llamado `-f` y deja el resto de los no seguidos en su sitio.
- `git restore -S .`: el árbol de trabajo **no se toca** (el fichero conserva su contenido
  sin comitear) y sólo se rehace el índice. Es el falso positivo preexistente que el
  desarrollador declara, y **queda confirmado**.

**Esperado vs observado** — lo que falla no es la conducta, es el contrato. CA-21 dice que la
entrada casa «**si y sólo si** el subcomando coincide y **todos** los tokens de la entrada
están presentes en ese segmento, **en cualquier orden**». En `git clean -- -f` el token `-f`
**está presente en el segmento** y la regla **no casa**. La tabla de equivalencias de CA-21.2
enumera `-f`, `-d`, `-n`, `-u`, `--all` y `stash save`, y **no** `restore --staged` ≡ `-S` ni
`restore --worktree` ≡ `-W`, que el código sí implementa y que son la causa del tercer cambio.
El fuente documenta el fin de opciones con detalle, y el banco fija dos de los tres casos —
pero `AGENTS.md` §9 pide el **criterio**, no el comentario ni el caso.

**Por qué es `contrato` y no `instrumento`:** el REQ afirma algo falso sobre lo construido, y
además en la dirección que afloja la puerta. No pido cambiar una línea de código: el código
está bien. Pido el **write-back** — una cuarta precisión en CA-21 con la regla del `--` (una
opción deja de buscarse; un token literal se sigue buscando, y por eso `git checkout -- .`
sigue en DENY), `-S`/`-W` añadidos a la lista de CA-21.2, y las tres transiciones
`deny → allow` declaradas con su medición. Cierra sin tocar `hooks/`.

**Casos de banco añadidos:** `QA 1.31.0 v2: fin de opciones 'git clean -- -f' -> allow`,
`… 'git reset -- --hard' -> allow`, y el control `… 'git checkout -- .' sigue deny`.

### QA-109 — una sección que **sí** existe pero no tiene ninguna entrada reconocible no rota nada, y tampoco lo dice
**REQ-004 · clase `instrumento` · severidad media · dueño `desarrollador` + analista**

**Reproducción** — mismo fixture que §4.2 pero con el nombre de sección **correcto**
(`## Historial de cambios`), 60 filas de tabla `| fecha | … |` y ~7 KB, muy por encima del
umbral de 1 000 bytes. Una parada real por `hooks/stop.sh`:

| | Observado |
|---|---|
| ¿Rota algo? | **no** — las 60 filas siguen en el origen |
| ¿Crea archivo de historia? | **no** |
| ¿Avisa por stderr? | **no** |
| ¿Lo dice el bloque derivado? | **no** |

**Causa** — `arnes_rotar_seccion` define entrada como lo que empieza a columna cero por
`- `, `* `, `### ` o `N. ` (CA-08), y CA-07 hace de las filas de tabla **continuaciones**. Una
sección hecha sólo de continuaciones tiene **cero** entradas y «no se toca» — correcto, y
silencioso.

**Por qué importa, y por qué igual no bloquea.** Es la **rama hermana** de CA-09: el mismo
error de mapeo, el mismo silencio que CA-09 declara inaceptable («un artefacto declarado que
no existe es un error de mapeo que hay que ver, no un acierto silencioso»), en el caso que el
arreglo no cubrió. Y no es hipotético: el `## Historial de cambios` de los REQ de **este
repositorio** es exactamente una tabla, así que ArnesJuan encendiendo su propia rotación no
rotaría nada y no se enteraría. Aun así: la rotación viene **apagada** por defecto (CA-01),
ningún criterio promete rotar una sección sin entradas, y el REQ no afirma nada falso — nadie
ve, decide ni cobra distinto. Por eso `instrumento`, y por eso **no bloquea** el cierre de
REQ-004.

**Caso de banco añadido:** `QA 1.31.0 v2: QA-109 seccion sin entradas reconocibles: no rota y
no avisa` — fija la conducta **medida hoy**; si se decide avisar, el caso cambia con el
criterio que lo ordene.

### QA-111 — un caso del banco es **flaky**: decide por reloj de pared contra un umbral sin margen
**Banco (`tests/escenarios/hooks/run.sh`) · clase `instrumento` · severidad media · dueño `desarrollador`**

**Reproducción** — dos corridas completas del banco contra la **misma** línea base (1.30.3),
sin cambiar nada entre ellas:

```
corrida 1: FAIL  DEV v3: heredoc CITADO de ~300 KB -> allow y barato
                 esperado=allow got=allow  1038ms (umbral 1000ms)     -> 457 PASS / 148 FAIL
corrida 2: PASS  DEV v3: heredoc CITADO de ~300 KB -> allow y barato
                 (allow en 616ms, umbral 1000ms)                      -> 458 PASS / 147 FAIL
```

El **veredicto es el correcto en las dos** (`got=allow`); lo que oscila es el reloj. Es
exactamente la diferencia entre mi cifra de línea base y la que se me entregó: las dos
mediciones son ciertas y ninguna es reproducible.

Contra la **candidata** el mismo caso midió 618, 715 y 717 ms en tres corridas: hay margen y
no falló ninguna vez. El riesgo no es de la candidata — es del **instrumento**: un caso que
decide por reloj de pared con ~30 % de margen en una máquina ociosa puede poner en rojo el
check `hooks-en-linux`, que es puerta **requerida** de `main`, por carga ajena; y un rojo que
la gente aprende a re-lanzar es un rojo que deja de significar algo.

**No bloquea** ningún REQ de negocio (atacar el guardián es deuda con dueño, `AGENTS.md` §6),
pero conviene resolverlo antes de publicar: subir el umbral con margen medido, repetir la
medición y quedarse con la mejor de N, o separar el veredicto (que es determinista) del
tiempo (que no lo es) en dos casos.

### QA-112 — el umbral de reloj de CA-59 está escrito en milisegundos absolutos y no se puede evaluar en otra máquina
**REQ-007 CA-59 · clase `instrumento` · severidad baja · dueño `analista-requerimientos`**

CA-59 exige, además del conteo de procesos, que «el reloj se mantiene en **≤ 18 ms por
invocación** en Linux/WSL2 sobre 200 invocaciones (v1.30.3 midió 16,1 ms; la candidata sin
este criterio, 26,0 ms)». Medido por mí, misma sesión, mismas condiciones, n=200 con warm-up:

| | ms/inv (mi medición) | ms/inv (referencia de CA-59) |
|---|---|---|
| v1.30.3 | **20,4** | 16,1 |
| Candidata `22636cb` | **24,7** | — (el umbral es 18) |
| `40312c6` | **30,3** | 26,0 |

En mi máquina **la propia línea base ya supera el umbral** (20,4 > 18). Aplicado
literalmente, CA-59 declararía FALLIDA una candidata que en realidad **cumple lo que el
criterio quiere**: el conteo de procesos vuelve al de v1.30.3 y el reloj baja de 30,3 a 24,7,
es decir de 1,49× a 1,21× la base. Un umbral absoluto de milisegundos, sin normalizar contra
la base medida **en la misma corrida**, no mide el cambio: mide la máquina.

No bloquea nada —los tres controles (a), (b) y (c) de CA-59 **pasan**: con el manifiesto roto
`echo x > src/a.ts` → DENY con aviso, `ls -la` y `npm run build` → ALLOW **sin** aviso, y el
`Edit` fuera de los globs gasta 2 ≤ 3—, y el conteo de procesos, que es la parte que sí
discrimina, está confirmado en §4.4. Lo que pido es que el umbral se exprese **relativo a la
base medida en la misma corrida** (p. ej. «≤ 1,25× v1.30.3 medida en la misma sesión»), con
las cifras absolutas como referencia informativa. Si no, cada QA en una máquina distinta abre
este mismo hallazgo.

## 6. No-regresión de los cinco REQ aprobados

No se rinden otra vez. Lo comprobado:

- El banco corre **entero** en las tres corridas de la candidata y no hay **ni un FAIL**:
  las secciones de REQ-002, 003, 006, 009 y 010 siguen verdes.
- Los veredictos son **idénticos caso a caso** en las tres corridas.
- Diferencial dirigido sobre REQ-002 —la superficie que el diff sí toca, porque QA-106 vive
  en `arnes_parse_manifest`, que leen **todas** las puertas—: cinco combinaciones de
  `exigir_fecha` (`true`/`false`/`"true"` con y sin fecha, y con `QA: pendiente`) dan el
  **mismo veredicto** en la candidata, en `40312c6` y en v1.30.3. El cambio de QA-106 añade
  un **aviso** y no mueve ninguna decisión, que es lo que el hallazgo pedía.
- **Ningún veredicto de los cinco cambió.** No hay hallazgo por esta vía.

## 7. Rendimiento

Los umbrales de los propios REQ están medidos en §4.4 (procesos y reloj; CA-26, CA-27, CA-44,
CA-50(d)). `AGENTS.md` no fija umbral de concurrencia ni de latencia de servicio para este
proyecto — no hay servicio, son hooks —, así que no hay prueba de carga que rendir.

**Lo que sigo sin poder declarar cumplido, igual que en la vuelta 1:**

- El coste en **Windows/MSYS**, la plataforma del desarrollador, donde `AGENTS.md` §2 cifra
  el fork en 1,2–6 s. Todo esto es Linux/WSL2. Ahora importa **menos** que en la vuelta 1,
  porque el camino común ya no añade ningún proceso; pero los +4,3 ms de shell no están
  medidos allí.
- El **banco completo en Windows**, que `AGENTS.md` §7 exige al menos una vez antes de pedir
  la fusión. No consta que se haya hecho para esta candidata.

## 8. Cola de aprobaciones y seguridad

`PENDING_APPROVAL.md` sigue **sin entradas** bajo `## Pendientes`. Los dos REQ de esta vuelta
son `Sensible a seguridad: sí` y `Rigor: critico`: no cierran sin `Seguridad: aprobado`, y el
`auditor-seguridad` no puede firmar antes que QA (`AGENTS.md` §6). Por eso emito el veredicto
`QA:` ahora y **no** toco ningún `Estado:`.

## 9. Veredicto de la vuelta

| REQ | Veredicto | Hallazgos abiertos |
|---|---|---|
| **REQ-004** | **QA: aprobado** | QA-109 (instrumento) — no bloquea |
| **REQ-005** | **QA: con-hallazgos** | QA-110 (contrato) — write-back del analista |

REQ-004 queda **aprobado**: QA-102 cerrado, los dos lados de CA-09 se cumplen y lo único
abierto es `instrumento`.

REQ-005 **no** se aprueba todavía, y quiero ser preciso sobre por qué: **el código está
bien**. QA-101 está cerrado y no encontré ni un falso positivo nuevo en 103 comandos. Lo que
falta es una línea de contrato: tres veredictos se movieron de DENY a ALLOW y CA-21 dice hoy
lo contrario de lo que la máquina hace. Es trabajo del `analista-requerimientos`, no del
`desarrollador`, y no requiere tocar `hooks/`.

**Vueltas gastadas (tope 3, por REQ, sin reinicio por hallazgo):** REQ-004 **2 de 3**
(termina aprobado); REQ-005 **2 de 3**, queda una.

---

# Re-validación, vuelta 2 del bucle (vuelta 3 de 3 del tope por REQ)

**Commit validado:** `430f4fd` · **Rama:** `cand/1.31.0-mecanismos`
**Línea base de no-regresión:** instalación estable **1.30.3** (`6c1b58a`).
**Alcance:** encargo **acotado** — REQ-005 contra el CA-21 **reescrito**, el cierre de QA-111,
el cierre de QA-109 y la puesta al día de los campos `Hallazgos abiertos:`. Los cinco REQ ya
aprobados (002, 003, 006, 009, 010) y REQ-004 no se rinden otra vez: sólo se comprueba que
ningún veredicto suyo se movió.
**Plataforma:** Linux (WSL2). Sondas con `timeout 30`; todo git, en proyectos desechables bajo
`/tmp`, **nunca** en este worktree. Canario positivo (`git clean -fd` → DENY) y negativo
(`ls -la` → ALLOW) antes de cada tanda, y el lector de la sonda trata la **salida vacía como
ALLOW**, que es lo que hace el hook.

## 1. Quality gates

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | verde |
| `jq -e . hooks/hooks.json` | verde |
| `jq -e .` sobre `plugin.json` y `marketplace.json` | verde (ambos `1.31.0`) |
| `jq -e . .arnes/config.json` | verde |
| `tools/arnes-lectura.sh` sobre este repositorio | 11 REQ, **0 anomalías**, cola de aprobaciones **0** |

## 2. Banco — cifras propias, con cuadre

| Corrida | PASS | FAIL | SKIP | Total | Cuadre |
|---|---|---|---|---|---|
| Candidata `430f4fd`, banco del desarrollador | **614** | **0** | 1 | 615 | = `CASOS_ESPERADOS` ✔ |
| Línea base 1.30.3 (`ARNES_HOOKS_DIR`) — corrida 1 | **462** | **152** | 1 | 615 | ✔ |
| Línea base 1.30.3 — corrida 2 | **462** | **152** | 1 | 615 | ✔ |
| Línea base 1.30.3 — corrida 3 | **462** | **152** | 1 | 615 | ✔ |
| Candidata, banco **con los 10 casos que añade QA en esta vuelta** | **624** | **0** | 1 | 625 | = `CASOS_ESPERADOS` (625) ✔ |
| Línea base 1.30.3, mismo banco de 625 | 468 | 156 | 1 | 625 | ✔ |

Las cifras de partida quedan **confirmadas**: candidata 614/0/1 y línea base 462/152/1, las dos
sobre 615. El SKIP es el de siempre y explica su condición en su línea (`ruta estilo Windows con
backslashes — sin cygpath: caso sólo de Windows`).

**La línea base ya no flakea.** Las tres corridas dan el mismo total **y el mismo veredicto caso
a caso**: comparadas las 615 líneas de resultado con los milisegundos recortados, `diff` da
**cero diferencias** entre la 1 y la 2 y entre la 1 y la 3. Es el cierre medido de QA-111 (§4).

De los 10 casos nuevos, **4 fallan contra 1.30.3** (los que esperan DENY: allí `guard-git` no
existe) y 6 pasan trivialmente por ausencia de puerta; por eso la base sube a 468/156.

**Determinismo de la candidata:** dos corridas completas del banco de 625 dan **624/0/1** las dos
y el **mismo veredicto caso a caso** (`diff` sin diferencias con los milisegundos recortados).

## 3. QA-110 — el CA-21 reescrito, verificado contra el código

El arreglo fue del **criterio**, así que lo que se verifica es que **el código cumple lo que
CA-21 dice ahora**, no al revés. Los 24 comandos, todos por `guard-git.sh` con la lista por
defecto, en la misma tanda y con sus dos canarios:

| Entrada | CA-21 pide | Medido |
|---|---|---|
| `git clean -- -f` | ALLOW (punto 4) | **ALLOW** ✔ |
| `git clean -- --force` | ALLOW (punto 4) | **ALLOW** ✔ |
| `git reset -- --hard` | ALLOW (punto 4) | **ALLOW** ✔ |
| `git checkout -- .` | DENY (control del punto 4: `.` es literal) | **DENY** ✔ |
| `git restore -S .` | ALLOW (excepción de `restore`, forma corta) | **ALLOW** ✔ |
| `git restore --staged .` | ALLOW (misma excepción, forma larga) | **ALLOW** ✔ |
| `git restore -W .` | DENY (`--worktree` sí arrasa) | **DENY** ✔ |
| `git restore -S -W .` · `git restore --staged --worktree .` | DENY | **DENY** los dos ✔ |
| `git clean --force` · `--force -d` · `-d --force` | DENY (punto 2) | **DENY** los tres ✔ |
| `git stash save "wip"` | DENY (alias) | **DENY** ✔ |
| `git clean -n` · `--dry-run` · `-nd` | ALLOW (CA-10 intacto) | **ALLOW** los tres ✔ |
| `git stash list` · `git stash show` | ALLOW (CA-08 intacto) | **ALLOW** los dos ✔ |
| `git clean -fd` · `-ffdx` | DENY (punto 1) | **DENY** los dos ✔ |
| `git clean -d` | ALLOW (`-f` no casa con `-d`) | **ALLOW** ✔ |
| `git stash` · `git stash -u` | DENY (punto 3) | **DENY** los dos ✔ |

**La tabla de equivalencias de CA-21.2, entrada por entrada, con reglas propias** (manifiesto
desechable con `git.prohibidos` a medida): `push --force` deniega `git push --force origin main`
**y** `git push -f origin main`, y deja pasar `--force-with-lease`; `stash --all` deniega
`git stash --all` y `git stash -a`; `clean -x` deniega `git clean -x` y `git clean -xdf` y deja
pasar `git clean -f`; `clean --directory` ≡ `-d` y `stash --include-untracked` ≡ `-u`, ya
medidas en la vuelta 2, siguen igual. **No hay ninguna equivalencia que el criterio prometa y el
código no tenga.**

### Lo que el criterio nuevo dejó fuera — los cuatro bordes que pedía el encargo

Medidos uno a uno; los cuatro son **correctos** y ninguno abre la puerta:

| Borde | Entrada | Medido | Por qué es correcto |
|---|---|---|---|
| Opción con **valor pegado** detrás de `--` | `git clean -- --force=x` · `git reset -- --hard=1` | ALLOW | detrás del `--` es el nombre de un archivo, valor pegado incluido |
| `--` **repetido** | `git clean -- -- -f` | ALLOW | el primero ya terminó las opciones |
| … y su mitad que sostiene la regla | `git clean -f -- -f` | **DENY** | la opción escrita **antes** del `--` sigue casando |
| Token **literal que parece opción** | `git checkout -- -` con la regla `checkout -` | DENY | ver §6, dato 1 |
| `--` como **único argumento** | `git clean --` · `git checkout --` · `git restore --` · `git reset --` | ALLOW | no queda opción que casar |
| … y con una entrada **sin tokens** | `git stash --` | **DENY** | `--` no es un posicional: sigue siendo la forma desnuda (CA-21.3) |

Y dos más de la excepción de `restore`, que el criterio no enumera y conviene fijar:
`git restore --staged -- .` → **ALLOW** (sólo rehace el índice) y `git restore -- --staged .` →
**DENY** (ahí `--staged` es una ruta y el comando sí restaura el árbol). Los dos, correctos.

**Diez de estos casos entran al banco** con el prefijo `QA 1.31.0 v3:` y `CASOS_ESPERADOS` pasa
de 615 a **625**.

## 4. QA-111 — cerrado, comprobado por mí

| Lo que pedía el cierre | Medido |
|---|---|
| Una sola función de medición | **sí**: `mide_hook` es el **único** sitio del banco con `date +%s%N` (líneas 233 y 235); los 10 casos cronometrados pasan por `cronometra_bash`, que la usa |
| El veredicto decide y no se reintenta | **sí**: un `got != esperado` falla en el acto, con su ms informativo |
| La respuesta del hook se comprueba por el código de salida, **también** donde se espera permitir | **sí**: `CRONO_RC -eq 124` se mira antes que nada, en las dos ramas |
| Techo holgado con reintento | **sí**: `CRONO_HOLGURA=4` × presupuesto y `CRONO_INTENTOS=3`, con la **mejor** de N |
| La línea base deja de flakear | **sí**: 3 corridas completas, **462/152/1** las tres, idénticas caso a caso (§2) |

**Y el caso que dependía del reloj sigue acreditando lo suyo.** Lo que quería probar —«el heredoc
citado de 300 KB se descuenta entero y no entra en el presupuesto de análisis»— hoy se comprueba
**sin reloj**, por el **motivo** del deny: con los mismos 300 KB citados más una escritura real
fuera del heredoc, el hook deniega **por la ruta** (`escribe en 'src/grande.ts'`) y **no** por el
presupuesto —hay un caso explícito que falla si el motivo dijera «demasiado grande para
analizarlo»—. Los dos hechos juntos son la propiedad entera: si los 300 KB hubieran entrado en el
presupuesto de 64 KiB, el motivo sería el del tamaño; y que el motivo nombre la ruta prueba que el
análisis corrió. El caso cronometrado se queda como **medición** del coste, ya sin poder de veto.

**Intento de romperlo:** un hook que muere **al instante** (no por `timeout`) devolvería salida
vacía, y `mide_hook` sólo mira el 124. Probado: con `ARNES_HOOKS_DIR` apuntando a una copia cuyo
`guard-codigo.sh` es `exit 1`, el banco **aborta** antes de contar nada («ABORT: el canario no
denegó; el hook no se está ejecutando. Con el hook muerto, todos los casos 'allow' pasarían en
falso»), con la evidencia del intento y el `rc` del segundo intento. El agujero está tapado por el
canario de sección, no por el temporizador — pero está tapado.

**Dueño y sitio:** QA-111 era un defecto del **banco** sin REQ. Queda asignado a **REQ-007**, que
es donde viven los criterios que ese banco acredita (Bloque H/I: CA-56, CA-57, CA-58, CA-62) y
donde nació el caso cronometrado. Clase `instrumento`; **cerrado** en `430f4fd`, así que no entra
en ningún `Hallazgos abiertos:`. Recomendación al analista, sin fuerza de hallazgo: que la política
de medición del banco («el veredicto decide; el reloj acompaña») quede escrita como criterio en el
Bloque I, para que el próximo caso cronometrado no nazca decidiendo por reloj.

## 5. QA-109 — la conducta está, y medida; el criterio no

Fixture propio (proyecto nuevo, `rotacion.activo: true`, `umbral_bytes: 1000`, artefacto
`## Historial de cambios` **correcto**, REQ de 7 004 bytes hecho de **60 filas de tabla** y ni una
entrada reconocible), una parada real por `hooks/stop.sh`:

| | Observado |
|---|---|
| ¿Rota algo? | **no** — md5 del origen idéntico ✔ |
| ¿Crea archivo de historia? | **no** — el directorio no existe ✔ |
| ¿Avisa por stderr? | **sí**, y el texto distingue el caso: «**SÍ contiene la sección** … pero no tiene ni una ENTRADA reconocible», con la lista de lo que cuenta como entrada ✔ |
| ¿Lo dice el bloque derivado? | **sí**, en **línea propia**: «*n* archivo(s) contienen la sección declarada y superan el umbral, pero **sin ninguna entrada reconocible**» ✔ |

**Los tres controles, todos correctos:**
1. **Por debajo del umbral** (misma tabla, 3 filas): stderr **vacío** y ninguna línea de Rotación
   en el bloque. El aviso no es ruido de cada parada.
2. **Con entradas de verdad** (60 viñetas `- `): **no avisa** y **rota** — 20 en el origen, 40 en
   `requirements/historial/`, un solo puntero.
3. **Texto distinto del de la sección que no existe**: con la sección declarada ausente sale el
   otro aviso («casa el artefacto declarado pero **NO contiene la sección**») y la otra línea del
   bloque. Con **las dos** averías a la vez (un REQ de cada clase en el mismo proyecto): **dos**
   avisos por stderr y **dos** líneas `**Rotación:**` separadas en el bloque derivado, cada una
   con su ejemplo. No se suman ni se confunden.

**Por qué QA-109 sigue abierto de todas formas.** El comportamiento nuevo —un aviso y una línea del
bloque derivado que antes no existían— **no está en ningún criterio de REQ-004**: CA-09 describe la
rama de la sección **que no existe**, y nada describe la rama hermana. Es exactamente la deriva que
`AGENTS.md` §9 prohíbe cerrar en silencio: *un hallazgo no se cierra hasta que el requerimiento lo
refleje*. Queda **abierto** como `instrumento` —no bloquea, REQ-004 sigue `QA: aprobado`— con el
write-back pendiente del `analista-requerimientos`: un criterio hermano de CA-09 con las cuatro
comprobaciones de la tabla de arriba y los tres controles. El código no tiene que cambiar.

## 6. Hallazgos nuevos de esta vuelta

### QA-113 — un token **entrecomillado** desaparece del análisis, y CA-21 afirma lo contrario
**REQ-005 CA-21 · clase `instrumento` · severidad media · dueño `analista-requerimientos`**

**Reproducción** (canarios de la tanda: `git clean -fd` → DENY, `ls -la` → ALLOW):

```
git clean "-f"        ALLOW      git clean -f          DENY   (el mismo comando, desnudo)
git clean '-f'        ALLOW      git clean -fd "src"   DENY   (el control que CA-21 sí cita)
git clean "-fd"       ALLOW      git clean --force""   DENY
git reset "--hard"    ALLOW      git clean -f""        DENY
git checkout "."      ALLOW
git restore "."       ALLOW
git clean --for"ce"   ALLOW
git clean "-"f        ALLOW
git clean \-f         ALLOW
git "clean" -f        ALLOW      "git" clean -f        ALLOW
git stash "save" wip  ALLOW
```

Idéntico a través de `guard.sh` (el despachador real), no sólo llamando a `guard-git.sh`.
Para bash, `git clean "-f"` **es** `git clean -f`: borra exactamente lo mismo.

**Causa** — `_arnes_desentrecomilla` (`hooks/lib.sh`) **borra el contenido** de cada par de
comillas antes de que nadie mire el comando, y `guard-git` usa ese mismo descuento a propósito
(CA-07, CA-13: `git commit -m "no uses git clean"` no puede ser un `git clean`). Lo borrado no
distingue entre un mensaje y un flag.

**Esperado vs observado** — lo que falla **no es la conducta, es una frase**. CA-21 cierra su
control diciendo: «el reconocimiento del subcomando y de sus flags **no depende de que los
argumentos vengan desnudos**, y el descuento de comillas … **no puede borrar el segmento del
comando**». El control que ese párrafo cita (`git clean -fd "src"`, con el **operando**
entrecomillado) **pasa**; la generalización a «sus flags» y «el segmento» es falsa, medida.

**Por qué `instrumento` y no `contrato` — la parte que decide, escrita entera para que se pueda
discutir.** No es una concesión de última vuelta:

1. **El hueco del producto ya está declarado, con dueño y ventana.** Es el mismo descuento de
   comillas que REQ-007 arregla en su **Bloque C (CA-14 … CA-18)**, asignado a **1.32.0** en el
   apartado «Dos ventanas». Y REQ-005 **ya lo dice**: comparte el descuento «no una segunda
   transcripción de esa regla», y «los Bloques C y G de REQ-007 arreglan **ese mismo** descuento …
   sus correcciones alcanzan a `guard-git` sin tocarlo». Incluso enumera «destino entrecomillado»
   entre los casos que REQ-007 cerrará.
2. **No es una regresión ni un empeoramiento.** Medido en la línea base: **v1.30.3 publicada**
   deja pasar `echo x > "src/a.ts"` exactamente igual (y deniega `echo x > src/a.ts`). La
   candidata se comporta igual en esa vía. La puerta nueva no abre nada que estuviera cerrado.
3. **La promesa de la Historia no alcanza aquí.** «Que una entrada declarada se escape **por cómo
   se escribe**» está acotada por sus dos ejemplos —la **ortografía de git** (`--force` ≡ `-f`) y
   el **alias del subcomando** (`stash save`)—, no por la sintaxis del shell, que el propio REQ
   declara descontada dos criterios antes.
4. **Precedente propio y consistente:** QA-108 tiene esta misma forma —un criterio que describe mal
   su control— y se clasificó `instrumento`, con REQ-010 aprobado. Lo contrario (QA-101) era una
   ortografía **de git** que nada declaraba, y por eso fue `contrato`.

**Write-back que pido** (una línea de texto, ningún cambio de código): que CA-21 acote su control a
lo que sostiene —el **operando** entrecomillado no desarma el reconocimiento— y declare que un
**token entrecomillado o escapado** (flag o subcomando) **no se ve**, con el reenvío a REQ-007
Bloque C y las tres formas medidas. Mientras esa frase diga lo que dice, hay una afirmación falsa
en el REQ.

**Casos de banco añadidos:** `QA 1.31.0 v3: QA-113 'git clean "-f"' -> allow`, `… 'git checkout
"."' -> allow` y el control `… 'git clean -f' desnudo sigue deny`. Fijan la conducta **de hoy**:
cuando el Bloque C aterrice, los dos `allow` pasarán a `deny` y el banco lo dirá en voz alta, que es
justo lo que se quiere de un hueco declarado.

### QA-114 — CA-59 exige a `git status` un conteo que otro REQ declara imposible, y su margen de reloj no discrimina
**REQ-007 CA-59 · clase `contrato` · severidad baja · dueño `analista-requerimientos`**

CA-59 (reescrito el 2026-09-06 con el write-back de QA-112) dice: «Dado un comando de `Bash` sin
ninguna escritura detectable (`ls -la`, `npm run build`, **`git status`**) … el número de procesos
externos … es **el mismo que en v1.30.3 publicada** … sobre las tres entradas de arriba», y que el
reloj «no supera la base en más de un 25 %».

**(a) El conteo.** Medido hoy con un `jq` instrumentado primero en el `PATH` que cuenta invocaciones
y delega en el real (invocaciones **totales** por llamada a `guard.sh`, preludio incluido):

| Entrada | Candidata `430f4fd` | v1.30.3 |
|---|---|---|
| `ls -la` | **2** | 2 ✔ |
| `npm run build` | **2** | 2 ✔ |
| `git status` | **3** | 2 ✘ |

El `+1` de `git status` es `guard-git` leyendo el manifiesto, y **REQ-005 lo declara legítimo**
(CA-27: «como máximo 1 lectura por invocación»; CA-26: el conteo agregado «lo gobierna CA-59 de
REQ-007»). Es decir: un REQ manda el agregado al otro, y el otro exige una igualdad que el primero
hace imposible. El código está bien; los dos criterios no pueden cumplirse a la vez.

**(b) El reloj.** n = 200 con warm-up de 20, base y candidata **en la misma tanda y en la misma
máquina**, como el criterio manda, repetido **tres** veces:

| Repetición | `ls -la` base → cand (cociente) | `npm run build` base → cand (cociente) |
|---|---|---|
| 1 | 17,1 → 21,8 ms (**1,27×**) | 17,2 → 22,1 ms (**1,28×**) |
| 2 | 17,5 → 21,9 ms (**1,25×**) | 17,8 → 21,9 ms (**1,23×**) |
| 3 | 17,1 → 21,9 ms (**1,28×**) | 17,2 → 22,2 ms (**1,29×**) |
| (`git status`, rep. 1) | 17,2 → 26,6 ms (**1,54×**) | — |

El umbral de 1,25× se cumple en **una** de tres repeticiones y se incumple en las otras dos, en una
máquina ociosa: **no es concluyente, y tirando a incumplido**. La causa es estructural y no de
carga: lo que la candidata añade es un **delta constante** (~4,7 ms de trabajo de shell — un archivo
más que `source`ar y un programa `jq` mayor), y un **cociente** castiga tanto más cuanto **más
rápida** sea la base. Relativizar el umbral arregló el problema de QA-112 (medir la máquina), pero
25 % sobre una base de 17 ms es 4,3 ms: menos que el delta que el propio criterio acepta como
correcto.

**Qué no es esto.** No es una suspensión de la candidata: el propio CA-59 dice que el conteo de
procesos «es la magnitud que gobierna este criterio — el reloj sólo la acompaña, y **por sí solo no
aprueba ni suspende nada**». Y el conteo se cumple en las dos entradas que no mencionan `git`.

**Write-back que pido:** (1) que la igualdad de procesos se enuncie sobre las entradas que **no**
mencionan `git` —o que `git status` lleve su techo propio, `≤ base + 1`, con la referencia a
REQ-005 CA-27—; y (2) que el margen del reloj se exprese como **cociente con suelo absoluto** (por
ejemplo «≤ 1,25× **o** ≤ base + 6 ms, lo que sea mayor»), porque un delta constante y un cociente no
miden lo mismo. Mientras tanto dejo las seis cifras de arriba para que nadie las tenga que repetir.

REQ-007 no cierra en esta ventana (su CA-33 exige las **dos** publicadas), así que este hallazgo no
detiene nada; entra en su `Hallazgos abiertos:` con clase.

## 7. Estado de los hallazgos vivos

| Hallazgo | REQ | Clase | Estado tras `430f4fd` |
|---|---|---|---|
| QA-101 | REQ-005 | contrato | cerrado (vuelta 2) |
| QA-102 | REQ-004 | contrato | cerrado (vuelta 2) |
| QA-103 | REQ-007 | instrumento | **cerrado**: motivo con la cifra + criterio en CA-55 («el motivo sigue imprimiendo el techo vigente en bytes») |
| QA-104 | REQ-007 | contrato | **cerrado**: conducta medida (§6a) + criterio **CA-59** |
| QA-105 | REQ-007 | instrumento | **cerrado**: conducta medida (vuelta 2) + criterio **CA-60** |
| QA-106 / QA-107 | REQ-002 / REQ-007 | instrumento | **cerrados**: conducta medida (vuelta 2) + criterio **CA-61**; casos del banco en verde |
| QA-108 | REQ-010 | instrumento | **abierto** (write-back del analista; no se re-mide) |
| QA-109 | REQ-004 | instrumento | conducta **implementada y medida** (§5); **abierto** hasta su criterio |
| QA-110 | REQ-005 | contrato | **cerrado**: CA-21 reescrito y verificado contra el código (§3) |
| QA-111 | REQ-007 (asignado aquí) | instrumento | **cerrado** (§4) |
| QA-112 | REQ-007 | instrumento | **cerrado** por el write-back de CA-59 (umbral relativo); su residual se abre como **QA-114** |
| **QA-113** | REQ-005 | **instrumento** | **abierto** (§6) |
| **QA-114** | REQ-007 | **contrato** | **abierto** (§6) |

`Hallazgos abiertos:` queda así, con clase en todos —un hallazgo sin clase hace que
`guard-completado` deniegue el cierre—:

- **REQ-002:** `QA-106 (instrumento)` → el criterio existe (CA-61) y la conducta está medida; lo
  dejo **como está** porque no es mi campo esta vuelta y el analista puede querer cerrarlo junto
  con REQ-007. Si nadie lo toca, no bloquea.
- **REQ-004:** `QA-109 (instrumento, conducta implementada y medida el 2026-09-06; falta su
  criterio en el REQ — dueño analista-requerimientos)`.
- **REQ-005:** `QA-113 (instrumento, dueño analista-requerimientos; el control de CA-21 generaliza
  de más sobre el descuento de comillas)`.
- **REQ-007:** `QA-114 (contrato, dueño analista-requerimientos; CA-59 exige a git status un
  conteo que REQ-005 CA-26/CA-27 declara imposible, y su margen de reloj no discrimina)` — los
  cuatro que figuraban (QA-103, QA-104, QA-105, QA-107) **están cerrados con criterio escrito** y
  salen del campo.
- **REQ-010:** `QA-108 (instrumento)`, sin cambio.

## 8. Datos, no hallazgos

1. **El token `-` a secas.** Con una regla propia `checkout -`, `git checkout -- -` sale **DENY**.
   Leído al pie de la letra, CA-21 dice que un token que «empieza por `-`» es una **opción** y deja
   de buscarse tras el `--`, así que pediría ALLOW. El código lo trata como **literal**, y en git
   tiene razón: `-` no es una opción, es un operando (`git checkout -` es «la rama anterior»).
   Falla **cerrado**, la regla no está en la lista por defecto y nadie la escribe hoy. Si el
   analista toca CA-21 por QA-113, una cláusula de cinco palabras («el `-` solo es literal») lo
   deja exacto.
2. **`-x` en la tabla de CA-21.2.** La tabla, ahora cerrada («declara **exactamente**»), enumera
   `-x` sin forma larga que le corresponda, y el motor no tiene ninguna entrada para él. No cambia
   nada —`clean -x` casa por letras sin necesitar la tabla, medido— pero una lista que dice
   «exactamente» y trae un elemento de más se lee peor de lo que funciona.
3. **`git clean --force""` y `git clean -f""` → DENY**, mientras `git clean --for"ce"` → ALLOW: el
   descuento borra el par de comillas y lo que queda pegado se re-une o no según dónde caiga la
   comilla. Es el mismo mecanismo de QA-113; lo apunto porque explica por qué la familia no es
   uniforme y por qué no basta con «prohibir las comillas».
4. **`git worktree remove --force .` → ALLOW**, igual que en la vuelta 1. Sigue fuera de la lista
   por defecto y el REQ lo declara explícitamente fuera de alcance. No es incumplimiento.

## 9. Rendimiento

Los umbrales que existen son los de los propios REQ y están medidos arriba: conteo de procesos y
reloj de CA-59 (§6), el `≤ 1` lectura de manifiesto y `< 200 ms` de REQ-005 CA-27 (medido: 26,6 ms
por invocación **completa** con `git status`, muy por debajo), y los cronometrados del banco, que
ahora pasan con techo holgado y reintento. `AGENTS.md` no fija umbral de concurrencia ni de latencia
de servicio para este proyecto —no hay servicio, son hooks—, así que no hay prueba de carga que
rendir.

**Lo que sigo sin poder declarar cumplido, por tercera vuelta:**

- El coste en **Windows/MSYS**, la plataforma del `desarrollador`, donde `AGENTS.md` §2 cifra el
  fork en 1,2–6 s. Todo lo de este informe es Linux/WSL2. El camino común no añade **ningún**
  proceso, así que el riesgo es menor que en la vuelta 1; pero los ~4,7 ms de shell y el `+1`
  proceso de los comandos de `git` no están medidos allí.
- El **banco completo en Windows**, que `AGENTS.md` §7 exige al menos una vez antes de pedir la
  fusión. **No consta** que se haya hecho para esta candidata.

Los dos son condición de `AGENTS.md` para pedir la fusión, no de mi veredicto; los dejo escritos
para que el gate humano los vea.

## 10. Cola de aprobaciones y seguridad

`PENDING_APPROVAL.md` sigue **sin entradas** bajo `## Pendientes` (0, leído con la misma función que
la puerta). Los siete REQ son `Sensible a seguridad: sí` y `Rigor: critico`: ninguno cierra sin
`Seguridad: aprobado`, y el `auditor-seguridad` no puede firmar antes que QA (`AGENTS.md` §6). Por
eso emito el veredicto `QA:` ahora y **no** toco ningún `Estado:`: el cierre no me corresponde.

## 11. Veredicto de la vuelta

| REQ | Veredicto | Hallazgos abiertos |
|---|---|---|
| **REQ-005** | **QA: aprobado** | QA-113 (instrumento) — no bloquea |
| REQ-004 | aprobado (sin cambio) | QA-109 (instrumento) |
| REQ-002, 003, 006, 009, 010 | aprobados (sin cambio) | QA-106, QA-108 (instrumento) |
| REQ-007 | `QA: pendiente` (parcial 1.31.0) | QA-114 (contrato) |

**REQ-005 queda aprobado.** El código cumple el CA-21 reescrito en los 24 comandos de §3, los
cuatro bordes que el criterio no enumeraba salen correctos y ninguno afloja la puerta, y los 33
controles ALLOW de las vueltas anteriores siguen sin un solo falso positivo. Lo único abierto es
`instrumento`: una frase de CA-21 que promete más de lo que el descuento de comillas —compartido,
declarado y con su arreglo ya asignado a REQ-007 Bloque C en 1.32.0— puede sostener.

**Ningún veredicto de los REQ ya aprobados se movió:** el banco corre entero, 0 FAIL, y los 10 casos
que añado no tocan ninguna otra sección.

**La ventana queda lista para el `auditor-seguridad`.**

**Vueltas gastadas (tope 3, por REQ, sin reinicio por hallazgo):** REQ-005 **3 de 3** — termina
**aprobado**, no agotado.

---

# Vuelta 4 (extra, autorizada) — Verificación del arreglo del veto — 2026-09-06

`qa-tester` (Opus, autoalojamiento) · rama `cand/1.31.0-mecanismos`, HEAD `a90d7af` ·
guardián y línea base de no-regresión: **1.30.3 publicada** (`6c1b58a`), tomada de la
**instalación estable** (`~/.claude/plugins/marketplaces/arnes-juan`) · línea base del
fail-before: la candidata **`79ff767`** de esta misma ventana · Linux (WSL2).

**Alcance, acotado por el encargo:** el archivo del usuario (`docs/ESTADO.md`), **SEC-009**,
**SEC-010**, **SEC-011** y la no-regresión del detector de escrituras. No se revalidan los
criterios ya rendidos en las vueltas 1 a 3 salvo como control.

**Método.** Sondas propias contra `hooks/guard.sh` —el despachador real, no los guardianes
sueltos—, con **canario positivo y negativo antes de cada tanda** (`git clean -fd` → DENY,
`ls -la` → ALLOW), `timeout 30` por sonda y `[ -z "$out" ] && dec=allow` **antes** de parsear:
el hook permite con salida vacía. Todo lo de git se probó **sólo a través del hook**, en
proyectos desechables bajo el scratchpad; **ninguna orden destructiva se ejecutó** contra este
worktree. El archivo del usuario se verifica por **`md5sum` antes y después**, nunca por
«contiene el bloque».

## 1. Quality gates

| Puerta | Resultado |
|---|---|
| `bash -n` de `hooks/*.sh` y `tools/*.sh` | **verde** (rc=0) |
| `jq -e . hooks/hooks.json` | **verde** |
| `jq -e . .claude-plugin/plugin.json` y `marketplace.json` | **verde** (versión `1.31.0`) |
| `bash -n tests/escenarios/hooks/run.sh` (con mis casos ya dentro) | **verde** |

## 2. Banco — cifras propias, con cuadre

`CASOS_ESPERADOS` pasa de **669** a **680**: 11 casos nuevos míos (§7).

| Corrida | PASS | FAIL | SKIP | Total | Cuadre |
|---|---|---|---|---|---|
| Candidata `a90d7af` — 1.ª | **679** | **0** | 1 | 680 | ✔ |
| Candidata `a90d7af` — 2.ª | **679** | **0** | 1 | 680 | ✔ |
| `79ff767` (fail-before) — 1.ª | 652 | **27** | 1 | 680 | ✔ |
| 1.30.3 publicada — 1.ª | 492 | 187 | 1 | 680 | ✔ |
| 1.30.3 publicada — 2.ª | 492 | 187 | 1 | 680 | ✔ |

- Contra `79ff767`: **27 FAIL, todos `esperado=deny got=allow`** o de aviso/hash ausente, y
  **cero** transiciones `deny → allow`. Los 27 son exactamente los arreglos de esta vuelta.
- Contra 1.30.3 publicada: **96** `deny` ganados y **exactamente 1** transición
  `deny → allow` — `REQ-007 CA-40 QA-014 '(instrumento, dueño REQ-007)'`, la declarada.
  Sigue siendo la única de la ventana.
- Antes de añadir mis casos medí las cifras del encargo sobre el banco de 669:
  candidata **668 / 0 / 1** (dos veces, idénticas) — coincide con lo entregado.

**Aviso de método, para quien repita esto (dato, no hallazgo).** Mi primera medición de la
línea base dio **472 / 196 / 1** en vez de los 483 / 185 / 1 del encargo. La causa no era el
código: extraje 1.30.3 con `git archive 6c1b58a hooks`, y el banco deriva la ruta de
`tools/arnes-lectura.sh` de `HOOKS_DIR/..`. Con un árbol **sólo de hooks**, 11 casos salen
`rc=127` y cuentan como FAIL por una razón que no es la que se quiere medir. Con el árbol
**completo** (o con la instalación estable) sale **483 / 185 / 1**, la cifra del encargo, y
los ficheros de `hooks/` son byte a byte los mismos (`diff -rq` vacío). **Una comparación
contra una copia parcial no es comparable**; conviene decirlo en el banco.

## 3. El archivo del usuario — los cuatro caminos, por hash

Verificado con `md5sum` antes y después, con texto humano real (líneas con acentos) fuera de
los marcadores. Pareja fail-before (`79ff767`) / pass-after (`a90d7af`):

| Camino | `79ff767` | `a90d7af` | Evidencia |
|---|---|---|---|
| Manifiesto **vacío** (0 bytes) | `stop.sh` **rc=1** (moría), sin bloque | **rc=0**, deriva, línea de degradado, texto humano íntegro | hash 201 B intacto en las líneas humanas |
| Manifiesto **`null`** | rc=0 pero derivaba **como si todo sano**, sin señal | rc=0, línea «enforcement degradado» + aviso por `stderr` | `grep` de la línea = 1 |
| **NUL** en `docs/ESTADO.md` | **hash cambia** `f556300…` → `c72a7fc…`, 201 B → 1144 B, y **`DESPUES-DEL-NUL` desaparece** (`grep -c` = 0) | **hash idéntico**, avisa «byte NUL», 0 temporales | pérdida real reproducida |
| ESTADO.md **sin permiso de lectura** | **hash cambia**, 979 B, y `TEXTO-HUMANO-DOS` **desaparece** (`grep -c` = 0) | **hash idéntico**, avisa, 0 temporales | pérdida real reproducida |

Los dos últimos **destruían de verdad** contenido escrito por una persona; lo comprobé por
ausencia de la cadena, no sólo por hash.

### Mis propias averías (todas contra `a90d7af`)

| Avería | Resultado | Hash | Temporales |
|---|---|---|---|
| `docs/` sin permiso de escritura (`chmod 500`) | rc=0, avisa «no se pudo escribir» | **idéntico** | 0 |
| Ruta del temporal ocupada por un **directorio** | rc=0, avisa | **idéntico** | 0 |
| **Disco lleno de verdad** (temporal → `/dev/full`, ENOSPC real) | rc=0, avisa | **idéntico** | **0** |
| `docs/ESTADO.md` es un **enlace simbólico** | rc=0; el destino real **conserva su texto** y no se toca | destino intacto | 0 |
| Fichero de **sólo lectura** (444, carpeta escribible) | rc=0, texto humano íntegro | **cambia** (ver QA-116) | 0 |
| **CRLF** (4 líneas con CR) | rc=0, las 4 CR se conservan; 2.ª parada sigue con **un** bloque | — | 0 |
| **Concurrencia**: 4 paradas a la vez × 10 rondas | 0 pérdidas, siempre **un** bloque, 0 basura | — | 0 |
| `jq` ausente del `PATH` | rc=0, aviso propio, no toca nada | **idéntico** | 0 |
| Límite `RLIMIT_FSIZE` (`ulimit -f 1`, SIGXFSZ) | archivo **intacto**, pero rc=153 y **1 temporal huérfano** sin aviso | **idéntico** | **1** → QA-118 |

**Ningún camino perdió un byte escrito por una persona.** El único con basura es el de muerte
por señal (QA-118), y ahí el archivo del usuario tampoco se tocó.

## 4. SEC-009 — **cerrado**

Las **seis** formas de CA-40 en DENY, idénticas para la sesión coordinadora y para el
`desarrollador` (12 sondas), y las cinco de CA-06 **siguen** en DENY (no se perdió nada de lo
que ya se veía). Además dan DENY, de paso, `timeout 30`, `stdbuf -o0`, `!`, `while/do`,
`else`, `elif`, `until/do`, `setsid`, `ionice` y `doas`.

**No ensanchamiento (CA-41), 31 controles, todos ALLOW y ninguno movido:** cadena
entrecomillada, comentario, heredoc citado, `github`/`gitk`/`mygit`/`legit`, `if true; then
git status; fi`, `if true; then echo git clean -fd; fi`, y el conjunto CA-08…CA-16 al
completo. Busqué **falsos positivos** de las palabras reservadas nuevas con 19 comandos
legítimos (`for f in git; do …`, `case git in …`, `function git_helper { git status; }`,
`grep -n "…do git clean -fd…"`, `git log --grep="git clean -fd"`, …): **ninguno denegó**.

### La frontera de la barra invertida — **el límite NO se cerró de rebote**

Es la trampa de la vuelta y sale bien:

| Forma | Veredicto | Debía ser |
|---|---|---|
| `git clean \-f` (barra **sin** salto) | **ALLOW** | ALLOW — LIM-10 vivo |
| `git clean \-fd`, `git reset \--hard` | **ALLOW** | ALLOW — LIM-10 vivo |
| `git clean` + `\` + salto + `-fd` | **DENY** | DENY — CA-40(vi) |
| `git clean` + `\` + **espacio** + salto | **ALLOW** | ALLOW (en bash la barra escapa el espacio; no es continuación) |
| `git` + salto **sin** barra + `clean -fd` | **ALLOW** | ALLOW (son dos órdenes de verdad) |
| `if true; then git clean \`+salto+`-fd; fi` | **DENY** | DENY (las dos correcciones componen) |

El plegado sustituye **exactamente** `\`+salto y nada más. LIM-10 sigue abierto, declarado y
con su ventana en 1.32.0, como CA-41 exige.

## 5. SEC-010 — **cerrado**

Ocho formas de manifiesto ilegible probadas (roto, vacío, `null`, `[]`, cadena, número,
booleano, `{"git":"si"}`): **DENY** en las ocho. En modo degradado deniegan además los diez
comandos de la lista por defecto, incluidas las formas de SEC-009. El motivo dice **MODO
DEGRADADO**, da la salida (`jq -e . .arnes/config.json`) y nombra el interruptor explícito
(`git.activo: false`).

**Controles (CA-43), todos correctos:** `git status`, `git log`, `git stash list`,
`git clean -n`, `git diff`, `git add -A`, `git commit -m x` y **`git push --force origin
main`** siguen en **ALLOW** durante la avería — el modo degradado deniega **la lista por
defecto**, no todo git. Con el manifiesto sano vuelve toda la conducta: `activo:false` →
ALLOW (5 comandos), lista propia `["push --force"]` → `clean -fd` ALLOW y `push --force`
DENY, `prohibidos: []` → ALLOW, `prohibidos` como cadena en manifiesto válido → DENY.

**Inerte ≠ ilegible, verificado con su control de método:** manifiesto **ausente** → ALLOW;
**sin `jq` en el `PATH`** → salida vacía, rc=0 y el aviso «jq no encontrado; enforcement
inactivo». Repetí esta última porque la primera pasada podía haber salido vacía por no
encontrar `bash`: con `env -i … /bin/bash` (ruta absoluta) y `command -v jq` devolviendo 1 en
el mismo entorno, la medición es válida, y la misma llamada **con** `jq` deniega.

## 6. SEC-011 — **cerrado** (las dos piezas de código; la tercera es del analista y ya existe)

1. **Aviso propio de la reparación:** emitido **una sola vez por llamada**, nombra el archivo,
   la herramienta (`Write`) y el `agent_type`, para los cinco emisores probados —coordinadora
   (sale «sesion coordinadora»), `qa-tester`, `auditor-seguridad`,
   `analista-requerimientos`, `desarrollador`—. **Control:** otra ruta durante la avería sigue
   **DENY** y **sin** ese aviso (0 apariciones); con el manifiesto sano y `.arnes/*` en los
   globs, la escritura vuelve a **DENY** y no hay aviso.
2. **La avería se ve en el bloque derivado:** línea «Manifiesto ilegible: enforcement
   degradado» presente en las cuatro formas de manifiesto ilegible; ausente con el sano.
3. **Los tres límites de CA-60 intactos:** `printf "{}" > .arnes/config.json; echo x >
   docs/n.md` → **DENY**; sólo la reparación → **ALLOW**.
4. El write-back del residual existe: **REQ-007 CA-63.3** enumera lo que queda sin gobernar y
   **cita** a REQ-005 CA-42 en vez de duplicarlo.

## 7. No-regresión del detector de escrituras — comprobado por mí

24 sondas idénticas contra **las tres** versiones (candidata `a90d7af`, `79ff767` y **1.30.3
publicada**): **24 PASS / 0 FAIL en las tres**, sin una sola diferencia.

- Los **ocho mecanismos** de escritura sobre código protegido (`>`, `>>`, `tee`, `cp`, `mv`,
  `install`, `sed -i`, `dd of=`) → **DENY** para la coordinadora en las tres.
- Las **ocho formas de CA-41** (las seis envolturas de SEC-009 más `if`/`for` con
  redirección, más `nohup cp` y `for … tee`) → **DENY** en las tres.
- Controles: el `desarrollador` **sí** puede (`>` y `if/then`), fuera de los globs cualquiera
  puede, y por `Write` la coordinadora y el `qa-tester` reciben DENY mientras el
  `desarrollador` recibe ALLOW.

**La afirmación del desarrollador se confirma con medición propia: el detector de escrituras
no está afectado.**

## 8. Los seis REQ ya firmados por QA y seguridad — sin movimiento

REQ-002, REQ-003, REQ-004, REQ-006, REQ-009 y REQ-010 no cambian de veredicto: el banco corre
entero con **0 FAIL**, los 11 casos que añado viven en secciones de REQ-005 y REQ-007, y la
única transición `deny → allow` de la ventana contra la publicada sigue siendo la declarada
(REQ-007 CA-40). Los `Hallazgos abiertos` de esos REQ (QA-106, QA-108, QA-109 — todos
`instrumento`) siguen como estaban.

## 9. Hallazgos nuevos de esta vuelta

### QA-115 — el código ve **más** de lo que el REQ declara, y una de esas formas está nombrada en «Fuera de alcance»

- **Clase:** `contrato` · **Severidad:** media · **REQ:** REQ-005 (CA-40(c) y «Fuera de
  alcance») · **Dueño:** `analista-requerimientos` (write-back) — **no requiere cambio de
  código y no consume vuelta dev↔QA**

**Reproducción** (proyecto desechable, manifiesto base, canarios en la misma tanda):

```
git clean -fd            DENY (canario+)     ls -la              ALLOW (canario-)
xargs git clean -fd      DENY                echo . | xargs git clean -fd   DENY
setsid git clean -fd     DENY                ionice -c3 git clean -fd       DENY
doas git clean -fd       DENY
```

**Esperado según el REQ vigente:** «Fuera de alcance» de REQ-005 dice, con nombre y apellido,
que «`xargs git clean -fd` y `find . -exec git clean -fd \;` (el comando viaja como
**argumento** de otro programa)» son sintaxis que **sigue sin verse** después de CA-40, y
CA-40(c) enumera como envoltorios tolerados exactamente **tres**: `nohup`, `timeout` y
`stdbuf`. **Observado:** `hooks/guard-git.sh` tolera **siete** (`nohup`, `setsid`, `stdbuf`,
`doas`, `xargs`, `timeout`, `ionice`) y **catorce** palabras reservadas frente a las seis de
CA-40(c) — así que `xargs git clean -fd` **deniega**. (`find . -exec …`, `eval` y `bash -c`
sí siguen en ALLOW, como el REQ dice.)

**Por qué es `contrato` y no `instrumento`:** el defecto no está en la prueba, está en que el
**REQ afirma algo falso sobre lo construido**. Y es exactamente la simetría de lo que CA-41
pide vigilar en LIM-10: allí se exige que un límite declarado no se cierre «por accidente y en
silencio»; aquí un límite declarado **se cerró** en silencio. La dirección es segura
(`allow → deny`, más protección, ningún falso positivo medido en 19 comandos legítimos), y por
eso no pido revertir nada.

**Salida (cualquiera de las dos, una línea):** que el analista retire `xargs` de «Fuera de
alcance» y amplíe la lista de CA-40(c) a los siete envoltorios y las catorce reservadas que el
código tolera; **o** que el desarrollador retire `xargs`, `ionice`, `setsid` y `doas` del
bucle. Dejo el caso `QA-115` en el banco midiendo la **realidad** (DENY), con su control
`xargs -n1 git status` → ALLOW, para que el desfase no se vuelva a olvidar.

### QA-116 — CA-64.2 promete «hash idéntico» para un caso en que el bloque **sí** se escribe

- **Clase:** `contrato` · **Severidad:** baja · **REQ:** REQ-007 (CA-64.2) · **Dueño:**
  `analista-requerimientos` · **No bloquea REQ-005** (REQ-007 está `en-progreso`, `QA: pendiente`)

**Reproducción:** `docs/ESTADO.md` con texto humano y `chmod 444`, carpeta escribible;
`Stop`.
**Esperado (CA-64.2):** «en las cuatro [causas] … `md5sum` del archivo antes y después
coincide», y una de las cuatro es «`docs/ESTADO.md` sin permiso de escritura».
**Observado:** rc=0, el bloque **se escribe**, el hash **cambia** y el modo pasa de **444 a
644** en silencio. El texto humano queda íntegro (comprobado por `grep`), así que **no hay
pérdida**.
**Causa:** en POSIX el permiso del fichero no protege frente a su sustitución si el
**directorio** es escribible; `mv -f` reemplaza el inodo y el nuevo hereda el umask. El
criterio dio por hecho que «sin permiso de escritura» implica «no se puede derivar», y no es
así. **Heredado, no regresión:** 1.30.3 publicada hace exactamente lo mismo (medido).
**Salida:** que CA-64.2 sustituya esa causa por una que sí impida escribir (carpeta sin
permiso, ya cubierta; ENOSPC, que añado en §10) y, si se quiere conservar el modo, que se
declare como criterio propio.

### QA-117 — «lo de fuera de los marcadores queda idéntico byte a byte» no es cierto del **orden**

- **Clase:** `contrato` · **Severidad:** baja · **REQ:** REQ-007 (CA-64.1) · **Dueño:**
  `analista-requerimientos` (+ `desarrollador` si se decide conservar el orden) · **No bloquea
  REQ-005**

**Reproducción:** `docs/ESTADO.md` con `HUMANO-ANTES-1/2`, los marcadores, y
`HUMANO-DESPUES-1/2`; `Stop`.
**Esperado (CA-64.1):** «lo de fuera queda **idéntico byte a byte** y sólo cambia lo de
dentro».
**Observado:** las cuatro líneas humanas **sobreviven** (0 pérdidas), pero las dos que estaban
**después** de los marcadores aparecen **antes**: el archivo queda `ANTES-1, ANTES-2,
DESPUES-1, DESPUES-2, <bloque>`. El bloque se reubica siempre al final. En cada parada el
texto de abajo sube.
**Causa:** la reconstrucción compone `"$fuera\n$cuerpo"`, y `fuera` es la concatenación de las
líneas de fuera **sin recordar su posición** respecto del bloque.
**Heredado, no regresión:** 1.30.3 publicada hace lo mismo (medido: `ANTES`, `DESPUES`,
`<bloque>`).
**Salida:** o CA-64.1 se acota a «no se pierde ninguna línea de fuera» —que es lo que la
máquina sostiene—, o el hook conserva la posición. Lo primero es una línea; lo segundo, un
cambio de 1.32.0.

### QA-118 — muerto por señal a mitad de la publicación: el archivo aguanta, el temporal se queda

- **Clase:** `instrumento` · **Severidad:** baja · **Dueño:** `desarrollador`, ventana
  **1.32.0** · **No bloquea**

**Reproducción:** `( ulimit -f 1; … | stop.sh )` con un `docs/ESTADO.md` de ~60 líneas. El
`printf` al temporal supera `RLIMIT_FSIZE` y el proceso recibe **SIGXFSZ**.
**Observado:** el archivo del usuario queda **byte a byte** (lo que importa), pero `stop.sh`
sale **153** (128+25) y deja `docs/ESTADO.md.arnes.tmp` de 1024 B **sin ningún aviso** —el
`rm -f` y el `arnes_warn` viven después del `&&` y nunca se ejecutan.
**Por qué es `instrumento` y no bloquea:** el producto (el archivo de la persona) está
intacto; lo dañado es la limpieza del propio arnés y la promesa de `AGENTS.md` §13 de que la
parada nunca sale mal. Y una muerte por señal deja basura en cualquier programa sin `trap`.
Con **ENOSPC de verdad** (temporal → `/dev/full`) el camino es impecable: rc=0, aviso propio,
cero temporales — o sea, el camino que se sufre en producción está bien resuelto.
**Salida barata:** `trap 'rm -f "$tmp"' EXIT INT TERM XFSZ` alrededor de la publicación.

### QA-119 — un caso del banco es **flaky** por reloj (segunda instancia de la familia de QA-111)

- **Clase:** `instrumento` · **Severidad:** baja · **Dueño:** `desarrollador` · **No bloquea**

**Caso:** `CA-09 idempotente: el bloque no cambia ni la celda gana otra elipsis`.
**Observado:** **1 fallo en 9 corridas completas** del banco, siempre en la línea base
(`esperado=iguales obtenido=distintos`), y **0 fallos en 20+20** corridas filtradas
(candidata y 1.30.3). Dos corridas consecutivas del **mismo** árbol dieron **483/185/1** y
**482/186/1**.
**Causa:** el bloque estampa `## Estado derivado — $(date '+%Y-%m-%d %H:%M')`
(`hooks/estado-derivado.sh:165`) y el caso compara las dos pasadas con `cmp` byte a byte. Si
las dos derivaciones caen a distinto lado de un **cambio de minuto**, el sello cambia y el
caso falla. La probabilidad es ≈ (separación entre las dos pasadas)/60 s.
**Por qué importa:** no es que el caso mienta sobre el producto —la idempotencia es real—,
es que **la cifra del banco no es determinista** y dos verificaciones honestas del mismo árbol
pueden diferir en 1. Repórtese como flaky, no como pase ni como fallo.
**Salida:** comparar excluyendo la línea del sello, o fijar el sello con una variable de
entorno durante el banco.

## 10. Casos que añado al banco (11) — `# QA 1.31.0 v4:`

`CASOS_ESPERADOS`: **669 → 680**.

| # | Caso | Cand. | `79ff767` | 1.30.3 |
|---|---|---|---|---|
| 1 | CA-64.2 disco lleno (ENOSPC vía `/dev/full`): byte a byte, 0 temporales, avisa, sale 0 | PASS | **FAIL** | **FAIL** |
| 2 | CA-64.2 cuatro paradas a la vez × 5: ni se pierde, ni se duplica, ni deja basura | PASS | PASS | PASS |
| 3 | SEC-009 frontera `\`+**espacio**+salto NO es continuación → allow | PASS | PASS | PASS |
| 4 | SEC-009 frontera salto **sin** barra son dos órdenes → allow | PASS | PASS | PASS |
| 5 | SEC-009 control `for f in git; do echo $f; done` → allow | PASS | PASS | PASS |
| 6 | SEC-009 control `case git in *) echo x;; esac` → allow | PASS | PASS | PASS |
| 7 | SEC-009 control `function git_helper { git status; }` → allow | PASS | PASS | PASS |
| 8 | SEC-009 control CA-41(b) `echo hola # git clean -fd` → allow | PASS | PASS | PASS |
| 9 | **QA-115** `xargs git clean -fd` → deny (el REQ lo declara fuera de alcance) | PASS | **FAIL** | **FAIL** |
| 10 | QA-115 control `xargs -n1 git status` → allow | PASS | PASS | PASS |
| 11 | SEC-010 control degradado `git push --force origin main` → allow | PASS | PASS | PASS |

Dos con **fail-before** real; los nueve restantes son **guardianes de no ensanchamiento** y de
la frontera de LIM-10: su valor es que un cambio futuro que los mueva grite en el banco.

## 11. Rendimiento

`AGENTS.md` no fija umbral de usuarios concurrentes ni latencia para este proyecto; los
umbrales aplicables son los de los REQ (CA-26, CA-27, CA-59), rendidos en vueltas anteriores y
**no re-medidos aquí**. Lo único que mido en esta vuelta es la **concurrencia de paradas**
(§3): 4 procesos simultáneos × 10 rondas, sin pérdida, sin bloque duplicado y sin basura.
El banco completo tarda **~18 s** en esta máquina (Linux/WSL2). **No hay prueba de carga
representativa y no la reclamo como cumplida.**

## 12. Cola de aprobaciones y seguridad

`PENDING_APPROVAL.md` — la cola es cosa de la coordinadora; **no marco ningún `Estado:`** y no
cierro nada. El REQ es `Sensible a seguridad: sí` y `Rigor: critico`, así que su cierre exige
`Seguridad: aprobado` sobre este árbol; mi veredicto es lo que desbloquea al
`auditor-seguridad` (`AGENTS.md` §6).

## 13. Veredicto de la vuelta 4

**REQ-005 → `QA: aprobado`.**

- **SEC-009: cerrado.** Las seis formas en DENY para los dos emisores, CA-06 intacto, 31
  controles de no ensanchamiento sin mover, cero falsos positivos en 19 comandos legítimos, y
  —lo que el encargo pedía mirar de cerca— **LIM-10 sigue vivo**: `git clean \-f` sin salto
  ALLOW, `\`+salto DENY. El límite no se cerró de rebote.
- **SEC-010: cerrado.** Ocho formas de manifiesto ilegible en DENY con la lista del código y
  motivo que dice modo degradado y salida; `git status` y el resto de lo no listado siguen
  pasando; con el manifiesto sano vuelve toda la conducta declarada; ausente y sin `jq`
  siguen **inertes**, que es cosa distinta.
- **SEC-011: cerrado** en sus dos piezas de código, con el write-back ya escrito en REQ-007.
- **El archivo del usuario: cerrado.** Los cuatro caminos verificados **por hash**, con la
  pérdida real reproducida en `79ff767`, más nueve averías propias. **Ninguna perdió un byte
  escrito por una persona.**
- **Sin regresión:** 0 FAIL en el banco, 0 transiciones `deny → allow` contra `79ff767`, una
  sola —la declarada— contra 1.30.3 publicada, y el detector de escrituras idéntico en las
  tres versiones sobre 24 sondas.

**Lo que mi aprobación NO hace: no cierra el REQ.** Queda abierto **QA-115** (`contrato`), que
por `AGENTS.md` §6 impide `Estado: completado` hasta el write-back. Es deliberado y es la vía
correcta: el código está bien y no se toca —por eso apruebo—, lo que está desfasado es el
texto de «Fuera de alcance» y CA-40(c), y eso lo arregla el `analista-requerimientos` con una
línea, **sin gastar vuelta dev↔QA**. Aprobar el código y bloquear el cierre hasta que el REQ
lo describa es exactamente lo que §9 pide para una **deriva**.

**Vueltas gastadas (tope 3 por REQ, sin reinicio por hallazgo):** REQ-005 sigue en **3 de 3**;
esta vuelta 4 fue autorizada como excepción por la coordinadora con delegación del propietario
y **no** consume contador. No pido ninguna vuelta más: no hay nada pendiente del
`desarrollador` en REQ-005.
