# QA — REQ-025, entrega 1 (coordinación orientada a entregas)

**Veredicto: `con-hallazgos`.** Vuelta **1 de 3** del ciclo dev↔QA de esta entrega.

| | |
|---|---|
| Árbol validado | `567eb38` (rama `feat/req-025-coordinacion-entregas`, worktree `/home/juan/dev/ArnesJuan-req025`, árbol limpio) |
| Delta revisado | `git diff 1de2b26 567eb38` — 14 archivos |
| Aplicación bajo revisión | `5162126` (+ `4df2b17`, sólo hashes de histórico) |
| Fecha del veredicto | 2026-09-21 |
| Ronda | R-1 (vuelta 1 de 3 del ciclo dev↔QA de la entrega 1; el contador es **de la entrega**, CA-14 punto 6) |
| Hallazgos abiertos | **4 `contrato`** (QA-025-01…04) · **2 `instrumento`** (QA-025-05, QA-025-06) |

---

## 0. Forma de los criterios (se comprueba ANTES de probar)

`requirements/README.md` §«Cómo se escribe un criterio que no se desmiente» proscribe tres formas:
**enumeración** sin marca ni puntero, **número** sin declarar si es operativo o de contrato, e
**igualdad** donde corresponde un techo. Revisados uno a uno los criterios que me aplican:

| Criterio | Forma | Resultado |
|---|---|---|
| CA-01 | — | Sin cifras. Los tres niveles son una partición declarada, no una enumeración de un conjunto que el código reconozca. **Conforme** |
| CA-02 | `número` | `5` · `5` · `0` · `2` declarados **operativos**, con corrida (2026-09-07/08), corpus (`docs/PENDIENTES.md`) y dirección admitida. Las instancias se declaran **no exhaustivas** con puntero al sitio único. **Conforme** |
| CA-09 (B) pt 5 | `enumeración` | El vocabulario de cuatro clases se declara **cerrado a propósito** y con sede única — que es la forma legítima de cerrar un conjunto. **Conforme en la forma.** Su defecto es de **contenido** (QA-025-02), no de forma |
| CA-10 reglas 5 y 6 | `número` | Cita el tope de 3 vueltas y **no lo redeclara**. **Conforme** |
| CA-14 pts 5 y 6 | `número` | Íd. **Conforme** |
| CA-15 pt 5 | `número` | «**no más de 0** archivos fuera» declarado **de contrato**, con quién elige su conducta por ese valor. **Conforme** |
| CA-11 pt 5 | `número` | Forzador **operativo** con dirección admitida hacia abajo y vencimiento. **Conforme** |
| CA-13 | `igualdad` | «no hay ningún FAIL» / «`CASOS_ESPERADOS` coincide con `PASS+FAIL+SKIP`» son puertas binarias, no criterios de coste: la igualdad es la propiedad, no un techo mal puesto. **Conforme** |

**Ningún hallazgo de forma.** Los cuatro `contrato` de abajo son de contenido y de contradicción.

---

## 1. Criterios, uno por uno

| Criterio | Resultado | Evidencia |
|---|---|---|
| **CA-01** — tesis y tres niveles escritos en la sede | **FALLA** | Ver QA-025-01. Nada de CA-01 está escrito en `AGENTS.md` ni en su gemela |
| **CA-02** — las dos clases dominantes y la dirección del fallo | **FALLA** | Ver QA-025-01 |
| **CA-09 (A)** — lo que todo encargo declara | **PASA** | `AGENTS.md:411-417` (regla 1): resultado construido y comprobado · criterios por identificador · qué queda fuera y cuándo detenerse · «documentar o revisar es avance cuando sirve a esa entrega y no la sustituye» |
| **CA-09 (B) pt 3** — decisiones humanas temprano y con su forma | **PASA** | `AGENTS.md:441-450` (regla 4): momento, (i)…(iv), agrupación, y las tres condiciones del trabajo que sigue permitido |
| **CA-09 (B) pt 5** — bloqueo con alcance, con la precisión (a) | **FALLA** | `AGENTS.md:418-428` escribe (i)…(iv) y la transición exacta, pero deja el **bloqueo mecánico sin clase**: ver QA-025-02 |
| **CA-09 (B) pts 1, 2 y 4**, propiedad y caso medido de cada pregunta, y la regla de `git add -A` | **FALLA** | Ausentes de la sede. Ver QA-025-01 |
| **CA-10** — regla 5 (presupuesto del ciclo) | **PASA** | `AGENTS.md:451-460`: seguimiento de la entrega a través de todos los roles · «cambiar de agente, de fase o de nombre no reinicia ningún límite» · cita el tope sin redeclararlo · «agotado no se aprueba por agotamiento» con las dos alternativas de «Loop de error» |
| **CA-10** — regla 6 (avance observable) | **PASA con reserva** | `AGENTS.md:461-467`: una línea, resultado obtenido, dependencia que falta, y por qué es necesaria la comisión siguiente; prohibición nombrada de fabricar progreso. **Reserva:** «el libro de comisiones» no está definido en ninguna parte de `AGENTS.md` (única aparición previa: `:406`), y en la gemela un proyecto nuevo no tiene tal libro — ver QA-025-06 |
| **CA-10** — primera mitad (el encargo deriva el estado y no lo recuerda) | **FALLA** | Ausente de la sede. Ver QA-025-01 |
| **CA-13** — puertas comunes | **FALLA** | Gates 3/3 en verde; **el banco completo reporta 1 FAIL reproducible**. Ver QA-025-05 |
| **CA-14 pt 1** — lo que NO cambia, primero | **PASA** | `AGENTS.md:358-363` abre «Loop de error» con las facultades conservadas y el veto, antes de cualquier otra cosa; repetido como apertura de la regla 3 (`:431-435`) |
| **CA-14 pt 2** — la clasificación y qué decide exactamente | **PASA** | `AGENTS.md:363-371` |
| **CA-14 pt 3** — la urgencia de seguridad se escala | **PASA** | `AGENTS.md:437-440` |
| **CA-14 pt 4** — los dos ejes no se renombran | **PASA** | `AGENTS.md:440-446`, con la sede única de las tres clases citada |
| **CA-14 pt 5** — discrepancia; mover de sitio no permite cerrar | **PASA** | `AGENTS.md:372-377`, incluida «**La ubicación no es un veredicto.**» Presente también, correctamente enrutada, en los dos agentes |
| **CA-14 pt 6** — el contador es del defecto o de la entrega | **PASA** | `AGENTS.md:382-387`: «ni por cambio de rol, de fase o de nombre… **ni abriendo un REQ nuevo**», con el reverso de la partición por alcance |
| **CA-15 pt 1** — una sola sede normativa | **FALLA** | Ver QA-025-03 |
| **CA-15 pt 2** — promesa completa, no matizada | **PASA** | «Loop de error» y «Mecanismo de gate» **reescritos**, no matizados: las dos frases absolutas ya no existen. Barrido por propiedad verificado por mí de forma independiente (§2) |
| **CA-15 pt 3** — gemela preparada y declarada no entregada | **PASA con reserva** | Gemelas **exactas** (§3). «Hacia 1.35.0» dice, en su primer párrafo destacado, que no ha llegado a ningún proyecto. **Reserva:** contenido propio de este repositorio en la plantilla — QA-025-06 |
| **CA-15 pt 4** — referencias en los agentes, no copias; nadie pierde facultades | **FALLA (la mitad «no copias»)** | Ninguna facultad se pierde (§4); pero son **copias**, no referencias — QA-025-03 |
| **CA-15 pt 5** — lo que no se toca; 0 archivos fuera del alcance | **PASA** | `git diff --name-only 1de2b26 567eb38` filtrado contra las rutas admitidas → **0 archivos fuera**. Sin cambios en `hooks/`, `hooks.json`, `.arnes/`, `.github/`, `tests/`, `tools/`, `.claude-plugin/`, ni en el `Rigor:`/`Sensible a seguridad:` de ningún REQ |
| **CA-15 pt 6** — `.arnes/plantillas-origen/` intacto | **PASA** | Ningún archivo de esa carpeta en el delta; sus dos copias conservan el texto antiguo, que es lo correcto |
| **CA-15 pt 7** — el ADR existe **y el REQ lo enlaza** | **FALLA (la mitad del enlace)** | ADR-008 existe y está bien formado (§5); el Historial de REQ-025 sigue diciendo «Pendiente de crear y enlazar aquí». Ver QA-025-04 |
| **CA-15 pt 8** — entrada del `CHANGELOG.md` con fecha real | **PASA** | `CHANGELOG.md:7-8`: `## [GitHub] — 2026-09-21`, con Origen · usuario · modelo · agente · rama · archivos. **Sin** marcadores tipo «FECHA DE APLICACIÓN». Y no declara entregado a ningún consumidor |
| **CA-11 pt 4** (orden) y **pt 5** (fecha y ronda) | **CUMPLO YO** | Este veredicto lleva fecha `2026-09-21` y ronda `R-1`; el `auditor-seguridad` firma después |
| **CA-11 pt 3** — el ensayo S1…S4 | **FUERA DE ESTA COMISIÓN** | Lo lanzará la coordinadora y lo acreditaré en otra comisión. **Hoy no está acreditado** |

---

## 2. Barrido por propiedad — independiente del que hizo el desarrollador

Propiedad (CA-15 punto 2): *toda frase —documento, plantilla, definición de agente o **mensaje que
un programa imprima**— que afirme que un hallazgo devuelve trabajo por sí solo, o que una decisión
pendiente detiene todo el trabajo, sin nombrar la acción que impide.*

**Método:** `grep` recursivo sobre todo el árbol (excluido `.git`) con cuatro familias de patrón
—«pipeline», «detiene/detén/detener», «no continúa / no avanza», «vuelve al / devuelve al»— y
revisión una a una de cada coincidencia. Corpus **ampliado** respecto al del desarrollador:
`requirements/README.md`, `templates/requirements-README.md.tpl`, `README.md`, `hooks/` y `tools/`.

**Resultado — los dos párrafos y las dos cabeceras de la cola quedan corregidos, y no encontré
ninguna sede superviviente de la promesa absoluta.** En detalle:

- **`hooks/` y `tools/`: limpios.** Ningún mensaje que el programa imprima afirma la promesa
  antigua. Al contrario: `hooks/guard-completado.sh:549` ya nombra la transición exacta
  —`"no se puede marcar '$rel' como '$estado_done': hay $abiertas aprobación(es) pendiente(s)"`—
  y `:545` hace lo propio para la cola ilegible. **La máquina decía la verdad antes que el texto**;
  lo que esta entrega arregla es que el texto la alcance. Sin hallazgo.
- **Los seis restos que el desarrollador enumeró: verificados uno a uno, los seis correctos.**
  `AGENTS.md:60`/`:121` (quién decide fusionar, acción nombrada) · `agents/qa-tester.md:43`/`:50`
  (la facultad de **no firmar**, que CA-14 pt 1 conserva) · `agents/auditor-seguridad.md:65` y
  `agents/desarrollador.md:67` (otro sentido de «devolver») · `skills/arnes-upgrade/SKILL.md:785`,
  `:801`, `:897` y `skills/arnes-close/SKILL.md:38` (otro sentido, o acción nombrada) ·
  `AGENTS.md:635` («`ask` detendría la llamada»: modo de hook) ·
  `templates/requirements-README.md.tpl:17` (vocabulario de estados).
- **Los que él no barrió: también limpios.** `requirements/README.md` no contiene ninguna frase con
  la propiedad (sus dos coincidencias, `:225` y `:520`, son «devuelve a la serie» y un título de
  REQ). `templates/requirements-README.md.tpl` íd. `README.md:165` describe el pipeline como
  estructura, no promete detención.
- **`agents/analista-requerimientos.md:41`** —«El REQ en conflicto **no avanza a desarrollo**
  (permanece en `borrador`)»— **nombra la acción y el estado**. Correcto que no se tocara.
- **`PENDING_APPROVAL.md:377`** («El pipeline está detenido…») vive bajo `## Resueltas`: es una
  entrada histórica, no texto normativo. Fuera de la propiedad.

**Una observación que no llega a hallazgo (OBS-A).** `PENDING_APPROVAL.md:33` y su gemela
`templates/PENDING_APPROVAL.md.tpl:33` conservan «un ejemplo dentro de la sección se cuenta como
una pendiente real y **bloquea todos los cierres**». *No* cae en la propiedad —nombra la acción,
«los cierres»—, así que no falsea CA-15 pt 2. Pero es la forma laxa («cerrar») catorce líneas
debajo de una cabecera que la entrega acaba de precisar a «marcar cualquier REQ como
`completado`», en el mismo archivo. Coste de alinearla: una frase. **No bloquea; conviene hacerlo
en la misma vuelta que QA-025-03**, que toca los mismos archivos.

---

## 3. Gemelas

| Par | Resultado | Método |
|---|---|---|
| `AGENTS.md` ↔ `templates/AGENTS.md.tpl` | **IDÉNTICAS** | Comparación de las **líneas cambiadas** de los dos diffs: sin diferencia alguna. Y comparación directa de los dos bloques completos: «Loop de error» (`AGENTS.md:357-380` ↔ `tpl:323-346`) difiere **sólo** en `Máximo **3**` ↔ `Máximo **{{MAX_REINTENTOS}}**`, marcador **preexistente**; el bloque de las seis reglas (`AGENTS.md:404-470` ↔ `tpl:370-436`) es **byte a byte idéntico** |
| `PENDING_APPROVAL.md` ↔ `templates/PENDING_APPROVAL.md.tpl` | **IDÉNTICAS** en lo nuevo | El bloque insertado es idéntico. Las diferencias del resto del archivo (`:26-30`, ruta concreta del lector; y las entradas de `## Resueltas`) son **preexistentes** y propias de la instancia |

**Nada específico de este repositorio en las plantillas: NO se cumple.** Ver QA-025-06.

---

## 4. ¿Pierden facultades QA y seguridad? — leídos como si fuera a trabajar con ellos mañana

**No pierden ninguna de las que CA-14 punto 1 conserva, y eso está bien hecho.** Leí
`agents/qa-tester.md` y `agents/auditor-seguridad.md` completos:

- **`qa-tester`.** Conserva detectar, registrar con clase, clasificar y bloquear; `:87` lo dice con
  todas las letras dos veces («eso es tuyo, no cambia, y tu `con-hallazgos` bloquea igual» ·
  «Ninguna clasificación retira, degrada ni pospone tu veredicto»). `:86` añade «**tú no pierdes
  nada**». Lo que **sí** cambia —que devolver el REQ al desarrollador pasa por la clasificación de
  la coordinadora— es precisamente lo que el propietario autorizó, y está enrutado, no suprimido.
  La discrepancia y la escalada le quedan disponibles.
- **`auditor-seguridad`.** El veto sigue entero y se refuerza: «**Declararlo no lo debilita**».
  `:141` conserva el veredicto frente a la clasificación y nombra la discrepancia. El párrafo nuevo
  de urgencia (`:138`) le **añade** una vía, no le quita ninguna.

**Lo que sí me confunde, y es la parte accionable de este apartado:** las dos referencias nuevas me
mandan clasificar mi bloqueo con un vocabulario que no tiene una clase para el bloqueo que yo
realmente impongo. Es QA-025-02, y no es teórico: mañana, escribiendo un veto o encolando una
decisión, no sabría qué palabra poner.

---

## 5. ADR-008 y `CHANGELOG.md`

**ADR-008 — bien formado.** Plantilla `templates/ADR.md.tpl` respetada (Contexto · Decisión ·
Alternativas consideradas · Consecuencias), `Fecha: 2026-09-21`, `Estado: aceptada`, número libre
(`ADR-001`…`ADR-007` existen en `main`; no hay `ADR-008` previo). Las decisiones **(a)** y **(b)**
del propietario van citadas entre comillas y coinciden con el literal del Historial de REQ-025.
Las consecuencias incluyen una fila `(=)` **«Lo que NO cambia»** y tres `(−)` con mitigación,
incluida la de que los proyectos instalados no reciben nada. Lo único que falta es **el enlace
desde el REQ** (QA-025-04).

**Y una ironía útil:** la cuarta alternativa descartada del propio ADR es *«**Distribuir las reglas
a cada definición de agente** — por qué no: dos transcripciones de la misma regla se desfasan en
silencio… Los agentes llevan **referencias**, no copias.»* El árbol entregado hace exactamente lo
que el ADR descarta. Ver QA-025-03.

**`CHANGELOG.md` — conforme.** Fecha real `2026-09-21`, `Origen: GitHub`, usuario, modelo, agente,
rama y lista de archivos. Sin marcadores. No declara entregado a ningún consumidor.

---

## 6. CA-13 — puertas comunes, corridas por mí

**Quality gates de `AGENTS.md` §7: 3 de 3 en verde.**

```
for f in hooks/*.sh tools/*.sh; do bash -n "$f" || exit 1; done     → OK
jq -e . hooks/hooks.json                                             → OK
jq -e . .claude-plugin/plugin.json && .../marketplace.json           → OK
```

**Banco completo (`tests/escenarios/hooks/run.sh`): 1 FAIL, reproducido.** Ver QA-025-05 para el
detalle y las tres corridas.

---

## Hallazgos

### QA-025-01 — `contrato` · Criterios declarados como medidores de la entrega 1 cuyo contenido no se entregó

**Qué dice el REQ.** «Qué criterios la miden, después de la partición: **todos los que quedan en
este REQ** —CA-01, CA-02, CA-09, CA-10, CA-11, CA-13, CA-14 y CA-15— … **Ningún criterio de este
REQ es hoy inaplicable**, que es exactamente lo que la partición vino a arreglar»
(`requirements/REQ-025.md:492-500`).

**Qué hay.** La lista de piezas de la entrega 1 (`:470-483`) contiene seis piezas, y ninguna
entrega el contenido de CA-01, de CA-02 ni de las partes de CA-09/CA-10 que se listan abajo.
El resultado es que **seis criterios o medios criterios declarados aplicables no tienen nada que
medir en el árbol entregado**.

| Falta | Criterio | Comprobación |
|---|---|---|
| La **tesis** en su forma general («una afirmación vale lo que valga la capacidad de su instrumento para desmentirla…») y los **tres niveles** con lo que cada uno **no** promete | CA-01, entero | `grep -n "capacidad de su instrumento\|lectura sin acreditar\|desmentirla" AGENTS.md templates/AGENTS.md.tpl` → **sin coincidencias** |
| Las **dos clases dominantes** (la sonda improvisada · la cifra sin procedencia), su propiedad común y **la dirección del fallo** (5 de 5 hacia el verde; 2 cifras retiradas) | CA-02, entero | `grep -n "sonda improvisada\|cifra sin procedencia" AGENTS.md` → una sola coincidencia, `:466`, y es una **mención de paso** dentro de la regla 6, sin definir la clase, sin la segunda clase y sin la asimetría |
| Preguntas previas **1** (¿algo mide ahora mismo?), **2** (¿el ámbito colisiona? derivado con `tools/arnes-paralelo.sh`) y **4** (¿la comisión lleva vencimiento?) | CA-09 (B) | `grep -n "mide ahora mismo\|ámbito colisiona\|vencimiento\|antes de despachar" AGENTS.md` → sólo `:390`, que es el residual de «Agotado el tope», no la pregunta |
| «cada una con **la propiedad que responde** y **con su caso medido**» | CA-09 (B), encabezado | Ningún caso medido en la sede: `grep -rn "3 h 41\|99,6\|192 líneas\|madrugada" AGENTS.md templates/AGENTS.md.tpl` → **sin coincidencias** |
| «mientras haya comisiones vivas, la coordinadora comitea **rutas nombradas, nunca `-A`**» | CA-09, párrafo final | `grep -n "add -A" AGENTS.md` → **sin coincidencias** |
| «Dado un **encargo** que afirme el estado del árbol o de la cola, Entonces lo **deriva** … y **no lo recuerda**» | CA-10, primera mitad —que el propio criterio declara parte de la entrega 1: *«Aquí queda la mitad que la **entrega 1** necesita … **la derivación del estado en el encargo** y las reglas 5 y 6»* (`:207-210`) | `grep -n "no lo recuerda\|lo deriva" AGENTS.md` → **sin coincidencias** |

**Esperado vs. observado.** Esperado: los criterios declarados aplicables se cumplen sobre el árbol
entregado. Observado: CA-01 y CA-02 no tienen ninguna contraparte, y CA-09/CA-10 la tienen a
medias.

**Por qué es `contrato` y no «trabajo que falta».** Porque el REQ **afirma** dos cosas
incompatibles sobre lo construido: que la entrega 1 son esas seis piezas, y que todos los criterios
restantes la miden. Una de las dos es falsa hoy. No lo resuelvo yo eligiendo cuál.

**Qué lo resuelve** (y la elección es de la coordinadora por la regla 3, con el propietario si toca
alcance): **(A)** escribir en la sede lo que falta —y entonces la lista de piezas de «Notas /
alcance» debe recogerlo—, o **(B)** que el `analista-requerimientos` haga el write-back declarando
qué criterios miden la entrega 1 y cuáles esperan a una entrega posterior, con su causa. **Lo que
no es salida:** cerrar la entrega dejando escrito que esos criterios la miden.

---

### QA-025-02 — `contrato` · El vocabulario cerrado no tiene clase para el bloqueo más común del arnés, y dos agentes reciben una instrucción que no pueden ejecutar

**El defecto, en tres líneas del árbol entregado:**

1. `AGENTS.md:418-423` (regla 2) — todo bloqueo, *«venga de un hallazgo, de un veto, **de la cola de
   aprobaciones** o de una dependencia»*, declara **(i) qué acción impide**, del vocabulario
   **cerrado** `implementar · probar · aprobar · publicar`, *«donde «aprobar» nombra las
   **aprobaciones humanas** de «Gates de aprobación humana», más abajo, que conceden personas y que
   **ningún hook impone**»*.
2. `AGENTS.md:490-492` (titular retocado) — *«son las que la regla 2 llama «**aprobar**», las
   concede una persona y **ningún hook las impone**. Lo que no está en esta lista **no se detiene**
   por ellas»*. Esto convierte la lista de cuatro viñetas en la **extensión** de «aprobar».
3. `AGENTS.md:508-510` (frontera ii) — la cola *«**no es** ninguna de las aprobaciones humanas
   normativas de «Gates de aprobación humana»»*.

**Consecuencia.** El bloqueo de la cola —y el del veto de seguridad, y el del orden de fases— no es
`implementar` (la frontera (i) dice que no lo impide), no es `probar` (íd.), no es `publicar`, y
**ya no puede ser `aprobar`**. El vocabulario declarado **cerrado** no clasifica el bloqueo que la
propia sede usa como ejemplo. Y la regla 2 remata: *«Cuando el bloqueo es mecánico, la declaración
**conserva su clase**»* — ¿cuál?

**Y el criterio dice lo contrario.** `requirements/REQ-025.md:196-199` escribe el ejemplo resuelto:
*«**clase de acción** «aprobar»; **acción impedida, exacta:** marcar un REQ como `completado`»*, y
dos líneas después, *«**(ii)** **no es** ninguna de las aprobaciones humanas normativas de §6»*.
**El criterio ya era contradictorio**: define «aprobar» por la lista de §6 y clasifica ahí algo que
declara no ser de esa lista. Lo que hace el árbol entregado es **cerrar la única lectura que lo
salvaba** —«aprobar» como *clase de acto*, con la lista de §6 como caso paradigmático y no como
extensión— mediante un retoque del titular que quedó **fuera de las seis piezas**.

**Respuesta a la pregunta del encargo sobre ese retoque:** *corrige una promesa y además la
estrecha*. Elimina bien el «el pipeline se detiene» absoluto; pero al añadir «son las que la regla
2 llama «aprobar»» y «lo que no está en esta lista no se detiene por ellas» **decide una cuestión
de contrato** que el propietario no decidió, y la decide en la dirección que deja sin clase al
bloqueo mecánico.

**Reproducción (mesa, sin código).** Soy el `auditor-seguridad` y veto REQ-XXX.
`agents/auditor-seguridad.md:24` me manda declarar *«qué acción impide — implementar · probar ·
aprobar · publicar»*. Mi veto impide **marcar REQ-XXX como `completado`**, y lo impone
`guard-completado`. Por `AGENTS.md:490-492`, eso **no** es «aprobar». No tengo clase válida.
Idéntico para el `desarrollador` en `:52` y para el `qa-tester` en `:86` y `:124-127`, que ya
esquivan el problema **omitiendo la clase** y nombrando sólo la transición — es decir, incumpliendo
el (i) de la regla 2 que ellos mismos citan.

**Qué lo resuelve** — dos salidas, y la elección es de contrato:
- **(A)** Que la sede diga que «aprobar» es una **clase de acción** cuyo caso normativo es la lista
  de §6 y que **también** cubre el bloqueo mecánico de la transición a `completado`, retirando
  «son las que la regla 2 llama aprobar» del titular y conservando la distinción por la vía que el
  propietario pidió: la **transición exacta** nombrada al lado de la clase.
- **(B)** Añadir una quinta clase — que la regla 2 declara **cambio de contrato**, luego decide el
  propietario.

En ambos casos hay **write-back** a CA-09 (B) punto 5 y propagación a `AGENTS.md`, su gemela, los
tres agentes y las dos cabeceras de la cola. **Barrer por propiedad, no por esta lista:** *toda
sede que instruya a alguien a clasificar un bloqueo con las cuatro clases*.

---

### QA-025-03 — `contrato` · Segunda y tercera transcripción de las reglas 2 y 3 en `agents/` (CA-15 punto 1)

**Qué exige el criterio.** CA-15 pt 1: *«Las seis reglas viven **una vez** … **Ningún agente,
plantilla, skill ni documento copia su texto**: remiten a ella. Una segunda transcripción de
cualquiera de las seis es hallazgo de clase `contrato` contra este REQ.»* CA-15 pt 4: *«como
**referencia, no como copia**: la frase se corrige **lo justo** para dejar de afirmar lo contrario
y **remite** a la sede»*.

**Qué dice la propia sede entregada.** `AGENTS.md:409-411`: *«**Aquí viven una sola vez:** ningún
agente, plantilla, skill ni documento copia su texto»*. Y `:419-420`, del vocabulario: *«**esta
línea es su sede única**»*.

**Dentro de `§6`: NO hay doble transcripción, y está bien resuelto.** El bloque nuevo **remite** en
los tres puntos donde podría haber copiado: la regla 3 dice *«…están escritos en «Loop de error»,
más arriba, y **no se transcriben aquí**»*; la regla 5, del tope de vueltas, *«que se **cita** y
**no se redeclara aquí**»*; y la regla 4, *«Qué impide exactamente la cola, y qué no, está en
«Mecanismo de gate», más abajo»*. La sede se disciplinó a sí misma. **El problema está fuera.**

**Qué hay en el árbol.** Inventario completo, obtenido con `grep -rn` por cada fragmento normativo:

| Fragmento de la sede | Sedes donde aparece **además** de `AGENTS.md` + su gemela |
|---|---|
| El vocabulario cerrado `implementar · probar · aprobar · publicar` —cuya línea se autodeclara **sede única**— | `agents/auditor-seguridad.md:24` · `agents/desarrollador.md:52` |
| Los **cuatro elementos** (i)…(iv) de la regla 2 | `agents/auditor-seguridad.md:24` · `agents/desarrollador.md:52` |
| «no se extiende solo… ni **se rodea con otro REQ, una reclasificación ni un cambio de herramienta**» | `agents/desarrollador.md:52` |
| Los **tres términos** de la clasificación de la regla 3 | `agents/qa-tester.md:87` |
| «**no retira, degrada ni pospone** un veredicto» | `agents/qa-tester.md:87` · `agents/auditor-seguridad.md:141` |
| La **discrepancia** que se resuelve o se escala + «mover el hallazgo de sitio no permite cerrar» | `agents/qa-tester.md:87` · `agents/auditor-seguridad.md:141` |
| La **forma** de la regla 4 (pregunta · opciones · recomendación · consecuencia) | `agents/qa-tester.md:86` · `agents/qa-tester.md:124` · `agents/auditor-seguridad.md:138` |
| El efecto concreto de la urgencia (regla 3 / CA-14 pt 3) | `agents/auditor-seguridad.md:138` |
| El «ni por cambio de rol, de fase o de nombre… ni abriendo un REQ nuevo» de la regla 5 / CA-14 pt 6 | `agents/qa-tester.md:109-112` |

**Y el desfase ya ocurrió, en el mismo commit que creó las copias** — no es un riesgo futuro:

1. **La copia del vocabulario pierde el glosario de «aprobar».** La sede dice
   `implementar · probar · aprobar · publicar`, *«donde «aprobar» nombra las aprobaciones humanas…
   que ningún hook impone»*, y añade la regla del bloqueo mecánico. Las copias de
   `auditor-seguridad.md:24` y `desarrollador.md:52` llevan **los cuatro términos sin el glosario y
   sin la regla del bloqueo mecánico**. Un auditor que lea sólo su agente elegirá «aprobar» para un
   bloqueo de máquina — exactamente la confusión que la precisión (a) del propietario existe para
   eliminar.
2. **La copia de la regla 3 cambia el alcance de la dependencia.** Sede: una dependencia *«abre
   **sólo lo que depende de ella**, sin ampliar el encargo»*. `qa-tester.md:87`: *«una
   **dependencia** abre **sólo lo que bloquea**»*. No es la misma extensión, y la variante del
   agente es la más ancha.

**Y el ADR de esta misma entrega descarta esto por su nombre**
(`ADR-008…md`, «Alternativas consideradas», cuarta viñeta): *«Distribuir las reglas a cada
definición de agente — por qué no: dos transcripciones de la misma regla se desfasan en silencio…
Los agentes llevan **referencias**, no copias.»*

**Y una sede más, que es la que se lee sin abrir nada**: `CHANGELOG.md:24` afirma *«Ningún agente,
plantilla, skill ni documento copia ese texto: **remiten** a él»*.

**Esperado vs. observado.** Esperado: `- Tienes poder de **veto**… Indica el motivo y la corrección
requerida, **y declara el alcance del veto con la forma de `AGENTS.md` §6, regla 2.**` Observado:
la frase anterior más la transcripción íntegra de la regla.

**Qué lo resuelve.** Sustituir cada copia por una remisión, en las nueve sedes de la tabla. **El
barrido se define por propiedad, no por esta tabla:** *todo texto fuera de `AGENTS.md` §6 y su
gemela que reproduzca el contenido enumerado de cualquiera de las seis reglas, en vez de citarlo*.
Incluye los comentarios y los mensajes de ejecución si alguno la reprodujera —hoy `hooks/` no lo
hace, comprobado— y las dos afirmaciones que dicen que esto ya se cumple (`AGENTS.md:409-411` está
bien; lo que hay que hacer cierto es el árbol, no retirar la promesa).

**Nota de alcance:** no reclamo que la entrada «Hacia 1.35.0» de `skills/arnes-upgrade/SKILL.md`
sea una transcripción. **Decidido: es descripción.** Nombra las seis reglas **por su título**,
describe qué secciones cambian y cómo clasificarlas, y **no reproduce** el vocabulario cerrado, los
cuatro elementos de la regla 2 ni los tres términos de la regla 3 (comprobado por `grep`). Un
artefacto de migración que no describiera el cambio no serviría para decidir si migrar, que es su
única función; y CA-15 pt 3 le pide exactamente que «lo describa». Su párrafo «Qué NO cambia»
enumera facultades conservadas, que es la apertura que CA-14 pt 1 ordena poner primero.

---

### QA-025-04 — `contrato` · CA-15 punto 7: el ADR existe pero el REQ no lo enlaza, y su Historial afirma que está por crear

**Qué exige el criterio.** CA-15 pt 7: *«El ADR del cambio de fondo **existe y el REQ lo enlaza**…
este REQ lo **enlaza** desde su «Historial de cambios». **La entrega no está completa sin él.**»*

**Pasos de reproducción.**
```
grep -n "ADR-008" requirements/REQ-025.md      →  (sin coincidencias)
sed -n '740p' requirements/REQ-025.md          →  … | **Pendiente de crear y enlazar aquí**:
                                                   el ADR de `docs/decisions/` que registra la
                                                   reescritura de «Loop de error» y «Mecanismo
                                                   de gate» (§9, cambio de fondo). …
```

**Esperado vs. observado.** Esperado: la columna `ADR` de la fila del 2026-09-21 apunta a
`docs/decisions/ADR-008-coordinacion-orientada-a-entregas-y-bloqueos-con-alcance.md`, y la nota
«Pendiente de crear y enlazar aquí» desaparece o se cierra. Observado: el ADR existe en el árbol y
el REQ sigue diciendo que está pendiente de crear. Es deriva de `AGENTS.md` §9 a escala de una
celda, y hace **falso** el Historial del REQ.

**Dueño del arreglo: `analista-requerimientos`** — el Historial es suyo (`AGENTS.md` §9, write-back);
el desarrollador hizo bien en no tocar `requirements/` sin tenerlo en su conjunto de escritura.

---

### QA-025-05 — `instrumento` · CA-13: el banco completo reporta **1 FAIL reproducible**, y el recuento del desarrollador no es verificable

**Lo que exige CA-13.** *«el banco completo reporta PASS, FAIL y SKIP por separado, **no hay ningún
FAIL**, cada SKIP explica en su línea la condición que lo salta, `CASOS_ESPERADOS` coincide con
`PASS+FAIL+SKIP` y el cuadre final no aborta»*.

**Lo que reportó el desarrollador:** `906 · 0 · 6`, **sin salida guardada** → no verificable. Lo
corrí yo tres veces en `567eb38`, en el worktree `/home/juan/dev/ArnesJuan-req025`
(Linux, WSL2 · kernel 6.18.33.2-microsoft-standard-WSL2), con la salida completa en disco.

| Corrida | PASS | FAIL | SKIP | Total | `rc` | Tiempo del caso desbocado |
|---|---|---|---|---|---|---|
| 1 | 907 | **1** | 4 | 912 | **1** | 4231 ms |
| 2 | 906 | **1** | 5 | 912 | **1** | 4255 ms |
| 3 | 906 | **1** | 5 | 912 | **1** | 4128 ms |

*(Los logs vivieron en el scratchpad de esta sesión, que no es persistente; el caso y sus cifras
quedan transcritos aquí, que es lo que un trabajo posterior necesita para re-derivarlas: comando
`bash tests/escenarios/hooks/run.sh` sobre `567eb38` en el worktree citado, tres corridas
consecutivas sin otra carga que la propia sesión.)*

**El FAIL, idéntico en las tres corridas — reproduce 3 de 3:**
```
FAIL  DEV v3: heredoc CITADO de ~300 KB -> allow y barato
      DESBOCADO: veredicto allow correcto, pero <T>ms en la mejor de 3 medidas
                 (presupuesto 1000ms, techo holgado 4000ms)
      T = 4231 [c1] · 4255 [c2] · 4128 [c3]
```
La sección en que vive: `DEV REQ-001 v3: presupuesto de analisis y coste lineal (QA-007)`. Los
cinco casos vecinos de esa misma sección **pasan**, incluido el `heredoc SIN citar de ~300 KB`
(5271 ms contra un techo de 20000 ms): el desbordamiento es **de este caso**, que es el único con
un techo de 4000 ms.

**Diagnóstico honesto, separando el dato de la interpretación.**
- **Dato:** el veredicto funcional es **correcto** (`allow`); lo que excede es el **presupuesto de
  tiempo**, y por **3,2 %–6,4 %** sobre un techo ya declarado «holgado». El caso toma la **mejor de
  3 medidas** internamente, así que ninguna de las tres es un pico aislado. Reproduce **3 de 3**, y
  las tres medidas caen en una banda estrecha (4128–4255 ms): **no es flaky, está consistentemente
  por encima**.
- **Dato:** el delta bajo revisión **no toca una sola línea de código** (`hooks/`, `tools/`,
  `tests/` intactos, verificado con `git diff --name-only`). El FAIL **no lo causa esta entrega**.
- **Interpretación:** es sensible a la plataforma y a la carga. No lo declaro flaky —no lo he visto
  pasar ni una vez en tres corridas—, y **no lo repito hasta obtener verde**: eso sería elegir la
  corrida que conviene. Queda como está, medido, con la variabilidad conservada.

**Por qué `instrumento` y no `usuario/dinero`:** el defecto está en el **presupuesto de la prueba**,
no en el producto; nadie ve, decide ni cobra distinto por ello. **No bloquea el cierre por su
clase** — pero **CA-13 sí lo hace por su propio texto** («no hay ningún FAIL»), y ese criterio es
puerta de cierre. Las dos cosas son compatibles: la clase gobierna `guard-completado`; CA-13
gobierna mi veredicto.

**Lo que esto NO acredita, y es lo que decide la fusión:** la puerta requerida de `main` es
`hooks-en-linux` **en CI**, no mi máquina. Si el banco sale verde en CI sobre `567eb38`, CA-13
queda satisfecho y este hallazgo se cierra como diferencia de plataforma —con la corrida de CI
citada—. Si sale rojo, es trabajo real y tiene dueño.

**Lo demás de CA-13, verificado:**
- **Cuadre:** `907+1+4 = 912`, `906+1+5 = 912`, `906+1+5 = 912`. El total **no varía entre las tres
  corridas** y el corredor **no abortó** por descuadre en ninguna (aborta si `CASOS_ESPERADOS` no
  coincide; no lo hizo). La oscilación 4↔5 SKIP es de los casos de sonda de `REQ-021`, que deciden
  su propio SKIP por convergencia, y está explicada en línea.
- **SKIP con motivo:** los 4–5 SKIP llevan su condición en la línea (caso sólo de Windows sin
  `cygpath`; dos de `REQ-017 CA-05` con su evidencia acreditada y el mando para repetirlos;
  `REQ-021 CA-08 (iii)` con la mitad no medible declarada `no-aplica`). **Conforme.**
- **Autoprueba del corredor:** las secciones de autoprueba corren dentro del banco y todas pasan;
  `tests/` no se toca en este delta, así que no hay regresión del instrumento atribuible a la
  entrega.

---

### QA-025-06 — `instrumento` · La plantilla arrastra contenido propio de este repositorio

**Qué exige el encargo y CA-15 pt 3.** La gemela se prepara para que un proyecto la reciba por
`arnes-upgrade`. Nada específico de este repositorio debería viajar en ella.

**Qué hay en `templates/AGENTS.md.tpl`:**

1. **Siete citas a `REQ-025`**, líneas `374`, `379`, `385`, `397`, `411`, `419`, `427`. La primera
   va cualificada —«el requerimiento **del arnés** que las introdujo»—, pero las **seis** de las
   reglas son citas desnudas del tipo `` (`REQ-025` CA-09 (A)) ``. En un proyecto consumidor,
   `requirements/REQ-025.md` o **no existe** o es un REQ **suyo**, sin relación. Hay precedente de
   citar identificadores del arnés en la plantilla (`SEC-020` en `:303`, `REQ-015` en `:698`), pero
   los dos son menciones históricas cualificadas, no **seis punteros normativos** que le dicen al
   lector dónde está el contrato de la regla que acaba de leer.
2. **«el libro de comisiones»**, en la regla 6 (`:427`) y en el encabezado del bloque (`:372`), sin
   antecedente que lo defina en toda la plantilla, y con las columnas concretas de este repositorio
   detrás en el criterio. Un proyecto recién inicializado **no tiene** libro de comisiones ni
   ninguna plantilla que lo cree: la regla 6 le manda anotar en un artefacto inexistente.

**Esperado vs. observado.** Esperado: la plantilla remite al contrato por una vía que un consumidor
pueda seguir —«el requerimiento del arnés que las introdujo, ver el `CHANGELOG` del plugin», o sin
identificador— y nombra el registro de comisiones de forma que un proyecto sepa qué es o pueda
omitirlo. Observado: siete punteros a un identificador ajeno y un artefacto no definido.

**Por qué `instrumento` y no `contrato`:** **nada de esto ha llegado a un consumidor** —CA-15 pt 3
lo declara preparado y no entregado, y así está escrito— así que hoy nadie ve, decide ni cobra
distinto. **No bloquea.** Pero es deuda con dueño y **vencimiento natural: antes de publicar
1.35.0**, porque publicar es el acto que lo pone en manos de alguien. Dueño propuesto:
`desarrollador`, en la misma vuelta que QA-025-03, que toca los mismos archivos.

---

## Observaciones que no abro como hallazgo

- **OBS-A — `PENDING_APPROVAL.md:33` y su gemela** conservan «bloquea todos los cierres». Detalle y
  motivo de no abrirlo, en §2.
- **OBS-B — colisión anunciada con `§14` de `rel/registro-1.33.0`.** La rama bajo revisión tiene
  `AGENTS.md` con **13** secciones; la rama `rel/registro-1.33.0` añade una **§14 «Reglas de trabajo
  de la sesión coordinadora»** que **no** está en `main` ni aquí (verificado:
  `git show main:AGENTS.md | grep "^## 1[0-9]\."` llega a `## 13`; el worktree principal llega a
  `## 14`). Cuando las dos ramas converjan, `§14 A` («(1) qué resultado exacto debe entregar… (4)
  qué queda fuera, y cuándo debe detenerse») será una **segunda sede de la regla 1**, y `§14 B`
  punto 4 («Corregir sin ampliar — los hallazgos adicionales van a la cola salvo que impidan el
  trabajo en curso») una **segunda sede de la regla 3**, ya desfasada: «van a la cola» ≠ «se
  registra con responsable en su sede y queda fuera de la entrega». **No es hallazgo contra
  `567eb38`** —§14 no está en su base— sino un aviso para quien decida el orden de fusión. Si las
  dos entran, CA-15 pt 1 quedará incumplido en el árbol fusionado sin que nadie haya tocado
  ninguno de los dos textos.
- **OBS-C — la referencia de los agentes apunta a un `§6` que el proyecto consumidor aún no tiene.**
  El desarrollador lo declaró en `docs/arnes/req-025-entrega1-preparacion.md` §7 punto 1, y la
  entrada «Hacia 1.35.0» lo dice. Lo confirmo y **no lo abro**: es inherente al diseño de CA-15 pt 4
  (los agentes llegan con el plugin; el `AGENTS.md` del proyecto está congelado), está declarado, y
  nadie lo recibe hasta publicar y migrar.

---

## Qué NO acredita este veredicto

Dicho por delante para que nadie lo lea de más:

1. **El ensayo S1…S4 de CA-11 punto 3.** No se ha ejecutado. Lo lanzará la coordinadora y lo
   acreditaré en otra comisión. Hasta entonces, **la condición 3 de CA-11 está sin satisfacer** y
   CA-13 la exige para cerrar.
2. **La conducta.** Que las seis reglas estén escritas no acredita que se sigan. Son **disciplina
   declarada con dueño** (CA-01 nivel 3, y la sede lo dice: «ninguna puerta las comprueba»). Nada
   aquí mide si una coordinadora las cumple.
3. **Nada sobre los proyectos consumidores.** No reciben nada hasta publicar y hasta que su
   propietario migre. No he probado ninguna migración.
4. **Ningún ahorro** de tiempo, de tokens ni de vueltas, y ningún **determinismo**: no lo he medido
   y el criterio prohíbe afirmarlo.
5. **El banco en CI.** Mi corrida es en WSL2 y no es la puerta requerida de `main`.
6. **La revisión de seguridad.** El REQ es `Sensible a seguridad: sí` y `Rigor: critico`: el
   `auditor-seguridad` firma **después** de mí y sobre el árbol validado (CA-11 pt 4).
7. **Rendimiento como NFR.** `AGENTS.md` §7 no fija umbral de usuarios concurrentes ni de latencia
   para esta entrega —no hay servicio que cargar: es texto normativo—. El único umbral temporal
   aplicable es el del banco, y está en QA-025-05. **Nada pendiente de acordar con el humano por
   este lado.**

---

## Cierre

**`QA: con-hallazgos`.** Quedan **cuatro hallazgos de clase `contrato`** abiertos
(QA-025-01, QA-025-02, QA-025-03, QA-025-04), que bloquean el cierre, y **dos `instrumento`**
(QA-025-05, QA-025-06), que no lo bloquean por su clase — aunque QA-025-05 impide dar CA-13 por
cumplido mientras no haya una corrida verde del banco sobre `567eb38`.

**Lo que esta entrega sí consiguió, y no es poco:** las dos promesas absolutas que el REQ vino a
matar **están muertas** en las cuatro sedes que las alojaban, reescritas y no matizadas; las
gemelas son exactas; el alcance se respetó con 0 archivos fuera; el ADR está bien construido; y
ningún agente perdió una facultad. Los cuatro `contrato` no revierten nada de eso: tres piden
completar lo que falta y uno resolver una contradicción que **ya venía en el criterio**.

**Reparto del trabajo, para que la coordinadora clasifique (regla 3) sin tener que releerlo todo:**

| Hallazgo | Quién lo repara | Necesita decisión de alcance |
|---|---|---|
| QA-025-01 | `analista-requerimientos` (write-back) **o** `desarrollador` (escribirlo) | **Sí** — propietario, si se mueve contenido a una entrega posterior |
| QA-025-02 | `analista-requerimientos` + `desarrollador` | **Sí** — propietario: la salida (B) es cambio de contrato por la propia regla 2 |
| QA-025-03 | `desarrollador` | No |
| QA-025-04 | `analista-requerimientos` | No |
| QA-025-05 | `desarrollador` (o se cierra con la corrida de CI) | No |
| QA-025-06 | `desarrollador` | No |

**Y una discrepancia que declaro por adelantado, con la forma de CA-14 punto 5.** Si la
coordinadora clasificara QA-025-02 como **independiente** —por ser «una contradicción que ya venía
en el criterio»— yo discrepo: **bloquea esta entrega**, porque la instrucción inejecutable está en
los agentes que **esta entrega** escribió, y porque CA-09 (B) punto 5 es uno de los criterios que
la miden. No es una clasificación firme mientras no la resolvamos con la evidencia a la vista o se
escale al propietario; y **moverla de sitio no la resuelve**.

*(Cerrado en la vuelta 2: la coordinadora **no** lo clasificó como independiente — lo llevó al
propietario, que resolvió con la **opción B**. La discrepancia no llegó a existir.)*

---
---

# Vuelta 2 de 3 — adenda

**Veredicto: `con-hallazgos`.** Cinco de los seis hallazgos de R-1 quedan **resueltos** y uno
**nuevo** se abre. Es la **segunda** de las tres vueltas de la entrega; queda **una**.

| | |
|---|---|
| Árbol validado | `0558a0e` (= `faf0db7` analista + `e566739` desarrollador + `0558a0e` coordinadora) |
| Delta revisado | `git diff 48c87b7 0558a0e` — 12 archivos. **Lo validado en R-1 que no cambió sigue vigente y no se repite** |
| Fecha del veredicto | 2026-09-21 |
| Ronda | **R-2** (vuelta 2 de 3 de la entrega; el contador es **de la entrega** y **no se reinicia** — CA-14 punto 6) |
| Hallazgos abiertos tras R-2 | **1 `contrato`** (QA-025-07) · **1 `instrumento`** (QA-025-05) |

---

## 1. Estado de cada hallazgo y observación

| Id | Clase | Estado tras R-2 | Dónde |
|---|---|---|---|
| **QA-025-01** | `contrato` | **RESUELTO** | §2 |
| **QA-025-02** | `contrato` | **RESUELTO** | §3 |
| **QA-025-03** | `contrato` | **RESUELTO** | §4 |
| **QA-025-04** | `contrato` | **RESUELTO** | §5 |
| **QA-025-05** | `instrumento` | **ABIERTO, sin cambio** | Banco no re-corrido por instrucción; el `1 FAIL` de tiempo de R-1 (3 de 3, 4128–4255 ms) sigue siendo la medida vigente, y sigue siendo **ajeno al delta** (`git diff --name-only 48c87b7 0558a0e` → **0 archivos** de `hooks/`, `tools/`, `tests/`, `.github/`, `.arnes/`, `.claude-plugin/`). Cierra con una corrida verde en CI |
| **QA-025-06** | `instrumento` | **RESUELTO** | §6 |
| **OBS-A** | — | **RESUELTA** | §6 |
| **OBS-B** (§14 de `rel/registro-1.33.0`) | — | **ABIERTA, sin cambio** — no está en esta base; sigue siendo aviso de fusión | §7 |
| **OBS-C** (referencias a un `§6` inexistente) | — | **ABIERTA y AGRAVADA**, con remedio propuesto | §8 |
| **QA-025-07** | `contrato` | **NUEVO — ABIERTO** | §9 |
| **OBS-D · OBS-E · OBS-F** | — | **NUEVAS**, no bloquean | §10 |

**QA-025-07 no reinicia nada.** Es una variante del mismo defecto de entrega —el REQ describiendo
mal su propio árbol— que ya produjo QA-025-04. Le doy identificador propio porque lo que QA-025-04
contrataba (que el enlace exista) **sí** está hecho, y esconder un defecto nuevo dentro de un
identificador viejo sería la reclasificación que CA-14 punto 4 prohíbe. **El contador de la entrega
va por 2 de 3**, y eso no cambia por abrir, cerrar ni renumerar hallazgos (`AGENTS.md` §6, «Loop de
error», y regla 5).

---

## 2. QA-025-01 — RESUELTO

**Qué contrataba:** que el REQ dejara de afirmar dos cosas incompatibles (la entrega son seis
piezas · todos los criterios restantes la miden).

**Qué hay ahora.** «Notas / alcance» sustituye «todos los que quedan» por una **tabla criterio a
criterio** (17 filas) con la comprobación de dependencia de cada uno, y una §«Entrega 1b» que
conserva lo diferido **`pendiente` dentro de este REQ**, sin ventana ni fecha. **CA-15 punto 9**
(nuevo) prohíbe por su nombre que ningún artefacto de la entrega 1 escriba que REQ-025 queda
terminado.

**Falsación — la parte que importa, porque la decisión del propietario condicionó el reparto a
«siempre que no sean dependencias necesarias de esas seis reglas».** Comprobé las **cuatro**
asignaciones «Sí, por dependencia» **contra la sede**, no contra la tabla:

| Lo que la tabla declara dependencia y por tanto entrega 1 | ¿Está en la sede? |
|---|---|
| CA-01, la frase del nivel 3 («disciplina declarada con dueño; ninguna puerta las comprueba») | **Sí** — `AGENTS.md:406-408`, literal |
| CA-02, el **puntero** cuando la sede usa el nombre de una clase | **Sí** — la regla 6 glosa «cifra sin procedencia» («una cifra dada por medida sin que se pueda volver a su corrida») **y** apunta a dónde está definida con sus instancias |
| CA-09 (B) pregunta 3, **con su caso medido** — que la propia tabla declara «hoy falta en la sede: es trabajo de la entrega 1» | **Sí, añadido en `e566739`** — regla 4: «**Caso medido:** una pregunta encolada **de madrugada** dejó parado trabajo que nada impedía». *(Que la tabla siga diciendo que falta es QA-025-07.)* |
| CA-09 (B) pregunta 5, con las cinco clases | **Sí** — regla 2, §3 |

**Y falsé las cinco asignaciones a 1b buscando una dependencia que el analista hubiera pasado por
alto.** No la encontré: la pregunta 4 (¿vencimiento?) queda cubierta por «cuándo debe detenerse» de
la regla 1; la pregunta 2 (colisión de ámbito) deriva de REQ-022 y ninguna de las seis la invoca;
la primera mitad de CA-10 (derivar el estado) no la necesita ninguna regla —la regla 2 (iii) pide
**citar** evidencia, no **derivar** estado—; CA-01 niveles 1 y 2 y CA-02 entero no condicionan que
ninguna de las seis se enuncie o se cumpla, y los dos puntos donde sí rozaban se resolvieron
**quedándose** (las dos primeras filas de arriba). **El reparto respeta la condición del
propietario.**

---

## 3. QA-025-02 — RESUELTO

**Qué contrataba:** que existiera una clase para el bloqueo mecánico, y que las instrucciones de
los agentes fueran ejecutables.

**Qué hay ahora.** Cinco clases glosadas una a una en la regla 2, sede única; «aprobar» = **sólo**
la aprobación humana normativa; «**cerrar**» = marcar el REQ como `completado`, con las condiciones
de `guard-completado` **citadas** (§6 y §13) y no redeclaradas; «Mecanismo de gate» nombra la clase
«cerrar», la acción exacta y **la regla que la impide**; el veto «conserva su alcance según la
regla que lo establece» y **puede impedir a la vez cerrar y publicar**.

**Falsación — cuatro ataques, los cuatro fallan:**

1. **¿Se cierra algún conjunto por extensión otra vez?** `grep` de «no se detiene», «cuatro
   clases», «quinta clase» y «aprobar/cerrar» en todo el árbol: **cero** coincidencias en sede
   normativa (sólo narrativa histórica en `CHANGELOG.md`, el Historial del REQ y el ADR, donde
   corresponde). El titular de «Gates» ya no dice «lo que no está en esta lista no se detiene»;
   dice lo contrario, con **ejemplos no exhaustivos** de los otros bloqueos.
2. **¿La glosa de «cerrar» redeclara las condiciones de `guard-completado`?** Las **enumera**
   (cola · hallazgo `usuario/dinero`/`contrato`/sin clase · gate en rojo · firma que falte) y
   **omite** dos que §13 sí tiene (la cabecera que no se puede medir; el veredicto con fecha). Eso
   sería forma (a) —enumerar un conjunto que el código reconoce— **si la lista se presentara como
   completa**. No lo hace: la misma regla 2 escribe «**Ninguna lista de bloqueos concretos enumera
   todos los bloqueos posibles —tampoco las de esta sección—**», y cita §6 y §13 como sede. **No
   es hallazgo.** El desarrollador se adelantó a este ataque.
3. **¿Siguen existiendo dos sedes del vocabulario?** `grep` del vocabulario de cinco en todo el
   árbol: sólo `AGENTS.md`, su gemela y el criterio que lo contrata. **Cero** en `agents/`,
   `skills/` y `PENDING_APPROVAL`. La autodeclaración «cuya sede única es esta regla» es ahora
   **cierta** — en R-1 no lo era.
4. **La prueba de uso, que es la que decide: ¿puedo yo escribir MI bloqueo con esta forma?** Sí, y
   lo hago aquí en vez de afirmarlo:
   > **(i) Acción impedida:** marcar `REQ-025` como `completado` — clase **cerrar**. **Regla que lo
   > impide:** `guard-completado`, que deniega el cierre con un hallazgo de clase `contrato`
   > abierto (`AGENTS.md` §6 y §13). **(ii) Parte afectada:** la entrega 1 de REQ-025; **no**
   > impide implementar ni probar, y ni la entrega 1b ni REQ-028 dependen de ello. **(iii)
   > Evidencia:** `requirements/REQ-025.md:619`, `:905` y `:931`, contra `AGENTS.md` regla 4 y
   > `docs/decisions/ADR-008-…md` §«Adenda». **(iv) Qué lo resuelve:** tres correcciones del
   > `analista-requerimientos` (§9).

   En R-1 **no existía palabra** para la primera línea. Ahora se escribe sin adivinar. Eso es
   exactamente lo que el hallazgo pedía.

---

## 4. QA-025-03 — RESUELTO

**Qué contrataba:** que los agentes remitieran en vez de copiar, sin perder facultades.

**Las nueve sedes del inventario de R-1** pasan a **referencia + una línea de conducta**. `grep`
del vocabulario, de los cuatro elementos, de los tres términos de la clasificación, de la forma de
la regla 4 y del «ni por cambio de rol…»: **cero** copias fuera de la sede y su gemela. El desfase
que medí en R-1 —`qa-tester.md` diciendo «abre sólo lo que **bloquea**» donde la sede dice «abre
sólo lo que **depende de ella**»— **desaparece con la frase entera**.

**Falsación 1 — ¿las referencias apuntan a algo que existe?** Seguí las **nueve** contra el texto
de `AGENTS.md` de esta misma cabeza. Las nueve resuelven: «Mecanismo de gate» existe y dice lo que
se cita; la regla 2 contiene las clases, los cuatro elementos y el «no se rodea» que el auditor ya
no copia; la regla 3 y «Loop de error» contienen los tres términos, la discrepancia, «mover el
hallazgo de sitio no lo resuelve» y «La ubicación no es un veredicto»; la regla 4 contiene el
efecto concreto que la urgencia del auditor cita; «Loop de error» y la regla 5 contienen el tope y
su no-reinicio. **Ninguna referencia cuelga dentro de este repositorio.** (Fuera de él, §8.)

**Falsación 2 — ¿qué desapareció de los agentes, y adónde fue?** Inventario del texto retirado:
«nadie puede rodearlo con otro REQ, una reclasificación ni un cambio de herramienta» (auditor) →
regla 2 ✓ · «lo que quede fuera se registra con responsable» (auditor) → «Loop de error» ✓ ·
«mover el hallazgo de sitio no permite cerrar» (QA) → «Loop de error» ✓ · «implementar y probar no
se detienen por ella» (QA) → «Mecanismo de gate» ✓ · «(por defecto 3)» y el no-reinicio (QA) →
«Loop de error» y regla 5 ✓. **Nada se perdió; todo se movió a un sitio que lo dice.**

**Falsación 3 — los leí enteros como si fuera a trabajar con ellos mañana.** Ninguna frase me quita
algo ni me confunde. Al contrario, dos **ganan**: el auditor recibe «**tu veto conserva el alcance
que tú le das** y puede impedir más de una acción», que es más de lo que tenía; y los dos reciben
«**no lo des por zanjado**» ante una clasificación con la que no estén de acuerdo, que es la
discrepancia dicha en imperativo y no en tercera persona. **Ningún agente pierde una facultad**
(CA-15 punto 4, CA-14 punto 1).

---

## 5. QA-025-04 — RESUELTO

«Trazabilidad» enlaza `ADR-008` por ruta relativa, y el conflicto (a) lo enlaza también (`:807`).
La celda «Pendiente de crear y enlazar aquí» queda **cerrada por la fila nueva del Historial**, que
conserva la anterior sin reescribirla (`AGENTS.md` §9, como debe). **El enlace existe.** Lo que lo
acompaña, no — eso es QA-025-07, que es otro defecto.

**ADR-008 — sin contradicción residual, comprobado.** El párrafo derivado de (a) **no se
reescribe**: queda sellado con «las dos primeras piezas de este párrafo quedaron corregidas el
2026-09-21… la adenda del final de esta sección **manda sobre ellas**… Las tres fronteras siguen
vigentes». La adenda lleva fecha, causa (`QA-025-02`), la decisión literal del propietario, qué
cambia en la sede punto por punto y **qué NO cambia**. Busqué una tercera sede del ADR que siguiera
afirmando cuatro clases: las demás apariciones están **dentro** de la adenda o son la cita literal
del propietario en la decisión (a), que es historia y se conserva. **Conforme.**

---

## 6. QA-025-06 y OBS-A — RESUELTOS

**QA-025-06.** `grep "REQ-[0-9]" templates/AGENTS.md.tpl` → las **siete** citas a `REQ-025` han
desaparecido (**7 → 0**). El puntero es ahora por propiedad: «el requerimiento del arnés que las
introdujo —el que nombra la entrada del `CHANGELOG` del plugin que las publicó—», que un proyecto
consumidor **sí** puede seguir. Y «el libro de comisiones» pasa a «la **bitácora de comisiones**
que el proyecto lleve —y si no lleva ninguna, la entrada del `CHANGELOG.md` de esa comisión—».

**¿Puede un proyecto recién inicializado cumplir la regla 6? Sí, comprobado:** `arnes-init` deja
`CHANGELOG.md` (existe `templates/CHANGELOG.md.tpl`), que es la rama de respaldo que la regla
nombra; y la regla añade «para esto **no** se abre ninguna columna, tablero ni medidor nuevo», así
que tampoco exige crear el artefacto. **Cumplible desde el minuto cero.**

**Los identificadores que quedan en la plantilla son preexistentes: confirmado.** `SEC-020`
(`:303`), `ADR-005`/`ADR-003` (`:568`, ejemplo de patrón), `REQ-015` (`:735`) y el `ArnesJuan` del
comentario HTML de `:39`. Los cuatro están **fuera** de los bloques que esta entrega escribe y
ninguno entró por ella. **La explicación del desarrollador se confirma.** Que sigan ahí es otra
cuestión → **OBS-D**.

**OBS-A.** Las dos sedes pasan a «impide la acción de clase «cerrar» —**marcar cualquier REQ como
`completado`**, la transición de su campo `Estado:` a ese valor—, porque la regla que lo impide,
`guard-completado`, no distingue un ejemplo de una pendiente». Acción concreta **y** regla, que es
la forma que la regla 2 pide, **y un porqué que antes no estaba**. Conforme.

---

## 7. Gemelas, alcance y puertas de la vuelta 2

| Comprobación | Resultado |
|---|---|
| Líneas cambiadas de `AGENTS.md` ↔ `templates/AGENTS.md.tpl` | **Sin una sola diferencia** (`diff` de los dos conjuntos de líneas `+/-`: vacío) |
| Bloque de las seis reglas completo (`AGENTS.md:404-497` ↔ `tpl:370-463`) | **Idéntico byte a byte** |
| «Gates» + «Mecanismo de gate» | Idénticos salvo `{{GATES_HUMANOS}}`, marcador **preexistente** |
| `PENDING_APPROVAL.md` ↔ `.tpl` | Bloque nuevo idéntico |
| Archivos de mecanismo en el delta | **0** — `hooks/`, `tools/`, `tests/`, `.github/`, `.arnes/`, `.claude-plugin/` sin una línea |
| Quality gates de `AGENTS.md` §7 | **3 de 3 en verde**, corridas por mí sobre `0558a0e` |
| CA-15 punto 9: ¿algún artefacto declara REQ-025 terminado? | **Ninguno.** Revisados `CHANGELOG.md`, `ADR-008`, `requirements/README.md`, `skills/arnes-upgrade/SKILL.md` y `docs/ESTADO.md`. El índice dice expresamente «aprobar la entrega 1 no completa REQ-025» y marca `en-progreso` |

**`skills/arnes-upgrade/SKILL.md` no se tocó en la vuelta 2 — comprobé si quedó desfasado. No.** La
entrada «Hacia 1.35.0» describe el cambio a un nivel que sobrevive a la opción B («nombra la
**transición exacta** que queda impedida, **marcar un REQ como `completado`**, y dice qué sigue
permitido») y **no menciona** el vocabulario de cuatro clases en ninguna parte (`grep`: cero).
Tampoco cita `REQ-025` (cero en todo `skills/`). **No hay sede desfasada ahí**, que es lo que
buscaba.

**OBS-B sigue abierta y sin cambio:** `§14` no está en esta base (`AGENTS.md` de `0558a0e` llega a
`## 13`), así que no es hallazgo contra este árbol; el aviso de fusión de R-1 sigue en pie y ahora
se le suma OBS-E, que vive en la misma §6.

*(Nota operativa, fuera de hallazgo: `docs/ESTADO.md:260` muestra `REQ-025 | borrador | pendiente`,
bloque derivado de una parada anterior. No está en el delta y se regenera solo; conviene que lo
haga antes del push para que no viaje una tabla que dice `borrador`.)*

---

## 8. OBS-C — abierta, agravada, y con remedio que no rompe CA-15 punto 1

**Qué cambió respecto de R-1.** En R-1 los agentes llevaban el contenido **dentro** —que era el
defecto QA-025-03—, así que un proyecto con el plugin actualizado y el `AGENTS.md` **sin migrar**
seguía teniendo la sustancia. Ahora llevan **sólo la referencia**. Y los agentes **no** llegan por
`arnes-upgrade`: llegan **al actualizar el plugin**, como dice la propia entrada «Hacia 1.35.0»
(«Sus definiciones llegan con las referencias a esta sede **al actualizar el plugin**, no por esta
migración»). Existe por tanto una ventana real: **plugin actualizado + `AGENTS.md` congelado = nueve
referencias que apuntan a un `§6` sin reglas.**

**Qué NO es.** No es pérdida de facultad: el poder de veto, el `con-hallazgos`, la clase del
hallazgo y el `Estado: bloqueado` siguen escritos **antes** de cada referencia. Lo que cuelga es la
**forma** nueva, no la potestad.

**Por qué no la abro como hallazgo.** Porque es la consecuencia directa de la decisión 3 del
propietario («los agentes deben remitir a la sede normativa») y de CA-15 punto 1, y la alternativa
—copiar— es el `contrato` que yo misma abrí. Documentar no es remediar, pero abrir un hallazgo
contra una decisión **cumplida** tampoco lo es.

**Remedio propuesto, y no es una copia** (una línea por agente; conducta, no contenido): *«Si el
`AGENTS.md` de tu proyecto todavía no tiene ese bloque, dilo en tu informe y declara el alcance con
tus propias palabras: la obligación de declararlo no depende de que la sede esté migrada.»* Cierra
la ventana sin transcribir ni una de las seis reglas. **Dueño: `desarrollador`. Vencimiento: antes
de publicar 1.35.0**, porque publicar es el acto que abre la ventana.

---

## 9. QA-025-07 — `contrato` · El REQ afirma, en presente y en secciones vivas, tres cosas falsas sobre su propio árbol

**Cómo nace.** La vuelta 2 fue **dos commits**: el analista escribió el contrato (`faf0db7`)
describiendo lo que **aún faltaba**, y el desarrollador lo hizo (`e566739`). Las frases que
describían ese pendiente **no se actualizaron**, y ahora dicen que falta lo que está hecho.

| Sede | Lo que afirma | Lo que hay en `0558a0e` |
|---|---|---|
| `requirements/REQ-025.md:619` — tabla **«Qué criterios la miden»**, sección **viva** | «El caso medido (la pregunta encolada de madrugada)… **hoy falta en la sede**: es trabajo de la entrega 1» | **Está en la sede.** `AGENTS.md`, regla 4: «**Caso medido:** una pregunta encolada **de madrugada** dejó parado trabajo que nada impedía» (y en la gemela) |
| `requirements/REQ-025.md:931` — **«Trazabilidad»**, sección **viva** | «*(Su texto se escribió con el vocabulario de **cuatro** clases y **no** recoge todavía la **opción B**…: la actualización va con la reparación de la sede)*» | **`ADR-008` la recoge.** Tiene §«Adenda — corregido el 2026-09-21… **cinco** clases de acción» con la decisión literal, y el párrafo antiguo queda sellado con «la adenda **manda sobre ellas**» |
| `requirements/REQ-025.md:905` — «Preguntas abiertas / conflictos» (f), «Lo que NO hace» | «la sede, su gemela, los tres agentes y las dos cabeceras de la cola **siguen** con el vocabulario de cuatro clases» | **Ninguno lo hace.** `grep` del vocabulario de cuatro en sede normativa: **cero** |

**Pasos de reproducción.**
```
sed -n '619p;905p;931p' requirements/REQ-025.md                  # las tres afirmaciones
grep -n "Caso medido" AGENTS.md                                  # la regla 4 lo tiene
grep -n "Adenda — corregido" docs/decisions/ADR-008-*.md         # el ADR lo recoge
grep -rn "implementar · probar · aprobar · publicar" AGENTS.md templates/ agents/ skills/   # → 0
```

**Esperado vs. observado.** Esperado: las secciones vivas del REQ describen el árbol que hay.
Observado: dos de ellas —la tabla que decide **qué mide la entrega 1** y la **Trazabilidad**—
dicen que falta trabajo que está hecho, y una tercera lo repite en el log de decisiones.

**Por qué `contrato`.** Es la definición literal: *el REQ afirma algo falso sobre lo construido*. Y
el efecto no es cosmético: **el siguiente en la cola es el `auditor-seguridad`**, y la Trazabilidad
le dice que el ADR que acredita el cambio de fondo contradice la decisión del propietario. O lo
cree y gasta una comisión, o desconfía del REQ entero. Las dos salidas son caras. Y `:619` es la
tabla que él usará para saber qué mide esta entrega.

**Por qué no lo escondo dentro de QA-025-04.** Porque QA-025-04 contrataba el **enlace**, y el
enlace está. Reusar su identificador para un defecto distinto sería reclasificar. **Y abrirlo con
número propio no reinicia ningún contador:** vamos por la vuelta **2 de 3** de la entrega, y ahí
seguimos.

**Qué lo resuelve.** Tres frases del `analista-requerimientos` — el Historial y la Trazabilidad son
suyos (`AGENTS.md` §9). **Y el barrido se define por propiedad, no por mi tabla de tres:** *toda
frase de `requirements/REQ-025.md` fuera de la sección «Historial de cambios» que describa en
presente un artefacto de la entrega 1 como pendiente, desfasado o por hacer*. Las filas del
**Historial** quedan fuera a propósito: son el registro congelado de un cambio y no se reescriben.

---

## 10. Observaciones nuevas

- **OBS-D — identificadores del autoalojamiento PREEXISTENTES en artefactos distribuidos.** La
  decisión 3 del propietario («las plantillas distribuidas no deben depender de identificadores ni
  documentos exclusivos del autoalojamiento») **se cumple para lo que esta entrega escribió** (7 →
  0). Fuera del delta siguen: `templates/AGENTS.md.tpl` con `SEC-020` (`:303`), `ADR-005`/`ADR-003`
  (`:568`) y `REQ-015` (`:735`); y `templates/requirements-README.md.tpl` con `SEC-014`, `SEC-020`,
  `SEC-121`/`SEC-144` y `REQ-016`. **Es hallazgo independiente, no de esta entrega** (regla 3):
  ninguno entró por ella, y repararlos «ya que estamos» sería la ampliación de alcance que la regla
  prohíbe. **Se registra con responsable: coordinadora**, para presentarlo al propietario junto con
  el alcance de 1.35.0. Forzador observable: número de identificadores propios de este repositorio
  en `templates/`, hoy **9**; **operativo**, dirección admitida hacia abajo.

- **OBS-E — las dos «roces» que el desarrollador señaló: DECIDIDAS, y las dos son observación.**
  - **`AGENTS.md:278` y su gemela** —«La comprobación se suma a las que ya hace antes de cualquier
    encargo —qué resultado exacto debe entregar, qué queda fuera, cuándo detenerse—»— enumera
    **tres de los cuatro** elementos de la regla 1 (falta «qué criterios de aceptación le aplican,
    citados por su identificador»). **Observación:** está redactada como **retro-referencia** («las
    que ya hace») y no legisla nada distinto. Es incompletitud, no divergencia.
  - **`AGENTS.md:274` y su gemela** —«**Los contadores no se reinician.** Una reparación que vuelve
    **al mismo agente** gasta vuelta, se llame como se llame, y agotar vueltas nunca equivale a
    aprobar»— es la que más cerca está de ser hallazgo: la sede dice que el contador es **del
    defecto o de la entrega** y que no se reinicia «ni por cambio de rol», mientras que «vuelve al
    mismo agente» **admite** la lectura estrecha de que ir a otro agente no gasta vuelta — la
    dirección **que abre**, justo la que CA-15 punto 1 teme. **La dejo en observación por dos
    razones, y las digo para que se puedan discutir:** (1) esa lectura es poco caritativa —el
    énfasis de la frase está en «se llame como se llame»—; y (2), sobre todo, **las dos viven en la
    misma §6 que la sede**, a la vista del mismo lector, que es una situación materialmente
    distinta de la que CA-15 punto 1 contrata —«ningún **agente, plantilla, skill ni documento**
    copia su texto»—: el daño que ese criterio nombra es el de **dos lectores que no se ven**.
  - **Remedio de las dos, barato:** sustituir las enumeraciones por un puntero a la regla 1 y a la
    regla 5. **Dueño: `desarrollador`. Vencimiento: antes de publicar 1.35.0.** Y lo digo sin
    ambigüedad: **si el `auditor-seguridad` lee `:274` como narrowing, tiene razón y esto se
    convierte en hallazgo** — no pienso defender la clasificación contra evidencia mejor.

- **OBS-F — una acción que el vocabulario de cinco todavía no nombra bien: el orden de fases.** El
  bloqueo «seguridad no firma antes que QA» impide **escribir un veredicto**, que no es
  *implementar*, ni *probar*, ni *publicar*, ni *cerrar* (bloquea la firma, no la transición), ni
  *aprobar* tal como la regla 2 lo define ahora (**sólo** la aprobación humana normativa). **No lo
  abro como hallazgo, y la razón es de fondo, no de cortesía:** la opción B convirtió las clases en
  **descriptivas** y puso el peso del contrato en «la **acción concreta** y la **regla que la
  impide**», que siempre se puede escribir; y **ningún agente recibe por esto una instrucción
  inejecutable** —el orden de fases es una regla permanente que nadie declara como bloqueo—, que
  era el daño concreto de QA-025-02. Queda anotado para que, si algún día hay que declararlo, se
  sepa que la clase se elegirá por aproximación y no por definición.

---

## 11. Lo que la vuelta 2 NO acredita

Se mantiene lo de R-1, con tres precisiones:

1. **El ensayo S1…S4 de CA-11 punto 3 sigue sin ejecutar.** Lo lanza la coordinadora; lo acredito
   en otra comisión. **CA-11 punto 3 sigue sin satisfacer, y CA-13 lo exige para cerrar.** Lo único
   que cambió es que su resultado esperado de S1 ya usa la clase «cerrar» (`0558a0e`).
2. **El banco completo NO se re-corrió**, por instrucción. Mi medida vigente es la de R-1 (3 de 3
   con un FAIL de presupuesto de tiempo). Las **22 secciones por ruta** que corrió el desarrollador
   (434 · 0) **no las he verificado y no las hago mías**: no son el banco completo y no había
   salida que releer. **CA-13 queda pendiente de la corrida en CI.**
3. Sigue sin acreditarse **la conducta** —las seis reglas son disciplina declarada y ninguna puerta
   las comprueba—, **nada sobre los proyectos consumidores**, ningún **ahorro** ni determinismo, y
   **la revisión de seguridad**, que firma después de mí sobre el árbol validado.
4. **Y lo que ninguna vuelta puede acreditar por sí sola:** que aprobar la entrega 1 termine
   REQ-025. No lo termina — **CA-15 punto 9**, y está bien que lo diga el propio contrato.

---

## 12. Cierre de la vuelta 2

**`QA: con-hallazgos (R-2, 2026-09-21, sobre `0558a0e`)`.** Queda **un `contrato`** abierto
(QA-025-07), que bloquea, y **un `instrumento`** (QA-025-05), que no bloquea por su clase aunque
CA-13 siga esperando la corrida verde de CI.

**Lo que la vuelta 2 consiguió, dicho sin rebajarlo:** cinco hallazgos de seis y una observación,
cerrados; el vocabulario pasa de no poder nombrar el bloqueo más común a que yo misma lo escriba
sin adivinar; las nueve copias de `agents/` desaparecen **sin que nadie pierda una facultad** —dos
ganan—; la plantilla deja de arrastrar este repositorio y la regla 6 es cumplible por un proyecto
recién inicializado; y el contrato deja de afirmar que mide lo que no medía, **conservando** lo
diferido en vez de enterrarlo. Las tres frases de QA-025-07 no revierten nada de eso: son el
residuo de haber hecho la vuelta en dos commits.

**Y una advertencia de calendario, que es lo más accionable de esta adenda: queda UNA vuelta.**
Agotada la tercera, `AGENTS.md` §6 sólo deja dos salidas y las dos son terminales —cerrar con el
residual declarado, o `bloqueado` y escalar—. Por eso, si se despacha la vuelta 3, **que lleve todo
lo que se quiera arreglar en esta ventana**, no sólo QA-025-07:

| Candidato para la vuelta 3 | Dueño | Coste |
|---|---|---|
| **QA-025-07** — obligatorio, bloquea | `analista-requerimientos` | 3 frases |
| **OBS-C** — línea de conducta en los tres agentes para la ventana «plugin actualizado, `AGENTS.md` congelado» | `desarrollador` | 3 líneas |
| **OBS-E** — punteros en `AGENTS.md:274` y `:278` y sus gemelas | `desarrollador` | 4 líneas |
| *(no)* **OBS-D**, **QA-025-05**, **OBS-B**, entrega 1b, REQ-028 | — | Independientes: **registrados con responsable, fuera de esta entrega** (regla 3) |

**Sobre la clasificación de la coordinadora: no discrepo de ninguna.** La de R-1 llevó QA-025-01 y
QA-025-02 al propietario, que es lo que yo pedí, y la discrepancia que dejé declarada no llegó a
hacer falta. Si en la vuelta 3 QA-025-07 se clasificara como **independiente**, entonces sí
discreparía —es texto que **esta entrega** escribió, en la sección que decide qué mide la entrega—,
pero no doy por hecho que vaya a ocurrir.
