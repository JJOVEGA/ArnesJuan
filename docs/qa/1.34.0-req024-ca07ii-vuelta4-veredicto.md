# `REQ-024 CA-07 (ii)` — QA acotada, **VUELTA 2** (dentro de la **vuelta 4 de `REQ-024`**)

**Veredicto: `con-hallazgos`.** Las **cuatro** comprobaciones del propietario: **1 ✔ · 2 ✔ · 3 ✔ ·
4 ✘**. Lo que falla es **sólo** la fidelidad del contrato: el criterio afirma sobre la **unanimidad**
y sobre el **suelo de detección** dos cosas que la medición desmiente. El **mecanismo** cumple la
condición vigente: el caso conocido de `1,28×` **queda bloqueado**, y el bloqueo **llega hasta el
job**.

**Numeración:** ésta es la **vuelta 2** de la QA acotada; el contador §6 de `REQ-024` **no se
reinicia** y sigue en la **vuelta 4**, autorizada expresamente por el propietario el 2026-09-11.

**Versión base:** `ab3e4cb`, rama `feat/1.34-reparaciones-astra`.
**Evidencia (25 corridas, calibraciones y método):**
`docs/qa/evidencia-req-024-ca07ii-ab3e4cb/` — `00-metodo-y-base.md` lleva el inventario
**elemento por elemento** y el procedimiento para re-derivar cada cifra.
**Nada se escribió fuera de `docs/qa/`.** No se tocó `hooks/`, `tests/`, `requirements/`, ni se
comiteó nada.

---

## La condición que rige, y cómo se leyó

El propietario modificó expresamente su condición el 2026-09-11: **ya no se exige demostrar
detección; se exige demostrar bloqueo.** Un `FAIL` por regresión y un `FAIL` por «no se pudo
acreditar» la satisfacen **los dos**, siempre que impidan integrar. El techo `1,250×` se mantiene y
**no se autoriza elevarlo** a `1,302×` ni a ninguna otra cifra observada. Nada de este informe
propone moverlo, y donde la medición podría leerse como presión para subirlo se dice que **no lo es**.

---

## Comprobación 1 — «el caso conocido de `1,28×` queda bloqueado» ✔

**Antes de poder comprobarlo hubo que reconstruir el caso, porque la receta ya no vale.** La
vuelta 1 lo fijó por **receta** (`QA-024-21`: insertar `for ((_qa=0;_qa<1500;_qa++))`, «la razón
verdadera ≈1,28×»). Medido hoy con un instrumento **independiente de la puerta** —mínimo de `k=8`
sobre **50 series entrelazadas** por árbol, la doctrina del propio arnés— y sobre **el mismo
`hooks/`** (sin cambios desde `69fc96a`, comprobado con `git diff --stat`):

| árbol | razón contra `v1.33.0` |
|---|---|
| candidato limpio | **1,107×** |
| candidato +1500 | **1,177×** ← la receta de `QA-024-21` |
| candidato +3400 | **1,255×** |
| candidato **+3700** | **1,274×** y **1,283×** (dos calibraciones) ← **éste es el caso de 1,28×** |
| candidato +5000 | **1,342×** |

La receta de `+1500` produce un árbol **conforme** (1,177× ≤ 1,250×), no el caso de `1,28×`. La
cifra de `QA-024-21` estaba mal medida: eso es un defecto **mío**, va como `QA-024-28`, y se corrige
aquí en vez de arrastrarlo.

**Sobre el caso de `1,28×` reconstruido por MAGNITUD (`+3700`), 10 corridas del banco:**

> **10 de 10 → `FAIL` por REGRESIÓN, `rc 1`, en el PRIMER intento, sin un solo reintento.**
> Las 40 razones publicadas caen en **1,329× – 1,440×**, todas por encima del techo, unánimes.

Ninguna corrida se descartó. **Interpretación por magnitud y no por receta**, y se razona: el
propietario habla de «una regresión conocida **de 1,28×**» —una magnitud—; `FACTOR07` del caso es
una magnitud (`1280‰`); y los rojos reales de CI que motivaron todo esto eran magnitudes
(`1,257×`–`1,338×`). Si se leyera literalmente la receta `+1500`, lo que se estaría exigiendo es
bloquear un árbol **conforme**, que es lo contrario de lo que el criterio contrata.

**Y lo que la misma campaña dice del árbol conforme `+1500` — se reporta porque no seleccionar
sólo lo favorable incluye esto:** 12 corridas sobre el **mismo árbol sin cambiar nada** dieron
**7 `PASS`** (correcto: 1,177× ≤ 1,250×), **4 `FAIL` «no se pudo acreditar»** y **1 `FAIL` por
REGRESIÓN** que afirma «es regresión, no ruido» sobre un árbol que no la tiene. El bloqueo de más
es conservador y no contradice la condición del propietario; la **afirmación** falsa sí es un
hallazgo (`QA-024-25`).

---

## Comprobación 2 — «fallo efectivo y la puerta obligatoria impide integrar» ✔

*«No basta con imprimir `FAIL` si el comando o workflow termina en éxito.»* La cadena se siguió
**eslabón por eslabón, ejecutándola**, no leyendo el código.

| eslabón | cómo se comprobó | resultado |
|---|---|---|
| el caso emite `FAIL` por **regresión** | 11 corridas (`inj3700-01..10`, `inj1500-01`) | línea `  FAIL  …` emitida |
| el caso emite `FAIL` **«no se pudo acreditar»** | 6 corridas (`inj1500-02/04/08/09`, `sinbase-sinpalanca`, `sinbase-conpalanca`) | línea `  FAIL  …` emitida |
| `run.sh` recuenta ese `FAIL` (sale del **texto**, no de variables: la sección corre en su propio subshell) | las 17 corridas imprimen `Resultado: 5 PASS, 1 FAIL` | contado |
| `run.sh` sale con **`rc ≠ 0`** | `rc` capturado en las 25 corridas | **`rc 1` en 17 de 17 `FAIL`; `rc 0` en 8 de 8 `PASS`** |
| el cuadre no lo tapa ni lo convierte en `ABORT` | 6 de 6 casos en las 25 corridas, cero `ABORT` | correcto |
| el paso `run: bash tests/escenarios/hooks/run.sh` falla | `.github/workflows/banco.yml:92`; **no hay** `continue-on-error`, `\|\| true` ni `if:` en todo el workflow (comprobado con `grep`) | — |
| el job `hooks-en-linux` queda en `failure` | **medido en CI, cinco veces, por ESTE mismo caso**: `docs/arnes/ci-1.34.0-no-discrimina/00-metodo-y-base.md` registra `67b06fe` → **failure** y `0caeac5` → **failure**, en ambos casos con el único `FAIL` de la corrida siendo `REQ-024 CA-07 (ii)` | **failure** |

**Por qué esto acredita el tramo que no corrí yo, y no es una deducción.** La parte
`línea FAIL → rc → paso → job` ya está **medida en este repositorio**: el mismo caso emitiendo
`FAIL` puso `hooks-en-linux` en `failure` dos veces con el resto de la corrida en verde. Y el tramo
del corredor que hace ese trabajo **no se tocó en esta reparación**:
`git diff 0caeac5..HEAD -- tests/escenarios/hooks/run.sh` sólo cambia `CASOS_ESPERADOS` (1090 →
1095) y comentarios; el `awk` que cuenta `/^  FAIL /` y la última línea
`[ "$FAIL" -eq 0 ] && [ "$PROBLEMAS" -eq 0 ]` son **idénticos byte a byte** a los de la corrida que
salió roja. El único `trap` de `run.sh` es de `EXIT` y no ejecuta `exit`, así que no reescribe el
código de salida — y eso no se dedujo: `rc 1` se capturó 17 veces.

**Lo que esto NO acredita:** que el mensaje **nuevo** («no se pudo acreditar») haya salido alguna
vez en el runner. No ha salido: en el runner el caso pasó. Pero el eslabón que el mensaje nuevo
recorre es el **mismo** —imprime `  FAIL  …` por el mismo camino, lo cuenta el mismo `awk` y lo
resuelve la misma última línea—, y eso sí está medido localmente 6 veces.

---

## Comprobación 3 — «el candidato sin regresión obtiene una acreditación válida, no un verde por `SKIP`» ✔

**Reutilizada, no re-medida**, como pidió el propietario. `hooks-en-linux` **success** sobre
`f2b74d9` (2m11s, 1084 PASS · 0 FAIL · 11 SKIP = 1095, cuadre exacto), con la línea:

```
PASS  REQ-024 CA-07 (ii) … máx(r) 1.212× <= techo en las 4 razones: 1.212× 1.193× 1.163× 1.172×
      · recorrido 1.042× · techo 1.250× · suelo de detección 1.302× · peor convergencia 1.018×
      · k=8 r=30 · carga 5.03 · jobs 6
```

**Verificado que es acreditación y no `SKIP`**, punto por punto: (a) la línea empieza por `PASS`,
no por `SKIP`; (b) publica **cuatro razones reales** con sus valores, no un motivo de abstención;
(c) **no hay ni una línea `(reintento)`** en el extracto, o sea que resolvió **al primer intento**,
sin pasar por la escalada; (d) el caso **no** figura entre los 11 `SKIP` de esa corrida; (e) el
cuadre total sale exacto (1095), así que el caso no se perdió por el camino. Reproducido localmente
sobre el candidato limpio: `PASS máx(r) 1.140×`, `rc 0`, 6 de 6 casos (`base-01.txt`).

**Margen, dicho sin adornarlo:** la peor razón del runner (`1,212×`) deja **3,1 %** de holgura bajo
el techo, y el propio recorrido de esa corrida (`1,042×`) es **mayor** que esa holgura. La
acreditación es **válida** —es lo que la comprobación pide— pero **no** demuestra que el coste
verdadero tenga margen: demuestra que esa medición cayó bajo el techo. Se dice porque presentarla
como margen sería exactamente lo que el propietario prohibió.

---

## Comprobación 4 — «el contrato describe fielmente lo implementado» ✘

**Lo que SÍ cumple, verificado uno por uno:**

- **Distingue incumplimiento demostrado de imposibilidad de acreditar.** El criterio lo escribe
  («Los dos `FAIL` no significan lo mismo…»), y **los dos mensajes se ejecutaron**: el de regresión
  dice «es regresión, no ruido»; el otro dice «Esto NO afirma que haya regresión: afirma que esta
  puerta no pudo MEDIRLO». No se confunden.
- **No presenta las corridas como garantía universal.** El criterio declara explícitamente que
  **no está demostrado** que la puerta detecte `1,28×` con sus mandos, y declara el suelo de
  detección como incertidumbre del método y no como rebaja del techo.
  **Y NO recomiendo cambiar esa frase** aunque hoy haya medido 10/10 bloqueos a `1,28×`: un host de
  12 núcleos en reposo no demuestra una propiedad del runner, y convertir mi campaña en «queda
  demostrado» sería el error que el propietario nombró.
- **El techo `1,250×` está intacto** en las cinco sedes; ninguna cifra observada se convirtió en
  límite nuevo; no hay `continue-on-error`; el caso sigue dentro de la puerta requerida.
- **Las tres formas prohibidas por `requirements/README.md`:** la enumeración de causas de
  abstención va marcada **«declaradamente NO exhaustivos»** con el puntero al sitio único (el código
  del caso) ✔; los números llevan su clase (`1,25×`, `2,2`, `0 procesos`, `3 intentos` → **operativo**
  con dirección; `k`/`r` → **operativas, «no constituyen contrato»**) ✔; y todos se enuncian como
  **techo**, nunca como igualdad ✔.

**Lo que NO cumple:** dos afirmaciones del criterio sobre el mecanismo están desmentidas por la
medición — `QA-024-25` (la unanimidad) y `QA-024-26` (el suelo de detección). Van abajo.

---

## Hallazgos de la vuelta 1: qué cierro y qué no

| hallazgo | clase | estado | por qué |
|---|---|---|---|
| `QA-024-20` | `contrato` | **CERRADO** | remediación verificada en las **cinco** sedes |
| `QA-024-21` | `instrumento` | **CERRADO** | no reproduce; lo que queda de él se abre de nuevo, medido, como `QA-024-25` y `QA-024-28` |
| `QA-024-22` | `instrumento` | **SIGUE ABIERTO** | el arreglo cayó en una rama que la puerta **no recorre** |
| `QA-024-23` | `instrumento` | **SIGUE ABIERTO** | no reproduce en este host, pero la propiedad no está cerrada |
| `QA-024-24` | `instrumento` | **CERRADO** | `Archivos:` ya lo declara |

### `QA-024-20` (`contrato`) — **CERRADO**, con la verificación de las cinco sedes

El write-back del analista (`ab3e4cb`) debía corregir **cuatro** sedes que yo nombré más **una
quinta** que él añadió por la regla de corregir la promesa completa. Verificadas leyendo el árbol:

1. **El criterio** (`REQ-024:386-391`): ya no dice «el mínimo de k repeticiones … no más de 1,25×».
   Dice **qué** se mide (varias razones del par intercalado, cociente de **mínimos**, convergencia
   por brazo) y **con qué regla** decide (**unanimidad**), y declara sus **tres** salidas con
   «Una medición inconclusa **NUNCA** es `PASS`». ✔
2. **La doctrina transcrita cuatro líneas más abajo** (`:405-407`): ya no enumera; dice «una sonda
   que **no resuelve el factor que vigila** —cualquiera que sea el motivo— **no emite PASS**». ✔
3. **La salida contratada** (`:483-511`): la abstención se enuncia como **propiedad decidible** («no
   puede afirmar la unanimidad de sus razones respecto del techo, incluido que alguna no llegue a
   existir»), con las causas como ejemplos **declaradamente no exhaustivos**; y lleva la **cota** que
   faltaba (3 intentos, operativo; agotados → `FAIL` «no se pudo acreditar», **nunca `SKIP` verde**)
   más la cota entre corridas del criterio hermano **con su limitación declarada en la misma sede**. ✔
4. **La nota 1 de «Qué queda SIN VERIFICAR»** (`:1212-1219`): **deja de transcribir** y remite al
   criterio como **sede única**, diciendo por qué («una transcripción se desfasa de su original»), y
   añade la actualización fechada del 2026-09-11. ✔
5. **El sitio único** (`requirements/README.md`, § «La abstención se enuncia por PROPIEDAD»): ya no
   enumera dos causas; enuncia la propiedad y marca los ejemplos como no exhaustivos con la sede en
   el código de cada sonda. ✔

Y la **mitad de máquina** que el hallazgo también pedía: la cabecera de `REQ-024` declara ahora
`QA-024-20 (contrato)` y `QA-024-21/22/23/24 (instrumento)` en `Hallazgos abiertos:`, donde
`guard-completado` los ve. ✔ **Lo cierro yo, que lo abrí, con esta evidencia.**

*Observación que NO abro como hallazgo:* la cota «no más de **1** corrida consecutiva» se **cita**
en `REQ-024` sin repetir su clase. No es la forma prohibida: el texto dice explícitamente que
«sigue rigiendo la cota del **criterio hermano**», y allí (`REQ-017 CA-08 (ii)`) está declarada
**`operativo`, se baja, suelo 1**. Se anota para que no se descubra dos veces.

### `QA-024-21` (`instrumento`) — **CERRADO**

Decía: «una regresión real de 1,28× cae DENTRO del ruido y sale `SKIP`, `rc 0` en 4 de 5». Hoy,
sobre el mismo `hooks/`:
- a **1,28× de verdad** (`+3700`, calibrado dos veces): **`FAIL` 10 de 10, `rc 1`, primer intento**;
- el caso real **no emite `SKIP` en ninguna de las 25 corridas** —no puede: la rama `SKIP` de
  `veredicto07` es inalcanzable desde él—, así que la forma exacta que el hallazgo describía
  (`SKIP` con `rc 0`) **ya no existe**;
- y la banda ciega que quedaba no está donde el hallazgo la ponía: la puerta **sobre**-estima la
  razón (mediana ~1,375× para un suelo de 1,283×), de modo que su error va hacia **bloquear de
  más**, no hacia dejar pasar.

Lo que sobrevive del hallazgo no es esa formulación sino la **dispersión**, que se abre medida como
`QA-024-25`, y la **cifra mal medida**, que es mía y va como `QA-024-28`.

**Y no lo cierro por el cambio de condición, que es la lectura que hay que descartar.**
`docs/arnes/req-024-ca-07-ii-reparacion/06-decision-del-propietario-techo-y-bloqueo.md` deja escrito
que la decisión del propietario **no cierra `QA-024-21`**, y tiene razón: una condición nueva cambia
contra qué se juzga un hallazgo, nunca lo retira. Lo cierro por una razón **independiente de la
condición**: la formulación del hallazgo —«1,28× sale `SKIP` con `rc 0`»— **no reproduce**, y no
reproduce en sus dos mitades a la vez (a 1,28× de verdad sale `FAIL` 10/10, y el caso real ya no
puede emitir `SKIP`). Cerrarlo sin más **sí** habría sido esconder trabajo, y por eso su sustancia
sale de nuevo medida en `QA-024-25` —la dispersión, que es lo que de verdad había debajo— y en
`QA-024-28` —la cifra—. El recuento de hallazgos abiertos de `REQ-024` **no baja** por este cierre:
`−2` (`QA-024-21`, `QA-024-24`) `−1` (`QA-024-20`) `+4` (`25`, `26`, `27`, `28`).

### `QA-024-22` (`instrumento`, dueño `desarrollador`) — **NO lo cierro**

**El arreglo existe pero está en una rama que la puerta nunca recorre.** El desarrollador hizo
condicional el texto `via` dentro de `veredicto07`, en su rama `*)` → `SKIP`. Esa rama es
**inalcanzable desde el caso real**: el caso llama a `veredicto07` **sólo** cuando `cierre07`
devuelve `emitir`, y `cierre07` devuelve `emitir` **sólo** para `pass|fail`. Confirmado por
ejecución: **0 `SKIP` del caso real en 25 corridas**.

El mensaje que la puerta **sí** emite —el `FAIL` «no se pudo acreditar»— lleva el texto
**incondicional**:

```
Salida: acreditar a demanda con ARNES_COSTE_RUTA_CRITICA=1 sobre ESTE candidato, o medir en un
host menos cargado; NUNCA subir el techo ni relajar la unanimidad
```

**Reproducción (2 s, determinista):** `sinbase-conpalanca.txt`, corrido **con**
`ARNES_COSTE_RUTA_CRITICA=1`. **Esperado:** la vía ofrecida no es la que ya se tomó. **Observado:**
el mismo texto, ofreciendo la palanca que esa corrida ya estaba usando. Es literalmente el defecto
de `QA-024-22`, mudado al mensaje nuevo.

**Segundo punto, menor, mismo dueño:** ese mensaje imprime la máquina **dos veces** —`LISTA07` ya
la lleva y la línea vuelve a concatenar `· $MAQ07`—. Visible en `inj1500-02/04/08/09`:
`… carga 0.82 · jobs 6 · máquina linux-gnu-x86_64-bash5.3 · carga 0.82 · jobs 6`.

### `QA-024-23` (`instrumento`, dueño `desarrollador`) — **NO lo cierro**

**No reproduce, y eso no es lo mismo que estar cerrado.** Con `k=8` la serie del brazo heredado
midió **174–200 ms** en mis calibraciones, o sea **3,5–4× el suelo de 50 ms**, y en las 25 corridas
(carga 0,03–2,34) **no hubo ni una abstención por «serie por debajo del suelo»**. `k=8` lo mitigó.
Pero el suelo sigue siendo una **constante absoluta** y el margen depende del host: en un host
~3,5× más rápido volvería. Se deja abierto con el margen medido y ese forzador escrito, en vez de
cerrarlo afirmando una propiedad que sólo comprobé en una máquina.

### `QA-024-24` (`instrumento`, dueño `analista-requerimientos`) — **CERRADO**

`Archivos:` de `REQ-024` declara ahora
`tests/escenarios/hooks/secciones/40-ausencia-que-abre-7-el-reloj-de-la-ruta-critica.sh`.
Verificado leyendo la cabecera.

---

## Hallazgos NUEVOS de esta vuelta

### `QA-024-25` — `contrato` · dueño `analista-requerimientos` · **bloquea**

**Lo que el REQ afirma y la medición desmiente.** El criterio dice que la unanimidad es «la
definición de **la decisión no depende del ruido**» y que un `FAIL` con `mín(r) > techo` en todas
significa «**así que no lo puso ahí el vecino**» (`REQ-024:387-391` y `:483-485`; transcrito
también en el comentario del caso).

**Medido:** sobre el **mismo árbol, sin cambiar un byte** (candidato `+1500`, suelo **1,177×**,
conforme), **12 corridas consecutivas dieron los TRES veredictos posibles**:

| veredicto | corridas | rc |
|---|---|---|
| `PASS` (correcto) | 7 — `03 05 06 07 10 palanca-1 palanca-2` | 0 |
| `FAIL` «no se pudo acreditar» | 4 — `02 04 08 09` | 1 |
| **`FAIL` por REGRESIÓN** — «es regresión, no ruido» | **1 — `01`**, `mín(r) 1.262×`, razones `1.291× 1.262× 1.309× 1.282×` | 1 |

En `inj1500-01` la unanimidad se cumplió **sobre un árbol conforme**: las cuatro razones cruzaron el
techo a la vez. Luego la unanimidad **no** implica lo que el criterio dice que implica; lo que hay
es una desviación **de corrida entera**, común a las cuatro razones, que la unanimidad no puede ver
porque las cuatro salen de la misma corrida.

**Y la magnitud de esa desviación está medida:** para un suelo fijo de 1,177×, las 48 razones
publicadas por la puerta recorren **1,095× – 1,490×** (mediana ≈1,222×), mientras el `recorrido`
**intra-corrida** que la puerta publica como su dispersión va de **1,017× a 1,219×** (mediana
≈1,04×). La variación **entre** corridas es cerca de **un orden de magnitud** mayor que la que el
instrumento mide **dentro** de una.

**Lo que este hallazgo NO autoriza, y va escrito porque es donde vive la tentación:** no autoriza
**subir el techo** —el propietario lo excluyó expresamente, y además la medición no lo pediría: lo
que está mal caracterizado es la **dispersión**, no el `1,250×`—; no autoriza relajar la unanimidad;
y no autoriza `continue-on-error`. El write-back que hace falta es que el criterio **deje de
afirmar** que la unanimidad excluye el ruido y diga lo que el instrumento realmente garantiza.

**Reproducción:** `docs/qa/evidencia-req-024-ca07ii-ab3e4cb/00-metodo-y-base.md`, bloque `+1500`;
las 12 corridas están en `corridas/inj1500-*.txt`.

### `QA-024-26` — `contrato` · dueño `analista-requerimientos` · **bloquea**

**El «suelo de detección» se publica y no gobierna nada.** El criterio lo presenta como la
**incertidumbre del método**: se deriva de cada medición (`techo × recorrido`), se publica, y su
lectura contratada es «**por debajo de este factor, ESTA corrida no habría podido probar nada**».

**Medido:** en `inj1500-01.txt` la **misma línea** publica `suelo de detección 1.296×` **y** afirma
`es regresión, no ruido` sobre un árbol cuyo suelo verdadero es **1,177×** — es decir, la corrida
afirmó haber probado algo **por debajo de su propio suelo de detección**, en la frase siguiente a
declararlo. Comprobado en el código: `resuelve07` **calcula** `suelo` sólo para el mensaje; ninguna
rama de decisión lo consulta. El número tiene la forma de una guarda y no lo es.

**Las dos salidas son del analista y del desarrollador, no mías:** o el criterio deja de presentar
el suelo como una cota de lo que la corrida puede probar, o el caso deja de afirmar por debajo de
él. **Ninguna de las dos es subir el techo.**

### `QA-024-27` — `instrumento` · dueño `desarrollador` · no bloquea

**La rama `SKIP` de `veredicto07` es código muerto para la puerta, y eso hace que una demostración
verde acredite un camino que nadie recorre.** El caso real no puede emitir `SKIP` (arriba, en
`QA-024-22`). Las demostraciones 1, 2, 4 y 5 ejercen esa rama con entradas sintéticas y salen
verdes, así que el banco **acredita el texto de un `SKIP` que la puerta requerida no imprimirá
jamás**, mientras el mensaje que sí imprime (`FAIL` «no se pudo acreditar») **no tiene ninguna
demostración que compruebe su contenido** — que es exactamente el hueco por el que se perdió
`QA-017-16`. Medido: **0 `SKIP` del caso real en 25 corridas**; y el defecto de `QA-024-22` sobrevivió
justamente en el mensaje sin demostración.

### `QA-024-28` — `instrumento` · dueño `qa-tester` (mío) · no bloquea

**Una cifra mía estaba mal medida y la corrijo en vez de arrastrarla.** `QA-024-21` declaró que
insertar `for ((_qa=0;_qa<1500;_qa++))` produce «la razón verdadera ≈1,28×». Medido hoy con un
instrumento independiente y sobre **el mismo `hooks/`** (sin cambios desde `69fc96a`): ese árbol vale
**1,177×**, y la inyección sólo añade **+6,9 %** sobre el candidato limpio (1,107× → 1,177×). Para
llegar a 1,28× hacen falta **≈3700** iteraciones.

**Consecuencia operativa:** el «caso conocido de `1,28×`» **no se reproduce por receta**; se
reconstruye **calibrando por magnitud** contra la línea base, y la calibración se vuelve a tomar
cada vez que cambie `hooks/` o el host. El procedimiento queda escrito en
`docs/qa/evidencia-req-024-ca07ii-ab3e4cb/00-metodo-y-base.md` con sus dos scripts, para que no haya
que re-derivarlo.

---

## Lo que esta validación NO acredita

1. **No es el runner.** WSL2 de 12 núcleos con carga 0,03–2,34, no 4 vCPU con `ARNES_JOBS=6` y carga
   ~5. Está medido en este repositorio que los dos **no predicen lo mismo**: el runner midió el
   candidato limpio en `1,163×–1,212×` y este host en `1,100×–1,140×`. Los **veredictos** de mi
   campaña son de esta máquina; lo que **no** depende de la máquina es la cadena `FAIL → rc 1` y la
   estructura del código.
2. **Dos instrumentos no coinciden en el valor verdadero, y no resuelvo cuál acierta.** El mío sitúa
   los árboles sistemáticamente **por debajo** del estimador de la puerta (1,177× contra mediana
   1,222×; 1,283× contra mediana 1,375×). **Las conclusiones no dependen de eso**: con **cualquiera**
   de los dos, `+1500` queda **bajo** el techo y `+3700` **por encima**, que es lo único que usan
   las comprobaciones 1 y `QA-024-25`.
3. **No hay prueba de carga de los NFR** más allá de lo que este criterio mide; no estaba en el
   encargo.
4. **Un comportamiento observado que no llegué a demostrar dañino, y lo digo en vez de convertirlo en
   hallazgo:** el bucle de reintento se detiene en la **primera** resolución, y «resolver» incluye
   `PASS`; en 5 de las 12 corridas de `+1500` el verde llegó **por reintento** tras dos intentos
   inconclusos. Sobre un árbol conforme eso es correcto y no produjo ni un veredicto erróneo. Sobre
   un árbol **justo por encima** del techo el mismo mecanismo daría más oportunidades de verde —
   **no lo medí**, y por eso no abro hallazgo. Queda escrito para quien mire `QA-024-25`.

---

## El PR de usar y tirar: **NO lo pido**, y el argumento

La coordinadora ofreció abrir un PR de usar y tirar con la regresión inyectada para ver el job en
CI. **No lo pido**, y no por rutina inversa:

1. **El tramo que el PR aportaría ya está medido, y sobre este mismo caso.** `hooks-en-linux` quedó
   en **`failure`** sobre `67b06fe` y sobre `0caeac5` **por el `FAIL` de `REQ-024 CA-07 (ii)` y por
   nada más** (`docs/arnes/ci-1.34.0-no-discrimina/00-metodo-y-base.md`, con `gh run view
   --log-failed`). Eso es exactamente `línea FAIL → rc → paso → job`, ejecutado en el runner.
2. **El eslabón que va de la línea al `rc` no se tocó en esta reparación.** Comprobado:
   `git diff 0caeac5..HEAD -- tests/escenarios/hooks/run.sh` cambia `CASOS_ESPERADOS` y comentarios,
   nada más; el `awk` que cuenta `/^  FAIL /` y el `[ "$FAIL" -eq 0 ] && [ "$PROBLEMAS" -eq 0 ]`
   final son idénticos a los de aquellas corridas rojas.
3. **El `rc` sí lo medí yo, 17 veces**, en las dos formas de `FAIL` y en cuatro escenarios distintos:
   no queda nada deducido en ese tramo.
4. **Y el PR no cambiaría el veredicto.** Las comprobaciones 1, 2 y 3 ya están acreditadas; la 4
   falla por dos afirmaciones del **texto** del criterio, que ningún runner puede acreditar ni
   desmentir.

**Lo único que el PR añadiría**, y se dice para que la decisión sea del propietario y no mía: saber
si el runner **también** bloquea el caso de `1,28×`. Hay motivo para pensar que sí —allí el
candidato limpio mide `1,163×–1,212×` frente a `1,100×–1,140×` aquí, así que el mismo sobrecoste
quedaría **más** por encima del techo—, pero es una expectativa, no una medición.

### Si el propietario lo quiere, está listo para lanzar

- **Rama:** `tmp/qa-req024-ca07ii-regresion-1.28x` (de usar y tirar, **nunca** sobre
  `feat/1.34-reparaciones-astra`), partiendo de `ab3e4cb`.
- **Diff exacto** — una línea, insertada **tras el shebang** de `hooks/guard-completado.sh`:

  ```diff
  --- a/hooks/guard-completado.sh
  +++ b/hooks/guard-completado.sh
  @@ -1,2 +1,3 @@
   #!/usr/bin/env bash
  +for ((_qa=0;_qa<3700;_qa++)); do :; done
   # guard-completado.sh — Invariantes A2, A3 y anti-deriva sobre la transición de un REQ a `completado`.
  ```

  **`3700` y no `1500`:** `1500` es un árbol **conforme** (1,177×) en este host; `3700` es el caso de
  `1,28×` (1,274×/1,283×, dos calibraciones). En el runner el factor por iteración será otro, así que
  **la corrida debe publicar sus razones** y se lee el número obtenido, no el esperado.
- **Qué espero ver:** el job `hooks-en-linux` en **`failure`**; una sola línea
  `FAIL  REQ-024 CA-07 (ii) el reloj de la ruta crítica…` —por **regresión** si las cuatro razones
  cruzan el techo, o por **«no se pudo acreditar»** si el techo cae dentro del recorrido en los tres
  intentos: **las dos satisfacen la condición**—; el resto de la corrida en `1094 PASS · 1 FAIL · 11
  SKIP` (cuadre 1095); y `rc ≠ 0` en el paso `bash tests/escenarios/hooks/run.sh`.
  **Lo que sería un defecto nuevo y grave:** `PASS`, o `failure` sin que el `FAIL` sea de este caso.
- **Retirar** la rama y el PR después; no se fusiona nada.
