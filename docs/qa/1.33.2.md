# QA acotada — parche v1.33.2 (`QA-P48-01`)

> ## ⚠️ SUPERADO POR LA VUELTA 2 — este informe cubre `a8e332a`, no la cabeza actual
>
> **Qué sigue vigente:** todo lo medido aquí. El ejecutable de `hooks/` es **idéntico** entre
> `a8e332a` y `195ae31` (sólo cambian comentarios; `declare -f` md5-idéntico), así que el
> barrido de ramas, la matriz de puerta, la no-contaminación y el hallazgo `QA-1332-01`
> **conservan su valor**.
>
> **Qué queda superado:** (a) el **veredicto**, que cubría `a8e332a` — el vigente es
> `QA: aprobado (vuelta 2 de 2, cabeza 195ae31)` en `docs/qa/1.33.2-vuelta-2.md`; y (b) el §6,
> porque `29f9af6` **reescribió el enunciado del contrato** y las cuatro afirmaciones que
> verifiqué son las del texto **anterior**. Las del texto vigente se verifican en la vuelta 2 §4.
>
> **Qué NO se toca, y es deliberado:** `1.33.2-falsacion/banco-corrida-unica.txt` conserva el
> nombre **viejo** del caso (`"D16: matiz no rebaja critico"`, línea 938). Eso **prueba** que
> esa corrida es anterior a `a529c49`, y reescribirlo sería falsificar evidencia. La corrida
> sobre la cabeza actual es un artefacto **nuevo**: `docs/qa/1.33.2-vuelta-2/banco-195ae31.txt`.

**Veredicto: `QA: aprobado (vuelta 1 de 2, QA-P48-01, 2026-09-10)`**

**Qué se validó:** los dos commits del parche y sus dependencias.
`09ccf63` (el arreglo en `hooks/lib.sh`, el banco y su contabilidad) y `a8e332a`
(el contrato heredado y la sonda). **No** es una revisión del historial ni de otros REQ:
revisión **proporcional**, según la política del propietario del 2026-09-10.

**Versión base:** worktree `/home/juan/dev/ArnesJuan-1.33.2`, rama `hotfix/1.33.2-rigor`,
cabeza **`a8e332a`**, árbol limpio (`git status --short` vacío).
`.claude-plugin/plugin.json` sigue en `1.33.1` — correcto, el parche no sube la versión.

**Dónde está la evidencia:** `docs/qa/1.33.2-falsacion/` — los cinco arneses con los que
se midió y sus salidas. Cada cifra de este documento se re-deriva ejecutando el arnés
que se cita a su lado; no hay ninguna que sólo exista en la conversación.

**Por qué no hay campo `QA:` que escribir:** este parche **no tiene REQ** (`git grep
QA-P48 -- requirements/` no devuelve nada). Se identifica por el número de hallazgo.
Consecuencia que el coordinador debe saber, y que no es un defecto del código: al no
haber cabecera de REQ, `guard-completado` **no mide** los veredictos de este cambio —
la puerta que el parche arregla no se aplica sobre el parche. El veredicto vive aquí.

---

## 1. Quality gates (§7) — todas en verde

| Puerta | Resultado |
|---|---|
| `bash -n` sobre `hooks/*.sh` y `tools/*.sh` | OK |
| `jq -e . hooks/hooks.json` | OK |
| `jq -e . .claude-plugin/plugin.json` · `marketplace.json` | OK |
| `git diff --check` | OK |
| Banco completo, `ARNES_JOBS=6` | **rc 0 · 908 PASS · 0 FAIL · 4 SKIP · cuadre 912 exacto** |
| Autoprueba del corredor | **106 PASS · 0 FAIL · rc 0** |

**Una sola corrida del banco**, por la política de hoy: este árbol es el candidato final y
nada posterior invalida esa evidencia. Los 4 SKIP traen su motivo en su línea (uno es de
Windows, dos son acreditación de fail-before con la palanca apagada, uno es la calibración
dependiente del reloj).

**Reparto observado 908/0/4 frente al 907/0/5 que reporta el desarrollador.** No se
promedia ni se elige la cifra mejor: el `rc`, el cuadre 912 y **FAIL = 0** son idénticos;
lo que se mueve es el SKIP de calibración que el propio banco declara dependiente del
reloj. Es la variabilidad ya registrada en `contrato-parche.md`, no un resultado nuevo.

**Integridad de dependencias: n/a.** El proyecto no usa gestor de paquetes ni lockfile
(no hay `package.json`, `*-lock.*`, `go.mod` ni `Cargo.toml`). Dependencias externas
efectivas: `bash` 5.3.9, `jq` 1.8.2, `awk`.

**Rendimiento: NO acreditado, y se dice.** Banco en 24 s de reloj, `loadavg` 0,03 antes y
1,62 al terminar, 12 núcleos, `ARNES_JOBS=6`. **Esta máquina no es el runner**, fue una
sola corrida y no hubo calentamiento: la cifra sirve para saber que el banco terminó, no
para afirmar nada sobre rendimiento.

---

## 2. Comprobación 1 — ¿cubre el argumento TODAS las ramas relevantes del lector?

**Encargo:** enumerar las **ramas del lector**, no más valores al azar, y decir si alguna
queda sin cubrir. **Si alguna hiciera que el matiz BAJE el rigor, es hallazgo.**

**Arnés:** `docs/qa/1.33.2-falsacion/ramas-lector.sh` · salidas `ramas-1.33.{0,1,2}.txt`.
**40 ramas nombradas × 4 estados de sensibilidad (`no` · `sí` · ausente · no reconocido)
= 160 combinaciones**, ejecutadas contra los **tres** lectores (1.33.0 y 1.33.1
instalados en la caché del plugin, y este árbol).

Las once ramas que el propietario nombró, todas cubiertas: valor vacío (R02), valor
ausente (R01), paréntesis sin cerrar (R25), paréntesis vacío (R17), paréntesis anidado
(R18), marcado de Markdown envolviendo (R12–R14), marcado + paréntesis juntos (R20–R22),
tabulador (R08, R23), CR (R36 y, a nivel de cabecera, §5), BOM (R32, R33), blanco de más
(R09), y el suelo de sensibilidad (las 4 columnas de cada fila).
Añadidas por falsación: paréntesis sin abrir (R26), paréntesis al principio (R27), dos
paréntesis (R19), paréntesis pegado sin espacio (R16), sólo paréntesis sin nivel (R31),
matiz sobre valor desconocido (R30), matiz cuyo texto **nombra otro nivel** (R37–R39),
acento NFC y NFD (R10, R11), mayúsculas (R07), espacio de anchura cero (R34) y
paréntesis de anchura completa no-ASCII (R35).

**Resultado: `RESPUESTA: NINGUNA RAMA BAJA EL RIGOR`.**
- **0 regresiones** de 1.33.2 contra 1.33.0 en las 160 combinaciones.
- Sólo **4 celdas** cambian contra 1.33.0 y las cuatro van **hacia arriba**: son
  `critico (<matiz>)` con la sensibilidad no afirmativa (R29 y R39, × sens `no` y
  ausente), `estandar` → `critico`. Es la corrección D16 de 1.33.1, conservada.
- **24 celdas** cambian contra 1.33.1, todas **hacia arriba**: cada forma de
  `ligero (<matiz>)` pasa de `ligero` a `estandar`. Es el fail-open que el parche cierra.
- Todas las ramas con `MATIZ=1` dan `estandar` o `critico`. Las únicas que dan `ligero`
  son las de **`ligero` sin matiz** (R03, R07–R14: mayúsculas, tabulador, blanco de más,
  acento NFC/NFD, y los tres marcados de Markdown), que es exactamente lo que el contrato
  dice que se conserva.
- Las ramas de byte raro caen **del lado seguro**: BOM, espacio de anchura cero y
  paréntesis de anchura completa dejan el valor irreconocible → derivación heredada
  (`estandar` / `critico`), nunca `ligero`.

### Y el antecedente del argumento se puede establecer POR CONSTRUCCIÓN, no sólo por barrido

`contrato-parche.md` § «Que ningún caso antes denegado pase a permitido» separa
honestamente lo medido (rigor nuevo ≥ viejo, **barrido**, dominio abierto) de lo
construido (monotonía de la puerta), y deja escrito que *«cubrir todas las ramas
relevantes del lector es encargo de QA»*. Hecho el encargo, la primera mitad **también**
sale por construcción, sobre cinco premisas verificadas en este árbol:

1. `arnes_norm_campo` es **byte a byte idéntica** entre 1.33.0 y este árbol
   (`diff` de la función: sin diferencias). Luego `ARNES_CAMPO` es el mismo en ambos.
2. En 1.33.0 el reparto era **incondicional**: `arnes_norm_campo "$5"; ARNES_RIGOR="$ARNES_CAMPO"`.
   Todo valor que contenga `(` era por tanto irreconocible → **nivel heredado, exacto**.
3. En este árbol `ARNES_RIGOR_MATIZ=1` ⟺ `(` ∈ `ARNES_CAMPO` — la misma condición de (2).
4. Con `MATIZ=1`, la guarda toma `max(nivel(declarado), nivel(heredado))`, y
   `nivel(heredado) ∈ {2,3}`: el resultado es **≥ heredado** siempre.
5. `arnes_rigor_efectivo` **sólo sube**: sus únicas asignaciones son `heredado` y
   `critico`, las dos bajo una comparación `-lt`. Y `arnes_rigor_nivel`,
   `arnes_sens_efectiva`, `arnes_veredicto` y `arnes_desenvuelve` son **idénticas** a
   1.33.0, así que «heredado» y los niveles significan lo mismo en las dos versiones.

De (1)–(5): para **toda** entrada, incluido el dominio abierto que el barrido no alcanza,
el rigor efectivo de 1.33.2 es ≥ el de 1.33.0. Unido a la monotonía de la puerta —que ya
era por construcción, y que las 120 ternas del §4 confirman empíricamente— ningún caso
antes denegado pasa a permitido. **No es un hallazgo: es la conclusión del encargo, y va
en la dirección segura.** El contrato queda **conservador**, no falso, así que no hace
falta write-back; el desarrollador puede fortalecerlo si quiere, con estas cinco premisas.

---

## 3. Comprobación 2 — ¿`ARNES_RIGOR_MATIZ` contamina la lectura siguiente?

**Lo ya medido, verificado y no repetido.** `docs/estabilizacion/sonda-matiz-no-contamina.sh`
se re-ejecutó una vez contra este árbol y **reproduce sus seis escenarios**
(`1.33.2-falsacion/sonda-reproducida.out`), incluido el (F) con el indicador
**preensuciado a mano**: `ligero` limpio sigue devolviendo `ligero` con `MATIZ=0`.
Sigue siendo evidencia válida porque `a8e332a` no toca `hooks/`. Cubre el **lector**.

**Lo que faltaba y es de QA: la misma pregunta a través de la PUERTA.**
**Arnés:** `contamina-puerta.sh` · salida `contaminacion-puerta.out`. El REQ testigo es
`Rigor: ligero` con **QA y Seguridad pendientes**: si se contaminara dejaría de cerrar, y
la contaminación se vería como un `deny`.

| Escenario | Casos | Resultado |
|---|---|---|
| (A) Puerta en **secuencia**, invocaciones consecutivas: matiz→limpio, limpio→matiz, y limpio otra vez | 5 | **5 PASS** |
| (B) REQ limpio con vecinos **con matiz en la misma carpeta**, en los dos órdenes alfabéticos | 2 | **2 PASS** |
| (C) `hooks/estado-derivado.sh`: **4 REQ en UN solo proceso**, en los dos órdenes | 2 corridas | cada REQ con su nivel correcto; el orden **no** altera nada |
| (D) `tools/arnes-lectura.sh`: **4 REQ en UN solo proceso** | 1 | `ligero 2 · estandar 2 · critico 0`, exacto |

(C) es la evidencia que importa: `estado-derivado.sh` es el **único** consumidor que lee
varios REQ en el mismo proceso, y `ligero (local)` → `estandar` junto a `ligero` → `ligero`
en la misma pasada, en ambos órdenes.

**Y por construcción, que es lo que cierra la clase:**
- `arnes_campos_normaliza` pone `ARNES_RIGOR_MATIZ=0` **al entrar**, antes del `case`.
- `arnes_campos_req` **no tiene ningún `return` temprano** (sólo `continue` y un `break`
  dentro de bucles), así que **siempre** llega a llamar a `arnes_campos_normaliza`. No
  existe camino que reinicie los campos y se salte el reinicio del indicador.
- `guard-completado.sh` llama a `arnes_campos_req` **una sola vez por invocación**
  (líneas 236–237, dos ramas de un `if`, no dos lecturas), y cada invocación es un
  proceso propio: entre dos decisiones de la puerta no hay estado que heredar.

**Sin hallazgo.**

---

## 4. Comprobación 3 — `ligero` limpio, suelo de seguridad y `critico (por suelo)`, POR CONDUCTA DE PUERTA

**Lo ya medido por la coordinadora sobre los lectores se verificó y no se repite:** una
sola celda cambia contra 1.33.0 con `Sensible: no` y va hacia arriba. Confirmado
independientemente en el §2 (4 celdas, que son esa misma fila × 2 ramas × 2 estados de
sensibilidad no afirmativa).

**Lo que es de QA: probar el contrato, no el síntoma — con las exenciones EJERCIDAS.**
**Arnés:** `puerta.sh` · salidas `puerta-1.33.{0,1,2}.txt`. Para cada forma de `Rigor:` y
cada sensibilidad se intenta **cerrar el REQ de verdad** con las tres combinaciones de
firmas que discriminan el nivel. La **terna** de decisiones identifica el nivel sin
ambigüedad y sin preguntarle nada al lector:

| Terna `(QA pdte,SEG pdte)` · `(QA ok,SEG pdte)` · `(ambas ok)` | Nivel efectivo |
|---|---|
| `allow allow allow` | `ligero` — **exención de QA ejercida** |
| `deny allow allow` | `estandar` — exige QA, no exige seguridad |
| `deny deny allow` | `critico` — exige ambas |

**20 formas × 2 sensibilidades × 3 combinaciones = 120 invocaciones de puerta** por
versión. **Cero ternas anómalas en las tres versiones**: la monotonía de las exigencias
de la puerta queda además confirmada empíricamente, no sólo por construcción.

Las dos filas que el propietario pidió nombradas:

| Caso | Esperado | Medido |
|---|---|---|
| No sensible, `Rigor: ligero (local)`, `QA: pendiente` → **debe DENEGAR** | deny | **deny** ✅ |
| No sensible, `Rigor: ligero` a secas, `QA: pendiente` y `Seguridad: pendiente` → **debe CERRAR** | allow | **allow** ✅ |

### Par fail-before / pass-after, comprobado

Contra `~/.claude/plugins/cache/arnes-juan/arnes-juan/1.33.1/hooks` (**la 1.33.1
publicada, con el defecto vivo**) frente a este árbol, medido con el **mismo** arnés:

- **8 filas** en las que 1.33.1 **afloja** respecto a 1.33.0: las ocho formas de
  `ligero (<matiz>)` con `Sensible: no`, todas con terna `allow allow allow` — es decir,
  el REQ **cerraba con QA pendiente Y seguridad pendiente**. Ése es el fail-open, medido
  por conducta y no por lector.
- **Las 8 restauradas** a `estandar` en 1.33.2, y **0 regresiones** contra 1.33.0.
- El banco reproduce el par en su propia forma: los casos `QA-P48-01` de la sección 40
  dan **28 PASS · 0 FAIL** aquí. (El desarrollador reporta 6 FAIL contra la 1.33.1: son
  los 6 casos **discriminantes** de la sección; mis 8 filas son **formas** del valor, no
  casos del banco. Las dos cifras miden cosas distintas y no se contradicen.)

**Sin hallazgo.**

---

## 5. Ataque de falsación propio: ¿se puede NEUTRALIZAR el matiz a nivel de cabecera?

No estaba en el encargo y es donde un arreglo de este tipo se rompe. **Arnés:**
`cabecera.sh` · salida `cabecera-ataque.out`. **16 casos de puerta, 16 PASS / 0 FAIL.**

Ninguna vía recupera la exención de QA con un matiz vigente: clave **decorada**
(`**Rigor**:`, `` `Rigor`: ``), clave **sangrada**, `Rigor :` con espacio antes de los dos
puntos, `Rigor:` **duplicado** en los dos órdenes, `ligero` limpio **comentado** con el
matiz vigente (y al revés), `ligero` limpio **en el cuerpo** del REQ tras el matiz de la
cabecera, comentario que **abre y no cierra** después del matiz, **CR final** de transporte,
y sensibilidad **no reconocida** (que impone el suelo `critico` tanto sobre el matiz como
sobre `ligero` limpio).

Los dos únicos `allow` son **legítimos y esperados**: cuando la declaración vigente acaba
siendo `ligero` **a secas** —porque un segundo `Rigor:` posterior lo dice (gana el último,
regla preexistente) o porque el paréntesis vive **dentro de un comentario** y un comentario
no declara campo—. En ambos el autor escribió `ligero` sin matiz, que es exactamente la vía
que el contrato documenta para pedir la exención.

---

## 6. El contrato heredado — ¿dice el texto nuevo lo que el código hace?

Las cuatro afirmaciones del enunciado del propietario, contrastadas **contra conducta de
puerta** (§4), no contra el lector:

| # | Afirmación del contrato | Medido | ¿Cierto? |
|---|---|---|---|
| 1 | «El rigor efectivo combina el nivel declarado con el suelo de seguridad» | toda fila `Sensible: sí` da `critico` cualquiera que sea el nivel declarado | ✅ |
| 2 | «Un matiz parentético conserva `estandar` y `critico`, sujeto a ese suelo» | `estandar (x)` → `estandar`; `critico (por suelo)` y `critico (ligero)` → `critico`; con `sí`, ambos → `critico` | ✅ |
| 3 | «`ligero` con matiz deriva a `estandar` si el REQ no es sensible **y a `critico` si lo es**» | las 8 formas de `ligero (…)`: `estandar` con `no`, `critico` con `sí` | ✅ |
| 4 | «`ligero` sin matiz conserva su comportamiento, sujeto al suelo de seguridad» | `ligero`, `LIGERO`, `  ligero  `, `**ligero**`, acento NFC/NFD, tabulador, blanco de más → `ligero` con `no`; `critico` con `sí` | ✅ |

Y la viñeta conservada —«un valor realmente desconocido conserva la derivación heredada»—
también: `inventado` e `inventado (x)` → `estandar` con `no`, `critico` con `sí`.

**Ninguna promesa absoluta con excepción fuera.** Se buscó expresamente el patrón que este
proyecto ya se ha comido: las cuatro afirmaciones llevan su condición («sujeto a ese suelo»,
«si el REQ no es sensible») **dentro** de la frase, y no se encontró ninguna rama del §2 que
las desmienta. La consecuencia que el contrato no nombra —**no existe forma de declarar
`ligero` con matiz**— está en el CHANGELOG y en `lib.sh`, y se intentó falsar en el §5:
no se encontró vía.

### Sedes del enunciado — recontadas, y la cuenta del desarrollador se sostiene

`git grep` del **enunciado** (no del archivo) sobre archivos **rastreados** devuelve las
tres declaradas: `requirements/README.md:108` y `templates/requirements-README.md.tpl:108`
(corregidas) y `docs/estabilizacion/actualizacion-candidata.md` (marcada **SUPERADO EN
PARTE**, nombrando qué punto queda superado, qué enunciado lo sustituye —**verbatim**, y
coincide con el vigente— y desde cuándo). `CHANGELOG.md` y `contrato-parche.md` citan la
frase vieja describiendo el cambio, que es su sitio.

Las dos sedes son **idénticas byte a byte** en la región 104–117 (`diff` sin diferencias),
re-verificado aquí y no sólo heredado del informe del coordinador.

**Un grep sobre el árbol completo devuelve dos archivos más, y NO son sedes.** Están en
`docs/estabilizacion/verificacion-instalacion-1.33.2/`, que **no está rastreado** (`git
ls-files` vacío) y se creó **después** de `a8e332a`: es la simulación de merge a tres vías
de la coordinadora. `base.tpl` es la plantilla **de v1.33.1** extraída con `git show`, así
que **debe** contener la frase vieja —es la base del merge—, y `c3.out` es la salida **con
marcadores de conflicto** del caso 3. Reescribirlas falsearía la evidencia, igual que con
`.arnes/plantillas-origen/`. Lectura correcta; no se tocan.

---

## 7. Hallazgos

### Bloqueantes: ninguno

### `QA-1332-01` — clase **`instrumento`** · NO bloquea · otra ventana

**`tools/arnes-lectura.sh` afirma algo falso sobre lo que hace la puerta ante un
`Rigor:` con matiz.**

*Reproducción* (`1.33.2-falsacion/informe-vs-puerta.sh`): un REQ con
`Sensible a seguridad: no`, `QA: aprobado`, `Seguridad: pendiente` y
`Rigor: critico (por suelo)`.

*Esperado:* el informe describe lo que la puerta va a hacer — ése es el único motivo por el
que existe, y lo dice él mismo en su cabecera: *«este informe existe justo para detectar ese
tipo de desfase»*.

*Observado:* el informe imprime

```
  REQ-1        Rigor:
                 escrito:  «critico (por suelo)»
                 se lee:   <critico(porsuelo)>
                 no es un nivel válido → se IGNORA y el REQ se juzga como si no lo declarara.
```

mientras la puerta lee `critico` y **deniega** el cierre por falta de firma de seguridad.
«Como si no lo declarara» daría `estandar`, que **cerraría**. La causa es que el informe
normaliza con `arnes_norm_campo` a secas (línea ~186) y **no** aplica el reparto del
paréntesis que `arnes_campos_normaliza` sí hace: es una lectura del rigor por una vía que
no es la de la puerta.

*Severidad y dirección:* **baja, y el error va del lado seguro.** El informe **subestima** el
rigor; la puerta entrega más. Nadie cierra nada que no debiera, así que nadie puede ver,
decidir o cobrar distinto: **`instrumento`**, no `usuario/dinero`. Tampoco es `contrato`: el
enunciado corregido del §6 no afirma nada sobre el informe.

*Procedencia — y esto es lo que decide que no sea de este parche:* medido contra las tres
versiones. En **1.33.0** el mensaje era **cierto** (la puerta también derivaba). Lo volvió
falso **1.33.1**, al enrutar el paréntesis por el lector común (la corrección D16). En
1.33.1 había **dos** filas falsas (`critico (x)` y `ligero (x)`); **1.33.2 deja una**,
porque `ligero (x)` pasa a `estandar`, que es lo que el mensaje ya decía. **Este parche
reduce el desfase a la mitad y no introduce nada.**

*Dueño:* `desarrollador`. *Ventana:* la que el propietario decida, **no** esta —se registra
por su orden de anotar los hallazgos de instrumento para otra ventana. *Forzador sugerido:*
que `arnes-lectura.sh` derive el nivel por `arnes_campos_normaliza` en vez de por
`arnes_norm_campo`, que es el sitio único; un caso de banco que compare el nivel que el
informe publica con el que la puerta aplica lo dejaría armado solo.

### Observación de gobernanza (no es hallazgo)

El parche **no tiene REQ**, así que no hay cabecera con `Estado:`, `QA:` ni `Seguridad:` y
`guard-completado` **no mide** nada de este cambio. Es decisión del coordinador y del
propietario, no defecto del código; se anota porque el veredicto de QA de un cambio al
mecanismo queda, por esta vez, sólo en este archivo y no en un campo que la máquina lea.

---

## 8. Límites de esta validación — lo que NO se acreditó

- **Rendimiento: no acreditado.** Una corrida, sin calentamiento, en una máquina que no es
  el runner. La cifra de reloj está en el §1 con su `loadavg` y no se presenta como puerta.
- **`.claude-plugin/` sin tocar**, por el encargo: la versión sigue en `1.33.1`. Publicar
  exige subirla, y eso no es de QA.
- **Seguridad va después.** Este veredicto **no** cubre la revisión de la plantilla
  heredada: la firma `Seguridad:` es del `auditor-seguridad` y no se emite aquí.
- **CI obligatorio pendiente.** El banco corrió en Linux local; la puerta requerida de
  `main` es la de CI y no la sustituye esta corrida.
- **`arnes-upgrade` no se ejecutó como skill.** La simulación de merge de la coordinadora
  se leyó y su lectura se confirmó correcta (§6), pero no se re-midió: está fuera del
  encargo acotado y no es conducta que este parche cambie.
- **Ningún archivo fuera de `docs/qa/` y `CHANGELOG.md` se modificó** en este worktree, y
  no se tocó ningún otro worktree.
