# Informe de traspaso — 2026-09-10

> **Único informe de traspaso de esta sesión.** Sustituye a cualquier bloque de continuidad anterior
> de `docs/ESTADO.md`, que quedan marcados **[HISTÓRICO]**. El puntero vigente es
> `docs/ESTADO.md` §«★★ RETOMAR AQUÍ».
>
> **Método:** toda cifra sale de un comando sobre el árbol o de un artefacto en disco; el comando o
> la ruta acompañan a cada una. Nada de este informe cierra un REQ, cierra un hallazgo, acepta un
> residual, relaja una prueba, fusiona ni publica.

---

## 1. Rama, commit remoto y cambios locales

| | |
|---|---|
| Rama | `rel/registro-1.33.0` |
| HEAD local | `af01436` |
| HEAD remoto (`origin/rel/registro-1.33.0`) | `af01436` — **iguales** |
| Commits sin empujar | **0** (`git log origin/rel/registro-1.33.0..HEAD` vacío) |
| PR | **#45, en borrador** |

**Cambios locales al escribir esto** (todos son documentación de este cierre, ninguno es mecanismo):
`docs/propuesta-cierre-1.34.0.md`, `docs/ESTADO.md`, `docs/TRASPASO-2026-09-10.md`,
`docs/propuesta-reglas-coordinacion.md`, `docs/arnes/ci-1.34.0-no-discrimina/`,
`docs/arnes/d16-alcance-real/`. **Se comitean en este cierre.**

## 2. Versión publicada y versión en desarrollo

| | |
|---|---|
| **Publicada** | **`v1.33.0`** — el tag existe (`git tag --list 'v*' \| sort -V \| tail -1`), `plugin.json` = `1.33.0` |
| **En desarrollo** | **1.34.0**, **sin publicar** y sin tag |
| Consumidor **verificado** de 1.33.0 | **este repositorio** (sus hooks corren la instalación estable). Que haya otros descansa en la afirmación de `AGENTS.md`, **no** en una comprobación |

## 3. Trabajo terminado, y la evidencia que lo acredita

Seis commits, todos empujados.

| Commit | Qué terminó | Evidencia |
|---|---|---|
| `b369094` | Propuesta de cierre de 1.34.0: la cola consolidada y los bloqueantes agrupados por causa | `docs/propuesta-cierre-1.34.0.md` |
| `a139155` | **`QA-017-24`**: write-back de `CA-03` — la cota vive en el **criterio**, no en el mensaje. Arreglo de **texto**, no de código | fila del `Historial de cambios` de `REQ-017`; razón decisiva verificada contra `registro-seguridad.md:5905-5911` |
| `f4a5f1f` | **`SEC-064`**, instancia de `CA-08 (ii)`: cota **1**, con dos bases medidas y no por analogía. Sede elegida por `ADR-011` | `REQ-017` `CA-08`; `SEC-064` §Remediación |
| `b55347e` | Veredicto de QA **vuelta 3 de 3** de `REQ-017`; `REQ-017` → **`bloqueado`** por la salida de §6; `D18` escalado | `docs/qa/1.34.0.md`, `docs/qa/evidencia-req-017-writeback-f4a5f1f/` |
| `67b06fe` | **`SEC-067`**: write-back en **`NFR-026-01`** — el primer NFR del proyecto | `REQ-026` §«Requisitos no funcionales» |
| `af01436` | **Reconciliación `R-028`** (auditor) + corrección de `CA-03` según la autorización `D18 (A)` + `D16` acotado con medición | `docs/seguridad/reconciliacion-campos-2026-09-10.md`, `docs/arnes/d16-alcance-real/` |

**Comprobado por máquina, no por mí:** la cola sigue en **16 pendientes**
(`tools/arnes-lectura.sh`); el campo de `REQ-026` conserva `SEC-067 (usuario/dinero — en-mitigación…)`
con clase y estado intactos; **ningún archivo de mecanismo** cambió en los tres últimos commits
(`git diff --name-only` sobre `hooks/ tools/ tests/ .github/ .arnes/ templates/ .claude-plugin/`).

### 3.b Tres cifras mías que se corrigieron con medición, y no se borran

1. **Bloqueantes: 16 → 22 → 23 → 34.** La cifra firme es **33 sobre `b55347e` y 34 desde `R-028`**.
   Mi `comm -23` reproducía los once **exacto**, pero leía el estado en la **cabecera de apertura**
   cuando el vigente es *la última declaración que nombra el hallazgo* —cabecera, sección
   `Estado: X → Y`, o **prosa sin encabezado**—. Sobraban cinco y **faltaban cuatro**.
   Y el auditor añadió lo que importa: **bajo la frontera, 34 devuelve la decisión igual que 17**;
   lo que cambia es que ahora se puede **desmentir fila por fila**.
2. **`D16`: la exposición es más estrecha de lo que dije.** Sondeado con el lector real
   (`docs/arnes/d16-alcance-real/`): `Rigor: critico (por suelo)` cae a `estandar` **sólo** si el REQ
   **no** está marcado `Sensible a seguridad: sí`; con `sí` el suelo de §6 lo rescata. En este
   repositorio, `grep -L` da **0 REQ no sensibles**: el defecto es **latente, no activo**.
3. **«El `FAIL` del banco no es una regresión» — retirado.** No se sigue de la evidencia. Ver §7.

## 4. Trabajo incompleto, archivos afectados y siguiente acción

| Qué | Archivos | Estado | Siguiente acción |
|---|---|---|---|
| ~~Re-validación de `CA-03` fuera del contador~~ | `requirements/REQ-017.md`, `docs/qa/1.34.0.md` | **TERMINADA** — `con-hallazgos`. Lo mecánico pasó (diff de mecanismo vacío, 3 gates verdes, banco 1057/0/7 rc 0) y las tres piezas de la promesa pasan; **`QA-017-31` NO se cierra** | ~~Alinear `CA-03:62` con `:74`~~ **HECHO** (2026-09-10). **No necesitaba firma nueva:** `D18` ya decía «revisa la promesa **completa**». Se enumeraron **las ocho sedes antes** de despachar y se entregó la lista en el encargo. Falta: **segunda revalidación acotada de QA** — ésa **sí** necesita su firma, porque la autorización cubría **una** |
| **Dos campos desfasados** | `requirements/REQ-013.md`, `requirements/REQ-024.md` | **NO despachado**, en cola **serial** | `analista-requerimientos`: alinear el campo con firmas que **ya existen** (`SEC-014` `mitigado` desde `R-006`; `SEC-083` desde `R-027`). **No es cerrar un hallazgo.** Serial porque `tools/arnes-paralelo.sh` declara que `REQ-013` y `REQ-024` **colisionan** con `REQ-017` en `run.sh` y `hooks/lib.sh` |
| ~~`SEC-055` al campo de `REQ-019`~~ | `requirements/REQ-019.md` | **HECHO** (2026-09-10) — el campo declara ahora **dos** `contrato`; `Estado` y ventana 1.35.0 intactos | su **mitad de fondo** (acotar o retirar la delegación de publicación) es **decisión tuya**, recomendación 4 del artefacto |
| **El NFR que cierra `SEC-085`** | `requirements/` | **NO empezado, y por CONFLICTO de instrucciones** | El artefacto lo prescribe (fila 220) y el auditor **no da `SEC-085` por cerrado sin él**; y tú **prohibiste NFR nuevos** en esta ventana. **Necesita tu palabra:** o entra como excepción nombrada, o `SEC-085` queda abierto con dueño y vencimiento |
| **Índice de sede de `SEC-056`** | `docs/seguridad/registro-seguridad.md` | deuda declarada del auditor | vencimiento **1.34.0** |
| **Propuesta de reglas de coordinación** | `docs/propuesta-reglas-coordinacion.md` | **escrita, sin ejecutar** | espera tu decisión (§6, decisión **7**) |

## 5. Hallazgos pendientes reconciliados — sin mezclar dos cosas distintas

Fuente: `docs/seguridad/reconciliacion-campos-2026-09-10.md` (`R-028`), once filas con método.

### 5.a Defectos SIN corregir (esto es trabajo o decisión)

| ID | Clase | REQ | Nota |
|---|---|---|---|
| `SEC-067` | **`usuario/dinero`** | `REQ-026` | `en-mitigación`. Write-back hecho; **su cierre pasa por `SEC-072`** |
| `SEC-072`, `SEC-073` | `contrato` | `REQ-026` | `SEC-073` es **código**, no redacción |
| `QA-017-31` | `contrato` | `REQ-017` | corrección hecha y **validada en lo que cubría**; **sigue abierto** por una **tercera sede** dentro de `CA-03`: `:62` declara **presente** («ya construidos») lo que `:74` retira, y su «citada abajo» apunta al párrafo que lo niega. Tercera vez la misma forma |
| `QA-024-19`, `SEC-084` | `contrato` | `REQ-024` | `SEC-084` es además `D17` |
| `QA-021-10`, `QA-021-11` | `contrato` | `REQ-021` | tope agotado |
| `SEC-020` | `contrato` | `REQ-013` | fail-open de `arnes-paralelo.sh` |
| `QA-114`, `QA-116`, `QA-117` | `contrato` | `REQ-007` | ventana 1.31.0 |
| `SEC-038`…`SEC-045` | `contrato` | `REQ-020` | **8**; y `REQ-020` **nunca se implementó** (`Acredita:` en **0 de 63** secciones) |
| `SEC-031`, `SEC-032`, `SEC-034`, `SEC-035`, `SEC-036` | `contrato` | `REQ-019`/`REQ-021` | **`en-mitigación`**, no `abierto`. **No vuelven al campo**: su residual viaja por `Seguridad:`, hoy `preventiva`. **La puerta los ve por el otro campo** |
| `SEC-085` | `contrato` | — | **nuevo (`R-028`)**: el índice que la frontera de publicación cita como **exhaustivo** no lo es |
| `SEC-075` | `contrato` | — | **debe resolverse ANTES de publicar `v1.34.0`** (dictamen del auditor). En **ninguna** de las dos sedes, por decisión correcta: es el contraejemplo que demuestra `SEC-085` |
| `SEC-023`, `SEC-029`, `SEC-030`, `SEC-055` | `contrato` | sin REQ atribuible | su vía de bloqueo es `PENDING_APPROVAL.md` |

### 5.b Discrepancias DOCUMENTALES (el campo no coincide con una firma que ya existe)

| ID | Registro | Campo | Efecto |
|---|---|---|---|
| ~~`SEC-014`~~ | `mitigado` desde `R-006` | **RETIRADO del campo** el 2026-09-10 | resuelto. `REQ-013` sigue sin poder cerrar por `SEC-020` |
| ~~`SEC-083`~~ | `mitigado` desde `R-027` | **RETIRADO del campo** el 2026-09-10 | defecto del **propio cierre** del auditor: `R-027` §2 escribió «se retira del campo» para `SEC-082` y §3 **omitió la frase**. Subsanado en `R-028` §3 |
| ~~`REQ-023` `Estado:`~~ | — | **ANOTADO** el 2026-09-10 | la cláusula falsa se **conserva literal como rastro** y la anotación declara que quedó falsa, citando la evidencia. El bloqueo **no se levantó** y el rastro de las tres vueltas se **confirmó**, no se sustituyó. **Y queda una consecuencia para ti:** el texto ya no afirma nada falso, pero **hoy no hay causa enunciada** — re-enunciarla es afirmación de alcance tuya. `D13` está en la cola y **no** se puso |

**La lección que dejó escrita el auditor:** *un cierre no está completo hasta que dice de qué campo
sale.*

### 5.c Descubierto en este cierre, registrado y NO investigado

- **`hooks/rotar-artefactos.sh:651`** conserva en comentario la cifra **no medida** que ya fue
  retirada del REQ. Clase probable `instrumento`. Dueño `desarrollador`. → §7 de
  `docs/propuesta-cierre-1.34.0.md`.
- **`guard-completado` deniega editar `REQ-017`** si el texto **menciona** los tokens `QA:` o
  `Seguridad:`, porque su cabecera lleva `Seguridad: aprobado` sobre `QA: con-hallazgos` (condición
  **preexistente**). Le denegó al analista su primera fila de historial. Dueño a determinar; se
  resuelve solo cuando los veredictos se reordenen.
- **`REQ-024 CA-07 (ii)`**: ver §7. **No es** `CA-09 (iii)`.
- **Numeración de las mitades de `SEC-064`**: el registro nombra dos (cláusula y señal) y los
  criterios cuentan «de qué máquina» como una de ellas. Dueño `auditor-seguridad`.

## 6. Decisiones humanas reales, agrupadas

Detalle y las cuatro columnas por decisión en `docs/propuesta-cierre-1.34.0.md` §4.
**Dieciséis entradas de cola ⇒ siete decisiones.**

| # | Agrupa | En una línea | Mi recomendación |
|---|---|---|---|
| **1** | `D2`, `D4` | Mover `REQ-020` a **1.35.0** sin retirar ninguno de sus 8 hallazgos | **Sí.** Es la palanca de más efecto y no retira nada |
| **2** | `D16`, `D17` | **Implementar** un `1.33.1` desde el **tag `v1.33.0`** | **Sí, con `SEC-084` SOLO.** Compartir causa no obliga a compartir vehículo: `QA-016-04` no tiene exposición verificada aquí |
| **3** | `D11`, `D12`, `D13` | Una regla para **los verdes que ya no cubren su árbol**; ratificar `ADR-011`; **corregir la fila de §13 que hoy es falsa** | **Sí** |
| **4** | `D13`, `D14` | `REQ-023`/`REQ-024` siguen `bloqueado` + **cuarta vuelta acotada**, incluida **la auditoría de `REQ-024` que nunca se hizo** | **Sí** |
| **5** | `D7`, `D8`, `D15` | Manifiesto en regla: ratificar o revertir `campos.ausencia_exige` (comiteado **sin su gate**, fallo mío); dejar **apagado** `veredictos.caducan_con_codigo` | **Revertir o ratificar explícitamente**; el interruptor de veredictos **apagado** |
| **6** | `D3` + `SEC-067` + gate de `ADR-008` | La **excepción de las garantías absolutas** de `REQ-026`: vía **A** (firma) o vía **B** (código) | **Sin ella `REQ-026` no cierra**, y no por falta de trabajo. La precondición de A es **hoy inalcanzable** |
| **7** | `D18` | `QA-017-31`: ya **autorizada** por ti y **ejecutada**; queda decidir el estado final tras el veredicto en curso | pendiente del veredicto |
| **+** | nuevo | **`SEC-075` antes de publicar `v1.34.0`**, y la prueba de `CA-09 (iii)`/`CA-07 (ii)` (§7) | **(B)**: declararlas **no acreditantes** con dueño y vencimiento, sin retirarlas |

### Separadas por naturaleza

- **Autorizar trabajo:** decisiones 1, 2 (sólo implementar), 4, 5, 6; los cuatro tramos «NO
  despachado» de §4.
- **Aceptar riesgos:** 1.34.0 sin `REQ-020`; el precio de `D12`; el `deny` nuevo de `SEC-084`; pasar
  del tope de §6 en tres REQ; y **firmar la vía A de `ADR-008` sin el intervalo residual medido**.
- **Autorizar publicación:** **ninguna se pide.** Ni `v1.33.1` ni `v1.34.0`.

## 7. Último CI — y qué quedó SIN acreditar

| Commit | `hooks-en-linux` | Mecanismo cambiado |
|---|---|---|
| `b55347e` | **success** | — |
| `67b06fe` | **failure** | **0 archivos** |
| `af01436` (= HEAD, = remoto) | **success** | **0 archivos** |

El rojo fue **`REQ-024 CA-07 (ii)`**: *el reloj de la ruta crítica = 1.320× > techo 1.250×
(76072µs sobre 57589µs)*. **No es `REQ-023 CA-09 (iii)`**: es un **segundo** criterio de techo de
reloj, en otro REQ.

**Lo que quedó sin acreditar, y es la parte que importa:** la comprobación pasó, falló y volvió a
pasar sobre **código idéntico byte a byte**, así que **no discrimina** — y una prueba que no
discrimina **no acredita en ninguna de sus dos direcciones**. Por lo tanto:

> **El `success` de `af01436` NO acredita `REQ-024 CA-07 (ii)`.** Y tampoco `REQ-023 CA-09 (iii)`,
> medido aparte con `FAIL → SKIP → PASS → PASS` sobre código idéntico.

**Me corrijo expresamente:** dije que *«el `FAIL` del banco no es una regresión»*. Eso **no se sigue**
de la evidencia. La variabilidad **no demuestra por sí sola que un FAIL sea falso**; tomar el verde
como acreditación es el mismo error con el signo cambiado. **La evidencia se conserva y no se
re-corrió buscando verde.** Detalle: `docs/arnes/ci-1.34.0-no-discrimina/`.

Consecuencia para la puerta: `hooks-en-linux` es la puerta **requerida y estricta** de `main`.
Mientras esto siga así, ninguna afirmación de la forma «el CI está en verde, luego X» tiene
fundamento **para los criterios de reloj**.

## 8. Estado final de agentes y worktrees

| | |
|---|---|
| **Worktrees** | **uno solo**: `/home/juan/dev/ArnesJuan` en `af01436` [`rel/registro-1.33.0`]. Ningún worktree de mutación quedó vivo |
| **Agentes vivos** | **ninguno.** El último (`analista`, terminación de la promesa de `CA-03` en su tercera sede) entregó y cerró. El último (`qa-tester`, re-validación de `CA-03` fuera del contador) terminó su encargo acotado; no se le amplió ni se abrió otra vuelta |
| **Agentes de esta sesión, terminados** | `qa-tester` (modo intercalado, `86a44c8`) · `analista` (write-back `QA-017-24` + cota de `CA-08 (ii)`) · `analista` (`SEC-067` → `NFR-026-01`) · `auditor-seguridad` (`R-028`) · `analista` (corrección `QA-017-31`) |
| **IDs de sesiones anteriores** | **muertos** desde el cambio de cuenta del 2026-09-09; `docs/ESTADO.md` lo dice en su bloque histórico |

## 9. Lo que este cierre NO hizo, dicho expresamente

No se cerró ningún REQ ni hallazgo, no se aceptó ningún residual, no se cambió ninguna garantía, no
se relajó ninguna prueba, no se retiró código, no se fusionó y no se publicó. **Cerrar la sesión no
fue cerrar la versión.** No se abrieron investigaciones nuevas: lo descubierto quedó en §5.c con su
siguiente paso.

---

## 10. «¿Cuánto falta para cerrar?» — y la respuesta depende de qué sede define la versión

Pregunta del propietario, 2026-09-10. Al medirla apareció un desacuerdo de inventario que **yo no
estaba reportando**, y es de la misma familia que `SEC-085`: **dos sedes dicen cosas distintas sobre
el alcance de 1.34.0, y el «cuánto falta» cambia por un factor de tres según cuál se lea.**

*Método:* `grep -l 'Versión destino: 1.34.0' requirements/REQ-0*.md`, descartando a mano el falso
positivo de `REQ-019` (su línea dice «1.35.0 (APLAZADO **desde 1.34.0**…)»), y leyendo `Estado:`,
`QA:`, `Seguridad:` y el campo de hallazgos de cada uno. Sobre `ce79fe7`.

### Sede A — la decisión del propietario del 2026-09-09 (`docs/PLAN.md`): **CUATRO trabajos**

`REQ-017 CA-08` · `REQ-026` · `REQ-023`+`REQ-024` · `REQ-027`.

| REQ | Estado | Bloqueantes | Falta |
|---|---|---|---|
| `REQ-027` | **`completado`** | 0 | nada |
| `REQ-017` | `bloqueado` | **1** (`QA-017-31`) | corrección **hecha**; falta la 2.ª revalidación de QA y la re-firma del auditor |
| `REQ-026` | `en-revisión` | **3** | **no cierra sin la decisión 6**; y `CA-13/14/16/17` sin implementar |
| `REQ-023` | `bloqueado` | **0** en el campo | `D13` (`CA-11` falso bajo dos firmas verdes) + `SEC-052` |
| `REQ-024` | `bloqueado` | **2** | + **la auditoría que nunca se hizo** (`Seguridad: pendiente`) |
| **Total** | | **6** | |

### Sede B — los campos `Versión destino:` de los propios REQ: **DOCE REQ**

A los cinco de arriba se añaden **siete**, y **seis de ellos no se han empezado**:

| REQ | Estado | `QA:` | `Seguridad:` | Bloqueantes |
|---|---|---|---|---|
| `REQ-020` | **`pendiente`** | pendiente | preventiva | **8** |
| `REQ-021` | `bloqueado` | con-hallazgos | preventiva | **2** |
| `REQ-008` | **`pendiente`** | pendiente | pendiente | 0 |
| `REQ-011` | **`pendiente`** | pendiente | pendiente | 0 |
| `REQ-022` | **`pendiente`** | pendiente | pendiente | 0 |
| `REQ-018` | **`borrador`** | pendiente | pendiente | 0 |
| `REQ-025` | **`borrador`** | pendiente | pendiente | 0 |
| **Total ventana** | | | | **16** |

Los cinco con **0 bloqueantes** no están limpios: están **sin construir**, así que nadie ha podido
encontrarles nada. Un `0` ahí significa lo contrario de lo que parece.

### La consecuencia, dicha sin adornos

**No puedo darte un «cuánto falta» sin que decidas qué es 1.34.0.** No es una medición que me falte:
es un desacuerdo entre dos sedes, y la que manda la fijas tú.

- **Con la sede A**, faltan **6 bloqueantes**, la auditoría de `REQ-024`, `D13` y la decisión 6.
  Es una versión cerrable.
- **Con la sede B**, faltan **16 bloqueantes** y **seis REQ sin empezar**, dos de ellos todavía en
  `borrador`. Eso no es un remate: es media versión.

**Y es la misma patología que `SEC-085`**, que el auditor acaba de abrir: un inventario que dos sedes
declaran de forma distinta, y la decisión se toma leyendo la que uno tiene a mano. Aquí la sede A es
la que yo he estado citando en todos mis informes —porque es tu decisión— y la sede B es la que la
máquina leería si alguien derivara el alcance del disco.

### Lo que recomiendo, y no lo ejecuto

**Reconciliar las dos sedes antes de cualquier estimación**, del `analista-requerimientos`: para cada
uno de los siete REQ que sólo están en la sede B, o su `Versión destino:` está desfasado y se mueve, o
la tabla de alcance de `docs/PLAN.md` está incompleta y se completa. **No lo decido yo:** mover la
ventana de un REQ es alcance de versión, y tú lo reservaste expresamente.

Con la sede A reconciliada, la estimación es acotable. **Sin reconciliar, cualquier cifra que te dé
sería falsa en un factor de tres**, y §14.B.2 me prohíbe presentar una hipótesis como compromiso.

---

## 11. Cierre de la sesión — 2026-09-10, `docs/ESTADO.md` §«★★ RETOMAR AQUÍ» es el puntero único

**Nada de este cierre cerró un REQ ni un hallazgo, aceptó un residual, cambió una garantía, relajó
una prueba, fusionó ni publicó.** Cerrar la sesión no fue cerrar la versión.

**Los tres actos finales, todos prescritos por el artefacto del auditor con dueño y archivo:**

1. `SEC-014` y `SEC-083` **retirados** de los campos de `REQ-013` y `REQ-024` — ya estaban `mitigado`
   por firma; lo desfasado era el espejo. Cuadre exacto: el conteo por campo dio **21**, la misma cifra
   que `R-028` midió como «los ve la puerta» **antes** de alinear.
2. `SEC-055 (contrato)` **declarado** en el campo de `REQ-019` — iba en dirección **estricta**: era un
   `contrato` abierto que **la puerta no podía ver**. Ahora ve **22**.
3. El `Estado:` de `REQ-023` **anotado**: dejó de afirmar abierto un hallazgo que QA retiró.

**Cifra firme: 32**, compuesta ahora **22** los ve la puerta · **5** sólo en el registro por diseño ·
**4** sin REQ atribuible · **1** `SEC-085`. Sigue valiendo lo del auditor: **bajo la frontera, 32
devuelve la decisión igual que 17**; lo que cambia es que se puede desmentir fila por fila.

**Comprobado con el lector real, no por mí:** `REQ-019` → `Estado=<bloqueado>`, ventana **1.35.0**
intacta; `REQ-023` → `Estado=<bloqueado>`. **Ningún bloqueo se levantó.** Cola en **16**.

**Y lo que este cierre NO pudo responder, porque no es medición:** «¿cuánto falta?» depende de qué
sede define 1.34.0 — cuatro trabajos según tu decisión, doce REQ según los campos, con **seis sin
empezar**. Está en §10, y su resolución es alcance de versión, que reservaste.
