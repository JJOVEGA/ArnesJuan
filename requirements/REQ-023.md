# REQ-023 — El carácter que no se ve apaga el enforcement: la guarda nombra el CR cuando la propiedad es «una cabecera que no se puede medir»
Estado: borrador
Módulo: hooks (la guarda en el escáner de cabecera de `lib.sh` — qué boca la publica lo decide CA-06, con su precio declarado; las dos ramas de denegación de `guard-completado.sh`) + `tools/arnes-lectura.sh` (el informe que no puede callar lo que la puerta se niega a medir) + banco `tests/escenarios/hooks` + superficie heredada (`AGENTS.md` §13, `templates/AGENTS.md.tpl`, `skills/arnes-upgrade`)
Archivos: hooks/lib.sh, hooks/guard-completado.sh, tools/arnes-lectura.sh, tests/escenarios/hooks/secciones/39-caracter-invisible-1-la-puerta.sh, tests/escenarios/hooks/secciones/39-caracter-invisible-2-la-clase-y-el-corpus.sh, tests/escenarios/hooks/secciones/39-caracter-invisible-3-los-lectores-y-el-coste.sh, tests/escenarios/hooks/run.sh, tests/escenarios/hooks/README.md, AGENTS.md, templates/AGENTS.md.tpl, skills/arnes-upgrade/SKILL.md, CHANGELOG.md
Prioridad: alta
Sensible a seguridad: sí
QA: pendiente
Seguridad: pendiente
Hallazgos abiertos: SEC-052 (contrato)
Rigor: critico
Versión destino: 1.34.0
NFR relacionados: (ninguno)

## Historia

Como proyecto que confía la puerta de cierre al arnés, quiero que **un campo que mi cabecera declara
en letra gobierne o deniegue, pero nunca desaparezca**, para que un carácter que ningún revisor puede
ver en el diff —un BOM que añade PowerShell al redirigir, un byte multibyte partido, un espacio de
anchura cero— no pueda hacer desaparecer **ninguno** de los campos de cuya presencia depende que la
puerta exija algo, ni con él la exigencia que ese campo activa (ejemplos **no exhaustivos**: el suelo
de rigor, el nivel de rigor, la validación, la auditoría, el bloqueo por un hallazgo; el conjunto se
**deriva** y no se lista — CA-02), y cerrar como `completado` un REQ que nadie validó y nadie auditó.

## Criterios de aceptación

- **CA-01 — La propiedad se enuncia por ESTADO: si la cabecera declara un campo, ninguna regla
  contratada lo retira y la máquina aun así no lo lee como ese campo, la cabecera no se puede medir y
  una puerta que no puede medir no deja pasar.** Dado un REQ cuya **cabecera** contiene una línea que,
  leída por una persona, declara un campo de cabecera —la clave tal como la escribe la plantilla—, y
  Dado que **ninguna regla contratada del lector la retira** (la frontera exacta está en CA-11), y Dado
  que el lector del arnés **no** resuelve esa línea como ese campo, Cuando se intenta la transición al
  estado terminal, Entonces la puerta responde **DENY** citando **esa línea** y nombrando qué la hace no
  medible, con el carácter representado de forma **imprimible** (su punto de código o su byte en
  hexadecimal: un carácter invisible dentro del motivo deja a la persona buscando texto que su editor no
  le muestra), y **nunca** permite por **ausencia** del campo que ese carácter borró.

  **Qué claves cuentan como campo de cabecera, con los dos sitios nombrados y sin fingir que es uno.**
  La **escritura** de la clave la fija un solo sitio, la plantilla de `requirements/README.md`; **qué
  claves reconoce la máquina** lo fija el lector (`hooks/lib.sh`, `arnes_campo_linea` y sus llamadores).
  Los dos pueden divergir, y para **este** criterio manda el **lector**, porque el criterio se ejerce
  sobre lo que la máquina lee. Escribir «un solo sitio» donde hay dos es exactamente la falta que
  `SEC-050` mide, y este criterio la llevaba (Historial, 2026-09-08).

  **El criterio se enuncia sobre lo que la cabecera CONTIENE, no sobre en qué función del lector vive
  la guarda:** reescribir el escáner no lo cambia. Ejemplos **no exhaustivos** de la clase: BOM/U+FEFF
  delante de la clave, U+200B–U+200F, U+2060, un byte de arranque UTF-8 sin su continuación (`0xc3`
  suelto), un byte de control C0 distinto de tabulador/LF/CR **ya presente en el disco**.

- **CA-02 — La denegación es por MEDIBILIDAD y no por veredicto, y el DOMINIO DE CAMPOS se DERIVA
  midiendo, no se enumera.** Dado un REQ con **todos** los veredictos en verde —`QA: aprobado`,
  `Seguridad: aprobado`— y un carácter de la clase de CA-01 en cualquier línea de su cabecera, Cuando se
  intenta el cierre, Entonces **deniega igual**, y el motivo es la línea no medible.

  Y Dado **cualquier** campo que pertenezca a la clase **«campo de cabecera cuya AUSENCIA la puerta
  resuelve del lado que ABRE»** —pertenencia que decide el **código**, no este documento, y que el banco
  **deriva midiendo** en cada corrida: se compara el veredicto con el campo declarado en el valor que
  **más** restringe contra el veredicto con el campo **ausente**, y el campo pertenece a la clase si el
  segundo **abre**—, Cuando un carácter de la clase de CA-01 hace desaparecer ese campo, Entonces la
  puerta **deniega**, y resolverlo como **ausencia** del campo **incumple**: la ausencia es justamente lo
  que la puerta perdona, así que un `deny` por «el campo falta» es un `allow` con otro nombre en cuanto
  el rigor declarado sea `ligero`.

  **El dominio no se enumera aquí, y no es una omisión: es el hallazgo.** `SEC-050` (`contrato`, R-013)
  nació de que **tres** textos firmados describieron esta superficie **por enumeración** y nombraron
  **dos** campos donde la medición encontró **cuatro** — y uno de los tres era la remediación que el
  propio auditor había escrito. Este REQ contenía **cinco** enumeraciones de la misma forma antes de la
  corrección del 2026-09-08 (Historial). Es la forma **(a)** de `requirements/README.md`, y aquí
  envejece **hacia el lado que abre**: cada campo nuevo del lector entra en la clase sin que ningún
  texto lo diga.

  **Dónde vive la lista exhaustiva, dicho como está y no como debería estar: en DOS sitios, no en uno.**
  `hooks/guard-completado.sh` decide la dirección de la ausencia de los veredictos y de la clase del
  hallazgo; `hooks/lib.sh` la decide para el suelo de sensibilidad y para el nivel de rigor
  (`arnes_sens_efectiva`, `arnes_rigor_efectivo`). Unificarlas en un sitio único es la remediación 1 de
  `SEC-050`, encargada a **REQ-024**. Hasta entonces **cualquier criterio que cite un solo sitio para
  esta lista es falso**, y está medido: el puntero de **REQ-016 CA-11** manda a `guard-completado.sh`, y
  el campo que retira el **suelo de rigor** no aparece ahí — quien audite la clase siguiendo el contrato
  concluirá que el suelo está a salvo, y no lo está.

  **Anti-vacuidad, y también es criterio:** el caso **aborta** si el dominio derivado queda **vacío**, o
  si **ninguno** de sus campos produce `allow` contra la línea base heredada. **Cuántos campos tiene el
  dominio se mide y se anota en el Historial de este REQ**, no en el criterio: R-013 §2 midió **cuatro**
  de los seis campos que el lector reconoce, es una cifra que **sube** con cada campo nuevo del lector y
  que **REQ-024 existe para bajar**. Escribirla aquí sería la forma **(b)**.

- **CA-03 — La clase se cierra por CONSTRUCCIÓN, no por lista: una entrada RESERVADA que nada nombra
  tiene que denegar igual.** Dado el banco, Cuando corre el caso de la clase, Entonces ejerce un
  **dominio declarado** con al menos tres familias —(i) bytes de control C0 distintos de
  tabulador/LF/CR presentes en el disco, (ii) puntos de código de anchura cero o de formato, (iii)
  secuencias UTF-8 mal formadas: arranque sin continuación, continuación huérfana, sobrelarga y
  sustituto codificado— **y además** una **entrada reservada extraída al azar en cada corrida** del
  complemento de esas familias, que el caso **publica** junto a su semilla para que un fallo se
  reproduzca; y Entonces la puerta responde **DENY** en **todas**, incluida la reservada.

  **El UNIVERSO del sorteo y el PROCEDIMIENTO de inyección se fijan aquí, porque sin ellos el criterio
  tiene tres lecturas con tres costes distintos y una de ellas no es satisfacible.** El sorteo se ejerce
  bajo esta lectura y **sólo** bajo ésta:
  - **Universo:** **cualquier** punto de código, sin restricción previa —no «la clase de CA-01», que
    exigiría un oráculo de la clase; ver abajo—.
  - **Procedimiento:** la entrada reservada se **INSERTA** en una línea que declara un campo
    **reconocido por el lector**, dejando la clave por lo demás intacta.

  Bajo esta lectura la propiedad no necesita ninguna enumeración de caracteres, porque enumera lo que
  **ya está enumerado**, que son las **claves**: retirado de la clave todo lo ajeno al alfabeto de las
  claves que el lector reconoce, si lo que queda **es** una de esas claves, entonces alguien insertó algo
  dentro de una clave y la línea **no se puede medir**. La cata del `desarrollador` (2026-09-08,
  Historial) midió que ese mecanismo cumple CA-04, CA-05 y CA-09 (iii) **sin ADR**, así que este criterio
  y CA-04 son simultáneamente satisfacibles con el lector que existe.

  **Y las otras dos lecturas quedan FUERA DE ALCANCE con su motivo medido, porque saber por qué se
  descartan es lo que impide que vuelvan** (reparto, dueño y ventana en «Fuera de alcance»):
  - **Sustitución por homóglifo** —`Е` cirílico en `Еstado`, `Ｅ` fullwidth—: **no satisfacible con lo
    que existe**. La clave resultante no contiene nada ajeno al alfabeto: es **otra** clave, y el lector
    la resuelve como «no hay campo». Sus tres salidas son las tres que este REQ ya prohibió o encareció.
  - **Universo = «la clase de CA-01»**: el sorteo necesitaría un **oráculo de la clase**, y el único
    disponible sería una tabla; si es la misma tabla que usa la guarda, **el caso pasa por
    construcción**. Es la forma **(d)** que REQ-021 cazó en la ventana 1.33.0, y por eso el universo se
    declara arriba sin restricción previa.

  **Este criterio está escrito para que una implementación por LISTA DE PROHIBIDOS lo incumpla, y eso
  es su razón de ser.** Si el caso pasa con las familias declaradas y falla con la entrada reservada, el
  hallazgo es **contra el código** y no contra el criterio: ensanchar la lista sería la **sexta**
  derrota medida de esa vía en este repositorio (`ADR-002`, SEC-020, SEC-024, SEC-025, H-01), y la
  propiedad que no envejece se enuncia sobre el **estado** —una cabecera que no se puede medir— y no
  sobre el carácter, que tiene una instancia nueva cada vez.

  **Anti-vacuidad, y también es criterio:** el caso **aborta** si el dominio queda vacío, si alguna de
  las tres familias no aporta ninguna entrada, o si **ninguna** de sus entradas produce `allow` contra
  la línea base heredada — «todas denegaron» es cierto por vacío cuando no había nada que denegar, y un
  instrumento que ante la ausencia de datos responde verde es la familia de defecto que `REQ-020`
  existe para cazar. Cuántas entradas de cada familia producían `allow` antes del arreglo se **mide y
  se anota en el Historial de este REQ**, no en el criterio: es una cifra que envejece con cada familia
  nueva.

- **CA-04 — Y no se estrecha NINGUNA tolerancia existente: esta guarda no puede convertirse en una
  prohibición de escribir en español.** Dado el corpus de cabeceras que el corredor del banco descubre
  **por glob** en su directorio de secciones (sitio único de ese corpus) **y** las cabeceras de los REQ
  de `requirements/` de este árbol, Cuando la versión con la guarda las juzga, Entonces decide
  **exactamente lo mismo** que la versión heredada —**campo a campo y decisión a decisión**, medidas las
  dos **en la misma corrida**—. En particular siguen gobernando idéntico: la **clave decorada o
  sangrada** (REQ-016 CA-04), el **paréntesis de evidencia**, el **plegado de ortografía** (REQ-010, con
  su frontera: la `ñ` no se pliega) y un documento entero en **CRLF** (REQ-016 CA-12).

  **Anti-vacuidad:** el caso **aborta** si el corpus no contiene al menos una cabecera con un carácter
  **no ASCII** legítimo **en la CLAVE**, al menos una con un carácter no ASCII legítimo en el **valor**,
  y al menos una con la **clave decorada** — sin las tres, la equivalencia es cierta por vacío y no
  acredita nada. Ejemplos **no exhaustivos** de caracteres legítimos que hoy viven en cabeceras de este
  árbol: `—`, `·`, `í`, `ó`, `ñ`, `§`.

  **La clave se separa del valor a propósito, y es la mitad que decide el coste de este REQ.** La
  plantilla de `requirements/README.md` escribe **dos** claves con letra no ASCII —`Módulo:` y, en este
  REQ, `Versión destino:`—, así que un conjunto admitido «en positivo» sobre el **segmento de clave** no
  es el alfabeto ASCII y tropieza con REQ-010 (`arnes_pliega_ortografia`, escrito con **escapes de
  bytes** y sin rangos, precisamente por CA-05). Un caso que sólo ejerza el valor daría verde a una
  guarda que deniega `Módulo:`. Es el dato que la pregunta abierta 1 necesita **antes** de escribir la
  guarda, y por eso vive en el criterio y no en una nota.

  **Y esta es la mitad que decide si el arreglo sirve:** un conjunto admitido demasiado estrecho
  deniega cabeceras legítimas, la fricción es constante, y la fricción termina con alguien apagando el
  guard (`AGENTS.md` §13) — un guard apagado protege menos que uno parcial.

- **CA-05 — El veredicto de la guarda es INVARIANTE al locale.** Dado el mismo documento, Cuando la
  puerta lo juzga bajo el locale del entorno y bajo `LC_ALL=C`, Entonces el veredicto y el motivo son
  **idénticos**, y la comprobación se ejerce en las dos configuraciones **en la misma corrida**. Motivo,
  y está medido dos veces en este arnés: las tablas de ortografía se escriben con **escapes de bytes**
  y sin rangos «porque no hay rangos ni clases sujetas a colación» (`hooks/lib.sh`,
  `arnes_pliega_ortografia`), y R-012 verificó la equivalencia del escáner **bajo los dos locales**
  precisamente porque era el único eje por el que el cambio podía abrir. Una clasificación que dependa
  de `LC_CTYPE` **deniega en el CI de Linux y permite en Windows/MSYS**, que es donde viven los
  proyectos consumidores y de donde sale el BOM: sería un fail-open **por entorno**, invisible en la
  puerta requerida de `main`.

- **CA-06 — Los lectores no divergen, y la guarda es OBSERVACIONAL.** Dado el mismo documento, Cuando
  lo leen la puerta (`hooks/guard-completado.sh`), el informe (`tools/arnes-lectura.sh`), el bloque
  derivado (`hooks/campos-req.awk`) y `tools/arnes-paralelo.sh`, Entonces el valor de **cada** campo es
  **idéntico** en los cuatro, y es el **mismo** que antes de esta versión: la guarda **observa** y no
  cambia ningún valor de campo, así que la transcripción declarada de `hooks/campos-req.awk` sigue
  diciendo lo mismo y el bloque derivado no cambia de opinión. La guarda **no se duplica** en ninguna
  boca: una guarda en una sola boca deja las otras abiertas, y dos transcripciones de la misma regla se
  desfasan. La regla que ordena las dos operaciones —primero el rango de cita, después el descuento del
  CR— **no se altera**: retirar un carácter no puede destruir un delimitador pero sí puede crearlo
  (H-01).

  **Y el SITIO de la guarda no es libre, porque `arnes_campo_linea` NO es la única boca — medido, y la
  redacción anterior de este criterio decía lo contrario.** `arnes_estado_cabecera` llama a
  `arnes_norm_clave` **directamente dos veces** (`hooks/lib.sh:1880` y `:1891`), y la de **1880** le pasa
  la línea **CRUDA, pre-cita, a propósito**: existe para distinguir «aquí no hay estado» de «alguien
  declara el estado desde dentro de una cita» (comentario en `hooks/lib.sh:1874-1878`). Por tanto, y es
  exigencia de este criterio: **la publicación de la guarda no viene de esa llamada cruda de
  `hooks/lib.sh:1880`.** Si viniera de ahí, un REQ cuyo campo está **legítimamente comentado** con
  `<!--` **sin espacio** dispararía la guarda —medido en la cata (Historial)— y el veredicto dependería
  de si quien escribió el comentario puso un espacio: viola CA-11 y CA-04.

  **Las dos salidas tienen precio, y se declara aquí en vez de dejarlo al implementador, porque es el
  RADIO del cambio:** publicar desde `arnes_campo_linea` deja el campo `Estado` —el estado terminal—
  **sin guarda en su propio lector**; publicar desde `arnes_norm_clave` exige un **interruptor por
  llamador**, con lo que la guarda deja de ser «la primera sentencia del único escáner» como sí lo es la
  del CR (invariante de REQ-016). Elegir es parte de la comisión; **no elegir en silencio** es lo que
  este criterio impide.

  **Y el conjunto de CLAVES no gana una transcripción más: la guarda lee UNA constante, no teclea su
  propia lista.** El número de transcripciones del conjunto de claves crece en **no más de 0**
  (**operativo**: **menos** es conforme y **no** es hallazgo — unificar las que ya existen cumple). La
  pertenencia al conjunto la decide el **código** y su enumeración vive hoy, **medido y no listado de
  memoria, en más de un sitio dentro de bash**: los brazos `case` de `arnes_campos_req`
  (`hooks/lib.sh:1833-1839`) y los dos de `arnes_estado_cabecera` (`:1881`, `:1892`); fuera de bash, la
  transcripción **declarada** de `hooks/campos-req.awk` (la cadena `else if (k == …)`, hoy en `:75-80`)
  y la **derivación** —no copia— de `tools/arnes-lectura.sh:66-72`, que lo extrae de `lib.sh` con `sed`
  precisamente para no envejecer. Si la guarda teclea su lista, un campo nuevo que entre en los brazos y
  no en la guarda **deja de estar protegido en silencio**: envejece hacia el lado que abre, que es
  exactamente la clase de `SEC-050`. **Entonces:** el conjunto se declara **una** vez en bash como
  constante delimitada al estilo de `ARNES_VOCAB_*` (`hooks/lib.sh:1741-1749`) y la guarda **la usa**, en
  vez de repetirla.

  **Y lo que este criterio NO exige, dicho para que no se haga «de paso»:** unificar la transcripción de
  `hooks/campos-req.awk` con esa constante. No se puede transcribir una constante de bash a awk sin
  duplicarla o sin pasarla por `-v`, es un cambio que **no** es observacional, y ese archivo está fuera
  de `Archivos:` a propósito (ver «Coordinación»). Que las claves vivan enumeradas en awk **y** en bash
  es la misma familia que `SEC-050` y le corresponde a la unificación que REQ-024 contrata, no a esta
  guarda. Si la implementación creyera necesitarlo, es una **desviación**: se declara con su entrada en
  el Historial y se re-examina este criterio.

  **Y el ALFABETO de las claves se DERIVA de esa constante con escape, nunca se teclea:** una clave
  futura que lleve `-`, `]` o `^` rompería la expresión de corchete que la guarda construya. Lo pidió el
  `desarrollador` en la cata y va en el criterio, no en su cabeza.

- **CA-07 — El informe no calla sobre lo que la puerta se niega a medir.** Dado un REQ con un carácter
  de la clase de CA-01 en su cabecera, Cuando corre `tools/arnes-lectura.sh`, Entonces lo reporta por
  la **vía única** por la que ya reporta anomalías —salida **≠ 0**— nombrando el REQ, la línea, el
  carácter en forma imprimible y la consecuencia («la puerta de cierre DENIEGA»). Medido en la ventana
  anterior por qué hace falta: sobre el caso peligroso equivalente el informe respondía «Ningún valor
  anómalo» con `rc=0` (REQ-016, H-03), y aquí el diff **tampoco** lo muestra, así que el informe es la
  **única** superficie donde una persona puede verlo antes de intentar cerrar.

- **CA-08 — Caso de banco con fail-before y controles en las DOS direcciones.** Dado el banco, Cuando
  corre la sección nueva, Entonces contiene:
  (i) **las dos filas medidas de SEC-047** reproducidas **como los fixtures concretos que son, no como
  la cobertura de la superficie** —quien cubre la superficie es CA-02, derivándola, y estas dos filas no
  la agotan ni pretenden hacerlo—: `Sensible a seguridad: sí` con BOM delante y `Rigor: ligero`, con
  `QA: pendiente` y `Seguridad: pendiente`; y un carácter invisible delante de `Hallazgos abiertos:` con
  un hallazgo de clase `contrato` declarado — las dos **fallan** contra los hooks de la versión heredada
  (**fail-before**) y **pasan** contra los de esta versión;
  (ii) **control negativo:** la misma cabecera **sin** el carácter decide **igual que antes** (DENY, por
  su propio motivo y citándolo);
  (iii) **control positivo de no-regresión:** un REQ cuyos veredictos **sí** autorizan el cierre y cuya
  cabecera no lleva ningún carácter de la clase **cierra**.
  Ningún caso marcado «(era ALLOW)» pasa contra la línea base.

  **Dependencia declarada, no contratada aquí:** el fail-before se mide materializando `hooks/` desde
  un **tag** del propio repositorio, que es la ruta de ejecución que **SEC-048** describe — clase, dueños
  y estado **citados** y no declarados aquí: `docs/seguridad/registro-seguridad.md:3689`. Este REQ **no**
  la contrata ni la arregla; la nombra para que nadie lea su verde como acreditación de que el árbol de
  referencia es inmutable. **Y es una precondición de calendario, no sólo una nota:** mientras ese gate
  humano no esté, `CA-08` no se puede medir — es uno de los dos motivos por los que este REQ salió de
  1.33.0.

- **CA-09 — El coste, con las DOS vías por las que se degrada y en la MISMA corrida.** Dado el camino de
  lectura de la cabecera, Cuando se le añade la guarda, Entonces, medidos **en la misma corrida** y
  contra la línea base de la versión heredada:
  (i) **no más de 0 procesos añadidos** por evaluación (**operativo**: **menos** es conforme y **no** es
  hallazgo);
  (ii) el **mínimo de k repeticiones** del reloj de la ruta crítica del banco es de **no más de 1,25×**
  el de la línea base medida en esa misma corrida (**operativo**: se **baja** con la medición); y
  (iii) el **cociente de duplicación** —doblar la longitud de línea y comparar los dos costes— se
  mantiene **lineal**: **no más de 2,2** (**operativo**), medido sobre **el escáner en el que la guarda
  de este REQ resida**, determinado por el **código** y no por este texto; si la guarda reside en más de
  uno, sobre **todos** ellos.
  El estadístico es el **mínimo de k**, nunca la media; k se elige para que el mínimo de cada serie
  supere el suelo por debajo del cual el reloj no distingue del ruido, y una sonda que no llega a ese
  suelo o no encuentra su línea base emite **SKIP con el motivo y con el número que sí obtuvo**, nunca
  PASS (doctrina y suelo: sitio único en `requirements/README.md`; instrumentos en `tests/util/` cuando
  REQ-021 suelte).

  **El SUJETO de (iii) se enuncia por propiedad y no con el nombre de una función, porque la primera
  redacción nombró el escáner equivocado y está medido.** La redacción anterior anclaba el cociente en
  `arnes_sin_cita`. La guarda que CA-01 necesita **no puede vivir ahí**: necesita el **segmento de
  clave**, que `arnes_sin_cita` no produce, y partirlo por `:` dentro sería una **segunda transcripción**
  de la regla de clave (lo que CA-06 prohíbe). Medido por el `desarrollador` (Historial): el cociente de
  `arnes_sin_cita` se queda en **1,06 con y sin guarda** —porque la guarda no está ahí— mientras un
  candidato cuadrático marca **2,63–3,65** en `arnes_norm_clave`, **donde nadie miraba**. Es decir: un
  (iii) que nombrara `arnes_sin_cita` **daría PASS a una guarda cuadrática**, y (ii) tampoco lo salvaría
  —a n=60 el cuadrático es ×3 sobre el camino de línea y puede diluirse por debajo de 1,25× en la ruta
  crítica—. Un criterio de coste escrito con el nombre de hoy envejece con la **primera mudanza** del
  código: ése es el defecto, y es la forma **(d)** una vuelta más.

  **Precisión que no se maquilla:** el mecanismo que la cata declara conforme marca **2,05 a n=2000
  contra un techo de 2,2**. Es **margen estrecho**, y sólo cumple porque se miden funciones distintas; si
  la implementación se aleja de ese mecanismo, el margen se vuelve a medir antes de darlo por bueno.

  **Qué magnitud mide y por qué es ésa la que se degrada — y son tres porque el mecanismo tiene tres
  vías ortogonales.** La guarda se ejecuta **una vez por línea de cabecera** y en cada evaluación de la
  puerta (dónde reside exactamente lo decide CA-06, con su precio declarado): se degrada (a) por un
  `fork`, que el
  reloj de una máquina rápida esconde; (b) por el reloj de la ruta crítica, que los procesos no ven; y
  (c) por el **orden de crecimiento en la longitud de línea**, que ni el reloj de una entrada pequeña ni
  los procesos ven — y que es exactamente la regresión de **10×** que la ventana 1.33.0 acaba de pagar
  (`${l%$CR}` era cuadrático, `CA-08` de REQ-016 daba verde midiendo procesos). Una comprobación «por
  cada carácter de la línea» escrita con indexación de cadena en bash es cuadrática por construcción:
  (iii) es el criterio que lo caza, y es **auto-anclado** —no depende de ningún tag— así que no envejece
  hacia el lado que abre.

- **CA-10 — La superficie heredada deja de nombrar el carácter y pasa a nombrar la propiedad.** Dado
  `AGENTS.md` §13 y **la misma fila** de `templates/AGENTS.md.tpl`, Cuando un proyecto nace del
  andamiaje o lo actualiza, Entonces la invariante está escrita **por propiedad** —una línea de la
  cabecera que la máquina no puede medir no deja cerrar— con el retorno de carro como **instancia** y no
  como definición, y con las tres fronteras conformes (el CR/LF **final** es transporte, el cuerpo del
  REQ no se restringe, **reabrir** no se bloquea). Y Dado `skills/arnes-upgrade/SKILL.md`, Entonces su
  apartado de migración dice, sin eufemismo, que las versiones afectadas **pudieron cerrar un REQ
  `critico` sin validación ni auditoría**; enuncia la pregunta que acredita ausencia de exposición
  **por estado** —«cuáles de mis REQ en estado terminal **no cerrarían hoy**»— **antes** de ofrecer
  ningún comando; y acompaña **cada** barrido por vía, en el mismo sitio que el comando, de las tres
  declaraciones que REQ-016 CA-09 (ii) ya exige: que interroga **una** vía y no la propiedad, el
  **nombre** de las vías conocidas que ese comando no encuentra (sitio único:
  `docs/seguridad/registro-seguridad.md`), y que **no hallar nada no acredita ausencia de exposición**.
  Ninguna frase del apartado afirma completitud sobre un barrido por vía.

  **Y el criterio se enuncia por PROPIEDAD para que su verdad no dependa de si la comprobación por
  estado existe todavía** —que es una contingencia de otro REQ y estuvo abierta como pregunta 2 de este
  documento—. La exigencia es: el texto **nombra** la pregunta por estado, y **sobre los comandos dice lo
  que sea cierto en el momento de escribirlo**: si en ese momento **ningún** comando la responde, lo dice;
  si alguno la responde (la herramienta que REQ-016 dejó en «Fuera de alcance» como `instrumento` con
  dueño `desarrollador`), lo **cita** y conserva igualmente las tres declaraciones de REQ-016 CA-09 (ii)
  junto a **cada** barrido por vía. En las dos ramas se **prohíbe** lo mismo: afirmar completitud sobre
  un barrido por vía. Así el criterio no envejece con el calendario de otra comisión, y **no** hace falta
  decidir de antemano si CA-10 entra «completa» o «sólo la fila».

- **CA-11 — La frontera con la mitad que NO va aquí, escrita como criterio y no como nota.** Dado un REQ
  en el que un campo de la clase de CA-02 **no llega a declararse** —porque no está, o porque **una
  regla contratada del lector lo retira**—, Cuando la puerta lo juzga, Entonces decide **exactamente lo
  mismo** que antes de esta versión: la ausencia se sigue perdonando por compatibilidad con los REQ
  anteriores a que los veredictos existieran, tal como **REQ-016 CA-11** lo contrata, y este REQ **no**
  lo toca.

  **Y el caso que la medición hace fallar, escrito como caso porque la elección obvia del sitio de la
  guarda lo incumple.** Dado un REQ cuyo campo está **legítimamente comentado** con la secuencia de
  apertura **sin espacio** antes de la clave —`<!--Estado: completado -->`, `<!--Rigor: ligero-->`—,
  Cuando la puerta lo juzga, Entonces el veredicto y el motivo son **idénticos a los de la versión
  heredada**, y **la guarda de este REQ no dispara**. Medido en la cata contra `arnes_estado_cabecera`
  real (Historial, 2026-09-08): publicando la guarda desde la llamada cruda de `hooks/lib.sh:1880`, las
  dos formas **sin espacio** disparan la guarda y la de **con espacio** no — un **falso positivo** cuyo
  veredicto depende de si quien escribió el comentario puso un espacio. La exigencia sobre el sitio de
  publicación que lo cierra es de **CA-06**; el caso vive aquí porque lo que se viola es esta frontera y
  la tolerancia de CA-04.

  **La línea que separa las dos mitades es una propiedad, no una comodidad: ¿la retirada de la
  declaración la DECIDE una regla contratada del lector, o no la decide nadie?**
  - **La decide una regla contratada** —la noción de **rango** de comentario de REQ-016, cuya semántica
    está escrita, firmada en R-009 y cuyo efecto un revisor reproduce leyendo el documento—: entonces es
    el dominio de la **semántica de la ausencia**, que es una **decisión** y no un defecto, y se revisa
    en su propio REQ (ver «Fuera de alcance»).
  - **No la decide ninguna regla** —la línea declara el campo, ninguna regla contratada la retira, y el
    lector aun así no la resuelve—: entonces es el dominio de **CA-01**, la cabecera **no se puede
    medir** y a partir de aquí **gobierna o deniega, nunca abre**.

  **Esta frontera se reescribió porque la anterior estaba MEDIDA falsa, y el criterio lo dice en vez de
  taparlo.** La primera redacción de este REQ decía «¿el documento lo declara **en letra**?». Bajo esa
  letra,
  `<!-- Sensible a seguridad: sí -->` **declara el campo en letra** —una persona lee `sí`—, así que
  caería en el dominio de CA-01 y el `ALLOW` medido en R-013 sería un **incumplimiento de CA-01** de una
  vía que este REQ no arregla ni pretende arreglar: el criterio se habría desmentido a sí mismo el día
  que alguien lo probara. La propiedad de arriba reparte los dos casos sin ambigüedad y sin mover ni un
  byte de alcance.

  **Y la equivalencia sobre la que se apoya no es una suposición: está medida** (R-013 §1, contra el
  **entrypoint real**, con control positivo y negativo en la misma tanda). Comentar la línea de un campo
  y **borrarla** deciden **idéntico** — es el control de equivalencia que REQ-016 CA-11 pone como
  criterio, y por eso comentar **no es un defecto nuevo** sino ese criterio funcionando. Medido en
  varios campos de la clase, incluido el que retira el **suelo** de rigor y el que fija el **nivel**; y
  medida también la **excepción** que delimita la clase, que sigue vigente: el veredicto de seguridad
  **deniega igual** comentado en un REQ de rigor efectivo `critico`. Ejemplos **no exhaustivos**: la
  tabla de `docs/seguridad/registro-seguridad.md` § R-013 §1, que es el sitio único de esas mediciones.

  **Y la acotación es de esa clase y sólo de esa:** cualquier otra vía por la que una cabecera que **sí**
  declara un campo, y que **ninguna regla contratada retira**, deje **cerrar** sigue **incumpliendo
  CA-01**, y se corrige en el código, nunca ensanchando esta frontera.

- **CA-12 — La noción de cita no gana una transcripción más, y el verde de este REQ no acredita la
  clase.** Dado que la propiedad de CA-01 —*lo que no se puede medir se deniega, nunca se resuelve del
  lado que abre*— vale para **cualquier** lector del arnés que descarte contenido, y Dado que existe un
  segundo lector con su **propia** noción de comentario y su **propio** contrato
  (`arnes_cola_pendientes`, cuya frontera `hooks/lib.sh` declara **por escrito** en la cabecera de
  `arnes_sin_cita`), Cuando se implementa esta guarda, Entonces:
  (i) el número de transcripciones de la noción de cita de la **cabecera** crece en **no más de 0**
  (**operativo**: **menos** es conforme y **no** es hallazgo — unificar las dos que ya existen, la
  función y su transcripción declarada en `hooks/campos-req.awk`, cumple);
  (ii) `arnes_cola_pendientes` cuenta **exactamente lo mismo** —entrada a entrada— **y devuelve el mismo
  código de retorno**, comparando **el corpus de colas que el banco ejerce en el momento de esta
  versión** contra la **versión heredada de esta misma ventana**; es una no-regresión **anclada a ese
  corpus y a esa comparación**, y **no** la afirmación abierta «la cola nunca cambia su conteo» (ver el
  conflicto registrado abajo); y
  (iii) ningún artefacto que este REQ escriba —criterio, sección del banco o texto heredado de CA-10—
  afirma ni sugiere que la clase quede **cerrada**: la superficie que comparte la propiedad y **no** se
  corrige aquí queda **nombrada** en «Fuera de alcance» con dueño, ventana y forzador.

  **Por qué es criterio y no nota: es la deriva más probable de esta comisión, y el propio código dice
  qué cuesta.** `SEC-051` propone como remediación que los dos lectores compartan **una** transcripción,
  y quien implemente este REQ va a estar editando **esa misma función y ese mismo archivo** en la misma
  ventana. Pero `hooks/lib.sh` declara esa frontera **a propósito** y deja escrito el precio: unificar
  cambia el **conteo** de la cola —`### Real <!-- nota -->` pasa a contar— y **un cambio de conteo en la
  cola es un cambio de veredicto en la puerta**, es decir un cambio del contrato de **REQ-009**
  (CA-04/CA-07), que está `completado`. Hacerlo «de paso», porque el archivo ya estaba abierto, es un
  cambio de alcance sin ADR y sin write-back: la deriva que `AGENTS.md` §9 nombra. (ii) es lo que lo caza
  **midiendo**, no recordando.

  **Y para ESTA guarda (ii) se cumple POR CONSTRUCCIÓN, verificado ejecutando — que vale más que la
  prueba y deja claro quién puede cambiar el conteo.** `arnes_cola_pendientes` (`hooks/lib.sh:1152-1181`)
  **no llama** a `arnes_sin_cita`, ni a `arnes_campo_linea`, ni a `arnes_norm_clave`: su **única** llamada
  es `arnes_lee_archivo`, y su noción de comentario es **propia e inline** (`:1160-1163`). Una guarda que
  viva en el escáner de la **cabecera** no puede alcanzarla. Lo que sí puede cambiar ese conteo es
  **REQ-024** —que lo cambia a propósito, con ADR—, no este REQ; y por eso (ii) es una no-regresión
  anclada y no una promesa perpetua.

  **CONFLICTO REGISTRADO con REQ-024 CA-08/CA-09, y por qué NO hay contradicción con la redacción de
  arriba.** `REQ-024` (`borrador`, ventana **1.34.0**) contrata en **CA-08** que un rango que abre y no
  cierra haga que `arnes_cola_pendientes` devuelva **rc ≠ 0** con salida vacía en vez de `0` con `rc=0`, y
  en **CA-09** que una entrada con una secuencia de **cierre huérfana** —`### Migrar A --> B`— pase a
  **contar**: los dos **cambian** el conteo y el `rc` para esas dos formas. Su propio analista lo registró
  (`requirements/REQ-024.md:514-522`). Con (ii) anclado a **su** corpus y a **su** comparación no hay
  contradicción: este REQ acredita que **él** no movió el conteo, y REQ-024 lo mueve donde declara
  moverlo. La contradicción **sí** existiría si (ii) se implementara como no-regresión **abierta**, y
  entonces REQ-024 rompería una prueba de este REQ y haría falta el write-back de este criterio. Por eso
  el caso del banco que ejerce (ii) **fija su corpus y su línea base en la corrida**, y no afirma nada
  sobre versiones posteriores.

*(12 criterios.)*

## Notas / alcance

### La ventana es 1.34.0, y aplazarlo no incumple ningún vencimiento

**Decisión del propietario, 2026-09-08** (la segunda del mismo día sobre este REQ): **1.33.0 se publica
sin REQ-023**, con REQ-017, REQ-021 y la partición de las tres secciones del banco. Este REQ pasa a
**1.34.0**.

**El motivo, medido:** su coste se midió **después** de meterlo en 1.33.0 — **cuatro** comisiones en
serie detrás de REQ-021, sin paralelismo posible (ver «Coordinación»), y con **dos precondiciones
ajenas**: `CA-09 (iii)` y `CA-09 (ii)` esperan a que REQ-021 suelte `tests/util/`, y `CA-08` depende del
tag desde el que se materializa la línea base (`SEC-048`, gate humano del propietario).

**Y aplazarlo no incumple nada, citado y no recordado:** el vencimiento de la mitad (1) de `SEC-047` es
el **cierre de 1.34.0** (`docs/seguridad/registro-seguridad.md:3684`, tabla en `:4229-4239`), así que
meterlo en 1.33.0 había sido un **adelanto** — y el propio registro lo dice: *«adelantar por decisión de
quien manda no crea un vencimiento nuevo, y desandar el adelanto no incumple ninguno»*
(`:4241-4244`). La cláusula de escalada vigente, por propiedad y no por fecha, está en `:4303-4317`, y
en qué ventana decida el propietario hacer el trabajo es **explícitamente** lo que **no** la dispara.

### El defecto, y por qué la guarda que ya existe está bien construida y aun así no lo ve

Medido por el auditor **ejecutando `guard-completado.sh` real** (R-012, § SEC-047; proyecto de prueba,
todo lo demás en verde): un **BOM** delante de `Sensible a seguridad: sí`, con `Rigor: ligero`, cierra a
`completado` un REQ con `QA: pendiente` y `Seguridad: pendiente`; sin el BOM, deniega. Un `0xc3` suelto
o un **U+200B** delante de `Hallazgos abiertos:` retiran un hallazgo `contrato` que estaba bloqueando.
El carácter **borra el campo**, y para todo campo de la clase de **CA-02** —la que se **deriva**, no la
que se lista— **la ausencia ABRE**. Los dos casos de arriba son las **filas medidas** por R-012, no la
superficie: R-013 midió después que la clase alcanza **más** campos de los que SEC-047 nombró, y ése es
`SEC-050`. Presente e **idéntico** en `v1.30.3`, `v1.31.0`, `v1.32.0`, `v1.32.1` y este árbol.

La guarda del CR **es correcta y está bien construida**: es una propiedad y no un sitio, es la primera
sentencia del único escáner, y está contratada en **REQ-016 CA-12** y firmada en **R-009**. Lo que está
mal trazada es su **extensión**: nombra **el CR** cuando la propiedad verdadera es *«una línea de
cabecera que la máquina no puede medir»*. El CR es **una instancia** de esa clase, no la clase — es
descripción **por enumeración** donde tocaba **por propiedad**, el defecto exacto que **REQ-012**
proscribió en los criterios, reaparecido en el código que esos criterios gobiernan.

**Y no hace falta malicia.** PowerShell añade BOM al redirigir y los proyectos consumidores trabajan en
Windows/MSYS. Es la forma que `AGENTS.md` §13 ya documenta —«una preferencia por la consola desactivó un
guardián sin que nadie relacionara las dos cosas»— una vuelta más.

### Latente, no vivo: no hay nada que reabrir

R-012 barrió el árbol y **toda la historia** de `requirements/`: **ningún** REQ, presente o pasado, llevó
jamás un carácter invisible en su cabecera. La vulnerabilidad es **latente**, no un incidente: **no hay
cierres contaminados** y este REQ **no reabre** ningún REQ `completado`. Lo que sí hereda un proyecto
consumidor es la **pregunta** (CA-10), porque su corpus no lo ha barrido nadie.

**R-013 repitió el barrido para la otra vía —la del comentario— y sale igual de limpio**, con control
positivo del propio barrido (inyectó un campo comentado en un blob real y lo encontró; sin él, cero):
**133** blobs únicos de `requirements/*.md` alcanzables desde todas las referencias, cross-check
independiente por otra vía y cross-check de rutas, **cero** comentarios en cabecera. Las **únicas**
apariciones de `<!-- Sensible a seguridad: sí -->` en toda la historia del repositorio son la prosa de
este mismo REQ documentando la hipótesis — **cuerpo, no cabecera**. Se anota porque el reparto de las dos
mitades se apoya en que **ninguna** de las dos esté viva: si la vía del comentario tuviera un cierre
contaminado detrás, aplazarla a 1.34.0 no sería defendible.

### Por qué el criterio central es una propiedad por ESTADO y no las otras dos salidas

Se descartan, con motivo, las dos alternativas que estaban sobre la mesa:

**(a) Añadir el BOM y el U+200B a la lista del carácter descontado — NO.** Sería la **sexta** derrota
medida de esa vía en este repositorio. Un `0xc3` suelto, un U+FEFF, un U+2060, un byte multibyte
inválido o el siguiente que nadie ha pensado la reproducen: una lista escrita en un contrato envejece
**hacia el lado que abre**. `CA-03` está redactado para que esta salida **falle la prueba**.

**(b) Reconocer la clave por una gramática restringida y NOMBRAR lo que no case — descartada como
mecanismo, adoptada como consecuencia.** Como mecanismo tiene dos problemas: si «no casa» se resuelve
como «no hay campo», *es* el fail-open de hoy con otro nombre; y para que fuese seguro tendría que
resolverse como «no se puede medir», que es (c). Además tocaría **dónde se aplica la tolerancia de
clave decorada**, que REQ-016 **descartó por nombre**: recortarla reabre un fail-open real —un estado
terminal escrito en forma decorada que ninguna puerta veía—. Lo que sí se adopta de (b) es su
**consecuencia observable**: la línea se **nombra** en vez de ignorarse (CA-01 en la puerta, CA-07 en el
informe).

**(c) La propiedad por estado, con DENY por medibilidad — SÍ, y es el patrón que ya funcionó.** Es
exactamente la forma de **REQ-016 CA-12** y de **CA-03** (rango que abre y no cierra), firmadas en
R-009: *una puerta que no puede medir no deja pasar*. Cuatro razones:

1. **No estrecha ninguna tolerancia, así que no puede reabrir nada.** Es el mismo argumento con el que
   se aceptó el arreglo del CR: `Estado: comple`+CR+`tado` deja de leerse como estado terminal **por
   denegación, no por ausencia**.
2. **Va en el mismo sitio** —primera sentencia del único escáner—, así que no reabre la clase de las
   **cuatro bocas** que R-009 y R-012 acreditaron cerrada.
3. **Es observacional:** no cambia ningún valor de campo, luego ningún lector aguas abajo puede cambiar
   de opinión y `campos-req.awk` no se duplica. Ésa es la propiedad que **contiene el radio** del
   cambio, y la razón técnica por la que es auditable.
4. **Enunciada por estado, no envejece.** No describe ninguna vía, así que cubre las dos caras conocidas
   y las que nadie ha descubierto — el mismo argumento con el que REQ-016 CA-09 (i) sustituyó «qué
   revisar» por «qué no cerraría hoy».

### La mitad que NO va en este REQ, y el motivo — es la parte que hay que discutir

**Que la ausencia de un campo ABRA es un problema independiente del carácter invisible.** La clase, y su
superficie derivada en vez de listada, están en **CA-02**; los dos sitios donde hoy vive la decisión son
`hooks/guard-completado.sh` (veredictos y clase del hallazgo) y `hooks/lib.sh` (`arnes_sens_efectiva`,
`arnes_rigor_efectivo`: suelo y nivel). SEC-047 pide las dos mitades y dice que «la (1) sin la (2) deja
abierta cualquier otra vía futura de hacer desaparecer una línea».

**Y R-013 midió que no hace falta ninguna vía futura: la (2) tiene instancia alcanzable HOY.** No es una
lectura: es ejecución contra el entrypoint real, en la instalada **1.32.1** y en `cand/1.33.0`, con
control positivo, negativo y de equivalencia en cada tanda. El bypass es **silencioso por completo** —ni
`deny`, ni `systemMessage`, ni aviso de vocabulario, porque los veredictos citados están *dentro* del
vocabulario y el campo de sensibilidad no se compara contra ninguno—. Tres variantes miden el mismo
mecanismo sobre tres campos distintos de la clase de CA-02, incluido el que retira el **suelo** y el que
fija el **nivel**. Consecuencia sobre el reparto, y es sólo ésta: el **forzador** de REQ-024 sube de
«decisión defendible» a «bypass alcanzable con una edición de una línea que aparece en el diff» — y que
un revisor humano lee como *«el REQ declara que es sensible»* mientras la máquina lee *«no declara
nada»*. Que se vea **no** la hace inocua: la hace **plausible**, que en un pipeline de agentes es peor.

**Alcance del bypass, medido, para no exagerarlo — con la salvedad que lo compone.** El `Rigor: ligero`
que se obtiene retirando la sensibilidad **no** salta la clase del hallazgo, **ni** las quality gates,
**ni** la cola de aprobaciones humanas: las tres siguen denegando. **Pero la cola sólo detiene si se
puede contar,** y `SEC-051` mide que una entrada anotada al estilo de este proyecto cuenta **cero**. Las
dos mitades **se componen**: retirar los campos y perder el gate humano dejaría un cierre sin ninguna
puerta en pie. Con el cambio de ventana, **las tres piezas caen ahora en 1.34.0** —este REQ, REQ-024 y
la remediación de SEC-051, contratada en REQ-024 CA-08/CA-09—, así que la composición se cierra entera
en la **misma** ventana. Queda escrita igual, porque un «alcance acotado» que no nombra con qué se
compone es media medición, y porque el reparto entre REQ **no** deja de tener motivo por caer los dos en
la misma ventana (motivos abajo: son de **naturaleza** del trabajo, no de calendario).

**Va en un REQ aparte —REQ-024, dueño `analista-requerimientos` para el REQ y `desarrollador` para el
código; desde el cambio de ventana, en la MISMA ventana que éste y detrás de él (ver «Coordinación»)— y
éstos son los motivos, en orden de peso. Ninguno de los cinco es de calendario, y eso importa: el
reparto se sostuvo igual cuando las dos ventanas eran distintas y cuando son la misma:**

1. **No son dos mitades del mismo tipo. La (1) es un defecto; la (2) es una decisión.** La (1) es la
   máquina leyendo algo distinto de lo que el documento dice: no hay decisión detrás, sólo un
   fail-open. La (2) fue **decidida a propósito** —compatibilidad con los REQ anteriores a que los
   veredictos existieran—, está **contratada** en REQ-016 **CA-11** y **firmada** en R-009. Cambiarla es
   cambiar la **base de la decisión**: exige **ADR** y write-back, no una sentencia.
2. **La (2) tiene radio de migración y la (1) no.** Si la ausencia deja de perdonarse, **todo REQ
   heredado** de **todo proyecto consumidor** que no declare el campo deja de cerrar: eso es una nota de
   migración en `skills/arnes-upgrade/SKILL.md`, una decisión sobre qué campos y una ventana de
   transición. Es trabajo de otra clase, no un delta más.
3. **La frontera es honesta y verificable, no una comodidad.** Toda fila de `AGENTS.md` §13 que el BOM
   falsifica habla de un documento que **declara** el campo (y son las que la clase de CA-02 activa; el
   conjunto se deriva, no se lista). Con el BOM el documento lo declara y ninguna regla contratada lo
   retira, así que la puerta debería imponerlo y no lo impone: la fila es **falsa**. Sin el campo no hay
   nada que imponer y la fila **no promete nada**. Cerrar la (1) **restituye la verdad de esas filas**;
   la (2) es la semántica de **no declarar**, sobre la que §13 no promete nada hoy.

   **Con una excepción que R-013 midió y que NO es de este REQ: la fila del rigor.** §6 promete sin
   condición que el nivel «no lo baja nadie sin firma», y §13 la lista como cumplida por máquina. Medido:
   en un REQ que declara `Rigor: critico` y **no** declara sensibilidad, retirar esa única línea baja el
   rigor efectivo y **quita la exigencia de auditoría**, sin firma de nadie. Aquí lo tapa la política de
   autoalojamiento —todo REQ nace `Sensible a seguridad: sí`, así que hay suelo—, **pero el plugin se
   instala en otros proyectos**, y ahí un REQ `critico` por dinero, por identidad o por cambio
   irreversible puede perfectamente no declarar sensibilidad: para ellos una línea retirada retira la
   auditoría. **La promesa se cumple por la política de un consumidor, no por el mecanismo que se le
   entrega.** Es la remediación 3 de `SEC-050`, dueño `analista-requerimientos` **+ gate humano** (es
   `AGENTS.md`), enrutada a REQ-024. **No entra en CA-10 de este REQ**, y la razón de que se diga aquí es
   que CA-10 edita **esa misma tabla**: quien esté ahí reescribiendo la fila del CR tendrá delante la
   fila del rigor, y arreglarla «de paso» es un cambio de `AGENTS.md` sin gate humano y sin su REQ. **Y
   con los dos REQ en la misma ventana el aviso vale más, no menos:** ahora los dos editan `AGENTS.md`
   §13 con pocas comisiones de diferencia, así que la tentación de «ya que estoy» tiene dos ocasiones en
   vez de una.
4. **Colgar un fail-open vivo de una decisión de diseño es lo que este repositorio ya prohibió dos veces
   por nombre** (REQ-016, «Fuera de alcance»): lo deja abierto mientras se decide otra cosa.
5. **Presupuesto, rederivado tras el cambio de ventana.** El argumento original era que 1.33.0 estaba
   declarada «sólo las cuatro palancas de coste» y ya se había detenido una vez por cupo de tokens. Con
   este REQ movido a 1.34.0 ese argumento **cambia de forma pero no desaparece**: lo que se sostiene es
   que **este REQ solo estimó cuatro comisiones en serie** (ver «La ventana es 1.34.0»), así que
   fundirle encima el ADR, la nota de migración y la superficie derivada de REQ-024 produciría **una
   comisión que no cabe en ningún cupo**, y la que se parta por la mitad será la que llegue al final. El
   vencimiento **se cita**, no se declara: cierre de 1.34.0 (`docs/seguridad/registro-seguridad.md:3684`,
   tabla en `:4238`) — el mismo para las dos mitades, lo que **elimina** el argumento de calendario y
   deja este punto apoyado sólo en el tamaño de la comisión.

**Y una razón para NO aplazarla más allá de 1.34.0, que en la primera redacción de este REQ estaba
DERIVADA POR LECTURA y ahora está MEDIDA — el cambio de instrumento se deja escrito porque es la mitad
del valor.** La versión
anterior de este párrafo decía que la (2) **no parecía** necesitar ninguna vía futura para ser
explotable, lo declaraba **no ejecutado** a propósito y lo dejaba como pregunta abierta 2. R-013 lo
ejecutó contra el entrypoint real: **confirmado**, con control positivo, negativo y de equivalencia, en
la instalada 1.32.1 y en `cand/1.33.0` (detalle arriba y en el Historial). La (2) tiene **instancia
alcanzable hoy**. Lo que **no** cambia es el reparto: los cinco puntos de arriba nunca dependieron de
que fuera cierto, y lo que la confirmación mueve es el **forzador** de REQ-024, no la ventana de este
REQ. Y lo que sí añade la medición, y la lectura no podía dar: la (1) tenía una mitigación implícita
—«hace falta un BOM, y lo mete PowerShell al redirigir»— y **la (2) no necesita nada de eso**.

### Lo que este REQ declara sobre sí mismo: la promesa de `AGENTS.md`

Hoy `AGENTS.md` §6 y §13 describen un enforcement que el árbol **no tiene en la extensión que la sección
promete**: toda fila que dependa de un campo de la clase de CA-02 es **falsa** sobre un documento que lo
declara con un carácter invisible delante. Si este REQ cierra, esas filas vuelven a ser ciertas **en lo
que este REQ cubre y sólo en eso** (y CA-10 reescribe la fila del CR para que nombre la propiedad y no el
carácter). La dependencia se nombra **aquí**, en el REQ; `AGENTS.md` **no se toca** desde esta comisión
(lo edita el `desarrollador`, CA-10).

**Y «sólo en eso» está MEDIDO, no matizado por prudencia:** *«cerrar la vía del carácter (REQ-023) no
cierra la clase, porque el comentario y el borrado siguen abiertos»*
(`docs/seguridad/registro-seguridad.md:3890`, R-013 §2). Las filas de §6/§13 que hoy son falsas siguen
siéndolo con este REQ dentro —eso es `SEC-050`— y son falsas **desde `v1.30.3`**, en cinco versiones
publicadas, de forma **latente**. Lo que sí acredita cerrarlo es que la cabecera vuelve a ser
**medible**, que es la mitad de la que depende la otra.

**La condición de escalada de `SEC-047` se CITA con archivo y línea, no se declara aquí — y la anterior
redacción de este pasaje la declaraba, inventada.** La cláusula vigente está en
`docs/seguridad/registro-seguridad.md:4303-4317` (R-014 §4, enmienda a R-012), enunciada **por propiedad
y no por fecha**: la mitad (1) de SEC-047 sube a `contrato` cuando ocurra la primera de tres —que
**1.34.0** cierre sin ella; que **deje de ser latente** (se mida un carácter no representable en la
cabecera de cualquier REQ de cualquier árbol o de la historia, o un cierre que pasó por esa vía); o que
**un texto firmado empiece a prometer la PROPIEDAD y no el carácter** mientras el código siga guardando
sólo el CR—. Y lo que **no** la sube, dicho allí mismo: en qué ventana decida el propietario hacer el
trabajo. El vencimiento y el forzador reales están en la tabla de
`docs/seguridad/registro-seguridad.md:4229-4239`; la clase sigue siendo **`instrumento`**.

**Y la consecuencia de máquina, corregida:** un hallazgo `contrato` abierto bloquea el cierre del **REQ
que lo declara en su propio campo `Hallazgos abiertos:`** —es lo que `guard-completado` lee
(`hooks/guard-completado.sh:486-527`)—, **no** «el cierre de una ventana». Lo que devuelve al propietario
la publicación de una ventana es el criterio de **gobernanza** de
`docs/gobernanza/autoalojamiento.md:148-155`, que no es una puerta.

> **La forma, que es lo reutilizable: una condición de escalada que sólo vive en el documento cuyo
> aplazamiento castiga no es un forzador — es un argumento con la firma de otro.** Misma familia que las
> tres tautologías que REQ-021 cazó en 1.33.0 —quien escribe el instrumento diseña el control
> que sabe pasar—, aplicada a un **forzador** en vez de a una sonda. De ahí la regla de redacción que
> este REQ adopta para sí y que el analista propone para `requirements/README.md` (ver «Preguntas
> abiertas»): **un REQ CITA la clase, el forzador y el vencimiento de un hallazgo con archivo y línea;
> no los DECLARA.** Un contrato que reescribe de memoria la condición que lo obliga acaba escribiendo la
> que le conviene, y nadie lo nota porque suena a cita.

**Y una fila queda falsa aunque este REQ cierre: la del rigor**, por la vía del comentario y no del
carácter (motivo completo en el punto 3 del reparto). Es `SEC-050` —clase, dueños y condición de escalada
**citados** en `docs/seguridad/registro-seguridad.md:4014` y siguientes—, enrutado a REQ-024 y con gate
humano por tocar `AGENTS.md`.
Dicho aquí para que nadie lea el verde de este REQ como que §6/§13 quedan ciertas **enteras**: quedan
ciertas en lo que este REQ cubre, que es lo que CA-12 (iii) exige no maquillar.

### Qué hay en `Hallazgos abiertos:` y qué NO, con el motivo de cada uno

**`SEC-052` (`contrato`) SÍ está en el campo, y bloquea el cierre de este REQ.** Es un hallazgo **sobre
este REQ**: dos pasajes suyos me atribuían al auditor una cláusula de escalada que nunca emitió, y le
atribuían a `guard-completado` una consecuencia que no tiene. Está declarado en
`docs/seguridad/registro-seguridad.md:4319-4364` (R-014), con dueño `analista-requerimientos`, y su
forzador es literal: *«no firmo `Seguridad:` de REQ-023 mientras la atribución esté en el documento»*.
**El write-back está hecho** —los cuatro puntos de la remediación, en «Lo que este REQ declara sobre sí
mismo» (atribución retirada y citada con línea; consecuencia de máquina corregida), en el punto 3 del
reparto de `SEC-051` (argumento **rederivado**, no corregido de fecha), y en la pregunta abierta 4 (la
regla de redacción propuesta para `requirements/README.md`)— y el hallazgo **queda declarado hasta que
el auditor lo verifique y lo cierre en su registro**, que es suyo y no de este documento. Se queda en el
campo a propósito: retirarlo yo sería exactamente la asimetría que el hallazgo nombra.

**`SEC-050` (`contrato`, alta) y `SEC-051` (`instrumento`, alta) NO entran en el campo de este REQ.** No
es una omisión ni una comodidad, y como un `contrato` en ese campo **impediría cerrar**
(`guard-completado`), la decisión se escribe con su motivo:

- **`SEC-050` es un hallazgo `contrato` sobre REQ-016, no sobre este REQ**
  (`docs/seguridad/registro-seguridad.md:4014` y siguientes — clase, severidad, dueños y condición de
  escalada **citados** allí, no declarados aquí). Lo que desmiente es el
  **alcance de la descripción** de un criterio de REQ-016 (`completado`, `Seguridad: aprobado (R-009)`),
  no la conducta de la máquina. El auditor decidió **no reabrir REQ-016** hoy y enrutar la reparación a
  REQ-024 como parte de su contrato, con vencimiento en el cierre de **1.34.0** y una condición explícita
  de escalada: si REQ-024 se escribe **sin cubrir la superficie derivada y sin corregir el puntero**,
  **reabre REQ-016** y entonces sí bloquea. Lo que a este REQ le tocaba de `SEC-050` es el **write-back**
  y está hecho: CA-02 enuncia la superficie por derivación y nombra los dos sitios, CA-01 deja de decir
  «un solo sitio» donde hay dos, y las cinco enumeraciones que este documento llevaba están corregidas
  (Historial). Al estar en `borrador`, corregirlas no genera hallazgo: es la entrega, no una deriva.
- **`SEC-051` es `instrumento` y no lo introduce ni lo toca este REQ.** R-013 lo dice por escrito: «no
  bloquea ningún REQ en curso». Su ventana es **1.34.0** y su reparto está en «Fuera de alcance». Lo que
  este REQ sí hace con él es **no empeorarlo por vecindad**, y eso es un criterio medible: CA-12.

### Relación con REQ-016 CA-12: se extiende, no se contradice

Tras este REQ el código cubrirá **más** de lo que CA-12 de REQ-016 promete —CA-12 nombra el CR; CA-01 de
aquí nombra la clase—. **CA-12 no se reescribe:** REQ-016 está `completado`, y `requirements/README.md`
(«Alcance temporal») prohíbe reabrir contratos cerrados por un motivo de redacción. La consecuencia
práctica, dicha para que nadie la redescubra: **la letra de CA-12 no es la frontera del control**; la
frontera es **CA-01 de este REQ**, y CA-12 queda como el caso particular con el que la clase se
descubrió. REQ-016 **CA-04** ya dejó escrita la conexión al acotar su tolerancia a «una cabecera
**medible**»: esta guarda es lo que da contenido a esa palabra.

### Coordinación: con qué colisiona, sin maquillarlo

El campo `Archivos:` está escrito **sin decoración de Markdown** a propósito: mientras **SEC-020** siga
abierto (`docs/seguridad/registro-seguridad.md:1305` — `contrato`, dueño `desarrollador`; clase y estado
**citados**, no declarados aquí), el marcado **elemento por elemento** hace que
`tools/arnes-paralelo.sh` responda `disjunto` con rc 0 sobre rutas que no existen.

Este REQ **colisiona** —y por tanto **no se despacha en paralelo**— con, al menos:

| REQ | Archivos compartidos |
|---|---|
| REQ-024 (`borrador`, **misma ventana**) | `hooks/lib.sh`, `hooks/guard-completado.sh`, `tools/arnes-lectura.sh`, `run.sh`, `README.md`, `AGENTS.md`, `templates/AGENTS.md.tpl`, `skills/arnes-upgrade/SKILL.md`, `CHANGELOG.md` |
| REQ-021 | `tests/escenarios/hooks/run.sh`, `tests/escenarios/hooks/README.md` |
| REQ-017 | `hooks/lib.sh`, `run.sh`, `README.md` |
| REQ-007 | `hooks/lib.sh`, `hooks/guard-completado.sh`, `tools/arnes-lectura.sh`, `run.sh` |
| REQ-020 | `run.sh`, `README.md`, y su glob `secciones/*.sh` cubre las tres secciones nuevas |
| REQ-011, REQ-018 | `hooks/lib.sh`, `run.sh`, `AGENTS.md`, `templates/AGENTS.md.tpl` |
| REQ-012, REQ-013, REQ-014, REQ-015, REQ-019, REQ-022, REQ-008 | `AGENTS.md`, `templates/AGENTS.md.tpl`, `skills/arnes-upgrade/SKILL.md`, `CHANGELOG.md`, `tests/escenarios/` |

Es correcto y no es un problema: significa que **la implementación va en serie**. **Y el cambio de
ventana añade una colisión que antes no existía: REQ-024 es ahora de la misma ventana que este REQ y
comparte nueve rutas**, incluidas las tres del mecanismo. Los dos tocan la misma superficie por motivos
distintos —éste la **medibilidad** de la línea, aquél la **semántica de la ausencia**—, y el orden entre
ellos no lo decide `tools/arnes-paralelo.sh`: **este REQ va primero**, porque su guarda decide si una
línea se puede medir y la de REQ-024 decide qué hacer cuando un campo no está; medir es previo a
resolver. El orden de fases no se paraleliza nunca, con `disjunto` o sin él (`AGENTS.md` §6).

**Lo que el campo NO declara, y por qué:** el propio archivo de este REQ, el índice de
`requirements/README.md`, `docs/ESTADO.md` y el log de QA de la ventana (`docs/qa/<versión>.md`) son
**artefactos de gobierno** que toda comisión toca por definición; declararlos hace que **cualquier** par
de REQ colisione siempre por una bitácora y el campo deja de responder la única pregunta para la que
existe (REQ-016, H-05). `CHANGELOG.md` **sí** se declara: es la bitácora compartida que una comisión
escribe a mano y donde el conflicto de fusión es real, y así sigue declarada en los demás REQ mientras
la ventana no decida otra cosa.

**Y `hooks/campos-req.awk` NO se declara a propósito:** CA-06 exige que la guarda sea **observacional**,
así que la transcripción en awk no debería necesitar ni una línea. Si la implementación tuviera que
tocarla, es la señal de que la guarda dejó de ser observacional: se declara como **desviación** con su
entrada en el Historial y se re-examina CA-06, no se cambia el campo en silencio.

**Y `31-cola-una-sola-regla.sh` tampoco, aunque CA-12 (ii) mida la cola.** El control de no-regresión de
`arnes_cola_pendientes` vive en las **secciones nuevas** de este REQ, que ya están declaradas: mide la
función, no reescribe los casos de REQ-009. Si la implementación necesitara tocar ese archivo, la señal
es la misma que con el awk —CA-12 (ii) dejó de ser una no-regresión y se está cambiando el conteo de la
cola—: se para, se declara la desviación y se re-examina CA-12, porque eso es `SEC-051` y no este REQ.
El campo `Archivos:` **no cambia** con la absorción de R-013, y eso es deliberado: un write-back que
ensancha el mapa de archivos ensancha las colisiones de la ventana entera.

### El techo honesto de la cata: lo que NO se midió, y que ningún criterio da por hecho

La cata del `desarrollador` del 2026-09-08 (sólo lectura, nada escrito en el árbol) resolvió la pregunta
abierta 1 y destapó los defectos de `CA-09 (iii)`, `CA-11` y `CA-06`. **Declaró también qué no midió, y
se anota aquí para que ningún criterio de este REQ se lea como acreditado por ella:**

- **La ruta crítica del banco —`CA-09 (ii)`— no está medida.** El +17 % que la cata obtuvo es **por
  función**, no en la ruta crítica; el número que decide sale de `tests/util/sonda-reloj.sh` **en la
  misma corrida**, y REQ-021 aún no ha soltado esos instrumentos. `CA-09 (ii)` sigue siendo trabajo por
  hacer, y una sonda que no llegue a su suelo emite **SKIP**, nunca PASS.
- **El corpus de fixtures del banco para `CA-04` no está medido.** Las cabeceras de los fixtures viven
  en **heredocs** dentro de los scripts de sección, y el primer grep de la cata midió **comentarios de
  script** y no cabeceras — lo declaró él mismo. La anti-vacuidad de `CA-04` (una clave no ASCII, un
  valor no ASCII y una clave decorada) sigue **sin verificar** sobre ese corpus.
- **`hooks/guard-completado.sh` no se tocó.** Las **dos ramas de denegación** y el **motivo imprimible**
  que `CA-01` y `CA-07` exigen **no están ejercidos**: la cata midió el lector, no la puerta.

Los números que la cata **sí** obtuvo, con su magnitud y su margen, están en el Historial. Ninguno de
ellos cubre los tres puntos de arriba.

### Fuera de alcance

- **La semántica de la AUSENCIA de un campo, sobre la superficie DERIVADA de CA-02 y no sobre dos
  campos.** Dueño: `analista-requerimientos`, en REQ propio: **`requirements/REQ-024.md`, que ya existe**
  (`borrador`, ventana **1.34.0**). Clase: la remediación 2 de **SEC-047** más las tres de **SEC-050**.
  Exige **ADR** (cambia la base de la decisión que REQ-016 CA-11 contrata) y nota de migración para los
  proyectos consumidores. Motivos completos arriba. **Y lo que su contrato tiene que cubrir, porque de
  ello depende que `SEC-050` no reabra REQ-016:** la superficie **completa** —derivada, no listada—, el
  **puntero real** (dos funciones nombradas, o unificadas en una), la **dirección de ausencia de cada
  campo nuevo** declarada en ese mismo sitio, y el alcance real de la fila del rigor de `AGENTS.md`
  §6/§13.
- **La SUSTITUCIÓN de un carácter de la clave por un HOMÓGLIFO** —`Е` cirílico en `Еstado`, `Ｅ`
  fullwidth—, que es la lectura de CA-03 que este REQ deja fuera. **No es satisfacible con lo que
  existe, y está medido** (cata del `desarrollador`, Historial): la clave resultante no contiene nada
  ajeno al alfabeto de las claves —es **otra** clave—, así que el mecanismo de CA-03 calla, y las **tres**
  salidas son las tres que este REQ ya prohibió o encareció: estrechar el alfabeto admitido de la
  cabecera (exige **ADR**, y produce la fricción constante que **CA-04** existe para impedir), una
  **tabla de homóglifos** (la enumeración que envejece hacia el lado que abre — sería la **sexta**
  derrota medida de esa vía, la misma que CA-03 cuenta) o el recorrido **cuadrático** de la clave (rompe
  el techo de **CA-09 (iii)**). **Aquí sí es
  donde CA-03 y CA-04 son satisfacibles por separado e imposibles juntos**, y por eso la salida no es un
  criterio sino un reparto. Dueño: `desarrollador` (la cata de mecanismo) y `analista-requerimientos` (el
  criterio y el ADR si lo hubiera). Ventana: **1.35.0**. Clase: `instrumento`. **Y por eso ningún
  criterio de aquí promete cubrir esa vía: el verde de este REQ NO cierra la clase** (es lo que CA-12
  (iii) exige no maquillar), igual que ocurre con el NUL/UTF-16 del punto siguiente.
- **El segundo lector con noción de comentario propia: la cola de aprobaciones humanas (`SEC-051`).**
  Clase, dueños, forzador y vencimiento **se citan, no se declaran aquí**:
  `docs/seguridad/registro-seguridad.md:4139-4145` (`instrumento`, severidad alta, dueños
  `desarrollador` para el lector y los casos del banco y `analista-requerimientos` para los criterios;
  forzador «va con REQ-024 o antes, en 1.34.0, y no después de él»; vencimiento el cierre de 1.34.0, con
  escalada a `contrato` si esa ventana cierra sin ello). Su contrato ya existe: **REQ-024 CA-08, CA-09 y
  CA-10**.

  **Por qué NO entra aquí, con los tres motivos y en orden de peso** (la propiedad que sí lo une a este
  REQ está enunciada y contratada en CA-12, para que la clase no se pierda por el reparto):
  1. **No es un defecto, son tres cosas de naturaleza distinta**, y sólo una comparte propiedad con
     CA-01. Los veredictos son de R-013 (**ejecutados**); esta clasificación es de **lectura** del código
     y se dice así: *(A)* que una entrada con anotación cuente cero es la semántica de **grano de línea**
     que `hooks/lib.sh` **declara por escrito** como frontera deliberada y que REQ-009 CA-04/CA-07
     documentan — cambiarla es cambiar un contrato `completado`, con ADR y write-back; *(B)* que
     `### Migrar A --> B`, **sin comentario ninguno**, cuente cero es un defecto llano que ningún texto
     declara; *(C)* que un rango **sin cerrar** devuelva `0` con `rc=0` contradice el contrato que la
     propia función se escribe en su cabecera («si no se puede medir, no devuelve 0: devuelve no lo sé»)
     y REQ-009 CA-15/CA-16 — **ésta sí** es la propiedad de CA-01 en un segundo lector. Un REQ con (A),
     (B) y (C) no es un delta: es un contrato con su ADR.
  2. **El coste marginal no es de dos líneas, y lo dice la remediación misma.** Pide que los dos lectores
     compartan **una** transcripción (refactor de una función cuya ruta de coste es la parada **y** cada
     evaluación de la puerta, en la ventana que acaba de pagar una regresión de 10× en esa misma ruta),
     que la conducta cambie en **tres** lectores cuadrados entre sí, y casos fail-before/pass-after en
     `tests/escenarios/hooks/secciones/31-cola-una-sola-regla.sh` — un archivo que este REQ **no**
     declara en `Archivos:` y que su glob de vecinos ya hace colisionar con REQ-020.
  3. **Su pareja es REQ-024, y ya la tiene: absorberlo aquí duplicaría un contrato que existe.** Este
     punto **se rederiva y no se corrige de fecha**, porque el argumento que llevaba —«los vencimientos
     son de ventanas distintas, y fundirlos importaría el vencimiento holgado dentro del que no puede
     resbalar»— **no tiene fuerza con los vencimientos reales**, que son **el cierre de 1.34.0 los dos**
     (`docs/seguridad/registro-seguridad.md:4238` para SEC-047 (1) y `:4143` para SEC-051), y menos aún
     ahora que **este REQ también es de 1.34.0**. No hay holgura que importar; el argumento de calendario
     está **retirado**. Lo que sí se sostiene, y es un hecho verificable en el árbol y no una proyección:
     la remediación de `SEC-051` **ya está contratada** en **REQ-024 CA-08** (el rango sin cerrar
     devuelve «no lo sé» en vez de `0`) y **CA-09** (el cierre huérfano no retira nada), con su **ADR**
     para la frontera de grano de línea (CA-10 de ese REQ). Absorberla aquí no adelantaría nada:
     escribiría **dos** contratos sobre la misma conducta, en la ventana en que el propio registro pide
     que los dos lectores compartan **una** transcripción — exactamente la duplicación que este REQ
     existe para no cometer. La familia de `SEC-051` es real, y **su pareja es la otra transcripción de
     la misma decisión, no el carácter invisible**.
- **La vía que un lector de bash no puede ver por construcción: el byte NUL y el archivo en UTF-16.**
  PowerShell 5.1 redirige en **UTF-16LE con BOM**, no en UTF-8; y bash descarta los NUL antes de que
  ninguna guarda los vea, así que **ninguna** comprobación sobre el texto ya leído puede detectarlos. La
  salida es la que la **cola de aprobaciones** ya usa —comprobar que lo leído es todo lo que el archivo
  contiene, y si no, denegar por medibilidad (`hooks/guard-completado.sh`, `arnes_cola_pendientes`)—,
  pero medir el tamaño cuesta un **proceso** por evaluación y 1.33.0 fue justamente la ventana de quitar
  procesos del camino de la puerta. Dueño: `desarrollador`. Ventana: **1.35.0** (movida con este REQ:
  antes decía 1.34.0, que es ahora la ventana de este REQ y no un aplazamiento). Clase: `instrumento` —y
  ésta **sí** se declara aquí y no se cita, porque no hay ningún `SEC` que la haya abierto: nace en este
  documento. **Y por eso ningún criterio de aquí promete cubrir esa vía:** CA-01 se ejerce sobre la
  cabecera que el lector **puede** leer, y el silencio de esta guarda ante un NUL **no acredita** nada.
- **Que el bloque derivado de `docs/ESTADO.md` publique campos de una cabecera que la puerta se niega a
  medir.** Es el residual de **SEC-024**, cuyo estado **se cita** y no se declara aquí: el registro lo
  dejó en **`mitigado`** con el residual del bloque derivado apuntado
  (`docs/seguridad/registro-seguridad.md:2570` y siguientes). Sin cambios y **no contratado aquí**: CA-07
  contrata el **informe**, que es la superficie donde una persona busca anomalías, y no el tablero, que
  es un derivado de continuidad.
- **El ruleset de tags y los permisos del workflow (`SEC-048`).** Gate humano del propietario; este REQ
  lo **usa** (CA-08 materializa la línea base desde un tag) y lo **nombra**, no lo arregla.
- **Ninguna llave nueva en `.arnes/config.json`** y ninguna migración de datos: el barrido de R-012
  salió limpio en este árbol, así que hacia esta versión no hay ninguna operación de andamiaje que
  aplicar. Lo único que un proyecto hereda que exige acción humana es la **auditoría** de CA-10.

## Preguntas abiertas / conflictos

Mientras haya una **pregunta** sin resolver en este apartado el REQ se queda en `borrador` y **no se
entrega a desarrollo**. Los **conflictos registrados** de abajo se declaran con su resolvedor y su
consecuencia, y se dice de cada uno si retiene el REQ o no — no se deja al lector deducirlo.

**Cuatro de las cinco preguntas que este REQ llevaba están RESUELTAS y retiradas** (con su antes→después
en el Historial): la 1 por la cata del `desarrollador` del 2026-09-08 —CA-03 y CA-04 **son**
simultáneamente satisfacibles con el lector que existe, **sin ADR**, y las tres correcciones que la cata
obligó están escritas en CA-03, CA-06, CA-09 (iii) y CA-11—; la 2 porque **CA-10 se reescribió por
propiedad** y su verdad ya no depende de si la comprobación por estado existe; la 3 porque
**`requirements/REQ-024.md` existe** (`borrador`, 1.34.0) y ya contrata la mitad (2); y la 4 porque los
criterios de `SEC-051` **tienen archivo**: REQ-024 CA-08, CA-09 y CA-10.

**Queda una, y no es mía:**

1. **Índice de `requirements/README.md` sin actualizar.** Este REQ no aparece todavía en la tabla del
   índice, y el ámbito de escritura de esta comisión es **sólo** `requirements/REQ-023.md`. Es un punto
   de la **Definition of Ready**, así que mientras no esté el REQ se queda en `borrador`. *Resuelve:* la
   coordinadora.

**Y una propuesta de doctrina que no puedo escribir desde aquí, con su dueño:** la remediación 4 de
`SEC-052` sugiere subir a `requirements/README.md` § «Cómo se escribe un criterio que no se desmiente»
una regla general — **un REQ *cita* la clase, el forzador y el vencimiento de un hallazgo con archivo y
línea; no los *declara***. La veo general y la he aplicado a este documento entero (ver «Lo que este REQ
declara sobre sí mismo» y «Fuera de alcance»), pero editar el README no está en el ámbito de esta
comisión. *Dueño:* `analista-requerimientos`, en una comisión con ámbito sobre `requirements/README.md`.
No bloquea la entrega de este REQ: es doctrina del proyecto, no un criterio de éste.

### Conflictos registrados

**Con REQ-024 CA-08/CA-09 — RESUELTO por anclaje, no por acuerdo verbal.** Está descrito arriba, en
CA-12, y en el origen (`requirements/REQ-024.md:514-524`): `CA-12 (ii)` de este REQ queda anclado a **su**
corpus y a **su** comparación, así que las dos redacciones son compatibles. Lo que hay que **no** hacer al
implementarlo es escribir ese caso como no-regresión **abierta**; si sale abierto de la implementación,
es un hallazgo `contrato` contra este REQ y hay write-back.

**Con REQ-024 CA-01 — REGISTRADO y CONDICIONAL, con su resolvedor nombrado.** `CA-11` de este REQ
contrata que la **ausencia** de un campo se sigue perdonando exactamente como hoy; **CA-01 de REQ-024
cambia esa conducta** a propósito, y su propio analista lo registró en el origen
(`requirements/REQ-024.md:503-513`). Las dos redacciones son compatibles **si y sólo si** `ADR-006`
—que todavía no existe— elige una **activación explícita**; si eligiera que la exigencia sea el
**defecto**, `CA-11` de aquí pasaría a describir una conducta que el árbol no tiene y sería un hallazgo
`contrato` **contra este REQ**, con write-back mío y, si este REQ ya estuviera en estado terminal, vuelta
a `en-progreso` (`AGENTS.md` §9). *Resuelve:* `ADR-006` + `auditor-seguridad`.

**Y por qué este conflicto NO retiene este REQ en `borrador`, dicho explícitamente en vez de dejarlo
implícito:** hoy **no hay contradicción** — `CA-11` es cierto contra el árbol de hoy y lo seguirá siendo
hasta que REQ-024 lo cambie. El conflicto es **futuro y condicional**, su resolvedor pertenece a un REQ
que va **después** de éste (ver «Coordinación»: este REQ va primero porque medir es previo a resolver), y
retener este REQ hasta que se firme el ADR de aquél sería un **bloqueo circular**. Se registra, se nombra
el resolvedor y se nombra la consecuencia. Lo que sí retiene el REQ en `borrador` es la pregunta 1 de
arriba.

**Sin conflicto con REQ-016.** La relación con CA-04 y CA-12 es de **extensión** y está argumentada
arriba: CA-12 de REQ-016 no se contradice ni se reescribe, y CA-04 ya acotó su tolerancia a una cabecera
«medible», que es lo que este REQ define. La relación con REQ-016 **CA-11** es de **frontera declarada**
(CA-11 de aquí), no de contradicción: la ausencia se sigue perdonando exactamente igual.

**Y un hallazgo `contrato` abierto contra un REQ `completado`, que no es un conflicto con este REQ pero
se declara para que no se descubra al cerrar:** `SEC-050` dice que **REQ-016 CA-11** describe esta
superficie con menos alcance del que tiene y con un puntero al archivo equivocado. El auditor decidió no
reabrir REQ-016 y enrutar la reparación a REQ-024 (motivo y condición de escalada arriba). Este REQ
**no** reescribe REQ-016 —está `completado`, y `requirements/README.md` § «Alcance temporal» lo
prohíbe—, y su propio CA-02 y CA-01 dejan de repetir la descripción corta.

## Trazabilidad
Origen: **SEC-047** (`docs/seguridad/registro-seguridad.md`, § Revisión **R-012**, 2026-09-07):
`instrumento`, severidad **crítica**, dueños `analista-requerimientos` (el criterio) y `desarrollador`
(la guarda). Hallado por el `auditor-seguridad` **ejecutando** `guard-completado.sh` contra un proyecto
de prueba, con las seis filas de la tabla medidas y el barrido de invisibles sobre el árbol y toda la
historia de `requirements/`. Clase, forzador y vencimiento **se citan**, no se declaran aquí: tabla en
`docs/seguridad/registro-seguridad.md:4229-4239`; cláusula de escalada vigente, por propiedad, en
`:4303-4317`. Ventana: **1.34.0** por decisión del propietario (2026-09-08), que **desanduvo** el
adelanto a 1.33.0 tomado ese mismo día — y desandar un adelanto no incumple ningún vencimiento
(`:4241-4244`).
Origen secundario, y es de dónde sale la mitad de la redacción de hoy: **R-013** (misma fuente,
2026-09-08) — investigación del **guardián publicado**, no auditoría de ningún REQ, que **ejecutó** la
pregunta abierta 2 de este REQ contra el entrypoint real (`hooks/guard.sh`) en la instalada **1.32.1** y
en `cand/1.33.0`, con calibración en las dos direcciones y control de equivalencia. De ella entran:
**SEC-050** (`contrato`, alta, dueños `analista-requerimientos` + `auditor-seguridad`) → el write-back de
CA-01, CA-02, CA-11 y las cinco enumeraciones de este documento; y **SEC-051** (`instrumento`, alta,
dueños `desarrollador` + `analista-requerimientos`) → **no absorbido**, enrutado a REQ-024, con la parte
que sí toca a este REQ contratada en **CA-12**.
Origen del write-back del 2026-09-08 (segunda vuelta): **R-014**
(`docs/seguridad/registro-seguridad.md:4180-4364`) — consulta de trazabilidad sobre SEC-047, que **no
firma nada** y de la que nace **SEC-052** (`contrato`, media, dueño `analista-requerimientos`, declarado
en el campo `Hallazgos abiertos:` de este REQ). Y la **cata del `desarrollador`** del 2026-09-08 (sólo
lectura, nada escrito en el árbol), que resolvió la pregunta abierta 1 y destapó los tres defectos de
`CA-09 (iii)`, `CA-11`/`CA-06` y el conjunto de claves; su **techo honesto** está declarado en «Notas /
alcance» y ningún criterio lo da por cubierto.
Doctrina aplicada: `requirements/README.md` § «Cómo se escribe un criterio que no se desmiente» (formas
a, b, c y d) · REQ-012 (criterios por mecanismo) · REQ-016 CA-12 y CA-03 (DENY por medibilidad) · R-009
y R-012 (las bocas del lector, la invariancia de locale) · R-013 §2 (la clase enunciada en una frase
contratable, y la superficie **derivada** en vez de listada) · R-014 §4 (una condición de escalada se
enuncia por propiedad y se **cita**, no se declara desde el documento al que obliga).
Tocado por: analista-requerimientos / 2026-09-08

## Historial de cambios
| Fecha | Antes → Después | Causa | ADR |
|------------|-----------------|-------------------------|--------|
| 2026-09-08 | (creación) | **SEC-047** (R-012, `instrumento`, severidad crítica): un carácter que no se representa y que la normalización no retira **borra un campo** de la cabecera, y para `Sensible a seguridad:` y `Hallazgos abiertos:` la **ausencia ABRE** — bypass completo de la capa de enforcement, medido ejecutando la puerta real, presente e idéntico desde `v1.30.3` | — |
| 2026-09-08 | **Reparto decidido y declarado: la mitad (1) —el carácter— aquí; la mitad (2) —la semántica de la ausencia— en REQ-024, ventana 1.34.0.** SEC-047 pide las dos. Motivo: la (1) es un **defecto** (la máquina lee algo distinto de lo que el documento dice) y la (2) es una **decisión** contratada en REQ-016 CA-11 y firmada en R-009, que exige **ADR** y nota de migración para los proyectos consumidores. La frontera es una propiedad verificable —**¿el documento declara el campo en letra?**— y coincide con lo que `AGENTS.md` §13 promete: cerrar la (1) restituye la verdad de la fila del suelo de rigor y de la de hallazgos; la (2) es la semántica de **no declarar**, sobre la que §13 no promete nada | Sin ADR: este REQ no cambia el alcance ni la decisión base — extiende la guarda de REQ-016 CA-12 del carácter a su clase, que es la propiedad que la doctrina ya exigía. **REQ-024 sí necesitará ADR** | — |
| 2026-09-08 | **Salida (a) —añadir el BOM y el U+200B a la lista— descartada por nombre, y `CA-03` redactado para que FALLE la prueba** (entrada reservada al azar en cada corrida, del complemento de las familias declaradas) | Sería la **sexta** derrota medida de la vía «ensanchar la enumeración» en este repositorio (`ADR-002`, SEC-020, SEC-024, SEC-025, H-01). Una lista escrita en un contrato envejece hacia el lado que **abre** | — |
| 2026-09-08 | **Añadidos dos criterios que SEC-047 no pedía: `CA-05` (invariancia de locale) y `CA-09` (iii) (cociente de duplicación de `arnes_sin_cita` ≤ 2,2, operativo)** | `CA-05`: una clasificación dependiente de `LC_CTYPE` denegaría en el CI de Linux y permitiría en Windows/MSYS, que es de donde sale el BOM — fail-open **por entorno**, invisible en la puerta requerida de `main`; es el eje que R-012 midió bajo los dos locales y la razón por la que las tablas de ortografía se escriben con escapes de bytes. `CA-09` (iii): la forma natural de «comprobar cada carácter» en bash es un bucle con indexación de cadena, que es **cuadrático por construcción** — exactamente la regresión de 10× que REQ-017 acaba de pagar y que `CA-08` de REQ-016 no vio por medir la magnitud equivocada (forma **(d)**) | — |
| 2026-09-08 | **Campo `Archivos:` declarado sin decoración y sin artefactos de gobierno** (12 rutas; se omiten el propio REQ, el índice, `docs/ESTADO.md` y `docs/qa/1.33.0.md`; se conserva `CHANGELOG.md`). Se omite `hooks/campos-req.awk` a propósito, con la desviación prevista si la guarda dejara de ser observacional | REQ-016 **H-05**: con el campo falso, `tools/arnes-paralelo.sh` respondió `disjunto` con rc 0 sobre un par que colisionaba en dos archivos — y ese `disjunto` es la **única** autorización para despachar en paralelo. **SEC-020** (abierto): la decoración elemento por elemento produce `disjunto` sobre rutas que no existen | — |
| 2026-09-08 | **Pregunta abierta 2 → RESUELTA: CONFIRMADA, y retirada del apartado.** Antes: «queda por **ejecutar** si `<!-- Sensible a seguridad: sí -->` + `Rigor: ligero` + `QA: pendiente` + `Seguridad: pendiente` cierra hoy; lo he derivado **leyendo** y no lo he corrido». Después: **cierra**, medido contra el **entrypoint real** en la instalada **1.32.1** y en `cand/1.33.0` con veredicto idéntico, y **en silencio total** —ni `deny`, ni `systemMessage`, ni aviso de vocabulario—. Dos variantes más del mismo mecanismo sobre otros dos campos de la clase de CA-02 (el que fija el **nivel** de rigor y un **veredicto**) también abren; y la excepción que delimita la clase sigue cerrando (el veredicto de seguridad comentado **deniega** en rigor efectivo `critico`). **Y el matiz que cambia el reparto: NO es defecto nuevo y NO se absorbe aquí.** El control de equivalencia lo prueba —comentar y borrar deciden **idéntico**—, así que es **REQ-016 CA-11 funcionando como se contrató** más la **decisión** del perdón de la ausencia, firmada en R-009; el auditor **no abrió `SEC`** por ella. Efecto único: el **forzador** de REQ-024 sube de «decisión defendible» a «bypass alcanzable con una edición de una línea, visible en el diff y que un revisor lee como higiene» | **R-013** §1 (11 filas, calibración en las dos direcciones, fixtures fuera del árbol), respondiendo a la pregunta abierta 2 que este REQ dejó **declarada como no ejecutada** en vez de afirmarla. La lectura estaba bien hecha y era insuficiente: lo que se le debía no era una segunda lectura, era la ejecución | — |
| 2026-09-08 | **Write-back de `SEC-050`: la superficie deja de enumerarse y pasa a DERIVARSE, y el puntero deja de mentir.** Antes: **cinco** pasajes de este documento describían la superficie de «la ausencia ABRE» nombrando **dos** campos (la Historia, CA-02, CA-11, el punto 3 del reparto y «Fuera de alcance»), y CA-01 decía «el conjunto de campos vive en **un solo sitio**» y a continuación nombraba **dos**. Después: **CA-02** enuncia la clase por propiedad, la hace **derivar midiendo** al banco (valor más restrictivo contra ausencia), nombra los **dos** sitios reales (`guard-completado.sh` para veredictos y hallazgo; `lib.sh` `arnes_sens_efectiva`/`arnes_rigor_efectivo` para suelo y nivel), declara **falso** cualquier criterio que cite uno solo, y manda el **recuento** al Historial (R-013 §2 midió **cuatro** de los seis campos que el lector reconoce) para no cometer la forma **(b)**; los otros cuatro pasajes se reescriben por propiedad. **CA-04** gana anti-vacuidad de **clave** además de valor | **SEC-050** (`contrato`, alta, R-013 §3): tres textos firmados —incluida la remediación que el propio auditor había escrito— describieron la superficie **por enumeración** y nombraron dos campos donde la medición encontró cuatro; el que faltaba en los tres es el que retira el **suelo de rigor**. Es la forma **(a)** de `requirements/README.md` dentro del código que esa doctrina gobierna. Al estar este REQ en `borrador`, la corrección es **entrega y no deriva**: no genera hallazgo | — |
| 2026-09-08 | **`CA-11` reescrito: la frontera pasa de «¿lo declara en letra?» a «¿la retirada la decide una regla contratada del lector, o no la decide nadie?»** Antes, con la letra anterior, `<!-- Sensible a seguridad: sí -->` **declaraba el campo en letra** —una persona lee `sí`— y por tanto caía en el dominio de CA-01, de modo que el `ALLOW` medido en R-013 sería un **incumplimiento de CA-01** por una vía que este REQ no arregla ni pretende arreglar. Después: la retirada **decidida** por la noción de rango de REQ-016 es dominio de la semántica de la ausencia (REQ-024); la retirada que **ninguna regla decide** es dominio de CA-01 | El criterio se habría **desmentido a sí mismo** el día que alguien lo probara: exactamente lo que `requirements/README.md` § «Cómo se escribe un criterio que no se desmiente» existe para evitar, y lo que el QA reporta como `contrato` **antes** de ejecutar. Lo destapó **R-013** §1 fila 2 al medir la vía que la frontera mal escrita se tragaba | — |
| 2026-09-08 | **`SEC-051` (`instrumento`, alta) evaluado y NO absorbido: va a 1.34.0, con REQ-024 o antes. Y en su lugar entra `CA-12`**, que contrata (i) ni una transcripción más de la noción de cita (**no más de 0** añadidas; **operativo**, menos es conforme), (ii) `arnes_cola_pendientes` cuenta y devuelve **exactamente igual** antes y después, y (iii) ningún artefacto de este REQ afirma que la clase quede cerrada | Tres motivos, y el primero es que **no es un defecto sino tres cosas de naturaleza distinta**: que una entrada anotada cuente cero es la semántica de **grano de línea** que `hooks/lib.sh` declara **por escrito** como frontera deliberada y que REQ-009 CA-04/CA-07 documentan (cambiarla es cambiar un contrato `completado`: ADR + write-back); que `### Migrar A --> B` **sin comentario** cuente cero es un defecto llano; y que un rango **sin cerrar** devuelva `0` con `rc=0` contradice el contrato que la propia función se escribe en su cabecera y REQ-009 CA-15/CA-16 — sólo **ésta** comparte propiedad con CA-01. Segundo: el coste marginal **no** es de dos líneas — la remediación pide **una** transcripción compartida (refactor de la ruta de coste que esta ventana acaba de pagar a 10×), conducta nueva en **tres** lectores cuadrados y casos en `31-cola-una-sola-regla.sh`, archivo que este REQ **no** declara. Tercero, y decide: los **vencimientos son de ventanas distintas** (la mitad (1) escala si cierra 1.33.0 sin ella; SEC-051, si cierra 1.34.0), y fundirlos impediría cerrar **ninguno** hasta que los dos estén — importando el vencimiento holgado dentro del que no puede resbalar. La familia es real, pero **su pareja es REQ-024**, la otra transcripción de la misma decisión. **`CA-12` existe porque la deriva es previsible:** quien implemente esto estará editando esa misma función en la misma ventana, con `SEC-051` sugiriéndole unificar | — |
| 2026-09-08 | **Declarado por qué `Hallazgos abiertos:` sigue en `(ninguno)` con dos hallazgos nuevos encima**, y `Archivos:` **no cambia** con la absorción | `SEC-050` es `contrato` **sobre REQ-016** (`completado`), no sobre este REQ, y R-013 declara por escrito que ninguno de los dos «bloquea ningún REQ en curso»; meter un `contrato` en ese campo **impediría cerrar** por un hallazgo que no es de este contrato. Y un write-back que ensancha el mapa de archivos ensancha las colisiones de la ventana entera (REQ-016 H-05) | — |
| 2026-09-08 | **`Versión destino:` 1.33.0 → 1.34.0.** 1.33.0 se publica **sin** este REQ, con REQ-017 + REQ-021 + la partición de las tres secciones del banco. Consecuencias escritas en el documento: sección nueva «La ventana es 1.34.0, y aplazarlo no incumple ningún vencimiento»; **REQ-024 entra en la tabla de colisiones** (misma ventana, nueve rutas compartidas) con el orden declarado —este REQ primero, porque **medir es previo a resolver**—; y la vía del **NUL/UTF-16** de «Fuera de alcance» se mueve de 1.34.0 a **1.35.0**, que es el aplazamiento que antes significaba «la ventana siguiente» | **Decisión del propietario (2026-09-08, la segunda del día sobre este REQ).** Su coste se midió **después** de meterlo: **cuatro** comisiones en serie detrás de REQ-021, sin paralelismo, con **dos precondiciones ajenas** (`CA-09 (ii)`/`(iii)` esperan a que REQ-021 suelte `tests/util/`; `CA-08` depende del tag, `SEC-048`). Y **no incumple nada**: el vencimiento de la mitad (1) de SEC-047 es el cierre de **1.34.0** (`docs/seguridad/registro-seguridad.md:3684`), así que 1.33.0 era un **adelanto**, y «desandar el adelanto no incumple ninguno» (`:4241-4244`) | — |
| 2026-09-08 | **Write-back de `SEC-052`: retirada una cláusula que nadie emitió, y corregida la consecuencia de máquina que le acompañaba.** Antes, en dos sitios: «*si **1.33.0** cerrara sin él, el auditor dejó dicho que **SEC-047 sube a `contrato`***… y un `contrato` abierto **bloquea el cierre de la ventana**» (`:450-452`), y «*la mitad (1) de SEC-047 escala a `contrato` si cierra **1.33.0** sin ella*» (`:566`). Después: la cláusula **se cita** con archivo y línea —`docs/seguridad/registro-seguridad.md:4303-4317`, enunciada **por propiedad** y no por fecha, con las tres condiciones y con «en qué ventana decida el propietario» declarado **explícitamente como lo que NO la dispara**—; y la consecuencia de máquina pasa a decir lo que la puerta hace: `guard-completado` lee el campo `Hallazgos abiertos:` **del REQ que se cierra** (`hooks/guard-completado.sh:486-527`), no un barrido del proyecto, así que un `contrato` bloquea **ese REQ**; lo que devuelve al propietario la **publicación de una ventana** es la **gobernanza** de `docs/gobernanza/autoalojamiento.md:148-155`. **`Hallazgos abiertos:` pasa de `(ninguno)` a `SEC-052 (contrato)`** y la sección que lo explicaba se reescribe como «Qué hay en el campo y qué NO» | **SEC-052** (`contrato`, media, R-014, dueño `analista-requerimientos`): el auditor trazó con `git log --all -S` que la frase aparece en **un solo** commit —el borrador de este REQ— y que el blob de R-012 donde nació SEC-047 ya decía **1.34.0**; el propio REQ se desmentía 20 líneas antes (`:431`). **La forma, que es lo reutilizable:** una condición de escalada que sólo vive en el documento cuyo aplazamiento castiga **no es un forzador, es un argumento con la firma de otro** — misma familia que las tres tautologías de REQ-021 (quien escribe el instrumento diseña el control que sabe pasar), aplicada a un **forzador** en vez de a una sonda. Al estar en `borrador`, corregirlo es **entrega y no deriva** | — |
| 2026-09-08 | **Argumento 3 de «por qué SEC-051 no entra aquí»: REDERIVADO, no corregido de fecha** (aviso literal del auditor: sustituir la fecha sin tocar el párrafo lo deja diciendo lo contrario de lo que argumenta). Antes: «los vencimientos son de ventanas distintas, y fundirlos importaría el vencimiento holgado dentro del que no puede resbalar». Después: el argumento de calendario queda **retirado** —los vencimientos reales son **el cierre de 1.34.0 los dos** (`:4238` y `:4143`), y ahora este REQ también es de 1.34.0—, y en su lugar va un hecho verificable en el árbol: la remediación de SEC-051 **ya está contratada** en REQ-024 CA-08/CA-09 con su ADR, así que absorberla aquí escribiría **dos** contratos sobre la misma conducta. Los otros dos motivos (son tres cosas de naturaleza distinta; el coste marginal no es de dos líneas) **siguen en pie sin cambios** y son los que sostienen el reparto. **La fila anterior de este Historial no se edita hacia atrás** —misma práctica que el registro de seguridad—: queda superada por ésta | **SEC-052**, remediación 3 (R-014 §3, párrafo final): con los vencimientos reales el argumento «no tiene fuerza: no hay vencimiento holgado que importar dentro de uno que no puede resbalar» | — |
| 2026-09-08 | **Pregunta abierta 1 → RESUELTA sin ADR, y con ella tres correcciones de criterio.** Antes: «¿son CA-03 y CA-04 simultáneamente satisfacibles sin un ADR?», con la palanca propuesta de un **conjunto positivo sobre el alfabeto de la clave con escapes de bytes**. Después: **sí, son satisfacibles con el lector que existe y sin ADR**, pero **no con esa palanca** — desmentida midiendo en sus tres formas implementables: por **bytes**, los admitidos se comparten con los ilegítimos, `U+00AD` (familia (ii) **declarada**) entra y además **diverge por locale**, que es CA-05; por **secuencias con sustracción**, el veredicto depende del **orden de la tabla** y para cada orden hay un malformado que admite (H-01 aplicado al alfabeto: retirar **crea** el carácter admitido); y el conjunto positivo correcto es **cuadrático** (cociente **3,65** contra un techo de 2,2, ×13,7 sobre la base). El mecanismo que sí pasa **no enumera caracteres: enumera lo que ya está enumerado, que son las CLAVES** — *retirado de la clave todo lo ajeno al alfabeto de las claves que el lector reconoce, ¿lo que queda **es** una de esas claves?* Dos expansiones y un `case`: **0 procesos**, **0 falsos positivos en 356 líneas** de cabecera, veredicto **idéntico** bajo `LC_ALL=C` y `C.UTF-8` (y por razón estructural: el corchete contiene sólo bytes ASCII), denegando las **tres** familias completas y las **seis** entradas reservadas imprimibles (`Ω`, CJK, emoji, `U+FE0F`, tag, `U+2028`), y callando sobre `Módulo:`, `Versión destino:`, los títulos con `—`/`«»`/`¿` y las cinco tolerancias nombradas de CA-04. Y la **premisa era más ancha de lo que decía**: el segmento de clave del corpus tiene **10** puntos de código no ASCII, no dos — las **líneas de título** llevan `:` y por tanto son «clave» para el lector | **Cata del `desarrollador` (2026-09-08, sólo lectura, nada escrito en el árbol)**, que es lo que la pregunta pedía: medir antes de escribir la guarda. Lo que la cata **no** midió está declarado en «Notas / alcance» § «El techo honesto de la cata» y **ningún criterio lo da por cubierto** | — |
| 2026-09-08 | **`CA-03` gana el UNIVERSO del sorteo y el PROCEDIMIENTO de inyección, que no declaraba** — y de ahí salían tres lecturas con tres costes. Después: se fija la **R1** (universo = **cualquier** punto de código; la entrada reservada se **INSERTA** en una línea que declara un campo reconocido), bajo la cual el mecanismo deniega siempre y no hace falta ADR. Y las otras dos se **nombran fuera de alcance con su motivo medido**: la **sustitución por homóglifo** (`Е` cirílico, `Ｅ` fullwidth) **no es satisfacible con lo que existe** —sus tres salidas son estrechar el alfabeto (ADR + fricción constante, que es lo que CA-04 existe para impedir), una **tabla de homóglifos** (la enumeración que envejece) o el recorrido **cuadrático** (techo de CA-09 iii)—: **ahí sí** los dos criterios son satisfacibles por separado e imposibles juntos, va a «Fuera de alcance» con dueño y ventana y con la frase que CA-12 (iii) exige (el verde de este REQ **no** cierra la clase); y el universo = «**la clase de CA-01**» se descarta porque el sorteo necesitaría un **oráculo de la clase**, y el único disponible sería una tabla — si es la misma que usa la guarda, **el caso pasa por construcción**, que es la forma **(d)** que REQ-021 cazó en la ventana 1.33.0 | Un criterio que no declara su universo ni su procedimiento admite la lectura que hace pasar la implementación más barata, y la barata aquí es la tautológica. Saber **por qué** se descartan las otras dos es lo que impide que vuelvan | — |
| 2026-09-08 | **`CA-09 (iii)` medía el SUJETO equivocado, y era lo más grave de la cata.** Antes: el cociente de duplicación se ancla en **`arnes_sin_cita`**. Después: el sujeto se enuncia **por propiedad** — «**el escáner en el que la guarda de este REQ resida**, determinado por el código y no por este texto; si reside en más de uno, todos ellos» —, más un párrafo que explica el defecto con sus números. Medido: la guarda que CA-01 necesita **no puede vivir en `arnes_sin_cita`** —necesita el **segmento de clave**, que esa función no produce, y partirlo por `:` dentro sería una segunda transcripción de la regla de clave (CA-06)—, y el cociente de `arnes_sin_cita` se queda en **1,06 con y sin guarda** mientras un candidato cuadrático marca **2,63–3,65** en `arnes_norm_clave`, **donde nadie miraba**: un (iii) que nombrara `arnes_sin_cita` **daría PASS a una guarda cuadrática**, y (ii) tampoco lo salvaría (a n=60 el cuadrático es ×3 sobre el camino de línea y puede diluirse por debajo de 1,25× en la ruta crítica). **Y el margen se escribe sin maquillar:** el mecanismo conforme marca **2,05 a n=2000 contra un techo de 2,2** — margen **estrecho**, y sólo cumple porque se miden funciones distintas | Cata del `desarrollador`. Es la forma **(d)** una vuelta más —techo bien puesto sobre la magnitud equivocada—, agravada por escribir el sujeto con el **nombre de hoy**: así el criterio envejece con la **primera mudanza** del código, que es exactamente el defecto que la cata encontró | — |
| 2026-09-08 | **`CA-11` gana el caso del campo legítimamente COMENTADO y `CA-06` gana la exigencia sobre el sitio de publicación — porque el sitio de la guarda no es libre y la elección obvia produce un falso positivo.** Medido contra `arnes_estado_cabecera` real: publicando desde la llamada **cruda** de `hooks/lib.sh:1880`, `<!--Estado: completado -->` y `<!--Rigor: ligero-->` (**sin** espacio) **disparan la guarda** mientras `<!-- Estado: completado -->` (**con** espacio) calla — la guarda denegaría un REQ cuyo campo está legítimamente comentado y el veredicto dependería de si quien escribió el comentario puso un espacio; viola **CA-11** y **CA-04**. Después: **CA-11** exige veredicto **idéntico al de hoy** en las formas sin espacio, **CA-06** exige que la publicación **no** venga de esa llamada cruda, y **CA-06 declara el RADIO del cambio** en vez de dejarlo al implementador: publicar desde `arnes_campo_linea` deja el campo `Estado` **sin guarda en su propio lector**; publicar desde `arnes_norm_clave` exige un **interruptor por llamador**, con lo que la guarda deja de ser «la primera sentencia del único escáner» como sí lo es la del CR (REQ-016). Se corrige además una afirmación **medida falsa** que CA-06 llevaba: «todas las bocas entran por `arnes_campo_linea`» — `arnes_estado_cabecera` llama a `arnes_norm_clave` **directamente dos veces** (`:1880` y `:1891`), y la de 1880 recibe la línea cruda **a propósito** (`:1874-1878`) | Cata del `desarrollador`. Un criterio que no declara el radio del cambio lo deja para que alguien lo descubra implementando, y aquí lo que se descubre es un falso positivo sobre un documento legítimo — la fricción que `AGENTS.md` §13 dice que termina con alguien apagando el guard | — |
| 2026-09-08 | **`CA-06` gana el tercer punto, de la clase de `SEC-050`: el conjunto de CLAVES se declara UNA vez como constante.** La guarda añadiría la **tercera** transcripción de ese conjunto —hoy vive en los brazos `case` de `arnes_campos_req` (`hooks/lib.sh:1833-1839`) y en `arnes_estado_cabecera` (`:1881`, `:1892`), y `tools/arnes-lectura.sh:66-72` lo **deriva** de ahí con `sed` en vez de copiarlo—, y entonces un séptimo campo que entre en los brazos y no en la constante de la guarda **deja de estar protegido en silencio: envejece hacia el lado que abre**. Después: constante delimitada al estilo de `ARNES_VOCAB_*` (`hooks/lib.sh:1741-1749`) usada en las tres, con las transcripciones creciendo en **no más de 0** (**operativo**), y el **alfabeto** de las claves **derivado con escape** y nunca teclado — una clave futura con `-`, `]` o `^` rompería la expresión de corchete | Es la forma **(a)** dentro del código que esa doctrina gobierna, la misma clase que `SEC-050`, y el desarrollador pidió que la nota del escape quedara escrita en el criterio y no en su cabeza | — |
| 2026-09-08 | **`CA-12 (ii)` ANCLADO a su corpus y su versión, y el conflicto con REQ-024 registrado como resuelto.** Antes: «cuenta exactamente lo mismo —sobre el corpus de colas que el banco ya ejerce— antes y después de esta versión». Después: la comparación se ancla al **corpus que el banco ejerce en el momento de esta versión** contra la **versión heredada de esta misma ventana**, y se dice que **no** es la afirmación abierta «la cola nunca cambia su conteo». Y se añade el dato que el `desarrollador` verificó **ejecutando**: `arnes_cola_pendientes` (`hooks/lib.sh:1152-1181`) **no llama** a `arnes_sin_cita`, ni a `arnes_campo_linea`, ni a `arnes_norm_clave` —su única llamada es `arnes_lee_archivo` y su noción de comentario es propia e inline, `:1160-1163`—, así que para **esta** guarda (ii) se cumple **por construcción** y lo que puede cambiar el conteo es **REQ-024**, no este REQ. **Retirada la pregunta abierta 3** («REQ-024 sin crear»): `requirements/REQ-024.md` existe | **Conflicto registrado en el origen por el analista de REQ-024** (`requirements/REQ-024.md:514-524`): sus `CA-08` y `CA-09` **cambian** el conteo y el `rc` para dos formas. No hay contradicción **si** (ii) queda anclado; **sí** la hay si se implementa como no-regresión **abierta**, y entonces REQ-024 rompería una prueba de este REQ | — |
| 2026-09-08 | **`CA-10` reescrito por propiedad, y con ello RETIRADA la pregunta abierta 2** («¿entra CA-10 completa o sólo la fila de `AGENTS.md` §13?»). Después: el texto **nombra** la pregunta por estado y **sobre los comandos dice lo que sea cierto en el momento de escribirlo** —si ninguno la responde, lo dice; si alguno la responde, lo **cita**—, conservando en las dos ramas las tres declaraciones de REQ-016 CA-09 (ii) junto a cada barrido por vía y la prohibición de afirmar completitud | Un criterio cuya verdad depende del **calendario de otra comisión** es un criterio que envejece sin que nadie lo toque. La contingencia era la herramienta de «comprobación por estado» que REQ-016 dejó en «Fuera de alcance»; enunciando el criterio por propiedad, deja de ser una pregunta que alguien tenga que responder antes de despachar | — |
