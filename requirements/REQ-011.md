# REQ-011 — La puerta que pregunta DESPUÉS: el estado terminal reconstruido entre dos expansiones sale allow, y preguntar antes no cierra la clase
Estado: pendiente
Módulo: hooks (`post-bash.sh` nuevo, `hooks.json`, `lib.sh`; `guard-completado.sh` **sin cambio**) + templates/ + skills/arnes-upgrade + banco tests/escenarios/hooks/run.sh
Prioridad: alta
Versión destino: 1.32.0
Sensible a seguridad: sí
QA: pendiente
Seguridad: pendiente
Hallazgos abiertos: (ninguno)
Rigor: critico
NFR relacionados: (ninguno separado; los umbrales aplicables están cuantificados en CA-18, CA-19, CA-20 y CA-21)

## Historia

Como responsable de que un REQ no se cierre por la puerta de atrás, quiero que el arnés deje de
preguntar **antes** si el texto de un comando *va a* escribir un estado terminal —pregunta abierta que
admite formas nuevas sin fin— y pregunte **después** si algún REQ cambió de estado o si cambió algún
archivo protegido sin pasar por la puerta, para que una escritura que rodeó la comprobación quede
delatada en el acto, con el comando que la causó, en vez de quedarse en disco y en silencio.

## Criterios de aceptación

### Bloque A — el caso medido, detectado a posteriori

- **CA-01** — [el caso medido · fail-before/pass-after] Dado un proyecto del arnés y un agente cualquiera, Cuando emite por `Bash` un `cat > requirements/REQ-XXX.md <<EOF` **sin citar el delimitador** cuyo cuerpo escribe el estado terminal como la concatenación de dos sustituciones (`Estado: $(echo compl)$(echo etado)`), Entonces el hook posterior emite un **hallazgo** que nombra el archivo, el REQ, el estado **antes → después** y el comando que lo causó. Contra **v1.31.0 publicada** (la línea base de no-regresión de 1.32.0) la llamada sale **allow** y el archivo queda en disco con la palabra reconstruida, sin que nada lo diga.
- **CA-02** — [la detección nombra el comando] Dado el hallazgo de CA-01, Cuando se inspecciona su texto, Entonces contiene el comando **literal** que lo produjo (el `command` del `tool_input`, recortado a un tope declarado en el código y con la marca de recorte visible) y el nombre del agente que lo emitió. Un aviso que dice «algo cambió» sin decir qué lo cambió obliga a una investigación que nadie hace.
- **CA-03** — [la regla es del disco, no del texto] Dado el mismo escenario escrito de **cuatro** formas distintas —(a) con una variable (`e=compl; echo "Estado: ${e}etado" > …`), (b) con `printf`, (c) con `base64 -d`, (d) con un intérprete (`python x.py` / `node x.mjs`) cuya ruta de escritura vive **dentro** del archivo y no en el texto del comando—, Cuando llegan las cuatro llamadas, Entonces la detección posterior emite hallazgo en las **cuatro**. Es el criterio que separa este REQ de un parche de texto: la puerta posterior no lee el comando, lee el disco.
- **CA-04** — [cambio de estado por shell] Dado un REQ cuyo `Estado:` en disco es distinto **antes** y **después** de una llamada de `Bash`, y cuyo valor posterior es el estado terminal declarado en `estados.completado` (leído con el mismo lector de `hooks/lib.sh` que usa la puerta), Cuando termina la llamada, Entonces se emite hallazgo: la transición ocurrió sin que ninguna puerta evaluara veredictos, cola de aprobaciones ni quality gates.
- **CA-05** — [código protegido tocado por quien no debe] Dado un archivo que casa con `codigo_app.globs` y una llamada de `Bash` emitida por un agente distinto de `agentes.agente_codigo`, Cuando el archivo cambia durante esa llamada, Entonces se emite hallazgo nombrando la ruta y el agente. Cierra como **detección** el hueco del intérprete, que como prevención `AGENTS.md` §13 ya declara imposible.
- **CA-06** — [archivo nuevo, archivo borrado] Dado el mismo escenario de CA-05 con un archivo protegido **creado** y otro **borrado** durante la llamada, Cuando termina, Entonces los dos se detectan: la comparación cubre existencia, no sólo modificación de lo que ya estaba.

### Bloque B — lo que NO detecta (controles: una puerta que grita siempre se apaga)

- **CA-07** — [control] Dado un `Bash` de sólo lectura (`ls -la`, `git status`, `grep -r x .`), Cuando termina, Entonces se emiten **0** hallazgos.
- **CA-08** — [control] Dado el agente `desarrollador` (el declarado en `agentes.agente_codigo`), Cuando escribe por `Bash` en un archivo de `codigo_app.globs`, Entonces se emiten **0** hallazgos: la puerta posterior aplica **la misma** regla de identidad que `guard-codigo`, no una segunda.
- **CA-09** — [control] Dado un `Bash` que escribe fuera de `codigo_app.globs` y fuera de `requirements/` (por ejemplo en `docs/` o en un temporal), Cuando termina, Entonces se emiten **0** hallazgos.
- **CA-10** — [control, no invade la vía que ya se juzga] Dado un REQ modificado por `Edit`/`Write`/`MultiEdit` (no por `Bash`), Cuando termina la llamada, Entonces la puerta posterior **no opina**: esa vía ya la juzgó `guard-completado` por delante, con el contenido resultante a la vista, y duplicar el veredicto produciría dos mensajes para una sola decisión.
- **CA-11** — [control, no se inventa una regla más ancha] Dado un `Bash` que cambia el `Estado:` de un REQ de `pendiente` a `en-progreso` (un estado **no** terminal), Cuando termina, Entonces se emiten **0** hallazgos: escribir un REQ desde la shell no está prohibido; lo que no puede hacerse por shell es **cerrarlo**. La puerta posterior detecta la regla que existe, no una nueva.
- **CA-12** — [control] Dado un `Bash` que reescribe un REQ **sin** cambiar su `Estado:` (añade una fila al historial, corrige una errata), Cuando termina, Entonces se emiten **0** hallazgos: lo vigilado es la transición, no la edición.

### Bloque C — convive con la puerta previa; no la sustituye ni la apaga

- **CA-13** — Dado el banco de `guard-completado` sobre `Bash` tal como quedó en **v1.31.0 publicada**, Cuando corre con la puerta posterior activa, Entonces **todos** los casos que respondían DENY siguen respondiendo DENY, con el mismo motivo: la puerta previa sigue siendo la primera red y este REQ no le quita ni un caso.
- **CA-14** — Dado `hooks/guard-completado.sh`, Cuando se compara con **v1.31.0 publicada**, Entonces la comprobación que busca el estado terminal en el texto del comando es **idéntica** (diff vacío en esa función): este REQ **no** ensancha el `grep` ni le añade la extracción de heredocs. Parchear el texto cubriría el caso medido y no la clase, y cada patrón nuevo compra un caso al precio de un falso positivo — la puerta previa se queda como está, **barandilla y no jaula** (`AGENTS.md` §13).
- **CA-15** — Dado que `PostToolUse` se ejecuta **después** de la llamada, Cuando se revisa el hook, Entonces **no** intenta denegar ni deshacer nada (la escritura ya ocurrió): el hallazgo viaja por la vía documentada de esa fase para que llegue al modelo, el hook **nunca** interrumpe la sesión y la limitación queda escrita en el comentario del código —mismo patrón que CA-14 de REQ-003 con `systemMessage`—. Una puerta posterior que pretenda bloquear miente sobre lo que puede hacer.
- **CA-16** — [el hook no escribe en el árbol] Dado el hook posterior, Cuando se revisa y cuando se ejecuta, Entonces **no** escribe en `requirements/`, en `docs/` ni en ningún archivo versionado: su única salida es el hallazgo. La marca de tiempo de referencia vive en el directorio temporal declarado (fuera del árbol o bajo una ruta ignorada por git); si no puede escribirla, lo **dice** y no bloquea.
- **CA-17** — [modo degradado, declarado y ruidoso] Dado que la marca previa no existe o no se pudo leer (primera llamada de la sesión, temporal borrado), Cuando termina un `Bash`, Entonces el hook lo declara en su salida (`sin línea base: esta llamada no se comparó`) y **no** afirma que no hubo cambios: una puerta que no puede medir no dice «todo bien».

### Bloque D — coste acotado (es lo que decide si esto se puede encender)

- **CA-18** — Dado el hook posterior, Cuando se revisa su implementación, Entonces la comparación se hace con **marca de tiempo, no con hash**, usando los operadores de bash (`-nt`, `-e`) sin arrancar `find`, `stat`, `sha256sum` ni `md5sum`: el número de procesos externos dentro del hook es **0** (medido con esos binarios instrumentados en el `PATH`, cuyos registros quedan vacíos), y el único proceso añadido por llamada es **el propio hook**.
- **CA-19** — Dado que en esta plataforma cada fork cuesta **1,2–6 s**, Cuando se revisa `hooks/hooks.json`, Entonces el hook posterior se registra **sólo** para `Bash` y para ninguna otra herramienta: `Edit`/`Write`/`MultiEdit` ya se juzgan por delante (CA-10) y registrarlo ahí pagaría el proceso sin comprar nada.
- **CA-20** — Dado un proyecto con **200** archivos protegidos y **60** REQ, Cuando termina una llamada de `Bash`, Entonces la comprobación completa termina en **menos de 200 ms** y la invocación del hook responde en **menos de 5 s**.
- **CA-21** — Dado el `PreToolUse` que ya corre para todo comando de `Bash`, Cuando se toma la línea base de la comparación, Entonces se toma **dentro de esa invocación que ya existe** y añade **0** procesos a esa ruta respecto a v1.31.0 publicada: la fotografía previa no puede costar un fork más en el camino que recorre todo comando de todo agente.
- **CA-22** — Dado el manifiesto, Cuando declara `puerta_posterior.activo: false`, Entonces el hook no hace **nada** (0 procesos, 0 hallazgos) y ni la sesión ni ninguna puerta cambian; y **por defecto**, con el bloque ausente, la puerta está **activa**: su ausencia dejaría en pie una afirmación del contrato que este REQ mide como incompleta. Si la medición de CA-20 no se sostuviera en la plataforma del desarrollador, el valor por defecto vuelve al analista con la cifra medida y se cambia con entrada en el Historial; no se decide por omisión.
- **CA-23** — Dado un entorno sin `jq` o un proyecto sin `.arnes/config.json`, Cuando termina una llamada de `Bash`, Entonces el hook es **inerte**, no emite hallazgos y no bloquea: el cambio no introduce ninguna dependencia dura nueva.
- **CA-24** — Dado el banco tal como quedó en **v1.31.0 publicada**, Cuando corre sobre la candidata, Entonces ningún caso que pasaba antes falla ahora y **ningún** caso pasa de `deny` a `allow`.

### Bloque E — evidencia obligatoria: fail-before / pass-after

- **CA-25** — Dado el banco ejecutado con `ARNES_HOOKS_DIR` apuntando a la instalación estable anterior (**v1.31.0 publicada**), Cuando se corren los casos CA-01, CA-03 (las cuatro variantes), CA-04, CA-05 y CA-06, Entonces **FALLAN** todos; y contra los hooks del worktree candidato, Entonces **PASAN** todos.
- **CA-26** — Dado el mismo banco contra la instalación estable anterior, Cuando se corren los controles CA-07 a CA-13, Entonces **PASAN también** contra ella: son controles de no-regresión, y un control positivo que falla antes del cambio estaría midiendo otra cosa.
- **CA-27** — [la prueba se hace en un shell real] Dado el caso de CA-01, Cuando se ejecuta el comando en un sandbox de verdad, Entonces se verifica que el archivo **queda escrito** con la palabra reconstruida: el caso no se da por bueno leyendo la salida del hook, sino comprobando el efecto que el hook no impidió. Es la lección de CA-16/CA-17 de REQ-001.

### Bloque F — puertas comunes de 1.32.0 (banco, plantillas, versión y cierre)

- **CA-28** — Dado el PR de la rama candidata, Cuando corre el check `hooks-en-linux`, Entonces el banco completo reporta PASS, FAIL y SKIP por separado, no hay **ningún** FAIL, y cada SKIP explica en su línea la plataforma o la condición que lo salta.
- **CA-29** — Dado el banco tras añadir los casos de este REQ en su propia sección, Cuando termina, Entonces `CASOS_ESPERADOS` coincide con el número real de casos ejecutados (`PASS+FAIL+SKIP`) y el cuadre final no aborta.
- **CA-30** — Dado el índice de git del PR, Cuando corre el paso de bits de ejecución del CI, Entonces `hooks/post-bash.sh` figura en modo `100755`, igual que el resto de puntos de entrada; y ejecutado **directamente** (no `source`) hace su propio preludio y decide igual que dentro del registro de hooks — producción y banco ejecutan **la misma** función, no dos copias que puedan desfasarse.
- **CA-31** — Dado `templates/arnes-config.json.tpl`, Cuando se revisa, Entonces declara el bloque `puerta_posterior` con su valor por defecto y un `_doc` que explica la diferencia entre la pregunta **abierta** de la puerta previa (*¿este texto escribirá?*) y la **cerrada** de la posterior (*¿cambió algo protegido?*), y por qué la segunda detecta pero no previene; sigue siendo JSON válido.
- **CA-32** — Dado `templates/AGENTS.md.tpl` y `templates/requirements-README.md.tpl`, Cuando se revisan, Entonces la tabla de enforcement por runtime distingue **prevención** (deniega antes) de **detección** (delata después), dice que la cobertura de `Bash` por delante sigue siendo parcial **a propósito** y que la puerta posterior no la sustituye; y **ninguna** plantilla recibe reglas exclusivas del autoalojamiento de este repositorio.
- **CA-33** — Dado `skills/arnes-upgrade/SKILL.md`, Cuando se revisa, Entonces contiene la sección `### Hacia 1.32.0` explicando que aparece un hallazgo nuevo **después** de ciertos comandos de `Bash`, que no bloquea nada, cuánto cuesta (un proceso por llamada de `Bash`) y cómo apagarlo.
- **CA-34** — Dado el commit de versión, Cuando se revisa, Entonces `.claude-plugin/plugin.json` y `.claude-plugin/marketplace.json` declaran `1.32.0`, ambos son JSON válidos y coinciden entre sí; `jq -e .` pasa sobre `hooks/hooks.json`; y `bash -n` pasa sobre todos los `hooks/*.sh` y `tools/*.sh`.
- **CA-35** — Dado `CHANGELOG.md`, Cuando se revisa la entrada de 1.32.0, Entonces describe el cambio **en la forma del hallazgo** (el estado terminal reconstruido entre dos expansiones dentro de un heredoc sin citar sale allow, y preguntar antes no cierra la clase), con fecha, origen, usuario, modelo y agentes, y **sin** nombres de proyectos cliente, identificadores de sus REQ ni datos suyos.
- **CA-36** — Dado este REQ, Cuando se intenta cerrarlo, Entonces sólo se acepta si **1.32.0 está publicada y verificada** (tag `v1.32.0` sobre el commit de fusión, verificado, e instalación estable actualizada), con `PENDING_APPROVAL.md` sin entradas bajo `## Pendientes`, `QA: aprobado` y `Seguridad: aprobado`.

## Notas / alcance

**Origen — medido, con control positivo y negativo (2026-09-05).** Un informe privado de defectos desde
un proyecto real que corre el arnés, reproducido por la coordinadora con sondas propias. La
comprobación de `guard-completado` que frena una escritura por `Bash` hacia el directorio de
requerimientos busca la palabra del estado terminal en el **texto crudo** del comando
(`hooks/guard-completado.sh`, el `grep -iqE` sobre `$bash_cmd`). Se evade partiendo la palabra entre
dos sustituciones dentro de un heredoc **sin citar**: la llamada sale **allow** y el archivo queda en
disco con la palabra reconstruida. Medido **allow en v1.30.3 y en la candidata 1.31.0**. Controles
medidos: la palabra entera → **deny** en las dos; sin la palabra → **allow** en las dos (la sonda no
dispara sola); la variante con una sola letra fuera de la expansión → **allow**.

**No lo cierra el trabajo de heredocs de 1.30.3, y esto conviene entenderlo bien.** REQ-001 enseñó al
detector a resolver sustituciones dentro de un heredoc sin citar, pero ese trabajo alimenta la
detección del **destino** de escritura (`arnes_bash_escrituras`): responde *¿en qué archivo escribe
este comando?*. La comprobación que aquí se evade es **otra ruta sobre el mismo comando**: una vez
sabido el destino, pregunta *¿el texto menciona el estado terminal?*. Son dos preguntas distintas
sobre el mismo texto, y el arreglo de la primera no alcanza a la segunda.

**La mitad que vale de este REQ es lo que NO hace.** Aplicar la extracción de heredocs también a esta
comprobación cierra el caso medido y **no la clase**: quedan una variable, `printf`, `base64 -d`,
`eval`, `${x:0:4}`, `$'\143'` y —el agujero grande— cualquier **intérprete** cuyo texto de escritura
vive dentro de un archivo que el hook no lee. Preguntar antes es una pregunta **abierta**: admite
formas nuevas sin fin, y cada patrón que se añade compra un caso al precio de un falso positivo que
termina con alguien apagando el guard. Preguntar después es una pregunta **cerrada**: *¿cambiaron los
archivos protegidos? ¿cambió el estado de algún REQ sin pasar por la puerta?* Se contesta mirando el
disco, no adivinando el shell, y se contesta igual para todas las formas de arriba (CA-03).

**Lo que gana y lo que no.** No puede deshacer la escritura —cuando el hook corre, ya ocurrió— y por
eso no pretende denegar (CA-15). Gana la **detección en el acto y con el comando que la causó**, que
es la diferencia entre un desvío que se descubre en la auditoría de cierre, semanas después y sin
saber quién lo hizo, y uno que se corrige en la misma llamada. Es exactamente el arreglo de fondo que
el propio arnés ya tiene nombrado para 1.32.0 en `docs/PENDIENTES.md`.

**La puerta previa se queda como está (CA-14).** No se ensancha el `grep`, no se persigue el shell
arbitrario. `AGENTS.md` §13 ya declara por escrito que la cobertura de `Bash` es parcial **a
propósito**; este REQ no la corrige, la **complementa** por el otro lado. Las dos conviven y el banco
lo mide (CA-13).

**Por qué es crítico y sensible.** Toca el mecanismo que gobierna a los demás proyectos, y lo que se
rodea es precisamente la puerta de cierre: un REQ que llega a estado terminal sin que se evalúen
veredictos, cola de aprobaciones ni quality gates. Un fallo en abierto aquí es un fallo en abierto en
todos los proyectos que instalan el arnés, y en silencio.

**Fuera de alcance.**
- Ensanchar la detección previa de texto en `guard-completado.sh` o en `arnes_bash_escrituras`
  (CA-14): es la vía que este REQ declara agotada.
- Deshacer, revertir o poner en cuarentena la escritura detectada: la puerta posterior delata, no
  restaura.
- Comparar **contenido** (hash) en vez de marca de tiempo: descarta un cambio que escribe los mismos
  bytes, y su coste no cabe en el presupuesto de CA-18/CA-20. Si alguna vez hiciera falta, es otro REQ
  con su propia medición.
- Vigilar herramientas distintas de `Bash` (CA-19), ni la Capa 0 opcional de `PostToolUse` sobre
  `Write|Edit` que figura por separado en `docs/PENDIENTES.md`.
- Registrar el hallazgo en un artefacto del proyecto (CA-16): el hook no escribe en el árbol.
- No se abre un NFR separado: los umbrales aplicables están cuantificados en CA-18, CA-19, CA-20 y CA-21.

## Preguntas abiertas / conflictos

(ninguna.)

**REQ-001 — sin conflicto de contenido, con una consecuencia declarada por delante.** REQ-001 está
`completado` y sus criterios siguen siendo ciertos: los casos CA-16 a CA-27 y CA-46 a CA-49 se refieren
a la detección del **destino** de escritura, y esa detección funciona; su sección «fuera de alcance»
declara además, literalmente, que cualquier otra mejora del detector de Bash queda fuera porque su
cobertura es **parcial por diseño**. Lo que este REQ mide como incompleto no es un criterio de REQ-001
sino la afirmación general de `AGENTS.md` §13 («la transición a `completado` no se hace por shell»),
que la propia §13 matiza en el párrafo siguiente. **Decisión tomada, no pendiente:** no se abre
hallazgo `contrato` contra REQ-001 y **REQ-001 no se reabre**; el write-back de esta medición es este
REQ-011 más la precisión de §13 que entra con él (CA-32). Si al revisar, el `auditor-seguridad`
juzgara lo contrario, la clase del hallazgo sería **`contrato`** —el requerimiento afirmaría algo falso
sobre lo construido— y el write-back correspondería a REQ-001, que volvería a `en-revisión`. Queda
escrito aquí para que esa decisión sea explícita y no se descubra al final.

> **Confirmado por escrito el 2026-09-05, y el write-back ya está en disco.** La coordinadora aceptó
> esta lectura de la analista, así que deja de ser una propuesta de este REQ y pasa a ser el registro
> de una decisión: **REQ-001 no se reabre y no recibe hallazgo `contrato`**. La deriva medida —la
> afirmación de §13 era más fuerte que lo que la máquina cumple— **ya está reflejada en los dos
> textos que la declaran**, y no queda pendiente de este REQ:
> - **`AGENTS.md` §13**, párrafo «**Y ancha no es infalible, y está medido**» (dentro del apartado
>   «`guard-completado` sí mira `Bash`, pero no lo juzga: lo DERIVA»): dice que esa detección lee el
>   **texto crudo del comando**, que partir la palabra del estado terminal entre dos expansiones
>   dentro de un heredoc sin citar la evade y **el archivo queda escrito**, que ensanchar el patrón
>   cubriría la forma y **no la clase**, y que la respuesta es la **puerta posterior** —este REQ—.
>   Escrito por la **coordinadora**.
> - **`templates/AGENTS.md.tpl`**, párrafo «**Y ancha no es infalible**», el mismo contenido
>   **espejado por el `desarrollador`** para los proyectos que heredan el arnés, con la frase que
>   cierra el motivo: *una promesa más fuerte que la que la máquina cumple es peor que ninguna,
>   porque se confía en ella*.
>
> Consecuencia para el QA y para el auditor: la no-deriva de esta medición **no** se verifica contra
> REQ-001, se verifica contra esos dos párrafos más los criterios de este REQ; y **CA-32** de aquí no
> introduce esa precisión desde cero —ya existe— sino que exige que la plantilla la conserve y que la
> tabla de enforcement distinga **prevención** de **detección**. Si esos párrafos desaparecieran o se
> debilitaran en una versión posterior, eso **sí** sería hallazgo `contrato`, y contra el documento
> que lo perdiera.

**REQ-007 — superficie compartida, sin contradicción y con orden.** CA-36, CA-38 y CA-39 de aquél
tocan el mismo detector de heredocs y comillas por el lado de la **prevención**; éste no lo toca en
absoluto (CA-14). Consecuencia práctica: REQ-007 va antes, en 1.31.0, y este REQ se mide **después**,
contra v1.31.0 ya publicada — de modo que si algún caso de aquí cambiara de veredicto, la causa sería
inequívocamente de este REQ y no del detector. Y al revés: los casos de CA-03 que hoy salen `allow`
seguirán saliendo `allow` en la puerta previa incluso con REQ-007 aterrizado; eso no es una regresión
de aquél, es la clase que sólo cierra la detección posterior.

**REQ-005 — sin conflicto, con una nota de orden en `hooks.json`.** `guard-git` es `PreToolUse` sobre
`Bash` y esta puerta es `PostToolUse` sobre `Bash`: no compiten por el mismo veredicto. Un comando
denegado por `guard-git` **nunca** llega a ejecutarse, así que la puerta posterior no corre para él y
no puede emitir un hallazgo sobre algo que no ocurrió; conviene que el banco lo mida como control.

**REQ-003, REQ-009 y REQ-010 — sin conflicto, con dependencia de lectura.** Los tres consolidan en
`hooks/lib.sh` el lector, el vocabulario y la normalización de los campos. CA-04 de este REQ lee el
`Estado:` de un REQ antes y después de la llamada y debe hacerlo con **esa** función, no con una
lectura propia: si la puerta posterior leyera el estado con otras reglas que la puerta previa, un REQ
cerrado con una tilde de más o de menos escaparía también a la detección — que es exactamente el
defecto que REQ-010 cierra por el otro lado.

## Trazabilidad
Origen: informe privado de defectos de un proyecto real que corre el arnés (2026-09), reproducido y
medido por la coordinadora en el autoalojamiento el 2026-09-05, con control positivo (palabra entera →
deny) y negativo (sin la palabra → allow; una letra fuera de la expansión → allow), en v1.30.3 y en la
candidata 1.31.0 con resultado idéntico (**allow** en las dos).
Causa raíz identificada por lectura: `hooks/guard-completado.sh` — la comprobación
`grep -iqE "(^|[^a-zA-Z])${estado_done}([^a-zA-Z]|$)"` se aplica al **texto crudo** del comando, no al
contenido que el comando producirá; es una ruta distinta de `arnes_bash_escrituras`, que sí resuelve
sustituciones en heredocs desde 1.30.3.
Arreglo de fondo ya nombrado por el propio arnés: `docs/PENDIENTES.md`, «Próxima: 1.32.0 — Puerta
posterior sobre el código de la app».
Write-back de la deriva (confirmado por la coordinadora el 2026-09-05, **REQ-001 no se reabre y no
recibe hallazgo `contrato`**) — los dos textos donde vive, y que este REQ sólo tiene que conservar:
- `AGENTS.md` §13, párrafo «**Y ancha no es infalible, y está medido**», dentro del apartado
  «`guard-completado` sí mira `Bash`, pero no lo juzga: lo DERIVA» — escrito por la **coordinadora**.
- `templates/AGENTS.md.tpl`, párrafo «**Y ancha no es infalible**», el mismo contenido **espejado por
  el `desarrollador`** para los proyectos que heredan el arnés (lo verifica CA-32).
Tocado por: analista-requerimientos / 2026-09-05 · analista-requerimientos (confirmación del
write-back de la deriva y punteros a los dos textos) / 2026-09-05

## Historial de cambios
| Fecha | Antes → Después | Causa | ADR |
|------------|-----------------|-------------------------|--------|
| 2026-09-05 | (creación) | El estado terminal reconstruido entre dos sustituciones dentro de un heredoc sin citar sale allow y el archivo queda escrito: la comprobación pregunta por el texto del comando, y preguntar antes es una pregunta abierta que no cierra la clase | — |
| 2026-09-05 | «Preguntas abiertas / conflictos», bloque **REQ-001**: la lectura de la analista («no se abre hallazgo `contrato`, REQ-001 no se reabre, el write-back es este REQ más la precisión de §13») estaba escrita como **decisión de este REQ a la espera de revisión** → queda **confirmada por escrito** y con el write-back **ya en disco**, con puntero exacto a los dos párrafos que lo contienen (`AGENTS.md` §13 «Y ancha no es infalible, y está medido», de la coordinadora; `templates/AGENTS.md.tpl` «Y ancha no es infalible», espejado por el `desarrollador`), añadidos también a la Trazabilidad. Se precisa el papel de **CA-32**: no introduce esa precisión, exige que la plantilla la **conserve** | La coordinadora aceptó la lectura de la analista (2026-09-05). Se registra para que ni el QA ni el auditor tengan que reconstruirla: el write-back de esta deriva **no** es un cambio pendiente en REQ-001, y la no-deriva se verifica contra esos dos párrafos más los criterios de este REQ. Queda declarado el reverso: si esos párrafos se perdieran o se debilitaran en una versión posterior, **eso** sería hallazgo `contrato` contra el documento que lo perdiera. Cambio **menor**: registra una decisión ya tomada; ningún criterio cambia | — |
