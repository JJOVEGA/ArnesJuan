# REQ-017 — El coste que ningún criterio miraba: la guarda del CR es cuadrática en la longitud de línea, y CA-08 dio verde midiendo procesos sobre una regresión de 10× de reloj
Estado: completado
Módulo: hooks (escáner de cabecera: `arnes_sin_cita` en `lib.sh`) + banco `tests/escenarios/hooks` + metodología de criterios (`requirements/README.md` y su plantilla heredable)
Archivos: hooks/lib.sh, tests/escenarios/hooks/secciones/37-coste-del-escaner-1-escala.sh, tests/escenarios/hooks/secciones/37-coste-del-escaner-2-la-seccion-caliente.sh, tests/escenarios/hooks/secciones/37-coste-del-escaner-5-el-camino-normal.sh, tests/escenarios/hooks/run.sh, tests/escenarios/hooks/README.md, requirements/README.md, templates/requirements-README.md.tpl, .github/workflows/banco.yml, docs/PENDIENTES.md, docs/qa/1.33.0.md, docs/decisions/ADR-004-dominio-de-equivalencia-por-invariancia-de-locale.md, requirements/REQ-021.md, docs/qa/1.34.0.md
Prioridad: alta
Sensible a seguridad: sí
QA: aprobado (2026-09-08, vuelta 1 de 3 del ciclo reabierto por §9, sobre 538c266: CA-08 (ii) PASA entero — unanimidad de las k, par discriminante ejecutado, techo ≤ 1,25× intacto, techo de coste y las TRES cifras del SKIP de no-convergencia; QA-017-16 (contrato) CERRADO con fail-before/pass-after y cinco mutantes, mensaje byte a byte el de 19b1822^; el piso de CA-18 411→448 verificado a mano y justificado —el bloque creció 37 líneas, medidas—; banco 882 PASS · 0 FAIL · 5 SKIP rc 0 y quality gates en verde; seis instrumento abiertos en `docs/qa/1.34.0.md`, ninguno bloquea)
Seguridad: aprobado (R-020, 2026-09-08, sobre 538c266 y después de QA; SUSTITUYE como veredicto vigente al de R-012 —2026-09-07, que no se retira pero cubría el árbol de 1.33.0, anterior a 19b1822—. Acredita: el rango no toca hooks/, tools/, .github/, .arnes/ ni nada heredable; ningún control aprobado se retiró ni se debilitó y TECHO47=1250 sigue intacto; la guarda nueva no puede emitir PASS donde tocaba FAIL, y QA-017-18 y QA-017-19 son inalcanzables desde el llamador de hoy —verificado por mí, incluida la vía del separador de campos—; sin fuga en repositorio público. NO acredita: quality gates, ejecución del banco, la derivación de k, el techo de coste 0,750× ni el comportamiento en el CI real. Residual declarado SEC-064 (instrumento, alta): la abstención de CA-08 (ii) no tiene cota. SEC-047 sigue instrumento y NO lo dispara este cierre, pero escala a contrato si 1.34.0 se publica sin su remediación)
Hallazgos abiertos: QA-017-07 (instrumento), QA-017-11 (instrumento), QA-017-13 (instrumento), QA-017-15 (instrumento), QA-017-17 (instrumento, dueño desarrollador), QA-017-18 (instrumento, dueño desarrollador), QA-017-19 (instrumento, dueño desarrollador), QA-017-20 (instrumento, dueño desarrollador), QA-017-21 (instrumento, dueño analista-requerimientos), QA-017-22 (instrumento, dueño analista-requerimientos), SEC-047 (instrumento, preexistente desde v1.30.3 o antes, dueño analista-requerimientos, R-012), SEC-048 (instrumento, dueño propietario para el ruleset de tags y desarrollador para el workflow, R-012), SEC-049 (instrumento, texto de CA-10 incompleto, dueño analista-requerimientos, R-012), SEC-064 (instrumento, la abstención de CA-08 (ii) no tiene cota, dueño analista-requerimientos y desarrollador, R-020), SEC-065 (instrumento, la exposición de SEC-058 registrada dos veces con urgencias distintas, dueño qa-tester y auditor-seguridad, R-020), SEC-066 (instrumento, el README del banco dice 886 casos y son 887, dueño desarrollador, R-020)
Rigor: critico
Versión destino: 1.34.0 (era 1.33.0, la versión en la que se publicó; reabierto el 2026-09-08 por la sonda de CA-08 (ii) — ver Historial)
NFR relacionados: (ninguno)

## Historia
Como proyecto que corre el banco del arnés en cada comisión y lo tiene como **puerta requerida de
`main`**, quiero que el escáner de cabecera deje de ser **cuadrático** en la longitud de línea y que el
arnés sepa **ver** esta clase de regresión la próxima vez, para no pagar 92 s donde pagaba 39 s ni volver
a dar verde sobre una degradación de 10× porque el criterio de coste medía la magnitud equivocada.

## Criterios de aceptación

- **CA-01 — La equivalencia se afirma sobre un DOMINIO declarado y comprobado en la corrida, no sobre «una línea cualquiera»: se abarata *cuándo* se paga la sentencia, no *qué* decide.** Dado `arnes_sin_cita` de `hooks/lib.sh`, Cuando se le pasa una entrada del **dominio de equivalencia** —definido por **propiedad de la heredada sola** y comprobado en la misma corrida, no por lista: las entradas sobre las que la implementación **heredada v1.32.1 publica un único y el mismo estado** (`ARNES_LINEA`, `ARNES_CITA`, `ARNES_CR`, `ARNES_CR_LINEA`) en **todas** sus evaluaciones dentro de esa corrida: **k ≥ 3** sobre la misma entrada y dentro del **mismo** proceso bajo el **locale del entorno**, **y k ≥ 3** más bajo **`LC_ALL=C`** (**k operativo**: se **sube** con la medición; **más** es conforme; QA usó 6). Son por tanto **dos** condiciones a la vez: que la heredada **repita** (determinismo) **y** que **decida lo mismo en los dos locales** (invariancia de locale); basta que falle una para que la entrada quede **fuera**—, Entonces este árbol —evaluado **k ≥ 3** veces bajo el locale del entorno y dentro del mismo proceso— es **también determinista** sobre ellas y su salida (`ARNES_LINEA`) y el estado que publica (`ARNES_CITA`, `ARNES_CR`, `ARNES_CR_LINEA`) coinciden **byte a byte** con los de la heredada, comprobado por comparación **diferencial** de las dos implementaciones sobre el mismo conjunto de entradas —el conjunto vive, **único**, en la sección de banco declarada en `Archivos:`, con semillas fijas para que el CI repita la misma corrida; ejemplos **no exhaustivos**: línea vacía, línea que es sólo el carácter, CR en la primera posición, CR sólo al final, CR final **y** otro interior, línea sin ningún CR, CR adyacente a `<!--` y a `-->`, y **byte multibyte inválido seguido de CR interior**—.

  **El dominio se traza por una propiedad de la HEREDADA SOLA, y eso es parte del criterio.** No se admite un dominio definido como «las entradas donde los dos árboles coinciden», ni ninguna variante que mire a este árbol para decidir la pertenencia: eso convierte CA-01 en un criterio que **no puede fallar**, y un criterio que no puede fallar no es una puerta. La comprobación es dura y barata: la clasificación dentro/fuera se calcula **sin haber evaluado todavía este árbol**.

  **Y el corpus NO se restringe al dominio: la partición se mide, no se supone.** El mismo conjunto de entradas **incluye** entradas que caen **fuera** del dominio —al menos secuencias **multibyte inválidas** bajo locale UTF-8, que es la única clase medida en la que la heredada se aparta de su propia semántica byte a byte—, y cada corrida **clasifica** cada entrada en dentro/fuera con la comprobación de arriba. Y las dos formas de quedarse sin entradas de fuera **no valen lo mismo**, porque una depende de este banco y la otra de la máquina: (a) si el **corpus** no contiene ninguna entrada de la clase declarada —el alfabeto perdió la ficha de byte inválido—, el caso **FALLA**, y eso se comprueba sobre el corpus, sin depender de la versión de bash ni del locale; (b) si el corpus **sí** las contiene pero en esa máquina la heredada resulta **determinista e invariante al locale sobre todas ellas** —otra versión de bash, o un locale del entorno que ya sea `C`/`POSIX`, donde el conjunto de fuera es vacío **por construcción** porque el entorno y el oráculo son el mismo locale—, el caso emite **SKIP citando bash, locale y el número de entradas clasificadas**, y **nunca PASS**, por la misma regla de CA-06: una sonda que no puede medir lo dice. La distinción no es formalismo: un corpus estrechado es **trabajo retirado** y tiene que doler; una máquina sin el defecto es **falta de sujeto** y poner rojo ahí es el modo de fallo que acaba con alguien apagando el caso. Un corpus que no puede generar el contraejemplo no acredita la equivalencia, sólo la repite —y ésa es exactamente la forma de verde que esta ventana persigue—. Lo que ocurre **fuera** del dominio lo contrata **CA-10**; aquí no se deja sin contratar, se contrata aparte.

  **Por qué un dominio y no «una línea cualquiera» — y por qué la propiedad es la invariancia de locale y no el determinismo, que es lo que este criterio dijo entre el write-back de la vuelta 0 y el de la vuelta 1 (`ADR-004`).** Sobre esa clase de entrada la heredada **no tiene un comportamiento único que igualar**: en bash 5.3.9 bajo locale UTF-8, `${l%$'\r'}` sobre una cadena con secuencia multibyte inválida no se comporta byte a byte, de modo que la heredada publica **un estado bajo UTF-8 y otro bajo `LC_ALL=C`**. «El estado de la heredada» deja de designar un objeto único, y una igualdad contra él no está definida hasta decir bajo qué locale: no era un techo exigente, era **insatisfacible**. Lo que **no** separa las dos clases es el **determinismo**, y está medido: la heredada incumple la **invariancia de locale** en **7 de 106** entradas (4 de 4 corridas) y el **determinismo** en **0–2**, los dos fenómenos **no coinciden**, y el conjunto divergente es el primero (`docs/qa/1.33.0.md`, QA-017-01 y QA-017-08). El motivo del error de la vuelta 0 se deja escrito porque es reutilizable: lo que se midió entonces era cierto del **valor intermedio** de la eliminación de sufijo —distinto en cada evaluación—, y este criterio contrata sobre el **estado publicado**, donde la rama que se toma suele ser la misma y **la heredada sí repite**. Se midió una magnitud correcta que no era la del contrato (la forma (d) de `requirements/README.md`, aplicada al **conjunto** en vez de a la magnitud). Un criterio insatisfacible no se arregla midiendo mejor; se le declara el dominio **por la propiedad que separa** y se contrata aparte lo de fuera. Lo que tampoco se hace —y por eso el fallo del párrafo anterior— es estrechar el corpus hasta que la clase desaparezca del verde.

  **La propiedad separa por construcción, no por casualidad — y por eso este criterio puede dar 0 sin dejar de poder fallar.** Sobre bytes, las dos sentencias hacen **la misma pregunta** —«¿hay un CR que no sea el último byte?»—: si el último byte es CR, `${l%$CR}` quita ése y lo que queda tiene un CR ⟺ había otro antes; si no lo es, no se quita nada y hay un CR ⟺ ese CR va seguido de algo. Bajo `LC_ALL=C` la heredada **es** esa pregunta; este árbol la implementa en **todos** los locales, porque no elimina sufijo con patrón. Luego los dos árboles divergen **exactamente** donde la heredada se aparta de su propia semántica byte a byte, que es la definición de «no invariante al locale». Valores de **referencia**, no techos (corpus de **106** entradas con secuencia multibyte inválida, 4 corridas del 2026-09-07): con este dominio quedan **dentro ~97–99** y **fuera no menos de 7** en **4 de 4** corridas, y la divergencia esperada dentro es **0** —las 6–7 divergencias observadas caen **todas** fuera—. Con el dominio anterior eran **6–7 dentro** (CA-01 falla) y **0 fuera en 3 de 4** (CA-10 sin sujeto). Y el criterio **sigue pudiendo fallar**: una entrada sobre la que la heredada sea invariante al locale y que aun así divergiera de este árbol sería un defecto real del arreglo y este caso lo vería, porque la clasificación no mira a este árbol.

- **CA-10 — Fuera del dominio de CA-01 la dirección está contratada contra un ORÁCULO: este árbol decide, en cualquier locale, lo que la heredada sólo acierta bajo `LC_ALL=C`.** *(va aquí, junto a CA-01, porque las dos son las mitades de una misma propiedad: dentro del dominio, equivalencia; fuera, dirección.)* Dado el mismo diferencial y las entradas que el corpus produce **fuera** del dominio de equivalencia —aquellas en que la heredada **no repite** o **decide distinto según el locale**—, Cuando se evalúa cada implementación **k ≥ 3** veces (**operativo**: se **sube** con la medición) sobre la misma entrada y dentro del mismo proceso, bajo el locale del entorno **y** bajo `LC_ALL=C`, Entonces (i) este árbol publica **la misma decisión** —`ARNES_CITA` y `ARNES_CR`— en todas esas evaluaciones y en los **dos** locales (**determinismo e invariancia de locale**: son propiedades, no cifras); (ii) esa decisión es **la misma que la heredada publica bajo `LC_ALL=C`** —el **oráculo**: ahí la eliminación de sufijo de la heredada es byte a byte, y las dos sentencias son equivalentes por construcción, así que ése es el único sitio donde existe un valor «correcto» contra el que medir. Sin (ii), (i) lo cumpliría también una implementación que decidiera **siempre lo mismo**, que es constante y no correcta—; y (iii) el diferencial **registra**, y **no falla por ello**, que la heredada **bajo el locale del entorno** incumple (i) o (ii): ése es el **fail-before** de este criterio. Si el corpus no produce entradas fuera del dominio, este criterio **no tiene sujeto** y emite **SKIP con su motivo** —bash, locale y entradas clasificadas—, **nunca PASS**, con la misma regla que CA-06 y con el reparto FALLA/SKIP que declara CA-01.

  **El oráculo se comprueba antes de usarse, porque un oráculo que no repite no es un oráculo.** Si sobre una entrada la heredada **tampoco publica una decisión única bajo `LC_ALL=C`**, esa entrada **no es clasificable**: el diferencial la **cuenta aparte** y el caso emite **SKIP citando cuántas fueron**, **nunca PASS** —misma regla que CA-06—, en vez de comparar contra un valor que no existe. Medido: **0 de 106** entradas en esa situación (`docs/qa/1.33.0.md`, QA-017-01). Valores de **referencia**, no techos, sobre ese mismo corpus y 4 corridas: la heredada incumple la invariancia de locale en **7 de 106** y el determinismo en **0–2**; este árbol, **0 y 0**.

  **Qué caso lo acredita — contratado aquí porque hasta la vuelta 1 este criterio no tenía ninguno (QA-017-08).** La **misma** pasada de clasificación que CA-01 exige alimenta a los dos criterios —se paga una vez— y de ella salen **no menos de tres casos** en la sección de banco que aloja el corpus único: uno por (i), uno por (ii) y uno por (iii). Cada uno publica en su mensaje **bash, locale del entorno y el recuento de la partición** —dentro / fuera / no clasificables—, porque ése es el dato con el que un SKIP se distingue de un verde vacío; el de (iii) publica además **cuántas entradas incumple la heredada y en cuál de las dos propiedades**, y **no falla por ello**. Un caso que no pueda nombrar su recuento no está midiendo la partición, y entonces no acredita ni (i) ni (ii): emite SKIP con su motivo.

  **Qué queda cerrado con esto, y por qué se declara.** REQ-017 no sólo abarata: en esa clase de entrada **cierra un fallo en abierto de v1.32.1** —la guarda que `AGENTS.md` §13 describe como «un CR que no es el que la termina no deja pasar»— porque la heredada, bajo locale UTF-8, publica `cr=0` sobre una línea que **sí** lleva un CR interior. Se declara igual que se declaró la pared de los 60 s: es un **beneficio no buscado** del arreglo, y un beneficio no declarado es una propiedad que nadie sabe que tiene y que la próxima refactorización retira sin enterarse. Valores de **referencia**, no techos (medidos por QA el 2026-09-07, 3/3 corridas, `docs/qa/1.33.0.md`): sobre la línea `\ \ 0xc3 \ CR ] CR` —y con cualquier prefijo ASCII, incluidos `Estado: ` y `QA: `— la heredada da `cita=0 | cr=0` bajo UTF-8 y ve el CR bajo `LC_ALL=C`; este árbol da `cr=1` en los dos.

  **Lo que este criterio NO afirma, y va escrito para que no se lea de más.** *Primero:* no afirma que el **veredicto de la puerta** cambie en esa clase. Está medido que **no** cambia —`guard-completado` recorre la cabecera **dos veces** (`ARNES_CR_INTERIOR` y `ARNES_ESTADO_CR`) y en v1.32.1 el segundo recorrido lo cazaba—: la divergencia está probada **en la función que CA-01 nombra**, no en la decisión de la puerta, y contratar lo segundo sería afirmar lo que nadie reprodujo. *Segundo:* fuera del dominio **no se contrata el valor del texto** que se publica —`ARNES_LINEA` y `ARNES_CR_LINEA`—, sólo la decisión. El motivo es medido y no es pereza: ese texto se construye con **sustitución y eliminación con patrón** (`${l//$'\r'/\\r}`, `${l%%<!--*}`, `${l#*-->}`), que es **la misma familia de operación** que hace no determinista a `arnes_norm_clave` (QA-017-07), preexistente en los dos árboles. Sobre esa parte este REQ **reduce** la exposición —una operación con patrón menos por línea— y **no** la cierra; su dueño está declarado en `Notas / alcance`.

- **CA-02 — El banco no cambia de veredicto en ninguna parte.** Dado el banco completo, Cuando se corre antes y después del cambio, Entonces el inventario ordenado de **caso → veredicto** es idéntico byte a byte (`tests/escenarios/hooks/inventario.sh`) y el cuadre por archivo cierra: ninguna sección pierde ni gana casos, y ningún caso pasa de PASS a SKIP.

- **CA-03 — La propiedad estructural: el coste no crece más que linealmente con el tamaño de la entrada.** Dado `arnes_sin_cita` medida dentro de **una sola corrida y un solo proceso**, Cuando se la llama k veces sobre una línea de S bytes y k veces sobre una de 2S (S ≥ 70 000; k tal que el **mínimo** de cada serie supere 50 ms), Entonces el cociente entre los **mínimos** de las dos series es de **no más de 2,6** (**operativo**: se **baja** con la medición; **menos** es conforme y **no** es hallazgo). Se contrata el **cociente de duplicación** y no un reloj: un cociente no depende de la velocidad de la máquina, y la carga sólo puede subir **los dos** términos —por eso el estadístico es el mínimo, no la media—. Hoy ese cociente es ≈ **4** (cuadrático: 0,075 → 3,664 s al pasar de 70 000 a 140 000 bytes) y un recorrido lineal da ≈ 2.

- **CA-04 — La regresión concreta, cerrada como razón contra la última versión que no la tenía.** Dado el mismo banco y la misma corrida, Cuando se mide **la boca que lee una línea de cabecera** —`arnes_campo_linea` en este árbol, que es `arnes_sin_cita` **más** `arnes_norm_clave`, contra `arnes_norm_clave` **sola** en el tag **v1.32.0**, la última sin la guarda del CR, donde `arnes_sin_cita` **no existe** porque la noción de cita nace en 1.32.1 (la boca vive, **única**, en `hooks/lib.sh`)— sobre la **misma** línea de 140 000 bytes **sin ningún** CR, Entonces el mínimo de este árbol es de **no más de 2,0×** el de la línea base (**operativo**: se **baja** con la medición; **menos** es conforme).

  **Se comparan bocas, no funciones homónimas — y ésa es la comparación correcta, no sólo la que se pudo hacer.** Lo que este criterio contrata es el coste de **leer una línea de cabecera**, que es el trabajo que de verdad se paga; ese trabajo lo hace en cada versión **la capa entera**, no una función con un nombre concreto, y los nombres se reorganizan entre versiones mientras el trabajo sigue siendo el mismo. Medir `arnes_sin_cita` contra un tag que no la tiene no da un número flojo: **no da ninguno**. Y de las dos lecturas posibles se contrata la **estricta**: capa contra capa da **1,27×**, comparar sólo el escáner daría **0,21×** — se toma la que menos margen deja, para que el techo no se cumpla por haber elegido bien el denominador.

  Valores de **referencia**, no techos: contra esa misma línea base el árbol **enfermo** (v1.32.1) da **7,9×** medido **en Linux**, así que el caso **falla** contra los hooks heredados y **pasa** contra este árbol, con **1,27×**. El **49×** que este criterio dijo hasta el 2026-09-07 está fechado en **otra plataforma** y **no se reproduce** — que es justamente el motivo de que estos criterios se contraten como razones y no como segundos.

- **CA-05 — La ruta crítica del banco, que es lo que de verdad se paga en cada PR: se demuestra sobre ella y se acredita una vez, con su número.** Dado `secciones/32-huecos-auditoria-r001.sh` corrida **aislada** con `ARNES_JOBS=1`, en la misma máquina y la misma corrida contra los hooks de este árbol y contra los del árbol heredado **v1.32.1**, Cuando se implementa este REQ —y cada vez que se vuelva a pedir explícitamente con `ARNES_COSTE_RUTA_CRITICA=1`—, Entonces (i) el mínimo de 3 relojes de este árbol es de **no más de 0,25×** el de la heredada (**operativo**: se **baja** con la medición; **menos** es conforme), (ii) la comparación no se compra dejando de probar **ni dejando de pasar**, y (iii) el número medido y su fecha quedan escritos en el **Historial de cambios** de este REQ.

  **El denominador no hace falta medirlo: basta acotarlo, y la cota queda probada.** La corrida heredada se lanza bajo `timeout` de **4 × mín(este árbol)**, calculado en esa misma corrida y **después** del numerador. Si el `timeout` **vence**, entonces heredada ≥ 4 × este y el cociente es **≤ 0,25×**: la desigualdad que el criterio contrata está **demostrada**, no estimada, y lo que se deja de conocer es el **valor** de la razón, que el criterio no pide. Si la heredada **termina** antes de vencer, el cociente es **> 0,25×** y el caso **FALLA**. El vencimiento del `timeout` es por tanto un resultado **conforme y positivo**, y **no** es un motivo de SKIP de CA-06: CA-06 sigue cubriendo lo que de verdad impide medir —no poder materializar el árbol heredado, no disponer de `timeout`, no alcanzar el mínimo por serie, o no poder **distinguir** un vencimiento de otra terminación anómala—.

  **El vencimiento se ACREDITA, no se supone: la muerte de un proceso no es la expiración de su plazo.** Que el plazo venció lo prueba (a) el estado de salida que **`timeout(1)` reserva** para la expiración —**124**, número **de contrato**: lo fija la interfaz de `timeout`, no este sistema, y por eso no se «ajusta con la medición»— **y** (b) la corroboración por **reloj** (`t1 − t0 ≥ plazo`), que ya está a mano en la misma corrida. Cualquier **otra** terminación anómala de la corrida heredada **no prueba nada** y emite **SKIP con su motivo** (CA-06), **nunca PASS**; ejemplos **no exhaustivos** de esa clase, con la lista real de estados de salida en la página de `timeout(1)` y en el convenio `128+señal` de la shell: **137**, que es lo que devuelve tanto matar al hijo **desde fuera** a los 0,6 s de un plazo de 60 s como matar al propio `timeout`. Va escrito porque el modo de muerte importa aquí más que en otros sitios: la corrida heredada es **el camino cuadrático**, el que más memoria y más `fork` gasta, así que en un runner apretado el **OOM killer** es su final más probable — y leer ese 137 como demostración produce un **verde falso etiquetado como prueba**, que es peor que un rojo.

  **Y la misma pregunta se le hace al denominador que a (ii) se le hace al numerador: «¿esta corrida midió algo?» — un plazo agotado por una corrida que no arrancó no acota nada.** Además de (a) y (b), el vencimiento sólo acredita la cota si (c) la corrida heredada **dejó escritos no menos de 1 caso mientras corría** (el **1** es número **de contrato**: es la definición de «esta corrida midió», no una magnitud que se ajuste con la medición; **más** es conforme). El recuento se toma de **lo que la corrida va dejando en disco a medida que avanza** —la sonda le fija la raíz de ese trabajo **antes** de lanzarla, y por eso puede contarlo sin que la corrida termine— y, si el corredor llegó a publicarla, de su salida recogida; las fuentes concretas viven, **únicas**, en la sonda de la sección de banco declarada en `Archivos:` (ejemplos **no exhaustivos**: el trabajo en disco, la salida recogida), porque lo que este criterio contrata es el **recuento**, no dónde se lee. Si venció **sin haber dejado ninguno**, el caso emite **SKIP con su motivo** (CA-06), **nunca PASS**. No es hipotético y tiene precedente **en esta misma ventana**: el árbol heredado extraído sin bit de ejecución hacía que el corredor hijo no arrancara y la corrida saliera «sin casos»; entonces **terminaba** y el código la mandaba a SKIP, pero si en vez de terminar se **bloquea** —un hook esperando en `stdin`, un cerrojo, un `read` sin `</dev/null`— el plazo vence, `timeout` devuelve 124, el reloj corrobora y una corrida que **no midió nada** se publica como **demostración** (`docs/qa/1.33.0.md`, QA-017-09). Es la familia de QA-017-03 un lado más allá: **una corrida que no midió no es una corrida lenta**, igual que una corrida rota no es una corrida rápida.

  **Por qué la señal es el trabajo escrito y no «una salida que no está vacía», que es lo que este criterio pidió hasta el 2026-09-07 — y es exactamente la clase que `ADR-004` acaba de diagnosticar en CA-01: una comprobación correcta sobre la señal equivocada.** El corredor del banco **no imprime un solo byte hasta que todas sus secciones terminan** —junta los parciales al final—, de modo que una corrida heredada **matada por `timeout` deja la salida vacía SIEMPRE**, haya trabajado 36 s o no haya arrancado nunca. Sobre esa señal la guarda no distingue las dos cosas que existe para distinguir: manda las dos a SKIP y convierte el **único PASS** de CA-05 (i) en **SKIP permanente** (verificado encendiendo `ARNES_COSTE_RUTA_CRITICA=1`). No era una guarda estricta: era **insatisfacible** — la misma forma que la igualdad byte a byte de CA-01 antes de `ADR-004` y que la línea base de CA-04 contra una función que no existía en el tag. **La propiedad contratada no cambia** —un plazo agotado por una corrida que no arrancó no acota nada—; lo que cambia es **cómo se observa**. Valores de **referencia**, no techos (medidos al implementar, 2026-09-07): la heredada matada a los 12 s deja **37** casos escritos, y una que no arranca —árbol sin bit de ejecución, hook esperando en `stdin`— deja **0**.

  **El acoplamiento que esta señal añade se declara, y falla del lado cerrado — pero EN SILENCIO, contra lo que este párrafo dijo hasta el 2026-09-07.** Contar el trabajo escrito ata esta sonda a **cómo el corredor dispone en disco el trabajo en curso**, dependencia que antes no tenía. Si esa disposición cambia, lo **esperado** es que el recuento caiga a **0** y el caso emita **SKIP con su motivo** y **nunca PASS** —la dirección cerrada que este criterio contrata para todo lo que no puede medir—; y eso, con la palanca encendida, **no está medido**: cerrarlo es del residual de `Notas / alcance`. Lo que **sí** está medido (QA, vuelta 2, 2026-09-07, `docs/qa/1.33.0.md`, QA-017-12) es que **en el modo por defecto la rotura no se nota**: con la disposición rota, los **veredictos** del banco son **idénticos** a los del árbol sano y la corrida sale **rc 0**; las **dos únicas líneas** que difieren son las cifras que CA-08 publica, que cambian en **toda** corrida y por tanto no señalan nada. Y no puede ser de otro modo, porque se sigue de este mismo criterio: **sin `ARNES_COSTE_RUTA_CRITICA=1` el caso de (i) ya es SKIP por «no se pide»**, así que la guarda (c) **no se ejercita** y **no hay ningún PASS de (i) que pueda desaparecer**. La rotura sólo se manifiesta **con la palanca encendida**, es decir el día en que alguien vuelva a acreditar el fail-before/pass-after — que por diseño de este criterio es un acto **a demanda y ocasional**, no una corrida de cada PR. Este párrafo afirmaba que el fallo era **«ruidoso — el único PASS de (i) desaparece del banco en la primera corrida»**: era **falso**, y lo era por la misma razón que las otras dos veces en este REQ —nadie lo ejecutó— (la clase, con sus tres casos, en `Notas / alcance`). Dueño, forzador y vencimiento del acoplamiento, en `Notas / alcance`, donde el **forzador se rehace** justo por esto: un modo de fallo silencioso no dispara al que pasa al lado.

  **Qué comprueba (ii), y por qué no compara inventarios contra el tag.** Con el denominador acotado, la corrida heredada no termina y su inventario completo no existe. La igualdad de inventario **contra el árbol heredado** la contrata **CA-02**, sobre el banco **entero** —superconjunto de la sección 32, con el mismo comparador `inventario.sh`—, así que aquí no se duplica; y **no** se sustituye por un inventario **registrado** del tag: un inventario congelado del banco de otra versión es una copia de la realidad sobre un banco que crece a propósito, condenada a divergir y a re-fijarse a mano desde aquello que dice validar (la clase de `ADR-002` y `SEC-025` en su forma más pura). Lo que (ii) comprueba es **auto-anclado**: las 3 corridas cronometradas de este árbol **produjeron casos**, **terminaron en verde** —el estado de salida del corredor interno se **comprueba**, no se descarta: **rc 0** y **no más de 0 casos FAIL**, número **de contrato** porque es la definición de corrida válida y no una magnitud que se ajuste con la medición— y dan el **mismo** inventario ordenado de caso → veredicto **entre sí**.

  **Y por qué las tres condiciones y no una: cada una tapa un modo de mentir de esta puerta.** Una corrida **sin casos** no es una corrida rápida, es una que no midió. Una corrida **roja** no es una corrida rápida, es una que midió **otra cosa** —y no lo es en abstracto: está demostrado que con `ARNES_HOOKS_DIR` apuntando a un `guard-completado` que **siempre permite**, la sección 32 sale en **14 FAIL y rc 1** en 3,79 s, el plazo derivado cae a 16 s, la heredada vence y las **dos** mitades dan **PASS**, incluida la que se llama «la comparación no se compra dejando de probar» (`docs/qa/1.33.0.md`, QA-017-03)—. Y tres corridas **igualmente rotas coinciden entre sí**, así que el ancla de (ii) no detecta nada por sí sola: el rc es lo que la sostiene. Un numerador roto abarata el numerador, y el numerador es lo que fija el plazo de la heredada: **una puerta cuyo umbral lo calcula la propia corrida que se está midiendo se compra rompiéndola**.

  **Es evidencia de fail-before/pass-after, no una puerta de cada PR** —y va escrito porque el coste de leerlo al revés está medido: correrla en cada corrida deja el banco en **144,76 s**, es decir la puerta requerida de `main` más lenta (145 s) que la regresión que este REQ arregla (92 s), y de forma permanente, contra la Historia de este mismo REQ. La línea base es un **tag congelado**: acreditada la razón, deja de medir la evolución de este árbol y envejece hacia el lado que abre. La vigilancia **permanente** del coste la contrata **CA-03**, que es una propiedad estructural, no envejece y cuesta milisegundos. Por eso, sin `ARNES_COSTE_RUTA_CRITICA=1` los casos de esta comparación emiten **SKIP citando que su evidencia está acreditada en el Historial, con su número y su fecha**, y **nunca PASS**.

  Hoy el cociente es **1,0** (sin arreglo); la medición de referencia lo sitúa en **0,125×** (9,60 s frente a 76,19 s).

- **CA-06 — Una sonda que no puede medir dice SKIP con su motivo, nunca PASS, y no sobrevive a su comisión.** Dado que la máquina puede estar cargada, el reloj no tener resolución suficiente o el árbol heredado no estar disponible, Cuando la sonda de CA-03, CA-04 o CA-05 no alcanza el mínimo por serie, no encuentra la línea base, o **no puede acreditar** que el plazo de CA-05 **venció** —terminación anómala de la heredada que no es la expiración: el estado de salida no es el que `timeout(1)` reserva para ella o el reloj no corrobora el plazo—, Entonces el caso emite **SKIP citando el motivo y el número que sí obtuvo** y **nunca PASS**; y al cerrar la sección el corredor comprueba que la sonda **no deja procesos vivos** —en esta ventana una sonda sobrevivió **3 h 41 min** a su comisión y falseó la línea base de otra medición, que concluyó «dentro del ruido» con toda lógica—.

- **CA-07 — La cuarta forma prohibida, escrita donde vive la regla y en lo que los proyectos heredan.** Dado `requirements/README.md` § «Cómo se escribe un criterio que no se desmiente» y su plantilla `templates/requirements-README.md.tpl`, Cuando un analista redacta un criterio de **coste**, Entonces encuentra nombrada la forma **«fijar la magnitud equivocada»** con su caso medido (CA-08 de REQ-016 midió **procesos** —4 = 4, medido correctamente— y dio verde sobre una regresión de **10×** de reloj en la ruta crítica del banco), la regla enunciada por propiedad —un criterio de coste **declara qué magnitud mide y por qué es esa la que se degrada**, y se escribe como **razón o propiedad estructural**, nunca como reloj absoluto, porque un reloj absoluto lo falsea la máquina y la carga— y la línea correspondiente en la **Definition of Ready**; y la plantilla heredable lleva la misma sección, con el mismo alcance temporal que las tres formas ya adoptadas.

- **CA-08 — Las dos magnitudes juntas, porque ninguna sola habría visto esto.** Dado el camino de lectura de una cabecera normal —un REQ real de 6 líneas y una cabecera de 200 líneas—, Cuando se compara este árbol con el heredado v1.32.1 en la misma corrida, Entonces (i) **no más de 0 procesos añadidos** por llamada respecto a la línea base —la magnitud que ya contrataba CA-08 de REQ-016; **menos** es conforme— y (ii) el mínimo de reloj es de **no más de 1,25×** el de la heredada (**operativo**: se **baja** con la medición). Las dos en el mismo criterio y comprobadas en la misma corrida: el arreglo no vale si compra tiempo con un `fork`.

  **El PROCEDIMIENTO de medida es parte del criterio: las series de los dos árboles se INTERCALAN, no se miden en bloque.** Se toman **no menos de 6** series por árbol (**operativo**: se **sube** con la medición) **alternando árbol a árbol** —a, b, a, b, …— dentro de la misma corrida, y el estadístico sigue siendo el **mínimo**. Medido el 2026-09-07 (`docs/qa/1.33.0.md`, QA-017-06): **en bloque**, las seis razones dan 1,081 · 1,158 · **1,217** · 1,204 · 1,032 · 0,984 —el máximo queda a un **2,6 %** del techo de 1,25×—; **intercaladas**, el mínimo de seis da **1,012×**. La varianza es del **procedimiento**, no del arreglo. No es cosmética: (ii) corre en la **puerta requerida de `main`**, así que un runner cargado la pondría en **rojo sin que exista regresión**, y ése es el modo de fallo que termina con alguien **subiendo el techo** para que deje de molestar — es decir, apagando la única mitad que ve el reloj. El techo **no se toca**: es `operativo` y su dirección admitida es **bajar**; lo que se corrige es la **sonda**.

  **Y la sonda declara su RESOLUCIÓN antes de juzgar: si no la alcanza, se abstiene — un rojo tiene que significar regresión.** Medido en la vuelta 1, con el procedimiento de arriba **todavía sin implementar** (3 series por árbol, en bloque): 26 medidas, **2 en rojo** —**1,252×** y **1,443×** contra el techo de 1,250×— y **1 de cada 4** vueltas del banco completo en el modo de la puerta requerida (`JOBS=6`) salió roja, con `load` **0,91** al arrancar, así que la carga previa no lo explica (`docs/qa/1.33.0.md`, QA-017-06). Que la causa siga siendo el instrumento y no el arreglo lo dice la misma serie: **en aislamiento la razón da 0,821–1,010**, y un coste real no puede ser negativo, de modo que el recorrido **0,821–1,443** sobre el **mismo** estimando es varianza de la sonda; bajo `JOBS=6` sube de forma sistemática porque los dos árboles se miden **en bloque** y ven vecinos distintos —contención que el propio banco fabrica y que la intercalación cancela por construcción—. Por eso, además de intercalar, en la **misma** corrida y con las **mismas** series se comprueba que el mínimo **convergió**: se compara el **segundo mínimo** de cada árbol con su **mínimo**, y si esa razón supera **el propio techo** en cualquiera de los dos árboles —**no es un número nuevo**: es el mismo, porque un instrumento tiene que resolver al menos el factor que vigila—, la sonda **no puede distinguir una regresión de su propio ruido** y emite **SKIP citando las dos razones de convergencia y la razón que sí obtuvo**, **nunca PASS y nunca FAIL** (CA-06). La abstención no tapa una regresión real: una regresión sube los dos mínimos del árbol nuevo por igual y **no** separa su serie de sí misma; lo que separa una serie de sí misma es el vecino. Esa comprobación es **necesaria y NO suficiente**, y desde el 2026-09-08 está medido por qué: responde «¿se asentó **cada serie**?», no «¿puede **este cociente** distinguir el factor que vigila?». La segunda la contrata el párrafo siguiente; la primera se mantiene tal cual —mismo umbral, mismo SKIP—.

  **La resolución se comprueba sobre la RAZÓN —la magnitud que (ii) juzga—, y el veredicto sólo se emite si NO depende del ruido.** Dentro de la misma corrida, el par intercalado de arriba se repite **no menos de k veces** (**operativo**: k **se sube** con la medición, y su valor **se deriva midiendo** —`REQ-012 CA-03`—, no se elige), y cada repetición produce **su** razón. El caso **publica las k razones, su recorrido `máx(r)/mín(r)` y el techo**, y decide así: **PASS** si `máx(r) ≤ techo` —conforme en **todas** las repeticiones—; **FAIL** si `mín(r) > techo` —excedido en **todas**, así que no lo puso ahí el vecino—; y **en cualquier otro caso, cuando el techo cae DENTRO del recorrido observado, SKIP** citando esas tres cosas (CA-06), **nunca PASS y nunca FAIL**. La unanimidad de las k repeticiones es **de contrato**: es la definición de «la decisión no depende del ruido», y no admite mayoría, promedio ni «la mejor de k» (`requirements/README.md` § «Cómo se escribe un criterio que no se desmiente»: el mínimo de k, nunca la media). Lo contratado es la **propiedad** —**un instrumento que no puede distinguir el factor que vigila se abstiene**—, no un número: es el patrón hermano del párrafo anterior («la sonda que no converge se **ABSTIENE**») aplicado a la magnitud correcta.

  **Por qué la razón y no los brazos, con el dato que lo obliga.** Medido sobre **cinco** corridas de la puerta requerida `hooks-en-linux` sobre **código idéntico** —ningún commit tocó `hooks/`, `tools/` ni `.github/` entre ellas, comprobado de forma independiente en la revisión `R-019` del `auditor-seguridad`; tabla y commits en `docs/PENDIENTES.md` § «PRIMER TRABAJO DE 1.34.0»—: la razón recorre **0,973–1,364** (factor **1,40**) mientras el techo que vigila es **1,25**, es decir que **el techo vive dentro del ruido del instrumento**. El dato que cierra la discusión es el **0,973×**: el árbol nuevo salió **más rápido** que la línea base, y una regresión real no puede ser más rápida. Y todo esto **con** las series intercaladas, ≥ 6 por árbol y la convergencia por brazo ya implementadas. Los **dos** rojos son justo aquellos en que **un** brazo converge al **borde** (**1,232×** y **1,249×** contra 1,250×) mientras el otro converge holgado (1,025× y 1,002×); los tres verdes tienen convergencias equilibradas. A 1,249× el instrumento resuelve **exactamente** el factor que vigila y ni un poco mejor, y sobre esa resolución afirmó un **1,364×**. Dos series pueden converger **cada una** dentro del techo y su **cociente** oscilar por encima: la dispersión de la razón procede de las condiciones **entre** brazos, no de la dispersión interna de cada brazo, y ninguna cota sobre los brazos la acota.

  **La clase que produjo esto, nombrada.** «Al menos el factor que vigila» es un argumento **correcto**, y de todos los valores compatibles con él se tomó **el más flojo, sin medir si alcanzaba**: un **criterio derivado sin comprobar su factibilidad**, la misma clase que el techo de 400 líneas de `REQ-014 CA-18` y que el 4× de `REQ-021 CA-08 (iii)` re-derivado a 6×, los dos corregidos en la ventana anterior. Por eso este criterio contrata la **propiedad** y manda **derivar midiendo** k y el techo de coste de abajo, en vez de fijarlos aquí.

  **El par discriminante, sin el cual la guarda no acredita nada.** La guarda se entrega con **dos casos**, que publican su motivo:
  - **(a) negativo — la guarda es la que decide.** Con la dispersión de la razón ensanchada por encima del techo **sin que exista regresión** —el árbol medido **contra sí mismo** (razón verdadera **1,0**) bajo la contención que el propio banco fabrica (`JOBS=6`), o un forzador equivalente que afecte sólo a parte de las repeticiones—, el caso emite **SKIP** con su motivo; y con la guarda **desactivada**, esa misma entrada produce **PASS o FAIL según la corrida**. Hacen falta las dos mitades: sin la segunda, un SKIP no demuestra que lo haya causado la guarda.
  - **(b) positivo — la abstención no se traga una regresión.** Con una regresión **sintética** que multiplica el reloj del árbol nuevo por **no menos de 2×** (**operativo**: se **baja** con la medición, siempre estrictamente por encima del techo), el caso emite **FAIL** y no SKIP; y con los dos árboles sin regresión y sin el forzador, **PASS**.

  Se exige **ejecutarlo**, y no argumentarlo, por un motivo medido dentro de este mismo REQ: «la abstención no tapa una regresión real» es **una afirmación sobre cómo se comporta el mecanismo**, y este REQ lleva **tres** de esa clase que resultaron falsas al ejecutarlas (Historial, 2026-09-07: CA-04 apuntando a una función que **no existe** en el tag; la guarda (c) de CA-05 sobre una señal **constante-cero**; y el modo de fallo «ruidoso» medido **silencioso**). Una guarda de abstención sin par discriminante sería la cuarta.

  **Y la guarda paga su propio coste, porque (ii) corre en la puerta requerida de `main`.** El tiempo de pared que este caso **añade** a la puerta requerida queda bajo un **techo declarado** (**operativo**: se **baja** con la medición; se escribe como **techo** y nunca como igualdad, y se deriva en la misma corrida en que se derive k). Si el k que hace resoluble la razón **no cabe** bajo ese techo, la salida **no** es subir el techo de regresión, ni relajar la unanimidad, ni `continue-on-error`: es llevar (ii) fuera de la puerta requerida como **acreditación fechada y re-ejecutable a demanda** —la vía que **CA-05 (i)** y **(ii)** ya usan en este mismo REQ con `ARNES_COSTE_RUTA_CRITICA=1`, que no apaga la señal, sólo deja de decidir cada PR con ella—, y esa es **decisión del propietario** (`AGENTS.md` §6) con entrada en `PENDING_APPROVAL.md`, no del `desarrollador` ni del `analista-requerimientos`. Precedente medido, y por eso escrito aquí: **CA-05** implementado tal como se contrató dejó la puerta requerida **más lenta (145 s) que la regresión que este REQ arregla (92 s)** (Historial, 2026-09-07).

  **El techo no se mueve en esta ventana, y si hubiera que moverlo no lo movería este REQ.** La lectura contratada es que la varianza es del **procedimiento** —evidencia arriba—, así que el trabajo es de la **sonda** y el ≤ 1,25× queda **intacto**. La lectura alternativa —que el arreglo cueste de verdad cerca del techo y 1,25× estuviera mal fijado— **no se descarta a ciegas: se deja con su condición de disparo**. Si con las series intercaladas, ≥ 6 por árbol y la comprobación de convergencia implementadas siguiera saliendo en rojo sin regresión, entonces el número está mal fijado y **subirlo es decisión del propietario con ADR** (`AGENTS.md` §6), no del `desarrollador` ni del `analista-requerimientos`, y **nunca** un ajuste dentro de la comisión que lo encuentra: un techo que sube dentro de la misma comisión que lo incumple no es un techo. Y la razón de que no se suba «por si acaso» está medida: el rango del instrumento hoy es 0,82–1,44 sobre un estimando que en aislamiento vale ≈ 1,0, de modo que un techo puesto por encima de ese ruido dejaría de ver la clase de regresión —**10×** de reloj— para la que este criterio existe.

  **La condición de disparo de arriba SE CUMPLIÓ el 2026-09-08 — y su consecuencia escrita era la equivocada.** Con las series intercaladas, ≥ 6 por árbol y la convergencia por brazo implementadas, (ii) salió en rojo **dos de cinco** corridas sin regresión: es literalmente el antecedente. Pero el consecuente —«el número está mal fijado y subirlo es decisión del propietario con ADR»— **no se aplica, y lo dice la misma medición**: el **0,973×** sitúa al árbol nuevo **por debajo** de la línea base, y un techo se sube cuando el coste real está cerca de él, no cuando el instrumento **no sabe dónde está** el coste real. El techo **sigue intacto** (≤ 1,25×, `operativo`, dirección admitida **bajar**) y el trabajo sigue siendo de la **sonda**, ahora sobre la magnitud correcta. La lectura alternativa **no se cierra**: se queda con una condición de disparo **nueva** — si con la resolución sobre la razón ya contratada el caso emitiera **FAIL unánime** (`mín(r) > techo`) sobre un árbol sin regresión, entonces sí el número está mal fijado, y **subirlo es decisión del propietario con ADR**, nunca un ajuste dentro de la comisión que lo incumple.

- **CA-09 — El umbral de los 60 s se mide y se escribe; moverlo no es alcance de este REQ.** Dado que un hook `PreToolUse` muere a los 60 s y **un hook muerto no deniega** (`AGENTS.md` §7), Cuando se cierra este REQ, Entonces queda **medido y anotado en el Historial de cambios** el tamaño de documento en que este árbol alcanza los 60 s —misma sonda que la vuelta 3 de 1.32.1: un `Write` cuya cabecera es **una sola línea** de longitud creciente, **restando el coste fijo de arranque**— junto al de la heredada v1.32.1 y al de v1.32.0, **cada cifra con la corrida de la que sale y con su DISPERSIÓN** —número de corridas (**no menos de 6** por árbol; **operativo**: se **sube** con la medición) y **rango observado**—, y **ninguna magnitud de pared se acredita como valor**. Lo único que este REQ contrata sobre la pared es la **dirección**, y en la forma que este REQ contrata todo lo demás: **en cada corrida**, el tamaño medido para este árbol es **no menor** que el de v1.32.1 medido **en esa misma corrida**. El **valor** de la pared es de **SEC-030**, preexistente en los dos árboles, con dueño y ventana propios, y **no** se cierra aquí.

  **Qué comprueba el caso del banco, y qué no — porque las dos exigencias de este criterio tienen sedes distintas.** La **medición con procedencia y dispersión** (no menos de 6 corridas por árbol y rango) es lo que se anota en el **Historial de cambios**, y se hace una vez: **no** es trabajo del banco, y pedirle seis sondas de pared por corrida multiplicaría por seis la única medición cara de este REQ. Lo que el **caso del banco** comprueba es **la dirección**, y es una comparación pareada dentro de **su propia** corrida: el tamaño medido para este árbol **no menor** que el de v1.32.1 medido al lado. **Publicar la magnitud no es comprobar la dirección**: un caso que da PASS porque el número que imprimió tiene la forma esperada seguiría dando PASS con este árbol **por debajo** de la heredada, que es exactamente lo contrario de lo único que este criterio contrata (`docs/qa/1.33.0.md`, QA-017-10). Si en esa corrida alguna de las dos cifras no sale —la sonda no converge, la heredada no se puede materializar—, el caso emite **SKIP con su motivo** y **nunca PASS** (CA-06).

  **Lo medido, con su corrida y su dispersión — y por qué aquí ya NO se acredita ninguna magnitud.** La cifra «**≈ 1,60 MB**» que este criterio acreditó hasta el 2026-09-07 **se retira**: la sonda **no repite**. Seis corridas de este árbol el 2026-09-07 (`docs/qa/1.33.0.md`, QA-017-05) dan **1,08 · 1,32 · 1,78 · 2,64 · 2,65 · 3,98 MB**, con orden medido entre **1,41 y 2,33**: un factor **~3,7** entre extremos. La heredada v1.32.1 sí es estable en esas mismas corridas (**1,01–1,19 MB**), y la **dirección** se sostiene **6 de 6**. Y las dos series que este criterio declaraba **divergentes** —2,01 frente a 1,46 de orden— **no discrepan: son dos extracciones de la misma distribución**, y la serie de seis las contiene a las dos; la «divergencia abierta» que este párrafo declaraba **no existía**. De **v1.32.0** sigue sin salir cifra: no resultó medible en ese rango en la corrida de Linux del 2026-09-07 (orden ~1), y el «≈ 1,56 MB» que este criterio dijo hasta esa fecha está fechado en otra plataforma y queda **retirado**.

  **Causa medida, porque es la misma que esta ventana persigue.** Los tres tiempos base de la sonda son **una sola muestra cada uno** —contra la regla del **mínimo de k** que enuncia `requirements/README.md` §«Cómo se escribe un criterio que no se desmiente», dos pantallas más arriba de donde se escribió—, dos de ellos entran como **diferencia de muestras únicas**, y el orden resultante va **en el exponente** de la extrapolación, de modo que un 1,41 frente a un 2,01 mueve la pared un factor ~2,5; además el arranque que se resta osciló **0,10–0,21 s** según la corrida fuera aislada o dentro del banco. Por eso este criterio pide dispersión y **prohíbe** la magnitud: una cifra de pared sin rango no se puede auditar, y **se publicó como medida en el CHANGELOG y en `docs/ESTADO.md`** antes de que nadie pudiera. Arreglar la sonda es de **SEC-030**, que es de quien es la pared: **no se cierra aquí y no bloquea**.

## Notas / alcance

**El defecto, ya diagnosticado — este REQ lo contrata, no lo investiga.** `hooks/lib.sh:1643` abre el
escáner con `case "${l%$'\r'}" in *$'\r'*)`. La eliminación de sufijo **con patrón** la resuelve bash
probando **cada posición**, de donde el orden cuadrático. Medido en la vuelta 3 de 1.32.1, 10 llamadas
sobre una línea de 140 000 bytes:

```
  ${l%$CR} sola                               3,8553 s   <- el culpable
  case "$l" in *$CR*   (glob de 1 carácter)   0,0263 s   <- 150 veces más barato
  case *'<!--'*        (glob de 4, ya existía) 0,0309 s
```

La forma exacta del arreglo la decide el `desarrollador`. Lo que **no** puede hacer, y va escrito porque
sería una deriva contra REQ-016: **la guarda no se mueve a ninguna de las cuatro bocas** —el lector de
línea, `arnes_jq_str`, la reconstrucción del documento del `Edit` con el CR en disco y
`tools/arnes-paralelo.sh`—. Sigue siendo la **primera sentencia del único escáner**; una guarda en una
boca deja las otras tres abiertas. Lo que se abarata es **cuándo se paga** la sentencia cara, no dónde
vive la comprobación.

**Lo que esto NO es, medido, para que nadie lo lea como una regresión de usuario.** Una llamada normal
no se resiente: REQ real de 6 líneas **0,1195 → 0,1188 s**; cabecera de 200 líneas **0,1487 → 0,1531 s**
(con 200 líneas el árbol de hoy es incluso más rápido que el de la vuelta 2). El régimen cuadrático no
se toca hasta **decenas de miles de bytes en una sola línea**, y una línea de cabecera de un REQ es una
declaración de campo. Lo que se paga hoy lo paga el **banco**, en cada PR: **39 s → 92 s**, con
`32-huecos-auditoria-r001` sola en **75,7 s** de esos 92 y siendo la **ruta crítica** de un corredor que
ya paraleliza secciones.

**Por qué los criterios de coste de aquí son razones y propiedades, y no segundos.** Un umbral de reloj
en segundos depende de la máquina, del runner del CI y del ruido; esta misma ventana midió lo que pasa
cuando una sonda desbocada envenena el reloj de otro. Un cociente de duplicación (CA-03) responde a la
pregunta que de verdad se degradó —**el orden de crecimiento**— y no cambia si la máquina es el doble de
rápida; una razón contra una línea base medida **en la misma corrida** (CA-04, CA-05, CA-08) cancela la
máquina por construcción. Es el reverso de la lección de CA-08 de REQ-016, de `ADR-002` y de `SEC-025`:
**una comprobación que fija la magnitud equivocada envejece hacia el lado que abre**.

**Por qué `requirements/README.md` aparece en `Archivos:` aunque también sea el índice.** El campo
excluye los artefactos de gobierno que **toda** comisión toca sin excepción y **en forma de apéndice**
(este REQ, el índice, `docs/ESTADO.md`, `CHANGELOG.md`). Aquí el archivo se toca como **producto**:
CA-07 le inserta una sección entera de metodología, no una fila de tabla. Declararlo falla del lado
cerrado, que es la dirección correcta para un campo cuyo modo de fallo es trabajo perdido.

**Y por eso el registro de la ventana `docs/qa/1.33.0.md` SÍ se declara, contra lo que decía este
párrafo hasta el 2026-09-07.** No es universal —es **por ventana**— y no es un apéndice: es donde el
`desarrollador` escribe la evidencia medida que CA-04, CA-05 y CA-09 acreditan, así que dos comisiones
que escriban ahí a la vez se pisan de verdad. Y hay un motivo que no es de gusto: la intersección se
calcula **sobre lo declarado**, de modo que un archivo que unos declaran y otros no sale `disjunto` en
**falso** —fail-open, exactamente lo que este campo existe para evitar—. REQ-007, REQ-008 y REQ-011,
abiertos y de esta misma ventana, ya lo declaran.

**`.github/workflows/banco.yml` y `docs/PENDIENTES.md` entran por el cierre de H-08, que era producto y
no gobierno.** El `fetch-depth: 0` del checkout es un cambio en la **puerta requerida de `main`**
—aprobado por gate humano, `AGENTS.md` §6— y `docs/PENDIENTES.md` es donde vive el hallazgo con su
dueño; ninguno de los dos se toca en toda comisión. La consecuencia operativa se dice aquí porque
condiciona el reparto: `tools/arnes-paralelo.sh` expande un directorio a todo lo que cuelga de él, así
que **cualquier REQ que declare `.github/`, `.github/workflows/` o el propio `banco.yml` colisiona
ahora con éste** — REQ-014 lo hace por partida doble, y el canal de informes (aún sin REQ escrito) sólo
sale disjunto si declara sus rutas de `.github/` una por una en vez del directorio.

**Residuales declarados de la implementación, con dueño, forzador y vencimiento — ninguno bloquea el
cierre y ninguno se arregla aquí.**
- **El acoplamiento de la guarda (c) de CA-05 a la disposición del trabajo del corredor.** Contar los
  casos que la corrida heredada deja escritos exige saber **dónde** los deja; la sonda lo fuerza
  fijando la raíz de ese trabajo (`TMPDIR`) antes de lanzarla, pero **la forma de ese árbol es del
  corredor, no de la sonda**. *Modo de fallo (corregido el 2026-09-07 — este residual declaraba
  «cerrado y ruidoso» y la mitad era falsa):* **cerrado y SILENCIOSO en el modo por defecto**. Medido
  por QA en la vuelta 2 (`docs/qa/1.33.0.md`, **QA-017-12**): con la disposición rota, los
  **veredictos** del banco son **idénticos** a los del árbol sano y la corrida sale **rc 0**; las dos
  únicas líneas que difieren son las cifras de CA-08, que cambian en toda corrida. **No hay ningún
  PASS que perder**, porque sin `ARNES_COSTE_RUTA_CRITICA=1` el caso de CA-05 (i) **ya es SKIP por
  «no se pide»** y la guarda (c) ni siquiera se ejercita. Con la palanca **encendida** lo esperado
  sigue siendo la dirección cerrada —recuento 0 → SKIP con motivo, nunca PASS—, y eso **no está
  medido**: es la primera cosa que el forzador tiene que comprobar. *Dueño:* `desarrollador`.
  *Forzador (REHECHO el 2026-09-07, porque el anterior no dispara):* sigue siendo el primer cambio en
  esa disposición —**REQ-014** (el banco en archivos por sección) y **REQ-021** (las sondas a
  `tests/util/`) son los dos REQ abiertos que la tocan—, pero ya **no se observa solo** y por eso deja
  de estar escrito como expectativa y pasa a estar escrito como **obligación**: la comisión que cambie
  la disposición corre CA-05 **una vez con `ARNES_COSTE_RUTA_CRITICA=1`** y comprueba el **recuento**,
  porque el modo por defecto —el único que esa comisión va a correr— **le va a dar verde con la sonda
  rota**. Coste medido de esa corrida única: la sección 37/2 pasa de **12,5 s apagada** a **76,1 s
  encendida** (fila de Historial del 2026-09-07), y fuera de la puerta requerida de `main`.
  *Vencimiento:* el cierre del primero de esos dos REQ que entre, **con esa corrida encendida escrita
  en su evidencia**; sin ella el residual **no vence**: sólo cambia de dueño sin que nadie lo haya
  mirado. Se escribe porque **es una dependencia nueva** —la sonda no la tenía antes de la vuelta 2, y
  una dependencia no declarada es lo que la refactorización siguiente rompe sin enterarse— y porque
  **un residual silencioso cuyo forzador se observa solo no vence nunca**: es la misma forma que
  `SEC-036` obligó a corregir en REQ-021 en esta misma ventana («un residual cuyo disparador es el
  daño que debía evitar no vence nunca»). *Lo que este REQ no puede hacer, y se dice:* la obligación
  vive **aquí**, y escribirla dentro de REQ-014 y REQ-021 es de sus comisiones o de la coordinadora al
  despacharlas: ninguna comisión de este REQ ha escrito criterios en otro requerimiento, y un residual
  cuya única sede es el REQ que lo hereda **se lee tarde**.
- **La resolución del caso de dirección de CA-09 en un régimen que este banco no ejercita.** Con **dos
  árboles idénticos** —razón verdadera **1,0**— la sonda de la pared decide por **ruido**: tres
  corridas dieron **SKIP, FAIL y PASS** sobre el mismo estimando (medido el 2026-09-07 al implementar
  el caso). *Por qué no muerde hoy, y por qué no se arregla aquí:* el caso compara este árbol contra el
  **tag congelado v1.32.1**, que no es idéntico a éste, y en ese régimen da **9 de 9 PASS con márgenes
  de 1,154× a 2,523×** —medidos por QA el 2026-09-07; el `desarrollador` los había declarado
  **1,25–3,40×** al implementar, y **el suelo real está por debajo del declarado**—. La corrección se
  escribe aquí, y no sólo en el Historial, porque la lección es la cifra: **un colchón declarado de más
  es cómo un residual aceptado se vuelve un rojo sorpresa**. Con el suelo medido el margen se sostiene
  igual **9 de 9**, pero **ya no** «muy por encima» de la dispersión de esta sonda, que en el régimen
  de dos árboles idénticos llega a producir un **FAIL** sobre una razón verdadera de 1,0: la distancia
  real al 1,0 es de **~0,15×**, no de **~0,25×**. No cambia el dueño ni el vencimiento —la sonda y su
  dispersión siguen siendo de **SEC-030**—; cambia **cuánto margen queda**. Subir a 3 pasadas cuesta
  **+4,4 s** en la
  **puerta requerida de `main`** y **no cierra la clase**, porque la dispersión de esa sonda es de
  **SEC-030** —dueña de la pared y de la sonda que la estima, ya declarada abajo—, no de este REQ.
  *Dueño:* **SEC-030**. *Forzador:* que la razón verdadera se acerque a 1,0, es decir, que el árbol de
  comparación deje de ser un tag con la regresión dentro. *Vencimiento:* la ventana declarada de
  SEC-030. Se escribe porque **un límite de resolución no declarado se descubre en la corrida en que
  falla**, no antes — que es la misma lección que CA-08 (ii) ya pagó en esta ventana.
- **`CA-01` puede dar PASS sobre un dominio VACÍO o colapsado** (**QA-017-11**, `instrumento`, vuelta
  2). El criterio guarda con cuidado las dos formas de quedarse sin entradas **de fuera** —(a) el
  corpus perdió la clase → **FALLA**; (b) la máquina no tiene el defecto → **SKIP**— y guarda las **no
  clasificables** (≠ 0 → SKIP), pero **no guarda el caso simétrico**: `dentro = 0`. Medido por QA el
  2026-09-07: con la evaluación de la heredada truncada, el dominio cae de **312 a 10** entradas y el
  caso **sigue en verde**. *Con su tensión declarada, porque es del propio criterio:* CA-01 argumenta
  en su texto que «un criterio que no puede fallar no es una puerta», y ésta es la mitad de esa
  propiedad que su caso todavía no comprueba — un universo encogido en silencio, que es una de las
  formas de estar verde sin haber medido que **REQ-020** va a contratar. *Por qué no muerde hoy:* el
  dominio real es de **97–99 de 106** en 4 de 4 corridas y la partición se **publica** en el mensaje
  del caso, así que un colapso se vería **al leerlo**; lo que falta es que lo vea la **máquina**. *Por
  qué no se arregla aquí:* añadir la guarda es cambiar `CA-01`, reimplementarlo y revalidarlo, es
  decir gastar la **última vuelta dev↔QA que queda** (`AGENTS.md` §6, contador que **no se reinicia**)
  en un instrumento que hoy no oculta ningún defecto. *Dueño:* `desarrollador`. *Forzador:*
  **REQ-021** mueve estas sondas a `tests/util/`; quien las mueva añade el recuento mínimo del lado
  `dentro`, que es exactamente donde ya vive el del lado `fuera`. *Vencimiento:* el cierre de
  **REQ-021**.

**La clase que ya va TRES veces en este mismo REQ: una afirmación sobre cómo se comporta el mecanismo,
escrita sin ejecutarla.** No son tres descuidos de tres personas distintas: es una **forma**, y por eso
se nombra aquí en vez de arreglarse tres veces. (1) **CA-04** fijó su línea base en `arnes_sin_cita`
**del tag v1.32.0**, donde esa función **no existe**: el criterio no era exigente, era **inejecutable**.
(2) La guarda (c) de **CA-05** nombró como señal «**su salida existe y no está vacía**», y en este
corredor la salida **no existe hasta el final**: constante-cero, y convertía el único PASS de (i) en
**SKIP permanente**. (3) Ese mismo párrafo llamó **«ruidoso»** al modo de fallo del acoplamiento, y está
medido que en el modo por defecto **no se nota nada**. Las tres veces el texto era **razonable**,
describía un mecanismo **real** y lo escribió alguien que lo conocía — y **nadie lo ejecutó**. La
diferencia con las cuatro formas que `CA-07` adopta es que aquéllas se ven **leyendo** el criterio (una
enumeración, un número sin tipo, una igualdad, una magnitud ortogonal) y **ésta no se ve leyendo**: la
única manera de verla es **correr contra el árbol lo que el criterio afirma**, antes de entregarlo. Y el
coste crece con lo tarde que se caza, medido en este REQ: (1) se cazó **antes** de despachar QA y costó
**cuatro líneas**; (2) se cazó en implementación y costó un **write-back**; (3) se cazó en la **vuelta 2
de 3** y es un hallazgo de clase **`contrato`** que **bloquea el cierre**. La regla que sale de aquí
—«un criterio que nombra una **señal**, una **función** o un **modo de fallo** del mecanismo no se
entrega sin haberlo ejecutado contra el árbol», con sede en la **Definition of Ready**— es candidata a
1.34.0 y está abajo, con las otras dos.

**Fuera de alcance, con su motivo:**
- **QA-017-07** — **un lector de guardián que no es determinista**, `instrumento`, **escalado al
  `auditor-seguridad`** y **preexistente en los dos árboles**. `arnes_norm_clave` —**idéntica** en
  v1.32.1 y en este árbol— devuelve una **clave distinta en cada llamada con la misma entrada** cuando
  la línea de cabecera **empieza** por una secuencia multibyte inválida bajo locale UTF-8: seis llamadas
  consecutivas del mismo proceso, seis claves (`docs/qa/1.33.0.md`, QA-017-07); bajo `LC_ALL=C` es
  estable. Hoy el efecto **observado** es fail-closed —la basura no se parece a ninguna clave conocida,
  así que la línea no declara campo—, pero eso es una **consecuencia del azar**, no una propiedad
  declarada, y el sujeto es una **puerta**. **REQ-017 reduce la exposición y no la introduce** —quita
  una de las dos operaciones con patrón por línea del camino de lectura—, así que **no reabre este
  REQ** y va con **dueño propio**, exactamente como se hizo con SEC-030. Se traza aquí, y no como
  criterio, por tres motivos: el defecto no está en lo que este REQ construye, ningún criterio suyo
  podría fallar por él sin contratar código ajeno, y el `auditor-seguridad` —que todavía no ha
  firmado— entra al tema por este REQ. Es también el motivo medido por el que **CA-10 no contrata el
  valor del texto publicado** fuera del dominio, sólo la decisión.
- **QA-017-13** — **el banco sale rojo 2 de 12 corridas por un caso AJENO a este REQ**, `instrumento`,
  vuelta 2. `secciones/25-presupuesto-de-analisis.sh` contrata **un reloj absoluto** de **4 000 ms** y
  bajo `JOBS=6` da **4 333 ms**; **aislado, el banco sale 12 de 12 verde** (medido por QA el
  2026-09-07). Es **la forma (d) que `CA-07` acaba de prohibir, viva en otro archivo**: un reloj
  absoluto lo falsea la máquina, lo falsea el runner del CI y lo falsea la carga — exactamente lo que
  la sección que este REQ está escribiendo enuncia. **No entra aquí**, y ninguno de los dos motivos es
  de gusto: el archivo **no está en el `Archivos:`** de este REQ, y el defecto es **preexistente** —no
  lo introduce este arreglo—, la misma disposición que ya se dio a QA-017-07 y a SEC-030. Y la regla de
  CA-07 rige, **por su propio alcance temporal**, para lo que se escriba **desde** 1.33.0: adoptarla no
  es una comisión para barrer el árbol, es una comisión para que el criterio siguiente no nazca así.
  *Dueño:* `desarrollador`. *Vencimiento:* la palanca «**¿esta prueba mide algo?**» (**REQ-020**), que
  es donde esta clase tiene sede. *Consecuencia operativa mientras tanto, y por eso se declara:* un
  rojo del banco **en esa sección** no es evidencia contra este REQ, y quien lea la puerta requerida de
  `main` necesita saberlo — si no, el ruido de otro archivo se cobra una de las vueltas que quedan.
- **SEC-030** — la pared de 60 s del hook. Preexistente en los dos árboles (los dos son cuadráticos ahí);
  este arreglo **sube** el umbral —**la dirección**, sostenida 6 de 6 corridas; la **magnitud no se
  acredita**, porque la sonda que la estima no repite (QA-017-05, dueño SEC-030)— y por eso CA-09 obliga
  a medirlo con su procedencia y su dispersión y dejarlo escrito para su dueño, pero cerrar SEC-030 no es
  de este REQ. Arreglar **la sonda** tampoco: es del mismo dueño que la pared.
- **`tests/util/` con las sondas compartidas** y la puerta de «¿esta prueba mide algo?»: son las otras dos
  palancas de coste de 1.33.0 y tienen comisión propia. Las sondas de CA-03..CA-06 nacen **dentro** de la
  sección de banco de este REQ; si `tests/util/` llega después, quien las mueva paga esa mudanza.
- **Partir la sección 32 en archivos** (REQ-014) y **SEC-020** (la gramática de `Archivos:`): otros REQ.
- **Generalizar a la metodología la regla que CA-05 aplica** —«un criterio de coste contra una **línea
  base congelada** declara si es puerta permanente o **acreditación única**, porque una razón contra un
  tag deja de medir en cuanto se acredita»—. Es de la misma familia que las cuatro formas de CA-07 y se
  escribiría en el mismo sitio, pero **no** entra aquí: CA-07 ya está adoptando una forma en esta
  ventana y añadir una segunda cambiaría el alcance del REQ. Dueño: `analista-requerimientos`,
  candidata para 1.34.0.
- **Adoptar como QUINTA forma prohibida «acotar por la propiedad equivocada»** —acotar el universo de un
  criterio por una propiedad que **no separa** la clase que el criterio quiere excluir, de modo que el
  contraejemplo queda **dentro** del dominio declarado y el criterio hermano se queda **sin sujeto**—.
  Es prima de la forma (d), pero sobre el **conjunto** en vez de sobre la magnitud, y su caso medido es
  el de este REQ: `ADR-004` y `docs/qa/1.33.0.md` § «Forma del criterio deficiente». **No** entra aquí
  por el mismo motivo que la anterior —CA-07 ya adopta una forma en esta ventana—, y por uno más: se
  escribiría en `requirements/README.md` y en su plantilla, que son los archivos que CA-07 está
  tocando; hacerlo en la misma comisión mezclaría dos adopciones en un solo diff. Dueño:
  `analista-requerimientos`, candidata para 1.34.0, junto a la de CA-05.
- **Adoptar en la Definition of Ready «lo que un criterio afirma del mecanismo se EJECUTA antes de
  entregarlo»** — la clase nombrada arriba, con sus **tres** casos medidos dentro de este mismo REQ
  (CA-04 apuntando a una función inexistente en el tag, la guarda (c) de CA-05 nombrando una señal
  constante-cero, y el «ruidoso» del acoplamiento medido silencioso). **No** es una quinta o sexta
  **forma** de criterio mal escrito: las cuatro de CA-07 se detectan **leyendo** y ésta sólo se detecta
  **corriendo**, así que su sede natural es la lista de la Definition of Ready —junto a la línea del
  mapa de archivos y a la de la forma (d)—, que es también una revisión humana y no de runtime. **No**
  entra aquí por los dos motivos de las candidatas anteriores: CA-07 ya adopta una forma en esta
  ventana, y se escribiría en `requirements/README.md` y en su plantilla, los archivos que CA-07 está
  tocando. Dueño: `analista-requerimientos`, candidata para 1.34.0, la tercera de la lista.
- **El núcleo de 1.33.0** —REQ-011, los dos barridos por estado, el rigor comprobable— no entra aquí.

**Sin ADR.** CA-07 **extiende** una sección ya adoptada por REQ-012 con su misma forma y su mismo alcance
temporal (rige para criterios escritos o modificados desde 1.33.0; los REQ `completado` no se reabren ni
se reescriben), y nace de un caso medido dentro de ella. No cambia la decisión base ni el alcance de
REQ-012, así que es cambio menor con entrada de Historial, no decisión de arquitectura.

**CON ADR el dominio de la equivalencia: `ADR-004`, y la clasificación «cambio menor» de la vuelta 0 queda
REVOCADA.** Aquel write-back acotó CA-01 a un dominio y creó CA-10 **sin ADR**, apoyándose en el
precedente de la pared de los 60 s. **Ese precedente no transporta**, y lo dice la propia comparación:
la pared se declaró **medida** y se cedió a **otro dueño** (`SEC-030`), así que no añadió obligación
alguna a este REQ y ningún criterio suyo podía fallar por ella; **CA-10 se contrata como criterio** —es
una puerta que hay que pasar para cerrar, añade trabajo en el banco y **cambia el significado de CA-01**,
que es donde este REQ define qué quiere decir «equivalencia»—, y eso es lo que `AGENTS.md` §9 llama de
fondo. Encima del argumento formal hay uno medido: **la decisión nueva —dónde se traza la frontera— es
justo la que salió mal**, y salió mal en silencio porque se tomó dentro de un cambio clasificado menor,
donde nadie tenía que justificar la elección de la propiedad. El ADR registra qué propiedad define el
dominio, **por qué ésa separa** las dos clases, qué se contrata fuera y las seis alternativas descartadas.
Lo declaró `qa-tester` en la vuelta 1 (`docs/qa/1.33.0.md` § «CA-10, ¿cambio menor o de fondo?») y el
`analista-requerimientos` lo acepta.

**Lo demás del write-back sigue siendo cambio menor, con el examen hecho y no supuesto.** **CA-05**,
**CA-06**, **CA-08** y **CA-09** se **precisan**: se corrigen sondas y procedimientos de medida, no
techos —el ≤ 0,25×, el ≤ 2,0×, el ≤ 1,25× y el ≤ 2,6 quedan **intactos**—, que es la dirección que la
clase `instrumento` exige. Ningún criterio se **relaja para que el código encaje**
(`requirements/README.md` § «Y el reverso, para que esto no sea una coartada»): la única restricción que
se estrecha es la de CA-01, y se estrecha por **imposibilidad demostrada** —«el estado de la heredada»
no designa un objeto único en esa clase—, con su complemento contratado al lado en CA-10 y **con más
carga**, no con menos: fuera del dominio se exige ahora coincidir con un **oráculo**, no sólo repetir. El
único archivo que entra en `Archivos:` es el propio **`ADR-004`**. Lo que sí sube el alcance de la
**implementación** —la ficha de byte inválido en el corpus, la clasificación por locale, los tres casos
de CA-10, el caso de dirección de CA-09, el «¿produjo casos?» del denominador y las series intercaladas
con su comprobación de convergencia— es trabajo de la **vuelta 2 de 3**, y por eso el REQ sigue
`en-progreso`.

## Preguntas abiertas / conflictos

**Ninguna sobre el contenido de este REQ** — sin conflicto de alcance con ningún REQ abierto: REQ-016
está `completado` y CA-07 no reescribe ninguno de sus criterios; el `Archivos:` de este REQ y el de
REQ-011, REQ-013 y REQ-014 se comprueban con `tools/arnes-paralelo.sh` antes de despachar en paralelo,
nunca por intuición. La lectura alternativa sobre el techo de CA-08 (ii) **no** queda abierta: está
contratada la lectura «la varianza es del procedimiento» con su evidencia, y la otra queda escrita con
su **condición de disparo** y su dueño (el propietario, con ADR) dentro del propio criterio.

**Conflicto de NUMERACIÓN con REQ-021 (`pendiente`) — RESUELTO el 2026-09-07 por la vía (a); se deja
escrito con su causa, que es lo que vale.** La coordinadora renumeró el ADR de **REQ-021** a
**`ADR-005`** (12 referencias, todas de texto, ningún archivo que mover, ningún criterio ni techo ni
alcance tocado; fila de Historial del propio REQ-021). En disco `ADR-004` es **único** y es el de este
REQ, y REQ-021 ya no reserva ese número: **el conflicto no bloquea el despacho de REQ-021** y esta
sección deja de decirlo. La consecuencia para este REQ es el campo `Archivos:`, que gana
`requirements/REQ-021.md` — ver la fila de Historial de **QA-017-14**.

*Enunciado tal como se declaró el 2026-09-07, antes de la resolución, porque su **causa** sigue viva y
sin dueño:* `ADR-004` estaba tomado por
este REQ —el archivo `docs/decisions/ADR-004-dominio-de-equivalencia-por-invariancia-de-locale.md`
**existe** desde el 2026-09-07— y **REQ-021 declaraba en su `Archivos:` un
`docs/decisions/ADR-004-instrumentos-compartidos-en-tests-util.md` que aún no existía**, citándolo en su
Trazabilidad, en sus `Notas` y en su Historial: dos decisiones distintas con el mismo número. **No
bloqueaba a este REQ** —su ADR existe, es único en disco y sus enlaces resuelven—, y **no** producía un
`disjunto` falso en `tools/arnes-paralelo.sh`, porque las dos rutas son distintas y REQ-021 ya colisiona
con éste por `docs/qa/1.33.0.md`. **Sí bloqueaba el despacho de REQ-021** hasta resolverse, y la
resolución era de la coordinadora, no de esta comisión: (a) **renumerar el de REQ-021 a `ADR-005`** —seis
referencias dentro de un REQ que aún no se ha empezado, ningún archivo que mover—, o (b) mover el de
aquí a `ADR-005` —un `git mv` más seis referencias en este REQ y en el propio ADR—. Se recomendó (a)
por coste, y (a) es lo que se hizo (resultaron **12** referencias, no seis). **Causa, que es lo que conviene retener:** el número de ADR **no tiene asignador**; un REQ
puede reservarlo en su `Archivos:` meses antes de que el archivo exista, y quien escribe un ADR nuevo
mira el disco —donde el número está libre— y no los REQ pendientes. Es la misma clase que este REQ
persigue: **una comprobación que mira la magnitud equivocada** (el disco en vez de las reservas).

## Trazabilidad
Origen: hallazgo **H-07** (`instrumento`) de `docs/qa/1.32.1-hallazgos-vuelta-3.md` §5 — residual
declarado con dueño `desarrollador`, ventana 1.33.0 y vencimiento en el cierre de 1.33.0; `docs/PLAN.md`
§ «1.33.0 — las palancas de coste van primero» (decisión del propietario, 2026-09-07); lección medida
sobre **CA-08 de REQ-016**; precedentes de la misma familia: `ADR-002` y `SEC-025`. Decisión de fondo del
dominio de equivalencia: **`ADR-004`** (`docs/decisions/ADR-004-dominio-de-equivalencia-por-invariancia-de-locale.md`),
que registra la propiedad elegida, las seis alternativas descartadas y la revocación de la clasificación
«cambio menor» de la vuelta 0. Relaciones declaradas
y **no** absorbidas: `SEC-030` (la pared de los 60 s y la sonda que la estima, dueña de QA-017-05) y
**QA-017-07**, escalado al `auditor-seguridad` (lector no determinista en `arnes_norm_clave`,
preexistente en los dos árboles; ver `Notas / alcance`).
Tocado por: `analista-requerimientos` / 2026-09-07 (creación, revisión de CA-05, write-back del mapa de
archivos tras el cierre de H-08 y corrección inline de CA-04 y CA-09 **antes** de despachar QA);
`desarrollador` / 2026-09-07 (implementación y mediciones en
`docs/qa/1.33.0.md`; sólo editó el `Historial de cambios` de este archivo, por instrucción de la
coordinadora); `qa-tester` / 2026-09-07 (validación **vuelta 0 de 3**, Opus: 8 de 9 criterios PASS, siete
hallazgos en `docs/qa/1.33.0.md`; escribió el veredicto `QA:` de la cabecera);
`analista-requerimientos` / 2026-09-07 (**write-back de los siete hallazgos** de esa vuelta: CA-01 acotado
a un dominio, **CA-10** nuevo, y precisiones en CA-05, CA-06, CA-08 y CA-09 — sin tocar `Estado:` ni `QA:`
ni ningún techo contratado); `qa-tester` / 2026-09-07 (validación **vuelta 1 de 3**, Opus: 7 de 10
criterios PASS; CA-01, CA-08 y CA-10 no pasan; seis hallazgos, tres de la vuelta 0 cerrados; escribió el
veredicto `QA:` de la cabecera y la recomendación de ADR); `analista-requerimientos` / 2026-09-07
(**write-back de la vuelta 1**: dominio de CA-01 redefinido por **invariancia de locale**, oráculo y caso
acreditador en CA-10, cláusula de convergencia en CA-08, «¿produjo casos?» en el denominador de CA-05 y
sede del caso de dirección en CA-09; **`ADR-004`** creado y la clasificación «cambio menor» de la vuelta 0
revocada — sin tocar `Estado:` ni `QA:` ni ningún techo contratado); `analista-requerimientos` /
2026-09-07 (**write-back de deriva sobre la guarda (c) de CA-05**, reportada por el `desarrollador` al
implementarla: la señal anterior —«su salida existe y no está vacía»— es **constante-cero** en este
corredor y dejaba el único PASS de CA-05 (i) en SKIP permanente; se nombra la señal que discrimina —los
casos que la corrida deja escritos mientras corre—, se declara el **acoplamiento** que eso introduce y se
anotan **dos residuales** con dueño, forzador y vencimiento. **Cambio menor, sin ADR**: la propiedad
contratada y todos los techos quedan intactos — sin tocar `Estado:` ni `QA:` ni `Seguridad:` ni
`Hallazgos abiertos:`); `qa-tester` / 2026-09-07 (validación **vuelta 2 de 3**, Opus: **los 10
criterios PASAN**; cuatro hallazgos —QA-017-11, QA-017-12, QA-017-13, QA-017-14—, de los que sólo
**QA-017-12** es `contrato`; escribió el veredicto `QA:` de la cabecera);
`analista-requerimientos` / 2026-09-07 (**write-back de la vuelta 2**: corregido el modo de fallo del
acoplamiento de la guarda (c) —«ruidoso» → **silencioso en el modo por defecto**— en CA-05 y en su
residual gemelo, con el **forzador rehecho** porque un fallo silencioso no dispara al que pasa al lado;
corregidos los márgenes de la sonda de dirección de CA-09 —**1,25–3,40× declarados → 1,154×–2,523×
medidos**—; declarados **QA-017-11** y **QA-017-13** con clase, dueño y vencimiento; `requirements/REQ-021.md`
añadido al `Archivos:` y el choque de numeración registrado como resuelto; y nombrada la **clase** de
las tres afirmaciones sobre el mecanismo escritas sin ejecutarlas. **Cambio menor, sin ADR** — sin
tocar `Estado:`, `QA:`, `Seguridad:`, `Hallazgos abiertos:`, ningún criterio contratado ni ningún
techo)

## Historial de cambios
| Fecha | Antes → Después | Causa | ADR |
|------------|-----------------|-------------------------|--------|
| 2026-09-07 | (creación) | H-07 (`instrumento`, vuelta 3 de 1.32.1): `arnes_sin_cita` cuadrática en la longitud de línea; el banco 39 s → 92 s, ruta crítica 7,6 s → 75,7 s. Y la segunda mitad: CA-08 de REQ-016 **se cumplía** midiendo procesos mientras se degradaba el reloj | — |
| 2026-09-07 | **CA-05**: puerta comparativa **en cada corrida** del banco → **acreditación única** de fail-before/pass-after, re-ejecutable a demanda con `ARNES_COSTE_RUTA_CRITICA=1` y en **SKIP con motivo** (nunca PASS) sin ella. Además: el denominador se **acota** con `timeout` de 4 × mín(este árbol) —el vencimiento **prueba** la cota ≤ 0,25×, no la estima— y la igualdad de inventario contra el árbol heredado deja de duplicarse aquí y se apoya en **CA-02** (banco entero). Techo contratado **intacto** (≤ 0,25×); mitad (i) intacta | Desviación escalada por el `desarrollador` (`docs/qa/1.33.0.md` § «CA-05 cuesta más que la regresión que certifica»): implementado **tal como estaba contratado**, el banco pasa de 45,14 s a **144,76 s** y la puerta requerida de `main` queda **más lenta (145 s) que la regresión que este REQ arregla (92 s)**, de forma permanente porque la línea base es un tag fijo — lo contrario de lo que dice la Historia de este REQ. Y el motivo de fondo: una razón contra un tag **congelado** es evidencia de un cambio, no una puerta que siga midiendo; la vigilancia permanente la da CA-03, que es estructural y no envejece. Se **descarta** cerrar (i) contra un inventario **registrado** del tag: una copia de la realidad sobre un banco que crece a propósito (clase `ADR-002` / `SEC-025`) | — |
| 2026-09-07 | **CA-09** — medición exigida por el criterio, **no** cambio de criterio: tamaño de documento en que se alcanzan los 60 s de un hook `PreToolUse`, con la sonda de la vuelta 3 de 1.32.1 (cabecera de **una sola línea** creciente) y **restando el coste fijo de arranque** (~0,21 s; sin restarlo la sonda colocaba la pared en 4,25 MB): **este árbol ≈ 1,60 MB** (orden medido 2,01) · **v1.32.1 ≈ 0,94 MB** (2,00; el REQ decía ≈ 0,99 — la misma cifra) · **v1.32.0 no medible en ese rango** (orden ~1 en Linux; el ≈ 1,56 MB del REQ está fechado en otra plataforma). Los **dos** árboles siguen cuadráticos en ese camino por `arnes_norm_clave`: es **SEC-030**, preexistente, con dueño y ventana propios, y **no** se cierra aquí. Este arreglo **sube** la pared de 0,94 a 1,60 MB **sin proponérselo**, y el beneficiario es quien escribe documentos de cabecera larga: gana ~0,66 MB de margen antes de que el hook muera — y un hook muerto no deniega | — |
| 2026-09-07 | **CA-04**, valores de **referencia** (los techos no se tocan): «hoy es **49×**» → **7,9×**, que es lo que da el árbol enfermo **en Linux** sobre la misma línea base; el 49× está fechado en otra plataforma y **no se reproduce**. Y la línea base: v1.32.0 **no tiene** `arnes_sin_cita` —la noción de cita nace en 1.32.1—, así que se comparó **la misma boca** (`arnes_norm_clave` de v1.32.0 contra `arnes_campo_linea` de hoy, que es `arnes_sin_cita` **más** `arnes_norm_clave`): la lectura **estricta**, 1,27× frente al 0,21× que daría comparar sólo el escáner | Desviaciones declaradas por el `desarrollador` (`docs/qa/1.33.0.md` § «Desviación menor declarada: la línea base de CA-04»). El techo (≤ 2,0×) se cumple y el caso **falla-antes / pasa-después**, que es lo que CA-04 contrata; se escribe porque **una razón medida en otra plataforma no es transportable** — y ese es justamente el motivo de que estos criterios sean razones y no segundos | — |
| 2026-09-07 | **CA-05**, implementación del write-back anterior (el criterio **no** se toca): el defecto de `ARNES_COSTE_RUTA_CRITICA` se **invierte** —apagada salvo `=1`—; el denominador se acota con `timeout` de **4 × mín(este árbol)** derivado en la misma corrida y **después** del numerador (`plazo = ⌈4 × u_este / 10⁶⌉` s, redondeo **hacia arriba**: abajo probaría una desigualdad más floja que la contratada); el **vencimiento es PASS** y nunca SKIP; y la mitad (ii) pasa a ser **auto-anclada** (las 3 corridas cronometradas de este árbol dan el mismo inventario **entre sí**, 40 casos). Guardas nuevas de CA-06: sin `timeout` en el PATH, con el numerador **bajo el suelo de 50 ms**, o con una heredada que termina **sin producir un solo caso** → SKIP con motivo. **CA-05 (iii), el número acreditado: 0,125× — 9,60 s frente a 76,19 s, medido el 2026-09-07.** Y el **fail-before / pass-after de la rama nueva**: con este árbol la heredada **no** termina en 36 s = 4 × 8,81 s → **PASS**; con `ARNES_HOOKS_DIR` := v1.32.1 —«este árbol» es el enfermo— la heredada **termina** dentro de 285 s = 4 × 71,17 s → **FAIL**. Coste: 37/2 baja de ~120 s a **12,5 s** apagada y **76,1 s** encendida | Delta de implementación del write-back de CA-05, comisionado por la coordinadora | — |
| 2026-09-07 | **H-08** (`instrumento`, `docs/PENDIENTES.md`) — **cerrado**, y **fuera del `Archivos:` declarado**: `.github/workflows/banco.yml` gana `fetch-depth: 0` en el checkout. Sin tags, el árbol congelado que 11 criterios materializan no existe en CI y todos salían **SKIP**: la puerta requerida de `main` dio verde en el PR #43 sobre el único REQ del PR sin ejecutar ni una de sus comprobaciones. Medido: banco **sin** tags 833 PASS · 12 SKIP · 45,85 s (reproduce exactamente el PR #43) → **con** tags y 37/2 apagada **842 PASS · 3 SKIP · 55,98 s**; **+10,1 s (+22 %) compran 9 criterios** que pasan de no medirse a medirse. Checkout: 0,202 s superficial → 0,346 s completo (+0,14 s; `.git` 1,2 → 1,6 MB). `fetch-depth: 0` y no `fetch-tags: true`: éste mantiene profundidad 1 y deja la prueba colgando del clon superficial, cuyo modo de fallo **es este hallazgo** | Ampliación de comisión **aprobada por el propietario** el 2026-09-07 — gate humano de `AGENTS.md` §6 (el workflow de CI es decisión humana). Lo aprobado es la **combinación**: sin apagar 37/2, recuperar los tags añadiría sus ~120 s a la puerta requerida | — |
| 2026-09-07 | **Campo `Archivos:`** — write-back de **deriva** (`AGENTS.md` §9), **cambio menor**: ningún criterio de aceptación se toca y el alcance no se mueve; se corrige el **mapa** para que coincida con lo construido. **Antes:** `hooks/lib.sh, tests/escenarios/hooks/secciones/37-coste-del-escaner-1-escala.sh, tests/escenarios/hooks/secciones/37-coste-del-escaner-2-la-seccion-caliente.sh, tests/escenarios/hooks/run.sh, tests/escenarios/hooks/README.md, requirements/README.md, templates/requirements-README.md.tpl`. **Después:** los mismos **+ `.github/workflows/banco.yml`, `docs/PENDIENTES.md`, `docs/qa/1.33.0.md`**. Y en `Notas / alcance`, la regla de exclusión que este REQ declaraba deja de cubrir **el registro de la ventana**: `docs/qa/<versión>.md` no es universal ni un apéndice, y como la intersección se calcula **sobre lo declarado**, un archivo que REQ-007, REQ-008 y REQ-011 declaran y éste no salía `disjunto` **en falso** —fail-open, justo el modo de fallo que el campo existe para evitar—. Consecuencia operativa declarada: con `banco.yml` dentro, **todo REQ que declare `.github/`, `.github/workflows/` o ese archivo colisiona con éste**, porque la herramienta expande un directorio a todo lo que cuelga de él. **Estado intacto** (`en-progreso`): un mapa de coordinación no reabre trabajo ni firma nada | Cierre de **H-08** en el delta de implementación (`cand/1.33.0`, `bf8ca8f`), con **ampliación de comisión aprobada por el propietario** el 2026-09-07 (gate humano de `AGENTS.md` §6): el `desarrollador` tocó tres archivos que el campo no declaraba y lo dejó anotado en la fila de H-08 en vez de corregir el campo, porque `Archivos:` es de la Definition of Ready del analista y su comisión lo acotaba al Historial. Sin este write-back el mapa diría algo falso sobre lo construido —clase `contrato`— y `tools/arnes-paralelo.sh` respondería sobre él | — |
| 2026-09-07 | **CA-04, el procedimiento** (el techo **≤ 2,0×** no se toca). **Antes:** «se mide `arnes_sin_cita` de este árbol **y la del tag v1.32.0**». **Después:** se mide **la boca que lee una línea de cabecera** — `arnes_campo_linea` hoy (que es `arnes_sin_cita` **más** `arnes_norm_clave`) contra `arnes_norm_clave` **sola** en v1.32.0, con el puntero al sitio único (`hooks/lib.sh`)—, más el párrafo que dice **por qué ésa es la comparación correcta y no sólo la posible**: lo contratado es el coste de **leer una línea de cabecera**, trabajo que hace la **capa entera** y no una función con un nombre concreto, y de las dos lecturas se toma la **estricta** (1,27× capa contra capa, frente al 0,21× de comparar sólo el escáner) | **No era una cifra vieja: era el criterio apuntando al vacío.** `arnes_sin_cita` **no existe** en v1.32.0 —la noción de cita nace en 1.32.1—, así que la mitad derecha de la razón no designaba nada y el criterio no era ejecutable tal como estaba escrito. Se corrige **inline** y no sólo en Historial porque el riesgo no es la pulcritud: es **gastar una de las tres vueltas dev↔QA en un hallazgo de redacción que cuesta cuatro líneas**, con un contador que **no se reinicia** (`AGENTS.md` §6). Es la vuelta más cara y más evitable del ciclo, y QA aún no estaba despachado. Origen del dato: desviación declarada por el `desarrollador` en `docs/qa/1.33.0.md` § «Desviación menor declarada: la línea base de CA-04» | — |
| 2026-09-07 | **CA-04, valores de referencia** (operativos, no de contrato): «hoy ese factor es **49×**» → **7,9×** del árbol enfermo v1.32.1 **medido en Linux**, y se escribe también el **1,27×** de este árbol, que es el que pasa el techo. El 49× queda declarado como **fechado en otra plataforma y no reproducible** | Mismo delta de implementación. Una razón medida en otra plataforma **no es transportable** — que es el motivo por el que este REQ contrata razones y no segundos, así que dejarla en pie contradecía su propia tesis | — |
| 2026-09-07 | **CA-09, valores de referencia y procedencia** (el criterio sigue exigiendo la **medición**, no un valor): heredada v1.32.1 «≈ 0,99 MB» → **≈ 0,94 MB** (la misma cifra dentro del redondeo, sustituida por la medida) · v1.32.0 «**≈ 1,56 MB**» → **no medible en ese rango** en Linux (orden ~1), cifra fechada en otra plataforma y **retirada** · se añade que **cada cifra lleva la corrida de la que sale**, y se **declara** la divergencia abierta: 2,01 y 2,00 de orden en la corrida de Linux del 2026-09-07 frente a un **1,46** posterior sobre un camino que se **sabe** cuadrático | Delta de implementación + dato nuevo del proyecto consumidor (2026-09-07). La divergencia **no se resuelve aquí**: es de **SEC-030** y de la validación de QA. Lo que sí es de este REQ es que dos series que discrepan queden **distinguibles** en vez de promediadas en una cifra que no midió ninguna de las dos — sin la corrida al lado, el número acreditado no se puede auditar | — |
| 2026-09-07 | **CA-01 acotado a un dominio + CA-10 nuevo** (write-back de **QA-017-01** `contrato` y **QA-017-02** `instrumento`). **Antes:** «Cuando se le pasa **una línea cualquiera**, Entonces … coinciden **byte a byte** con los del árbol heredado v1.32.1», con un corpus de 8 fichas **todas ASCII**. **Después:** la equivalencia se afirma sobre el **dominio** en que la heredada es **determinista** —propiedad comprobada en la corrida con k ≥ 3 evaluaciones de la misma entrada en el mismo proceso, no una lista—; el corpus **incluye** entradas de **fuera** (byte multibyte inválido) y la partición dentro/fuera se **mide**, con **FALLA si el corpus perdió la clase** (comprobable sin depender de la máquina) y **SKIP con motivo si es la máquina la que no tiene el defecto** (otra versión de bash, otro locale), nunca PASS; y lo de fuera se contrata en **CA-10**: determinismo de la **decisión** (`ARNES_CITA`/`ARNES_CR`), **invariancia de locale** frente a `LC_ALL=C` como oráculo, y el incumplimiento de la heredada **registrado** como fail-before. CA-10 declara además **lo que no afirma**: ni el veredicto de la puerta (medido: no cambia, la cabecera se recorre dos veces) ni el **valor del texto** publicado fuera del dominio (`ARNES_LINEA`, `ARNES_CR_LINEA`), que se construye con las mismas operaciones con patrón de QA-017-07 | **El criterio no era exigente: era insatisfacible.** Contraejemplo reproducible de QA (`docs/qa/1.33.0.md`, QA-017-01, 3/3 corridas): bajo locale UTF-8 en bash 5.3.9, `${l%$'\r'}` sobre una secuencia multibyte inválida **no devuelve un sufijo**, devuelve basura **distinta en cada evaluación de la misma entrada**, así que la igualdad byte a byte **no la cumple ni v1.32.1 consigo misma**. Y QA-017-02: el corpus excluía **por construcción** la única clase donde no hay equivalencia — un verde que no acredita lo que su nombre dice, la forma exacta que esta ventana persigue. **Consecuencia declarada, como se declaró la pared de los 60 s:** en esa clase REQ-017 **cierra un fallo en abierto de v1.32.1** (la heredada publica `cr=0` sobre una línea con CR interior). Un beneficio no declarado es una propiedad que nadie sabe que tiene y que la siguiente refactorización retira sin enterarse | — |
| 2026-09-07 | **CA-05 (ii) y CA-06, precisión de la sonda** (write-back de **QA-017-03** y **QA-017-04**, los dos `instrumento`; el techo **≤ 0,25×** y la mitad (i) **no se tocan**). **Antes:** (ii) exigía que las 3 corridas del numerador **produjeran casos** y coincidieran **entre sí**, y el vencimiento del plazo se daba por bueno con cualquier terminación anómala. **Después:** (ii) exige además **rc 0 y no más de 0 casos FAIL** del corredor interno —número **de contrato**: es la definición de corrida válida—; y el vencimiento se **acredita** con el estado de salida que `timeout(1)` **reserva** para la expiración (**124**, de contrato) **corroborado por reloj** (`t1 − t0 ≥ plazo`), mientras que cualquier otra terminación anómala —ejemplos **no exhaustivos**: **137**— emite **SKIP con motivo** por CA-06, que gana esa causa | Medido por QA: (a) con los hooks sustituidos por un `guard-completado` que **siempre permite**, la sección 32 sale en **14 FAIL y rc 1**, el plazo derivado cae a 16 s, la heredada vence y **las dos mitades dan PASS** — incluida la que se llama «la comparación no se compra dejando de probar»; tres corridas **igualmente rotas coinciden entre sí**, así que el ancla de (ii) no ve nada sin el rc, y **una puerta cuyo umbral lo calcula la corrida que se está midiendo se compra rompiéndola**. (b) Matar al hijo **desde fuera** a los 0,6 s de un plazo de 60 s devuelve **137**, igual que el OOM killer — que es el final más probable de la corrida heredada, el camino cuadrático—, y el caso lo leía como «DEMOSTRADO»: un verde falso etiquetado como prueba | — |
| 2026-09-07 | **CA-08, el procedimiento de medida** (write-back de **QA-017-06**, `instrumento`; el techo **≤ 1,25×** **no se toca** — es `operativo` y su dirección admitida es **bajar**). **Antes:** «el mínimo de reloj … en la misma corrida», sin decir **cómo** se toman las series. **Después:** las series de los dos árboles se **intercalan** (a, b, a, b, …), **no menos de 6** por árbol, estadístico el **mínimo** | Medido el 2026-09-07: **en bloque**, el máximo de seis razones queda a un **2,6 %** del techo (1,217 contra 1,25); **intercaladas**, el mínimo de seis da **1,012×**. La varianza es del **procedimiento**, no del arreglo. Y (ii) corre en la **puerta requerida de `main`**: un runner cargado la pondría en rojo sin que exista regresión, y ése es el modo de fallo que termina con alguien **subiendo el techo** — apagando la única mitad que ve el reloj | — |
| 2026-09-07 | **CA-09 deja de acreditar magnitud** (write-back de **QA-017-05**, `instrumento`, dueño **SEC-030**, **no cerrado aquí**). **Antes:** «este árbol **≈ 1,60 MB** (orden 2,01) · heredada **≈ 0,94 MB**», más una **«divergencia declarada»** entre 2,01 y 1,46. **Después:** la cifra **se retira**; el criterio exige medición, **procedencia** y **dispersión** (**no menos de 6** corridas por árbol y **rango**), **prohíbe** acreditar una magnitud, y contrata sólo la **dirección**: en **cada** corrida este árbol mide **no menos** que v1.32.1 en **esa misma** corrida. Se registra la serie medida —**1,08 · 1,32 · 1,78 · 2,64 · 2,65 · 3,98 MB**, orden 1,41–2,33, factor ~3,7 entre extremos; v1.32.1 estable en 1,01–1,19— y que las dos series «divergentes» **no discrepan: son dos extracciones de la misma distribución** | La sonda **no repite**, y la causa es la regla que enuncia el propio `requirements/README.md`: sus tres tiempos base son **una sola muestra cada uno** —«el mínimo de k, nunca la media»—, dos entran como **diferencia de muestras únicas** y el orden va **en el exponente** de la extrapolación (1,41 frente a 2,01 mueve la pared un factor ~2,5); el arranque restado osciló 0,10–0,21 s. **La dirección del beneficio se sostiene 6 de 6; la magnitud, no** — y se había publicado como medida en el CHANGELOG y en `docs/ESTADO.md`. Lo cazó el endurecimiento de CA-09 del 2026-09-07 (obligar a que cada cifra nombre su corrida), que **se pagó a sí mismo en su primera validación** | — |
| 2026-09-07 | **CA-01, el dominio se redefine por la propiedad que SÍ separa las dos clases; CA-10 gana su oráculo y su caso** (write-back de **QA-017-01** `contrato` y **QA-017-08** `instrumento`, vuelta 1). **Antes:** dominio = «las entradas sobre las que la heredada **es determinista**» (k ≥ 3 evaluaciones en el mismo proceso publican el mismo estado). **Después:** dominio = «las entradas sobre las que la heredada publica **un único y el mismo estado** en k ≥ 3 evaluaciones bajo el **locale del entorno** **y** k ≥ 3 bajo **`LC_ALL=C`**» — determinismo **e** invariancia de locale, y basta que falle una para caer fuera. Se añade como parte del criterio que el dominio se traza por una propiedad de la **heredada sola**, nunca por la coincidencia de los dos árboles (un dominio así hace un criterio que no puede fallar). **CA-10** pasa de contratar «determinismo + invariancia» a contratar la decisión de este árbol **contra un oráculo** —la heredada bajo `LC_ALL=C`, único sitio donde existe un valor correcto—, con la guarda de que el oráculo se comprueba antes de usarse (si la heredada tampoco repite bajo `LC_ALL=C`, la entrada **no es clasificable** → SKIP), y con **tres casos contratados** (uno por mitad) que publican el recuento de la partición. **`Archivos:`** gana `docs/decisions/ADR-004-…md` (producto de este write-back, no apéndice; declarado por archivo y no por directorio, que la herramienta expande) | **La propiedad anterior no separaba, y está medido** (`docs/qa/1.33.0.md`, QA-017-01; 106 entradas multibyte inválidas, k = 6, 4 corridas): **104–106** caían **dentro** del dominio y **6–7 de ellas divergen** —CA-01 exige 0—, mientras que en **3 de 4** corridas **cero** entradas caían fuera y **CA-10 se quedaba sin sujeto**. La clase divergente es «la heredada **no es invariante al locale**» (**7 de 106**, 4/4); el no determinismo es un fenómeno distinto y más raro (**0–2**) que **no coincide** con ella. Causa del error: lo que la vuelta 0 midió era cierto del **valor intermedio** de `${l%$CR}` y el criterio contrata sobre el **estado publicado**, donde la heredada **sí repite** — se publicó como medido en el `CHANGELOG.md` y ya está corregido allí. Comprobado antes de escribir: con el dominio nuevo **CA-01 puede dar 0** (las divergencias quedan todas fuera) y **CA-10 tiene sujeto ≥ 7 en 4 de 4** | `ADR-004` |
| 2026-09-07 | **CA-08 (ii), la sonda declara su resolución y se abstiene si no la alcanza** (write-back de **QA-017-06** en su **segunda** aparición, `instrumento`, vuelta 1; el techo **≤ 1,25×** **no se toca**). **Antes:** series intercaladas, ≥ 6 por árbol, estadístico el mínimo. **Después:** lo mismo **más** la comprobación de convergencia en la misma corrida —`segundo mínimo / mínimo` de **cada** árbol contra **el propio techo**, que no es un número nuevo—: si la supera, **SKIP citando las dos razones de convergencia y la razón obtenida**, **nunca PASS y nunca FAIL**. Y se deja escrita la condición de disparo de la lectura alternativa: si con el procedimiento implementado siguiera saliendo rojo sin regresión, **subir el techo es decisión del propietario con ADR**, nunca un ajuste dentro de la comisión que lo incumple | El procedimiento contratado en la vuelta 0 **no se implementó** (siguen 3 series en bloque) y el criterio pasó de rozar el techo a **cruzarlo**: 26 medidas, **2 rojas** (1,252× y 1,443× contra 1,250×) y **1 de cada 4** vueltas del banco completo en el modo de la puerta requerida (`JOBS=6`) en rojo, con `load` 0,91 —la carga previa no lo explica—. La lectura contratada es que la varianza es del **instrumento**: en aislamiento la misma razón da **0,821–1,010**, y un coste real no puede ser negativo, así que 0,821–1,443 sobre el mismo estimando es ruido de la sonda, que `JOBS=6` sube porque los dos árboles se miden en bloque con vecinos distintos. Sin la abstención, el caso queda **flaky en la puerta requerida de `main`**, que es el camino más corto a que alguien lo apague | — |
| 2026-09-07 | **CA-05, el denominador responde a la misma pregunta que el numerador: «¿esta corrida midió?»** (write-back de **QA-017-09**, `instrumento`; techo **≤ 0,25×** intacto). **Antes:** el vencimiento acreditaba la cota con (a) `rc = 124` y (b) el reloj. **Después:** además (c) la corrida heredada **produjo al menos un caso** —su salida existe y no está vacía—; si vence **sin producir ninguno**, **SKIP con motivo** (CA-06), nunca PASS | Medido por QA: una heredada que **se cuelga sin producir un caso**, o cuyo archivo de salida **ni siquiera existe**, agota el plazo y el caso publica «≤ 0,250× — DEMOSTRADO, no estimado». Hay precedente en esta misma ventana (el árbol heredado extraído sin bit de ejecución no arrancaba el corredor hijo); allí **terminaba** y caía en SKIP, pero un bloqueo —un hook esperando en `stdin`, un cerrojo— lo convierte en vencimiento. Es la familia de QA-017-03 un lado más allá: **una corrida que no midió no es una corrida lenta** | — |
| 2026-09-07 | **CA-09, cada exigencia con su sede** (write-back de **QA-017-10**, `instrumento`; el criterio no cambia de contenido). **Antes:** el criterio pedía medición con dispersión (≥ 6 corridas por árbol) **y** la dirección en cada corrida, sin decir qué mitad vive en el Historial y qué mitad en el banco. **Después:** la **medición con procedencia y dispersión** se anota en el **Historial** y se hace una vez; el **caso del banco** comprueba **la dirección**, pareada dentro de su propia corrida, y se dice que **publicar la magnitud no es comprobarla** | El caso existente da PASS porque el número impreso tiene forma de MB y **no compara** con la heredada, que mide al lado y sólo imprime: una corrida con este árbol **por debajo** de v1.32.1 seguiría en verde, que es lo contrario de lo único que CA-09 contrata. Y la lectura de que el banco debía repetir la sonda **6 veces por árbol** multiplicaría por seis la única medición cara de este REQ sin añadir nada: la dispersión es del Historial, la dirección es del caso | — |
| 2026-09-07 | **QA-017-07 trazado en `Notas / alcance` — y deliberadamente NO convertido en criterio.** `arnes_norm_clave`, **idéntica** en los dos árboles, devuelve una **clave distinta en cada llamada con la misma entrada** bajo locale UTF-8 cuando la línea **empieza** por una secuencia multibyte inválida (seis llamadas, seis claves; estable bajo `LC_ALL=C`). Se declara `instrumento`, **escalado al `auditor-seguridad`**, con dueño propio y sin reabrir este REQ | **Un lector no determinista dentro de una puerta.** Hoy el efecto observado es fail-closed, pero por **azar**, no por propiedad declarada. No entra como criterio porque el defecto **no está en lo que este REQ construye** y ningún criterio suyo podría fallar por él sin contratar código ajeno; **REQ-017 reduce la exposición y no la introduce** (una operación con patrón menos por línea), como se hizo con SEC-030. Queda escrito aquí porque el `auditor-seguridad` aún no ha firmado y entra al tema por este REQ, y porque es el motivo medido de que **CA-10 no contrate el valor del texto** publicado fuera del dominio | — |
| 2026-09-07 | **CA-05, la guarda (c) del denominador nombra la señal que SÍ existe en este corredor** (write-back de **deriva detectada en implementación**, `AGENTS.md` §9; **cambio menor**: la propiedad contratada, el techo **≤ 0,25×**, las mitades (i)/(ii)/(iii) y las guardas (a) y (b) quedan **intactos**). **Antes:** «(c) la corrida heredada **produjo al menos un caso** —**su salida existe y no está vacía**—». **Después:** «(c) la corrida heredada **dejó escritos no menos de 1 caso mientras corría**» —el **1**, número **de contrato**: es la definición de «esta corrida midió»—, leído de **lo que la corrida va dejando en disco a medida que avanza** (la sonda le fija la raíz de ese trabajo **antes** de lanzarla) y, si existe, de la salida recogida, con las fuentes en el **sitio único** de la sonda, marcadas **no exhaustivas**, y el **recuento** como lo contratado. Se añaden dos párrafos: **por qué** la señal anterior no podía cumplirse nunca aquí, con sus valores de **referencia** (**37** casos matada a los 12 s · **0** si no arranca), y el **acoplamiento** nuevo a la disposición del trabajo del corredor, con su fallo **cerrado y ruidoso** (recuento 0 → SKIP, nunca PASS) y con dueño y vencimiento en `Notas / alcance` | **La señal anterior es constante-cero: no era una guarda estricta, era insatisfacible.** Medido por el `desarrollador` al implementarla: el corredor **no imprime un solo byte hasta que todas sus secciones terminan** —junta los parciales al final—, así que la heredada **matada por `timeout` deja 0 bytes SIEMPRE**, trabajara o no; implementada al pie de la letra convierte el **único PASS** de CA-05 (i) en **SKIP permanente**, verificado encendiendo la palanca. Es la clase que `ADR-004` acaba de diagnosticar en CA-01 —**una comprobación correcta sobre la señal equivocada**— y la misma de CA-04 apuntando a una función que no existía en el tag. **Origen del error, que es lo reutilizable:** QA-017-09 propuso el remedio como «que la heredada **haya producido al menos un caso** —**o** que su salida exista y no esté vacía—» y el write-back de la vuelta 1 tomó la **glosa** por la **señal**, sin comprobar que en este corredor la salida no existe hasta el final. Se corrige **inline** y **antes** de la vuelta 2 —la última que permite `AGENTS.md` §6, con un contador que **no se reinicia**— por el mismo motivo que la corrección inline de CA-04: gastar la última vuelta dev↔QA en un problema de redacción es la vuelta más cara y más evitable del ciclo | — |
| 2026-09-07 | **Dos residuales declarados en `Notas / alcance`, con dueño, forzador y vencimiento** (ningún criterio se toca, ningún techo se mueve): (1) el **acoplamiento** de la guarda (c) de CA-05 a la disposición del trabajo del corredor — dueño `desarrollador`, forzador el primer cambio de esa disposición (**REQ-014**, **REQ-021**), vencimiento el cierre del primero que entre, fallo **cerrado y ruidoso**; (2) la **resolución** del caso de dirección de CA-09 en el régimen de **dos árboles idénticos** (razón verdadera 1,0), donde la sonda decide por ruido — dueño **SEC-030**, forzador que la razón verdadera se acerque a 1,0, vencimiento la ventana declarada de SEC-030 | (1) **nace con el arreglo de la guarda (c)** y no existía antes: una dependencia nueva sin declarar es lo que la refactorización siguiente rompe sin enterarse. (2) medido el 2026-09-07 al implementar el caso: con dos árboles idénticos, tres corridas dieron **SKIP, FAIL y PASS** sobre el mismo estimando; contra el tag congelado —el único régimen que este banco ejercita— da **9 de 9 PASS con márgenes 1,25–3,40×**, y subir a 3 pasadas cuesta **+4,4 s** en la puerta requerida de `main` **sin cerrar la clase**, porque la dispersión de esa sonda es de **SEC-030**. **No bloquea y no se arregla aquí**; se escribe porque un límite de resolución no declarado se descubre en la corrida en que falla | — |
| 2026-09-07 | **El modo de fallo del acoplamiento de la guarda (c) de CA-05 estaba medido FALSO** (write-back de **QA-017-12**, `contrato`, vuelta 2; **cambio menor**: no se toca el Dado/Cuando/Entonces de CA-05, ni las mitades (i)/(ii)/(iii), ni las guardas (a)/(b)/(c), ni el techo **≤ 0,25×**). **Antes**, en CA-05 y en su residual gemelo de `Notas / alcance`: «el fallo … es **ruidoso** —el único PASS de (i) desaparece del banco en la primera corrida— y no silencioso», modo de fallo *cerrado y ruidoso*. **Después:** **cerrado y SILENCIOSO en el modo por defecto** — con la disposición rota los veredictos son **idénticos** y la corrida sale **rc 0**; **no hay ningún PASS que perder**, porque sin `ARNES_COSTE_RUTA_CRITICA=1` el caso de (i) **ya es SKIP por «no se pide»** y la guarda (c) no se ejercita; y lo esperado **con la palanca encendida** —recuento 0 → SKIP con motivo, nunca PASS— queda marcado como **no medido**. Y el **forzador se REHACE**, que es la consecuencia que importa: de «quien cambie la disposición repunta la sonda» (expectativa que **no dispara**) a **obligación** —la comisión que cambie la disposición corre CA-05 **una vez con la palanca encendida** y comprueba el recuento (**+63,6 s** una sola vez, de 12,5 s a 76,1 s, fuera de la puerta requerida)—, con el **vencimiento condicionado a que esa corrida conste en su evidencia**: sin ella el residual no vence, sólo cambia de dueño | **Medido por QA en la vuelta 2** (`docs/qa/1.33.0.md`, QA-017-12): rota la disposición del corredor y comparado el inventario contra el sano, **los veredictos son idénticos y rc 0**; las dos únicas líneas distintas son las cifras de CA-08, que cambian en toda corrida. Los dos forzadores declarados —**REQ-014** y **REQ-021**— van a cambiar la disposición, correr el banco y **verlo verde**. El residual **se mantiene** (el acoplamiento existe); lo que era falso era su justificación, y con ella el mecanismo por el que iba a vencer: **un residual silencioso cuyo forzador se observa solo no vence nunca** — la misma forma que `SEC-036` obligó a corregir en REQ-021 en esta misma ventana | — |
| 2026-09-07 | **La clase nombrada, porque va TRES veces en este mismo REQ: una afirmación sobre cómo se comporta el mecanismo, escrita sin ejecutarla** (párrafo nuevo en `Notas / alcance` + candidata de 1.34.0; **ningún criterio se toca**). Los tres casos: **CA-04** con la línea base en `arnes_sin_cita` **del tag v1.32.0**, donde **no existe**; la **guarda (c) de CA-05** nombrando «su salida existe y no está vacía», **constante-cero** en este corredor; y el **«ruidoso»** de este mismo acoplamiento, medido **silencioso**. Se propone para 1.34.0 como línea de la **Definition of Ready** —«un criterio que nombra una señal, una función o un modo de fallo del mecanismo no se entrega sin haberlo ejecutado contra el árbol»—, **no** como quinta forma prohibida | **Las cuatro formas de CA-07 se detectan leyendo; ésta no.** Las tres veces el texto era razonable, describía un mecanismo real y lo escribió alguien que lo conocía — y **nadie lo ejecutó**. El coste crece con lo tarde que se caza, y está medido dentro de este REQ: (1) cazada **antes** de despachar QA → **cuatro líneas**; (2) cazada en implementación → **un write-back**; (3) cazada en la **vuelta 2 de 3** → hallazgo `contrato` que **bloquea el cierre**. No entra en esta ventana porque CA-07 ya adopta una forma y se escribiría en los mismos archivos que CA-07 está tocando | — |
| 2026-09-07 | **CA-09, márgenes de la sonda de dirección: dato DECLARADO sustituido por dato MEDIDO** (residual (2) de `Notas / alcance`; el criterio, su dirección contratada, el dueño **SEC-030** y su vencimiento **no se tocan**). **Antes:** «9 de 9 PASS con márgenes **1,25–3,40×**, muy por encima de la dispersión observada». **Después:** «9 de 9 PASS con márgenes **de 1,154× a 2,523×**», con la consecuencia escrita: el margen se sostiene igual 9 de 9, pero **ya no** «muy por encima» de una sonda que en el régimen de dos árboles idénticos produce **FAIL** sobre una razón verdadera de 1,0 — la distancia real al 1,0 es de **~0,15×**, no de ~0,25× | Los **1,25–3,40×** los declaró el `desarrollador` al implementar; **QA midió 1,154×–2,523×** el 2026-09-07 y **el suelo real está por debajo del declarado**. Se corrige inline y no sólo en Historial porque la lección **es la cifra**: **un colchón declarado de más es cómo un residual aceptado se vuelve un rojo sorpresa** | — |
| 2026-09-07 | **Dos residuales nuevos de la vuelta 2, escritos con su clase** (`Hallazgos abiertos:` ya los llevaba; ningún criterio se toca). **(1) QA-017-11** (`instrumento`): `CA-01` da **PASS sobre un dominio vacío o colapsado** — guarda `fuera = 0` y `no clasificables ≠ 0`, **no** `dentro = 0`; con la evaluación de la heredada truncada el dominio cae de **312 a 10** y sigue verde. Dueño `desarrollador`, forzador «**REQ-021** mueve estas sondas a `tests/util/`», vencimiento el cierre de REQ-021; va en la lista de residuales de la implementación. **(2) QA-017-13** (`instrumento`): el banco sale rojo **2 de 12** por un caso **ajeno** —`25-presupuesto-de-analisis.sh` contrata un **reloj absoluto** de 4 000 ms y da 4 333 ms bajo `JOBS=6`; aislado, **12 de 12** verde—. Dueño `desarrollador`, vencimiento la palanca «¿esta prueba mide algo?» (**REQ-020**); va en `Fuera de alcance` | (1) Un **universo encogido en silencio** dentro del criterio que argumenta que «un criterio que no puede fallar no es una puerta»; no muerde hoy —el dominio real es 97–99 de 106 y la partición se **publica** en el mensaje del caso—, y añadir la guarda gastaría la **última vuelta dev↔QA que queda** (`AGENTS.md` §6, contador que **no se reinicia**) en un instrumento que no oculta ningún defecto. (2) Es **la forma (d) que CA-07 acaba de prohibir, viva en otro archivo**: no entra aquí porque el archivo **no está en el `Archivos:`** y el defecto es **preexistente**, la misma disposición dada a QA-017-07 y a SEC-030 — el alcance temporal de la forma (d) rige para lo que se escriba **desde** 1.33.0. Se declara porque **un rojo del banco en esa sección no es evidencia contra este REQ** | — |
| 2026-09-07 | **`Archivos:` gana `requirements/REQ-021.md`** (write-back de **QA-017-14**, `instrumento`, vuelta 2; **sin decoración**, porque **SEC-020** sigue abierto y envolver elemento a elemento produce un `disjunto` con rc 0 sobre rutas que no existen). Y en `Preguntas abiertas / conflictos`, el **choque de numeración de ADR queda registrado como RESUELTO** por la vía (a) —la coordinadora renumeró el ADR de REQ-021 a **`ADR-005`**, 12 referencias de texto—, de modo que ya **no bloquea el despacho de REQ-021**; el enunciado original se conserva marcado como tal porque su **causa** sigue viva | `requirements/REQ-021.md` se tocó en esta ventana como consecuencia del choque que **este** REQ declaró, y el campo no lo decía: un mapa que no declara lo que se toca es la clase `contrato` aplicada al reparto. Aquí **no** podía producir un `disjunto` falso —QA comprobó los **9 pares** con `tools/arnes-paralelo.sh` y **todos colisionan igual**—, y por eso es `instrumento`. La causa raíz sigue sin dueño: **el número de ADR no tiene asignador**, así que quien escribe uno mira el disco y no las reservas de los REQ `pendiente` | — |
| 2026-09-08 | **CA-08 (ii): la resolución se comprueba sobre la RAZÓN, no sobre cada brazo** (el techo **≤ 1,25×** **no se toca** —sigue `operativo`, dirección admitida **bajar**—; la mitad (i), la intercalación y las ≥ 6 series por árbol quedan **intactas**). **Antes:** el veredicto lo autorizaba la convergencia **por brazo** —`segundo mínimo / mínimo` de cada árbol contra el techo— y se emitía PASS/FAIL sobre **una** razón. **Después:** el par intercalado se repite **no menos de k veces** (k **operativo**, se **sube**, y **se deriva midiendo**), el caso **publica las k razones, su recorrido `máx(r)/mín(r)` y el techo**, y sólo emite **PASS** si `máx(r) ≤ techo`, **FAIL** si `mín(r) > techo`, y **SKIP** en cuanto el techo cae **dentro** del recorrido (CA-06); la **unanimidad** de las k es **de contrato**. Se añaden además: el **par discriminante** obligatorio —(a) negativo: con la dispersión ensanchada y **sin** regresión el caso emite SKIP, y **desactivada la guarda** esa misma entrada da PASS o FAIL según la corrida; (b) positivo: con regresión **sintética** de **no menos de 2×** (operativo, se **baja**) emite **FAIL** y no SKIP—; un **techo de coste** para lo que la guarda añade a la puerta requerida (operativo, se **baja**, escrito como techo) con la salida declarada si no cabe —sacar (ii) de la puerta requerida como acreditación fechada, **decisión del propietario** con entrada en `PENDING_APPROVAL.md`—; y la marca de que la convergencia por brazo es **necesaria y no suficiente**. La condición de disparo de la lectura alternativa se declara **cumplida**, su consecuente **descartado por la medición**, y se le pone una condición nueva | **La sonda no resuelve el factor que vigila, medido sobre cinco corridas de la puerta requerida `hooks-en-linux` con código idéntico** (tabla y commits en `docs/PENDIENTES.md` § «PRIMER TRABAJO DE 1.34.0»; la identidad del código la comprobó de forma independiente el `auditor-seguridad` en `R-019`): la razón recorre **0,973–1,364** —factor **1,40**— contra un techo de **1,25**, con **2 FAIL de 5**, y el **0,973×** dice que el árbol nuevo salió **más rápido** que la línea base, así que el recorrido entero es varianza del instrumento y no coste. El remedio que CA-08 ya prescribía —intercalar y comprobar convergencia— **está implementado y no basta**: mide la dispersión **dentro** de cada brazo, y el ruido de la razón viene de las condiciones **entre** brazos; los dos rojos son justo los dos en que un brazo converge al **borde** (1,232× y 1,249× contra 1,250×) y sobre esa resolución el caso afirmó un 1,364×. Clase: **criterio derivado sin comprobar su factibilidad** —«al menos el factor que vigila» admitía margen y se tomó el valor **más flojo** sin medir si alcanzaba—, la misma de `REQ-014 CA-18` (400 líneas) y de `REQ-021 CA-08 (iii)` (4× → 6×), corregidos en la ventana anterior. Cambio **menor**: no se mueven el alcance, el techo ni la magnitud contratada; cambia la **regla de emisión** y lo que hay que ejecutar para acreditarla | — |
| 2026-09-08 | **CA-08 (ii) implementado: `k` DERIVADO MIDIENDO y el par discriminante ejecutado** (ningún criterio se toca; el techo **≤ 1,25×** queda **intacto**). `k = 4` repeticiones del par intercalado, cada una con su razón, veredicto por **unanimidad** y publicación de las k razones + recorrido + techo. **Cómo se derivó `k`, que es lo que el criterio manda:** 56 repeticiones reales —**56 válidas de 56 intentadas**, `estado=ok`, 0 descartadas— en **tres** entornos: máquina ociosa (20), con 5 vecinos del mismo tipo que el banco fabrica (20), y con **todo fijado a 2 CPU y 6 procesos encima** (16) para reproducir la **forma** del runner de la puerta requerida. El recorrido observado **crece con el tamaño de ventana y satura**, y `k = 4` es el **menor** tamaño cuya **peor** ventana ya alcanza el recorrido de la muestra entera en las **cuatro** series (2 CPU: **1,268× de 1,268×** y **1,262× de 1,262×**); con `k = 3` una de ellas veía **1,178× de 1,262×**, es decir **infradeclaraba su propio ruido**. `k = 1` —lo que había— no tiene recorrido: es la sonda ciega que produjo los dos rojos. **Coste medido y techo declarado:** banco entero con `JOBS=6`, mínimo de 3 vueltas, **49,19 s** sin la guarda y **77,19 s** con ella → **+28,0 s = 0,569×**, bajo un techo **operativo** de **0,750×** (dirección **bajar**, escrito como techo). **Cabe, y no se activa la salida del propietario.** Casos: 7 → **9** en `37/5` (par discriminante (a) y (b)); total del banco 884 → **886** | Implementación del write-back de CA-08 de esta misma fecha, comisionada por la coordinadora. **Tres límites declarados, porque son la diferencia entre acotar y acreditar:** (1) la contención de 2 CPU es una **emulación de la forma** del runner y **acota** el comportamiento bajo carga; **no** equivale al CI real, que es quien **decide** la puerta —el `1,40` de recorrido y los dos rojos vienen de `hooks-en-linux`, no de esta máquina, y aquí ninguna de las 56 razones cruzó el techo (máx. **1,087×**), así que `k` está derivado sobre la **saturación del recorrido** y no sobre la frecuencia del rojo, que localmente es **0**—; (2) el **techo de coste no lo vigila un caso** y se acredita en este Historial: el numerador sólo se mediría cronometrando la puerta **entera desde dentro de una de sus secciones** —circular—, y todo cociente local sale fijado por construcción en `(k-1)/1`, que no mide nada; (3) el forzador del par (a) es **sintético** —la vía que el criterio admite— y se elige sobre la contención real porque es **determinista**: una contención que unas veces dispersa y otras no sería un **rojo intermitente en la puerta requerida**, el modo de fallo que este REQ existe para quitar | — |
| 2026-09-08 | **Reapertura** (`AGENTS.md` §9, REGLA DE ESTADO): `Estado:` **completado → en-progreso**; `Versión destino:` **1.33.0 → 1.34.0**, conservando en el propio campo que 1.33.0 es la versión en que se publicó; `Archivos:` **+ `docs/qa/1.34.0.md`** (declarado **sin decoración**, porque `SEC-020` sigue abierto). Los veredictos de la cabecera **no se tocan** aquí: los re-emiten el `qa-tester` y el auditor en su turno, y el índice de `requirements/README.md` los refleja tal cual mientras sólo cambia el estado | El write-back de CA-08 de esta misma fecha cambia la regla de emisión de un caso que decide la **puerta requerida de `main`**: un REQ que cambia **re-recorre el ciclo** (dev → QA → auditoría), no se edita el texto y ya. `docs/qa/1.34.0.md` entra porque la ventana de validación pasa a ser **1.34.0** y el registro de ventana **no** lo cubre la regla de exclusión de `Notas / alcance` (write-back del 2026-09-07): sin declararlo, `tools/arnes-paralelo.sh` respondería `disjunto` **en falso** frente a los REQ de 1.34.0 que sí lo declaren —fail-open, el modo de fallo que el campo existe para evitar—. Consecuencia de coordinación, declarada: este REQ **colisiona** con todo REQ de la ventana que declare ese archivo | — |
| 2026-09-08 | **QA-017-16 (`contrato`) — cerrado en el código, y ningún criterio se toca.** *Antes:* la abstención por convergencia de brazo **retornaba con `R47` sin asignar** y la razón se calculaba **después** del retorno (`37/5:276` y `:278`), así que el SKIP citaba dos cifras y no tres: «… por encima de su propio techo 1.250×, y sobre eso no se firma». *Después:* la razón obtenida se calcula **antes** de abstenerse y se publica en **`MOT47`, nunca en `R47`** —`R47` no vacío significa «hay razón válida para juzgar» y así lo lee `veredicto08_47`, de modo que la abstención lo sigue dejando **vacío**—, y el mensaje vuelve a ser **byte a byte el de `19b1822^`**: «… por encima de su propio techo 1.250× — no puede distinguir una regresión de su ruido. **La razón que sí obtuvo es 1.600×**, y sobre eso no se firma» (comprobado con `cmp` sobre la misma entrada). El **umbral no se toca** (`TECHO47=1250`, `máx(ce,ch) > TECHO47`), ni `k=4`, ni el techo de coste 0,750×. *Y el segundo defecto, que es el que dejó pasar al primero:* la autoprueba de la cláusula (`37/5:349-366`) reduce cada salida a la **palabra** del veredicto con `sed -nE 's/^  (PASS\|FAIL\|SKIP)  .*/\1/p'` y descarta el mensaje, así que **nada del banco miraba el contenido**. Se añade **un caso** que llama a `razon08_47` —la función que decide, no una copia— y exige las **tres** cifras con valores elegidos **distintos entre sí y del techo** (1,400× · 1,020× · 1,600× frente a 1,250×), más que `R47` quede vacío. **Fail-before / pass-after:** contra el código de `19b1822` el caso da **FAIL** nombrando la cifra ausente; contra este árbol, **PASS**. Banco completo **882 PASS · 0 FAIL · 5 SKIP · rc 0**, total **887** (era 886), autoprueba del corredor 106 PASS · 0 FAIL y quality gates en verde. `CASOS_ESPERADOS_SECCION` 9 → 10 y `CASOS_ESPERADOS` 886 → 887; `PISO_AUTONOMO_SECCION` 411 → **448** porque el bloque indivisible crece 37 líneas (el caso nuevo ejerce la función que decide y no puede vivir fuera de él) — **y no se sube para comprar techo**: el archivo queda en **504** líneas, que ya cabían bajo el techo **anterior** de 514 | **QA-017-16**, vuelta 1 de 3 del ciclo abierto por la reapertura de §9 (`docs/qa/1.34.0.md`). `REQ-012 CA-06` cierra la vía de write-back: un criterio que el código no cumple **no se relaja**, se corrige el código. Los **tres artefactos** que QA midió afirmando algo falso —`REQ-017.md:80` «mismo umbral, mismo SKIP», `tests/escenarios/hooks/README.md:325-326` y la entrada del `CHANGELOG.md` de `19b1822`— **vuelven a ser verdaderos con el arreglo y NO se tocan**: el mensaje emitido es idéntico byte a byte al de `19b1822^`. **QA-017-17 sigue abierto** (`instrumento`, no bloquea): `veredicto08_47` continúa retornando en la primera repetición sin razón y las repeticiones 2..k siguen sin publicarse — el arreglo sólo hace que la repetición que aborta cite ahora **su** razón | — |
| 2026-09-08 | **Cierre del ciclo reabierto por §9**: `Estado:` **en-revisión → completado**, con `QA: aprobado` (2026-09-08, sobre `538c266`) y `Seguridad: aprobado` (`R-020`, 2026-09-08, sobre `538c266`) re-emitidos **sobre este árbol**. Ningún criterio se toca. Quedan **nueve** hallazgos abiertos y los nueve son `instrumento`, con sus dueños | El ciclo se recorrió entero en **una** vuelta: analista (write-back de `CA-08 (ii)`) → desarrollador (`19b1822`) → QA (`con-hallazgos`, siete hallazgos) → desarrollador (`538c266`, cierre de `QA-017-16`) → QA (`aprobado`) → auditoría (`R-020`). La firma de `R-012` que había en la cabecera era de **1.33.0** y `veredictos.caducan_con_codigo` está en `false`, de modo que la puerta **habría aceptado cerrar con ella**: el auditor la sustituyó y lo dejó escrito, porque una firma que no cubre el árbol que cierra es la clase de fallo en abierto que este REQ persigue. **Lo que este cierre NO acredita, dicho aquí y no sólo en el informe:** que `k = 4` resuelva en el runner de `hooks-en-linux` —todo lo medido es **local**— y el techo de coste `+28,0 s = 0,569×`, **acreditado por el desarrollador y no verificado por nadie** (`QA-017-20`, `QA-017-21`). Y `SEC-064`: con el ruido del instrumento (1,40) mayor que el techo que vigila (1,25), una regresión real en esa banda es **SKIP corrida tras corrida** y nada cuenta las abstenciones consecutivas | — |
