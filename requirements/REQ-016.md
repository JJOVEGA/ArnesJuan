# REQ-016 — La cabecera tiene noción de cita: un veredicto citado dentro de un comentario HTML cerró un REQ crítico sin auditoría aprobada
Estado: en-progreso (REABIERTO el 2026-09-10 por `AGENTS.md` §9 REGLA DE ESTADO. Forzador concreto, no genérico: sus `QA: aprobado` y `Seguridad: aprobado` del 2026-09-07 se emitieron sobre un `CA-11` **distinto del que hay hoy en el archivo** —el write-back de `QA-024-02` movió su puntero del sitio único—, así que **no acreditan el criterio vigente**. Los dos veredictos **NO se retiran**: desfirmar por otro es la misma falta que firmar por otro; los renueva quien revalide. Es la **segunda** de las tres obligaciones de `REQ-024 CA-03 (ii)`; falta la tercera, el re-recorrido. Aplicado por la coordinadora bajo la delegación de 24 h del propietario, igual que con `REQ-017`)
Módulo: hooks (lector de cabecera: lib.sh, campos-req.awk, guard-completado.sh) + tools/arnes-lectura.sh + tools/arnes-paralelo.sh + banco tests/escenarios/hooks + skills/arnes-upgrade + templates/
Archivos: hooks/lib.sh, hooks/campos-req.awk, hooks/guard-completado.sh, tools/arnes-lectura.sh, tools/arnes-paralelo.sh, tests/escenarios/hooks/secciones/36-cabecera-comentario-html-1-la-puerta.sh, tests/escenarios/hooks/secciones/36-cabecera-comentario-html-2-los-lectores.sh, tests/escenarios/hooks/secciones/36-cabecera-comentario-html-3-comentar-retira.sh, tests/escenarios/hooks/secciones/36-cabecera-comentario-html-4-el-informe-y-los-textos.sh, tests/escenarios/hooks/secciones/36-cabecera-comentario-html-5-el-cr-que-no-termina.sh, tests/escenarios/hooks/run.sh, tests/escenarios/hooks/README.md, skills/arnes-upgrade/SKILL.md, templates/AGENTS.md.tpl, AGENTS.md, CHANGELOG.md
Prioridad: alta
Sensible a seguridad: sí
QA: aprobado (re-recorrido de CA-11, 2026-09-10, a57eecc)
Seguridad: aprobado (R-009, 2026-09-07)
Hallazgos abiertos: H-07 (instrumento), QA-016-01 (instrumento), QA-016-02 (contrato)
Rigor: critico
Versión destino: 1.32.1
NFR relacionados: (ninguno)

## Historia
Como proyecto que confía la puerta de cierre al arnés, quiero que el lector de la cabecera de un REQ
distinga una **afirmación** de una **cita** —lo que se escribe dentro de un rango `<!-- … -->` no
declara campo—, para que documentar la historia de mis veredictos no pueda convertir un veredicto
histórico en el vigente y cerrar un REQ `critico` sin auditoría aprobada.

## Criterios de aceptación

- **CA-01 — El interior de un rango de comentario no declara campo, y lo dicen igual todos los
  lectores.** Dado un REQ cuya **cabecera** contiene un rango de comentario HTML cerrado, y dentro de
  él una línea que empieza por la clave de un campo de cabecera —decorada o desnuda—, Cuando cualquier
  lector de cabecera del arnés lee ese documento, Entonces el valor vigente de **cada** campo es
  exactamente el que leería sobre la misma cabecera **sin ese rango**, y los dos lectores devuelven
  valores **idénticos** campo a campo sobre el mismo documento. El conjunto de campos de cabecera y la
  regla de lectura viven en **un solo sitio** —el lector de `hooks/lib.sh` (`arnes_norm_clave` y el
  bucle de cabecera que lo usa)—; `hooks/campos-req.awk` es su **transcripción declarada** y no una
  segunda regla. Ejemplos **no exhaustivos** de campos afectados: `Estado:`, `Seguridad:`.

- **CA-02 — Un comentario sólo puede estrechar el juicio de la puerta, nunca abrirlo — y la
  invariante rige para el rango que AÑADE, no para el que ENVUELVE lo que ya estaba.** Dado
  cualquier cabecera del corpus de cabeceras del banco (los casos que el corredor descubre por glob
  en su directorio de secciones, que es el sitio único de ese corpus), Cuando se le añade un rango de
  comentario **cuyo interior no contiene ninguna declaración de campo que ya estuviera en esa
  cabecera** —contenido nuevo, o contenido que no declara ningún campo—, Entonces **no existe**
  ningún caso en que un `deny` de la puerta de cierre se convierta en `allow`. La dirección admitida
  es la contraria: un comentario puede hacer que un REQ **deje** de poder cerrarse, y eso es
  conforme. Y el ejercicio de esta propiedad **no acredita nada si ninguna cabecera base produce
  `deny`**: la comprobación exige que el corpus contenga **al menos una** base que deniegue y aborta
  si no, porque «ningún `deny` se volvió `allow`» es cierto por vacío cuando no hay ninguno.

  **Lo que esta invariante NO cubre, dicho aquí y no en una nota:** el rango que **envuelve** una
  declaración que ya existía no añade una cita, **retira** una declaración; su conducta la fija
  **CA-11**. La acotación es de esa clase y **sólo** de esa: cualquier otra vía por la que un `deny`
  se vuelva `allow` sigue **incumpliendo** este criterio y no se acota para que encaje (ejemplo **no
  exhaustivo**: un delimitador de comentario que el lector **fabrique** a partir de un carácter que
  descuenta antes de escanear el rango).

- **CA-03 — Un rango que abre y no cierra es una cabecera que no se puede medir, y una puerta que no
  puede medir no deja pasar.** Dado un REQ cuya cabecera abre un rango de comentario que no se cierra
  antes del fin de la cabecera, Cuando se intenta la transición al estado terminal, Entonces la puerta
  responde **DENY** citando el rango sin cerrar como motivo, y **nunca** permite por **ausencia** del
  campo que el rango dejó fuera de lectura.

- **CA-04 — La tolerancia de clave decorada sigue gobernando fuera de los rangos: este REQ no la
  restringe.** Dado un REQ cuya cabecera declara un campo con la clave decorada o sangrada **fuera** de
  todo rango de comentario, Cuando la puerta lo lee, Entonces ese campo **gobierna** igual que antes de
  esta versión —no se exige la clave a columna cero, ni desnuda, ni en ninguna posición— y, en
  particular, una cabecera cuyo estado terminal está escrito sólo en forma decorada sigue siendo
  **vista** por la puerta y **denegada** cuando los veredictos no la autorizan.

  **Frontera —dónde empieza a aplicarse la tolerancia, no una excepción a ella: opera sobre una
  cabecera MEDIBLE.** Lo que este criterio tolera es la **decoración** de la clave (énfasis,
  sangría, blancos) en una línea que la puerta puede leer. Una línea con un **retorno de carro
  interior** no lo es, y su decisión la fija **CA-12** (DENY por medibilidad) aunque lo que el CR
  parta sea precisamente la clave. **Y la acotación es de esa clase y sólo de esa:** ninguna otra
  forma decorada o sangrada fuera de los rangos se restringe, y cualquier forma decorada que dejara
  de gobernar seguiría **incumpliendo** este criterio.

- **CA-05 — El informe avisa de la línea decorada que gobierna, aunque su valor sea impecable.** Dado un
  REQ en el que el valor **vigente** de un campo lo manda una línea con la clave decorada, y Dado que
  ese valor pertenece al vocabulario del campo, Cuando corre `tools/arnes-lectura.sh`, Entonces el
  informe lo nombra: REQ, campo, la línea **tal como está escrita**, el valor que la máquina lee y la
  consecuencia. Hoy el informe sólo habla cuando el valor resulta **ilegible**, que es justamente el
  caso que ya falla cerrado.

- **CA-06 — Y separa el aviso de forma del hallazgo, para no volver a enterrar lo real bajo lo
  inofensivo.** Dado un proyecto que escribe sus cabeceras decoradas a propósito, Cuando corre el
  informe, Entonces el aviso de CA-05 **no** altera el código de salida por sí solo; y Dado que en la
  cabecera existe **otra** declaración del mismo campo y la que gobierna es la decorada, Entonces eso
  **sí** se reporta como anomalía por la vía única por la que el informe ya reporta anomalías, con
  salida **≠ 0**, porque entonces el documento dice dos cosas y la máquina elige una sin avisar.

- **CA-07 — Un caso de banco que reproduce el fail-open, con fail-before y controles en las dos
  direcciones.** Dado el banco, Cuando corre la sección nueva, Entonces contiene un caso que ejerce
  `hooks/guard.sh` sobre un REQ `critico` cuyo veredicto de seguridad vigente **no** autoriza el cierre
  y cuya cabecera cita un veredicto autorizante dentro de un comentario, y ese caso **falla contra los
  hooks de la versión heredada** (fail-before) y **pasa** contra los de esta versión; y Entonces el
  mismo caso trae sus dos controles: (i) el REQ que **debe** denegar deniega citando el **veredicto
  vigente** —y el motivo citado **no depende** del carácter de énfasis con que se escribió la cita, que
  hoy decide si el fallo cae del lado que cierra o del que abre—, y (ii) el REQ cuyo veredicto vigente
  **sí** autoriza el cierre **cierra**, aunque su cabecera contenga la misma cita.

- **CA-08 — El coste de la lectura no sube.** Dado el camino de lectura de la cabecera, Cuando se le
  añade la noción de cita, Entonces gasta **no más de** ningún proceso añadido por REQ respecto a la
  línea base de la versión heredada (**operativo**: **menos** es conforme y **no** es hallazgo; la
  medición fechada va al Historial de este REQ). El lector sigue trabajando por expansión de parámetros
  en la puerta y en la misma pasada única en el informe derivado.

- **CA-09 — La instrucción de auditoría para quien ya corrió una versión afectada, y que declara su
  propio límite.** Dado `### Hacia 1.32.1` de `skills/arnes-upgrade/SKILL.md`, Cuando un proyecto lee
  su migración, Entonces encuentra dicho, sin eufemismo, que **pudo cerrar un REQ `critico` sin
  auditoría aprobada**; que la pertenencia de versiones afectadas **no es una lista escrita a mano**
  —se decide por el historial del lector de cabecera, con el comando que lo comprueba tag a tag—; y
  las dos cosas siguientes, que son **separables y se comprueban por separado**:

  **(i) Lo que hay que auditar se enuncia por ESTADO, no por vía.** El texto enuncia la propiedad
  —**cuáles de mis REQ en estado terminal NO cerrarían hoy**, leídos con el lector de esta versión—
  **antes** de ofrecer ningún comando, y deja dicho que ésa, y no la presencia de una forma concreta
  de escritura, es la pregunta que acredita ausencia de exposición: es una discrepancia entre «está
  cerrado» y «hoy no cerraría», y por eso **no envejece con la vía siguiente que alguien descubra**.

  **(ii) Todo barrido por vía declara qué NO encuentra, y que su silencio no acredita nada.** Cada
  comando que el apartado ofrezca va acompañado, **en el mismo sitio que el comando**, de tres cosas:
  que interroga **una vía** y no la propiedad de (i); el **nombre** de las vías conocidas que ese
  comando no encuentra; y, literal, que **no hallar nada no acredita ausencia de exposición**. Y
  ninguna frase del apartado afirma completitud sobre un barrido por vía —«son los únicos que pudieron
  verse afectados» y cualquier equivalente—: el motivo es que un comando que describe una vía no puede
  darla, así que la prohibición **no se acota al patrón de hoy**, y cualquier frase que prometa
  completitud sobre una vía incumple este criterio, con el patrón que sea. Las vías conocidas al
  publicar la versión viven en **un solo sitio** —`docs/seguridad/registro-seguridad.md`, SEC-024 y
  SEC-025—, que es de donde el texto las toma; ejemplos **no exhaustivos**: el delimitador de apertura
  **fabricado** y la **clave fabricada**.

  **Lo que este criterio NO exige hoy, dicho aquí y no en una nota:** la **existencia** de la
  comprobación por estado —un modo que responda «REQ en estado terminal que hoy no cerrarían»— no se
  contrata aquí; es de clase `instrumento`, con dueño (`desarrollador`) y ventana (1.33.0) en «Fuera
  de alcance». Lo que sí se exige hoy es que el texto **remita** a ella y diga que **ningún comando
  del apartado la responde todavía**. **Y la acotación es de esa clase y sólo de esa:** no amnistía
  ninguna otra frase — cualquier promesa de completitud en la superficie que los proyectos heredan
  sigue **incumpliendo**, y se corrige en el texto, no ensanchando esta excepción.

- **CA-10 — La regla heredable: la invariante manda sobre cualquier preferencia de herramienta.** Dado
  `templates/AGENTS.md.tpl` —además de `AGENTS.md` §13—, Cuando un proyecto nace del andamiaje o lo
  actualiza, Entonces esa frase está escrita **con su motivo**: una preferencia de sesión por la
  consola frente a las herramientas de edición desactiva una puerta sin que nadie relacione las dos
  cosas. Va en la plantilla y no sólo en §13 porque **quien configura una sesión no suele ser quien lee
  §13**.

- **CA-11 — Comentar una declaración la RETIRA, y un campo ausente se perdona MIENTRAS la
  exigencia no esté activada, salvo `Seguridad:` en un REQ de rigor efectivo `critico`.** Dado un REQ cuya cabecera declara un campo de veredicto,
  Cuando esa declaración queda **dentro** de un rango de comentario cerrado —en el disco, en el
  contenido entrante o en los dos—, Entonces la puerta de cierre decide **exactamente lo mismo** que
  si esa línea se hubiera **borrado**: el campo queda **ausente** para todos los lectores. **La
  equivalencia es el criterio**, y es lo que se comprueba: cualquier par (línea comentada / línea
  borrada) que produzca decisiones distintas incumple — y lo es en **los dos** estados de la llave
  del párrafo siguiente, porque lo que este criterio contrata es que la **vía** de la desaparición
  no cambie la decisión, no cuál sea esa decisión.

  **Qué hace la máquina ante esa ausencia depende de un estado declarado, y el criterio lo dice en
  vez de prometer sin condición.** **Mientras el proyecto no haya activado** la exigencia de la
  dirección de la ausencia —vía, llave y defecto los fija **`ADR-009`** (`campos.ausencia_exige`,
  que nace **apagada**; que esta versión no cambie ese defecto lo contrata **`REQ-024 CA-05`**)—, la
  ausencia se **perdona**, por compatibilidad con los REQ anteriores a que los veredictos
  existieran. **Activada**, la ausencia deja de abrir: **gobierna** (se aplica el valor que más
  restringe) o **deniega** nombrando el campo que falta, según la dirección declarada para ese campo
  (**`REQ-024 CA-01`**). **Y la excepción está medida y también es criterio:** en un REQ cuyo
  **rigor efectivo** es `critico`, la ausencia de `Seguridad:` **no** se perdona **con la llave
  apagada**, así que envolver su `Seguridad: pendiente` en un comentario responde **DENY** —no
  cierra— igual que borrarla; con la llave **activada** ese campo declara `deniega` en el sitio
  único, así que la conducta no se relaja.

  **El sitio único, nombrado por PROPIEDAD y no por archivo de conveniencia.** Qué hace la máquina
  cuando un campo de cabecera **no llega a declararse** —comentado, borrado o nunca escrito— lo
  decide **un solo sitio**, y ahí vive la **lista exhaustiva** de campos con su dirección: la **tabla
  de la dirección de la ausencia de `hooks/lib.sh`** —la constante que la declara y la función que la
  consulta—, que es el sitio único que **`REQ-024 CA-02`** contrata y **`ADR-009`** decide. Se nombra
  por propiedad porque reescribir el despacho de claves no lo cambia; y **cambiar de sitio exige
  `ADR` y write-back en este criterio**, que es lo que `REQ-024 CA-02` declara **de contrato**.
  **Ya no es `hooks/guard-completado.sh`, y seguir ese puntero al pie de la letra deja fuera
  precisamente los campos peligrosos:** en ese archivo viven las comprobaciones de `QA:` y de
  `Hallazgos abiertos:`, y **no** las de `Sensible a seguridad:` ni `Rigor:` —los dos que retiran el
  **suelo** de sensibilidad y el **nivel** de ceremonia—, que están en `hooks/lib.sh`. Eso es el
  puntero falso que **`SEC-050`** midió, y su cuenta la **publica el banco en cada corrida**
  (`REQ-024 CA-03`): no se escribe aquí porque **sube o baja con cada campo del lector** y no es
  umbral de nada. Ejemplos **no exhaustivos** de campos de la clase: `QA:`, `Hallazgos abiertos:`.

- **CA-12 — Una línea de cabecera con un retorno de carro que no es el que la termina deja una
  cabecera que no se puede medir, y una puerta que no puede medir no deja pasar.** Dado un REQ cuya
  **cabecera** contiene al menos una línea con un retorno de carro **interior** —cualquiera que no
  sea el único CR final que termina esa línea—, Cuando se intenta la transición al estado terminal,
  Entonces la puerta responde **DENY** citando **esa línea** en el motivo, con el CR representado de
  forma imprimible (un CR crudo en el motivo se pisa a sí mismo en cualquier terminal). El criterio
  se enuncia sobre lo que la **cabecera contiene**, no sobre en qué función del lector vive la
  guarda: reescribir el escáner no lo cambia.

  **Y la denegación es por MEDIBILIDAD, no por veredicto — esto es lo que se comprueba, no un
  matiz.** Un REQ con **todos** los veredictos en verde y su estado terminal escrito
  `Estado: comple`+CR+`tado` **deniega igual**, y el motivo es la línea no medible. Cerrar
  **incumple**; y denegar alegando que el estado no es terminal, o que el campo falta, **también
  incumple**: eso resuelve el caso como **ausencia**, y la ausencia es justo lo que la puerta
  perdona. Las dos caras conocidas son **la misma propiedad y no dos reglas** (ejemplos **no
  exhaustivos**): el **delimitador fabricado** —`<!`+CR+`--`, `-`+CR+`->`, que aparenta abrir o
  cerrar una cita— y la **clave fabricada** —`Seg`+CR+`uridad: aprobado`, que no necesita rango
  ninguno—. La lista exhaustiva de caracteres de control que el lector de cabecera trata, y el
  tratamiento de cada uno, viven en **un solo sitio**: `hooks/lib.sh`.

  **Lo que este criterio NO restringe, dicho aquí y no en una nota:** (i) el CR que **termina** la
  línea es **transporte** y no cuenta — un documento entero en CRLF decide **idéntico** a su gemelo
  en LF, campo a campo y decisión a decisión; exigir lo contrario **incumple** este criterio, porque
  convertiría una guarda en una prohibición de Windows; (ii) el CR **fuera** de la cabecera no lo
  mira nadie: el cuerpo del REQ no queda restringido; (iii) la guarda cierra **sólo** el cierre —
  una cabecera no medible **no** impide **reabrir** un REQ ni salir del estado terminal. **Y la
  acotación es de esa clase y sólo de esa:** cualquier otra vía por la que una cabecera no medible
  deje **cerrar** sigue **incumpliendo**, y se corrige en el código, nunca ensanchando esta
  frontera. Este criterio habla de la **puerta de cierre**; que el bloque derivado publique el
  veredicto fabricado sobre una cabecera que la puerta se niega a medir está registrado como
  residual de clase `instrumento` (`docs/seguridad/registro-seguridad.md` § SEC-024, dueño
  `desarrollador`, ventana 1.33.0) y **no** se contrata aquí.

*(12 criterios.)*

## Notas / alcance

### El defecto, y por qué las tres reglas que lo producen son correctas por separado
Un REQ `critico` cuyo veredicto de seguridad vigente **no** autoriza el cierre **cierra** si en su
cabecera hay un rango `<!-- … -->` que contenga una línea que empiece por la clave del campo, decorada
con negrita, y un valor autorizante — incluso si el comentario dice literalmente que es histórico. Sale
de la suma de tres reglas que ninguna está mal:

1. la **tolerancia de énfasis en la clave**, incorporada en la versión donde nace la regresión;
2. que estos campos toman la **última** aparición de la cabecera (sólo el estado toma la primera);
3. que el lector **no tiene noción de cita**: un rango de comentario no se excluye.

Juntas: cualquier línea de la cabecera que **empiece** por la clave —viva donde viva— se convierte en
el veredicto vigente. Y **la dirección del fallo depende del carácter de énfasis**: cuando el par de
énfasis cruza los dos puntos, su cierre queda al principio del valor y el desenvoltorio lo limpia, así
que el valor sale legible y **gobierna**; cuando el marcador de cierre queda al final, el valor queda
sucio, ilegible, y el REQ **deja de poder cerrarse**. Es decir: hoy el arnés falla cerrado o abierto
según cómo se escribió una nota, y las formas que se salvan lo hacen **por accidente del
desenvoltorio, no por una comprobación** (de ahí el control (ii) de CA-07).

**El sitio del fallo lo empeora.** El lugar donde un proyecto disciplinado escribe *«este veredicto es
histórico y no es el vigente»* es precisamente un comentario HTML: la convención que existe para no
confundir a la máquina es la que la confunde, y **quien mejor documenta la historia de sus veredictos
se expone más**.

**Y es la cuarta instancia de una lección propia** (`AGENTS.md` §13, `ADR-002`, SEC-020): cuando un
mecanismo interpreta texto humano libre, **ensanchar la tolerancia no gana la clase**. Aquí no se
ensanchó un patrón: se ensanchó la tolerancia de una **clave**, y el efecto es el mismo. La salida es
una **pregunta cerrada** —el rango del comentario está delimitado— y no más tolerancia.

### Decisión sobre las propuestas descartadas, declarada
**(b) exigir la clave a columna cero y sin decorar para que la línea CUENTE — NO se incluye.** Motivo:
la tolerancia de clave **cerró un fail-open real** —un estado terminal escrito en forma decorada que
ninguna puerta veía— y (b) lo **reabre** por construcción: un campo declarado sólo en forma decorada
volvería a ser invisible para la puerta, y una cabecera invisible no se deniega, se ignora. Lo que falta
no es la tolerancia: es **acotar dónde se aplica**. CA-04 fija esa frontera como criterio para que
nadie la recorte por el camino.

**(c) que una aparición decorada no pueda desbancar a una limpia a columna cero — NO se adopta como
regla del lector; su discriminante se adopta como reporte (CA-06).** Tres motivos, en orden de peso:

1. **No cierra la clase.** (c) sólo actúa cuando existe una rival limpia. Un campo que la plantilla
   permite **omitir** —el rigor, los hallazgos abiertos— no tiene rival, así que una cita dentro de un
   comentario seguiría gobernándolo. (a) lo cierra en todos los casos.
2. **Cambia la semántica de documentos bien formados** para arreglar uno malformado: introduce una
   precedencia nueva —«limpia gana a decorada»— que se aplica a todo REQ, incluidos los que no tienen
   ningún comentario, y hace que **la misma línea signifique cosas distintas según si existe otra**.
   Esa dependencia del contexto es exactamente lo que hizo este fallo difícil de anticipar.
3. **Cuesta una transcripción más.** La regla tendría que vivir idéntica en los dos lectores; el
   defecto que este arnés existe para cazar es que **el informe lea distinto de la puerta**, y cada
   regla duplicada es una divergencia futura.

Lo que sí vale de (c) es su **discriminante**: «hay dos declaraciones y gobierna la decorada» es una
señal barata y precisa de que el documento dice dos cosas. Se adopta **como reporte, no como
precedencia** (CA-06): defensa en profundidad sin una segunda semántica en la puerta. Y hay residual
que la justifica: tras (a), una línea decorada **fuera** de todo comentario —producida por el **corte
de un párrafo**, no por su contenido— sigue pudiendo gobernar. Eso es forma legítima (CA-04) y no se
prohíbe: se hace **visible** (CA-05).

### La clase del defecto: `contrato`, y por qué no `instrumento`
Se declara **`contrato`**. Es un defecto del propio arnés, y aun así no es `instrumento`: la clase
`instrumento` existe para que un defecto de un **control** no impida cerrar la **función de negocio**
que vigila — y aquí la puerta **es** la función de negocio entregada. La promesa documentada («un REQ
`critico` no cierra sin `Seguridad: aprobado`») es **falsa sobre lo construido**, que es la definición
de `contrato` en `requirements/README.md`; y bloquea el cierre hasta el write-back, que es el efecto
correcto.

**La consecuencia útil de esto —y es una regla, no un caso—: la clase de un defecto del arnés depende
de quién es el dueño del mecanismo.** El mismo defecto, reportado por un proyecto consumidor contra un
REQ **suyo** de negocio, es `instrumento` para él y no le bloquea el cierre; reportado **en este
repositorio**, donde el mecanismo es el producto, es `contrato`. No hay contradicción: es el mismo
criterio aplicado a dos dueños distintos.

No es `usuario/dinero` porque nadie ve, decide ni cobra distinto **por el valor** de este campo; la
decisión humana que se apoya en él (fusionar, publicar) queda cubierta por `contrato`, y las dos clases
bloquean igual, así que la elección no relaja nada.

### Medición del reportante, y lo que NO sirve como sonda
La regresión llega **bisecada**, ejecutando el guardián de cuatro versiones instaladas contra el mismo
payload y el mismo fixture (tabla fechada en el Historial). Dos detalles que ahorran una tarde a quien
reproduzca:

- La variable de entorno que fija el directorio del proyecto tiene **prioridad** sobre el `cwd` del
  payload. Sin apuntarla al fixture, el guardián resuelve el proyecto **real**, el REQ de prueba le
  queda fuera y **permite todo**: la sonda parece pasar sin haber ejercido nada.
- Una cita en la que la clave **no encabeza** la línea —dentro de una frase, de un elemento de lista o
  de una celda— **no se lee** y por tanto **no sirve como sonda**: no demuestra ni el fallo ni su
  arreglo. El caso tiene que poner la clave al principio de la línea.

En el barrido de un proyecto de uso diario apareció **una sola** anomalía de esta familia en todo su
corpus, y **en la dirección que falla cerrado**; la causó el **corte del párrafo**, no su contenido —el
ajuste de línea dejó la clave al principio de una línea—, y se corrigió reformulando el comentario
**sin tocar ninguna línea de veredicto**.

### Coordinación
Este REQ **colisiona** con REQ-015, y desde el 2026-09-07 los dos campos `Archivos:` lo dicen en la
forma que la máquina lee: los archivos compartidos son **siete** — `hooks/lib.sh`,
`tests/escenarios/hooks/run.sh`, `tests/escenarios/hooks/README.md`, `AGENTS.md`,
`templates/AGENTS.md.tpl`, `skills/arnes-upgrade/SKILL.md` y `CHANGELOG.md` —, así que **no se
despachan en paralelo** y corrieron en serie. El campo está escrito sin decoración de Markdown a
propósito mientras SEC-020 siga abierto: con cada elemento decorado la herramienta responde
`disjunto` con rc 0 sobre rutas que no existen.

### Fuera de alcance
- La regla de **última aparición** de los campos y la de **primera** para el estado: se conservan tal
  cual. Este REQ acota **dónde** se lee, no **cuál** gana.
- La puerta **posterior** (REQ-011) y la gramática restringida del campo `Archivos:` (SEC-020): son de
  diseño, van en su ventana y no tienen relación con esto. Colgar un fail-open de una ventana de diseño
  lo deja abierto mientras se decide otra cosa.
- **La comprobación por ESTADO que acredita ausencia de exposición: fuera de esta ventana, con dueño
  y ventana — y por eso CA-09 exige que el skill la NOMBRE, no que exista.** Un modo de
  `tools/arnes-lectura.sh` que responda «de mis REQ en estado terminal, cuáles **no cerrarían hoy**»
  es el control de verdad: cubre la cita, el delimitador de apertura fabricado, la clave fabricada,
  un veredicto que en versiones viejas vivía dentro de `## Historial de cambios` y **las vías que
  nadie ha descubierto todavía**, porque no describe ninguna vía. Y es barata: el lector ya existe
  (`arnes_campos_req "$disco" ""` da el juicio sobre el documento tal como está en el disco).
  **Dueño:** `desarrollador`. **Ventana:** 1.33.0, junto a SEC-020/SEC-021 y REQ-011, donde ya vive
  el trabajo sobre lectores y campos. **Clase:** `instrumento` (SEC-025, remediación 2) — y ésa es la
  razón de fondo por la que no se cuelga de aquí: un defecto de un **control** no puede ser condición
  para cerrar (`AGENTS.md` §6), y exigirlo dentro de CA-09 le subiría la clase a bloqueante por la
  puerta de atrás, sin la firma de nadie, dejando además el fail-open vivo de SEC-024 esperando a una
  herramienta nueva.
- Ninguna llave nueva en `.arnes/config.json`, ninguna migración de archivos de proyecto: hacia esta
  versión no hay **ninguna** operación de andamiaje que aplicar; lo único que un proyecto hereda que
  exige acción humana es la **auditoría** de CA-09.
- **Que el informe nombre un campo que existe SÓLO citado: fuera de esta ventana, con dueño y
  ventana — y por eso ningún criterio de aquí promete esa visibilidad.** Medido (H-03): sobre un REQ
  cerrado cuya cabecera dice, en letra, `Hallazgos abiertos: SEC-9 (usuario/dinero)` dentro de un
  comentario, `tools/arnes-lectura.sh` responde «Ningún valor anómalo» con `rc=0`. No es un fallo de
  CA-05 ni de CA-06: CA-05 habla de claves **decoradas** y CA-06 de **dos** declaraciones del mismo
  campo, y un campo declarado **sólo** dentro de un comentario no es ninguna de las dos. La
  conducta de la puerta queda **contratada** por CA-11, que es lo que el write-back necesitaba; hacer
  además que el informe hable es una **mejora de defensa en profundidad**, no la corrección de una
  promesa falsa, y colgarla de esta ventana dejaría el fail-open de CA-02/CA-03 (H-01) esperando a
  una decisión de diseño — exactamente lo que este mismo apartado prohíbe dos puntos más arriba.
  **Dueño:** `analista-requerimientos` (REQ propio). **Ventana:** 1.33.0, junto a SEC-020/SEC-021 y
  la puerta posterior (REQ-011), que es donde ya vive el trabajo sobre lectores y campos. **Y el
  discriminante se deja escrito para que no haya que redescubrirlo, porque es la parte difícil:** lo
  anómalo **no** es que existan veredictos citados —esa es justamente la práctica que este REQ
  protege, y avisar de ella enterraría lo real bajo lo inofensivo, que es lo que CA-06 existe para
  evitar—, sino que la **única** declaración de un campo viva dentro de un comentario: entonces el
  documento afirma un valor en letra y la máquina lee **ausencia**. Ése es el aviso, y sólo ése.

## Preguntas abiertas / conflictos

Las dos decisiones abiertas del insumo, (b) y (c), quedan resueltas y argumentadas arriba. Y al
escribir **CA-11** aparece un conflicto que **no se resuelve dentro de este REQ** y por eso se
registra, en vez de dejarlo implícito:

**CA-11 (nuevo) contradice, en letra, dos textos heredados que prometen sin condición.**
`requirements/README.md` («El hook `guard-completado` **impide** marcar `completado` sin
`QA: aprobado`») y la fila equivalente de `AGENTS.md` §13 («No completar sin `QA: aprobado`, salvo
`Rigor: ligero`») se leen como incondicionales; medido por QA (H-03, control de D1/D2), un REQ que
**no declara** el campo `QA:` **cierra**, porque la ausencia se perdona por compatibilidad con los
REQ anteriores a que los veredictos existieran. CA-11 escribe esa conducta real; los dos textos
heredados siguen diciendo lo contrario, y son superficie que los proyectos heredan por
`arnes-upgrade` — la misma clase que REQ-015 CA-08 y la regla de `ADR-002`: **ninguna frase más
ancha que lo que el código hace**.

**Lo que NO se hace aquí, y por qué:** la salida son dos líneas de prosa («de los REQ que **declaran**
el campo») o una puerta más estricta, y las dos caen en `AGENTS.md` y `requirements/README.md`, que
en esta ventana son alcance de las comisiones de **desarrollo** de REQ-015 (CA-08) y REQ-016 (CA-10).
Decidirlo por mi cuenta añadiría trabajo de desarrollo a la vuelta 1 por un defecto que **no
introdujo esta ventana** —es la regla de compatibilidad de siempre— y dejaría el fail-open de H-01
esperando. **Queda para la coordinadora:** o entra como corrección de prosa en esta ventana, o va a
1.33.0 con el mismo dueño y ventana que el residual de visibilidad de «Fuera de alcance». **No
bloquea el ciclo de este REQ**: la divergencia es entre un texto heredado y una conducta medida, y
CA-11 es lo que la deja de tapar.

**Pendiente de ejecución por la coordinadora (2026-09-10): la reapertura de este REQ y el
re-recorrido de su ciclo.** `REQ-024 CA-03 (ii)` dice, **literalmente**, que si el sitio único es
otro «**`REQ-016` vuelve a `en-progreso`**, su CA-11 **se reescribe** con el puntero real y **el
ciclo se re-recorre** (`AGENTS.md` §9)»: eso son **tres** obligaciones sobre una **premisa** («el
sitio único es otro»), que ya es cierta y **no** es una obligación de nadie —la cumplió el código,
`ARNES_AUSENCIA` en `hooks/lib.sh`—. La tabla de `QA-024-02` cuenta la premisa **como** la primera
obligación y omite el re-recorrido del ciclo, y de ahí su «sólo una está hecha»; leído al pie de la
letra, antes de hoy estaban hechas **cero** de las tres, y hoy queda hecha **una**: la reescritura
del puntero (fila del 2026-09-10 del Historial). Las otras dos —**volver a `en-progreso`** y
**re-recorrer el ciclo**— siguen pendientes, y en ese orden: la segunda no puede empezar antes de la
primera. **Ninguna de las dos la ejecuta el analista:** este REQ sigue `Estado: completado` con
`QA: aprobado` y `Seguridad: aprobado`
del 2026-09-07, veredictos que se emitieron sobre un CA-11 **distinto del que hay hoy en el
archivo**, así que **no acreditan** el criterio vigente. Mi lectura, escrita para que la decisión no
sea implícita: **sí reabre**, por `AGENTS.md` §9 —«cuando un REQ ya `completado` cambia, vuelve a
`en-progreso` o `en-revisión` y **re-recorre el ciclo**»—, porque lo que cambió no es prosa
explicativa sino **lo que el criterio contrata**: el sitio que otros criterios citan como lista
exhaustiva y la condición bajo la que la ausencia se perdona. Es el mismo motivo por el que se
reabrió `REQ-017` en esta misma ventana. **Quien lo ejecuta:** la coordinadora, que aplica el
cambio de `Estado:`, devuelve `QA:` y `Seguridad:` a `pendiente` y lo registra; hasta entonces este
REQ está **cerrado sobre un criterio no validado**, y esa es la mitad de `QA-024-02` que el
write-back **no** puede cerrar por sí solo.

## Trazabilidad
Origen: informe de regresión de un proyecto consumidor (2026-09-07), ejercido ejecutando el guardián
contra un fixture aislado con controles en las dos direcciones y con la regresión bisecada entre
versiones instaladas. Confirma además, de forma independiente, el agujero del intérprete ya medido en
este repositorio.
Tocado por: analista-requerimientos / 2026-09-07
Tocado por: analista-requerimientos / 2026-09-10 (write-back de `QA-024-02`: el puntero de CA-11)

## Historial de cambios
| Fecha | Antes → Después | Causa | ADR |
|------------|-----------------|-------------------------|--------|
| 2026-09-07 | (creación) | Regresión reportada por un proyecto consumidor: fail-open en la puerta de cierre por una cita de veredicto dentro de un comentario HTML en la cabecera | — |
| 2026-09-07 | Bisección medida por el reportante (referencia de CA-07, fail-before): 1.30.2 **deniega** · 1.30.3 **deniega** · 1.31.0 **permite** · 1.32.0 **permite** (hereda: sus `hooks/` son byte a byte los de 1.31.0). Mismo payload, mismo fixture, cuatro guardianes instalados en la misma máquina | Fijar la línea base contra la que el caso de banco debe fallar antes del arreglo | — |
| 2026-09-07 | Informe de lectura sobre el caso que abre la puerta: **rc 0**, «ningún valor anómalo» (referencia de CA-05: el caso peligroso pasa callado; los que ya fallan cerrado sí se reportan) | Justificar (d) con la medición, no con la impresión | — |
| 2026-09-07 | `pendiente` → `en-revisión` (implementación completa: los 10 criterios) | Implementación de REQ-016 por el `desarrollador` | — |
| 2026-09-07 | Fail-before medido contra el árbol de REQ-015 (el heredado en el índice): **20 FAIL de 43** casos nuevos, y **ningún** caso marcado «(era ALLOW)» pasa. Pass-after: banco completo **790 PASS · 0 FAIL · 1 SKIP** (el SKIP es el caso de rutas Windows, sin `cygpath`), `autoprueba-corredor.sh` **73 PASS · 0 FAIL** | Acreditar CA-07 (fail-before/pass-after) con número y no con impresión | — |
| 2026-09-07 | CA-08 medido en el **mismo camino de decisión** (mismo REQ, misma decisión `allow`, 20 llamadas por lado, binarios instrumentados en el `PATH`): **5 procesos por llamada antes y 5 después**; 92 ms → 95 ms, dentro del ruido. El bloque derivado sigue en **una** pasada de `awk` con citas y sin ellas | CA-08 exige «no más de ningún proceso añadido»; comparar un `deny` contra un `allow` mediría la decisión y no el lector | — |
| 2026-09-07 | CA-02 ejercida sobre el corpus: **58 cabeceras** cosechadas por glob del directorio de secciones × 3 posiciones = **174 variantes**; ningún `deny` se volvió `allow`. El banco pasa de **14 s a 32 s** de reloj; el producto cartesiano completo (614 variantes) costaba **61 s** sólo en esa sección y se descartó | Dejar dicho el coste de la invariante y por qué se reparten las formas del rango en vez de multiplicarlas | — |
| 2026-09-07 | **Desviación del campo `Archivos:`, declarada:** la sección nueva se parte en `36-cabecera-comentario-html-1-la-puerta.sh` (22 casos) y `36-cabecera-comentario-html-2-los-lectores.sh` (21 casos), con la convención `-N-` que ya usan las secciones 28 y 33 | `autoprueba-corredor.sh` (CA-18) exige que ningún archivo de sección pase de 400 líneas y la sección entera salía en 591. Es desviación de **nombre**, no de alcance | — |
| 2026-09-07 | **Alcance añadido y declarado:** `tools/arnes-paralelo.sh` pasa a leer su campo con el lector único (`arnes_campo_linea`). No está en el campo `Archivos:` | CA-01 habla de **cualquier** lector de cabecera del arnés, y ése lo es: sin el cambio, un `Archivos:` citado dentro de un comentario seguía gobernando el reparto — una divergencia nueva entre dos lectores, que es el defecto que este REQ cierra | — |
| 2026-09-07 | **Lo que CA-02 no cubre, medido y escrito:** *comentar* una línea de veredicto que ya existía no es «añadir un rango» sino **retirar una declaración**, y un campo ausente es lo que la puerta perdona por compatibilidad; y un `## ` dentro de un rango sigue terminando la cabecera (regla estructural de la sección 14), así que lo que venga detrás no es cabecera para ningún lector y no cierra el REQ | Que la invariante no se lea de más: las dos fronteras tienen su caso nombrado en el banco y su párrafo en `docs/qa/1.32.1.md` | — |
| 2026-09-07 | **CA-02 acotada y la conducta aceptada SUBIDA A CRITERIO (CA-11, nuevo).** Antes, CA-02: «Cuando se le añade un rango de comentario en la cabecera **con cualquier contenido**, Entonces **no existe** ningún caso en que un `deny` … se convierta en `allow`». Después: la invariante rige para el rango **cuyo interior no contiene ninguna declaración de campo que ya estuviera en esa cabecera**, exige que el corpus tenga **al menos una base `deny`** (si no, la propiedad es cierta por vacío y no acredita nada) y declara que **la acotación es sólo de esa clase** — cualquier otra vía sigue incumpliendo, con el delimitador **fabricado** como ejemplo no exhaustivo. Y lo que antes era un párrafo del Historial —«comentar retira; la ausencia se perdona»— pasa a ser **CA-11**, con la excepción medida dentro: `Seguridad:` ausente **no** se perdona en rigor efectivo `critico`. El REQ pasa de **10 a 11** criterios | **H-03** (`docs/qa/1.32.1-hallazgos.md` § H-03, clase `contrato`): el criterio decía «no existe ningún caso» y QA midió dos (D1 `Hallazgos abiertos: SEC-9 (usuario/dinero)` comentado y D2 `QA: con-hallazgos` comentado, DENY en 1.32.0 → ALLOW en 1.32.1). **El estado alcanzable no es nuevo** —comentar equivale a borrar y la ausencia es lo que la puerta perdona por compatibilidad, decisión vieja que esta ventana **no toca**—; lo nuevo era la distancia entre lo que el documento dice en letra y lo que la máquina decide, y que el contrato afirmaba lo contrario. **Anti-coartada (`requirements/README.md`, «Y el reverso»):** la acotación **no** cubre el CR interior de **H-01**, que sigue siendo incumplimiento de CA-02 y CA-03 y se arregla en el código, no en el criterio. **Sin ADR:** no cambia el alcance ni la decisión base; CA-11 escribe la conducta que ya existía | — |
| 2026-09-07 | **Visibilidad de un campo declarado sólo dentro de un comentario: decidida (b), fuera de esta ventana**, con dueño (`analista-requerimientos`), ventana (1.33.0) y el **discriminante** ya escrito en «Fuera de alcance». En consecuencia **ningún criterio de este REQ promete esa visibilidad**, y CA-02 ya no se apoya en ella | **H-03**: `docs/qa/1.32.1.md` afirmaba que «la vía por la que se ve es el informe (CA-05/CA-06)» y QA midió que el informe **calla** (`rc=0`, «Ningún valor anómalo»). Motivos de (b) frente a (a): el write-back queda honesto **escribiendo la verdad** (CA-11), no añadiendo una función; colgar el fail-open abierto de H-01 de una mejora de diseño es lo que el propio apartado «Fuera de alcance» prohíbe; y el aviso mal acotado enterraría lo real bajo lo inofensivo (CA-06). Nota de coste: (b) **no** añade trabajo de desarrollo en la vuelta 1 | — |
| 2026-09-07 | **Campo `Archivos:` reconciliado contra la realidad medida.** **Corregido:** `tests/escenarios/hooks/secciones/36-cabecera-comentario-html.sh` (no existe) → los **dos** archivos reales, `-1-la-puerta.sh` y `-2-los-lectores.sh`. **Añadidos:** `tools/arnes-paralelo.sh` (alcance añadido que hasta hoy sólo estaba declarado en prosa, tres filas más arriba), `tests/escenarios/hooks/run.sh` y `tests/escenarios/hooks/README.md` (los dos llevan el cuadre total del banco). **Retirados:** `requirements/REQ-016.md` (autoinclusión) y `docs/qa/1.32.1.md` | **H-05** (`docs/qa/1.32.1-hallazgos.md` § H-05, clase `contrato`): con el campo falso, `tools/arnes-paralelo.sh` respondió **`disjunto` con rc 0** sobre un par que colisiona en dos archivos, uno de ellos `run.sh` — y ese `disjunto` es la **única** autorización para despachar en paralelo (`AGENTS.md` §6). Misma familia que SEC-020: un mapa corrompido con éxito aparente. **Y el criterio para los que vengan, que es por lo que se retiran y no sólo se tachan:** el campo declara los archivos **del producto** que la implementación del REQ toca, **no** los artefactos de gobierno que toda comisión toca por definición —el propio archivo del REQ, el índice de `requirements/README.md`, `docs/ESTADO.md` y el registro de la ventana en `docs/qa/`—. Si se declaran, **cualquier** par de REQ colisiona siempre por un archivo de bitácora y el campo deja de responder la única pregunta para la que existe; es el cuello que este repositorio ya midió cuatro veces. Queda **pendiente para 1.33.0** el único caso que no se decide aquí: `CHANGELOG.md`, la bitácora compartida que una comisión sí escribe a mano y donde el conflicto de fusión es real — sigue declarado en los dos REQ | — |
| 2026-09-07 | **Cifra del corpus de CA-02, fechada y declarada derivada.** La fila de más arriba dice «**58 cabeceras** × 3 = **174** variantes»; medido de nuevo el **2026-09-07** sobre el banco de **39 archivos de sección** da **62 × 3 = 186** (lo imprime el propio caso). La fila anterior **no se reescribe**: queda como la medición de su fecha. El número **no es umbral de nada** —el corpus se cosecha por glob y crece con cada sección nueva—, así que vale **sólo** junto a su fecha y su número de secciones, y **no** se fija en ningún criterio: CA-02 se enuncia sobre «el corpus que el corredor descubre por glob», que es la forma que no envejece | Defecto de forma 3 del informe de QA (`docs/qa/1.32.1-hallazgos.md` §4): una cifra desnuda en un Historial envejece sola a cada sección nueva del banco | — |
| 2026-09-07 | **CA-09 reescrita: de «qué revisar» a «qué se acredita y qué no».** Antes: «Entonces encuentra dicho … qué revisar (**los REQ cerrados cuya cabecera contiene un rango de comentario que cita un veredicto**), con qué comando barrerlo, y que la pertenencia de versiones afectadas no es una lista escrita a mano». Después, dos exigencias **separables**: **(i)** el texto enuncia la propiedad por **estado** —«cuáles de mis REQ en estado terminal **no cerrarían hoy**»— **antes** de ofrecer ningún comando; **(ii)** todo barrido **por vía** declara, junto al comando, que interroga una vía y no la propiedad, **nombra** las vías conocidas que no encuentra (sitio único: `docs/seguridad/registro-seguridad.md`, SEC-024/SEC-025; ejemplos **no exhaustivos**: delimitador de apertura fabricado, clave fabricada) y dice que **no hallar nada no acredita ausencia de exposición**; y **ninguna** frase del apartado afirma completitud sobre un barrido por vía. **Alcance decidido y declarado:** la **existencia** de la comprobación por estado **no** se contrata aquí — pasa a «Fuera de alcance» con dueño (`desarrollador`), ventana (1.33.0) y clase `instrumento`; CA-09 exige que el skill la **nombre** y diga que hoy ningún comando la responde. Sigue habiendo **11** criterios | **SEC-025** (`docs/seguridad/registro-seguridad.md` § SEC-025, clase `contrato`): el skill afirmaba que los REQ con `<!--` en la cabecera «son los únicos que pudieron verse afectados» — promesa de completitud **falsa** con dos contraejemplos medidos (el delimitador fabricado de SEC-024 y la clave fabricada, que no necesita comentario alguno y cierra un `critico` desde 1.30.3). El skill **cumplía** CA-09: el criterio acotaba «qué revisar» a la vía del comentario, así que era el criterio el que se quedó corto y es el criterio el que se arregla — no la implementación (la frase la corrige el `desarrollador`, es una cláusula en cada sitio). **Y el defecto de fondo no es el patrón, es la pregunta:** ensanchar el patrón interroga al **mecanismo**, que tiene una vía nueva cada vez — la jugada con cinco derrotas medidas en este repositorio (`ADR-002`, SEC-020, H-01) —; la pregunta de **estado** no describe ninguna vía y por eso no envejece. **Por qué sólo la mitad declarativa bloquea hoy:** la otra es `instrumento`, y un `instrumento` no puede ser condición para cerrar (`AGENTS.md` §6); colgarla de CA-09 dejaría el fail-open vivo de SEC-024 esperando a una herramienta nueva, que es lo que «Fuera de alcance» ya prohíbe dos veces. **Anti-coartada (`requirements/README.md`, «Y el reverso»):** la acotación **no** amnistía nada — cualquier otra frase heredada que prometa completitud sin poder darla sigue incumpliendo, y **CA-02 no se toca**: SEC-024 es incumplimiento **exacto de su letra** y se arregla en el código. **Sin ADR:** no cambia el alcance ni la decisión base del REQ; CA-09 exige ahora lo que el skill debía haber dicho desde el principio | — |
| 2026-09-07 | **El tapón subido a criterio: CA-12 (nuevo), y CA-04 acotada.** Antes: ningún criterio, ninguna fila de `AGENTS.md` §13 ni de `templates/AGENTS.md.tpl` mencionaba el **retorno de carro**; el control vivía sólo en el código, en 19 casos de banco y en el registro de seguridad. Después: **CA-12** contrata la propiedad —*una línea de la cabecera con un CR que no es el que la termina deja una cabecera que **no se puede medir**, y una puerta que no puede medir **no deja pasar** → DENY citando la línea*—, enunciada **por propiedad y no por sitio** del escáner, con las dos caras como **la misma** propiedad (ejemplos **no exhaustivos**: delimitador fabricado `<!`+CR+`--`, clave fabricada `Seg`+CR+`uridad:`), con el **sitio único** de la lista exhaustiva de caracteres de control (`hooks/lib.sh`) y con la exigencia de que la denegación sea **por medibilidad y no por veredicto** (`Estado: comple`+CR+`tado` con todo en verde deniega **por la guarda**; resolverlo como **ausencia** incumple, porque la ausencia es lo que la puerta perdona). Declara además las tres fronteras medidas y conformes: el CR **final** es transporte (CRLF decide **idéntico** a LF), el cuerpo no se restringe y **reabrir** no se bloquea. **CA-04** no se reescribe ni pierde fuerza —la tolerancia a la clave decorada fuera de los rangos sigue sin restringirse—: se le añade la **frontera** de que opera sobre una cabecera **medible**, con la anti-coartada de siempre. El REQ pasa de **11 a 12** criterios. Se añade también **una fila** a la tabla de invariantes de `AGENTS.md` §13 y **la misma** a `templates/AGENTS.md.tpl` | **SEC-024** (`docs/seguridad/registro-seguridad.md` § SEC-024, clase `contrato`, estado `abierto` → `en-mitigación`) por **R-008**: el auditor verificó el cierre del código en las dos caras (sondas 3 y 5 movidas de ALLOW a DENY; orden de descuentos de CR verificado por **enumeración cerrada**) y **no firmó** el REQ, con el hueco nombrado exacto: **«CA-02 contrata el agujero; nada contrata el tapón»** — CA-02 prohíbe que un rango **añadido** vuelva un `deny` en `allow`, pero la puerta podría cumplirla **reconociendo** `<!`+CR+`--` como apertura (salida descartada con razón: vuelve a fabricar); la **clave fabricada** no lleva rango y en R-007 quedó escrito que no desmentía criterio alguno, mientras hoy la puerta la deniega; y CA-04 quedó **estrechada sin decirlo**. Consecuencia que lo hace `contrato` y no pedantería: **un control que ningún criterio contrata se puede retirar en la ventana siguiente y sólo lo notarán 19 casos de banco cuya justificación vive en un comentario** — deriva de `AGENTS.md` §9. **Sin ADR:** no cambia el alcance ni la decisión base; describe lo construido y medido. **Sin código:** el desarrollador no interviene, así que no gasta vuelta dev↔QA (`AGENTS.md` §6) | — |
| 2026-09-07 | **Conflicto registrado** en «Preguntas abiertas / conflictos»: antes «(ninguna)»; después, la contradicción entre **CA-11** y dos textos heredados que prometen sin condición que un REQ no cierra sin `QA: aprobado` (`requirements/README.md` y la fila de `AGENTS.md` §13), cuando la ausencia del campo **sí** se perdona. Se registra con su salida propuesta y se **deja a decisión de la coordinadora** (esta ventana o 1.33.0); no bloquea el ciclo de este REQ | Consecuencia de escribir CA-11: la conducta real queda contratada y los textos heredados quedan **más anchos** que el código, que es lo que `ADR-002` y REQ-015 CA-08 prohíben. Registrarlo es obligatorio (rol del analista, `AGENTS.md` §9); resolverlo tocaría archivos que en esta ventana son alcance de desarrollo | — |
| 2026-09-10 | **CA-11: el puntero del sitio único, corregido — y la promesa del perdón, condicionada.** **Antes** (líneas 152-153): «Qué campos perdona la ausencia lo decide **un solo sitio**, `hooks/guard-completado.sh`, y ahí vive la lista exhaustiva; ejemplos **no exhaustivos**: `QA:`, `Hallazgos abiertos:`», y el `Entonces` afirmaba **sin condición** que «la ausencia se perdona por compatibilidad». **Después:** el puntero remite a la **tabla de la dirección de la ausencia de `hooks/lib.sh`** —nombrada **por propiedad** (la constante que la declara y la función que la consulta), no por línea—, que es el sitio único que **`REQ-024 CA-02`** contrata y **`ADR-009`** decide, con la cláusula de que **cambiar de sitio exige ADR y write-back en este criterio**; se dice **por qué** el puntero viejo era falso —en `hooks/guard-completado.sh` viven `QA:` y `Hallazgos abiertos:`, y **no** `Sensible a seguridad:` ni `Rigor:`, los dos que retiran el **suelo** y el **nivel**— y la cuenta se deja **al banco, que la publica en cada corrida** (`REQ-024 CA-03`) en vez de escribirla aquí, porque sube o baja con cada campo del lector. Y el perdón deja de ser incondicional: se perdona **mientras la exigencia no esté activada** (`campos.ausencia_exige`, que nace **apagada**; `ADR-009`, y que 1.34.0 no cambia el defecto lo contrata `REQ-024 CA-05`); **activada**, la ausencia **gobierna o deniega** (`REQ-024 CA-01`). La **equivalencia** comentado ≡ borrado —que es lo que este criterio contrata— **no se toca** y se declara válida en los dos estados de la llave: lo que CA-11 contrata es que la **vía** de la desaparición no cambie la decisión, no cuál sea la decisión. Sigue habiendo **12** criterios; ningún otro criterio se modifica | **`QA-024-02`** (`docs/qa/1.34.0.md` § «`QA-024-02` · `contrato` · severidad **alta** — `CA-03` no está acreditado», y la fila de `CA-03` del «Veredicto por criterio» de REQ-024; clase `contrato`, **bloqueante**): `REQ-024 CA-03` eligió su salida **(ii)** —el sitio único es **otro**— y de sus **tres** obligaciones sólo estaba hecha la primera; el árbol quedaba en la **tercera salida que CA-03 prohíbe**, «dejar el puntero citando un archivo donde falte algún campo de la clase». Motivo de que el sitio sea `hooks/lib.sh` y no la puerta, **medido y no elegido por gusto** (`ADR-009` §Alternativas): `hooks/lib.sh` lo cargan **siete** puntos de entrada y sólo uno es la puerta; una tabla en `guard-completado.sh` sería invisible para los otros seis y el informe diría `estandar` donde la puerta dice `critico`. **Deriva de `AGENTS.md` §9**, no pedantería: un puntero que manda al sitio equivocado es peor que ninguno —un auditor que lo sigue al pie de la letra concluye «no falta nada» sobre los dos campos peligrosos—, que es la familia de **`SEC-050`**. *(Este write-back se hizo **leyendo** el árbol —`hooks/lib.sh` § «EL SITIO UNICO DE LA DIRECCION DE LA AUSENCIA», `hooks/guard-completado.sh`, `ADR-009`, `.arnes/config.json`— y **sin ejecutar nada**; la acreditación de que el puntero es cierto **por construcción** la da el caso de banco de `REQ-024 CA-03`, no esta fila.)* | [ADR-009](../docs/decisions/ADR-009-la-ausencia-de-un-campo-no-se-resuelve-del-lado-que-abre.md) |
| 2026-09-10 | **`Estado: completado` → `en-progreso`**, y **`QA-024-02 (contrato)` entra en `Hallazgos abiertos:`** | **`AGENTS.md` §9**, disparada por el write-back de `CA-11` (fila anterior). El `analista-requerimientos` reescribió el criterio y **se paró** en la reapertura, que es acto de coordinación. **Por qué el hallazgo entra en el campo y no sólo en el registro:** hasta ahora la puerta **no veía esta deuda en este REQ** —el campo sólo llevaba `H-07 (instrumento)`—, así que `REQ-016` podía cerrarse sin el re-recorrido que `CA-03 (ii)` exige. Con la clase `contrato` declarada, `guard-completado` **lo impide por máquina** y no por prosa. Lo propuso el analista como extra para la decisión y **no lo ejecutó**; lo ejecuto yo | `ADR-009` |
