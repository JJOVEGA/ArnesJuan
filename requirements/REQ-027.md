# REQ-027 — Las reglas de trabajo de la coordinadora, en la sede canónica que leen todas las herramientas: hoy sólo existen como conversación en este repositorio
Estado: pendiente
Módulo: Gobernanza de la sesión coordinadora — sede escrita en el documento canónico (AGENTS.md) y en el andamiaje que reciben los proyectos nuevos
Archivos: AGENTS.md, templates/AGENTS.md.tpl, requirements/REQ-027.md, docs/qa/REQ-027.md
Mide: sí (sólo bytes y conteos sobre archivos; ninguna medición de reloj ni de procesos, así que **no** depende de las sondas de REQ-021, que está `bloqueado`)
Prioridad: alta
Versión destino: 1.34.0
Sensible a seguridad: sí
QA: pendiente
Seguridad: pendiente
Hallazgos abiertos: (ninguno)
Rigor: critico
NFR relacionados: (ninguno separado; el único umbral aplicable está cuantificado en CA-05)

## Historia

Como propietario de un arnés que otros proyectos instalan, quiero que las **reglas de trabajo de la
sesión coordinadora** vivan en el documento canónico que **cualquier** herramienta lee al abrir el
proyecto, para que las coordinadoras de los proyectos instalados las apliquen en lugar de que existan
sólo como conversación en este repositorio.

**Por qué `AGENTS.md` y no `CLAUDE.md`.** Las reglas de coordinación son **comunes a todas las
herramientas**, así que necesitan **sede canónica compartida**: `AGENTS.md` se declara a sí mismo
«leído por Claude Code, Codex, Cursor y otros», y `CLAUDE.md` se limita a importarlo. Ponerlas en
`CLAUDE.md` habría reducido la cobertura a una sola herramienta para esquivar una colisión de
calendario que, comprobada, era **hipotética** (REQ-019 está `pendiente`, sin comisión viva sobre
`AGENTS.md`). El calendario se resuelve ordenando comisiones —ver «Dependencia de calendario»—, no
cambiando de archivo.

**El forzador, con sus instancias medidas el 2026-09-08** (evidencia citada del encargo; no
re-medida aquí): cuatro encargos corregidos en vuelo sobre REQ-026; el techo `0,60×` de
`REQ-019 CA-07`, insatisfacible contra un suelo medido después en `0,68×`; el `≤ 1,5 s` de
`REQ-026 CA-15`, escrito sin ninguna medición que lo fundara; la contradicción entre `CA-13` y
`CA-17` del propio REQ-026; y sellos **UTC** de GitHub escritos como fechas del proyecto, que son
**locales**, propagando el desfase a 45 sitios en 7 archivos. Las tres primeras las habría cazado la
comprobación previa de CA-03/CA-04 **antes** de gastar la comisión; la cuarta es la separación de
responsabilidades de la regla C.

## Criterios de aceptación

> Los números de este REQ declaran su tipo en el propio criterio (`operativo` / `de contrato`), y
> toda cifra de línea base lleva su corrida.

- **CA-01 (doble sede, y ninguna de las dos sola).** Dado el repositorio después del cambio, cuando
  se comparan los dos archivos, entonces el bloque de reglas está **byte a byte idéntico** en
  `AGENTS.md` y en `templates/AGENTS.md.tpl`. **Par discriminante:** sólo en `AGENTS.md` es no
  conforme (los proyectos nuevos no lo reciben) y sólo en la plantilla también lo es (este
  repositorio predicaría lo que no se aplica a sí mismo).

- **CA-02 (contenido, por puntero al sitio único).** Dado el bloque, cuando se comprueba su
  contenido, entonces aparece **un ítem propio por cada título** de la sección «Reglas contratadas»
  de este REQ, que es el **sitio único** donde vive esa lista; ningún otro criterio la re-enumera.
  Falta cualquiera de esos títulos → no conforme.

- **CA-03 (la comprobación previa discrimina por procedencia de la cifra, no por presencia de un
  número).** Dado el encargo de REQ-019 tal como se despachó el 2026-09-08, con `CA-07` fijando un
  techo de `0,60×` sin medición previa, cuando se le aplica la comprobación de antes de despachar,
  entonces la **pregunta 3** identifica esa cifra como supuesto sin comprobar y el encargo no sale
  hasta medir el suelo (medido después: `0,68×`, que la hacía insatisfacible). **Y** dado un encargo
  cuya cifra cita la corrida que la funda, entonces la comprobación **no** lo detiene.

- **CA-04 (la comprobación previa detecta criterios mutuamente insatisfacibles).** Dado el par
  `CA-13`/`CA-17` de REQ-026 tal como estaba redactado el 2026-09-08, cuando se le aplica la
  **pregunta 2**, entonces se declara que no pueden cumplirse a la vez y el encargo no se despacha
  hasta resolver **esa** contradicción — sin ampliar el encargo. **Y** dado un par de criterios que
  se restringen mutuamente sin excluirse, entonces la comprobación **no** lo detiene.

- **CA-05 (coste: las dos vías, en la misma corrida, y la cifra anotada para REQ-019).** Dado
  `AGENTS.md` antes del cambio, cuando se mide después en la **misma corrida**, entonces (i) el
  bloque añade **no más de 2 600 B** al archivo — número **operativo**: se **baja** con la medición
  y **menos es conforme y no es hallazgo** — **y** (ii) el resto de `AGENTS.md` queda **byte a byte
  idéntico**: nada más crece por la puerta de atrás. La magnitud es *bytes leídos en cada comisión*,
  que es la que se degrada; por eso no basta (i) sin (ii). El **delta medido** se anota en el
  Historial de este REQ con su corrida, para que REQ-019 mida contra un número y no contra un
  recuerdo. *(Líneas base del encargo del 2026-09-08, **no re-medidas aquí**: `AGENTS.md` = 33 827 B
  de los 74 255 B ≈ 18 500 tokens del ritual de arranque.)*

- **CA-06 (forma del bloque, y los dos bordes que ya costaron caro aquí).** Dado cada uno de los dos
  archivos, cuando se cuentan los marcadores, entonces hay **exactamente uno** de apertura y
  **exactamente uno** de cierre, en ese orden: `<!-- arnes:coordinacion:inicio -->` y
  `<!-- arnes:coordinacion:fin -->` (nombres **de contrato**: `arnes-upgrade` migrará por ellos los
  proyectos ya instalados; cambiarlos exige ADR y write-back). **Par discriminante:** cero
  marcadores, dos aperturas, o una apertura que no cierra → no conforme. Y en la misma comprobación:
  **el conjunto de títulos `## N.` existentes no cambia** —el bloque entra como sección propia sin
  **renumerar** ninguna, porque las referencias `§N` de este documento, de los agentes y de los REQ
  se romperían en silencio—, y `templates/AGENTS.md.tpl` conserva intactos sus marcadores de
  sustitución `{{…}}`.

- **CA-07 (las vías de lectura y los dos límites, escritos y no supuestos).** Dado el bloque, cuando
  se lee su texto, entonces declara **cómo llega la regla a cada coordinador** —Claude Code por
  `CLAUDE.md` → `@AGENTS.md`; Codex y Cursor leyendo `AGENTS.md` directamente— y declara los dos
  límites: (a) **una herramienta sin vía documentada no recibe estas reglas**, y (b) **un proyecto ya
  instalado tiene su `AGENTS.md` congelado**: no recibe nada hasta migrar con `arnes-upgrade`. **Par
  discriminante:** un texto que afirme o insinúe cobertura de todo coordinador, o que dé por hecho
  que los proyectos instalados ya las tienen, es no conforme **aunque contenga las reglas
  completas**.

- **CA-08 (cómo se sabrá si sirven, y cuándo se decide).** Dado el cierre de la ventana **1.34.0**,
  cuando se revisa este REQ, entonces están anotadas las tres señales con su corrida: (a) **encargos
  corregidos en vuelo** —línea base **4** sobre REQ-026, sesión del 2026-09-08—, (b) **reaperturas
  de REQ** —línea base: REQ-014, reabierto el 2026-09-08— y (c) **tokens por resultado entregado**,
  cuya línea base se toma en la **primera** comisión de 1.34.0 despachada ya con el bloque. Las tres
  son cifras **operativas** de **una** ventana, se leen como **razón contra su propia línea base** y
  **menos no acredita nada por sí solo**. **Este criterio no contrata que las señales bajen**: sólo
  que, con lo anotado, se decida si las reglas siguen, se corrigen o se retiran. Y si al cerrar la
  ventana falta la medición de alguna, **no se declara que las reglas sirven** —ausencia de datos no
  es verde— y REQ-025 no puede citar este REQ como evidencia de que las reglas bastan.

## Notas / alcance

### Reglas contratadas (sitio único de la lista; el texto final vive en el bloque)

**A. La comprobación de antes de despachar** — obligatoria, y **por escrito en el propio encargo**,
antes de enviar cualquier comisión:
1. ¿Qué **resultado exacto** debe entregar?
2. ¿Los **criterios pueden cumplirse simultáneamente**?
3. ¿Qué **supuesto o cifra** necesita comprobarse primero?
4. ¿Qué queda **fuera**, y **cuándo debe detenerse**?
Si falta algo, se resuelve **únicamente esa dependencia**; no se amplía el encargo.

**B. Las seis reglas, una línea cada una** (títulos que CA-02 comprueba):
1. **Separar evidencia de interpretación** — dato medido / cálculo / estimación / hipótesis; una
   medición correcta no valida la conclusión construida encima.
2. **No convertir propuestas en compromisos** — ahorro, duración o cobertura son **hipótesis** hasta
   medirlas.
3. **Mantener visibles las decisiones vigentes** — consultarlas en una referencia breve (alcance
   aprobado, prioridades, excepciones, pendientes), no reconstruirlas de conversaciones largas.
4. **Corregir sin ampliar** — ante un defecto, comprobar su **efecto concreto**; los hallazgos
   adicionales van a la cola salvo que impidan el trabajo en curso.
5. **Cerrar cuando la evidencia alcance** — ante una observación externa, decidir si aporta defecto
   nuevo, algo ya cubierto o mejora opcional; no abrir una ronda por cada una.
6. **Responder con evidencia breve** — resultado, evidencia, limitación material, siguiente paso.

**C. Separación de responsabilidades** — la coordinadora **organiza y propone**; las **herramientas**
**verifican lo mecánico**: toda afirmación cuya verdad se decide leyendo el disco o corriendo un
comando (ejemplos **no exhaustivos**: fechas, versiones, archivos modificados, conteos, pruebas).
Instancia medida el 2026-09-08 (arriba): UTC leído como local, 45 sitios en 7 archivos; una
comprobación de tres líneas lo habría cazado al instante.

**D. Vías de lectura y límites** — los de CA-07.

### Fuera de alcance (explícito)

- **No añade un agente revisor de la coordinadora.** Es deliberado: la puerta la contempla
  **REQ-025** (`borrador`), y la propuesta del propietario dice **reglas primero, agente sólo si
  hacen falta**. Este REQ es su **precursor barato** y REQ-025 es su continuación.
- **No construye el comprobador mecánico de `tools/`.** Se nombra como candidato (REQ-025 lo prevé
  como `tools/arnes-comprobar.sh`) y se deja fuera.
- **No edita `skills/arnes-upgrade/SKILL.md`** (es de otra comisión) — ver la nota de migración.
- **No reescribe ni reordena nada existente de `AGENTS.md`**: eso es REQ-019.

### Nota de migración pendiente (no la ejecuta este REQ)

`templates/` sólo alimenta `arnes-init`, es decir **proyectos nuevos**. Un proyecto ya instalado
tiene su `AGENTS.md` **congelado**, que es exactamente la deriva que `skills/arnes-upgrade` existe
para cerrar. Hace falta una entrada en «Migraciones conocidas» de `skills/arnes-upgrade/SKILL.md`
**para la versión 1.34.0**, que inserte el bloque en un `AGENTS.md` existente sin pisar el texto
propio del proyecto; los marcadores de CA-06 existen para que esa inserción pueda ser idempotente.
**Hasta que un proyecto migre, sus coordinadoras NO tienen estas reglas**, y así se escribe en el
bloque (CA-07 b).

### Dependencia de calendario con REQ-019

REQ-019 declara `AGENTS.md` en su `Archivos:`. Hoy no hay colisión —está `pendiente`, sin comisión
viva y sin cambios sin comitear en ese archivo—, pero **las dos comisiones no se despachan a la vez
sobre `AGENTS.md`**: es una dependencia de **orden**, no de sede, y `tools/arnes-paralelo.sh` las
declarará **no disjuntas**. Preferencia: que este REQ entre **antes**, porque añade 2 600 B como
techo y REQ-019 mide su adelgazamiento contra un archivo estable; si entra después, la línea base de
REQ-019 se re-mide.

### Relación con REQ-025 (solapamiento declarado, no conflicto)

REQ-025 (`borrador`) declara en su `Módulo:` una «sede escrita de las reglas de la coordinadora» y
lleva `AGENTS.md` en su `Archivos:`. **No hay contradicción hoy** —REQ-025 no está aprobado y este
REQ escribe primero—, pero sí una restricción sobre el futuro: **dos transcripciones de la misma
regla se desfasan**, así que la sede de REQ-025 debe **apuntar** a este bloque, no re-transcribirlo.
Falta una línea en REQ-025 que lo diga; la escribe quien tome esa comisión.

### Por qué `Rigor: critico` y `Sensible a seguridad: sí`

Con `AGENTS.md` dentro, esto deja de ser andamiaje: es **el documento canónico que gobierna a los
agentes de todos los proyectos que instalan el arnés**, y del que el propio `auditor-seguridad` toma
la disciplina que hace cumplir. Una regla mal redactada ahí —por ejemplo «cerrar cuando la evidencia
alcance» leída como permiso para cerrar antes— **debilita el criterio de cierre en todos los
proyectos, y en silencio**: es un fallo en abierto, la propiedad que `AGENTS.md` §6 usa para llamar
crítico a algo. Además, la política de autoalojamiento de §6 fija `critico` y `sí` **por defecto**
para todo REQ de este repositorio, y sólo el propietario puede bajarlo: esto no es una excepción
editorial, porque tiene efecto en lo que los proyectos heredan. No toca autenticación, autorización,
datos personales ni secretos —de ahí que la sensibilidad sea **de gobernanza**, no de datos—, pero
el suelo que impone es el mismo.

## Preguntas abiertas / conflictos

(ninguna)

## Trazabilidad

Origen: propuesta del propietario del 2026-09-08 (reglas de trabajo de la coordinadora), sobre los
fallos de coordinación medidos ese mismo día. Corrección de sede del 2026-09-08 (de
`templates/CLAUDE.md.tpl` a `AGENTS.md` + `templates/AGENTS.md.tpl`): las reglas son comunes a todas
las herramientas y `CLAUDE.md` sólo lo lee Claude Code. Precursor de REQ-025. Dependencia de orden
con REQ-019.
Tocado por: analista-requerimientos / 2026-09-08

## Historial de cambios

| Fecha | Antes → Después | Causa | ADR |
|------------|-----------------|-------------------------|--------|
| 2026-09-08 | (creación) | Propuesta del propietario; las reglas de la coordinadora sólo existían como conversación en este repositorio | — |
| 2026-09-08 | Sede `templates/CLAUDE.md.tpl` + `CLAUDE.md` → `AGENTS.md` + `templates/AGENTS.md.tpl`; `Rigor: estandar` → `critico`; `Sensible a seguridad: no` → `sí`; CA-07 pasa a contratar las vías de lectura y los dos límites | Corrección del encargo (coordinadora, 2026-09-08): la colisión con REQ-019 era hipotética y no justificaba reducir la cobertura a una sola herramienta; con `AGENTS.md` dentro sube el rigor | — |
