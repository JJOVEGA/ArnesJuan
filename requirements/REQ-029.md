# REQ-029 — Fidelidad al encargo: detectar omisiones, sustituciones y decisiones de negocio no autorizadas antes de implementar
Estado: en-revisión
Módulo: Gobernanza del despacho y documentación heredable (regla 1 de `AGENTS.md` §6 y su gemela, plantilla del REQ y su gemela, definiciones de `analista-requerimientos` y `qa-tester`, entrada «Hacia 1.35.0» de `arnes-upgrade`)
Archivos: requirements/REQ-029.md, requirements/README.md, requirements/REQ-025.md, AGENTS.md, templates/AGENTS.md.tpl, templates/requirements-README.md.tpl, agents/analista-requerimientos.md, agents/qa-tester.md, skills/arnes-upgrade/SKILL.md, CHANGELOG.md, docs/qa/REQ-029.md, PENDING_APPROVAL.md
Prioridad: alta
Versión destino: 1.35.0
Sensible a seguridad: sí
QA: pendiente
Seguridad: pendiente
Hallazgos abiertos: (ninguno)
Rigor: critico
NFR relacionados: (ninguno separado; los únicos números del REQ son techos de alcance de contrato, declarados en CA-02 y CA-11)

## Historia

Como **propietario del arnés**, quiero que el pedido que hago llegue al requerimiento **con su fuente
identificable** y que cada obligación verificable de ese pedido quede **emparejada con el criterio que
la cubre** —o marcada como omitida, sustituida, excluida o añadida, con la autorización que lo permite
o como pregunta abierta—, para que una omisión, una sustitución o una decisión de negocio que yo no
tomé se **detecte antes de implementar**, y no la descubra al ver el producto terminado.

**El caso que lo origina, dicho sólo hasta donde llega la evidencia.** En la demo del conciliador
aparecieron cuatro diferencias entre lo pedido y lo construido —criterio de emparejamiento,
resolución automática de ambiguos, exclusión de asociaciones manuales, tolerancia de fechas— que **no
figuraban en el encargo que recibió aquella sesión**. El informe es **aportado por el propietario**,
no leído por esta comisión (la propuesta de diseño lo declara inaccesible con HTTP 404 desde la
cuenta de trabajo). **No se sabe** si esas obligaciones se perdieron al trasladar el pedido o si nunca
llegaron, y **este REQ no atribuye** a ningún rol haber ignorado instrucciones que no consta que
recibiera: cubre los dos casos —conservar la fuente hace visible el primero, y comprobar la resolución
antes de implementar hace comprobable el segundo—.

## Criterios de aceptación

Todos los criterios se validan por **revisión del texto** entregado (y, en CA-12, aplicando ese texto
a ejemplos acotados). **Ninguno acredita conducta observada** de un agente: que las reglas se
cumplan en un encargo real **no** es contrato de este REQ (ver CA-13 y «Notas / alcance»).

- **CA-01** — [la obligación de la coordinadora vive en la regla 1 de `AGENTS.md` §6]
  Dada la regla 1 del bloque «La coordinación se orienta a entregas» de `AGENTS.md` §6 (hoy
  `AGENTS.md:417-422`), sede única acreditada por la entrega 1 de REQ-025 (su CA-15 punto 1),
  Cuando se lee tras esta entrega,
  Entonces la regla 1 gana **un** párrafo que obliga a la coordinadora a:
  1. en todo encargo al `analista-requerimientos`, **incluir el pedido del propietario con fuente
     identificable** —archivo y commit, o mensaje con su fecha— **separado del resumen de la
     coordinadora**; y si esa fuente no existe, **decirlo** (`fuente no disponible`) en vez de
     reconstruirla;
  2. **antes de despachar la implementación** de cualquier parte que dependa de una fila de la
     «Correspondencia con el encargo» (CA-03) marcada como omitida, sustituida, excluida o añadida
     que **requiera al propietario**, comprobar que esa fila **quedó resuelta** por él, con la
     autorización citada; **presentarla** (regla 4) **no la resuelve**;
  3. mientras no esté resuelta, avanzar **sólo** en el trabajo que la regla 4 ya permite —el que no
     depende de esa decisión, está autorizado y está suficientemente definido—;
  4. **no** pasar por esta comprobación las **elecciones técnicas ordinarias** (las que no cambian lo
     que el propietario ve, decide o cobra; ejemplos **no exhaustivos**: estructura interna, nombres,
     técnica de prueba).

  Escenarios de error y borde que el texto debe decidir sin interpretación:
  - Dado un encargo al analista **sin** pedido con fuente y **sin** la declaración `fuente no
    disponible`, Entonces ese encargo **incumple** la regla 1 (es lo que el texto dice, no una
    inferencia del lector).
  - Dado un encargo que trae **sólo el resumen** de la coordinadora presentado como si fueran las
    palabras del propietario, Entonces **no cuenta** como fuente identificable.
  - Dada una diferencia **presentada** al propietario y **sin respuesta**, o con una respuesta que no
    decide entre las opciones, Entonces **no está resuelta** y lo que depende de ella **no se
    despacha**.
  - Dada una elección técnica ordinaria, Entonces el texto **no** exige aprobación humana adicional
    para despacharla (es el caso 4 de CA-12).

- **CA-02** — [gemelas idénticas byte a byte en lo que cambia]
  Dadas las dos parejas sede/gemela —`AGENTS.md` / `templates/AGENTS.md.tpl` y
  `requirements/README.md` / `templates/requirements-README.md.tpl`—,
  Cuando se comparan las líneas **añadidas y retiradas** por esta entrega en cada miembro de la
  pareja (`git diff cfb1106 -- <archivo>`, filtradas a las líneas que empiezan por `+` o `-` y sin las
  cabeceras `+++`/`---`), **contando sólo las de secciones que existen en los dos miembros de la
  pareja** —las secciones espejo: la regla 1 de §6 y la `## Plantilla`—; las de una sección que
  **sólo existe en la sede** por ser propia de este repositorio **no cuentan**, porque la gemela no la
  lleva por construcción (hoy, el `## Índice` de `requirements/README.md`),
  Entonces el `diff` entre las dos salidas tiene **no más de 0** líneas distintas por pareja
  (**de contrato**: lo fija REQ-025 CA-15 punto 1 para la sede y su gemela, y quien elige su conducta
  por ese valor es el proyecto consumidor que migra con `arnes-upgrade`).
  - Borde: si la línea de contexto del párrafo difiere entre sede y gemela (numeración distinta), eso
    **no** es diferencia; sólo cuentan las líneas `+`/`-`.

- **CA-03** — [la plantilla del REQ lleva la fuente y la correspondencia]
  Dada la sección `## Plantilla` de `requirements/README.md` (hoy `requirements/README.md:493-495`,
  `## Trazabilidad`) y su gemela (`templates/requirements-README.md.tpl:403-405`),
  Cuando se lee tras esta entrega,
  Entonces:
  1. `Origen:` pide **fuente identificable** (archivo y commit, o mensaje y fecha) **o** el literal
     `fuente no disponible`, y dice que **un resumen del agente no es fuente**;
  2. existe una subsección `### Correspondencia con el encargo` **dentro de `## Trazabilidad`** —es
     decir, **debajo del primer `## `**, fuera de la cabecera que leen las puertas—, con una tabla de
     columnas: obligación del pedido (con su cita) · criterio que la cubre · relación · autorización
     citada o `→ Preguntas abiertas`;
  3. la columna «relación» admite **exactamente** los valores `cubierta`, `omitida`, `sustituida`,
     `excluida` y `añadida` (decisión de negocio no pedida). **El sitio único de ese vocabulario es la
     plantilla**; cualquier otro texto que los nombre remite a ella.
  - Borde: un REQ cuyo pedido no se conservó rellena la tabla con `fuente no disponible` en lugar de
    citas inventadas; **nunca** reconstruye palabras del propietario.

- **CA-04** — [vida de la correspondencia: carga proporcional]
  Dada la subsección de CA-03,
  Cuando se lee su instrucción,
  Entonces dice que la correspondencia:
  1. se establece **al crear el REQ**;
  2. tiene **una fila por obligación verificable** del pedido —algo cuyo cumplimiento se puede decidir
     como sí/no contra el REQ o lo construido—, **no una por frase**: una frase con dos obligaciones da
     dos filas, y una frase de contexto o de motivo no da ninguna;
  3. se actualiza **sólo cuando cambia el alcance**, y en la **misma** fila del Historial de cambios
     que registra ese cambio (`AGENTS.md` §9);
  4. **una reparación que conserva el contrato la reutiliza**: no se crea tabla nueva ni se repite la
     comparación completa.
  - Error: una reparación que **sí** cambia una fila (altera qué obligación cubre un criterio) **no**
    es «reparación que conserva el contrato»: es cambio de alcance y va por el punto 3.

- **CA-05** — [sin bloqueo retroactivo por conversaciones ausentes]
  Dado un REQ **existente** con contrato y decisiones **aprobados** cuya conversación original no está
  disponible,
  Cuando el texto entregado (plantilla de CA-03 o definición del analista de CA-06, en **una** sola
  sede y con remisión desde la otra) se aplica a él,
  Entonces:
  1. la ausencia del pedido original **no** lo devuelve a `borrador` ni retira, rebaja o reabre
     ninguna aprobación ni veredicto;
  2. se **declara la limitación de trazabilidad** (`Origen: fuente no disponible` y, si se escribe
     correspondencia, la misma marca), y **se conservan las aprobaciones**;
  3. **sólo** se pregunta al propietario por **decisiones realmente pendientes** o **contradicciones
     detectadas**, que siguen las reglas ya vigentes de `Preguntas abiertas / conflictos`;
  4. **nadie** reabre un REQ existente sólo para añadirle la correspondencia (mismo alcance temporal
     que «Cómo se escribe un criterio que no se desmiente» § «Alcance temporal»: rige hacia delante).
  - Contraste obligatorio: en un **encargo nuevo** la fuente se conserva **desde el inicio**; este
    criterio **no** exime a los REQ nuevos de CA-01 punto 1 ni de CA-06.
  - Borde: un REQ existente en `borrador` (sin contrato aprobado) **no** cae en este criterio: sigue en
    `borrador` por sus propias preguntas, no por la ausencia de la fuente.

- **CA-06** — [el analista conserva la fuente, empareja y no resuelve lo que no le toca]
  Dada `agents/analista-requerimientos.md` (sección «Entrevista», hoy `:44-46`, y su Definition of
  Ready, hoy `:108`),
  Cuando se lee tras esta entrega,
  Entonces:
  1. obliga a **conservar el pedido con su fuente**, separado del resumen del analista, y a rellenar la
     correspondencia al crear el REQ, **remitiendo** a la plantilla (CA-03, CA-04) en vez de copiar su
     instrucción;
  2. toda fila `omitida`, `sustituida`, `excluida` o `añadida` que **no cite autorización** del
     propietario va a `Preguntas abiertas`, y el REQ **entero** queda en `borrador` —regla ya vigente;
     **no** se crea estado parcial—; el analista **no** la resuelve ni la da por autorizada porque se
     presentó;
  3. si el pedido no está disponible, escribe `fuente no disponible` y **no reconstruye** sus
     palabras;
  4. la Definition of Ready gana **un** punto: `Origen:` identificable o `fuente no disponible`
     declarada, correspondencia rellenada, y ninguna fila no autorizada fuera de `Preguntas abiertas`.
  - Error: una «autorización» que no se puede localizar (sin fuente ni fecha) **no** cuenta como
    citada.

- **CA-07** — [partir un REQ no concede autoridad sobre el alcance]
  Dada la misma definición del analista,
  Cuando describe la partición por alcance,
  Entonces dice que:
  1. el analista **puede preparar y registrar** la partición conforme a las reglas vigentes
     (`AGENTS.md` §6, «Loop de error»: partir por alcance no elude ningún contador);
  2. si la partición **excluye, sustituye o difiere alcance comprometido** —saca criterios de la
     entrega o de la versión a la que estaban comprometidos, o cambia qué cubren—, **necesita la
     decisión correspondiente del propietario**, citada; en la duda de si difiere, **se trata como que
     difiere** (fail-closed);
  3. **nunca** se usa para **esquivar preguntas pendientes** —las preguntas viajan abiertas con la
     parte a la que pertenecen, y la parte que avanza no depende de ellas— ni para **reiniciar
     contadores**.
  - Error: una partición registrada **sin** esa decisión cuando mueve alcance comprometido fuera de su
    entrega **incumple** el texto.

- **CA-08** — [QA: un solo veredicto, con la fidelidad en su evidencia]
  Dada `agents/qa-tester.md` (sección «Cuestionar la calidad del REQ», hoy `:23-24`),
  Cuando se lee tras esta entrega,
  Entonces:
  1. QA **contrasta la correspondencia antes de probar**, contra la fuente citada cuando está
     disponible y contra los criterios, **sin rehacer el análisis** —ni la entrevista ni los
     criterios—, y en una reparación que conserva el contrato **no** la vuelve a contrastar entera;
  2. hay **un solo veredicto**, el campo `QA:` con su vocabulario vigente; **no** se crea una segunda
     firma, campo ni estado de aprobación. El paréntesis de evidencia de ese veredicto dice cuál de
     estas tres cosas pasó: **contrastó** el REQ con la fuente sin diferencias no autorizadas, **halló
     diferencias** (y cuáles), o **no pudo comprobarlo** (y por qué: `fuente no disponible`, fuente
     citada inaccesible);
  3. dice **expresamente** que la conformidad del código con el REQ **no acredita por sí sola** la
     fidelidad al pedido **ni permite cerrar** con decisiones de alcance pendientes;
  4. una diferencia **sin resolver que requiere al propietario** —ejemplos **no exhaustivos**: fila no
     autorizada fuera de `Preguntas abiertas`, obligación citada sin criterio, correspondencia ausente
     en un REQ nuevo— es
     hallazgo de clase **`contrato`** abierto **antes de probar**, con lo que `guard-completado` ya
     impide cerrar por el campo `Hallazgos abiertos:` (mecanismo vigente, **sin** cambio de hook).
  - **Frontera con el diseño:** el diff de la propuesta escribe «conformidad con el REQ y fidelidad
    al pedido son **dos veredictos distintos**» (`propuesta-fidelidad-encargo.patch:58-61`). **Esa
    redacción no es conforme** con este criterio y **no** se aplica tal cual.
  - Borde: `aprobado` con «no pudo comprobarlo» en el paréntesis es válido **sólo** si no hay ninguna
    decisión de alcance pendiente; con una pendiente, el veredicto **no** es `aprobado`. Y
    «halló diferencias» con `aprobado` sólo vale si todas las diferencias citan autorización.

- **CA-09** — [una sede por obligación; los demás remiten]
  Dado el texto nuevo de esta entrega en los seis archivos normativos (`AGENTS.md`, su gemela, la
  plantilla, su gemela, las dos definiciones de agente),
  Cuando se barre **por propiedad** —*todo texto del árbol que enuncie la obligación de la
  coordinadora de CA-01, la vida de la correspondencia de CA-04, la regla de CA-05 o la de CA-07*—,
  Entonces cada obligación se **enuncia en una sola sede** (CA-01 → `AGENTS.md` §6 regla 1; CA-03 y
  CA-04 → la plantilla; CA-05 → **una** de las dos sedes que CA-05 admite, a elección del
  desarrollador y declarada en su entrega; CA-06, CA-07 → la definición del analista; CA-08 → la
  definición de QA), las gemelas cuentan como la misma sede (CA-02), y todo otro sitio que la mencione
  **remite** a la sede sin copiar su texto. Una segunda transcripción es hallazgo `contrato` contra
  este REQ, por el mismo motivo que REQ-025 CA-15 punto 1.
  - Borde: los agentes **no** copian el párrafo nuevo de la regla 1 (REQ-025 CA-15 punto 1 rige
    también sobre él).

- **CA-10** — [distribución preparada, no declarada entregada]
  Dada `skills/arnes-upgrade/SKILL.md` § «Hacia 1.35.0» (hoy `:870`),
  Cuando se lee tras esta entrega,
  Entonces gana una entrada que **nombra todo archivo heredable que esta entrega toca** —el sitio único
  de esa lista es el campo `Archivos:` de este REQ; hoy son cuatro: `templates/AGENTS.md.tpl`,
  `templates/requirements-README.md.tpl`, `agents/analista-requerimientos.md`, `agents/qa-tester.md`
  (recuento **operativo**, derivado del campo; si el campo cambia, cambia con él)— y dice qué cambia en
  cada uno y que **no cambia ningún hook**; y **ningún texto** afirma que el cambio haya llegado a un
  proyecto consumidor: un proyecto instalado tiene su `AGENTS.md` congelado hasta migrar, y migrar es
  acto suyo.

- **CA-11** — [lo que no se toca, comprobado en el diff]
  Dado `git diff --stat cfb1106` sobre la cabeza entregada,
  Cuando se leen las rutas tocadas,
  Entonces:
  1. el número de archivos tocados **fuera** del campo `Archivos:` es **no más de 0** (**de contrato**:
     quien elige su conducta por ese valor es el propietario, que autorizó este alcance, y el auditor
     que lo revisa; subirlo exige su decisión);
  2. en particular, **no más de 0** archivos en `hooks/`, `tools/`, `tests/`, `.github/`, `.arnes/`,
     `.claude-plugin/` (**de contrato**, mismo motivo): no cambian hooks, herramientas, pruebas,
     umbrales, permisos, versión ni la entrada de ninguna puerta;
  3. en `requirements/REQ-025.md` sólo cambia **una fila añadida** al «Historial de cambios»: ni su
     cabecera ni ninguna otra sección;
  4. las quality gates de `AGENTS.md` §7 salen en verde, y el banco completo corre en CI en verde como
     en cualquier cambio: como ningún archivo del mecanismo ni del banco cambia, **no se espera
     cambio** en su resultado, y un FAIL se investiga y se reporta, **no** se descarta por «no
     esperado»;
  5. toda ruta, sección o identificador que cite el texto nuevo **existe** en la cabeza entregada
     (ninguna referencia rota).
  - Verificado por esta comisión el 2026-09-23 sobre `cfb1106`: ninguna línea de `hooks/`, `tools/` ni
    `tests/` contiene `Trazabilidad`, `Correspondencia` ni `Origen` (búsqueda literal); ninguna
    puerta lee la subsección nueva.

- **CA-12** — [validación con ejemplos acotados de texto]
  Dados **al menos cuatro** ejemplos acotados de texto (fragmento de pedido + fragmento de REQ)
  escritos por el `qa-tester` en `docs/qa/REQ-029.md` —**no** en `tests/`— (recuento **de contrato**:
  son los cuatro casos que este REQ nombra; se pueden añadir, no quitar),
  Cuando QA aplica **literalmente** el texto entregado a cada uno,
  Entonces el texto decide, **sin que QA tenga que interpretar**:
  1. **encargo nuevo** con una obligación **omitida o sustituida sin autorización** → la fila queda
     marcada, va `→ Preguntas abiertas`, el REQ queda en `borrador`, y la coordinadora **no despacha**
     la implementación de lo que depende de esa fila;
  2. **REQ existente aprobado sin conversación original** → limitación declarada, aprobaciones
     conservadas, **sin** vuelta a `borrador`;
  3. **reparación que conserva el contrato** → reutiliza la correspondencia vigente, **sin** tabla nueva
     ni contraste completo;
  4. **decisión técnica ordinaria** → avanza **sin** aprobación humana adicional.
  - Error: si algún ejemplo **no** se puede decidir con el texto, o el texto lo decide al revés, es
    hallazgo `contrato` contra este REQ.
  - Límite declarado en el propio log: estos ejemplos acreditan **lo que el texto dice**, no **lo que
    un agente hará** (conducta no observada).

- **CA-13** — [lo que el REQ no afirma]
  Dado todo el texto nuevo de esta entrega (los archivos de `Archivos:`),
  Cuando se barre por propiedad —*toda frase que afirme o prometa reducción de tokens, tiempo o
  dinero, que la fidelidad al pedido queda garantizada, o que los agentes cumplirán el texto*—,
  Entonces hay **no más de 0** frases así (**de contrato**: es la instrucción literal del propietario).
  Y **ninguna** comisión inventa un encargo para medir ahorro.

- **CA-14** — [la medición que la propuesta sitúa después, registrada y no afirmada]
  Dada la «Validación mínima» de la propuesta autorizada (`propuesta.md` §6: «medir en el **primer
  REQ real** cuánto cuesta rellenar la tabla y si detecta alguna diferencia»),
  Cuando se entrega este REQ,
  Entonces «Notas / alcance» la registra como **observación pendiente, posterior a la adopción** —no
  medida ni afirmada aquí—, y el log de QA de este REQ anota como **dato previo, no como esa
  medición**, lo que la correspondencia de REQ-029 detectó sobre su propio encargo (número de filas y
  de diferencias por relación, leídos de la tabla).

## Notas / alcance

**Diseño de partida.** `/home/juan/dev/ArnesJuan-evidencia/fidelidad-encargo/propuesta.md` (revisión 2)
y `propuesta-fidelidad-encargo.patch` (128 líneas, seis archivos), en la rama
`evidencia/prueba-despacho-2026-09-14` cuya cabeza es `d4e7a4f` (leída en
`.git/refs/heads/evidencia/prueba-despacho-2026-09-14`; esta comisión **no** comprobó que el árbol de
trabajo de esa rama esté limpio). **El diff no se aplica en esta comisión**; lo aplica el
`desarrollador` **ajustado** a estos criterios. Tres ajustes son obligatorios porque el diff, tal cual,
los contradice:
- `agents/qa-tester.md`: «dos veredictos distintos» → **un solo veredicto** con la fidelidad en su
  evidencia (CA-08, precisión 1).
- La regla de «fuente ausente → `añadida` → `borrador`» de la propuesta (§4) aplica a **encargos
  nuevos**; para REQ **existentes** aprobados rige CA-05 (precisión 2). El diff no tiene hoy ninguna
  frase para CA-05: hay que escribirla en una sola sede.
- `agents/analista-requerimientos.md`: «la salida es partir el REQ por alcance, una decisión que se
  registra» → la partición **no concede autoridad** sobre alcance comprometido (CA-07, precisión 3).

**Compatibilidad comprobada entre «detectar antes de implementar» y «no bloquear retroactivamente».**
No chocan porque actúan sobre poblaciones distintas: la detección previa (CA-01, CA-06, CA-12 caso 1)
rige para **encargos nuevos** y para **cambios de alcance** posteriores a la adopción, que son los que
todavía no se han implementado; la precisión 2 (CA-05) protege a los REQ **existentes** con contrato y
decisiones aprobados, donde lo que falta es la conversación, no una decisión. Una contradicción real
detectada en un REQ existente no queda sin tratar: sigue las reglas **ya vigentes** de conflictos.

**Por qué no hay ADR.** Este REQ es nuevo y la decisión que lo funda es del propietario, citada literal
abajo; para REQ-025 el párrafo añadido a la regla 1 es cambio **menor** (§9): no cambia su alcance, su
decisión base ni el significado de ninguna de las seis reglas, y ADR-008 no queda superado. Si QA o el
auditor concluyen que es de fondo, el ADR exige añadir `docs/decisions/*.md` al campo `Archivos:`, lo
que es decisión del propietario (CA-11 punto 1).

**Observación pendiente de CA-14.** La propuesta no le asigna dueño ni sede, y este REQ **no** se los
asigna: imponer una obligación a un tercero que el propietario no escribió es lo que REQ-025 registró
como OBS-I. Se presenta a la coordinadora para que el propietario la asigne si lo decide. Nada de este
REQ depende de ella.

**Dependencia no conflictiva con REQ-019** (adelgazar `AGENTS.md` y `requirements/README.md`,
`pendiente`): este REQ **añade** texto a los dos documentos que REQ-019 adelgaza (+944 B y +753 B en el
diff de diseño, cifras **operativas** de la propuesta, no re-medidas aquí). No contradice ningún
criterio de REQ-019 leído por esta comisión —REQ-019 mide cuando corre—, pero su línea base cambia.
Los dos declaran `AGENTS.md` y `requirements/README.md`: `tools/arnes-paralelo.sh` responderá
`colisiona`, que es lo correcto. Igual con REQ-025 (comparte `AGENTS.md`, su gemela, los dos agentes y
`skills/arnes-upgrade/SKILL.md`).

**Fuera de alcance:** hooks, herramientas, pruebas del banco, permisos, umbrales, versión del plugin,
consumidores, REQ-019, las demás entregas de REQ-025 (1b) y REQ-028, las sondas, la demo del
conciliador, y **retroajustar** ningún REQ existente con la correspondencia.

## Preguntas abiertas / conflictos

(ninguna abierta)

Registrado para la coordinadora, **sin** efecto en el estado: la cita de la decisión del propietario
que recibió esta comisión trae **dos elisiones** marcadas «[…]» (ver «Trazabilidad»). La
correspondencia cubre todas las obligaciones **visibles**; lo elidido no se reconstruye. Si lo elidido
contuviera una obligación verificable, sería una fila nueva y un cambio de alcance (CA-04 punto 3).

## Trazabilidad

Origen: mensaje del propietario (Juan) del **2026-09-23**, recibido por esta comisión **transcrito por la
coordinadora** en el encargo, «literal en lo esencial» y con **dos elisiones** marcadas «[…]»; el
analista **no** vio el mensaje original. Más la propuesta de diseño `d4e7a4f`
(`fidelidad-encargo/propuesta.md` revisión 2 y `propuesta-fidelidad-encargo.patch`). Texto recibido:

> «Autorizo implementar la propuesta de fidelidad del encargo preparada en `fidelidad-encargo/`,
> commit `d4e7a4f`, incorporando estas tres precisiones en la misma entrega. 1. **Un solo veredicto de
> QA.** No crees dos firmas, campos ni estados de aprobación. En la evidencia del veredicto vigente, QA
> indica si contrastó el REQ con la fuente, encontró diferencias o no pudo comprobarlo. La conformidad
> del código con el REQ no acredita por sí sola fidelidad al pedido ni permite cerrar con decisiones de
> alcance pendientes. 2. **Sin bloqueo retroactivo por conversaciones ausentes.** La falta del pedido
> original no devuelve automáticamente a `borrador` un REQ existente con contrato y decisiones
> aprobados. Conserva esas aprobaciones y declara la limitación de trazabilidad. Pregunta únicamente
> por decisiones realmente pendientes o contradicciones detectadas. En encargos nuevos, conserva la
> fuente desde el inicio; no reconstruyas palabras del propietario. 3. **Partir un REQ no concede
> autoridad sobre el alcance.** El analista puede preparar y registrar la partición conforme a las
> reglas vigentes. Si implica excluir, sustituir o diferir alcance comprometido, necesita la decisión
> correspondiente del propietario. No uses una partición para esquivar preguntas pendientes o
> reiniciar contadores. **Objetivo:** detectar omisiones, sustituciones y decisiones de negocio no
> autorizadas antes de implementar. Reutiliza la correspondencia en reparaciones sin cambio
> contractual; no exijas una tabla nueva ni una revisión completa para cada corrección. […] Conserva
> una sede normativa con referencias; no copies la regla completa en cada rol. […] No inventes un
> encargo para medir ahorro. No afirmes reducción de tokens, tiempo o dinero.»

Tocado por: `analista-requerimientos` / 2026-09-23 (creación).

### Correspondencia con el encargo

Primera aplicación de la forma que este mismo REQ contrata (CA-03), escrita antes de que la plantilla
la tenga. P = pedido del propietario (arriba); D = propuesta autorizada `d4e7a4f`; C = resumen de la
coordinadora en el encargo (**no** es fuente del propietario: sus filas `añadida` se autorizan por la
política vigente o son elección técnica ordinaria).

| Obligación del pedido (cita) | Criterio que la cubre | Relación | Autorización citada, o pregunta abierta |
|---|---|---|---|
| P: «Autorizo implementar la propuesta … commit `d4e7a4f`» | CA-01, CA-03, CA-04, CA-06, CA-08, CA-10 | cubierta | P |
| P: «incorporando estas tres precisiones en la misma entrega» | CA-05, CA-07, CA-08 | cubierta | P |
| P1: «No crees dos firmas, campos ni estados de aprobación» | CA-08 punto 2 | cubierta | P |
| D §2.4 y diff `qa-tester.md`: «dos veredictos distintos» | CA-08 punto 2 y frontera | sustituida | P1 (la sustitución la ordena el propietario) |
| P1: «QA indica si contrastó el REQ con la fuente, encontró diferencias o no pudo comprobarlo» | CA-08 punto 2 | cubierta | P |
| P1: «La conformidad … no acredita por sí sola fidelidad … ni permite cerrar con decisiones de alcance pendientes» | CA-08 puntos 3 y 4 | cubierta | P |
| P2: «La falta del pedido original no devuelve automáticamente a `borrador` un REQ existente…» | CA-05 punto 1 | cubierta | P |
| D §4 «Fuente ausente … el REQ queda en `borrador`» aplicado a REQ existentes | CA-05; «Notas / alcance» | sustituida | P2 (sólo para REQ existentes aprobados; en encargos nuevos se conserva) |
| P2: «Conserva esas aprobaciones y declara la limitación de trazabilidad» | CA-05 punto 2 | cubierta | P |
| P2: «Pregunta únicamente por decisiones realmente pendientes o contradicciones detectadas» | CA-05 punto 3 | cubierta | P |
| P2: «En encargos nuevos, conserva la fuente desde el inicio; no reconstruyas palabras del propietario» | CA-01 punto 1, CA-03 borde, CA-05 contraste, CA-06 punto 3 | cubierta | P |
| P3: «El analista puede preparar y registrar la partición conforme a las reglas vigentes» | CA-07 punto 1 | cubierta | P |
| P3: «Si implica excluir, sustituir o diferir alcance comprometido, necesita la decisión … del propietario» | CA-07 punto 2 | cubierta | P |
| P3: «No uses una partición para esquivar preguntas pendientes o reiniciar contadores» | CA-07 punto 3 | cubierta | P |
| P: «Objetivo: detectar omisiones, sustituciones y decisiones de negocio no autorizadas antes de implementar» | CA-01 punto 2, CA-06 punto 2, CA-12 caso 1 | cubierta | P |
| P: «Reutiliza la correspondencia en reparaciones sin cambio contractual; no exijas una tabla nueva ni una revisión completa» | CA-04 punto 4, CA-08 punto 1, CA-12 caso 3 | cubierta | P |
| P: «Conserva una sede normativa con referencias; no copies la regla completa en cada rol» | CA-09 | cubierta | P |
| P: «No inventes un encargo para medir ahorro» | CA-13 | cubierta | P |
| P: «No afirmes reducción de tokens, tiempo o dinero» | CA-13 | cubierta | P |
| D §6: «medir en el primer REQ real cuánto cuesta rellenar la tabla y si detecta alguna diferencia» | CA-14 (registro; la medición es posterior) | excluida (de esta entrega, por su propio calendario: ocurre después de adoptar la regla) | D §6, autorizada por P, la sitúa en «el primer REQ real»; sin dueño asignado → se presenta a la coordinadora (Notas) |
| D §6: write-back en REQ-025 con fila de Historial y gemela byte a byte | CA-02, CA-11 punto 3 | cubierta | P |
| C: validación con ejemplos acotados de texto, distinguiendo revisión del texto de conducta observada | CA-12 | añadida (técnica de validación) | elección técnica ordinaria; no cambia lo que el propietario ve, decide o cobra |
| C: `Rigor: critico`, `Sensible a seguridad: sí` | cabecera | añadida | política de autoalojamiento, `AGENTS.md` §6 (sólo el propietario baja) |

**Limitación de trazabilidad, fuera de la tabla a propósito:** el texto elidido «[…]» (dos sitios de P)
no da fila, porque ninguno de los cinco valores de «relación» describe una obligación que no se conoce,
y fabricar un sexto violaría CA-03 punto 3. No se reconstruye (ver «Preguntas abiertas / conflictos»).

## Historial de cambios
| Fecha | Antes → Después | Causa | ADR |
|------------|-----------------|-------------------------|--------|
| 2026-09-23 | (creación) — 14 criterios, `pendiente`, con su propia «Correspondencia con el encargo» (primera aplicación de la forma que contrata) y el write-back mínimo en REQ-025 (una fila de Historial) | Decisión del propietario del 2026-09-23 (citada en «Trazabilidad») que autoriza implementar la propuesta `d4e7a4f` con tres precisiones | — (ver «Notas / alcance», «Por qué no hay ADR») |
| 2026-09-23 | **(1) `Archivos:`** + `PENDING_APPROVAL.md` (sin decoración, al final): la coordinadora escribió en su §«Resueltas» (`6860226`) la decisión literal del propietario del 2026-09-23, y el campo declaraba de menos frente a CA-11 punto 1 (0 archivos fuera del campo contra `cfb1106`). **(2) CA-02**: «las líneas añadidas y retiradas … en cada miembro de la pareja» → + «contando sólo las de secciones que existen en los dos miembros —regla 1 de §6 y `## Plantilla`—; las de una sección que sólo existe en la sede por ser propia de este repositorio (hoy el `## Índice` de `requirements/README.md`) no cuentan». **El número no cambia** (sigue «no más de 0», de contrato) y la dirección no se abre: la gemela nunca llevó el índice, así que exigirle la fila de REQ-029 era medir la magnitud equivocada, no un techo más estricto. **Nada más se toca**: `Estado:`, `QA:`, `Seguridad:`, `Hallazgos abiertos:` y `Rigor:` quedan como estaban | (1) Registro de la procedencia de la decisión fuera del artefacto que autoriza —la figura de **SEC-104** (`docs/seguridad/registro-seguridad.md`, adenda R-041-A)—, recomendado por el informe de creación de este REQ (juicio 4) y ejecutado por la coordinadora. (2) Medición del `desarrollador` al pie de la letra: **1** línea distinta en `requirements/README.md` ↔ gemela, que es la fila del índice de este REQ; **0** en la `## Plantilla`. Criterio mal escrito en su origen (el analista), corregido antes de QA | **Ninguno.** Cambio **MENOR** (`AGENTS.md` §9): el campo declara lo que ya se escribió y CA-02 aclara **qué** mide sin cambiar su número ni su alcance |
